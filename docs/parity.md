# Port status

The full-port goal resumed after the native Bend cleanup on 2026-09-18. See [native-bend.md](native-bend.md) for the approved representation decisions. The native cleanup replaces earlier object-compatibility milestones; Git history retains their implementation history.

Reference: pi 0.85.1, commit `46c9de402`. This checklist describes the requested destination, not completed features. JavaScript extension compatibility is explicitly excluded by the user.

- [ ] Native build and dependency setup
- [ ] JSON and incremental SSE parsing
- [ ] HTTPS, streaming, cancellation, timeouts, retries, proxies, WebSockets
- [ ] OpenAI OAuth credential loading, refresh and persistence
- [ ] Provider adapters and model catalog parity
- [ ] Agent loop, event ordering, steering, tool validation and cancellation
- [ ] Read, bash, edit, write, grep, find, ls tools with upstream semantics
- [ ] Project instructions, skills, prompt templates, settings and themes
- [ ] Session persistence, tree navigation, continue/resume/fork and compaction
- [ ] Print, JSON event and RPC modes; embedding interface
- [ ] Terminal editor, differential rendering, Markdown, images, shortcuts and dialogs
- [ ] Bend extension interface, discovery, hooks and custom tools/UI
- [ ] Native Linux/macOS portability; account for upstream Windows support
- [ ] Upstream-derived behavioral and terminal snapshot tests
- [ ] Final pure-Bend pi-bend subagent develops and tests a feature

Authentication preflight: upstream pi successfully called `openai-codex/gpt-5.6-sol` using the existing private auth file on 2026-09-17. No interactive login was required. This is not yet evidence for authentication implemented in Bend.

## Milestones

- Native JSON parser/serializer: nested structures, exact number spelling, escapes, surrogate pairs, malformed input rejection tested.
- Native OS effects: file I/O, subprocess output/status/timeouts, streaming HTTP through libcurl. Local HTTP fixture splits Unicode into individual bytes to test transport boundaries.
- SSE parser: split events, CRLF, comments, multiline data and terminal marker tested.
- Native OpenAI call succeeded with `PI_BEND_OPENAI_OK`. Credential loading and streaming execute in the native executable. Refresh/persistence is implemented but has not yet been exercised against expired live credentials.
- Print-mode agent loop and read/write/exact-edit/bash tools compile. Tests cover files, offsets, rejected ambiguous edits, exit codes and timeouts. Fuzzy/multi-edit, image reading and exact truncation parity remain open.
- Native session persistence and explicit-file resume pass a local provider fixture. Entries have version-3 session headers, linked IDs, canonical messages, and custom raw-provider transcript snapshots. Full upstream session import, branching, compaction and recovery remain open.
- Provider fixture verifies transient-error retry, tool items preserved when completion omits them, continued tool conversations, and disabled tools refusing unsolicited calls.
- A live native pi-bend process implemented a Python slugify function and three passing tests in a disposable project. A subsequent invocation developed the Bend truncation module in this repository; review and parity validation are separate from that execution proof.
- Native Unicode segmentation covers combining marks, emoji sequences, CJK, controls and word boundaries through ICU. Terminal rendering is not implemented yet.
- The subagent-written truncation module, subsequently reviewed and adapted to the canonical package/API layout, passes all nine upstream cases, including the 300,000-character regression and 57,405 byte-limit checks. Review removed a redundant tail copy that made large inputs quadratic and added UTF-16 compatibility. The module has not yet replaced the prototype tool truncation paths.
- Upstream tests are inventoried by source hash: 549 suites discovered across pi-mono, with applicability review still needed for auxiliary packages. The truncation, generic event-stream and system-message-replay suites have full native ports; regional-indicator width, max-thinking and models-runtime coverage are partial; the remaining suites are pending. See `tests/UPSTREAM.md` for the acceptance rules and current gaps.
- Pure-Bend runtime foundations: unsigned/wide arithmetic passes 272 independent vectors; binary64 addition/subtraction/multiplication/division, comparison and integer conversion pass 1,077 vectors covering signed zero, subnormals, rounding ties, cancellation, overflow, infinities and NaNs. These modules use no custom C/JS effects. Decimal parsing and complete integration remain open; shortest formatting is tested separately below.
- `packages/ai/src/types.bend` starts the typed library port with faithful content, usage, model-cost records and stop reasons. Numeric fields use binary64, and optional values remain distinct. The module is partial; model data records are implemented, while schema and provider-option contracts remain open; schema-data declaration comparison is implemented separately.
- Native library data now uses immutable Bend values and explicit returned updates. Structural tool equality and immutable event snapshots are approved adaptations. The JS reflection/dynamic-object and DOM event compatibility layers have been removed; historical milestone claims for them are retired. See [native-bend.md](native-bend.md).
- Current supplemental coverage includes typed option/configuration projection, pricing, schema primitive coercion, transcript/tool history, agent policy and request/event sequencing. These checks do not complete the full agent or validation suites. See [tests/UPSTREAM.md](../tests/UPSTREAM.md) for the current commands and counts.

- Resumed canonical agent integration: after-tool finalization invokes and awaits hooks, applies overrides and converts hook failures to error results. Nine native scenarios pass with channel-gated async checks. The agent event-stream factory now settles message histories on agent_end and owns its callback cleanup. Public loop entry points, tool execution/update tracking, preparation/validation and batch scheduling remain unfinished; upstream suite statuses are unchanged.

- Added a native update scope for each tool invocation, with affine completion tickets, late-update rejection, failure propagation and separate resource-drain settlement. Source-oracle checks confirm upstream failure selection; native checks pass on one/four threads, including 256 concurrent completions. Event forwarding and executor integration remain pending. No upstream suite statuses were promoted.

- Prepared tool execution now composes the executor, invocation-scoped update forwarding, ordered emission starts, asynchronous listener settlement and typed error conversion. Eight gated scenarios pass on one/four threads. A separate composed test runs actual execution through after-tool finalization, termination policy, result-message emission and canonical agent-stream settlement. Validated preparation, batch scheduling and public loop entry points remain pending; upstream suite statuses are unchanged.

- Native tool preparation now composes lookup, optional adaptation, required injected validation, before-hook updates and cancellation/blocking with actual prepared execution. Twelve native pipeline cases cover order, failure precedence, first-match selection, context updates and no revalidation after hook changes. The canonical schema validator, batch scheduling and public loop entry points remain pending; upstream suite statuses are unchanged.

- Added a pure-Bend typed schema evaluator for primitive kinds, structural constants/enumerations, allOf/anyOf/oneOf/not, nested object properties/required/additional properties and array tuple/item constraints. It passes 930 checks against pi-ai's pinned TypeBox 1.3.27 on one/four threads. Native fixtures cover ordinary dictionary keys, missing versus explicit null, immutable inputs, exclusive unions and non-finite numbers. JSON-schema compilation, remaining constraints, references, diagnostics and integration with coercion/tool validation remain pending; this does not promote the validation suite.

- Extended native schema evaluation with inclusive/exclusive numeric bounds, item/property counts and contains-count bounds. The expanded TypeBox differential set contains 2,700 checks on one/four threads, plus native edge fixtures. String-length constraints require the pinned dependency’s grapheme behavior; those and uniqueItems (subsequently approved with native signed-zero equality), multipleOf, schema loading, diagnostics and full validation integration remain pending. Upstream suite statuses are unchanged.

- Added a native core JSON-schema loader returning typed errors with schema paths. Loaded declarations feed the existing evaluator directly; 1,980 TypeBox comparisons, 16 unsupported-keyword rejections and native malformed/nested-path tests pass on one/four threads. Unsupported keywords are rejected explicitly, including nested alternatives. Object/array cross-keyword behavior uses the whole schema rather than field order. Remaining vocabulary, references, diagnostics and normalization/coercion integration remain open; upstream suite statuses are unchanged.

- Connected collection-count and contains constraints to the native schema loader. Count limits and accumulated counts use compact exact integers, avoiding machine-word overflow and unary allocation for large declarations. Expanded typed/loaded comparisons cover 3,150 cases each; native checks cover invalid bounds and exact conversion through the largest finite binary64 integer. Full coercion, references, remaining schema vocabulary and tool-validation integration remain pending.

- Connected native schema checking to immutable optional-null normalization. Required, nullable and referenced null fields survive; optional non-nullable fields are omitted, and declared nested properties/items are traversed. The source-derived helper/cache oracle passes 244 comparisons on one/four threads. A boolean-schema cache quirk remains an explicit pending behavior decision, and unsupported nullability checks return errors. Recursive coercion, full validation/reference handling and tool-entry integration remain pending.

- Added native recursive plain-schema coercion: ordered schema composition, original-first union checks, isolated candidates, scalar coercion and nested object/array traversal. All 388 source-derived comparisons pass on one/four threads, including retained caller inputs and independent failed candidates. Boolean-cache behavior and unsupported union constraints remain explicit gaps. Full conversion/normalization/validation orchestration, references, diagnostics and public tool-validation entry points remain pending.

- Composed native plain-schema tool validation and connected it to an agent preparation/execution fixture. All 285 upstream-function comparisons and the original named AJV-compatible primitive case’s 15 assertions pass on one/four threads; invalid calls never execute. Validation errors preserve typed categories and original/coerced data, while exact diagnostic text remains pending. The validation suite is now partial: inventory totals are 3 ported, 4 partial and 542 pending. Builder conversion, references, remaining vocabulary and full-loop integration are still open.

- Added native validation-message/path formatting and two-space JSON rendering for original arguments. Renderer comparisons pass for 12 complete upstream messages, 42 paths and 47 JSON values on one/four threads. Issue records are supplied by the test oracle for this stage; native issue collection and wiring exact text into validation failures remain pending. No upstream suite statuses changed.

- Added immutable structured diagnostics for the current executable schema vocabulary: typed reasons, field/index paths, union branch isolation and exact counts. Issue-presence comparisons match all 3,150 TypeBox acceptance results on one/four threads, with native issue-content/order checks. Source-keyword metadata, exact diagnostic integration and remaining schema vocabulary stay pending; upstream suite statuses are unchanged.

- Preserved diagnostic distinctions in native schema loading: type arrays now remain one typed constraint; keyword ordering matches pi and property dictionaries retain native declaration order. Loaded and diagnostic acceptance each pass 3,150 comparisons, and composed plain-tool validation passes 285 comparisons on one/four threads. Contains/additionalItems diagnostic handling and exact tool-message wiring remain open; upstream suite statuses are unchanged.

- Connected native structured issues to complete plain-tool validation messages and actual agent rejection. All 54 complete upstream messages, 42 paths and 47 JSON formatting cases pass on one/four threads; 6,300 loaded/diagnostic acceptance comparisons and 285 composed validation comparisons remain green. Added four original validation cases (eight assertions), bringing that suite to five named cases/23 assertions. Contains bounds preserve separate diagnostics and additionalItems reports the first invalid extra item. Builder conversion, references, remaining vocabulary and full agent-loop integration stay pending; suite totals remain 3 ported, 4 partial and 542 pending.

- Added pure-Bend URI component decoding, JSON pointer lookup and acyclic local schema references. All 394 decoder comparisons and 536 composed reference outcomes/exact error messages pass on one/four threads; the 3,150 loaded-schema regression set stays green. The original nullable-reference case now passes, bringing validation to six named cases/24 assertions. Recursive references, anchors, resource IDs, external resources, builder conversion and remaining schema vocabulary stay pending; suite totals remain 3 ported, 4 partial and 542 pending.

- Implemented structural `uniqueItems` checking and formatted tool rejection, including the user-approved rule that `0` and `-0` are duplicates. All 82 upstream comparisons and two declared differences pass on one/four threads. Added typed builder primitive conversion and exact binary64 truncation; 1,067 source comparisons pass. Full builder declarations/container conversion/integration remain pending, including the separate object-conversion behavior decision. No suite statuses changed.

- Added explicit immutable builder conversion policies for arrays and tuples, including nested combinations. All 660 source comparisons pass on one/four threads; array conversion wraps scalar inputs, tuple conversion preserves non-arrays and unmatched positions. Output comparisons check binary64 bits recursively, independently of structural schema equality. Builder declarations, object/union conversion and public validation integration remain unfinished; upstream suite statuses are unchanged.

- Added typed literal conversion and union branches pairing executable schemas with immutable conversion policies. All 1,518 container/literal/union source comparisons pass on one/four threads, including preservation of already-valid nullable values, independent candidate conversion and acceptance by a different union branch. This extends the conversion dependency; full declarations, object conversion and public builder validation remain pending. No upstream suite statuses changed.

- Added native schema declarations that keep wire schemas, executable constraints and conversion policies together. The primitive/literal/unknown/never/array/tuple/union factories have 828 source comparisons covering all three projections, recompiled JSON validation and diagnostic consistency. Native nullable-union checks preserve null and convert numeric text. Object/optional declarations, general builder options and public tool validation integration remain pending; no upstream suite statuses changed.

- Added required/optional object fields and immutable object conversion, including nested objects, arrays and unions. All 1,128 declaration/projection/check/conversion cases pass on one/four threads, and the native normalization stage handles pi’s optional-null example. Two separate review fixtures document the pending literal-key differences from TypeBox’s regexp-key conversion; these are not counted as parity or approved adaptations. Public builder validation and schema-valued additional-property conversion remain pending. Upstream suite statuses are unchanged.

- Added immutable builder options for annotations and supported scalar/collection constraints, with explicit errors for unsupported structural changes and malformed schemas. Union conversion now retains branch matching separately from the whole configured constraint used to select candidates. The expanded 1,635 declaration cases and 1,518 conversion regressions cover this distinction; native tests check option replacement, error paths and nested composition. Public builder validation and the object-semantics decisions remain pending; no upstream suite statuses changed.

- Added native `minLength`/`maxLength` checking, schema loading, structured diagnostics, formatted tool errors and builder options. The schema-specific Unicode grouping policy is kept separate from terminal segmentation. Coverage includes 206 source length counts and 432 composed validation/message comparisons, plus malformed-bound and builder integration checks. Existing diagnostic regressions remain part of verification; upstream suite statuses are unchanged.

- Added a native sequential batch scheduler with explicit state handoff, ordered results, post-delivery cancellation checks and batch-wide termination reduction. All 702 source scheduling comparisons and a channel-gated ordering test pass on one/four threads. Per-call operations are injected in these tests; integration with the complete native tool lifecycle, parallel scheduling and public loop entry points remain unfinished. Upstream suite statuses are unchanged.

- Connected sequential scheduling to the canonical per-tool lifecycle through an explicit capture/input adapter. Ten native scenarios exercise actual validation, before-hook argument/context updates, blocking, execution updates, after-hook finalization, result delivery, cancellation and delivery failures. An owner collection retains invocation update callbacks through successful and failed delivery until tool-held aliases retire. Parallel scheduling, public loop entry points and full builder-validation integration remain pending; no upstream suite statuses changed.

- Added a pure-Bend ordered concurrent collector needed by parallel tool batches. Gated native tests verify concurrent starts, input-ordered successful results, failure settlement while siblings remain suspended, stable repeated observations and sibling draining; a 256-call case checks collection under concurrent completion. These are runtime dependency checks, not coverage of the complete parallel agent scheduler. Upstream suite statuses are unchanged.

- Connected parallel batches to canonical validation, sequential preparation, concurrent execution, after hooks and ordered result delivery. Immediate outcomes emit execution-end during preparation; deferred calls recheck cancellation before execution. The owned run separates prompt failure from worker draining. Twelve composed cases, 112 source-scheduler comparisons and two channel-gated three-tool cases cover lifecycle ordering, final preparation context, reversed completion, listener failure and surviving siblings. Public loop entry points and full upstream agent-loop tests remain pending; suite statuses are unchanged.

- Added the common native `executeToolCalls` dispatcher over both lifecycle implementations. Forty-five composed cases cover global/tool overrides, first-match duplicate names, unrelated sequential tools, filtering non-tool content, empty batches and delivery failures. The gated three-tool tests now enter through this dispatcher and retain prompt failure plus sibling draining. Full loop assembly, turn-end/history integration and complete upstream agent-loop tests remain pending; suite statuses are unchanged.

- Connected real tool execution to turn completion, ordered context/history updates, `turn_end`, stop-hook snapshots, steering/follow-up precedence and final `agent_end`. The new checks cover 28 composed cases and 87 comparisons with the actual pinned `runLoop` control flow, using injected provider/declaration/tool helpers on the source side and native batch execution on the Bend side. This completes the successful-turn tail component, not the public loop; assistant failure/truncation and request/preparation orchestration still need integration. Upstream suite statuses are unchanged.

- Added native tool-loadout reconciliation and connected next-turn preparation through the provider request boundary. All 190 comparisons with upstream `declareToolChanges` cover replay, last-system intent, duplicates, removals, empty fields and preserved metadata. Native IO checks cover timestamp creation/preservation and the approved order-insensitive schema adaptation. Forty-five composed preparation cases and 45 comparisons with the actual upstream preparation block cover context/model/reasoning updates, steering precedence, initial-turn behavior, declaration delivery, histories and failures on one/four threads. Full agent-loop orchestration and upstream suite promotion remain pending.

- Assembled the native internal `runLoop` with real provider-stream consumption, declarations, validation, tool batching, assistant completion and next-turn preparation. All 145 comparisons with the pinned internal loop and its real helpers cover normal/length/error/aborted responses, batch termination, steering/follow-up precedence, stop hooks, model updates, generated history, provider transcripts and delivery failures on one/four threads. Truncation uses the existing shared helper with typed caller-supplied error details; its ordered error-artifact regressions remain green. These are internal-loop integration checks. Public loop entry points, stream wrappers, the original full agent-loop suite and upstream suite promotion remain pending.

- Added explicit-provider `runAgentLoop` and `runAgentLoopContinue` entry points over the native loop. All 310 comparisons with the pinned public entry functions and actual loop/helpers pass on one/four threads, covering initial declaration/prompt delivery, continuation history, custom-message acceptance, empty/assistant-ended context errors before callbacks, and event-delivery failures. The previous 145 internal-loop comparisons remain part of regression coverage. Default-provider fallback, asynchronous stream wrappers and the full named upstream suite remain pending; suite statuses are unchanged.

- Added native default-provider registration and public-loop fallback resolution at the upstream boundary: after initial events and prompt delivery, before initial steering. All 112 source comparisons cover explicit precedence, missing defaults, installation/removal/replacement during entry events, retained provider selection after steering changes, continuation error precedence and listener failures on one/four threads. The 310 entry-point and 145 internal-loop regressions remain green. Services and unresolved/resolved provider captures are separate typed records. Asynchronous stream wrappers and the complete named upstream suite remain pending; suite statuses are unchanged.

- Added asynchronous `agentLoop`/`agentLoopContinue` with owned worker/resource draining, repeatable typed results and the approved closure-on-failure behavior. All 204 wrapper scenarios and five of 24 named upstream agent-loop cases pass on one/four threads; the 145 internal-loop, 310 entry and 112 default-provider comparisons remain green. Reproduced and fixed a native stream race by publishing terminal events and reserving result ownership atomically; gated regressions and 128 completion/close races pass alongside existing stream tests. The agent-loop suite is now partial: totals are 3 ported, 5 partial and 541 pending. The remaining 19 named loop cases, public Agent integration and the broader port remain unfinished.

- Ported three more original agent-loop tests through the asynchronous public wrapper: transcript-only provider context, custom-message filtering and transform-before-convert ordering. All eight named cases now pass on one/four threads. The new fixtures check preserved system fields, exact callback order/provider messages and unchanged generated history under provider-context pruning. JS system-message identity is represented by immutable field equality. No production changes were needed for these contracts. Sixteen named loop cases remain pending; suite totals remain 3 ported, 5 partial and 541 pending.

- Ported original public-loop tool execution/usage and truncated-output cases. The new fixtures preserve all usage counters and costs, verify hook-patched usage in events/history/provider input, and prove schema-valid truncated arguments never execute while the loop continues. Both pass on one/four threads, as do the previous eight named cases. Existing production code satisfied these contracts; no production changes were needed. The agent-loop suite now has ten of 24 named cases ported and 14 pending; suite totals are unchanged.

- Ported the original public-loop argument preparation and before-hook replacement tests through real native validation. Both pass on one/four threads, with exact callback order, one validation, exact executor arguments and immutable raw assistant history. Hook mutation is represented by the previously approved explicit argument update. No production changes were needed. The agent-loop suite now has 12 of 24 named cases ported; 12 remain pending, and suite totals are unchanged.

- Ported the original parallel completion/source-order and queued-message batch-boundary tests through the public async loop. Gated overlap, execution-end ordering, source-ordered message/turn results, provider input, final history and steering-after-batch assertions pass on one/four threads. Replaced the source ordering timer with deterministic channel handoffs. The steering fixture exposed Clang’s generated-code nesting limit; the pure build now raises that limit only for Clang. Native runtime logic needed no changes. The named agent-loop suite is 14 of 24 cases; ten remain pending and suite totals are unchanged.

- Ported all three original public-loop execution-mode cases using deterministic gates: sequential tool override, mixed sequential/default tools, and explicit parallel tools. The expanded fixture’s four cases pass on one/four threads, checking suspension/start boundaries, second-executor completion state, original arguments and source-ordered results. Added progress events serve only as test synchronization in place of source timers. Native production code needed no changes. The agent-loop suite is now 17 of 24 named cases, with seven pending; suite totals remain unchanged.

- Ported the original prepareNextTurn snapshot and shouldStopAfterTurn tests through the public loop. Both pass on one/four threads, including prepared guidance in the second request, completed-turn hook snapshots, exact stop-event sequence, persisted history and suppression of later steering/follow-up work. No production changes were needed. The named agent-loop suite is now 19 of 24 cases; five batch-termination cases remain pending and suite totals are unchanged.

- Completed the five original public-loop batch-termination cases: terminating results, terminating blocks, mixed blocked/successful batches, mixed parallel termination and after-hook termination. Audited the meaningful assertions and exact name mapping for all 24 upstream agent-loop cases, then freshly rebuilt all seven native fixtures and passed every case on one/four threads. The pinned agent-loop suite is now ported; inventory totals are 4 ported, 4 partial and 541 pending. Refreshed stale package documentation and renamed a test binding that conflicted when imported. Native adaptations remain documented. The higher-level Agent API, full schema/provider integration and the overall pi port are still unfinished.

- Added canonical immutable AgentState and initial-state types, default model/conversion, transcript-backed initialization/prompt queries, native pending-ID sets and pure event/start/finish reductions. All 154 snapshots match the pinned Agent initialization/reducer/lifecycle helpers on one/four threads; default-model, retained-snapshot and library-type checks pass. Agent-end remains distinct from idle settlement. Public Agent ownership, options, queues, listeners, reset, abort and prompt/continue wiring remain pending; no upstream suite statuses changed.

- Added immutable pending-message queues, paired steering/follow-up operations and a pure continuation planner. All 1,754 queue snapshots and 1,024 continuation decisions match the pinned source on one/four threads; native queue isolation/snapshot and library-type checks pass. Busy and unusable transcripts preserve queues; assistant-tail continuation prioritizes steering and returns the initial-poll skip flag. The source comparison ends at run selection. Public Agent ownership, atomic run claiming, prompt/continue execution and skip-flag consumption remain pending; suite statuses are unchanged.

- Added an internal Agent lifecycle owner with atomic state preparation/run claiming, cancellation, repeatable idle observation, stale-completion protection and typed execution/recovery/finalization. Native tests pass 64 competing-start/restart scenarios plus six success/failure/cancellation/recovery combinations on one/four threads; canonical library types pass. Completed handles remain valid until explicit owner disposal. Public Agent state/queue integration, options, subscriptions, failure-message emission and prompt/continue wiring remain pending; suite statuses are unchanged.

- Composed Agent state, pending queues and lifecycle into one synchronized runtime. Prompt/continue preparation and queue consumption commit with the run claim; reset, queue updates and event reductions use that same owner. All 588 source snapshots, 161 state comparisons including reset, 64 simultaneous continuation races, lifecycle regressions and library-type checks pass on one/four threads where applicable. Busy reset preserves state, reset replays the current baseline, and agent-end remains distinct from idle. Public options, listener dispatch, provider/loop execution and failure-message construction remain unwired; suite statuses are unchanged.

- Added ordered awaited Agent subscriptions and event delivery over the synchronized runtime. Eight source scenarios preserve live subscription changes, duplicate/removal behavior, failure ordering and signal forwarding; three gated native cases verify committed state, cancellation during await, agent-end versus idle, busy reset, failure cleanup and idle-event rejection on one/four threads. Registry callbacks run outside locks and remain borrowed until active dispatches drain. Canonical library types pass. Public Agent construction, provider/loop wiring and failure-event synthesis remain pending; upstream suite statuses are unchanged.

- Added Agent failure-message construction, awaited recovery-event emission and final cleanup after success or recovery-listener failure. All 40 upstream-source scenarios pass on one/four threads, including cancellation, empty errors, either subscriber failing at each recovery phase, intermediate state/history commitment and final idle settlement. Native complete-message, zero-usage and shared wall-clock timestamp checks pass; canonical library types pass. This is the stateful Agent recovery boundary, separate from the approved low-level typed stream failure. Public construction and provider/loop wiring remain unfinished; no upstream suite statuses changed.

- Added run-owned steering/follow-up readers and configuration/context binding. All 24 comparisons with pinned `createLoopConfig`/`createContextSnapshot` pass on one/four threads, covering independent initial-skip flags, live queue/mode changes, model/thinking selection, retained context and forwarded session/transport/retry/execution settings. Library types pass. Reduced the test fixture's live configuration captures after it hit the compiler's argument-count limit; no compiler change was retained. Public options/hook adaptation and provider-loop/event-emitter wiring remain pending; suite statuses are unchanged.

- Added signal-aware Agent stop/preparation adapters and installation into the loop configuration. All 64 source scenarios pass on one/four threads, preserving captured stop callbacks, live preparation replacement/removal, context-aware precedence, legacy inputs, independent fresh configurations, typed updates/errors and active/aborted/idle signal forwarding. Canonical library types pass. Public options construction and provider-loop orchestration remain unfinished; no named public-Agent suite status changed.

- Made provider opening failures explicit in `StreamFn` and propagated the typed result through request dispatch, provider registration/selection and public loop wrappers. All 149 internal-loop comparisons and 220 wrapper scenarios pass on one/four threads, including failed openings, error precedence, no downstream tool execution, repeatable error results and the approved iteration closure without synthetic `agent_end`. All 24 named upstream loop cases, 310 public-entry comparisons, 112 default-provider comparisons and native request regressions also pass after fresh builds on one/four threads. Full configuration/normalized-context/option typing checks pass with the new error parameter. This closes an interface gap; public Agent/provider orchestration and the broader port remain unfinished, and upstream suite statuses are unchanged.

- Replaced awaited-only Agent subscription callback results with the existing immediate/pending delivery protocol. Listener initiation runs inline; state reduction precedes it; pending completion retains ordered, live subscriber selection. Awaited recovery and started loop-facing delivery use the same path. Eight subscription and 40 failure-recovery source scenarios, three gated Agent event cases and two causal-overlap/pending-error cases pass on one/four threads; the latter are also checked against actual upstream `processEvents`. Library types pass. Public Agent construction and provider-loop orchestration remain unfinished; inventory statuses are unchanged.

- Added `agent-run.bend`, composing claimed Agent runtime plans with real prompt/continue loop entries, active cancellation, queue readers, started event delivery, failure recovery and final idle settlement. Ten comparisons against the actual pinned Agent and loop pass on one/four threads, including provider and listener failures, abort, steering/follow-up handling, assistant-tail continuation skip and a complete no-argument tool round trip. Event-state snapshots, final state and provider transcript order match. Library types pass. Run resources remain explicitly owned until batch work drains and tool-held aliases retire; borrowed Agent/provider resources outlive that disposal. The public constructor/options adapter, prompt overloads, full schema integration and named Agent suite remain unfinished; inventory is unchanged.

- Added the native Agent owner, all 19 constructor option fields, construction-time provider fallback, default converter ownership, per-run hook adapters and resource retention. Public text/single/batch prompting, continuation, subscription, queue insertion/clearing, cancellation, reset, signal lookup and idle waiting compose the real run bridge. Ten owner/source comparisons and six prompt-input comparisons pass on one/four threads, including constructor defaults, borrowed converter survival, default-provider retention, busy prompt/continue/reset rejection from listeners and live prompt timestamps. Library types pass. State/configuration and queue-mode mutation accessors plus complete named Agent tests remain pending; inventory statuses are unchanged. Bend automatically updated to 2.0.6 during this work; all six recorded compiler patches applied after compatibility checks and their regressions passed, with updates still enabled.

- Added atomic immutable Agent model/thinking/tool/history updates, transcript-derived system-prompt access, queue-mode accessors and queue queries. Eight of 27 named `agent.test.ts` cases now run through the real owner and pass on one/four threads, with documented adaptations for typed fixtures and immutable snapshots. Supplemental queue-mode checks pass. Ten fresh composed owner/source comparisons also pass with a thinking update in every listener, explicitly checking that active state and pending-tool membership/removal survive while traces verify partial messages, queued work and recovery errors. Canonical types pass. Agent suite status is now partial: totals are 4 ported, 5 partial and 540 pending. Configuration mutation APIs and the remaining 19 named Agent cases remain unfinished.

- Added native Agent session ID updates and ported five more original Agent cases: reset baseline, default-provider fallback, session forwarding/setter, assistant-tail follow-up and one-at-a-time steering. All 13 named Agent cases and supplemental queue-mode checks pass after fresh builds on one/four threads. Library types and all six compiler-patch regressions pass on Bend 2.0.7 after reapplying compatible patches following its automatic update. Fourteen named Agent cases and other configuration mutation APIs remain pending; inventory totals stay 4 ported, 5 partial and 540 pending.

- Ported four original Agent failure/subscriber cases through the native public API. Exact recovery event order and assistant/Agent error fields, suspended subscriber completion versus prompt and waitForIdle, and active signal propagation/cancellation pass on one/four threads after fresh builds. The provider cancellation fixture waits on a native abort observation instead of polling timers; subscriber absence checks retain the source 10 ms observation window after channel entry barriers. Existing production behavior satisfies these contracts. Agent coverage is now 17 of 27 named cases; the ten remaining cases and wider library port remain pending. Inventory totals remain 4 ported, 5 partial and 540 pending.

- Ported six original Agent busy-operation and tool-declaration cases. Exact busy errors, reset transcript preservation, cancellation/join behavior, three successive provider declaration histories, merged system sections/timestamps/schema payloads and executable-loadout precedence pass after fresh builds on one/four threads. Typed declaration records guarantee executor exclusion without reflection. No production changes were needed. Agent coverage is now 23 of 27 named cases; two late-update and two turn-hook cases remain pending. Suite totals remain 4 ported, 5 partial and 540 pending.

- Completed the pinned 27-case `agent.test.ts` port with the two AgentOptions turn-hook tests and two settled-tool update tests. Hook signal/context/request counts and late callback suppression after prompt completion or during a blocked parallel sibling pass on one/four threads. All eight Agent fixtures then passed together, with exact upstream names checked once per thread setting. Empty unasserted argument/detail fixtures use Unit adapters; complete schema inference/validation is not claimed. The Agent test suite is now ported: totals are 5 ported, 4 partial and 540 pending. No production changes were needed for these final contracts. Full Agent configuration accessors, providers and broader library parity remain incomplete.

- Added public thinking-budget, transport, retry-delay and tool-execution getters/setters alongside session ID, using an immutable Settings record and atomic functional updates. All five fields preserve unrelated settings and explicit optional clearing. A real two-request native run retains its captured settings after the provider changes future values; the next prompt receives those values. The same scenario passes against the actual upstream Agent/loop. New integration and four rebuilt Agent behavior regressions pass on one/four threads; canonical library types pass. Public callback replacement APIs and broader parity remain pending. Suite totals are unchanged.

- Added public streamFunction, convertToLlm and optional transformContext getters/setters as atomic immutable option updates. A real tool run replaces all three from its first provider call, retains old callbacks for its second request, and uses replacements on the next prompt; a third prompt verifies transform clearing. Exact invocation order matches the actual pinned upstream Agent/loop. Native ownership checks retain the owned default converter through replacement and borrowed converters/transforms through Agent disposal. The integration passes on one/four threads and canonical types pass. API-key, payload/response and tool/turn-hook accessors remain pending; wider schema/provider/library parity is unchanged.

- Added public getApiKey callback accessors with atomic replacement and optional clearing. A real multi-request tool run retains its original key callback after replacement; later prompts use the new callback or no callback. Missing-key return, lookup failure before provider opening, Agent error recovery and a subsequent successful prompt match the actual upstream Agent/loop. Getter and caller-owned callback lifetime checks pass. Native integration passes on one/four threads and canonical types pass. Payload/response and tool/turn-hook accessors remain pending; suite totals are unchanged.

- Added onPayload/onResponse getters/setters as atomic immutable option updates. A fixture provider invokes forwarded callbacks and checks model/response metadata, payload return and response completion. Two requests retain callbacks captured before mid-run replacement; the next prompt uses replacements and later clearing removes both. Exact invocation order matches the actual upstream Agent/loop. Caller-owned callbacks remain callable after Agent disposal. Fresh native integration passes on one/four threads and canonical types pass. This verifies Agent forwarding, not real provider networking/payload handling. Tool/turn-hook accessors and broader parity remain pending; suite counts are unchanged.

- Added beforeToolCall/afterToolCall getters/setters with atomic replacement and clearing. A real before hook replaces both callbacks; the current run retains its original after hook, the next run uses replacements and clearing restores unmodified tool results. Exact provider/hook/executor order, validated fixture arguments, live signals, original after-hook result and persisted patched content match the actual upstream Agent/loop. Fresh native integration passes on one/four threads and canonical types pass. The three turn-hook accessors and broader library/provider parity remain pending; suite counts are unchanged.

- Added the three turn-hook getter/setter pairs with immutable atomic updates to the live hook record. Three real Agent scenarios preserve captured stop callbacks, live context-preparation priority and legacy fallback, self-clearing, late enablement deferred to the next run, and mid-run clearing. Exact traces and request counts match the actual upstream Agent/loop and pass on one/four threads; canonical types pass. Source/API audit now covers all 16 public mutable configuration fields plus all 19 AgentOptions fields. The static audit is part of the existing owner driver and passes. Public configuration accessor coverage is complete; complete schema/provider/package parity remains unfinished. Suite counts are unchanged.

- Added a production plain-schema tool-validation service with an explicit caller-owned callback and application error mapping. Public Agent integration verifies coercion, optional-null omission, unchanged original arguments, exact rejection diagnostics, skipped invalid execution and continuation after either result against the actual pinned Agent/loop/validator. Both original and hook-replaced input variants preserve structured unsupported-schema failures. Fresh native integration passes on one/four threads; the upstream reference and canonical types pass. Builder-schema integration, typed argument inference and remaining schema vocabulary are unfinished. Suite counts are unchanged.

- Added native schema-valued additional object fields with explicit validation and conversion policies. Declared fields take precedence, missing fields remain absent, and boolean additional-property overrides clear the extra-field converter. All 1,965 builder declaration/check/conversion comparisons (330 new cases) pass against pinned TypeBox on one/four threads; canonical library types pass. This fills a builder dependency gap. Literal-key conversion approval and public builder-validation integration remain pending; suite counts are unchanged.

- Added pure Bend binary64 remainder and end-to-end `multipleOf` support in schema checking, loading, builder options and exact diagnostics. All 976 bit-exact remainder vectors, 625 actual-Pi validation outcomes/messages, 65 TypeBox builder comparisons and five malformed-schema rejections pass on one/four threads; canonical types pass. Pinned tolerance and reciprocal-shortcut behavior are preserved. Remaining schema vocabulary and public builder validation are unfinished. Suite counts are unchanged.

- Added native conditional schemas with selected-branch evaluation, accepting defaults for omitted branches and explicit malformed-branch paths. All 774 composed outcomes/exact messages match actual Pi validation on one/four threads, including nested conditions, local references, unions and the pinned then/else diagnostic forms. Four malformed-path checks and canonical library types pass. Builder structural options and remaining schema vocabulary are unfinished; suite counts are unchanged.

- Started the canonical provider transcript-transform module with typed image-capability handling. All 312 user/tool content combinations match the actual upstream transform on one/four threads, including placeholder collapsing, signatures and metadata preservation. Canonical types pass. This is the image stage only; reasoning replay, tool IDs, orphan results, provider serialization and networking remain pending. Suite counts are unchanged.

- Added typed assistant reasoning/signature replay based on provider, API and model identity. All 1,224 combinations match the actual upstream transform on one/four threads, covering signed/empty/redacted thinking, cross-model text conversion, signature removal, whitespace boundaries and preserved metadata. Extracted shared whitespace classification; all 365 existing numeric parsing comparisons still pass, and canonical types pass. Tool-call ID normalization, orphan-result repair and complete provider integration remain pending; suite counts are unchanged.

- Added the native provider transcript repair pass with immutable pending/result/system-message state and an injectable clock. All 409 transcripts match actual Pi on one/four threads: partial and duplicate results, missing-result order/fields, user/assistant interruptions, held system messages, and removal of error/aborted assistant turns. Canonical types pass. Tool-ID normalization and the combined public transform remain pending; no upstream suite is promoted.

- Completed the public typed transcript transform by composing image handling, reasoning/signature replay, sequential ID normalization and orphan repair. Normalizers receive original assistants and remain caller-owned; failures return typed errors before repair. All four named upstream Copilot migration cases and 87 actual-source composition/callback comparisons pass on one/four threads; canonical types pass. The migration suite is now ported: totals are 6 ported, 4 partial and 539 pending. Untyped/session decoding, provider serialization and networking remain unfinished.

- Added the canonical simple-options module’s thinking-budget helpers with typed optional overrides and immutable response allocations. All 350 actual-source comparisons pass on one/four threads, covering every reasoning level, default/partial overrides, omitted versus explicit caller caps, answer-room boundaries, fractional and non-finite inputs. Absent reasoning is preserved. Numeric comparisons treat signed zeros as equal. Context estimation, buildBaseOptions and provider request integration remain pending; no upstream suite is promoted.

- Added native context estimation and completed the shared base-options builder. Immutable accounting preserves timestamp applicability, previous valid usage after stale/error replies, per-message rounding, UTF-16 text lengths, image estimates, tool declarations and argument serialization. Generic entry points accept pure encoders while retaining native parameter/argument records. Both original context-estimate tests, 174 full-transcript/message source comparisons, 114 complete option comparisons, typed-record composition and borrowed payload/response callbacks pass on one/four threads. The context-estimate suite is now ported: 7 ported, 4 partial, 538 pending. Provider-specific serialization, dispatch and transports remain pending.

- Added immutable strict JSON-schema derivation and the shared constrained-sampling strict/prefer/require policy. An explicit typed work stack preserves annotations, recursively handles properties/items/unions, makes optional fields nullable and reports ordered policy errors. All 243 schemas, 2,430 policy resolutions and strict/unchanged parameter selections match actual Pi on one/four threads. The original declaration-preservation test is ported through native builders (one of six constrained-sampling cases); the suite is partial. Grammar conversion, Responses tool/message conversion and stream processing remain pending. Inventory is 7 ported, 5 partial, 537 pending.

- Added native grammar-input validation and append-only JSON-delta generation. Buffers are immutable snapshots; updates return the next buffer and an optional delta, while invalid replacements/closed changes return typed errors. UTF-16 prefix handling supports provider chunks that split surrogate pairs. All 102 source sequences (404 successive transitions) and 43 argument checks pass on one/four threads, including escapes, empty input, repeated closes, invalid-state recovery and exact errors. The original append-only grammar test now passes, bringing constrained-sampling to 2/6 named cases; exact JSON output is checked in place of the host JSON.parse assertion. Grammar capability resolution, Responses conversion and stream integration remain pending; inventory totals are unchanged.

- Completed grammar capability selection and declaration-to-input-property mapping; added typed Responses function/custom-tool declarations and their JSON encoding. Lark preference, whitespace-only rejection, independent provider capabilities, strict null/false/true/absence, deferred loading, duplicate names and first-error ordering match 177 actual-source cases on one/four threads. Four of six original constrained-sampling cases are now ported; replay and provider-stream cases remain pending. Native diagnostics replace engine exceptions/truthiness-based distinctions for malformed raw grammar schemas, without adding object/array runtime emulation. Inventory totals are unchanged.

- Added pure-Bend shortHash, unpaired-surrogate cleanup and typed Responses tool-call ID normalization. All 654 hashing/cleanup inputs and 536 ID-normalization cases match actual pinned Pi on one/four threads, including supplementary scalars, explicit surrogate units, composite/foreign IDs, ignored extra separators, 64-unit truncation, underscore trimming and fc-prefix handling. Canonical types pass. These are message-conversion dependencies; the complete Responses converter and provider stream remain pending, and no suite status changes.

- Added a strict pure-Bend JSON decoder for canonical immutable values and used it in the Responses text-signature codec. All 498 JSON acceptance/value comparisons and 2,000-level nesting pass on one/four threads; numeric leaves retain binary64 values and top-level numeric cases also compare exact bits. All 134 signature decodings and 24 encodings/round trips match the actual upstream helpers, including legacy fallback, version/phase checks and Unicode escapes. Canonical types pass. Partial JSON recovery/repair and complete Responses message assembly remain pending; no suite status changes.

- Added typed Responses text/image input content, user input and tool-result output with explicit JSON serializers. All 144 source comparisons pass on one/four threads both directly and after shared image downgrading. Cases preserve interleaved user blocks, joined tool text followed by ordered images, empty-string versus empty-block omission, pre-sanitization text-presence checks, no-output/image fallback strings and data URLs. Canonical types pass. This is the content stage; complete assistant replay/message assembly and provider streaming remain pending, so suite statuses are unchanged.

- Added typed Responses assistant replay for reasoning signatures, output-text messages, function calls and custom-tool calls. Both content and canonical AssistantMessage entry points match 301 actual-source cases on one/four threads, including text-ID limits, phases, namespaces, model-sensitive item IDs, grammar validation and first-error ordering. Native argument records use a pure encoder at the provider boundary; invalid reasoning JSON returns a typed error. Canonical types pass. Full transcript assembly and provider streaming remain pending; no original suite is promoted by this stage.

- Completed the shared public Responses transcript converter, combining normalization, model/image/signature migration, tool-result repair, system updates, tool additions/search and typed wire serialization. All 144 actual-source transcript comparisons pass on one/four threads. The original grammar-call replay test now passes through this public path, bringing constrained-sampling to 5/6 named cases. A small Bend compiler fix selects the live arm when converting a static sum layout; its standalone native/JS regression, prior static-layout regression and existing transform/migration tests pass. Provider streaming, request dispatch and transports remain unfinished; overall suite statuses are unchanged.


- Ported the complete original Responses message-ID, foreign-toolcall-ID and empty-tool-result suites, plus 2/5 namespace cases. All five named tests pass on one/four threads through the public transcript converter. Assertions retain exact IDs, uniqueness, foreign hash/shape/length, no-output placeholder and namespace absence across every original target. Typed fixture models substitute for generated catalog lookup; catalog behavior is not claimed. Streaming-dependent namespace cases remain pending. Inventory is now 10 ported, 6 partial, 533 pending.

- Added native JSON string-literal repair and `parseJsonWithRepair`, using explicit immutable scanner states and the existing strict decoder. All 365 repaired-spelling and parse-result comparisons match pinned upstream on one/four threads. Existing escapes and non-string text remain intact; raw string controls and invalid backslashes are repaired. Unchanged failures retain the original typed parse error. Incomplete JSON recovery and Responses stream processing remain pending; suite statuses are unchanged.

- Added pure-Bend incomplete-JSON recovery and completed `parseStreamingJson` with the original fallback order. Persistent cursor/container state retains completed values across truncation without mutable host objects or a fixed nesting depth. Both the default dependency parser and full Pi helper match 581 actual-source cases on one/four threads; every numeric leaf retains its exact bits. Native checks cover 2,000 unclosed arrays and literal prototype-like dictionary keys. The third-party optional allow-mask API is not exposed; Pi uses its default mode. Responses stream processing remains pending, and original suite counts are unchanged.

- Broadened Responses argument encoders to immutable JSON values so streamed scalar, array and null arguments survive replay until tool validation. Native application argument types retain pure encoders. All 321 assistant replay comparisons and 154 full transcript comparisons pass on one/four threads, alongside the five existing named Responses conversion cases and canonical type check. Null grammar arguments are rejected through the typed missing-string error rather than JavaScript property-access failure. Provider streaming remains pending; suite counts are unchanged.

- Added the Responses content-stream reducer with typed item/event variants, private scratch state, immutable emissions, reasoning/text finalization, partial function JSON and grammar input updates. All 94 sequences match the actual pinned `processResponsesStream` on one/four threads, including initial input, done-only corrections, mismatched kinds, signature fallback, namespace timing and errors. Canonical types pass. These are single-slot supplemental checks: output-slot multiplexing, terminal/backfill/usage/pricing and the public async driver remain pending; original suite statuses are unchanged.

- Connected Responses content transitions to a persistent typed output-slot manager and canonical assistant events. All 67 multiplexed sequences match actual upstream emitted event payloads, content snapshots and stop reasons on one/four threads, covering reused/out-of-order indices, done-only items, pre-existing content, mismatches, final-answer phase changes and errors after prior emissions. The 94 content sequences and canonical types remain green. Terminal/backfill/usage/pricing and the public async driver remain pending; original suite counts are unchanged.

- Added native Responses terminal preparation/finalization: encrypted-reasoning signature backfill, cache-token subtraction/clamping, usage replacement or retention, base cost recomputation, raw/normalized stop reasons and provider errors. All 119 actual-source sequences pass on one/four threads, including repeated terminal events, absent usage, cache-write pricing, nullable wire-error codes, failed responses and early EOF. Fixture builds are batched to bound compiler memory. Service-tier hooks and the async driver are being integrated and verified separately; original suite statuses are unchanged.

- Connected the shared public Responses stream processor to typed asynchronous readers/sinks and borrowed service-tier hooks. All 32 actual-source comparisons pass on one/four threads, including failure snapshots, iterator cleanup/error precedence, pricing order, early EOF and events after terminal responses. Ten of eleven original terminal-event instances also pass, retaining all three phase rows; the provider-wrapper early-EOF case remains pending. Canonical types and all seven toolchain regressions pass with the isolated experimental compiler memory patch, now recorded with reproduction instructions; the installed compiler remains unchanged. Inventory is 10 ported, 7 partial and 532 pending suites. Provider wrappers, JSON/SSE decoding and HTTP/TLS integration remain unfinished.

- Completed all five original Responses namespace cases and all six constrained-sampling cases. The public async processor feeds native transcript replay with namespaces first received at item completion, and configured grammar input preserves its initial event snapshot, final arguments and concatenated JSON deltas. All 14 original conversion/sampling cases and 32 driver regressions pass together on one/four threads; inventory is 12 ported, 5 partial and 532 pending suites. Integration exposed and reduced a Bend typed-do shadowing bug; the verified eighth compiler patch fixes parsing before global name resolution without renaming application bindings. Large generated C blocks are tracked as BEND-016. Provider wrappers, wire decoding and HTTP/TLS remain pending.

## UTF-8 transport foundation (2026-09-19)

Added a pure Bend UTF-8 encoder and immutable incremental decoder, preserving the pinned OpenAI SDK 6.40.0's complete-buffer replacement/BOM behavior. The new runtime test passes 80,002 comparisons on one/four native threads. Coverage includes every single byte, every two-byte sequence with a non-ASCII lead, malformed/truncated sequences, all splits of selected valid and random sequences, empty chunks, BOMs, surrogate replacement and encoding boundaries. Inputs are runtime data; the generated runner is about 245 KiB of C, rather than expanding with vector count. No compiler changes were made. This does not complete a pi-mono suite or HTTP/SSE integration; suite statuses remain unchanged.

Oracle scope matters: the SDK's `internal/utils/bytes.mjs` calls `TextDecoder.decode` without streaming mode, so those cases and encoding are checked against both Node 24.18.0 and Bun 1.4.0. On this host, Node's streaming decoder drops both BOMs for chunks `[239]`, `[187,191,239,187,191]`, while Bun returns the second BOM. Python's `utf-8-sig` drops an incomplete BOM prefix at EOF, and its ordinary incremental UTF-8 codec delays replacement of `[237,160]` until more input/EOF. Those alternatives therefore cannot establish exact chunk emission timing. The incremental extension is checked against Bun, including explicit chunk boundaries; no such Node quirk is introduced into Bend and the actual SDK's non-streaming path remains matched.

## Byte-to-line transport foundation (2026-09-19)

Added the native immutable line decoder and double-newline finder used to build SDK-compatible SSE processing. The decoder preserves the pinned OpenAI SDK 6.40.0's exact per-call outputs, including deferred carriage returns, CR-text-LF, repeated flush, reuse after flush, empty/null chunks, malformed UTF-8 and surrogate replacement on string input. All 11,940 direct SDK comparisons pass on one/four native threads, with exhaustive short CR/LF/text combinations, every two-way split of selected sequences and seeded random bytes. The SDK's Apache license and source attribution are retained. No compiler changes were made.

Reversed byte lists replace concatenated mutable buffers, and a prefix automaton handles delimiter search. The automaton also avoids a newly reduced repeated-byte-pattern type-checking cliff, recorded as BEND-018 with a standalone reproducer; that compiler issue remains open. The native test runner generates 568,732 bytes of C and receives test vectors as data. SSE event decoding, HTTP/TLS and provider wire integration remain pending, and no pi-mono suite status changed.

## SSE byte framing (2026-09-19)

Added immutable SSE chunk framing, matching the pinned OpenAI SDK 6.40.0's private `iterSSEChunks` function. The test exposes that exact function body with its actual SDK byte-encoding and delimiter-search dependencies; it compares both frame content and the input-read index that releases each frame. All 10,297 comparisons pass on one/four native threads. Coverage includes exhaustive short newline/text combinations, every two-way split, one-byte and empty chunks, null chunks, text encoding, seeded random bytes and unterminated EOF fragments. No compiler patch was needed and pi-mono suite statuses are unchanged.

Event decoding is still pending. Gregor approved clearing retained comment/unknown-field diagnostic lines at every blank SSE block (2026-09-19), preventing accumulation across heartbeats. Event names and data must keep the SDK behavior; malformed-JSON diagnostics will contain only the current block. This replaces the SDK's diagnostic retention across empty blocks and does not affect byte framing.

## Raw TCP candidate contract and baseline checks (2026-09-19)

Expanded the uninstalled byte-primitive candidate's tests to cover invalid-byte atomic rejection, empty sends, zero/short reads, complete reconstruction, repeated EOF, socket reuse and connection-reset read/send errors. Native one/four-thread and JS executions pass. Existing text-TCP and UTF-8 fixtures generate byte-identical C with and without the candidate; initial build-time and memory samples are retained under BEND-015 with their limited scope. No compiler/primitive patch was changed or installed. Backpressure/partial-write scheduling, sustained resources and runtime throughput still need verification before adoption. This is dependency work and changes no pi-mono suite status.

## SSE field decoding and TCP wait/resumption checks (2026-09-19)

Added immutable SSE event-field decoding. All 3,306 line sequences pass against the pinned SDK with only the approved diagnostic-reset adaptation; the oracle additionally checks that adapting diagnostic storage leaves every original event name, data value and emission unchanged. Cases include comments, unknown fields, first-colon splitting, exactly one optional ASCII space, overwritten/empty event names, multiline and empty data, trailing CR, Unicode, EOF without a blank block and 1,000 heartbeat blocks. An empty `event:` value remains present across an otherwise empty block, as in the SDK; only diagnostics clear. Full byte-to-event iterator/provider integration remains pending and no pi-mono suite status changes.

The isolated raw-TCP candidate now passes explicit read-wait/retry and write-backpressure tests on one/four native threads and JS. The test uses scheduler notifications, one injected read retry and real kernel write EAGAIN, then verifies an exact 65,536-byte transfer. A JS list-append stack overflow encountered by the test is reduced and recorded as BEND-019; a tail-recursive payload builder preserves the same test bytes. No compiler or primitive change was installed. Sustained resource and runtime throughput checks remain pending.

## Composed SSE byte-to-event decoding (2026-09-19)

Added `runtime/src/sse-stream.bend`, connecting the three verified pure layers without shared mutable buffers. All 3,819 scenarios pass against the pinned SDK's complete `_iterSSEMessages` async iterator on one/four native threads. The comparison records the input-read index of every event, not only concatenated final output, and verifies original event names/data/timing independently from the approved diagnostic-retention adaptation. Coverage includes all two-way splits of selected streams, single-byte chunks, null/empty inputs, typed bytes and text, ArrayBuffer input adaptation, mixed CR/LF patterns, split Unicode and malformed bytes, BOMs, multiple events per input, explicit/absent/empty event names, heartbeat history and EOF variants.

At EOF the decoder flushes the pending frame and line but never invents an event-separating blank line. The final API consumes its state and returns emitted events; uncompleted fields are discarded with that stream's state. The compiled test runner is 709,334 bytes of C and receives cases as runtime data. No compiler patch or installed primitive changed. This completes the pure byte-to-event composition only: the resource-owning async reader, cancellation/close semantics, SSE-to-JSON/provider-wire mapping and HTTP/TLS remain unfinished. No pi-mono suite status changed.

## Affine pull-based SSE reader (2026-09-19)

Added the effectful SSE cursor over borrowed next/close callbacks. A pull drains buffered events before requesting another byte chunk, and empty/non-event chunks continue reading without an artificial fuel bound. Successor cursors distinguish fresh, active, exhausted-with-buffered-events and closed states. Closing before first pull, after EOF or after a read failure does not call the source's return callback. Early close of an active source calls return once; failure propagates but the successor remains closed. Callback resources remain owned by their creator.

All 806 source-derived next/return traces pass on one/four native threads, comparing the actual SDK `_iterSSEMessages` generator with scripted input iterators. Coverage includes read/close rejection, no-extra-read delivery of multiple buffered events, EOF-flushed events, truncated input, null/empty chunks, repeated return, next after termination and byte/text input. The compiler rejects copying the affine cursor (`expected Data; observed Type`), and test callback/reference handles are explicitly disposed after use. The runner emits 876,200 bytes of C. No compiler or primitive patch changed. HTTP body acquisition/missing-body handling, request abort integration, JSON/provider event mapping and native HTTP/TLS remain pending; pi-mono suite statuses are unchanged.

## SSE JSON dispatch policy (2026-09-19)

Added a pure immutable dispatch stage for the pinned SDK's `Stream.fromSSEResponse`. All 1,120 cases pass against the real SDK on one/four native threads. Cases cross absent/empty/ordinary/thread event names, synthesized envelopes, receiving/completed states, primitive/container values, malformed JSON, `[DONE]` prefix variants and error-field values including signed zero, underflow and overflow. The oracle verifies the SDK's diagnostic data/raw context and destination; the native stage retains its supplied diagnostic lines. Typed parser errors replace engine-specific SyntaxError text. Native values are compared structurally, preserving the approved numeric/object equality rules.

Ordinary events promote active `error` fields to typed error payloads; `thread.*` events always yield event envelopes, including when their JSON contains `error`. Empty/false/zero/null error fields remain ordinary data. `[DONE]suffix` completes just like `[DONE]`; after completion the stage suppresses later JSON and parse errors while leaving input draining to its caller. This is not the complete SDK Stream: APIError message/metadata construction, diagnostic effects, abort/consumed-stream handling and HTTP/TLS integration remain pending. No compiler patch changed and no pi-mono suite status changed (12 ported, 5 partial, 532 pending).

## Composed SSE JSON reader lifecycle (2026-09-19)

Added `ai/src/api/openai-sse-reader.bend`, connecting the immutable byte/event decoder and JSON policy through the acquired source interface. The cursor is affine; each next/close transfers ownership to its successor. Borrowed callbacks implement source reads/return, request abort and diagnostics. Cancellation is a typed transport outcome rather than an exception-name check. Diagnostic callbacks can return ordinary failures or cancellation, with the same cleanup and result behavior as SDK logger exceptions.

All 5,670 operation traces pass against the actual SDK 6.40.0 `Stream.fromSSEResponse` iterator on one/four native threads. They compare ordered reads, source return, diagnostic destination, request abort, yielded string messages/envelopes and normalized errors. Coverage crosses next/return sequences, callback success/failure/cancellation, both envelope modes, buffered events, EOF-flushed events, truncated input, malformed JSON, provider error payloads, and DONE followed by data/read failure/cancellation. In particular, body/diagnostic failures override failing source return; explicit early return exposes non-cancellation close failures; unopened close does not read or abort; completion drains input and suppresses subsequent parse errors without suppressing later ordinary source failures. The type checker rejects copying the cursor. Callback/reference resources in the harness are disposed after final return.

The test runner emits 7,212,837 bytes of C. No compiler/primitive change was made. This is an acquired-reader lifecycle port: response/body acquisition, `.tee`, factory-level consumption checks, APIError messages/metadata, provider wire decoding and actual HTTP/TLS cancellation remain pending. Parser/dispatch payload coverage remains in the separate 1,120-case policy test. Original pi-mono suite counts remain 12 ported, 5 partial and 532 pending.

## Responses JSON output-item and terminal decoding (2026-09-19)

Added reusable runtime JSON decoding combinators and native decoders for Responses output items, terminal responses, usage details and provider errors. All 126 source-derived boundary vectors pass on one/four native threads. They cover message phases, text/refusal content, function/custom tool fields, empty/missing/nullable values, namespaces, complete opaque reasoning records, unknown item kinds, status/service-tier strings, usage and nested cache/reasoning counters, and precise failure paths through arrays and fields. These tests validate the boundary representation; they are not a replacement for the existing actual-upstream processor comparisons and do not promote any pi-mono suite.

The decoders use functional `Result` composition and immutable dictionary/list values. Declared field-type violations produce typed protocol errors, without reproducing JavaScript's incidental coercion of malformed values. No supported upstream test behavior was changed by this additive work. Top-level event dispatch and connection to the acquired SSE JSON reader remain pending. Inspection also identified that provider output indices are currently unary `Nat`; converting large sparse wire indices directly to that representation would allocate in proportion to their numeric magnitude. That is an application representation concern, not a newly demonstrated Bend defect; the next integration step must address it without imposing an arbitrary protocol limit. Local assistant content positions are a separate concept.

A reduced imported-pattern name collision is retained under BEND-012. The decoder test also reproduced the known high compiler-memory/generated-code costs, recorded under BEND-001/BEND-016. No compiler patch or installation changed. Suite totals remain 12 ported, 5 partial and 532 pending.

## Responses event decoding and provider numeric indices (2026-09-19)

Provider output indices now use the existing fixed-size binary64 number type throughout the content-slot state machine. Assistant content positions retain their separate native index representation. This removes allocation proportional to a sparse provider key's numeric magnitude and retains the original numeric-key behavior, including signed-zero equality. All 74 actual-upstream processor comparisons pass on one/four threads, adding keys at 2^32−1, 2^32, 2^53−1, 1e100, negative/fractional values and −0/+0 interoperation. Existing terminal/driver fixture generators now construct the same numeric keys directly. The original terminal fixture was type-checked after the API change; 32 async-driver fixtures were regenerated, but their native executions were not repeated in this milestone.

Added top-level JSON dispatch for every event branch consumed by the pinned Responses processor: creation, output-item add/done, both reasoning text deltas and summary-part completion, output/refusal text, function/custom deltas and completion, terminal completion/incompleteness/failure, and stream errors. Unknown event kinds remain ignored. Dispatch follows the JSON `type` field, and SDK-synthesized event envelopes remain ignored because they have no such top-level discriminator. Known malformed fields return the already documented typed protocol errors with paths. All 1,146 source-derived boundary cases pass on one/four native threads, including large numeric keys and malformed nested payloads.

The initial literal-pattern implementation exposed a reduced compiler code-size cliff: 315 MiB for the full fixture, overwhelmingly in string dispatch. An immutable wire-name table followed by typed event-kind dispatch reduces the fixture to about 25 MiB without changing any test results. Both source strategies passed the same 1,146 cases. The reduced compiler reproducer, control and measurements are recorded under BEND-016; no compiler installation changed. The acquired SSE reader still needs a typed-provider adapter and connection to `processResponsesStream`; HTTP/TLS remains unfinished. No pi-mono suite status changed.

## Raw SSE to public Responses processor integration (2026-09-19)

Added an owned adapter from the SSE JSON cursor to the existing injectable Responses reader. The adapter serializes next/close operations across asynchronous calls while transferring the affine cursor between them. Source errors and typed wire-decoding errors map into the caller's chosen error domain; a protocol failure closes the acquired stream before returning, preserving its error over cleanup failure. An affine owner releases adapter-owned callbacks and cursor state after aliases/calls retire, leaving borrowed transport hooks to their creator.

All 88 comparisons with the actual SDK iterator feeding the actual pinned pi processor pass on one/four native threads. The tests compare full final assistant snapshots, immutable emission snapshots and ordered reads, source close, request abort and diagnostic effects. Cases cover chunked text/bytes, one-character chunks, Unicode, buffering, null/empty inputs, large provider indices, reasoning replay/backfill, namespaced function/custom tools, usage, terminal states/errors, sink failures, failed/cancelled close, source failures/cancellation, DONE draining, unknown events, malformed JSON and truncated/early EOF. Three separate native protocol-error cases verify close/abort and original-error precedence; those do not claim to match incidental JavaScript behavior for malformed typed fields. Owner duplication is rejected. Serial-resource tests additionally verify asynchronous exclusion and release after typed failure on one/four threads.

The full pipeline required the isolated uninstalled compiler memory experiment. The installed compiler type-checks it but hit the 40 GiB guard before emitting C; this limitation remains open, and no performance-neutral compiler fix is claimed. The generated experimental fixture is 66,308,279 bytes of C. HTTP/TLS, response acquisition, APIError formatting/metadata, request construction and provider-wrapper lifecycle remain incomplete. These dependency/integration checks do not change pi-mono suite counts: 12 ported, 5 partial, 532 pending.

## Typed provider-error body normalization (2026-09-19)

Added `ai/src/utils/error-body.bend`, preserving the original `NormalizedProviderError` fields and normalization/formatting function names. Provider failures carry an optional numeric status, a message and ordered typed body candidates. Text, parsed JSON and unavailable bodies are distinct variants; unread response resources and private transport state never become serializable diagnostic objects. The selected body is trimmed only after selection, so a present empty/whitespace text body suppresses lower-priority bodies just as upstream. Empty parsed objects and parsed non-object values are skipped. The 4,000-unit cap, truncated-count suffix, message containment and optional provider-prefix behavior match pi, including UTF-16 boundaries and lone surrogates. JSON-valued extension failures use a separate `normalizeValue` entry point. Serialization/number-formatting failure remains an explicit typed result.

All 306 comparisons against the actual pinned `utils/error-body.ts` pass on one/four native threads using the installed compiler. The test checks the exact 16 original scenario names and compares all normalized fields plus formatted output, then adds source-precedence, empty-body, prefix/status and Unicode-boundary cases. SDK class/stream fixtures become explicitly unavailable native body candidates. The suite is partial: tests supply status after extraction, and provider-specific adapters still need to select status sources and construct ordered candidates from real failures. These tests do not establish HTTP/provider catch-block integration or reproduce arbitrary JavaScript thrown-object/prototype behavior. `safeJsonStringify` of arbitrary JS objects is intentionally not a native API; native JSON failure values are immutable acyclic data. Inventory now contains 12 ported, 6 partial and 531 pending suites.

## Responses compatibility, cache policy and provider environment (2026-09-19)

Added the complete ten-field resolved `OpenAIResponsesCompat` policy, preserving field names, default values and explicit false overrides. Affinity-format detection uses the original provider/URL predicate; explicit formats override it. Cache policy preserves explicit retention, the `PI_CACHE_RETENTION` long-only default, ordinary `24h` retention, explicit-mode/30-minute options, cache-none session suppression, and OpenAI/OpenAI-no-session/OpenRouter affinity defaults. The prompt-cache key helper caps Unicode code points at 64, including adjacent-surrogate normalization, while affinity headers retain the full session ID. Empty session IDs remain present as cache keys but generate no affinity headers. The two valid prompt-cache-option shapes use a native sum rather than arbitrary JSON.

All 9,219 comparisons pass on one/four native threads against the actual extracted pinned policy/key helpers and `createClient` with a capturing SDK constructor. Tests cover all 512 boolean combinations, each explicit/default affinity format, cache modes, provider/URL detection, absent/empty/scalar/surrogate session IDs and individual overrides. The oracle checks all ten resolved fields. This validates the internal policy, not full provider-wrapper or HTTP behavior: model/caller header merging, transport header normalization, Copilot dynamic headers, payload construction, callbacks, retry and streaming integration remain pending.

Added `utils/provider-env.bend`, using Bend's existing native `IO.get_env` primitive. Nonempty scoped overrides take precedence; empty/missing overrides fall back to the process value; empty process values are absent and whitespace is preserved. The public native cache-retention resolver now accepts the canonical scoped environment record and performs IO only when explicit retention is absent; a separate pure value helper supports policy tests. All 168 scoped/process/cache combinations pass against pi's actual environment and retention functions on one/four threads. The Bun-specific compiled-JavaScript `/proc/self/environ` workaround is not reproduced: Bend's native primitive directly calls `getenv`. This validation is for native Bend, not a claim about a Bun-compiled JS backend.

Both fixtures build with the unchanged installed compiler: policy build sampled peak group RSS 795,452 KiB, environment build 834,072 KiB, generated C 1,102,059 and 979,485 bytes respectively. These are observed build costs, not controlled performance comparisons. No compiler patch changed. Original suite counts remain 12 ported, 6 partial and 531 pending because the upstream Responses compatibility suite exercises the complete provider/request path.

## Responses request construction (2026-09-19)

Added native request-field assembly and an IO `buildParams` composition using the existing transcript, Responses message and tool converters. The request layer resolves request tools from transcript changes, passes mid-conversation/additional-tool/tool-search and strict/grammar compatibility into the converters, preserves the upstream tool-call provider allowlist, and serializes their typed results only at the wire boundary. Message/tool conversion failures retain typed error variants. Compatibility, grammar-input mappings and cache retention are supplied by the provider layer; acquisition, retry, callbacks and streaming remain outside this builder.

Field assembly preserves model/input/stream/store defaults; cache key/retention/options; the 16-token minimum with zero/absent suppression and compatibility gating; temperature, service tier and tool choice; model reasoning mappings and explicit-null off behavior; Copilot's omitted default reasoning; xAI's encrypted-content inclusion; and custom sampling overrides applied last, including explicit null values. The internal `RequestOptions<Choice>` is a projection of request-body fields, not a replacement for the full public stream options. Tool choice remains a caller-typed value with an explicit wire encoder until the native provider SDK union is supplied. Transport and callback options are not flattened into JSON.

All 914 final-field comparisons pass against the actual upstream `buildParams` with preconverted input/tool boundaries. A separate 192-case native integration fixture passes against `buildParams` with the actual upstream transcript, message and tool converters. These compare complete payloads structurally on one/four native threads, crossing tool-free/function/grammar declarations, tool addition/removal/redefinition, mid-conversation system messages, additional-tool/tool-search flags and strict mode. The full fixture exercises successful builds; its error propagation is type-checked but new builder-level failure-path comparisons remain pending. It supplies an empty grammar-input map and contains no assistant grammar call replay, which remains covered separately in shared-converter tests rather than by this request fixture.

Both fixtures compile and pass with the unchanged installed compiler. The full fixture peaks at a sampled 39,350,920 KiB group RSS and emits 27,255,095 bytes of C; costs are recorded under BEND-001/BEND-016. Public `stream`/`streamSimple` wrappers, canonical Responses-specific options, header construction/normalization, HTTP/TLS, callback and retry integration remain incomplete. Original suite counts stay 12 ported, 6 partial and 531 pending; the Responses compatibility suite calls the full provider through fetch and is not promoted by builder-only coverage.

## Provider retry parsing and pure delay policy (2026-09-19)

Added a reusable pure-Bend longest-decimal-prefix parser, backed by the existing correctly rounded binary64 conversion. It preserves signs/negative zero, leading whitespace, incomplete-exponent fallback, Infinity prefixes, underflow/overflow and rounding boundaries. It does not add JavaScript object/coercion semantics. Provider retry policy now handles explicit retry-header overrides, connection-error status absence, retryable status codes, millisecond-header priority and numeric-prefix fallback, seconds conversion, server-delay caps and capped exponential jitter. Date-valued headers produce a typed instruction carrying the original text; the driver must parse it and supply epoch/current-time values before applying the shared cap. Backoff is deliberately not constrained by the server-delay limit.

All 1,035 comparisons pass on one/four native threads using the installed compiler, including 427 parser comparisons with `Number.parseFloat` and 608 comparisons with pi's actual retry eligibility/delay helpers. Date-selection tests observe the actual helper requesting date parsing; separate timestamp cases compare its subtraction and delay validation, not a native date parser. The native policy preserves delay/limit numbers in a typed error; exact error rendering is covered by the retry-error formatter below. Header values are supplied after normalization, so these checks do not cover HTTP Headers behavior.

Provider classification/adapters, HTTP-date parsing and provider-wrapper integration remain pending; the injectable retry loop is described below. The five original `provider-retry.test.ts` cases exercise asynchronous requests/timers and are not marked ported by these pure comparisons. Suite counts remain 12 ported, 6 partial and 531 pending. An additive owned timer and pure Bend abortable sleep adapter are implemented against an isolated compiler candidate; lifecycle, cancellation and cleanup checks pass on one/four native threads and Bun. See [timer implementation and validation](../patches/experimental/timer/README.md). The primitive remains uninstalled pending adoption validation. Its native retry-sleep adapter and loop integration are described below; complete provider wrappers remain unfinished.

### Provider retry loop

`utils/provider-retry.bend` adds the asynchronous `retryProviderRequest` loop with typed provider/application failures, `ProviderRetryOptions`, and borrowed effect callbacks for date parsing, clock, jitter and sleep. Requests execute at least once. Success returns immediately even when the signal is already aborted; after a failure, cancellation takes precedence over exhaustion and classification. Provider overrides, delay-cap validation, retry indexing and original error retention follow the pinned helper. Retry counts use native optional `Nat`; the loop decreases structurally without an unsafe recursion annotation. Typed failures retain delay-limit values and the provider message; their exact rendering is covered below.

`tests/provider_retry_check.py` compares twelve complete request/effect/result traces with the original TypeScript helper on one/four native threads. Cases cover retryable/non-retryable errors, cap rejection/disabling, sleep rejection, default exhaustion, non-provider errors, two backoffs, pre-aborted success/failure, explicit retry override and invalid date effect ordering. The oracle replaces sleep with a controlled callback and date/random effects with deterministic values; these checks do not establish real timer scheduling or complete the five original asynchronous tests. The original suite remains pending and totals remain unchanged. The native sleep effect is now exercised through this loop as described below; complete production effect assembly, HTTP-date parsing and provider integration remain unfinished.

### Provider retry error messages

`utils/provider-retry-error.bend` renders retry-generated errors with the upstream name and message: `AbortError` / `Request aborted`, and the server-delay error with each millisecond value divided by 1,000 and rounded upward before formatting. Original provider/application failures use the caller's own renderer. The reusable `runtime/src/f64-round.bend` supplies pure binary64 `ceil`, preserving signed zero and non-finite values. Decimal conversion failures remain an explicit typed result.

On one/four native threads, `tests/provider_retry_error_check.py` passes 618 bit-level rounding comparisons against `Math.ceil` (ignoring NaN payloads), 58 exact delay-error messages and one abort error against the pinned pi helper, plus three native checks for preserved original-error rendering and typed abort rendering. Delay cases cover fractional millisecond limits, rounding boundaries, subnormal limits, huge values, infinity and Unicode provider messages. The existing twelve retry-loop traces also pass with the full delay-limit error name/message included. Both fixtures use the unchanged installed compiler; these checks do not establish the outstanding real timer/provider integration.

### Native retry sleeps

`utils/provider-retry-sleep.bend` connects `SleepInput` to the owned timer and pure Bend abortable sleep. Signals are optional; absent signals use a directly owned timer, while present signals use removable abort observations. The adapter maps cancellation to the retry loop's abort result and retires resources before returning. `runtime/src/timer-delay.bend` preserves the local Node runtime's scheduling conversion: values outside 1 through 2,147,483,647 milliseconds (including NaN) become 1 ms, and valid fractions truncate toward zero. This remains separate from the primitive's literal U32 deadline API.

`tests/timer_delay_check.py` passes 918 comparisons on one/four threads against the local Node timer constructor and its inspected insertion rule. `tests/provider_retry_native_sleep_check.py build/bend-timer-candidate` runs all twelve retry-loop traces with real native sleeps on one/four threads. A disposable instrumented binary counts timer creation and parked cancellation and checks zero live timers, timer waiters and channels at exit. The 277,403-second backoff creates one timer, cancels it while parked and returns an abort without a second request. Normal delays, capped delays, retry exhaustion and invalid-date fallback retain the expected traces. A separate adapter fixture checks absent signals, NaN delay normalization and pre-aborted signals on native one/four threads and Bun.

These tests use the isolated additive timer candidate; the ordinary injectable-loop comparisons still pass with the installed compiler. Real-clock observations do not prove the upstream fake-clock assertions at exactly 999/1,000 and 1,999/2,000 ms, or every abort/deadline interleaving. The original suite remains pending. Timer adoption validation, virtual-clock boundary tests, complete default effect assembly, date parsing and provider-wrapper integration remain unfinished.

### Native retry randomness

`runtime/src/random.bend` implements SplitMix64 in pure Bend using immutable 64-bit state transitions and the existing synchronized reference for shared draws. It follows [Sebastiano Vigna's public-domain reference](https://prng.di.unimi.it/splitmix64.c). The top 53 bits convert exactly into a binary64 value in [0,1), supplying the retry policy's jitter input. This is a non-cryptographic generator for jitter/sampling; authentication and token generation must use cryptographic entropy directly.

`runtime/src/random-seed.bend` initializes the generator from eight Linux `/dev/urandom` bytes through Bend's existing `File.open/read_bytes/close` primitives. It closes the file after successful and failed reads. Open/read errors and short seeds remain explicit typed initialization failures; there is no fixed or timestamp fallback. The source path is injectable for testing. `ai/src/utils/provider-retry-random.bend` exposes a callback borrowing the generator, which the caller retires after disposing dependent callbacks. Other operating-system entropy sources and complete default provider-effect assembly remain unfinished.

`tests/random_check.py` passes on native one/four threads and Bun: 4,096 exact word/value vectors from 32 seeds compared with a test-only C reference, 384 concurrent draws checked for the same output multiset, eight exact conversion boundaries including zero and the largest value below one, known seed bytes, all short lengths, missing-file/read failures, and live OS-seeded callback draws. These finite checks establish the implemented algorithm and integration contracts, not cryptographic security or a new statistical quality result. No compiler patch or production C/JS dependency was added. Original provider retry suite status remains pending.

### Date parser building blocks

`runtime/src/calendar.bend` represents proleptic Gregorian UTC fields for literal years 0000–9999 and converts validated values to exact signed millisecond words. Validation distinguishes year, month, day and clock errors. Four-digit year interpretation, timezone rules, leap-second interpretation and any date rollover policy belong to the eventual date interpreter; this primitive does not choose provider parsing behavior. `tests/calendar_check.py` passes 4,279 cases on one/four threads, including leap centuries, year zero, pre-1970 values, millisecond boundaries and invalid field ranges. Valid timestamps are compared with explicit UTC ISO inputs to Node's date parser; constructor validation cases are native contracts.

`runtime/src/http-date-syntax.bend` recognizes the three HTTP date token layouts from [RFC 9110 §5.6.7](https://www.rfc-editor.org/rfc/rfc9110.html#section-5.6.7), retaining their format, weekday and numeric fields. It does not yet resolve two-digit years, validate calendar combinations, infer timezone offsets or produce retry delays. Surrounding/repeated spaces are accepted; alternate date spellings and zones are outside this recognizer's current coverage. `tests/http_date_syntax_check.py` passes 1,517 field-recognition cases on one/four threads. The recognizer retains syntactically numeric but invalid calendar values for explicit interpretation, so its implementation does not silently choose rejection versus rollover.

Two meaningful changes from pi's `Date.parse` are awaiting user decisions: interpreting timezone-less legacy HTTP dates as UTC (as HTTP specifies) instead of host-local time, and rejecting impossible dates instead of rolling them forward. Neither change has been applied to provider behavior. This is unfinished date-parser implementation, not full retry-date parity. Default effect assembly and the original provider retry suite remain unfinished.

### Shared retry-sleep signal validation

The native abortable-sleep fixture now exercises eight cohorts of 128 waits sharing a signal, repeated aborts with different reasons, independent signals, and 90 deadline/abort races per process. Eight repetitions pass on each of native one/four threads and Bun. Every observed run reaches 128 live timers, creates/closes 1,151 timers and cancels 1,056 parked waits; exit audits find zero live timers/waiters, plus zero native channel rows. Bun channel counts are not measured. The real-clock race result may vary; either winner must satisfy the retained-reason and cleanup contracts. Results are in `docs/bend-issues/2026-09-19-abortable-sleep-concurrency.json`. No production behavior changed, and these supplementary tests do not complete the original virtual-clock retry tests or the full port.

### Owned native retry runtime

`ai/src/utils/provider-retry-runtime.bend` assembles the native random, wall-clock and cancellable-sleep callbacks into an affine `Runtime<Reason>` owner. `new` obtains an OS seed and returns typed initialization failure; `fromGenerator` supports explicitly supplied deterministic generators. `borrow` returns the owner alongside reusable effect handles. `dispose` closes its callbacks and generator after all requests finish, while leaving the caller-owned date parser alive. Borrowed handles are not statically tracked, so callers must join requests before disposal. Date parsing remains an explicit injected dependency while the interpretation choices are pending; this does not supply a complete default date parser or wire provider HTTP wrappers.

`tests/provider_retry_runtime_check.py build/bend-timer-candidate` passes on native one/four threads and Bun. It checks deterministic jitter outputs, a real wall-clock sample, parser forwarding, a real sleep, one failed request followed by a successful jittered retry, OS-seeded construction at a second reason type, and use of the borrowed parser after both runtimes are disposed. Native exit instrumentation finds zero live timers, timer waiters and channels. An attempted owner copy fails typechecking. The fixture uses the isolated additive timer candidate; no compiler or timer implementation changed. Results are retained in `docs/bend-issues/2026-09-19-retry-runtime.json`.

### Original provider retry test suite

All five original named retry tests now pass on native one/four threads using the actual retry loop, native sleep adapter and timer candidate with a test-only virtual monotonic clock. The fixture checks request counts at 999/1,000 ms and 1,999/2,000 ms, preserves a provider-marked non-retryable error, checks the server-delay error text, disables the cap, and cancels a long backoff with exactly one pending timer before abort and zero afterward. It also verifies no second request on cancellation and zero live timers/waiters/channels at exit. A deliberately early clock advance fails the request-count assertion as required.

`tests/provider_retry_named_check.py build/bend-timer-candidate` copies the candidate into a temporary test compiler, adds test-only effect declarations, replaces only the emitted `io_tick` implementation with a virtual clock, and appends test controls. Production timer effect source is verified byte-identical. The source's error-identity matcher becomes original-value preservation and its abort-name matcher becomes the corresponding native typed variant; exact rendering is separately verified. The five-case suite is now ported: **13 ported, 6 partial, 530 pending** suites. Earlier pending statements above describe the preceding component milestones. Timer adoption, complete date interpretation and provider HTTP integration remain unfinished; this does not complete the overall port.

### HTTP header normalization and provider projection

`runtime/src/http-header.bend` supplies typed header-name/value normalization following the [Fetch header rules](https://fetch.spec.whatwg.org/#concept-header-value-normalize). It validates nonempty ASCII token names, lowercases ASCII names, strips only leading/trailing HTTP whitespace, rejects interior NUL/CR/LF and values outside the byte range, and retains valid byte values and empty strings. Invalid names, invalid values and non-byte values are typed failures. This is an immutable string/value boundary, not JavaScript object coercion or reflection. It does not yet implement a full header collection, duplicate-value handling, browser guards or wire encoding.

`ai/src/utils/headers.bend` now ports `providerHeadersToRecord` against the canonical `ProviderHeaders`/`Nullable` types. It preserves caller spelling and string values, removes explicit null entries and returns absence for missing or empty/all-null input. It deliberately does not apply HTTP normalization at this earlier configuration boundary. The companion `headersToRecord` function awaits the native header-collection implementation.

On one/four native threads, `tests/http_header_check.py` passes 5,799 comparisons with Node's actual `Headers` constructor, covering name tokens/casing, byte boundaries, edge/interior whitespace and control characters, non-ASCII values and seeded byte-string cases. Error success/failure is compared, not host exception messages. `tests/provider_headers_check.py` passes 707 projections against pi's actual helper for typed string/null inputs. Both fixtures build with the installed compiler, without new runtime effects. Provider/header transport integration remains unfinished, and original suite counts remain 13 ported, 6 partial and 530 pending.

### Immutable HTTP header collection and response projection

`runtime/src/headers.bend` now implements a persistent server-side header collection with normalized names/values, functional `append`, `set` and `delete`, validated `get`/`has`, `getSetCookie`, and sorted `entries`. Duplicate values retain append order. Ordinary entries combine with comma-space; `Set-Cookie` entries remain separate, while `get` combines them like Fetch. Empty values remain distinct from absent names. Rejected updates return typed errors and leave the caller's retained collection usable. The implementation uses native records and lists, with no runtime effects or host-language production dependency. Browser guards and HTTP wire parsing/encoding remain separate unfinished work.

`ai/src/utils/headers.bend::headersToRecord` now consumes that collection. As in the original helper, later `Set-Cookie` entries replace earlier values in the single-value result. Provider configuration projection continues to preserve raw spelling and null-removal semantics at its earlier boundary. JavaScript prototype setters and numeric object-key enumeration are not part of the native dictionary contract.

`tests/headers_check.py` passes 504 operation sequences against Node's actual `Headers` and pi's actual `headersToRecord`, plus one independent native dictionary sequence for prototype-looking names: 8,164 total operations on both one and four native threads. Each operation checks the previous snapshot, return/error, updated entries, cookies and projected record. Sequences cover case folding, append/set/delete, empty/missing values, ordering, duplicate cookies and invalid names/values. The existing 5,799 normalization and 707 provider-configuration comparisons also remain green on both thread counts. The new checker is included in `tests/native-cleanup.sh`. No original provider transport suite is promoted by these component checks; totals remain **13 ported, 6 partial, 530 pending**.

### Responses request-header assembly

`ai/src/utils/provider-headers.bend` separates immutable configuration overlays from application to native HTTP headers. Overlays retain spelling and replace values at an existing field's position. Applying configuration to inherited HTTP headers validates names/values, replaces case-insensitively, preserves empty strings, and deletes headers explicitly marked null. Null is therefore retained until this boundary instead of being lost through the display-oriented `providerHeadersToRecord` projection. This API applies an ordered configuration to a known base; it does not implement the SDK's branded nullable-header representation or its array-valued input forms, which are outside pi's typed `ProviderHeaders` boundary.

`ai/src/api/openai-responses-headers.bend::defaultHeaders` extracts the pure assembly within upstream `createClient`: user agent, model headers, supplied Copilot dynamic headers, session-affinity headers and caller options, in that order. The caller still supplies the user-agent string and dynamic Copilot record; deriving the latter from messages and integrating the assembly into the HTTP client remain unfinished. This uses the existing native session-affinity policy and canonical provider-header types.

`tests/request_headers_check.py` compares 717 configurations on native one/four threads against the actual extracted upstream `createClient` body and the installed OpenAI SDK 6.40.0 `buildHeaders`. Test-only dependencies supply the user-agent value, resolved affinity, dynamic Copilot record and a recording client constructor. Both the unnormalized configuration and applied HTTP collection are compared. Cases cover caller/session/default precedence, null removal of inherited authorization/user-agent/session headers, empty values, casing collisions, cookies, invalid names/values and validation timing when an earlier value is overwritten. The checker is included in native-cleanup. These are component comparisons, not complete provider transport tests; suite totals remain 13 ported, 6 partial and 530 pending.

### Copilot dynamic headers and transcript integration

`ai/src/api/github-copilot-headers.bend` ports `inferCopilotInitiator`, `hasCopilotVisionInput` and `buildCopilotDynamicHeaders` over the canonical generic `Message` type. Empty history or a final user message selects the user initiator; any other final role selects agent. Vision detection examines user block content and tool-result blocks throughout the history. The explicit `hasImages` input to the dynamic-header builder remains independent, matching the upstream API. No JSON conversion, runtime mutation or host dependency is involved.

`openai-responses-headers.bend::forContext` now derives these headers from a typed `TranscriptContext` only for the `github-copilot` provider, then uses the existing configuration assembly. Model defaults precede dynamic headers; session and caller headers follow them. The lower-level `defaultHeaders` remains available for testing pure assembly. Actual HTTP client integration remains unfinished.

`tests/copilot_headers_check.py` compares the three actual upstream Copilot functions and the extracted Responses `createClient` body with native behavior on one/four threads. All 1,324 histories pass: exhaustive histories through length three across nine role/content shapes, 500 seeded longer histories and four long-history cases. It checks both explicit vision flags independently of detected images, derived Copilot headers, ordinary-provider behavior and caller overrides including explicit null removal. The checker is included in native-cleanup. These component tests do not promote any original transport suite; inventory remains 13 ported, 6 partial and 530 pending.

### Responses credential-selection policy

`ai/src/api/openai-responses-auth.bend` ports `getClientApiKey`: a nonempty explicit API key wins unchanged; otherwise a non-null, nonblank `authorization` or `cf-aig-authorization` configuration value supplies the upstream `unused` client-key sentinel. Missing credentials return the typed `MissingApiKey{provider}` error, whose renderer preserves the upstream message. The check reuses the shared text-whitespace classifier rather than HTTP-value trimming: a whitespace-only explicit key remains nonempty, whereas a whitespace-only authorization header does not satisfy this policy. Transport validation remains a separate later step. Header-name matching uses ASCII case normalization for these two ASCII HTTP names; this is not a general Unicode case-conversion API.

`tests/responses_auth_check.py` passes 4,668 comparisons against the actual extracted upstream helpers on one/four native threads. Cases cover explicit-key precedence, empty keys, missing/null/blank headers, both accepted authorization names, casing collisions, misleading names, every scalar through 1,024, extended whitespace boundaries and seeded configurations. Error messages are compared after native typed-error rendering. All values are synthetic; no credential files or authenticated requests are involved. The checker is included in native-cleanup. This component does not complete the original pre-generation-error suite or the provider HTTP integration; inventory remains 13 ported, 6 partial and 530 pending.

### Bounded octet-level HTTP line reader

`runtime/src/http-line.bend` provides incremental CRLF framing for the future HTTP parser. HTTP message framing operates on octets ([RFC 9112, section 2.2](https://www.rfc-editor.org/rfc/rfc9112.html#section-2.2)); the existing SDK/SSE line decoder performs UTF-8 decoding and recognizes additional line endings, so it is not reused at this boundary. The new immutable reader preserves byte values, stops at the first CRLF, returns unconsumed bytes from the supplied chunk, and reports typed invalid-byte, payload-limit and incomplete-line errors. Its explicit payload limit excludes the delimiter. A pending CR is charged as payload only if the next byte is not LF, allowing a full line's terminator to span chunks. Stored input is bounded by the payload limit plus the pending-CR flag.

This is a framing primitive, not an HTTP acceptance policy: bare LF and CR not followed by LF remain payload for a later syntax validator. It does not yet parse status lines or header fields, choose body framing, handle trailers, or implement the network transport. No compiler/runtime patch is needed.

`tests/http_line_check.py` passes 7,672 cases on native one/four threads against an independent buffered-prefix reference. Coverage includes exhaustive short sequences and chunk partitions over LF/CR/ASCII/high-byte inputs, three payload bounds, seeded byte sequences, empty chunks, out-of-range inputs, exact-limit terminators split across chunks, EOF, and unconsumed remainder preservation. The checker is included in native-cleanup. These are native primitive checks; upstream suite counts remain unchanged.

### HTTP response status-line syntax

`runtime/src/http-status.bend` parses protocol/version digits, a three-digit status and reason-phrase octets into `StatusLine`, with typed syntax errors. `read` composes it with the bounded CRLF reader and retains the remaining bytes. The syntax follows [RFC 9112 section 4](https://www.rfc-editor.org/rfc/rfc9112.html#section-4), with Node Fetch's accepted omission of the trailing separator when the reason phrase is empty. Raw reason octets remain in the parsed value; `statusText` applies the existing UTF-8 decoder to match Node's public response text. In the installed Node 24.18.0 embedded Undici source, `onStatus` uses `buf.toString()`; loopback responses independently confirm replacement of malformed UTF-8. The initial Latin-1 text assumption failed on byte 128 and was corrected before this milestone.

Syntax parsing deliberately does not select supported HTTP versions, reject semantically invalid three-digit status codes, skip informational responses or decide upgrade/body behavior. Those remain transport policy work; a syntactically parsed status is not yet an accepted Fetch response. Broader noncanonical syntax compatibility is also not established. Header parsing, body framing, HTTP/TLS transport and provider integration remain unfinished.

`tests/http_status_check.py` passes 4,990 cases on native one/four threads, including 251 actual Node Fetch responses from an ephemeral loopback server. Grammar checks cover each byte substitution through 256 in a representative line, truncated prefixes, seeded versions/statuses/reasons and reason-byte boundaries. Every split point in representative framed lines checks incremental composition and leftover bytes. Fetch comparisons cover HTTP/1.0 and HTTP/1.1, empty/absent reasons, preserved spaces, accepted reason octets and valid/malformed multibyte UTF-8. The Fetch oracle uses only local synthetic responses and closes its server. The checker is included in native-cleanup; upstream suite counts remain unchanged.

### Received HTTP field-line parsing

`runtime/src/http-field.bend` parses a framed field line into an end-of-headers marker or a typed header. It validates ASCII token names, lowercases names, rejects missing colons and invalid value bytes, and preserves received values as byte-valued strings. Control validation occurs before whitespace handling, so an invalid CR/LF cannot disappear through trimming. This component rejects continuation/folded lines, matching the observed installed Node Fetch behavior. It does not yet collect a complete header section or decide body framing.

Loopback comparisons exposed an important difference from public `Headers` construction: Node Fetch's received-field path skips leading spaces/tabs but retains trailing spaces/tabs. The initial normalization-based implementation failed on `X: ! ` and was corrected to retain the trailing space. `http-header.bend` remains the separate caller-input normalization boundary. The received parser returns its validated `Header` directly; future collection integration must not reapply caller-input trimming. Header value bytes are preserved as Latin-1 code points, whereas the status-text accessor decodes UTF-8.

`tests/http_field_check.py` passes 5,126 cases on native one/four threads, including 515 actual loopback Fetch comparisons. It checks name/value substitutions through scalar 1,024, seeded byte strings, empty/whitespace-only values, colons inside values, invalid controls and field names, high bytes, leading/trailing whitespace and folded-line syntax. The local Fetch oracle supplies synthetic responses only. The checker is included in native-cleanup. These transport-component checks leave upstream suite counts unchanged; HTTP/TLS transport and the full pi port remain unfinished.

### Incremental HTTP response-head assembly

`runtime/src/http-head.bend` combines the status and received-field parsers into an immutable incremental reader. Callers supply explicit per-line payload, complete-head byte and field-count limits. The complete-head bound includes the status line, all delimiters and the final empty line; the field bound counts occurrences, including duplicates. Completion returns ordered individual fields plus unconsumed bytes from the current chunk, preserving duplicate framing declarations for later validation. Typed failures distinguish syntax, framing, each limit and premature EOF. The `headers` view imports validated received fields without reapplying caller-value trimming. No compiler/runtime patch is required.

The resulting `ResponseHead` is parsed syntax, not an accepted Fetch response: version/status policy, informational responses, framing conflicts, body decoding, TLS and network/provider integration remain unfinished. Limits are caller-supplied primitives rather than a claim that final defaults match Node's transport configuration.

Duplicate integration checks found and corrected an earlier header-collection gap: Node combines `Cookie` values using semicolon-space, while ordinary fields use comma-space and `Set-Cookie` entries remain separate. `headers.bend` now applies this distinction to both `get` and `entries`. A new `appendField` operation imports already validated fields; normal `append` still performs caller-input normalization. Earlier generic comma-space statements above should be read with the `Cookie` exception. The existing seeded collection cases are retained, with one added cookie sequence.

`tests/http_head_check.py` passes 7,130 assembly cases on one/four native threads, including every split point for representative/generated heads, single-byte chunks, empty chunks, exact and exceeded bounds, truncation, malformed fields/statuses, invalid input octets, and preservation of body remainder without parsing it. The projected headers also match 104 real loopback Fetch responses, including duplicate cookies, empty values and trailing whitespace. The expanded `tests/headers_check.py` passes 506 sequences and 8,170 operations on both thread counts. The head checker is included in native-cleanup. Original suite counts remain 13 ported, 6 partial and 530 pending.

### Exact received Content-Length parsing

`runtime/src/http-content-length.bend` parses decimal lengths into the existing two-limb `U64`, checking against `floor((2^64 - 1) / 10)` and its remainder before multiplication/addition. It handles leading zeros without floating-point rounding or body allocation. Typed errors distinguish invalid syntax, overflow and duplicate declarations; absence is a separate optional value. `fromFields` consumes the response head's individual fields and rejects a second Content-Length even if both values agree, matching the installed Node Fetch transport. Comma-separated values are also rejected. The received-value grammar accepts leading spaces/tabs and trailing spaces; a trailing tab after digits is rejected, as observed in Node's numeric header parser.

`tests/http_content_length_check.py` passes 3,073 cases on native one/four threads, including 227 actual loopback Fetch HEAD responses. Python arbitrary-precision integers validate the exact limb results around 32-, 53-, 63- and 64-bit boundaries, seeded large values, leading zeros, whitespace and invalid character boundaries. HEAD permits acceptance comparisons for huge declared lengths without waiting for or allocating those bodies. The maximum unsigned 64-bit value is accepted; the next integer overflows. Duplicates, absence and unrelated fields are checked separately. The checker is included in native-cleanup. This implements the numeric/declaration component only; Transfer-Encoding precedence, response-body policy and decoding remain unfinished, and upstream suite totals are unchanged.

### Streaming fixed-length HTTP bodies

`runtime/src/http-fixed-body.bend` implements the pure body reader used after framing policy selects an exact Content-Length. Its persistent state is only the remaining `U64` count. Each call emits the consumed portion of its supplied chunk, returning either the successor reader or completion with untouched leftover bytes. It does not retain previously emitted payload or allocate based on the declared body size. Invalid octets inside the consumed portion are typed failures; bytes beyond the body boundary are neither inspected nor consumed. EOF before completion returns an `IncompleteBody` carrying the exact remaining count. Network ownership, cancellation and backpressure remain responsibilities of the pending effectful transport wrapper.

`tests/http_fixed_body_check.py` passes 1,241 cases on native one/four threads. It checks short-body chunk partitions, seeded bytes/chunks, empty chunks and zero lengths, exact completion, extra input preservation, early EOF, invalid input, and arithmetic across 32-, 53-, 63- and 64-bit boundaries using short payloads. Test initialization goes through the actual Content-Length parser. Sixty-two synthetic loopback Fetch responses confirm final payload or truncation-failure behavior; network chunk boundaries are not assumed to match the fixture's chosen boundaries. The checker is included in native-cleanup. Body-framing selection, chunked/close-delimited decoding, socket integration and the full port remain unfinished; upstream suite counts are unchanged.

### Checked chunk sizes and extension syntax

`runtime/src/http-chunk-size.bend` parses CRLF-framed size-line octets into an exact `U64`. It checks the upper 60-bit threshold before multiplying by sixteen, permits leading zeros and both hex cases, validates extension tokens/quoted values/escaped pairs, and discards extension metadata. Typed failures distinguish invalid sizes, overflow and invalid extensions. The parser retains only its phase and numeric accumulator; the bounded line reader will supply its input in the pending chunked-body decoder.

The implementation was informed by the [llhttp state-machine source](https://github.com/nodejs/llhttp/blob/v9.4.2/src/llhttp/http.ts), with acceptance checked against the installed Node Fetch rather than inferred solely from Node's reported native llhttp version. Fetch's embedded parser has observable differences from that source's character tables: an unescaped DEL inside a quoted extension value is accepted, while an escaped DEL is rejected. The initial shared header-character classification failed this comparison and was corrected locally to the extension context. Existing header validation is unchanged. Empty extension values and other accepted extension transitions follow the observed transport behavior; this is not claimed as a generic strict RFC quoted-string validator.

`tests/http_chunk_size_check.py` passes 3,074 cases on one/four native threads, including 1,236 real loopback Fetch extension comparisons. Exact numeric checks cover leading zeros, both cases, integer boundaries and overflow using Python integers. Extension checks cover token/name/value transitions, empty values, quotes, escapes, byte-class boundaries, out-of-range native inputs and seeded extension lists. Synthetic network cases all declare one-byte chunks, avoiding body allocation based on large numeric test values. The checker is included in native-cleanup. Complete chunked-body framing, trailers and HTTP transport integration remain unfinished; upstream suite counts are unchanged.

### Incremental chunked-body decoder

`runtime/src/http-chunked-body.bend` now composes checked size lines, exact payload counters, required payload delimiters and received-field trailer parsing. It emits payload available in each supplied input chunk without waiting for a complete HTTP chunk, and does not retain previously emitted payload. A zero-size chunk switches to trailers; completion requires the final empty line and returns unconsumed input. Trailers remain ordered individual fields, separate from response headers. Callers supply explicit size-line, trailer-line, total-trailer-byte and trailer-count limits. Stored metadata is bounded by these limits; declared chunk sizes do not determine allocation size.

Typed errors cover syntax, delimiters, limits, invalid octets and premature EOF. If malformed framing follows payload in the same call, the error result includes that already decoded prefix; the effectful wrapper still determines delivery/error ordering. The decoder does not enforce a response's advertised `Trailer` field, select framing from headers, manage connections or provide cancellation/backpressure. Those remain integration work. It also does not merge trailer fields into the public header collection.

`tests/http_chunked_body_check.py` passes 4,296 cases on one/four native threads, including every split point across generated chunked bodies, single-byte and empty chunks, per-call payload emission, terminal extensions, duplicate/empty trailers, retained trailing value whitespace, exact trailer bounds, rejected malformed framing, invalid octets, large counters, premature EOF and untouched remainder bytes. Fifty-six actual loopback Fetch responses validate complete payloads, malformed/truncated-body failures and absence of trailer fields from Fetch's response headers. Network chunk boundaries are not presumed equal to the fixture boundaries. The checker is included in native-cleanup. Full transport integration and the original pi port remain unfinished; original suite counts are unchanged.

### Unified HTTP body decoding and SSE composition

`runtime/src/http-body.bend` exposes one immutable streaming interface for no-body, exact Content-Length, chunked and close-delimited modes. It delegates fixed/chunked decoding to the existing implementations, preserves their typed errors and leftover/trailer results, and validates octets for close-delimited streams. Close-delimited completion requires an explicit clean EOF; I/O failures must remain failures in the pending transport wrapper. Body mode is selected explicitly by a caller; this module does not infer it from a response head. Previously emitted payload is not retained by the body reader.

`tests/http_body_sse_check.py` passes 16,144 native one/four-thread integration cases. A local synthetic HTTP server supplies 72 fixed/chunked/close-delimited responses to actual Node Fetch and OpenAI SDK `_iterSSEMessages`. The native fixture passes decoded body bytes directly into the existing SSE stream decoder and matches final event names/data across every input split point and single-byte input streams. HTTP chunks intentionally split inside multibyte UTF-8 characters and SSE delimiters. Cases cover malformed UTF-8 replacement, multi-line data, named events, comments, CRLF, unfinished events, empty payloads, leftover bytes for no-body/zero-length modes, invalid native byte input and premature EOF. Diagnostic raw-line retention is not compared here because its approved adaptation is covered separately.

The checker is included in native-cleanup. These tests verify pure body/SSE composition against the real upstream stream decoder; the Bend code still receives synthetic byte lists rather than owning sockets. Response framing selection, connection management, TLS, cancellation/backpressure integration and complete provider wrappers remain unfinished. Original suite counts are unchanged.

### Ordered framing-header analysis

`runtime/src/http-framing-fields.bend` analyzes validated, individual response fields before public coalescing. It reuses exact Content-Length parsing and duplicate rejection, classifies the installed Fetch runtime's terminal transfer-encoding behavior, and returns typed framing conflicts. Its sequential immutable state preserves observed handling of empty/repeated Transfer-Encoding fields and Content-Length order. A later empty transfer-encoding value retains the previous transfer classification; an empty field before Content-Length does not establish transfer encoding, whereas a Transfer-Encoding field encountered after Content-Length is rejected. These wire observations are distinct from JavaScript object semantics.

For a response already determined to carry a payload, `bodyFraming` maps the analyzed state directly to the unified chunked, fixed-length or close-delimited reader mode. It does not decide HEAD, informational, upgrade or null-body behavior. Transfer classification is intentionally the installed transport's behavior, not a general transfer-coding grammar or compression implementation. Source context was checked against [llhttp's header-state logic](https://github.com/nodejs/llhttp/blob/v9.4.2/src/llhttp/http.ts), while actual Node Fetch comparisons remain authoritative for the installed embedded parser.

`tests/http_framing_fields_check.py` passes 790 ordered configurations on native one/four threads and against real loopback Fetch responses. Cases include repeated/case-varied fields, empty values, terminal transfer-coding variations, whitespace, conflicts in both orders, invalid/overflowing/duplicate lengths and seeded combinations. GET checks distinguish chunk decoding from close-delimited raw bytes; HEAD checks length/conflict acceptance without allocating declared bodies. Native checks additionally verify exact length values and the selected typed body-reader mode. The checker is included in native-cleanup. Complete method/status policy, connection management, TLS and provider transport integration remain unfinished; original suite counts are unchanged.

### Response method/status policy

`runtime/src/http-response-policy.bend` consumes a parsed head and normalized request method for ordinary Fetch requests. It validates accepted textual protocol-version labels, analyzes framing fields, and returns either an informational-head decision or a final response's wire framing and public-body visibility. HEAD, 204 and 304 select no wire body; 205 retains header-selected wire framing while hiding the public body. This separation follows the native framing logic and Fetch's null-body handling rather than conflating disposal with absence of wire payload. Statuses below 100 are invalid, and the installed ordinary Fetch path rejects 100/101; subsequent informational statuses await a final head.

The accepted version labels are 0.9, 1.0, 1.1 and 2.0 within this textual parser. Acceptance of the 2.0 label does not implement the HTTP/2 binary protocol, and 0.9 here is a status-line label rather than a body-only legacy response. Request method validation/normalization, CONNECT and upgrade transports, interim connection directives, hidden-body draining and response-delivery/error timing remain outside this policy. Source evidence for wire framing is [llhttp's native response logic](https://github.com/nodejs/llhttp/blob/v9.4.2/src/native/http.c); public body removal was also inspected in the installed Node embedded Undici source. Fetch comparisons verify public outcomes but do not independently prove connection reuse or 205 drain behavior.

`tests/http_response_policy_check.py` passes 628 cases on native one/four threads and against actual loopback Fetch. It covers every one-digit version pair, representative informational/final/status-boundary codes, GET/HEAD/POST, no declared length, fixed/chunked/close-delimited modes, null-body statuses and malformed/duplicate/conflicting framing fields. Informational fixtures precede a final response without prematurely declaring connection closure. Native checks verify typed selected framing and visibility; the Fetch oracle checks errors, final status, body nullness and body text. The checker is included in native-cleanup. Full response-stream/socket/TLS integration and the overall port remain unfinished; original suite counts are unchanged.

### Assembled incremental HTTP response stream

`runtime/src/http-response-stream.bend` now composes head parsing, method/status policy and unified body readers. Its immutable events distinguish informational heads, the final head with public-body visibility, payload bytes and completion with separate trailers. Informational heads reset the bounded head reader and consume a caller-supplied count budget. A final head switches to the selected body decoder, forwarding the entire remaining input chunk rather than emitting per-byte body events. Completion returns untouched remainder; failures retain events already produced in that call. Clean EOF completes close-delimited bodies and rejects incomplete heads or framed bodies.

Hidden bodies (notably 205) still pass through their wire decoder, while payload events are suppressed. The final-head event permits a future effectful wrapper to publish the public response before draining finishes; the pure module does not itself schedule that publication. Connection directives, socket reuse, actual read cancellation/backpressure, transport failures, hidden-drain error delivery, TLS and provider integration remain unfinished. In particular, informational connection-close directives still require connection-management handling; the tested interim fixtures keep the connection open until the final response.

`tests/http_response_stream_check.py` passes 4,640 cases on native one/four threads, covering every split point in representative/generated complete responses, empty and single-byte chunks, ordered informational/final/data/completion events, separate trailer propagation, hidden-body consumption, no-body boundaries, exact leftovers, informational limits, early EOF and syntax/body failures with earlier events retained. Forty-two actual loopback Fetch responses verify final statuses, body nullness and payloads across fixed/chunked/close-delimited modes, HEAD, 204/205/304 and informational prefixes. Public outcomes are compared without assuming matching network event batch sizes or delivery timing. The checker is included in native-cleanup; upstream suite counts are unchanged and the overall port remains unfinished.

### Owned HTTP response reader

`runtime/src/http-response-reader.bend` connects the response decoder to an injectable acquired byte-source lease. Its affine cursor returns immutable events and drains each pending batch before requesting another source chunk. Completion, clean EOF, decoder errors, read errors and early closure invoke the release callback exactly once, including closure before the first read. Release receives the last final response head and, on framed message completion, untouched remainder bytes for the future connection owner. The external owner must grant a source lease to only one cursor and retain its borrowed callback handles until cursor retirement; the cursor type prevents copying an existing cursor, not independently granting the same source twice.

Release occurs as soon as decoding reaches a terminal outcome, before terminal events are consumed. Prefix head/data events survive failures; a release failure suppresses the completion event and follows the retained prefix as a typed error. Combined read/decoder and release errors preserve both causes. A terminal error is delivered once; subsequent reads return no event. Early closure discards queued events and returns any pending error. The release callback must relinquish the lease even when it reports failure, so closing again does not retry against a possibly retired or reused resource.

`tests/http_response_reader_check.py` passes 698 lifecycle traces on native one/four threads, with real Bend callback/ref effects. It covers every split of a fixed response, empty and bytewise reads, early/repeated closure, no reads while events remain queued, complete remainder/head metadata, close-delimited EOF, premature EOF, read failures, malformed chunk delimiters with retained payload, trailers, informational heads, hidden 205 bodies, and release-only/combined failures. The type checker rejects cursor duplication. This is a native resource contract, not an upstream Fetch event-timing claim or socket-lifetime audit. The checker is included in native-cleanup; upstream suite counts remain unchanged. Concrete connection management, interruption of blocked reads, TLS and provider wiring still need implementation.

### Owned socket interruption candidate

`runtime/src/socket-interrupt.bend` now owns a separate descriptor for cancelling a live TCP connection without retiring the descriptor held by pending IO. Cancellation policy, synchronization, typed errors and lifetime management are pure Bend. Two small additive Base effects supply close-on-exec descriptor duplication and bidirectional shutdown; the [candidate implementation record](../patches/experimental/socket-control/README.md) contains preparation commands and its ownership contract. The candidate remains uninstalled.

`tests/socket_interrupt_check.py` passes on native one/four threads and Bun. Each backend verifies 12 scheduler-parked reads and 12 backpressured writes wake under cancellation, exactly one competing canceller wins, repeated cancellation does not issue another shutdown, normal disposal preserves communication, and a retained interrupt descriptor cannot target a replacement connection that reused the original descriptor number. Test instrumentation observes all 53 expected socket closes and verifies close-on-exec on each duplicate. Interrupt-owner duplication is rejected. An existing ordinary TCP fixture generates identical C and JavaScript under the baseline and candidate; broader performance validation is still needed before installation. These tests validate the new resource primitive and owner, not provider abort semantics. HTTP source/AbortSignal wiring, cancellation during connection establishment, TLS and full provider integration remain unfinished. Upstream suite statuses are unchanged.

### AbortSignal-aware sockets and HTTP source integration

`runtime/src/abortable-socket.bend` owns a connected socket, one interrupt descriptor and one removable AbortSignal observation for the connection lifetime. Reads/writes observe the retained abort reason before and after socket IO, so local shutdown cannot turn cancellation into successful EOF or hide it behind a socket error. A racing abort after the final observation can follow an already completed operation. Pre-aborted adoption closes the supplied socket; duplication failure also closes it and returns a typed error. Closing removes the observation, joins its watcher, retires the interrupt owner and closes the socket. The caller retains the signal and disposes it only after the socket owner closes. Cancellation during connection establishment remains outside this module.

`runtime/src/http-socket-source.bend` supplies the HTTP response reader's injectable byte-source/release callbacks using that owner. Callback factories and the serial resource are explicitly retired after the cursor. Zero read sizes are rejected so a zero-byte receive cannot masquerade as peer EOF. Source failures retain typed abort/socket errors, and release failures remain available to the reader's combined-error result. This implementation owns a dedicated connection and closes it at response completion/error/early return; pooled reuse and remainder handoff require a separate connection owner. It consumes an already connected socket: request serialization/transmission, DNS, TLS and provider wiring still need integration.

`tests/abortable_socket_check.py` passes 24 lifecycles per backend on native one/four threads and Bun, covering pre-abort, ordinary data/close with subsequent signal use, supplied/default reasons during blocked reads, abort after a partial read, rejected reads/writes after abort, first-reason retention and clean peer EOF. `tests/http_socket_source_check.py` exercises the real socket-to-HTTP path with 21 lifecycles per backend: fixed/chunked/close-delimited payloads, abort of an unfinished close-delimited response, early cursor closure, truncated fixed bodies and invalid read size. Payload and completion/error outcomes are checked independently of network read chunk boundaries. The checkers require the isolated socket candidate and do not alter the installed compiler. These are component/integration tests, not newly ported upstream suites; overall suite statuses and full-port incompleteness are unchanged.

### HTTP request method and head encoding

`runtime/src/http-request-method.bend` validates token syntax, rejects Fetch's forbidden methods and normalizes the six common methods while preserving other spellings. This follows the [Fetch method rules](https://fetch.spec.whatwg.org/#methods), verified against the installed Node Request implementation. `runtime/src/http-request-head.bend` encodes an HTTP/1.1 request line and ordered fields into raw bytes. It accepts an already encoded origin-form target, rejects control/space/fragment/non-ASCII target characters, and validates field names and outgoing field-value bytes. Field values use Latin-1 wire bytes. The encoder does not silently add, coalesce, reorder or normalize header fields; URL preparation, Host derivation and consistent body framing remain the request builder's responsibility.

`tests/http_request_head_check.py` passes 1,259 cases on native one/four threads. The oracle uses actual Node Request method behavior and Node HTTP header validators; expected serialized bytes are constructed independently. Cases include all casing combinations of common/forbidden/PATCH methods, invalid method/name/value bytes, duplicate fields, empty and high-byte values, and accepted/rejected encoded targets. Target validation tests the explicit origin-form boundary, not URL parsing equivalence. The checker is included in native-cleanup.

`tests/http_request_exchange_check.py` composes the head encoder, UTF-8 body encoder, abortable socket sends and incremental response decoder for complete cleartext JSON POST exchanges. Each native backend (one/four threads) and Bun completes 12 exchanges against a loopback server; the server compares method, encoded target, endpoint-derived Host, explicit content type, a Latin-1 header, UTF-8 body bytes and byte-based Content-Length against 12 equivalent actual Node Fetch requests. Dynamic endpoint ports are validated separately and normalized only when comparing captures. Responses are consumed through the same pure Bend response decoder. This is a composed buffered exchange, not the finished Fetch/provider wrapper: URL parsing, default/forbidden-header policy, automatic framing/body preparation, streaming uploads, DNS, TLS, connect cancellation, pooling and provider integration remain outstanding. Upstream suite statuses are unchanged.

### Buffered request body preparation

`runtime/src/http-buffered-body.bend` prepares absent, text and byte-list inputs into immutable request content, an exact binary byte length, normalized method and persistent headers. Text uses the shared UTF-8 encoder, including surrogate replacement; its default `text/plain;charset=UTF-8` content type is added only when no content-type field exists. Explicit values, including an empty value, are preserved. Raw byte bodies add no default content type and reject values outside 0..255. Empty text/byte bodies remain present content, so GET/HEAD reject them along with nonempty bodies. Absent content is kept distinct from length zero. The module computes body length using the existing binary `BigNat` representation, without retaining an additional unary counter proportional to payload size. It does not set Content-Length or choose HTTP framing; absent-body framing is method-dependent and belongs to request transport preparation.

`tests/http_buffered_body_check.py` passes 726 cases on native one/four threads: 723 actual Node Request comparisons plus three invalid-byte boundary checks. Comparisons cover methods, content presence, explicit/default/empty content types, binary data, Unicode and surrogate sequences, BOM preservation, UTF-8 byte counts and the 65,536-byte counter carry. The real JSON POST exchange fixture now uses this module for encoding, header preservation and byte length; 12 exchanges per backend (native one/four threads and Bun) continue to match the actual Node Fetch wire contract. The pure checker is included in native-cleanup. This module intentionally describes buffered inputs; streaming uploads, form data and blob lifetimes still need their own implementations. Automatic framing, URL/Host preparation, DNS, TLS, connection pooling and provider integration remain unfinished. Upstream suite statuses are unchanged.

### Buffered request framing and chunked upload encoding

`runtime/src/http-buffered-framing.bend` now validates and canonicalizes Content-Length for prepared buffered bodies. Invalid decimal syntax and present-body length mismatches return typed errors. Absent content retains the transport's method-dependent framing: POST/PUT/PATCH/QUERY/PROPFIND/PROPPATCH emit zero length, while other methods omit it; valid supplied numeric lengths do not override that absent-body result. Present nonempty content normally emits its exact byte length, and zero-length content follows the same method-dependent omission rule. An explicit zero length with nonempty content on methods without payload semantics selects chunked framing, matching the installed Fetch transport. Caller-supplied Transfer-Encoding is rejected. The result includes an explicit encoding choice and preserves the original entity content/length, so retry data is distinct from wire framing.

`runtime/src/http-chunked-upload.bend` encodes a buffered entity as one hexadecimal-sized chunk plus the terminating empty chunk; empty content emits only the terminator. Its size formatting uses exact binary integer limbs without numeric rounding. The convenience wire encoder materializes the framed bytes; this is not a streaming uploader or a zero-copy claim. The full exchange fixture now obtains both headers and wire content from the framing module rather than adding Content-Length itself.

`tests/http_buffered_framing_check.py` passes 1,620 cases on native one/four threads against actual Fetch using a raw loopback peer (so server-side method restrictions cannot be confused with client errors). It covers absent/empty/nonempty bodies, method-sensitive zero-length behavior, caller length syntax and mismatches, leading zeros, large numeric declarations, explicit Transfer-Encoding and chunked fallback. Successful framing and error outcomes are compared; exact failure timing and socket side effects of rejected calls are not claimed equivalent. `tests/http_chunked_upload_check.py` passes 52 exact framing/hexadecimal cases on native one/four threads, including payloads around the 65,536-byte boundary and integer formatting through 512-bit carries. The integration test completes 12 alternating fixed-length POST and chunked DELETE exchanges on each native thread configuration and Bun, matching Node Fetch's tested request fields and decoded entity bytes. Both pure checkers are included in native-cleanup. URL/Host/default-header preparation, streaming bodies, DNS/TLS, pooling and the provider wrapper remain unfinished; upstream suite counts are unchanged.

### Owned buffered request/response driver

`runtime/src/http-buffered-request.bend` combines pure body preparation, framing selection and request-head encoding into an immutable prepared request. Callers can validate before connecting and retain the entity content for retries. `runtime/src/http-socket-exchange.bend` consumes an already connected cleartext socket, writes the prepared head/body through the shared AbortSignal-aware owner, and exposes response events through an affine exchange. One abort observation spans request writes and response reads. Invalid read sizes and pre-abort close the supplied socket without writing. Write failures close the connection and retain both the original error and any cleanup failure. At response EOF/error the driver disposes callback factories and the source owner automatically, returning a closed exchange whose subsequent reads return no event. Early consumers must explicitly close; dropping an affine value is not resource disposal.

The existing complete-exchange fixture now uses these production APIs instead of coordinating socket creation, individual sends and response decoding itself. Its 12 alternating fixed/chunked JSON exchanges pass on native one/four threads and Bun against the actual Fetch wire captures. `tests/http_socket_exchange_check.py` adds 24 owned lifecycle traces per backend for fixed/chunked/close-delimited completion, retained abort reasons, truncation, early closure, zero-size rejection, pre-abort and terminal-state retirement. `tests/http_exchange_write_error_check.py` injects EPIPE only into a disposable compiler's body-send effect: native one/four threads and Bun retain the write error, send no body prefix, close all three descriptors and finish the upload task and abort watcher. This is controlled fault injection, not an observed compiler defect or a general leak proof.

This driver now uploads concurrently with response reads, with the ownership and early-response checks described below. URL/Host/default-header preparation, connection establishment/cancellation, TLS, pooling, streaming inputs and the provider wrapper also remain pending. The checkers require the isolated socket candidate; no compiler installation changed and no upstream suite status was promoted.

### Concurrent socket writer

`runtime/src/socket-writer.bend` gives an existing AbortSignal-aware connection a separate affine write descriptor. It shares the connection's signal and interrupt watcher, so reads can progress while a write is parked without adding another observation. Creation returns the original connection owner on both success and failure. Sends preserve the retained abort reason through the same before/after checks as connection IO. Closing the writer closes only its descriptor; callers must join and close every writer before closing the connection owner or disposing its signal. This borrowing lifetime is a caller obligation, not a statically enforced cross-owner relationship.

`tests/socket_writer_check.py` verifies native one/four threads and Bun with a loopback peer that deliberately withholds upload reads. Each backend observes eight actual EAGAIN parks, sends early replies while uploads remain unfinished, then verifies cancellation and the exact partial upload prefix. Ordinary writer transmission and continued communication after writer close, pre-aborted writer rejection, affine-copy rejection, one shared shutdown per cancelled connection and all 29 expected descriptor closes are checked. Instrumentation and reduced socket buffers exist only in a disposable compiler copy. The writer is pure Bend using the existing isolated socket primitives; no compiler or production effect changed. The HTTP exchange integration below owns, stops and joins the uploader before retiring its response source. No upstream suite status changed.


### Concurrent HTTP upload and response ownership

`runtime/src/socket-upload.bend` owns a single task that writes request head and buffered body through an independent socket writer. Its shared state distinguishes an active upload, a settled write result and intentional stopping. Settlement and stop compete through a pure atomic state transition. A genuine write failure is retained before interrupting the connection to wake a response read; the source checks that result before and after socket IO so local shutdown cannot become successful EOF. Observing that failure marks it reported, preventing cleanup from emitting it again. Unobserved write failures and interrupt cleanup failures remain available from upload disposal.

`http-socket-source` now owns a lease containing the connection and optional uploader. Every release path stops and joins the upload before closing the connection watcher, interrupt descriptor and original socket. Stopping an unfinished upload suppresses the resulting write error without changing the caller's AbortSignal. A completed upload needs no internal shutdown. `http-socket-exchange.start` returns after launching the uploader; subsequent write failures therefore arrive through response reads rather than requiring startup to wait for all request bytes. The earlier synchronous start-error test is adapted to require the same EPIPE through the read result, a closed exchange and closure of all three descriptors.

`tests/http_early_response_check.py` verifies 15 early HTTP 413 exchanges per backend (native one/four threads and Bun). The server waits for an observed write park before responding and withholds upload reads until connection interruption. Fixed-length, chunked and close-delimited responses complete successfully; a truncated response retains its delivered prefix and decode error; early consumer closure joins the uploader. Each backend verifies the exact partial upload prefix, unchanged caller signal, one shutdown per connection and all 45 descriptor closes. `tests/http_early_response_oracle.py` independently checks the corresponding response-body and consumer-cancellation outcomes against actual Node Fetch; it does not claim identical upload timing or byte counts. These are finite integration tests, not general race/leak proofs. The socket candidate remains isolated; URL/default headers, connection establishment/cancellation, TLS, pooling, streaming request inputs and provider wiring remain unfinished. Upstream suite statuses are unchanged.

Reference inspection of the installed Node 24.18.0 embedded Undici (`process.binding('natives')['internal/deps/undici/undici']`, `onMessageComplete` and `onHttpSocketError`) confirms completion interrupts an unfinished upload. It also identifies a remaining transport case: ECONNRESET after a response has started can call parser finish when that response does not permit keep-alive, rather than always becoming a socket failure. That difference is addressed by the reset-aware source and framing policy below. The early-response tests above deliberately keep the peer open until client cleanup; the separate reset tests control reset timing after the client acknowledges a consumed response prefix.


### Framing-dependent response reset handling

`http-response-reader` now distinguishes transport bytes, EOF, ordinary failure and reset carrying its original error. Its existing Result-based source remains supported; reset-aware transports use `TransportSource`. `http-socket-source` classifies raw platform ECONNRESET through the minimal `Socket.isConnectionReset` primitive, while retained AbortSignal errors and unrelated socket failures remain ordinary failures. The pure `http-response-termination` policy derives persistence from protocol version, Connection tokens and framing. `http-response-stream` retains the decision once per final head, avoiding repeated header scans during body reads. A permitted reset finishes the existing decoder: a close-delimited body completes, while incomplete fixed/chunked framing still fails. Resets before a final head or on persistent framing retain their source error. Release uses an explicit `ResetEOF` reason rather than mislabelling a reset as clean EOF.

The baseline native real-socket run rejected all seven reset cases; actual Node Fetch completed the four close-delimited cases. `tests/http_reset_check.py` now matches those seven outcomes on native one/four threads and Bun, with an explicit client acknowledgement before the peer enables zero-linger reset. It covers HTTP/1.1 implicit close delimitation, an explicit keep-alive field overridden by close delimitation, HTTP/1.0, non-chunked transfer coding, and incomplete fixed/chunked bodies. It additionally checks Bend's typed decode-versus-source failure distinction. The existing reader lifecycle checker expands from 698 to 826 traces per native thread configuration, including reset before any/final headers, informational-only reset, ordinary read failure on close-delimited content, reset completion, truncation, early closure and cleanup failure suppressing completion while retaining earlier events.

`tests/socket_reset_classifier_check.py CANDIDATE BASELINE` validates 514 errno values on each backend and byte-identical generated C/JavaScript for an unrelated TCP fixture. The primitive uses the native platform constant or the Bun platform's errno value; Darwin remains untested. It changes no compiler/scheduler code and the candidate remains uninstalled. Identical emission for one unrelated fixture is not a broad compiler performance measurement. Reset races with unfinished uploads, pooling, URL/default headers, connect cancellation, TLS and full provider integration still need further work; no upstream suite status changed.


### URL percent encoding and byte decoding

`runtime/src/url-percent.bend` implements the URL Standard's eight percent-encode sets over UTF-8: controls, fragment, query, special query, path, user information, component and form. Form encoding turns spaces into plus signs; escaped bytes use uppercase hexadecimal. The decoder performs one forgiving pass over bytes, preserving malformed/incomplete escapes and decoding `%2520` to literal `%20`. It is separate from the strict URI-component decoder used by schema validation. Both directions use explicit accumulator state in pure Bend; no host URL implementation supplies production behavior.

`tests/url_percent_check.py` passes 2,392 cases on native one/four threads and Bun. Expected encode sets are independently enumerated; component/form encodings are additionally checked against actual Node built-ins. Byte decoding is compared with Python's `unquote_to_bytes`, including every byte value, incomplete escapes and overlapping percent signs. Test inputs convert JS surrogate sequences to valid Unicode scalar strings before crossing into Bend; this does not claim that invalid native Char construction is portable. The checker is included in native-cleanup. URL parsing, linked search parameters, form-body integration and provider URL construction are separate work; no upstream suite status changes.


### Numeric URL hosts

`runtime/src/url-ipv4.bend` adds a pure IPv4 address value, parsing, dotted-decimal serialization and the numeric-suffix classification used by special URL hosts. It accepts the URL Standard's one-to-four-part decimal/octal/hexadecimal forms and optional single trailing dot. Numeric accumulation checks U32 bounds before multiplication; an overflow remains distinguishable from invalid digits so a huge hexadecimal suffix followed by `g` is not misclassified as numeric. The module consumes an already-normalized ASCII domain; IDNA, percent-decoded host preparation, IPv6 and the complete URL parser remain implementation work. Non-fatal URL validation warnings are not exposed.

`tests/url_ipv4_check.py` passes 1,290 cases on native one/four threads and Bun, comparing normalization/failure with actual Node URL parsing and numeric-suffix classification with an independent regular expression. Cases cover abbreviated-address range boundaries, octal/hex forms, missing/excess fields, trailing dots, randomized hosts and 65,536-character numeric prefixes. The original recursive decimal predicate overflowed the JS stack; explicit tail state fixes the production path while retaining those large cases. The reduced backend observation is recorded in the Bend issue log. The checker is included in native-cleanup; upstream suite statuses are unchanged.


### IPv6 URL literals

`runtime/src/url-ipv6.bend` implements IPv6 literal parsing and canonical serialization in pure Bend. The address is four U32 words in network order, so its value type contains exactly 128 bits. Parsing handles one zero-compression marker, exactly eight expanded pieces and a final strict dotted-decimal IPv4 suffix. Embedded IPv4 octets reject leading zeros, abbreviations, nondecimal syntax and values above 255. Serialization uses lowercase hexadecimal and compresses the first longest zero run of at least two pieces, including all-zero and trailing-zero cases. The module accepts the literal inside URL brackets; bracket handling and the full host/URL parser remain separate work. Zone identifiers are rejected as required by URL host syntax.

`tests/url_ipv6_check.py` passes 2,733 cases on native one/four threads and Bun, with actual Node URL results independently cross-checked against Python `ipaddress` (excluding Python's additional zone-ID support). Coverage includes every zero/nonzero arrangement across eight pieces, randomized canonical/expanded/compressed forms, excess or missing pieces, repeated compression, embedded IPv4 boundaries, and rejected inputs through 65,536 repeated characters. The checker is included in native-cleanup. DNS/IDNA, URL authority preparation, connection support for these address values and provider integration remain unfinished; upstream suite statuses are unchanged.


### Unicode-scalar Punycode

`runtime/src/punycode.bend` exposes pure encoding/decoding without the `xn--` prefix. Separate encoder/decoder modules share the checked generalized-integer and bias-adaptation code in `punycode-core.bend`. The implementation follows [RFC 3492](https://www.rfc-editor.org/rfc/rfc3492.html), preserves basic-character case and distinguishes invalid input from arithmetic overflow using the reference codec's 31-bit limit. Encoding accumulates output in reverse; decoding inserts into an immutable sequence and keeps code points numeric until the complete parse succeeds, then validates Unicode scalars before constructing native characters. Structural input-length bounds control the outer loops.

`tests/punycode_check.py` passes 3,199 encode/decode cases on native one/four threads and Bun. Node's actual Punycode codec supplies output and error-class comparisons, with successful encodings additionally checked against Python's codec. The test-only Node adapter validates decoder code points immediately before `String.fromCodePoint`, preventing JS from combining invalid surrogate values; it changes neither the codec's parsing nor its arithmetic. Tests cover multilingual text, basic/control characters, random scalar sequences, malformed encodings, repeated labels through 1,024 repetitions, long deltas and explicit overflow. No host codec supplies production behavior.

The codec performs no case folding, Unicode mapping, NFC, contextual/bidirectional checks, label-length policy or domain splitting. Those IDNA layers are our next implementation work; having Punycode alone does not establish hostname or provider URL parity. The list-based insertion and repeated encoder scans follow the ordinary algorithm and are not a claim of linear complexity for arbitrarily large labels. The checker is included in native-cleanup; upstream suite statuses are unchanged.


### Unicode 17 canonical normalization

`runtime/src/unicode-normalization.bend` implements NFC and NFD over scalar strings in pure Bend. It uses fully expanded canonical decomposition mappings, algorithmic Hangul decomposition/composition, stable combining-class ordering and blocked canonical recomposition. Callers reuse an immutable `unicode-normalization-data.Tables` value from `unicode-17-normalization.tables()`. The generated balanced tables contain 3,045 properties and 961 composition pairs. Unicode 17 matches the reference Node 24.18.0/ICU 78.3 runtime; generation pins official source hashes and retains the Unicode License V3 alongside derived data. Python generates constants and runs tests only; normalization performs no host calls or file reads.

`tests/unicode_normalization_check.py` passes 79,622 distinct NFC/NFD comparisons on native one/four threads and Bun, covering the canonical-form assertions in all 20,034 rows of Unicode 17's NormalizationTest and stability for all 1,094,978 other Unicode scalars. The official expected values are independently cross-checked against Node/ICU. NFKC/NFKD are not implemented or claimed. `tests/unicode_normalization_growth.py` adds 14 length/order-checksum comparisons through 65,536 combining marks on all three configurations. Native-cleanup runs the conformance checker, which reuses its build for these growth checks.

The growth checks exposed Base's non-tail `List.length` on Bun. `stable-sort.bend` now uses tail-recursive length counting, run construction and pass traversal, retaining Base's stable merge operation. Its 132 record comparisons pass on native one/four threads and Bun, including 65,536 records and equal-key stability. The reduced failure and investigation remain in the Bend issue log; no compiler/Base installation changed. Unicode IDNA mapping, contextual/bidirectional validity, host parsing and provider integration remain implementation work. Upstream suite statuses are unchanged.


### Unicode 17 IDNA mapping and preparation

`runtime/src/idna-mapping.bend` implements UTS #46 mapping with explicit transitional/nontransitional modes and typed first-disallowed-code-point errors. Its immutable generated table contains 8,201 coalesced ranges covering all Unicode code points. `u32-table.floor` supplies reusable predecessor lookup; range entries retain explicit upper bounds. The generator pins official Unicode 17 source hashes, verifies contiguous coverage, and uses the existing Unicode License V3. `idna-prepare.bend` composes mapping and NFC using a caller-owned immutable context.

`tests/idna_mapping_check.py` passes 23,606 comparisons on native one/four threads and Bun: exact outputs around official range boundaries and for mixed strings, plus ordered block checksums covering all 1,114,112 code points in both modes. Surrogates and out-of-range values are tested numerically without constructing invalid native characters. `tests/u32_table_check.py` passes 540 exact/predecessor lookup pairs per backend, including empty/singleton tables, gaps and U32 boundaries.

`tests/idna_prepare_check.py` passes 6,401 nontransitional mapping-plus-NFC input comparisons per backend using official IDNA source inputs and Node's Unicode 17 normalization. Ten deliberately valid domain examples additionally match Node's public `domainToUnicode`. These are preparation checks, not the full IDNA conformance assertions: ACE label processing, contextual/bidirectional validity, domain policy and URL host integration remain implementation work. All three checkers are included in native-cleanup. Upstream suite statuses are unchanged.


### IDNA bidirectional label rule

`runtime/src/idna-bidi.bend` implements the six conditions from [RFC 5893 section 2](https://www.rfc-editor.org/rfc/rfc5893.html#section-2) as pure immutable transitions. A shared `unicode-bidi-class.Class` represents all 23 Unicode Bidi_Class values. `start`, `push` and `finish` allow classification during character traversal; `analyze` accepts an existing class sequence. The analysis returns both rule validity and the presence of R/AL/AN, continuing detection after a failed rule. This lets the eventual domain processor apply the rule to every label when any label establishes a Bidi domain. Empty sequences fail the rule; domain-level empty/root-label policy remains the caller's responsibility.

`tests/idna_bidi_check.py` passes 33,974 comparisons on native one/four threads and Bun. The declarative reference checks first/last classes, permitted sets and digit coexistence independently of the incremental state implementation. Coverage includes all sequences through length three over all 23 classes, all four-character sequences over the eleven permitted classes plus a rejected class, randomized longer inputs and suffixes through 65,536 NSMs. The checker is included in native-cleanup. Unicode property lookup, application to decoded domain labels and full IDNA conformance remain unfinished; these rule tests do not establish complete hostname parity.


### IDNA contextual joiner rules

`runtime/src/idna-contextj.bend` implements the ContextJ requirements from [RFC 5892 Appendix A.1/A.2](https://www.rfc-editor.org/rfc/rfc5892.html#appendix-A.1) with pure `start`/`push`/`finish` transitions. ZWJ requires an immediate canonical-combining-class 9 predecessor. ZWNJ additionally accepts the joining-type context L/D, optional transparent characters, ZWNJ, optional transparent characters, R/D. A pending right context must resolve before completion; repeated join controls cannot bypass it. The state retains only the immediate virama flag, left joining context and pending right requirement. `unicode-joining-type.bend` supplies all six Unicode joining types, separate from bidirectional class.

`tests/idna_contextj_check.py` passes 12,138 comparisons on native one/four threads and Bun. Its independent oracle scans backward and forward around each joiner instead of reproducing the incremental state machine. Cases include all sequences through length four over the six joining types, transparent/nonjoining viramas and both joiners; randomized longer labels; and transparent runs through 65,536 characters. Empty labels pass this particular rule because they contain no join controls; other label requirements remain separate. The checker is included in native-cleanup. Real Unicode property lookup and full domain integration remain implementation work; abstract-property tests do not establish whole-domain parity.


### Unicode 17 label properties and text analysis

`runtime/src/unicode-17-idna-properties.bend` contains 1,874 immutable ranges for Bidi_Class, Joining_Type, canonical combining class and General_Category=Mark. Generation pins the official extracted-property files and UnicodeData hashes. Broad and script-specific `@missing` defaults are applied before explicit entries, preserving unassigned RTL/currency defaults. Lookup accepts numeric code points, including surrogate properties, without constructing invalid native characters; out-of-Unicode values return `None`. Property presence does not mean IDNA permits that character.

`idna-label-context.bend` walks a scalar label once, looking up properties and composing the Bidi and ContextJ transitions. It returns leading-mark presence, both Bidi facts and ContextJ validity, retaining typed lookup failure. Mapping/NFC/ACE checks and domain policy remain separate unfinished layers. `tests/idna_properties_check.py` independently interprets official intervals/defaults, compares exact property values around source boundaries and checks every one of the 1,114,112 code points through ordered block checksums. Real-text cases include randomized mixtures, viramas, Arabic/Hebrew and the compatibility-review examples below. All 43,196 comparisons pass on native one/four threads and Bun, and the checker is included in native-cleanup. Full URL parity is not claimed.

### Pending behavior decision: Node joiner validation

Node 24.18.0 (Ada 3.4.4) accepts five retained labels that violate ContextJ or Bidi requirements. The [review record](idna-context-review.json) contains exact escaped inputs, actual public `domainToASCII`/URL outputs, versions and the bundled Ada source hash. [Node's bundled source](https://github.com/nodejs/node/blob/v24.18.0/deps/ada/ada.cpp#L9420) returns a whole-label result after the first join control, before later controls and Bidi checks. Its ZWNJ path searches for any qualifying joining character on either side, instead of requiring intervening characters to be transparent. The native standards-based rule tests reject these cases.

`python3 tests/idna_context_review.py` is a separate review command and exits nonzero while the recorded disagreement exists; it is not counted as passing parity or included in native-cleanup. Following Unicode rules was proposed to the user; the choice remains pending. No public hostname policy has been finalized around this difference. This is a reference-runtime compatibility decision, not a Bend defect.


### Corrected transitional capital sharp S mapping

Reviewing [Unicode 17 UTS #46 section 4](https://www.unicode.org/reports/tr46/tr46-35.html#Processing) exposed an implementation/test-oracle defect: the Unicode table maps U+1E9E capital sharp S to U+00DF, but the processing algorithm separately requires `ss` in transitional mode. The earlier table-only oracle repeated our omission. `idna-mapping.bend` now implements that exception, while nontransitional mapping remains `ß`. The checker adds explicit sharp-S strings and reads both official `FAẞ.de`/`FAẞ.DE` conformance rows for independent expected values. All 23,614 comparisons pass on native one/four threads and Bun, retaining complete code-point checksums in both modes. This corrects our standards implementation; it is not a requested behavior deviation or a Bend compiler issue.

### Strict ACE label decoding

`runtime/src/idna-ace.bend` recognizes an already-mapped `xn--` prefix, requires an ASCII encoding, invokes the pure Punycode decoder and rejects empty/ASCII-only decoded values. Typed failures distinguish non-ASCII encodings, codec invalid-input/overflow and ASCII-only output. Successful results preserve the exact decoded text and whether the label was encoded. Later validation must not remap ACE output and must use nontransitional status rules even for a transitional domain. The module implements the strict `IgnoreInvalidPunycode=false` path; it does not claim tolerant display conversion.

`tests/idna_ace_check.py` passes 1,491 cases on native one/four threads and Bun, using the actual Node Punycode codec with scalar validation plus independent encoding of generated examples in Python. Tests include malformed/basic-only encodings, decoded deviation characters, uppercase/basic characters, dots, non-NFC text and reserved prefixes, which are intentionally retained for subsequent validation. Plain/encoded long-input cases reach 8,192 input characters. The checker is included in native-cleanup. NFC/status/hyphen validation and complete domain orchestration remain unfinished; raw successful decoding is not a valid-hostname assertion.


### Prepared-label syntax and character status

`runtime/src/idna-label-rules.bend` implements the syntax/status portion of Unicode 17 UTS #46 validity: optional hyphen-edge and third/fourth-position checks, reserved `xn--` prefix rejection when hyphen checks are disabled, embedded-dot rejection, optional STD3 lowercase-letter/digit/hyphen restrictions and allowed mapping statuses. Nontransitional labels permit valid/deviation statuses; transitional labels permit only valid status. Mapped, ignored, disallowed or absent statuses produce typed failures rather than silently remapping decoded text. Empty-label validity is left to domain/DNS policy. Character positions count native scalars.

`tests/idna_label_rules_check.py` passes 6,384 comparisons on native one/four threads and Bun. A declarative Python reference covers all policy combinations, exhaustive short syntax strings, ASCII/control boundaries, supplementary characters before hyphen positions, random inputs and long labels through 8,193 characters. Status cases cover every mapping constructor and absent lookups; the composed `check` path uses a small explicit range table to exercise mode selection, gaps, range bounds and first-failure behavior. These tests do not replace integration against generated Unicode tables. The checker is included in native-cleanup. NFC, leading-mark/contextual validation and the assembled public domain processor remain unfinished; upstream suite statuses are unchanged.


### Composed strict label validation

`runtime/src/idna-label.bend` now composes strict ACE decoding, NFC verification, syntax/status checks and property-backed leading-mark/ContextJ analysis using reusable immutable tables. It accepts a single label after domain-wide mapping/NFC and splitting. It never repairs decoded ACE text, and automatically forces nontransitional status rules for that text even when the surrounding processing mode is transitional. Explicit options control hyphen, STD3 and joiner checks. Failures retain typed stage-specific reasons; successful labels retain text, emptiness and both Bidi facts for the subsequent domain-wide decision. Empty labels still require caller DNS/domain policy, and the caller must apply Bidi validity to all nonempty labels when any establishes a Bidi domain.

`tests/idna_label_check.py` passes 13,379 exact result comparisons on native one/four threads and Bun, with all three full Unicode 17 tables. Coverage includes every mode/hyphen/STD3/joiner option combination for curated and randomized inputs, plain/encoded deviation characters, invalid or ASCII-only ACE output, decoded non-NFC text, marks, status rejection, hyphen/prefix/dot errors, Bidi metadata, and long inputs through 8,193 characters. Another 8,835 distinct label inputs are taken from official IDNA source/Unicode-result columns; these exercise label-stage behavior, not the corpus's whole-domain assertions. The oracle combines actual Node Punycode/NFC primitives with independently interpreted official intervals and declarative/context-scanning rule checks. It does not use Node's defective whole-domain joiner policy as the expected result.

The independent Unicode reference is shared with `tests/idna_properties_check.py`; all 43,196 existing comparisons still pass after that test-only extraction. Native-cleanup includes the new composed checker. DNS lengths, domain assembly, whole-domain conformance and production URL/provider integration remain unfinished. The pending Node/Unicode compatibility choice remains pending; no default hostname policy was selected. Upstream suite statuses are unchanged.


### Domain-wide Bidi and strict DNS lengths

`runtime/src/idna-domain-policy.bend` folds the label validator's immutable Bidi facts into domain applicability and aggregate validity. It does not decide validity prematurely: every nonempty label participates if any label establishes a Bidi domain, regardless of label order. Empty labels are skipped for Bidi and remain subject to other domain policies. The module also checks already-ASCII label lengths and the combined domain bound with saturating counters, rejecting non-ASCII input and empty/oversized labels. This implements UTS #46 `VerifyDnsLength=true`, including rejection of a trailing empty root label; it is distinct from optional-length URL-host processing.

`tests/idna_domain_policy_check.py` passes 17,483 comparisons on native one/four threads and Bun. Tests exercise the public whole-list Bidi API over exhaustive short label-fact combinations and 8,193-label late-RTL/invalid cases. DNS coverage includes 63/64-character labels, 253/254-character domains, leading/interior/trailing empty labels, ASCII boundaries, random label lengths, and long inputs. An independent wire-length calculation agrees with all 12,778 ASCII-result length assertions extracted from both transitional and nontransitional columns of Unicode 17 IdnaTestV2. Only the A4_1/A4_2 assertions are claimed, not whole-domain conformance. Native-cleanup includes the checker; upstream suite statuses are unchanged. Mapping, splitting, label validation, encoding and these domain policies still need a combined domain entry point and integration tests. The pending Node acceptance-policy decision remains open.


### Complete strict domain-to-ASCII composition

`runtime/src/idna-domain.bend` joins domain mapping/NFC, dot splitting, strict ACE/label validation, Punycode encoding, whole-domain Bidi and optional strict DNS lengths into `toASCII`. Callers reuse the immutable Unicode tables through `idna-label.Context` and supply explicit label/Bidi/length options. Errors distinguish preparation, indexed label or encoding failure, Bidi and DNS-length rejection. Output is assembled with tail-recursive reverse accumulation, preserving labels and empty separators when strict DNS lengths are disabled. ACE-decoded text stays unmapped and uses nontransitional validation. No default URL compatibility policy has been selected.

`tests/idna_domain_check.py` passes 408,896 official Unicode 17 ToASCII assertions on each of native one thread, native four threads and Bun. It exercises both transitional modes and all 32 combinations of CheckHyphens, UseSTD3ASCIIRules, CheckJoiners, CheckBidi and VerifyDnsLength. Successful output is compared exactly with the official columns; failed conversions are checked for rejection, as permitted by the corpus. The corpus provides 6,389 scalar-input rows. Two ill-formed UTF-16 input rows are explicitly reported as inapplicable, using the corpus's allowance for implementations whose strings cannot represent them; they are not counted as passes. Valid surrogate pairs are decoded into scalars. Source hashes and generated tables are checked before testing, and argument batches are bounded by both count and byte size.

This establishes the tested strict ToASCII operation over the scalar corpus and those flag combinations, not tolerant ToUnicode conversion, ill-formed-string boundary handling or complete URL parsing. URL forbidden-host-character checks, numeric-host classification and transport/provider integration remain unfinished. The pending Node joiner-acceptance decision remains open; official Unicode conformance is not presented as equivalence to Node's known incorrect acceptance cases. Native-cleanup includes the conformance checker. Upstream pi suite statuses remain unchanged.


### Typed URL hosts and shared host stages

`runtime/src/url-host.bend` introduces native domain/IPv4/IPv6/opaque/empty variants with canonical serialization. `classifyDomain` accepts an already-processed ASCII domain, rejects forbidden domain characters or emptiness and invokes IPv4 parsing when the final component is numeric. It does not repeat IDNA, percent decoding or lowercasing. `parseOpaque` implements the non-special host path, preserving case and literal percent sequences while encoding Unicode/C0 bytes. Leading brackets dispatch to IPv6 before opaque-host rejection; unmatched closing boundaries return a typed failure. Parser errors retain offending code points or underlying address errors. Nonfatal URL validation warnings remain unexposed.

`tests/url_host_check.py` passes 2,095 host-stage cases on native one/four threads and Bun. Of these, 1,876 use actual Node URL parsing/serialization. The others check stage-specific forbidden/non-ASCII/bracket boundaries independently, since full URL parsing would consume delimiters and strip certain controls before host parsing. Special-domain test inputs receive ASCII case normalization in the adapter to honor this stage's precondition. Cases cover all byte-valued characters, Unicode opaque hosts, literal/malformed percent escapes, empty hosts, abbreviated and overflowing IPv4, IPv6 normalization and invalid brackets, random inputs and long inputs through 8,195 characters. The checker is included in native-cleanup; upstream suite statuses are unchanged.

The URL Standard inspected on 2026-09-19 has an ASCII-domain fallback that bypasses unsuccessful IDNA validity checks, while the pinned Node 24.18.0 rejects `xn--abc-`, `xn--8i7caa` and `xn--` in both public domainToASCII and HTTP URL parsing. The future special-host adapter must preserve the reference behavior rather than silently adopting that newer fallback. This shared classification stage does not choose either behavior. Special-host decoding/IDNA integration, full URL parsing and the pending Node joiner-policy decision remain unfinished.


### Special-host byte preparation

`runtime/src/url-host-input.bend` composes UTF-8 encoding, one-pass percent decoding and UTF-8 decoding without BOM removal. The returned `Prepared` value distinguishes a parsed IP literal from domain text awaiting IDNA/classification. Literal opening brackets dispatch to IPv6 before any percent decoding; an encoded opening bracket remains domain data. Plus signs are not form-decoded. Invalid/incomplete UTF-8 produces replacement scalars for the later IDNA stage to reject, and leading/interior BOMs remain available for Unicode mapping. Empty raw tokens return a typed error. No normalization or compatibility policy is selected at this stage.

`tests/url_host_input_check.py` passes 3,614 cases on native one/four threads and Bun. Python's percent-byte/UTF-8 decoder is independently cross-checked against a JS byte walker and actual `TextDecoder` with BOM preservation; IPv6 literals use Node URL results after enforcing raw-token boundaries. Cases include every escaped byte, 2,816 two-byte combinations, malformed percent sequences mixed with Unicode, restricted/unfinished UTF-8, encoded/literal brackets, preserved plus signs and BOMs, randomized inputs and 16,384-character prefixes ending with incomplete encoded sequences. The checker is included in native-cleanup. End-to-end special-host IDNA dispatch and the complete URL parser remain unfinished; upstream suite statuses are unchanged.


### URL scheme and port tokens

The pure runtime now has typed special schemes, ASCII scheme normalization, default-port lookup and bounded decimal port parsing. `python3 tests/url_scheme_port_check.py` passes 67,206 cases on native one/four threads and Bun. The corpus covers every integer from 0 through 65,536 across eight scheme spellings, all default ports, empty and zero ports, non-ASCII and invalid characters, overflow, and 8,192-character inputs. An independent Python oracle checks complete outputs; 66,354 comparable cases also match actual Node URL construction. Token inputs that full URL parsing would preprocess or delimit, and file authorities whose port restriction belongs outside this component, are checked independently rather than misreported as constructor comparisons.

These functions operate on already-delimited tokens. They do not claim full URL parsing, setters, relative resolution, authority validation or provider HTTP integration. The pending IDNA-context and malformed-form choices are unchanged. Upstream suite statuses remain unchanged. Scheme/default-port and port parsing follow the [URL Standard](https://url.spec.whatwg.org/#port-state), checked against the pinned Node reference.


### URL authority scanning

`python3 tests/url_authority_check.py` passes 2,873 authority token/boundary cases on native one/four threads and Bun. It checks credential escaping, multiple at-signs, password colons, raw percent preservation, optional empty credentials/ports, bracket-sensitive splitting, malformed host tokens passed to later validation, and special/non-special backslash boundaries. Long cases include 8,192 repeated at-signs or password colons. An independent Python oracle checks every returned token and remainder; 3,789 actual Node constructor comparisons across HTTP, HTTPS and custom schemes verify credential/host/port outcomes where the same lexical boundary applies. This is authority scanning, not full URL or host validation.

The shared test text formatter now uses a tail accumulator, preserving exact output while removing the reporting stack cliff exposed by long escaped credentials. Its 80,002 UTF-8 oracle cases pass on native one/four threads; authority output comparisons also pass on Bun. Upstream suite statuses remain unchanged.


### Hierarchical URL paths, queries and fragments

`python3 tests/url_path_check.py` passes 8,627 cases on native one/four threads and Bun: 8,562 path-state cases plus 65 path-start transitions. The path-state cases have an independent byte-encoding/segment oracle, with 8,544 comparable cases additionally checked against actual Node URL construction. The 65 path-start expectations come directly from Node. Coverage includes literal and encoded dot segments, repeated separators, special-scheme backslashes, normalized file drives and protected drive roots, existing base segments, absent versus empty query/fragment values, and distinct path/query/fragment percent-encoding sets. Long cases use 8,192 segments, parent traversals, or Unicode characters. Controls removed by full URL preprocessing are tested independently at the path-state boundary.

`url-path.start` consumes the remainder from authority scanning; `parse` accepts already prepared base segments for direct path-state entry. This does not implement complete relative resolution, opaque paths, preprocessing, setter overrides, file authority parsing or provider HTTP integration. Meaningful upstream suite statuses remain unchanged. Compiler memory findings and the retained reduced/control programs are recorded separately in the Bend issue log.


### Immutable URL record and serialization

`python3 tests/url_serialize_check.py` passes 3,206 normalized URL records, each serialized with and without its fragment, on native one/four threads and Bun. An independent Python record serializer provides exact expectations, and actual Node URL serialization confirms both forms for every record. Coverage includes all six special schemes, custom hierarchical URLs, opaque paths, absent/empty hosts, domain/opaque/IPv4/IPv6 host variants, empty versus present credentials, boundary ports, absent/empty query and fragment, percent-escape preservation, and hostless double-slash path protection. Long cases contain 8,192 characters or segments. These checks validate serialization of normalized records; they do not claim a complete Bend parser or arbitrary-field validation. Upstream suite statuses are unchanged.


### URL input preparation and scheme prefix

`python3 tests/url_input_check.py` passes 6,198 exact input/prefix cases on native one/four threads and Bun. An independent Python trim/filter/regular-expression oracle checks constructor preparation, the separate tab/newline-only operation, normalized schemes, and untouched relative references when a scheme candidate fails. Actual Node constructors agree before/after preparation in 12,396 comparisons using absent and supplied bases, including 8,838 successful parses; the remainder compare rejection outcomes rather than being reported as successful URL parses.

Coverage includes every pair of C0/space edge characters, every byte at scheme/body boundaries, Unicode whitespace and BOM preservation, invalid first/continuation scheme characters, multiple colons and case preservation after failed candidates. Long fixtures retain 16,384-character scheme candidates and a 98,306-character input with embedded tabs/newlines. A compact test-only repetition protocol generates oversized inputs inside Bend to avoid the OS argument-size limit; expected full outputs are unchanged. This is constructor preparation and scheme detection, not complete absolute/relative URL parsing or setter behavior. Upstream suite statuses remain unchanged.


### Opaque URL paths and shared query parsing

`python3 tests/url_opaque_path_check.py` passes 2,730 cases on native one/four threads and Bun. An independent byte encoder and delimiter oracle verifies exact path/query/fragment values; 1,299 cases also match actual Node data-URL construction. Inputs that full construction would preprocess or route to a hierarchical path remain explicit state-level checks rather than constructor-parity claims. Coverage includes literal slashes/dot segments/drives, preserved percent escapes, controls and Unicode, empty query/fragment distinctions, and the rule encoding only the final literal space immediately before `?` or `#`. Long cases include 8,192 characters, repeated segments, or Unicode scalars.

The existing 8,627 hierarchical path/path-start cases still pass on all three backends after moving query parsing into `url-query.bend`. Its UTF-8 query encoding preserves plus signs and existing escapes and selects special-scheme apostrophe escaping where required. Full parser entry selection, relative resolution and URL setter behavior remain unfinished; upstream suite statuses are unchanged.


### Additional pending IDNA compatibility cases

The composed special-host work identified four more concrete Node/Unicode differences: `1.א`, `_.א`, `א._` and `א.1a`. Strict whole-domain Bidi validation rejects their ASCII labels, whose first characters have EN or ON directionality rather than L/R/AL. Node 24.18 accepts them. Its bundled Ada implementation copies ASCII labels without calling its label validator at [the ASCII fast path](https://github.com/nodejs/node/blob/v24.18.0/deps/ada/ada.cpp#L9754). The earlier five joiner-related cases and these four inputs are retained in `docs/idna-context-review.json`; `tests/idna_context_review.py` still exits nonzero for these unresolved differences. The broader Unicode-consistent versus Node-compatible policy choice is pending. A test that explicitly supplies strict Unicode options does not settle that public default or count a known Node divergence as parity.


### Composed special-host parsing

`url-special-host.bend` now connects raw host preparation, strict IDNA processing with caller-supplied options, and final domain/IPv4 classification. Literal IPv6 brackets dispatch before percent decoding, whereas escaped brackets remain domain input and fail validation. Unicode normalization and mapping occur before numeric-host detection, so width/circled-digit variants can become IPv4 addresses. Preparation, IDNA and classification failures retain typed reasons. Reusable Unicode tables come from the caller; the module does not choose a public URL compatibility default.

`python3 tests/url_special_host_check.py` passes 2,215 cases on native one/four threads and Bun with explicit nontransitional Unicode options (Bidi/joiner checks enabled; hyphen, STD3 and DNS-length checks disabled). Of these, 2,176 compare with Node, 30 check raw-token boundaries that Node construction would consume/preprocess, and nine assert the explicitly selected strict policy while remaining pending Node-compatibility differences. The latter are not counted as Node parity. Coverage combines escaped UTF-8/BOMs and malformed byte sequences, ACE validation, NFC/case/width mapping, Unicode-to-IPv4 transitions, randomized numeric hosts, bracketed IPv6 and long domains. Full URL parser composition and provider transport remain unfinished; upstream suite statuses are unchanged.


### Absolute URL composition and reference-corpus scope

`url-absolute.bend` now composes the URL stages into a typed absolute parser, including special-scheme slash handling, non-special authority/opaque dispatch, required-host checks, credential/port handling, file-drive authorities and localhost normalization. The parser has no base argument and does not claim relative resolution or mutable URL setters. Domain-validation options remain explicit.

The independent `tests/url_absolute_wpt_check.py` reads a SHA-256-pinned [WPT URL corpus](https://github.com/web-platform-tests/wpt/blob/c48d58747e1f211527fb695fd60548a997fae617/url/resources/urltestdata.json) from ignored build storage. All 560 inputs with no base are checked; 336 base-dependent cases remain pending. The adapter performs the URL Web IDL scalar-value conversion on corpus strings containing lone UTF-16 surrogates, rather than omitting those rows or extending Bend strings with invalid scalars. The checks currently compare canonical serialization or rejection, not every URL getter or origin calculation.

Eight absolute inputs differ between the current WPT expectations and pinned Node 24.18. They concern ASCII ACE labels such as `xn--` and `xn--pokxncvks`: the corpus accepts them while Node rejects them. Bend follows the Node reference on these inputs, as required by the pi port; these eight cases are not claimed as WPT conformance. The pinned source/hash, node versions, counts and exact differences are retained in `docs/url-wpt-reference-differences.json`. This is distinct from the nine still-pending Unicode-versus-Node validation choices.


Absolute-parser build limitation: installed-toolchain native emission exceeded a 32 GiB RSS guard. Its checker guards native builds and fails if the build cannot finish; it does not silently switch compilers. The passing cleanup script currently runs this parser on Bun. Native validation with the existing isolated cache-lifetime/GC candidate is recorded separately. The candidate remains uninstalled, and the installed native build is not claimed complete.


Absolute parser validation: `tests/url_absolute_check.py` passes 2,222 cases on installed Bun and candidate-generated native code with one/four threads. Of those, 2,213 compare directly with Node and nine assert the explicitly selected strict Unicode policy. `tests/url_absolute_wpt_check.py` additionally passes all 560 applicable corpus inputs against Node on those backends. The authority refactor preserves all 2,873 previous cases on installed native one/four threads and Bun. These runtime results do not resolve the installed native compiler’s memory guard failure; the isolated build procedure and measurements are recorded in `docs/bend-issues.md`. No upstream pi suite status changed.

### Base URL resolution draft

`runtime/src/url-resolve.bend` composes immutable base inheritance with the existing scheme, authority, host and path states. It handles empty/query/fragment references, credentials and port inheritance, network references, special-scheme backslashes, same-scheme references, custom hierarchical schemes, and file host/drive/root behavior. It is not wired into provider transport, and its opaque-base acceptance policy is not final.

`tests/url_resolve_check.py --backend js --wpt-only` passes all 336 base-dependent inputs from the existing pinned WPT corpus, with no WPT/Node differences. The full 3,458-case Bun differential run matches Node on 3,433 cases and intentionally remains failing on 25 unresolved cases: the draft rejects path/query references against opaque bases, while Node accepts them when a fragment occurs later in the input. Fragment-only references work in both. The [review record](url-opaque-base-review.json) retains every mismatch, the Node version and its parser-source hash. No cases are skipped or counted as Node parity. The optional WPT-only command is a named subset, not a replacement for the full differential check.

The user has been asked whether to retain fragment-only opaque-base resolution or reproduce Node's broader acceptance. No answer or approved default is assumed. Native validation now matches Bun on one/four threads: all 336 WPT inputs pass, and the full runner retains exactly the same 25 opaque-base mismatches on each backend. Compilation used the isolated weak-cache compiler; the installation is unchanged. The build and source hashes are retained in `docs/bend-issues/2026-09-19-url-resolve-build.json`. Upstream suite statuses remain unchanged.


### HTTP URL projection and buffered request preparation

`runtime/src/http-url.bend` projects a normalized parsed URL into a typed connection endpoint, effective port, serialized authority and origin-form target. HTTP and HTTPS select plaintext/TLS respectively; unsupported schemes and embedded credentials are rejected. Fragments are omitted, percent escapes are retained, and an explicitly empty query preserves its trailing `?`. The typed host remains available for later DNS/socket selection instead of being reduced to an untyped connection string.

`http-buffered-request.prepareURL` composes this endpoint with existing body preparation, framing and head serialization, retaining both the endpoint and prepared request. It sets Host from the URL authority, overriding a caller-supplied Host to match the pinned Node Fetch wire behavior. The existing lower-level `prepare` API retains explicit target/header inputs. This does not yet implement DNS, TLS, blocked-port policy, redirects, connection management or full provider transport.

`tests/http_url_check.py` passes 1,942 normalized-record projections on installed native one/four threads and Bun, including default/non-default ports, IPv4/IPv6/domain hosts, credentials, encoded paths, empty queries, fragments and long targets. Eight actual local Fetch exchanges independently verify projected Host/target values. `tests/http_url_request_check.py` passes five composed requests on all three backends: absent, text, binary and empty-text bodies, UTF-8 Content-Length, and a caller-supplied Host override. Fields owned by other Fetch layers are not claimed. The composition was also built and tested in a detached checkout containing only this change, without pending form-encoding drafts. Both checkers are in the native cleanup suite. The [validation record](http-url-validation.json) retains oracle observations, build guards and source/artifact hashes. Upstream suite statuses are unchanged.

### Numeric IPv6 connection primitive

The installed `TCP.connect` handles numeric IPv4 only. The additive `TCP.connect_ipv6` candidate now provides numeric IPv6 connection establishment with the existing nonblocking scheduler. Its six numeric inputs cover the four address words, checked port and interface scope. `runtime/src/ipv6-connect.bend` accepts the existing native address value; address parsing remains pure Bend. This is new standard-library implementation work, not a defect report.

On Linux native one/four threads and Bun, each backend passes 128 real loopback/IPv4-mapped exchanges plus 192 expected refusal/out-of-range-port failures. Disposable syscall-boundary tests verify every sockaddr field without contacting external addresses. Existing text-TCP and UTF-8 fixtures produce byte-identical C/JS with and without the addition. Twenty-round compiler timing differences are small, and memory observations are mixed; no blanket performance-neutrality claim is made. The candidate remains isolated and uninstalled. The [implementation record](../patches/experimental/ipv6-connect/README.md) links reproducible commands and measured results.

This does not yet provide DNS, cancellation during connection establishment, TLS, Happy Eyeballs or URL-to-socket/provider integration. The prepared HTTP endpoint already preserves IPv6 as a native value, ready for that integration. Upstream suite statuses remain unchanged.

### IPv6 URL-to-HTTP exchange integration

The IPv6 primitive, typed URL projection, buffered request preparation and existing owned socket exchange now run together in `tests/ipv6-http-exchange.bend`. A local IPv6 server captures 24 requests per backend. Native one/four threads and Bun match Node Fetch for bracketed endpoint authorities, caller Host replacement, empty-query preservation, fragment omission, Latin-1 header values, UTF-8 JSON payloads and fixed/chunked uploads. The same runs consume fixed-length, chunked-with-trailers and EOF-delimited responses; every server peer observes the client closing after response consumption.

This is composition coverage for the existing layers using numeric IPv6 loopback and the isolated additive effects. It was built and tested in a detached committed checkout without form-encoding drafts. The fixture chooses a plaintext IPv6 endpoint explicitly; it is not a complete production connector or provider wrapper. DNS, cancellation during connect, TLS, Happy Eyeballs and pooling remain implementation work. The [validation record](runtime-validation/2026-09-20-ipv6-http.json) retains wire observations, build guards, source hashes and candidate provenance. No upstream suite status was promoted.

### Owned cancellable connection primitive

An isolated additive primitive now exposes numeric IPv4/IPv6 connection attempts before they finish. The affine `Connect` owner is consumed by wait or close; the copyable cancellation handle can close an actual pending connection and remove its parked wait. Completion transfers one socket. Repeated/stale cancellation returns false, including after native registry-slot reuse. This avoids leaving a detached connection attempt running merely because an abort won a higher-level race.

Core lifetime, pending-wait cancellation, competing cancellers, 64-attempt cohorts and stale-reuse tests pass on native one/four threads and Bun, both with disposable resource audits and unchanged production effects. Audits observe actual parked cancellations and zero live attempt rows/owned descriptors/waiters at exit; native registry growth is bounded by cohort size in these runs. Refused connections and invalid ports also pass. These are finite Linux tests, not proof of every completion/cancellation race or all resource lifetimes. The owner cannot be copied. Existing C/JS output controls remain identical; small shared-host compiler timing/memory differences are recorded without a blanket neutrality claim.

The [implementation record](../patches/experimental/connect/README.md) defines commitment, ownership and reproduction commands. The candidate remains uninstalled. Pure Bend AbortSignal composition, deadlines, DNS/address selection, Happy Eyeballs, TLS and production HTTP/provider integration remain unfinished. No upstream suite status changed.

### AbortSignal composition for connection attempts

`runtime/src/abortable-connect.bend` now composes owned numeric IPv4/IPv6 attempts with typed AbortSignal reasons in pure Bend. Pre-aborted calls open nothing. Pending cancellation closes the attempt; every return removes/drains the observation and joins its watcher. A final retained-reason check closes an already completed socket if abort was observed, following the existing abortable-socket policy. The caller retains ownership of the signal.

All 42 local cases pass on native one/four threads and Bun, both with resource instrumentation and unchanged production programs. Audits observe actual parked cancellation and zero live attempts, owned descriptors, waiters and channels; the pre-abort cases invoke no connection primitive. The post-completion closure branch is exercised deterministically through the settlement helper, not presented as exhaustive scheduling coverage. See the [implementation and validation record](../patches/experimental/connect/README.md#pure-bend-abortsignal-adapter). The candidate remains isolated; deadlines, DNS/address selection, Happy Eyeballs, TLS and production HTTP/provider integration remain implementation work. Upstream suite statuses are unchanged.

### Cancellable numeric HTTP exchange

`runtime/src/http-connect-exchange.bend` composes numeric connection establishment with the existing buffered HTTP exchange. A single caller-owned AbortSignal spans connect, upload and response reads. Socket adoption rechecks its retained reason, covering cancellation between the connection watcher retiring and the exchange watcher starting. The result distinguishes typed connection failures from exchange-start failures. Invalid read size is rejected before opening a connection; success transfers the existing exchange owner, which the caller must consume or close before disposing the signal.

The selected network address is separate from request authority, allowing later DNS/address selection without replacing the prepared Host header. This layer is explicitly cleartext and numeric; it does not select HTTPS, resolve names, follow redirects, pool connections or implement a complete provider transport. The [validation record](runtime-validation/2026-09-20-http-connect.json) records the integration checks and remaining scope. No upstream suite status is promoted by these supplemental checks.

All 42 integration cases pass on native one/four threads and Bun in a clean checkout of `5579d54` plus the new module and tests. Coverage includes both numeric address families, preserved logical Host authority, real HTTP upload/response, abort while the peer withholds the response body, pre-abort, refused/invalid connections, a zero-read-size guard and abort against a saturated listen queue. Peers observe socket closure; listening peers observe no connection from the pre-abort and read-size guards. These production-program checks use scheduling delays and do not claim every possible handoff interleaving or resource-table audits. The underlying connection primitive's separate audits remain applicable to that primitive.

To reproduce, prepare socket effects with `python3 scripts/prepare-socket-candidate.py build/FRESH-SOCKET`, add connection effects with `python3 scripts/prepare-connect-candidate.py build/FRESH-HTTP-CONNECT --base build/FRESH-SOCKET`, then run `python3 tests/http_connect_exchange_check.py build/FRESH-HTTP-CONNECT`. The checker guards native compilation at 24 GiB and JS emission at 8 GiB. This candidate remains uninstalled.

### Pure DNS name wire codec

`runtime/src/dns-name.bend` implements binary DNS labels and compressed-name decoding as a dependency of the forthcoming native resolver. `Name` stores octets per label, preserving spelling, case, embedded zeroes and non-text bytes. Encoding produces an uncompressed name and enforces nonempty labels of at most 63 octets and an expanded name of at most 255 octets including the root. Empty label lists represent the root. Hostname/IDNA conversion and case-insensitive question comparison belong to higher layers.

A validated immutable packet index supports offset lookup without repeated list scans. It rejects non-octets and messages over 65,535 bytes. Decoding returns the native name and the next offset in the original wire field, even after following compression. Pointers must target an earlier name segment; their strictly decreasing ceiling rejects forward/self/cyclic references. Expanded labels have the same 255-octet bound. A 65,536-step structural budget exceeds the maximum possible strictly decreasing 14-bit pointer chain plus bounded label bytes. The codec detects reserved label forms, missing bytes and invalid offsets; it does not know the enclosing message's field boundaries or verify that every target was a previously parsed name field.

The implementation follows [RFC 1035 section 2.3.4](https://www.rfc-editor.org/rfc/rfc1035.html#section-2.3.4) and [section 4.1.4](https://www.rfc-editor.org/rfc/rfc1035.html#section-4.1.4). `python3 tests/dns_name_check.py` passes 1,987 cases on native one/four threads and Bun, checked against an independent byte interpreter. Cases include arbitrary binary labels, exact size boundaries, compressed suffixes, pointers to pointers, 8,192-pointer chains, truncation, cycles, all first-octet forms, U32-max offsets, and the message-size limit. The [record](runtime-validation/2026-09-20-dns-name.json) retains source/build hashes and guarded build measurements. No compiler/effect changes were required. DNS message parsing, transaction matching, transport, configuration, caching and provider integration remain implementation work; upstream suite statuses are unchanged.

### DNS message structure and query encoding

`runtime/src/dns-message.bend` builds on the binary name codec with native `Header`, `Question`, `Record` and `Message` values. It parses all four counted sections, preserving their order, unknown record types/classes, all flag bits and full 32-bit TTL values. Each resource record retains a bounds-checked data span in the original immutable packet so record-specific compressed names can still resolve packet-relative offsets. It does not reinterpret unknown records or copy their data through text.

Complete parsing rejects missing header/field/data bytes, malformed names and leftover bytes after the counted sections. A separately exposed header parser lets the resolver inspect truncation before choosing transport fallback. The generic section walker uses a static pure parser and explicit immutable progress. `query` encodes one uncompressed question with caller-supplied ID and flags, rejecting fields outside 16-bit range before serialization. This is structural wire handling: transaction/source/question matching, response acceptance, record interpretation, DNS transport, configuration and caching remain resolver work.

`python3 tests/dns_message_check.py` passes 890 cases on native one/four threads and Bun against an independent Python byte parser and packed query bytes. Coverage includes mixed/compressed sections, binary names, unknown records, full-width fields, zero-length data, 256-record ordering, compressed CNAME spans, randomized messages, every truncated prefix of a representative response, excess section counts, trailing data and query field/name limits. The [validation record](runtime-validation/2026-09-20-dns-message.json) retains hashes and guarded build measurements. The native build completed in 6.94 seconds with 1,633,700 KiB sampled process-group peak RSS; these are individual observations, not a performance comparison. No compiler/effect patch was needed and no upstream suite status changed. Wire layout follows [RFC 1035 section 4.1](https://www.rfc-editor.org/rfc/rfc1035.html#section-4.1).

### Typed DNS address, alias and authority records

`runtime/src/dns-record.bend` interprets IN-class A, AAAA, CNAME, NS, PTR and SOA records as native values. Address records reuse the existing IPv4 and IPv6 representations, preserving network byte order. Name records require the compressed wire field to consume exactly the declared data span while following pointers in the original packet. SOA keeps both names and all five full-width numeric fields. Unknown types and non-IN classes remain opaque records with their metadata and original span. The entry point validates spans even when callers construct records directly.

`python3 tests/dns_record_check.py` passes 2,306 cases on native one/four threads and Bun against an independent binary interpreter. Coverage includes arbitrary address words, zero/max numeric fields, binary/compressed names, SOA truncations, exact-length checks with following bytes still present in the packet, invalid/manual spans through U32 limits, opaque class/type handling, and composition with the real message parser. The retained earlier nested-dispatch JS program passes the same cases with `--baseline-js build/dns-record-nested.js`. The [validation record](runtime-validation/2026-09-20-dns-record.json) captures this scope and build hashes. Layout references are [RFC 1035](https://www.rfc-editor.org/rfc/rfc1035.html#section-3.3) and [RFC 3596](https://www.rfc-editor.org/rfc/rfc3596.html#section-2.2). These decoders do not choose cache lifetimes or trusted answers; response matching, alias traversal, transport and resolver integration remain work. No upstream suite status changed.

### DNS transaction response matching

`runtime/src/dns-response.bend` matches an incoming packet against explicit selected local/remote endpoints, transaction ID, opcode and one expected question. Endpoint equality includes address family, every address word, IPv6 scope and port in both directions. The transport must supply actual socket metadata rather than wildcard bind addresses. Question comparison preserves label boundaries and applies only ASCII letter folding; arbitrary binary octets remain significant. Parsed names keep their original spelling.

The decision distinguishes ignored/malformed packets, a matching truncated response and a matching structurally complete message. Truncation is returned only after endpoint/header checks and a complete matching question, before interpreting potentially incomplete record sections. Complete responses pass the existing strict message parser. Header flags and response codes remain untouched for later policy: matching does not assert authentication, successful resolution or permission to cache unrelated records. Random transaction IDs/source ports, transport retry, record trust and address/alias selection remain implementation work.

`python3 tests/dns_response_check.py` passes 1,370 cases on native one/four threads and Bun. Coverage includes both address families, each IPv6 address word and scope, local/remote address and port mismatches, IDs/opcodes, query-vs-response flags, question counts/types/classes, every possible single label octet, ASCII case comparisons, binary label boundaries, matching/mismatching truncated responses, every truncated prefix of a query-sized response and a complete compressed-owner answer. The [record](runtime-validation/2026-09-20-dns-response.json) retains offline test scope and build hashes. Native compilation completed in 4.23 seconds with 969,868 KiB sampled group peak RSS; no compiler changes were required. The matching and folding contracts are based on [RFC 5452 section 9.1](https://www.rfc-editor.org/rfc/rfc5452.html#section-9.1) and [RFC 4343 section 3](https://www.rfc-editor.org/rfc/rfc4343.html#section-3). This is not full resolver or RFC 5452 implementation coverage; upstream suite statuses are unchanged.

### Incremental DNS TCP framing

`runtime/src/dns-tcp-frame.bend` implements the two-octet network-order length prefix and incremental payload reader. Prefix and body can be split across arbitrary reads. Each call returns one complete frame and the unread suffix, enabling caller-controlled processing of coalesced messages without accumulating a batch of arbitrary size. EOF succeeds only between frames; partial prefixes/bodies report incomplete framing. Invalid octets fail explicitly. Encoding validates the 65,535-byte length bound and yields prefix plus payload in one buffer. Empty payloads are representable as frames but fail the separate DNS message parser.

`python3 tests/dns_tcp_frame_check.py` passes 1,759 cases on native one/four threads and Bun against a concatenated-byte reference. Tests include exhaustive partitions of a short multi-frame stream, empty reads, randomized coalescing/splitting, invalid octets at every position, frames preceding errors, partial EOF, encoder limits, and a maximum-size frame delivered one byte at a time. Test output checks per-frame byte count and rolling byte fingerprint; it is not a claim of literal output comparison for every large payload. The [record](runtime-validation/2026-09-20-dns-tcp-frame.json) retains source hashes and guarded build measurements. Native compilation took 3.69 seconds and peaked at 904,700 KiB sampled group RSS; no compiler changes were required.

This implements the framing requirement in [RFC 7766 section 8](https://www.rfc-editor.org/rfc/rfc7766.html#section-8), not its full transport lifecycle. Before TCP integration, response matching must implement [section 7](https://www.rfc-editor.org/rfc/rfc7766.html#section-7)'s rule for replies without a question section; the existing single-question matcher is deliberately not presented as that TCP policy. Cancellable socket composition, pipelining/transaction dispatch, retries, reuse and resolver integration remain implementation work. Upstream suite statuses are unchanged.

### TCP-specific DNS response matching

`dns-response.inspectStream` now implements the established-TCP matching distinction from [RFC 7766 section 7](https://www.rfc-editor.org/rfc/rfc7766.html#section-7). It accepts an absent question section, while a present question must still match name/type/class. More than one question is rejected. Endpoints, transaction ID, response flag and opcode are checked first, and non-truncated messages still pass complete structural parsing. The returned message retains its actual zero-question header/list; the matcher does not synthesize a question. `inspect` retains the complete-question datagram contract.

The expanded `tests/dns_response_check.py` passes 2,912 cases on native one/four threads and Bun. It retains the original datagram cases, repeats present-question and metadata cases through the stream path, and adds zero-question headers, complete answers, every prefix of an answer, trailing bytes, wrong IDs/opcodes/endpoints and query flags. The [stream validation record](runtime-validation/2026-09-20-dns-response-stream.json) pins the updated sources and guarded build results. These remain offline matching checks; socket metadata integration, TCP transaction routing/retry and complete resolver behavior remain work. A truncated TCP response remains an explicit indication for later policy, not an implemented retry loop. No upstream suite status changed.

### Owned cancellable DNS TCP connection

`runtime/src/dns-tcp-connection.bend` composes the frame codec with an owned abortable byte socket. Sending writes the validated prefix and payload together; encoding failure preserves the connection. Receiving retains unread coalesced frames, returns them in arrival order and distinguishes clean EOF from an incomplete frame. Read errors and EOF remain available on subsequent reads with a valid read size. Retained abort state is checked even before delivering a buffered frame. A zero read size is rejected before socket IO. The caller closes the affine connection after EOF/failure; close joins the existing cancellation watcher and retires its descriptors. A transport send error may follow a partial write and requires closure rather than retrying a frame on that connection.

`python3 tests/dns_tcp_connection_check.py CANDIDATE` passes 36 Linux local cases on native one/four threads and Bun, using the existing isolated socket/owned-connect candidate. Both address families cover three sends before reads with reverse-order replies, one-byte receives, incomplete prefixes/bodies at EOF, repeated terminal results, invalid-write/read-size guards, pending receive cancellation and deterministic cancellation with a test-only buffered suffix. The server validates complete DNS query frames and observes client termination. These are production-program integration checks without resource-table instrumentation; they do not prove all scheduling interleavings or general leak freedom. The [record](runtime-validation/2026-09-20-dns-tcp-connection.json) pins scope, build/candidate hashes and limitations.

The fixture composes cancellable numeric connection establishment and socket adoption before the new connection owner. The production layer itself adopts an existing abortable socket. Transaction multiplexing/response matching, actual endpoint metadata, DNS retries/cache/alias policy, TLS and complete resolver/provider integration remain unfinished. No new effect or compiler patch was required, and upstream suite statuses are unchanged.

### Numeric socket endpoint observation

`runtime/src/socket-endpoint.bend` exposes native `Local`/`Peer` selection and typed IPv4/IPv6 endpoints, preserving IPv6 scope. Its additive `Socket.endpoint` OS primitive returns the original affine socket on both success and failure. It only observes metadata; address parsing and protocol policy remain pure Bend. The candidate changes neither compiler nor scheduler and remains uninstalled. The [implementation record](../patches/experimental/socket-endpoint/README.md) contains preparation and validation commands.

[Live and boundary validation](runtime-validation/2026-09-20-socket-endpoint.json) passes on native one/four threads and Bun. Live Linux tests compare both endpoints against Python for IPv4, IPv6 and IPv4-mapped TCP, with 144 byte exchanges after inspection/error checks, plus three unconnected UDP cases. The 96 disposable boundary cases cover full address words, port/scope extremes, unsupported families, short lengths and errors. Simulated Darwin JS layouts are covered; actual Darwin FFI is not verified.

The endpoint-only candidate preserves exact C and JS output for the existing text TCP and UTF-8 compiler controls. Twenty alternating-order compiler samples yield median candidate/baseline wall ratios of 1.017 and 0.992; median peak RSS is 151,662/152,260 KiB for text TCP and 414,936/409,710 KiB for UTF-8. These shared-host measurements show small mixed differences, not a general proof of performance neutrality. [Raw performance samples](runtime-validation/2026-09-20-socket-endpoint-performance.json) and [JS emission checks](runtime-validation/2026-09-20-socket-endpoint-js-controls.json) retain the limited scope. DNS transport still needs to incorporate endpoint observations into transaction matching. Resolver/provider integration and upstream suite statuses are unchanged.

### DNS TCP endpoint and matching composition

`runtime/src/dns-tcp-peer.bend` consumes a connected socket, observes both endpoints, converts address words/ports/scope to the DNS matcher's native types and adopts the existing cancellable TCP connection. Its immutable route constructs expected transactions and inspects replies using the observed reverse direction. Inspection failures close the input socket; adoption retains the existing abort check and watcher lifecycle. The signal remains caller-owned. The returned peer exposes route and connection separately so callers can send multiple queries and route returned frames without an implicit filtering loop dropping another transaction's response.

`python3 tests/dns_tcp_peer_check.py build/bend-endpoint-candidate` passes 42 live Linux IPv4/IPv6 cases on native one/four threads and Bun. The independent peer validates query bytes, compares the observed endpoints against its own socket metadata, and sends replies in reverse query order. Cases cover matching replies, an omitted question, mismatched ID/question, truncation, malformed DNS followed by a valid frame, and cancellation after connect but before adoption. Each connection terminates with peer-observed EOF. Three additional runs each repeat 32 failed peer inspections on unconnected UDP sockets and rebind their former ports, verifying that this failure path releases the socket.

The [validation record](runtime-validation/2026-09-20-dns-tcp-peer.json) captures sources, binaries, candidate hashes, guarded builds and limitations. Main native compilation took 24.20 seconds with 7,253,428 KiB sampled group peak RSS under an 8 GiB guard. No compiler or effect changes were required. Local-endpoint failure injection, exhaustive cancellation interleavings, automatic transaction multiplexing, DNS retry/cache/alias policy and complete resolver/provider integration remain work. Upstream suite statuses are unchanged.

### Immutable DNS pending transactions

`runtime/src/dns-pending.bend` tracks outstanding transactions for one TCP connection in Bend's native persistent map. Registration rejects out-of-range and duplicate IDs without replacing an entry. Each entry retains the full expected transaction and a caller-defined data token. Reply dispatch reads the first two octets to select an entry, then delegates endpoint/header/question/message validation to the existing stream matcher. Unknown IDs, malformed IDs and mismatched replies retain all outstanding queries. A matching response or matching truncation removes exactly its entry and returns its token with the parsed message/header. Explicit removal supports driver cancellation and failed sends; it does not grant an old ID permission to cancel a later transaction after reuse.

`python3 tests/dns_pending_check.py` passes 56 sequences containing 8,760 transitions on native one/four threads and Bun against an independent Python dictionary model. Tests cover every high ID byte through 768 simultaneous entries, reverse-order delivery, zero/max and invalid IDs, duplicate registration, unknown/duplicate replies, malformed packets, wrong endpoints/questions/opcodes, omitted questions, truncation, removal, reuse and retained/restored immutable snapshots. Each sequence drains remaining entries to detect collateral deletion or replacement. The [record](runtime-validation/2026-09-20-dns-pending.json) retains source hashes and guarded builds. Native compilation took 18.26 seconds with 5,909,060 KiB sampled group peak RSS. No compiler/effect patch was required.

This is the pure dispatch state, not an asynchronous multiplexing driver. Random ID allocation, send/read serialization, caller completion delivery, deadlines, retry policy and connection-wide failure cleanup remain implementation work. Socket composition continues through `dns-tcp-peer` and `dns-tcp-connection`; upstream suite statuses are unchanged.

### Owned DNS TCP session

`runtime/src/dns-tcp-session.bend` composes the observed peer route, affine TCP connection and immutable pending table. `submit` encodes the query, rejects duplicate IDs before writing, registers the expected transaction and writes its frame. Several queries can be submitted before receiving. `receive` returns one frame decision, including ignored replies, so scheduling stays with the caller. Accepted responses/truncation settle their matching tokens; ignored frames retain pending requests. A zero read size is rejected without changing session state.

Transport failure or EOF permanently ends the session and returns every outstanding entry. Failed writes may be partial, so the attempted request is included in this settlement and no later submission writes bytes. Repeated reads retain the terminal reason but return no already-settled entries. `close` remains required after termination; it joins the existing socket watcher and returns any entries still pending during an early close. The new `Pending.drain` uses the native map traversal; settlement ordering is not a submission-order guarantee.

`python3 tests/dns_tcp_session_check.py build/bend-endpoint-candidate` passes 42 live Linux cases across IPv4/IPv6 on native one/four threads and Bun. The peer validates exact query frames and checks client EOF with no unexpected bytes. Coverage includes reverse-order replies preceded by malformed/unknown-ID frames, matching truncation, duplicate/invalid submissions, zero-size read rejection, EOF with pending queries, incomplete frame failure, cancellation of a forked read, a pre-aborted write attempt, repeated terminal results, stopped submissions and explicit early close. The [record](runtime-validation/2026-09-20-dns-tcp-session.json) retains traces, hashes and build scope. Native compilation completed in 26.80 seconds with 6,428,528 KiB sampled group peak RSS under an 8 GiB guard; no compiler/effect changes were required.

This is an owned asynchronous session with serialized operations, not yet a concurrent command-queue driver accepting new submissions during a blocked read. The tests do not inject partial-write syscall failures, audit every internal resource table or prove all cancellation interleavings. Random ID allocation, per-request deadlines/cancellation, retry policy, resolver integration and complete provider behavior remain work. Upstream suite statuses are unchanged.

### Per-request DNS cancellation tickets

Pending registration now returns a `Ticket{id, serial}`; successful session submission exposes that ticket. Each successful registration advances the session-local serial once. Duplicate/invalid registration preserves it. Serials do not wrap: after the final U32 serial, registration returns typed `Exhausted` while existing requests remain removable/dispatchable. The driver can replace an exhausted session. `Pending.cancel` compares the ID and serial, so a stale or repeated cancellation leaves a newer request untouched. `Session.cancel` returns the removed entry, preserves other requests and performs no socket shutdown or detached IO.

Tickets belong to one session history; they are not globally unique or unforgeable capabilities. Local cancellation cannot retract an already transmitted query or distinguish an old matching response after reuse of the same wire ID. The future driver must still choose an ID-reuse policy and serialize cancellation admission with its other commands.

The [cancellation validation record](runtime-validation/2026-09-20-dns-cancellation.json) captures 8,802 transitions in 58 pure-table sequences per backend, plus 48 live session cases across native one/four threads and Bun. Pure checks extend the independent model with serial preservation, repeated/stale cancellation, reuse and test-initialized exhaustion near U32 maximum. Live IPv4/IPv6 tests retain all previous session cases and add cancellation of one of two queries, an ignored reply before ID reuse, successful reuse, harmless stale cancellation and continued delivery for both active requests. Candidate effects and the compiler are unchanged. Concurrent command admission, timer/deadline wiring, wire-ID reuse policy and resolver integration remain work; upstream suite statuses are unchanged.

### Owned deadline scope

`runtime/src/deadline.bend` composes the existing owned timer and removable abort observations in pure Bend. The affine scope owns a child abort controller and two joined tasks: parent forwarding and timer expiry. Its borrowed signal distinguishes inherited parent reasons (including default abort) from deadline expiry. A parent already aborted at the initial check creates no timer. Otherwise the literal millisecond deadline begins at `Timer.new`, and parent forwarding and expiry compete through the child's existing first-settlement rule. Parent cancellation also cancels the timer; child expiry never aborts the parent.

Closing the scope cancels unused timer/parent observation work, joins both tasks, reads the immutable settled reason and disposes the child controller. Callers finish all borrowed-signal operations before close and retain ownership of the parent. Expiry or parent forwarding racing with close may legitimately win before cleanup finishes. This is cooperative cancellation; it does not forcibly terminate an operation that ignores its signal.

`python3 tests/deadline_check.py build/bend-timer-candidate` passes six unmodified runs and 24 disposable instrumented runs on native one/four threads and Bun. Pre-aborted default/custom reasons allocate zero timers. Each full run creates and retires 194 timers: 32 early closes, 32 close/expiry races, two expirations with a reusable parent, a broadcast to 64 scopes and 64 parent/expiry races. Instrumented exits have zero live timers/waiters; native exits additionally have zero live channel rows. Bun channels are not audited. The [validation record](runtime-validation/2026-09-20-deadline.json) retains source hashes, samples and guarded emission statistics. These finite lifecycle checks are not a runtime benchmark or an exhaustive race proof.

No timer primitive, compiler, scheduler or installation changes were needed. The isolated owned-timer candidate remains required. DNS/provider deadline wiring and a concurrent DNS command driver remain implementation work; upstream suite statuses are unchanged.

### Deadline-bounded DNS TCP exchange

`runtime/src/dns-tcp-query.bend` runs one query on a dedicated numeric TCP connection. One owned deadline spans connect, socket adoption, query submission and all response reads. Unknown or malformed replies leave the expected request pending and do not restart the deadline. A matching answer or truncation returns its native parsed value; response codes remain untouched for resolver policy. Invalid read sizes are rejected before creating a deadline or opening a socket. Every typed result path closes the session, then the deadline scope. The report retains the primary result separately from a socket cleanup error and the final deadline reason; a cancellation racing after completion does not retroactively replace a successful answer.

`python3 tests/dns_tcp_query_check.py build/bend-dns-deadline-candidate` passes 90 local Linux IPv4/IPv6 cases across native one/four threads and Bun. Cases cover success, ignored unknown-ID and malformed frames before success, truncation, preserved response codes, EOF, incomplete framing, silent-peer deadlines, deadlines amid repeated irrelevant responses, parent/default/pre-abort handling, invalid query/read-size guards, connection refusal and deadline cancellation of pending connection establishment. The pending-connect fixture first verifies that a nonblocking control probe stalls against a saturated local accept queue. Established peers validate exact query frames and observe closure; pre-aborted and invalid-size calls make no connection. These production-program tests do not audit internal timer/socket/channel tables.

The [record](runtime-validation/2026-09-20-dns-tcp-query.json) pins sources, candidate hashes and guarded builds. Native compilation completed in 25.81 seconds with 6,653,892 KiB sampled group peak RSS under a 16 GiB guard. The combined candidate adds the unchanged owned timer to the unchanged endpoint/socket/connect candidate: `python3 scripts/prepare-timer-candidate.py build/bend-dns-deadline-candidate --base build/bend-endpoint-candidate`. The preparer now supports an explicit base and verifies byte preservation of existing files. No compiler, scheduler, effect implementation or installation changed.

This is a usable transport operation, not a complete resolver or a replacement for the multiplexed session API. Server/address selection, secure query-ID allocation, retry policy, response-code interpretation, answer trust, aliases, caching and provider integration remain unfinished. Upstream suite statuses are unchanged.

### Address answer selection and CNAME traversal

`runtime/src/dns-address-answer.bend` selects IN A/AAAA records from a matched complete response. It indexes answer owners by encoded DNS names with ASCII-only folding; label lengths and arbitrary binary octets remain distinct. It follows CNAMEs independent of record order, detects repeated names with a visited index, and uses answer-count-derived structural fuel rather than an arbitrary hop limit. Different targets for one alias, or CNAME coexistence with A/AAAA, return typed conflicts. Duplicate aliases with equivalent names are accepted. Alias traversal and loop errors follow [RFC 1034 §3.6.2](https://www.rfc-editor.org/rfc/rfc1034.html#section-3.6.2); the single-target constraint is described in [RFC 2181 §10.1](https://www.rfc-editor.org/rfc/rfc2181.txt).

Only answer-section records contribute. Unrelated owners, other classes and authority/additional addresses are not promoted into results. Relevant malformed address/CNAME RDATA returns a typed error, while unrelated opaque data is not decoded. Selected addresses retain wire order and raw TTLs. The result includes the terminal canonical name even if no address is present; an empty result is not proof of a cacheable negative answer. Nonzero ordinary header RCODE is returned separately. Extended errors, negative/alias caching, follow-up queries, full DNSSEC/CNAME coexistence validation and answer-trust policy remain implementation work.

`python3 tests/dns_address_answer_check.py` passes 900 cases on native one/four threads and Bun against an independent record-set model. Coverage includes all single-octet binary labels, label-boundary collisions, case folding, both address families, raw TTL extremes, duplicates, shuffled chains, compressed owners and self-alias targets, malformed relevant/unrelated data, non-IN records, section separation, alias conflicts, 512-hop chains and 1,000 duplicate self-alias records. The [record](runtime-validation/2026-09-20-dns-address-answer.json) retains source hashes and guarded build results. Native compilation completed in 15.55 seconds with 3,793,860 KiB sampled group peak RSS under an 8 GiB guard. No compiler/effect patch was needed, and upstream suite statuses are unchanged.

### Address lookup across DNS responses

`runtime/src/dns-address-lookup.bend` composes the dedicated TCP exchange and address selector for caller-selected recursive servers. A and AAAA requests use IN class and recursion desired. A response containing aliases but no terminal addresses triggers a query for the terminal name; a response with neither addresses nor a new alias ends with an empty selection. One deadline scope spans every connection and response. Each exchange closes before a follow-up begins, and the scope closes after the final result. Truncation, response/selection errors and transport failures remain typed; cleanup failure stops the lookup and retains the exchange report.

`Answer.selectWithHistory` returns immutable history of all traversed alias owners, including intermediate aliases within one packet. The unresolved terminal name is not marked as traversed, allowing its follow-up query. Revisiting an earlier alias owner then fails even across packet boundaries. The original `Answer.select` API remains available with fresh history. `Query.exchange` borrows an existing deadline signal and does not create or reset a timer; the standalone `Query.query` API retains its existing behavior.

`python3 tests/dns_address_lookup_check.py build/bend-dns-deadline-candidate` passes 156 local integration cases across A/AAAA requests, IPv4/IPv6 server sockets and native one/four threads/Bun. Coverage includes direct and in-packet answers, aliases split over responses, a 33-connection chain, a loop through an intermediate alias from an earlier packet, empty/alias-empty answers, response codes, truncation, pre-abort and argument guards. The deadline test delays the first alias response by 700 ms under a one-second total deadline, then requires the second connection to close before a fresh one-second timeout could expire. Peers verify exact requested names/types and closure before each follow-up, and reject extra connections. The original 900 address-selection cases and 90 standalone query cases also pass after the shared-function changes.

The [validation record](runtime-validation/2026-09-20-dns-address-lookup.json) captures inputs, hashes, query traces and guarded builds. The combined native fixture compiled in 43.40 seconds with 9,557,308 KiB sampled group peak RSS under a 16 GiB guard. No compiler/effect/installation changes were made. These are local network checks, not internal resource-table audits. OS resolver configuration, hosts files, ID entropy, server failover, retry/cache policy, family ordering and provider integration remain unfinished; upstream suite statuses are unchanged.

### Bounded OS entropy

An isolated additive `Entropy.bytes` effect now provides up to 256 OS-random bytes on Linux, with typed failures and no fallback generator. It uses a nonblocking OS request, rejects oversized counts before the syscall, and returns no partial output after a short read. Native one/four-thread and Bun checks pass 5,523 requests covering live output shape, all supported sizes, all byte values, syscall counts and injected errors. Existing C/JavaScript compilation controls remain byte-identical; 20-pair compilation measurements show no material timing change in the two tested workloads. The [implementation record](../patches/experimental/entropy/README.md) includes reproducible commands and the [raw validation](runtime-validation/2026-09-20-entropy.json). No compiler/scheduler code or installation changed. Darwin support and resolver-default integration remain implementation work; existing DNS APIs still take caller-supplied IDs. Upstream suite statuses are unchanged.

### DNS ID sources and OS-random lookup

`dns-id.bend` decodes two octets into the full 16-bit ID range and preserves OS source errors. `dns-address-lookup.lookupRandom` invokes this source independently before every exchange, including alias follow-ups. IDs may repeat legitimately; the matcher still verifies the observed route, ID and question. The fixed-ID `lookup` API remains available. `lookupWith` takes a static source function and a borrowed Data environment, allowing stateful deterministic sources without introducing a callback registry or transferring ownership of their resources.

Unsupported query types and zero read sizes reject before the source runs. An already retained deadline abort also skips the source; the existing connect operation checks again afterward. A source failure returns a typed `Identifier` error and closes the shared deadline scope, without starting another exchange. User-supplied sources must return promptly: the deadline cannot force-cancel arbitrary injected effects. Parent cancellation is asynchronously forwarded to the deadline signal, so cancellation during a source call may briefly permit a connect or partial send before the child observes it. Tests require the retained cancellation reason, closure of any such connection and no additional exchange, rather than asserting an instantaneous parent-to-child handoff.

`python3 tests/dns_lookup_ids_check.py build/bend-dns-entropy-candidate` passes 180 local integration cases: injected and real OS sources, IPv4/IPv6 servers, A/AAAA requests, and native one/four threads/Bun. The injected sequence verifies exact IDs 0, 65535 and 4660 on the wire and one draw per exchange, including after ignored replies. Other cases cover failures before the first exchange and after an alias response, pre-abort, argument guards, cancellation during a draw and the original end-to-end deadline. Peers observe closure before a follow-up and reject extra connections. These are local socket/lifetime checks, not an internal resource-table audit or a statistical entropy test. `tests/dns_id_check.py` passes 2,544 cases per backend covering every octet in both positions, boundaries, invalid shapes/ranges and retained source errors. All 156 original fixed-ID lookup cases still pass.

The [validation record](runtime-validation/2026-09-20-dns-lookup-ids.json) retains all three test results and guarded builds. The combined candidate adds the unchanged entropy effect to the existing DNS/deadline candidate; no compiler, scheduler or installed toolchain changed. DNS configuration, hosts files, server failover, retry/cache policy, family ordering, Darwin entropy and provider integration remain unfinished. Upstream suite statuses are unchanged.

### Resolver configuration syntax

`resolver-address.bend` reuses native IPv4 arithmetic and IPv6 parsing with configuration-specific boundaries. It accepts libc-style decimal/octal/hex and abbreviated IPv4 tokens but rejects URL-only trailing-dot and empty-hex allowances. IPv6 zone text is preserved separately from the address; an OS adapter must still resolve or validate it. No URL percent decoding, bracket removal or Unicode host normalization occurs. The configuration grammar and address rules were checked against [glibc's resolver initializer](https://raw.githubusercontent.com/bminor/glibc/master/resolv/res_init.c) and [IPv4 parser](https://raw.githubusercontent.com/bminor/glibc/master/resolv/inet_addr.c).

`resolver-config.bend` returns immutable typed entries in file order. It recognizes column-zero keywords separated by spaces/tabs, full-line comments, and nameserver/domain first-token rules. Search domains, option tokens and sort-list tokens remain explicit lists. Unknown lines and invalid server tokens remain available for diagnostics or later policy; repeated directives are preserved rather than prematurely collapsed. It does not claim to apply resolver configuration. File decoding/loading, defaults, environment overrides, scope resolution, numeric option policy, server limits, search planning and wiring into lookups remain unfinished. These layers must be completed before replacing system name resolution.

`tests/resolver_address_check.py` passes 5,029 tokens on native one/four threads and Bun against the host Linux libc `__inet_aton_exact` and `inet_pton`, including every supported numeric family, boundary/overflow syntax, embedded IPv4, long inputs and unresolved scopes. `tests/resolver_config_check.py` passes 413 source documents per backend against an independent syntax model, including repeated/unknown directives, invalid servers, comments, whitespace/CRLF behavior, Unicode preservation and large lists/documents. This is parser validation, not system resolver equivalence or interpretation of option semantics. The [validation record](runtime-validation/2026-09-20-resolver-config.json) includes hashes and guarded build observations. No compiler/runtime patch was made; upstream suite statuses are unchanged.

### Resolver source precedence

`resolver-settings.bend` now folds the configuration syntax into immutable settings. Valid nameservers retain declaration order and duplicates; if none remain, the server list defaults to 127.0.0.1. The final file domain/search directive replaces earlier search lists. An explicit typed search override takes precedence over every file directive, including when its supplied list is empty. Without either source, the hostname suffix after its first dot supplies the local search domain. Ordered file option tokens precede override option tokens; sort-list tokens accumulate independently. Invalid server tokens and unknown lines remain ordered diagnostics.

This API accepts decoded values, not raw `LOCALDOMAIN`/`RES_OPTIONS` strings. Environment-string decoding, OS hostname/file access, option/sort interpretation, server limits, IPv6 scope resolution and network application remain separate unfinished layers. The source-precedence rules follow the relevant paths in [glibc's resolver initializer](https://raw.githubusercontent.com/bminor/glibc/master/resolv/res_init.c); preserving pending tokens does not imply their semantics are implemented.

`python3 tests/resolver_settings_check.py` passes 247 cases on native one/four threads and Bun against an independent model, including empty/missing typed overrides, default/local hostname domains, replacing file search directives, invalid-only server lists, duplicate and long server lists, file/override option ordering and a 3,000-entry stress configuration. The [validation record](runtime-validation/2026-09-20-resolver-settings.json) retains hashes and guarded builds. The fixture emitted C in 15.53 seconds at 5,920,060 KiB sampled group peak RSS under an 8 GiB guard. No compiler/runtime patch was made; upstream suite statuses are unchanged.

### Typed resolver options

`resolver-options.bend` represents ndots, timeout seconds and attempts as native numeric fields and resolver features as a duplicate-free list of typed variants. Defaults are 1, 5 and 2; documented caps are 15, 30 and 5. Repeated numeric options replace earlier values and repeated flags are idempotent. Supported features cover rotation, EDNS, single-request/reopen, no-TLD querying (both spellings), no-reload, TCP, AD trust and no-AAAA. These values describe policy; transport behavior for each feature is still implementation work.

The explicitly named strict parser checks complete unsigned decimal tokens and exact feature names, validates all digits after reaching a cap, and cannot wrap on large input. Malformed numeric tokens and unknown options remain ordered diagnostics in a report with partial values. No network entry point consumes these reports. Whether to ignore diagnostics, fail configuration or retain libc's permissive interpretation is a [pending behavior decision](native-bend.md#pending-malformed-resolver-option-policy), not an approved change to pi behavior.

`python3 tests/resolver_options_check.py` passes 226 sequence comparisons against the host glibc 2.39 `res_ninit` on native one/four threads and Bun. The test-only C observer sends no queries and exposes only the relevant option fields. Comparisons seed Bend with supported host flags and reset numeric fields, so they do not assume an empty host configuration. A separate 23-case group checks strict diagnostics, native defaults, repeated settings, long decimals and an 8,000-token sequence against an independent model. It also records the observed libc quirks underlying the pending choice. The [validation record](runtime-validation/2026-09-20-resolver-options.json) retains results, hashes and guarded builds. No compiler/runtime patch was needed; option integration and all upstream suite statuses remain unchanged.

### DNS presentation names

`dns-presentation.bend` converts text into immutable binary DNS labels and an explicit absolute-name flag. An unescaped final dot marks an absolute name; escaped dots remain label data. Three-digit decimal escapes cover all byte values, including NUL. The parser rejects incomplete/out-of-range escapes, empty interior labels, labels over 63 octets and wire names over 255 octets. It validates raw-byte input and rejects literal NUL rather than adopting C-string truncation. Unicode text is UTF-8 encoded before parsing; IDNA, hostname acceptance and search expansion remain separate layers.

`tests/dns_presentation_check.py` passes 2,198 cases per backend against Linux glibc `ns_name_pton`, comparing both qualification status and complete wire bytes. Cases include all decimal escapes 000–999, escaped characters, binary NUL, Unicode byte lengths, boundary-sized labels/names and a 100,000-character rejected name. Three additional byte-boundary checks run on native one/four threads and Bun. The syntax oracle's implementation is [available in glibc](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/ns_name_pton.c); it performs no DNS queries.

The long-input case exposed a stack overflow in the shared UTF-16 normalizer's use of Base `String.join`, investigated below in the Bend issue log. A native reversed-character accumulator replaces that join without changing normalization semantics. All 80,002 existing UTF-8 vectors pass on native one/four threads. Direct normalization checks pass 810 cases on native and 414 constructible scalar cases on Bun, plus a 100,000-scalar length/digest check on each backend. Raw-surrogate constructor cases remain native-only because of the separately documented BEND-021 inconsistency; this work does not resolve it. The [validation record](runtime-validation/2026-09-20-dns-presentation.json) includes hashes, builds and the regression scope. No compiler/runtime installation changed, and upstream suite statuses remain unchanged.

### DNS search candidate cursor

`dns-search.bend` yields immutable candidates with their origin (initial bare name, indexed search suffix, or final bare name) and a next cursor. It respects explicit ndots/default-name/search/no-TLD policy, absolute-name termination, the single-suffix case when full searching is disabled, duplicate suffixes, root suffixes and final-attempt suppression. `stopSearch` discards unvisited suffixes without pretending a later root suffix was tried. Every yielded name retains typed encoding failure rather than silently dropping an overlong or invalid candidate. Concatenation uses native label lists and tail accumulators.

The caller supplies textual dot count and absolute status explicitly: a binary label may itself contain a literal escaped dot, so label count cannot reconstruct every source-text policy. Host-alias lookup, deriving those text facts at the public resolver boundary, interpreting response failures, retaining final error precedence and executing candidates remain separate integration work. This cursor alone does not implement complete resolver search behavior.

`tests/dns_search_check.py` passes 678 cursor cases per backend, crossing policy flags, thresholds, absolute/relative names, root/duplicate suffixes, early stopping, invalid/overlong composition and a 1,000-suffix sequence. A test-only libc client then points its resolver state exclusively at a loopback UDP server that returns NXDOMAIN; 192 sequences compare the actual queried wire names with the candidate order. This covers ordinary names and escaped interior dots, but not arbitrary response-error policies. The ordering rules are grounded in [glibc's search implementation](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_query.c). The [validation record](runtime-validation/2026-09-20-dns-search.json) retains query traces, hashes and guarded builds. No compiler/runtime patch was made; upstream suite statuses are unchanged.

### DNS search response policy

`dns-search-response.bend` now reduces classified candidate failures into either a continuation cursor/history or an immediate resolver error. Missing names, empty answers and server failures can continue suffix searching. Other temporary/unrecoverable failures skip remaining suffixes while preserving a permitted final bare-name attempt. Connection refusal during suffix search terminates immediately as a temporary resolver error; it is distinct from a DNS REFUSED response. Initial bare-name errors are retained for final precedence, followed by suffix no-data, suffix server failure, then the last error. Successful answers finish in the enclosing driver without applying failure precedence.

The module is pure Bend and takes explicit cause variants rather than inspecting errno or packet objects. Packet/transport classification and connection to actual DNS exchanges remain integration work; the generic execution driver below provides a separate typed path for non-DNS failures. In particular, abort/entropy/cleanup failures must not be converted into ordinary searchable name failures. The policy follows the control flow in [glibc's search implementation](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_query.c).

`tests/dns_search_response_check.py` passes 2,401 traces on native one/four threads and Bun, covering every three-outcome sequence over success and six failure causes in seven search contexts. It compares candidate order, early termination and final errors with an independent model. An additional 108 loopback sequences compare actual libc query order and final success/error results under mixed NXDOMAIN, empty NOERROR and positive A replies. Other classified errors are covered by the model, not claimed as packet-level comparisons. The [validation record](runtime-validation/2026-09-20-dns-search-response.json) retains inputs, traces, source hashes and guarded builds. No compiler/runtime patch was made; upstream suite statuses remain unchanged.

### Asynchronous DNS search driver

`dns-search-run.bend` connects the immutable candidate cursor and response policy through a statically injected asynchronous Bend request function and borrowed environment. It returns the exact successful value immediately, applies search precedence only to classified DNS failures, and preserves a typed terminal failure without further requests. Invalid/overlong candidates never invoke the callback. The callback owns each exchange until returning; the driver does not dispose the caller's environment or impose an arbitrary retry limit.

`tests/dns_search_run_check.py` passes 2,588 cases each on native one/four threads and Bun against an independent model. Requests suspend for one millisecond, then supply scripted answers, DNS causes or typed halts. Tests cover candidate order, exact values, request counts, final error precedence, invalid candidates, and caller disposal of the environment after completion. Another 187 cases on each native thread configuration run with disposable exit instrumentation and leave zero live channels. This is driver validation with injected requests, not a network integration test. Packet/transport classification and wiring to address exchanges are still pending. The [validation record](runtime-validation/2026-09-20-dns-search-run.json) retains source hashes and guarded build measurements. No compiler/runtime patch was made; upstream suite statuses remain unchanged.

### Address lookups under a caller-owned deadline

`dns-address-lookup.exchangeWith` runs one name and its alias follow-ups under a borrowed deadline signal, without creating or retiring a timer. Record type and read size are validated before entropy/network effects. Each invocation starts fresh alias history, while the caller can reuse the scope and ID source across search candidates. The existing scope-owning `lookupWith` entry point delegates to this path and still closes its scope after completion.

`tests/dns_lookup_shared_check.py` passes 96 loopback cases across native one/four threads, Bun, IPv4/IPv6 and A/AAAA. Two sequential calls exercise exact answers and IDs, pre-abort, invalid type/read size, source failures on the first or second call, expiry between calls, and expiry during the second call. Expiry between calls prevents a second entropy draw or connection. The during-call case delays the first answer and verifies that the second connection closes within the remaining deadline rather than a restarted timeout. Every accepted exchange closes, and the caller disposes the shared scope and source afterward. The existing 180 injected/OS-random ID cases also pass after rebuilding, including alias follow-ups, entropy failure and cancellation. These are network/lifecycle checks, not internal resource-table audits.

The [validation record](runtime-validation/2026-09-20-dns-lookup-shared.json) retains case outputs, source hashes and guarded builds. It uses the existing isolated entropy/socket/connection/timer candidate; no compiler or effect patch was changed or installed. Packet/transport classification and the complete search-to-exchange adapter remain integration work. Upstream suite statuses remain unchanged.

### Single-server DNS search over TCP

`dns-address-search.bend` composes the search cursor/driver with address lookup against one caller-selected numeric recursive server. One owned deadline spans all candidate names and alias follow-ups; each exchange closes before the next begins. Static source injection preserves the borrowed source environment, and `searchRandom` supplies OS entropy through the existing ID primitive. Type/read-size validation occurs before creating the scope or consuming entropy.

NXDOMAIN maps to missing name, SERVFAIL to server failure, other nonzero basic response codes to unrecoverable DNS failure, and successful empty address selections to no data. These classes participate in search continuation and final precedence. Successful selections retain their canonical name, addresses and TTLs. The response-code mapping follows [glibc's query implementation](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_query.c); address selection remains a separate Bend layer. DNS REFUSED is not a refused TCP connection.

Non-DNS lookup errors currently stop this single-server operation with their original typed details. In particular, earlier missing-name/no-data responses cannot mask entropy failure, cancellation, expiry or cleanup failure. This is not yet complete resolver behavior: server retry/failover and transport-error classification must be implemented before the public OS-backed resolver is complete. The module does not claim UDP, hosts files, OS configuration, family selection or HTTP integration.

`tests/dns_address_search_check.py` passes 384 cases with actual loopback TCP packets on native one/four threads and Bun, over IPv4/IPv6 with A/AAAA requests. Cases cover all 16 basic response codes, positive and empty answers, search precedence, suffix skipping after REFUSED/FORMERR, root-suffix suppression, aliases within search, alias failure followed by the next suffix, source failure, pre-abort, validation, truncation and a deadline shared across candidate exchanges. Expected query names, wire IDs, final values/errors, source draw counts and connection closure are checked. The [validation record](runtime-validation/2026-09-20-dns-address-search.json) retains results, hashes and guarded builds. This uses the existing isolated primitive candidate without modifying/installing compiler or runtime patches. Upstream suite statuses remain unchanged.

### DNS transport classification

The single-server search completion adapter now distinguishes connection refusal from other connection/read/write socket errors, EOF and incomplete TCP frames. Refusal enters the existing `ConnectionRefused` search policy; other covered network failures enter `TemporaryFailure`. In suffix search, refusal ends immediately, whereas other temporary failures skip unvisited suffixes and retain a permitted final bare-name attempt. Earlier direct-name failure precedence remains intact. The send/query failure classification follows the negative-send-result path in [glibc's query implementation](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_query.c).

Cancellation and expiry have distinct typed variants and bypass errno classification. Entropy, cleanup, validation, endpoint/adoption and other unclassified errors also retain their typed terminal details. A cleanup error wrapping a network failure is not unwrapped into a searchable failure. Server retry/failover is still unfinished; this change only classifies completed attempts for search.

A minimal additive `Socket.isConnectionRefused` primitive keeps platform errno constants out of pure Bend policy. Native code compares against `ECONNREFUSED`; Bun uses the supported Linux/Darwin platform constants. The existing compiler, scheduler and effects remain unchanged, and no candidate is installed. See the [primitive implementation record](../patches/experimental/socket-refused/README.md).

The primitive passes 514 errno cases on native one/four threads and Bun; unrelated C/JS emission matches the previous candidate byte for byte. The expanded network checker passes 468 actual loopback cases across all three backend configurations and IPv4/IPv6 A/AAAA. It includes held-bound, non-listening ports for refusal, EOF, partial-prefix close and TCP reset, together with every previous response-code, alias, deadline, source-failure and validation case. Exact request counts distinguish immediate suffix termination from final-name continuation. These are network/lifecycle checks, not internal resource-table audits. The [validation record](runtime-validation/2026-09-20-dns-transport-classification.json) retains results and compile controls. Upstream suite statuses remain unchanged.

### Ordered TCP server driver

`dns-tcp-servers.bend` executes a supplied server list in order under a borrowed deadline, preserving duplicate entries. The exact query/ID is reused across the pass. Every exchange closes before a subsequent attempt starts. A reply, including a DNS error code or truncation, returns immediately. Covered network failures can advance; abort, validation, adoption, local-state and cleanup failures stop with the original typed report. An empty list returns `NoServers`, and exhaustion retains the last failed report rather than manufacturing a new error.

The outer TCP pass follows [glibc's transport implementation](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_send.c): TCP visits the server order once, and response codes are not handled with UDP-style server failover. This is an independently tested driver, not complete TCP resolver parity or an integrated public resolver. In particular, glibc reconnects once on ECONNRESET while reading a frame length, but not on payload-read resets. The current error types lose that phase; preserving phase and adding per-server reconnect recovery are required before address lookup/search adopts this driver. Local socket-creation failures also need to remain distinguishable from connect failures for accurate terminal/retry policy. Rotation, UDP rounds and higher-level integration remain pending.

`tests/dns_tcp_servers_check.py` passes 360 loopback cases across native one/four threads, Bun, IPv4/IPv6 and A/AAAA. It checks next/third-server success, final failure selection, duplicate server entries, refusal, EOF, partial frames, the driver's current reset-to-next behavior, all 16 basic DNS codes, truncation, pre-abort, invalid read size, empty server lists and one deadline shared across servers. Query bytes, IDs, accepted-server order, unused listeners and connection closure are checked. The reset-to-next case validates this lower-level driver only; it is not presented as the reference's per-server reconnect behavior. The [validation record](runtime-validation/2026-09-20-dns-tcp-servers.json) retains case outputs, hashes and guarded builds. No primitive/compiler changes or installations were made. Upstream suite statuses remain unchanged.

### DNS TCP read phase and reset recovery

`dns-tcp-connection.Transport` now retains `Sending`, `ReadingLength` or `ReadingPayload` alongside the original socket error. Both states of the two-byte length prefix map to `ReadingLength`; receiving a complete prefix advances to payload. Repeated terminal reads preserve the operation and error. Existing consumers have been updated for the additional field without changing their error classification.

`dns-tcp-recover.bend` wraps one closed query exchange and permits exactly one reconnect after a length-read ECONNRESET with successful cleanup. The second exchange returns unchanged, so repeated resets cannot loop. Writes, payload reads, aborts, expiry, cleanup failure and non-reset socket errors do not trigger recovery. The original query/ID and borrowed deadline span both connections. `dns-tcp-servers` now uses this per-server recovery layer before moving to another server, matching the length-read reset path in [glibc's TCP transport](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_send.c).

The expanded failover checker passes 420 loopback cases across native one/four threads and Bun, including recovery on the same server, two-reset exhaustion followed by the next server, partial-prefix resets, payload resets without reconnect, a reset while starting another frame after an ignored reply, and deadline expiry during recovery. The previous reset-to-next expectation now supplies two resets to exhaust the permitted recovery. Existing framing checks, all 48 session cases and all 468 single-server search cases pass after rebuilding with the expanded error type. Focused policy cases also cover cleanup overriding an otherwise retryable reset, write/connect resets and abort variants. The [validation record](runtime-validation/2026-09-20-dns-tcp-recovery.json) retains results, source hashes and guarded builds.

No compiler/effect patch or installation changed; the implementation uses the existing reset-classification primitive. Server-driver integration into address lookup/search, socket setup/connect failure distinctions, rotation and UDP remain unfinished. Upstream suite statuses are unchanged.

### Address lookup with server failover

`dns-address-lookup` now routes questions through the ordered TCP server driver and its per-server reset recovery. New `lookupServersWith`/`lookupServersRandom` entry points own one deadline for the supplied list, and `exchangeServersWith` borrows a caller-owned deadline. Existing `lookup`, `lookupWith`, `lookupRandom` and `exchangeWith` signatures remain available through singleton lists. Empty server lists are rejected with `NoServers` before entropy or deadline creation; type/read-size validation remains explicit.

One source draw belongs to one DNS question. Failover and reset recovery reuse that ID; each alias follow-up draws a fresh ID and restarts the supplied server order. Alias history persists across packets and servers, so a cycle cannot evade detection by changing servers. Successful canonical names, address order and TTLs flow through the same selector. The final failed exchange is converted through the existing lookup error/cleanup handling without replacing typed details. Single-server search now inherits this reset recovery, although a public multi-server search interface is still pending.

`tests/dns_address_servers_check.py` passes 228 real loopback cases over native one/four threads, Bun, IPv4/IPv6 and A/AAAA. They cover failover, refusal, aliases after failover, reset recovery followed by alias failover, cycles, DNS errors, empty answers, final transport errors, empty server lists, entropy failures, pre-abort, validation, duplicate servers, truncation and deadlines across both servers and aliases. Queries, IDs, entropy draw counts, selected values and connection closure are checked. The single-server search reset case now uses two resets to exhaust the recovery budget before moving to the final name, and a new case verifies successful same-name recovery with no additional source draw.

All 180 injected/OS-random ID regressions, 480 expanded single-server search cases and 96 shared-deadline cases pass after rebuilding. The [validation record](runtime-validation/2026-09-20-dns-address-servers.json) retains evidence and guarded builds. No compiler/effect patch or installation changed. Socket setup/connect error-stage distinctions, rotation, OS configuration, UDP and HTTP integration remain unfinished. Upstream suite statuses are unchanged.

### Multi-server DNS search

`dns-address-search.searchServersWith` accepts a supplied ordered server list and injectable ID source; `searchServersRandom` uses OS entropy. The request context now stores the server list and calls the borrowed-deadline address-lookup API. One owned scope therefore spans suffix candidates, aliases, server failover and same-server reset recovery. The existing `searchWith` and `searchRandom` signatures delegate through singleton lists. Validation rejects empty lists before scope creation or source calls.

The existing pure search cursor and error-precedence reducer remain unchanged. Each new candidate or alias question restarts the supplied server order and draws a new ID; attempts for that question reuse it. DNS error replies still return through TCP query policy rather than introducing UDP response-code retries. Classified search failures and typed terminal failures retain their established handling.

`tests/dns_search_servers_check.py` passes 264 real loopback cases across native one/four threads, Bun, IPv4/IPv6 and A/AAAA. Cases combine suffix failover, aliases after failover, reset recovery followed by another suffix, DNS REFUSED versus exhausted connection refusal, final-name fallback, no-data/initial-error precedence, source failure after an earlier DNS error, pre-abort, validation, duplicate servers, alias cycles, absolute names, invalid candidates and deadlines during suffix failover or alias reset recovery. The peer verifies exact questions/IDs, expected server order, unused listeners and connection closure. All 480 existing single-server search cases also pass after rebuilding. The [validation record](runtime-validation/2026-09-20-dns-search-servers.json) stores shared case definitions once and references them from backend execution traces.

No compiler/effect patch or installation changed. This completes the explicit-server-list TCP search composition, not the overall resolver or pi port. Socket setup/connect error-stage distinctions, rotation/configuration, hosts files, family ordering, UDP and HTTP integration remain unfinished. Upstream suite statuses remain unchanged.

### Immutable DNS server rotation

`dns-server-rotation.bend` represents the original server order and the next rotated order as immutable lists. Each enabled `choose` returns the current order and advances one position; disabled choices return the original order and retain the current position. Empty/singleton lists stay structurally unchanged, duplicates survive, and no global counter or integer-wrap behavior is imposed on callers. The generic state can carry actual server records. Initialization preserves the caller-supplied order; the owning resolver must serialize updates when sharing state.

`tests/dns_server_rotation_check.py` passes 1,795 sequences per backend (native one/four threads and Bun), checking all eight-step enable/disable combinations across seven lists, duplicate/extreme U32 values, empty input, a 16,384-entry list and 10,002 successive selections. Complete returned orders are compared with an independent model. A test-only libc client then sends 216 real loopback TCP queries across nine sequences with one, two or three servers. The observed random initial server aligns the Bend state; all subsequent enabled/disabled choices match. This checks cyclic selection and non-advancement while disabled, not the random initializer or process-global concurrency behavior. The control-flow reference is [glibc's nameserver offset implementation](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_send.c).

The [validation record](runtime-validation/2026-09-20-dns-server-rotation.json) retains compiler/source hashes, backend checks and libc traces. Production code is pure Bend; the C program is only an oracle. No compiler/effect patch or installation changed. Integrating this state at each new question, random initialization, reload policy and concurrent resolver ownership remain unfinished; the live resolver still uses the supplied fixed order. Upstream suite statuses remain unchanged.

### Owned DNS server selection

`dns-server-source.bend` owns a rotation state in a Bend `Ref`. Its borrowed `Source` is either a fixed list or a rotating selector; concurrent choices serialize pure state transitions without a background worker. The owner closes only after all borrowed calls finish. Disabled rotation retains its next position and returns the configured order. Initialization currently uses the caller's order.

Lookup and search now accept borrowed sources. Every new question obtains its ID, checks cancellation again, then selects an order once. Failover and reset recovery reuse that order and ID; alias and search follow-ups select anew. Entropy failure, pre-observed cancellation and input validation do not consume a selection. Cancellation can still race a later selection or connection; this is not a claim of atomic cancellation across all effects. Existing single-server and list APIs wrap fixed sources.

The new tests pass 228 live rotating-search cases and 210 concurrent-selector cases across native one/four threads and Bun. Search tests verify exact questions, IDs and server order through aliases, suffixes, retries, wraparound, deadlines, disabled rotation and typed failures. Concurrent tests compare complete order multisets and retained next positions, without imposing scheduling order; disposable native instrumentation reports zero live channels after selector/task teardown. This audit covers the standalone selector, not every network resource.

Random initial selection, configuration reload, OS resolver ownership, socket setup/connect distinctions, hosts files, family ordering, UDP and HTTP integration remain implementation work. No compiler/effect patch or installation changed; upstream suite statuses remain unchanged.

Rebuilt regressions pass all 264 fixed-list multi-server search, 480 single-server search, 180 injected/OS-ID and 96 shared-deadline cases. The [validation record](runtime-validation/2026-09-20-dns-server-selection.json) retains new traces, regression counts/hashes, build provenance and failed memory-guard attempts. The single-server fixture required the instrumented baseline compiler run described in [BEND-001](bend-issues.md); its output is byte-identical to the isolated cache experiment. No experimental compiler change was installed or needed in the tested application binary.

### Initial DNS rotation position

`dns-server-rotation.startAt` and `dns-server-source.createAt` accept an initial offset independently of the configured order. Offsets wrap by server count, empty input remains empty, and disabled choices return the configured list without consuming the retained position. Initialization counts and splits with tail recursion and joins the suffix/prefix once; it does not repeatedly rotate the entire list. Existing default constructors retain their original behavior.

`tests/dns_server_position_check.py` passes 2,563 sequences per backend (native one/four threads and Bun), checking both pure state and owned selection. Coverage includes every six-step enable/disable sequence over five lists and eight offsets, duplicate servers, maximum-U32 offsets, 8,192 servers, 10,002 successive selections and empty flag input. Native owner teardown leaves zero live channels. Existing rotation comparisons and concurrent-selector tests also pass. The [validation record](runtime-validation/2026-09-20-dns-server-position.json) retains hashes, builds and scope. This adds explicit initial positioning; OS-random seed selection, configuration/reload ownership and the rest of the resolver remain unfinished. No compiler/runtime patch or upstream-suite status changed.

### OS-random DNS selector initialization

`random-index.bend` implements validated four-octet decoding and rejection sampling in pure Bend over the existing OS entropy primitive. It accepts bounds 1 through 2^32, rejects invalid bounds before invoking the source, and skips entropy for the single possible index. The accepted word interval consists of complete equal-sized residue buckets, avoiding modulo bias. Rejection retries without a fallback seed; source failures remain typed. The generic static source/environment API stays caller-owned, and arbitrary injected IO is not force-cancelled.

`dns-server-initialize.random` and `createWith` create the existing owned selector only after sampling succeeds. Empty/singleton lists require no entropy. Initial position affects enabled rotation; disabled choices still return configured order. The full configuration/reload owner and OS resolver facade remain unimplemented. No compiler/effect patch or installation changed; these entry points require the existing isolated entropy candidate.

`tests/dns_server_initialize_check.py` passes 2,925 index comparisons, 146 byte-decoding checks, 12 scripted initializations, five direct sampler checks and 20 OS-random initializations on each of native one/four threads and Bun (9,324 checks total). Cases include bucket boundaries, bounds near/full 2^32, malformed bytes, source failure after rejection, 10,002 successive rejections followed by acceptance, empty/singleton no-draw behavior and preservation of configured/rotated orders. Native teardown reports zero live channels in every initializer run. Live entropy tests assert valid output and rotation consistency, not statistical quality or uniqueness. The [validation record](runtime-validation/2026-09-20-dns-server-initialize.json) retains source/compiler hashes and guarded builds. Upstream suite statuses remain unchanged.

### Reusable explicit TCP resolver owner

`dns-tcp-resolver.bend` combines resolved numeric servers, parsed search domains, search policy, time/read limits and rotation into an owned configuration. Borrowed resolvers share its selector while retaining immutable configuration values. Each lookup owns a fresh deadline across that call's suffix, alias and server attempts; server rotation persists across calls. Default initialization and question IDs use OS entropy, with static injected sources available separately for both. Disabled rotation does not invoke the initialization source.

Replacement is explicit and requires all borrowed operations to finish first. Construction failure returns `Unchanged` with the still-usable previous owner and its retained rotation position. Success constructs the replacement, retires the old selector, and returns `Updated`. There is no implicit disposal of still-running operations, automatic reload or lease tracking.

All 120 live reuse scenarios pass across native one/four threads and Bun, covering two calls per scenario, rotation/fixed order, suffix and alias questions, failover, reset recovery, absolute names, unsupported types, empty servers and expiry followed by a successful call with a fresh deadline. Exact questions, IDs, selected servers and exchange closure are checked. The owner fixture additionally verifies failed and successful replacement, fixed-order replacement without entropy, and zero live native channels after teardown. The [validation record](runtime-validation/2026-09-20-dns-tcp-resolver.json) retains traces, build provenance and hashes.

OS configuration loading, IPv6 interface resolution, configuration diagnostic policy, automatic reload, hosts files, UDP, family ordering and HTTP integration remain unfinished. No compiler/effect patch or installation changed; upstream suite statuses are unchanged.

### Owned file folding for configuration input

`file-fold.bend` builds a reusable file-reading layer in pure Bend over existing `File.open`, `read_bytes` and `close` primitives. Its pure static consumer returns an updated affine or copyable state plus a continue/stop decision. EOF and early stop are distinct, source errors retain open/read stage and primitive details, and state survives every return. The reader closes the file before returning; invalid zero read size fails before opening. Short nonempty reads continue normally. This supplies the input lifetime needed by configuration loading without selecting text-decoding, diagnostic or fallback policy.

`tests/file_fold_check.py` passes 365 file cases and one live-pipe early-stop case per backend (native one/four threads and Bun), 1,098 checks total. It compares complete binary contents across chunk boundaries, preserved state markers, EOF versus stop, missing-file/open and directory/read failures, zero-size validation, and repeated early closes. The pipe writer remains open until after the reader returns, verifying that early stop does not wait for EOF. Disposable native instrumentation scans descriptors 3–511 for fixture-owned paths before process exit and finds none, including named pipes. The runtime's own wakeup pipes are excluded by ownership, not mistaken for leaked input files.

The [validation record](runtime-validation/2026-09-20-file-fold.json) retains source/compiler hashes and guarded builds. No compiler/runtime patch or installation changed. The existing close primitive returns no typed close error; this API also does not add cancellation/deadlines. OS resolver configuration loading and upstream suite completion remain pending.

### Bounded resolver file loading

`resolver-file.readWith` now reads a bounded configuration source through `file-fold`, calls an explicitly supplied decoder after EOF, and parses the resulting complete text. `loadWith` applies the existing settings precedence with typed caller overrides. Open/read errors retain their stage and primitive details; decoder errors remain generic typed values. Size overflow stops reading and closes the file. Neither failure nor overflow applies a parsed prefix. An exact-limit file is accepted, including the empty-file/zero-limit case; comments and newlines count toward the source-byte limit. This bounds retained input bytes, not arbitrary allocations inside a caller-provided decoder.

All 680 cases per backend pass on native one/four threads and Bun (2,040 total). The file fixture explicitly chooses replacement UTF-8 decoding with BOM preservation and separately tests a rejecting decoder; these are test-selected policies, not production defaults. Cases cover chunk boundaries inside multibyte input, incomplete/invalid UTF-8, exact/short limits, decoder versus file/overflow error precedence, missing files, directory read failure, zero chunk size, settings overrides, retained diagnostics and a larger repeated-directive file. Native descriptor auditing finds no fixture-owned file handles after each invocation. A separate native smoke check successfully loaded `/etc/resolv.conf`; its contents are not retained in the repository.

The [validation record](runtime-validation/2026-09-20-resolver-file.json) retains source hashes, fixtures and guarded builds. No compiler/runtime patch or installation changed. OS environment/hostname capture, missing-file policy, resolver option diagnostics, IPv6 zone resolution, automatic reload and live configuration conversion remain unfinished. Upstream suite statuses are unchanged.

### Resolver environment overrides and combined loading

`resolver-environment.bend` captures `LOCALDOMAIN` and `RES_OPTIONS` separately, keeping present-empty values distinct from absence. The platform boundary recognizes the existing Linux/JS primitive's `ENOENT` result; other failures preserve the variable, code and message. Injected getters share this normalization and are called once per variable in a defined order. `LOCALDOMAIN` terminates at newline and retains an initial root suffix for empty or initially blank input. This can affect bare-name versus suffix query order; it is represented as a native search-domain value. Option strings remain tokens rather than applying an unresolved numeric/unknown-option policy. The grammar reference is [glibc 2.39 resolver initialization](https://raw.githubusercontent.com/bminor/glibc/glibc-2.39/resolv/res_init.c).

`resolver-load.bend` now composes environment capture with the bounded file loader and settings precedence. Capture errors stop before file opening; file and decoder failures preserve their distinct types. Hostname and decoding remain explicit caller inputs. A caller with its own environment getter can supply the capture result to the same loading path. This is not an atomic process-environment snapshot or automatic reload.

Tests pass 360 environment/precedence cases and six injected getter/error-order cases per backend, plus 125 combined file/environment cases per backend: 1,473 Bend checks across native one/four threads and Bun. Eleven present-value search lists also match a test-only libc observer, including empty/root values, newline termination and retained non-delimiter control characters. The combined native tests leave no fixture-owned descriptors open. A real `/etc/resolv.conf` load with controlled child-process overrides verified both domain and option precedence; host configuration contents are not retained. The [validation record](runtime-validation/2026-09-20-resolver-environment.json) contains source hashes, builds and reference traces.

No compiler/runtime patch or installation changed. Hostname discovery, missing-file fallback, option diagnostic decisions, IPv6 interface resolution, live resolver conversion and automatic reload remain unfinished. Upstream suite statuses are unchanged.
