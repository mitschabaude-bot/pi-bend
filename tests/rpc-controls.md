RPC settings and user-bash checks are pinned to pi-mono v0.87.1 by SHA-256 in `rpc_controls_check.py` and `rpc_controls_fixture_check.py`. Build the CLI and fixture with `BEND=/home/agent/code/pi-bend/build/bend-system-identity/bend2/main.ts BEND_TUS=8 PI_BEND_OPT=-O0 sh scripts/build-pure.sh`, then run the native one/four-thread protocol checks and Bun fixture check. The native checks keep stdin open while a command runs, cancel it through `abort_bash`, and verify exactly one bash message in live context and persisted entries.

The Bun fixture exercises the RPC work queue and live settings without stdin. Bun's current process effect reports `spawn: Function not implemented (38)`, so shell execution is verified on native. For a Bun CLI reproducer, start `bun build/rpc-controls-cli.js --mode rpc --no-session --no-tools`, write a bash JSONL request, and leave stdin open: `main.bend` waits in synchronous `File.read_bytes(/dev/stdin)`, preventing its forked work task from advancing until EOF. Native runs the task concurrently. Live Bun CLI execution needs an asynchronous stdin reader; it is outside this RPC/session slice.
