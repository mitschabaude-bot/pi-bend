# Upstream test parity

The behavioral target is the pinned pi-mono revision recorded in `upstream-inventory.json`. Port the upstream assertions, fixtures, failure cases and regression scenarios with each component. Additional local smoke tests and successful live model calls do not establish parity.

The inventory includes every discovered upstream test/spec source, including packages whose applicability still needs review. `pending` means unported, `partial` means some assertions have native equivalents, and `ported` requires every applicable assertion to pass. A suite may be excluded only with a written scope reason. JavaScript extension loading is excluded by the user's request, but extension hooks, lifecycle and behavior remain requirements for Bend extensions.

Check the pinned source and inventory with `python3 scripts/test-inventory.py`. Changes to upstream source hashes require review. Never change expectations solely to make the Bend implementation pass.

`truncate_differential.py` compares native results with upstream code through a test-only TypeScript oracle. This is supplemental coverage, not a substitute for porting the named upstream tests. Production code does not import or execute TypeScript.

The truncation suite exposed isolated UTF-16 surrogate handling, which is now preserved by JSON and replaced with U+FFFD when a partial UTF-8 tail is decoded. All nine upstream truncation tests have native equivalents, including the exhaustive and seeded fuzz cases. The grep line helper counts UTF-16 units. Terminal tests still require a virtual-terminal harness and native editor/rendering implementation; API-specific Unicode sanitization remains a separate provider requirement.

The five generic event-stream cases are ported in `packages/ai/test/event-stream.bend`, retaining their names and assertions. `sh tests/event-stream.sh` runs them on one and four runtime threads, alongside four supplementary lifecycle and explicit resource-handle regressions. Exact integer fixtures use U32 as the generic event type, and the string-result fixture remains a separate instantiation. This test suite does not cover all async-generator protocol methods or JavaScript scheduler behavior; those module gaps remain explicit in `packages/ai/README.md`.

All nine system-message-replay cases are ported, including declaration projection from an executable tool. `sh tests/transcript.sh` runs the nine original cases plus native text and transport-tool edge cases. `python3 tests/record_vectors.py` checks native persistent dictionaries. Replay fixtures use plain JSON Schema values. Dynamic-object/identity supplemental tests were retired during the native Bend migration; see `docs/native-bend.md`. Full schema builders and validation remain unported.

The state-change case is implemented in `packages/ai/test/tool-state.bend` and invoked by the replay test entry point. It preserves both original assertions: changed definitions produce an ordered removal/addition, and identical states produce empty changes. Its empty-object schema fixture matches the pinned TypeBox output; it does not claim builder/validator parity. `python3 tests/tool_state_vectors.py` directly compares 72 state pairs with upstream, including the original inputs and duplicate-definition regressions.

The combined history case preserves the original transcript, additive and redeclared-message assertions. `python3 tests/tool_history_vectors.py` additionally compares both history predicates directly with upstream for 55 cases on one and four threads. JS-only invalid-schema/reflection fixtures are retired. Native schemas are immutable data, and structural equality is tested independently.

The executable declaration case uses `agent.AgentTool` with typed reusable callbacks and an explicit base-tool projection, preserving the three original equality assertions. An additional counter check ensures projection never invokes preparation or execution. The nine-case suite passes on one and four threads. TypeBox static relationships, schema validation and agent execution lifecycle remain open independently of this suite.

The pi-ai max-thinking suite is partial: `packages/ai/test/model-thinking.bend` preserves the two standalone model-metadata cases and their original fields/assertions. Catalog lookups and Codex payload checks remain unported. `python3 tests/model_metadata_vectors.py` compares the original upstream leaf helper bodies with Bend across thinking, clamping, identity and API matching cases; the TypeScript extraction/type erasure is test-only.

The models-runtime suite is partial: `packages/ai/test/model-cost.bend` ports the original request-wide pricing-tier case and all its charge assertions. `python3 tests/model_cost_vectors.py` also compares 159 complete results with the unmodified upstream cost function and checks the returned cost value on one and four threads. Registry/provider/auth cases remain unported. Costs are immutable values; no nested reference disposal is required.


The current inventory contains 4 ported, 4 partial and 541 pending suites. The validation suite became partial after its named plain-schema primitive case was ported through the composed checking path. `sh tests/native-agent.sh` replaces the former supplemental API-key/context/response/queue/stop/preparation/emission/truncation identity-and-mutation harnesses with native request, hook, failure and snapshot checks. This is not a claim that every former supplemental scheduling scenario has been retained. All 24 named cases in the pinned upstream agent-loop suite are now ported; broader agent suites remain pending. The progress entries below retain their historical coverage counts.

Other retained supplemental checks include 84 tool selections, 126 execution policies and 121 termination cases, 74 before-hook decisions, 195 turn updates, 108 result merges, 21 completion cases, 159 pricing vectors, 760 primitive coercions, 418 schema-type coercions, 2,670 type predicates, 7 schema-type filters, 50 dictionary sequences and 115 typed sampling configurations. These counts describe vectors, not fully ported upstream suites. JSON checks cover 892 quoting inputs and 114 native structured values. Native structural equality tests explicitly cover reordered dictionaries and ordered arrays.


After resuming the full port, `packages/agent/test/tool-finalization.bend` adds nine native scenarios for the actual hook invocation/finalization path, with channel gates checking awaited settlement. `agent-event-stream.bend` checks nonempty/empty final histories, buffered ordering and ignored late pushes. Both run through `tests/native-agent.sh` on one/four threads. They supplement the still-pending full `agent-loop.test.ts` port and do not change its status.


`tests/tool_update_scope.py` adds supplemental coverage for invocation-scoped update tracking. It executes the pinned `executePreparedToolCall` helper for failure-selection/late-update checks, runs native scope transitions and 256 concurrent completions on one/four threads, and verifies that copying a completion ticket is rejected by the compiler. The full `agent.test.ts` cases “should ignore tool updates after the tool execution settles” and “should ignore a settled parallel tool update while another tool is still running” still require the agent/executor and remain unported.


`packages/agent/test/tool-execution.bend` checks prepared execution with delayed/immediate update delivery, original versus validated arguments, tool/listener error precedence, late callbacks and resource drain. Its eight cases use channels rather than timing assumptions. `tool-execution-stream.bend` composes the production execution helper, update emitter, after-tool hook, termination policy, result-message emission and canonical stream, checking ordered events and final history. These run on one/four threads through `tests/native-agent.sh`; complete public-agent test cases still require loop orchestration and remain pending.

`tool-preparation-pipeline.bend` adds twelve supplemental preparation/execution scenarios derived from `agent-loop.ts` and the upstream “should execute mutated beforeToolCall args without revalidation” and “should prepare tool arguments for validation” contracts. The native hook returns explicit arguments/context. A strict fixture decoder rejects unexpected raw/adapted arguments; it is not a schema-validator port. Effect logs check lookup/adapter/validation/hook/cancellation/execution order and exactly-once validation; assertions cover first matching tool, errors, block reason fallback, abort precedence, ignored unblocked termination and retained context updates. The full upstream agent-loop suite remains pending.

`python3 tests/schema_check.py` compares the native typed schema evaluator with the exact TypeBox 1.3.27 dependency pinned by pi-ai: 2,700 checks on one/four threads. Its fixture translator only constructs native schema values for the listed constraints; it is test infrastructure, not a production JSON-schema loader. The script installs its test-only dependency beneath ignored `build/schema-reference` with lifecycle scripts disabled and checks the version. Additional native fixtures assert that prototype-like names are ordinary dictionary keys, required fields distinguish missing from null, inputs remain immutable, exclusive unions reject multiple matches, and JSON numeric types reject non-finite values. Schema loading, other keywords, validation diagnostics and full `validation.test.ts` parity remain pending.

Schema comparisons now cover inclusive/exclusive numeric bounds, adjacent binary64 values, array/property count bounds and contains/minContains/maxContains. Fixtures include zero/contradictory bounds, missing contains, zero-match acceptance, nested arrays and signed zero. The evaluator remains a typed dependency; JSON-schema loading and full tool-validation integration are still pending.

The user approved native numeric equality for uniqueness: `[0, -0]` is non-unique. TypeBox accepts that input because its hash distinguishes the bit patterns; tests record this as an explicit approved difference, not upstream parity.

The production `schema-load.bend` now compiles core JSON-schema values into the native evaluator, covering boolean/type/const/enum/composition, object properties/required/additional properties, array item schemas and numeric bounds. `schema_check.py --loaded` passes 1,980 checks against the same TypeBox fixture expectations, verifies 16 unsupported-keyword rejections and runs native malformed/nested-path tests on one/four threads. The typed fixture translator is not used in this mode. Loader support for counts/contains, references, remaining keywords, validation diagnostics and coercion integration remains pending; inventory statuses are unchanged.

The loader now supports item/property count bounds and contains/minContains/maxContains. Both typed and loaded modes cover 3,150 TypeBox comparisons, including zero/default/contradictory bounds, keyword reordering, missing contains, signed zero, limits above 32-bit and 53-bit ranges, and `1e100`. Native checks reject negative, fractional and non-finite counts and verify exact decimal values for compact integer conversion through the largest finite binary64 integer. These extend the same supplementary schema fixture set; they do not promote the upstream validation suite.

`python3 tests/normalization_check.py` compares the native immutable optional-null normalization stage with the actual pinned `normalizeOptionalNulls` and validator-cache helper bodies. It passes 244 comparisons on one/four threads, including the original optional-null and nullable-reference fixtures, required fields, nested objects, homogeneous/tuple arrays and untouched additional/union-only properties. Caller inputs remain unchanged. One literal-false-schema case is an explicit pending decision, not a parity pass: upstream’s boolean WeakMap-key failure preserves null, while native schema checking would omit it. Unsupported nullability constraints also produce typed errors until their vocabulary is implemented. These stage-level assertions do not complete the full upstream `validateToolArguments` tests.

`python3 tests/recursive_coercion_check.py` runs the pinned upstream coercion and real validator-cache helper bodies against native `coerceWithJsonSchema`. The 388 comparisons pass on one/four threads and cover scalar/type-union phases, ordered allOf/anyOf/oneOf phases, preservation of already-valid union members, independent failed candidates, untouched originals, declared/additional properties and homogeneous/tuple arrays. Literal-true union arms expose the same pending boolean-key cache decision; that case is an explicit pending result, not a parity pass. Unsupported union checks fail explicitly. These comparisons cover the recursive plain-schema coercion stage, not TypeBox conversion or final `validateToolArguments` behavior.

`plain-validation-upstream.bend` ports “coerces serialized plain JSON schemas with AJV-compatible primitive rules” with all 15 original assertions. `python3 tests/plain_validation_check.py` also compares 285 composed success/rejection results against the actual upstream `validateToolArguments` function, including normalization and final checking. Failures currently retain typed categories, received/coerced values and schema data; exact diagnostic strings are not yet ported. Builder conversion, references, remaining vocabulary and the other named cases remain pending, so `validation.test.ts` is now partial, not complete. `validated-tool-execution.bend` runs the native checker through actual agent preparation/execution and asserts that invalid calls never execute. All of these checks pass on one/four threads.

`python3 tests/validation_errors_check.py` verifies rendering separately from issue collection: 12 full messages from actual upstream `validateToolArguments`, 42 `formatValidationPath` cases and 47 two-space JSON values pass on one/four threads. It covers required/multiple/nested/array/union failures, escaped tool names, unusual paths, original-versus-coerced arguments, empty containers, control characters and Unicode. TypeBox supplies issue records to the test generator; native issue generation is not claimed. Native cases also cover the empty-issue fallback, missing-tool text and approved dictionary insertion order. The compact JSON regression suite remains part of the gate.

`python3 tests/schema_check.py --diagnostics` checks native structured-issue presence against 3,150 TypeBox acceptance results on one/four threads. Native fixtures verify missing versus null, path segments, additional-field order, union child suppression/ambiguity, numeric bounds and compact contains counts. These are runtime diagnostics, not exact upstream message comparisons; loader diagnostic metadata and tool-error wiring remain pending. No upstream suite statuses changed.

The loader now preserves type-array diagnostics as one expected-types issue, orders keywords as pi does, and retains native declaration order for property diagnostics. Native fixtures distinguish type arrays from anyOf and test keyword/property order independently of input value order. The 3,150 loaded validity checks, 3,150 diagnostic acceptance checks and 285 composed plain-tool validation comparisons remain the regression gates on one/four threads. Full diagnostic text wiring and source-specific contains/additionalItems behavior are still pending.

`validation_errors_check.py` now compares 54 complete messages through the production native validation path, alongside its isolated renderer tests, 42 paths and 47 indented values. This covers type arrays, ordered/nested failures, numeric and collection limits (including large binary64 bounds), union ambiguity, separate contains bounds and first-failing additional items. `validated-tool-execution.bend` asserts the complete production-formatted message and verifies the invalid tool is never executed. `plain-validation-cases-upstream.bend` adds four original named cases and eight assertions: nullable oneOf preservation, nullable union coercion, nullable arrays (native compiled checker plus public validation), and invalid primitive coercions. The validation suite now has five named cases/23 assertions ported; builder cases and references remain pending, so suite totals are unchanged.

The composed 285-case comparison now checks exact text for every failure, as well as success values and typed rejection/original-input preservation. The dedicated 54-message set also checks the eight-issue limit with overflowing arrays, objects and unions; the runtime exposes an explicit capped collector while retaining its uncapped API.

`schema_references_check.py` compares 536 acyclic local-reference tool-validation outcomes and exact failure messages against actual upstream validation on one/four threads: optional/required values, reusable definitions, chained references, siblings, escaped/Unicode names, tuple-pointer traversal, repeated references and unresolved targets. The original nullable-reference case is now ported in `plain-validation-cases-upstream.bend`, bringing validation to six named cases/24 assertions. Recursive references, anchors and external resources remain explicit unsupported cases. `uri_component_check.py` checks 394 percent-decoding inputs against `decodeURIComponent`, including invalid UTF-8, plus native pointer/list/dictionary tests. Native reference tests cover cycle aliases and ordinary reserved-looking dictionary keys without JS reflection. Suite totals remain unchanged.

`unique_items_check.py` verifies 82 composed outcomes/exact messages against upstream and two explicitly approved signed-zero differences (direct and nested). `schema_convert_check.py` compares 1,067 typed builder primitive conversions against TypeBox with exact binary64 output bits, plus native signed-zero/non-finite/literal cases. `schema_container_check.py` additionally checks 1,518 container/literal/union conversions, including nested policies, scalar wrapping, short tuples, untouched extra positions, original-value preservation, independent union retries and whole-union candidate checks; numeric outputs are compared bit-for-bit at every depth. These conversion policies are not yet a complete declaration/conversion pipeline and do not promote any upstream suite.

`schema_builder_check.py` checks 1,635 native declaration cases against pinned TypeBox: JSON projection, direct executable constraints, validation after recompiling the projected JSON, diagnostic consistency between the two validators, and immutable conversion output. It covers primitive JSON types, literals, unknown/never, arrays, tuples, unions and object fields, plus direct native nullable-union and optional-null normalization checks. Required/optional fields are explicit native variants. Options now support annotations, numeric bounds, collection limits, uniqueness and boolean additional-property constraints, with schema recompilation and updated union candidate validation. Unsupported structural overrides and schema-valued additional-property conversion fail explicitly; other options and integration into public tool validation remain pending. Object conversion currently implements the proposed literal-key policy; approval of its TypeBox differences is still pending. The original object-shaped nullable-union and optional-null tests are not marked ported by this dependency coverage.

`schema_object_policy_check.py` keeps two pending differences separate from parity comparisons: a declaration named `a.b` does not convert an undeclared `axb` key, and `[x]` does not convert `x`. The pinned source interprets both declared names as regular expressions and converts those other keys. These proposed native differences are executable review cases, not approved adaptations or public tool-validation behavior. Schema-valued additional-property conversion remains unimplemented pending the separate decision about applying it only to undeclared fields.

`schema-builder-options.bend` adds native checks for invalid-option paths, unsupported structural overrides, immutable replacement of options, and configured declarations nested inside unions. It separately verifies that a union preserves an original branch match but rejects converted candidates that violate its outer constraints, matching the pinned conversion implementation.

`schema_string_check.py` compares 206 schema-specific grapheme counts and 432 composed string-bound validation outcomes/exact messages against the pinned dependency. It covers combining marks, variation selectors, joined sequences, regional-indicator pairs, coercion, non-string instances, contradictory bounds and large binary64 limits. Native tests reject malformed bounds with keyword paths; builder-option checks exercise the same constraints. `schema-string.bend` deliberately implements the dependency’s limited grouping policy, not general UAX #29 segmentation: skin-tone modifiers, Hangul composition and unlisted combining-mark ranges retain the pinned behavior.

`tool_batch_sequential_check.py` compares 702 scheduler cases with the pinned `executeToolCallsSequential` function using injected per-call operations: processed-call order, result order, explicit state propagation, cancellation-check timing, delivery-failure propagation and batch termination. A channel-gated native test proves the next invocation waits for completion of the current operation. The scheduling oracle injects per-call operations; a separate `tool-call-sequential.bend` test connects the complete native preparation/execution/finalization lifecycle. Parallel scheduling and public loop integration remain pending. This dependency coverage does not promote the original agent-loop suite.

`tool-call-sequential.bend` composes the sequential scheduler with real schema validation, before-hook argument/context updates, execution updates, after-hook finalization and result-message emission. Ten native scenarios cover valid/invalid/missing calls, blocked calls, initial and post-result cancellation, failures at all five delivery boundaries and retained update callbacks after completion. The lifecycle owner retains update resources even when delivery fails; tests retire aliases before disposal. Full agent-loop entry points and parallel batching remain unfinished, so original suite statuses are unchanged.

`packages/runtime/test/concurrent-all.bend`, registered in `runtime-concurrency.sh`, checks empty collection, concurrent starts before gate release, input-ordered results, prompt failure from a later call while siblings are suspended, stable repeated failure observations, draining and 256 concurrent completions. Runs pass on one/four threads. This runtime helper supports the upcoming parallel scheduler; it does not yet exercise the original `executeToolCallsParallel` lifecycle or promote its suite.

`tool-call-parallel.bend` composes the canonical validator and tool lifecycle in twelve scenarios covering immediate outcomes, preparation-time cancellation, execution-start cancellation rechecks, blocking, update/end failures and ordered message-delivery failures. `tool_batch_parallel_check.py` compares 112 combinations against the actual pinned `executeToolCallsParallel` function with injected per-call helpers; the Bend side uses real preparation, validation, hooks and update scopes. The oracle covers scheduler composition, not independent equivalence of those injected helpers.

`tool-call-parallel-gated.bend` runs three real executors concurrently and forces reverse completion order. It exercises the execution-end/result-message ordering portion of upstream `agent-loop.test.ts`’s “should emit tool_execution_end in completion order but persist tool results in source order”; turn-end and full loop persistence assertions remain pending. A second gated case returns a listener failure while two sibling tools are still suspended, then releases and drains them. Both cases verify final preparation context in every after hook and inactive retained update callbacks. This is component coverage; the original agent-loop suite remains pending.

`tool-execution-dispatch.bend` exercises the common `executeToolCalls` entry point with 27 combinations of global, first-matching-tool and later-tool mode settings, three empty batches, and 15 delivery failures. Every nonempty fixture also includes a duplicate declaration with a sequential override, an unrelated sequential tool and interspersed text content. It checks actual event order, validated execution, hook counts, context updates, error propagation and inactive retained updates. The gated parallel lifecycle test now enters through the common dispatcher too; its reverse-completion and prompt-failure checks cover real routing into concurrent execution. The full upstream loop assertions remain pending.

`turn-completion.bend` composes real validated tool batches with history commitment, `turn_end`, stop hooks, steering/follow-up queues and `agent_end` in 28 scenarios. Hook/event assertions distinguish prior context from generated history and check that queued messages are not committed before the next turn. `turn_completion_check.py` adds 87 comparisons against the actual pinned `runLoop` body. Its source oracle injects provider, declaration and tool helpers, stops at next-turn preparation, and preserves the loop’s actual history, event and queue-control logic. These checks cover the successful-turn tail and failure propagation, not full provider streaming, failed/truncated assistant handling, next-turn preparation or public loop entry points.

`tool_changes_check.py` compares 190 cases against the pinned `declareToolChanges` function and its real transcript helpers. It verifies baseline replay, replacement of the last pending system message’s tool fields, duplicate declarations, removals, explicit empty lists, text blocks, sections and timestamp decisions. `tool-changes-clock.bend` exercises the native IO wrapper, unchanged existing timestamps, absent tools and the separately approved adaptation that reordered schema fields do not cause redefinition.

`turn-preparation.bend` checks 45 composed initial/next-turn preparations with real declaration reconciliation. `turn_preparation_check.py` compares those cases with the actual upstream preparation block and declaration helper. Assertions cover hook snapshots, context/model/reasoning updates, retained versus newly polled steering, event order, separate committed/generated histories and failure boundaries. Conversion and tool execution callbacks are forbidden in these fixtures. These are request-boundary components, not completion of the full upstream agent-loop suite.

`main_loop_check.py` compares 145 composed internal-loop scenarios with the pinned `runLoop` and all of its original helpers, including declaration reconciliation, streaming, sequential/parallel dispatch and completion. The source oracle supplies a deterministic provider and callbacks, loads the actual upstream validator with pinned TypeBox, and checks hook snapshots and provider model changes. Bend uses native streams, callbacks, schema validation and execution. Assertions compare ordered generated history, provider transcripts, event/callback order and invocation counts across all five response endings, termination/queue/stop combinations and listener failures. In particular, length-limited, error and aborted responses must never reach validation or execution. The fixture starts at internal `runLoop`, so it does not cover initial public-entry event emission, continue validation, default-provider selection or public event-stream lifecycle. The full upstream `agent-loop.test.ts` suite remains pending.

`loop_entry_check.py` runs 310 source comparisons through the explicit-provider `runAgentLoop` and `runAgentLoopContinue` entries and the full native internal loop. It extends the existing response/queue/termination matrix with public-entry event prefixes, prompt delivery failures, continuation without replaying old messages, and rejection of empty or assistant-ended contexts before any event/provider callback. Errors preserve the original text through typed native values. The shared oracle executes the actual public entry functions and helpers. These supplemental cases do not yet port the named `agentLoop`/`agentLoopContinue` stream-wrapper tests or the default-stream regression; the original suite remains pending.

`default_stream_check.py` adds 112 comparisons through public entries and the actual upstream `setDefaultStreamFn`/`getDefaultStreamFn` implementation. Fixtures cover an explicit provider with absent or different defaults, a configured fallback, missing fallback, registration/removal/replacement from initial event callbacks, replacement during steering, and two-turn retention of the selected callback. Invalid continuations and listener failures must take precedence over default lookup. Both callbacks remain owned until runs drain; replacing the registry value does not dispose an in-flight provider. The 310 public-entry and 145 internal-loop checks also pass with the new native selection API. This does not yet complete the named default-provider regression through `agentLoop`’s asynchronous stream wrapper.


`packages/agent/test/agent-loop.bend` now ports five of the 24 named `agent-loop.test.ts` cases through the native asynchronous wrappers: default-provider fallback, AgentMessage event/history types, empty-context continuation rejection, continuation without replaying user events, and custom last-message continuation. All five pass on one/four threads. Exceptions and omitted provider arguments become typed errors and explicit fallback selection; the remaining 19 named cases are pending.

`python3 tests/loop_stream_check.py` adds 204 wrapper scenarios, comparing event and callback order separately with the pinned underlying loop and checking repeated results, drained iteration and gated asynchronous return. Callback/default-provider failures close the stream with a typed error, as explicitly approved by the user, without a synthetic `agent_end`. These failure-closure checks are native adaptations, not comparisons with upstream unhandled-rejection behavior. The 145 internal-loop, 310 public-entry and 112 default-provider comparisons also pass on one/four threads. Gated surviving-tool failure/drain coverage remains at the lower batch layer rather than the new wrappers.

`packages/ai/test/event-stream-atomic.bend` reproduces a native terminal-publication race with a suspended extractor: iteration formerly ended before receiving the terminal event. Atomic event/terminal/result ownership fixes that race and rejects pushes after a concurrent close. The gated tests and 128 completion/close races pass on one/four threads, together with the existing generic and assistant stream suites via `sh tests/event-stream.sh`.


The named agent-loop port now also includes “should build provider context exclusively from transcript messages”, “should handle custom message types via convertToLlm”, and “should apply transformContext before convertToLlm”. The provider receives the typed messages-only transcript; the first case checks preserved system text, empty tool declarations and timestamp instead of JS reference identity. The conversion cases retain the original notification and four-message pruning fixtures, assert exact provider messages and callback order, and check that provider pruning does not replace generated history. All eight named cases pass on one/four threads. This brings the suite to eight of 24 named cases, with 16 pending; its status remains partial.


`packages/agent/test/agent-loop-tools.bend` ports “should handle tool calls and results” and “should not execute tool calls from a length-truncated assistant message” through the public async wrapper and real native plain-schema validation. The first retains every original token/cost value, asserts original usage observed by the after hook, and checks patched usage in execution-end events, persisted messages and the next provider request. The second preserves schema-valid truncated arguments, requires zero executor calls, checks the complete truncation error and a second provider call ending in an assistant message. The fixture stores the single tool-detail value as a String; no original assertion depends on JS object identity or reflection. Both new cases and the previous eight pass on one/four threads. The suite is now ten of 24 named cases, with 14 pending; totals remain 3 ported, 5 partial and 541 pending.


`packages/agent/test/agent-loop-arguments.bend` ports “should execute mutated beforeToolCall args without revalidation” and “should prepare tool arguments for validation” through the public async loop with real native plain-schema validation. The first returns numeric `123` as an explicit immutable hook update after validating the original string. The second adapts the original flat before/after edit into the nested edits array before validation. Exact callback traces require one validation and one execution, and history checks preserve the original assistant arguments and successful result. The fixture details use String; the original assertions concern executor arguments. Both pass on one/four threads, bringing the named suite to 12 of 24 cases; the remaining 12 are pending. Existing production code satisfied these contracts.


`agent-loop-parallel.bend` ports “should emit tool_execution_end in completion order but persist tool results in source order”. Channel handoffs prove the second executor overlaps the suspended first; the stream consumer releases the first only after observing the second execution-end. Assertions preserve all three original event/result orderings and additionally check provider input and final history. `agent-loop-steering.bend` ports “should inject queued messages after all tool calls complete”, retaining sequential execution, one-shot steering eligibility after the first execution, both successful end events, tool-message-before-interrupt ordering and interrupt presence in the second provider context. Both pass on one/four threads; 14 of 24 named cases are now ported, with ten pending. The pure build supplies Clang a higher parser nesting limit because the new generic callback combination produces a C match tree beyond its default 256; this is generated-code compilation, not foreign runtime logic.


`agent-loop-parallel.bend` now also ports the three original execution-mode tests: a single sequential tool overriding the parallel default, a mixed batch with one sequential tool, and explicitly parallel tools. The sequential fixtures emit a progress update while the first executor is suspended; the consumer requires exactly one execution-start before releasing it, then the second executor verifies the first resolved. The parallel fixture retains the opposite gate and requires overlap. All original executor arguments and mixed-tool ordering assertions are retained, with extra source-order checks on events, provider requests and final history. The added progress event is test synchronization, replacing source timers. All four cases in the expanded fixture pass on one/four threads. The named suite is now 17 of 24 cases; seven remain pending and suite totals are unchanged.


`agent-loop-turns.bend` ports “should use prepareNextTurn snapshot before continuing” and “should stop after the current turn when shouldStopAfterTurn returns true”. The preparation case retains the original guidance message and timestamp, returns the immutable context snapshot, and asserts one preparation call and two provider requests with guidance in the second. The stop case preserves the exact original 14-event sequence, context/history roles, tool-result ID, executed argument, one steering poll, zero follow-up polls and one provider request. Both pass on one/four threads through real tool validation/execution and the public asynchronous wrapper. There are now 19 of 24 named cases ported; the five remaining batch-termination cases keep the suite partial.


The pinned `packages/agent/test/agent-loop.test.ts` suite is now ported: all 24 original names map exactly once across seven fixtures (8 base, 2 tool usage/truncation, 2 argument preparation, 4 concurrency/modes, 1 steering, 2 turn hooks, 5 termination). Every fixture was freshly rebuilt and passed on one/four threads for this promotion. The final `agent-loop-termination.bend` cases preserve provider counts, history roles, turn-end counts, blocked executor exclusion/error text, mixed-batch continuation and after-hook termination. Source assertions were reviewed alongside the name map; matching names alone is not the basis for promotion.

Declared native adaptations remain explicit: immutable field equality instead of system-message identity, explicit hook argument/context updates instead of mutation, typed errors and default-provider selection, and channel/progress-event gates instead of timers. Fixture-specific custom/detail payloads use native generic values without recreating JS object machinery. All fixtures execute the actual native loop, tool lifecycle and plain-schema validator through injected provider callbacks; these tests do not prove complete builder/schema, provider, `Agent` object, extension, harness or CLI parity. Current suite totals are 4 ported, 4 partial and 541 pending.


`python3 tests/agent_state_check.py` adds 154 supplemental comparisons with the pinned `Agent` constructor, actual `processEvents` and `finishRun`, and the unmodified run-start block. Seven initial configurations each traverse 22 snapshots, checking model/thinking selection, transcript prompt/declaration replay, default custom-message filtering, streaming/pending/error transitions, duplicate/missing tool IDs, agent-end versus finish semantics and restart. Native checks also cover complete unknown-model defaults and retained immutable snapshots. All pass on one/four threads. This exercises the new state component, not the native public `Agent` owner or subscription lifecycle; `agent.test.ts` remains pending and inventory counts are unchanged.

`python3 tests/agent_queues_check.py` compares 434 operation sequences (1,754 snapshots) against the actual pinned `PendingMessageQueue`, and 1,024 decisions against `Agent.continue`. Cases cover both modes, mode changes, clearing, repeated/empty drains, busy rejection, empty/system-only and six other transcript shapes, steering precedence, follow-up fallback, retained queued messages and the initial-steering-poll skip flag. The source oracle replaces only the selected prompt/continuation run entries to observe their arguments; native execution and consumption of the skip flag are outside this comparison boundary. Native assertions additionally check independent queue clearing/mode changes, mode preservation when clearing both, and immutable retained snapshots. All pass on one/four threads. These are component comparisons, not named `agent.test.ts` ports; inventory status remains unchanged.

`packages/agent/test/agent-lifecycle.bend` adds native concurrency and typed-error checks for the internal lifecycle owner. Each of 64 scenarios races two starts, checks preparation/error precedence, observes cancellation without premature settlement, drains two completion waiters, restarts with a fresh signal, rejects stale/repeated finishing handles and inspects the retained completed signal. Six additional execution/recovery combinations check success, executor failure, cancellation, successful recovery and recovery failure, including active-state visibility during recovery and final cleanup afterward. All pass on one/four threads; canonical library types pass. These assertions exercise the lifecycle component using a small immutable state fixture, not the public Agent or its named subscriber tests. No upstream suite status changes.

`agent_runtime_check.py` adds 588 composed snapshots against the actual pinned Agent constructor, queue methods, prompt/continue selection, `runWithLifecycle`, event reducer, reset and finish behavior. Its oracle replaces the selected loop entry with a gated executor while retaining actual upstream lifecycle handling; native checks exercise the combined state/queue/lifecycle owner. The 12 sequences cover three initial histories, both modes for each queue, busy and unusable-history errors, queued-input precedence, event reduction outside a run, agent-end versus idle, system-baseline replay, mode changes, drains and individual/combined clearing. All pass on one/four threads. `agent-runtime-concurrency.bend` adds 64 races proving that only the winning continuation consumes the configured batch and that finish retains remaining messages. The state oracle now includes reset in all seven configurations (161 snapshots), checking retained model/thinking/executable tools and replayed baseline. Lifecycle regressions and canonical library types remain green. Native public listener/provider execution is not part of these checks, and `agent.test.ts` remains pending.

`agent_listeners_check.py` compares eight three-delivery scenarios with actual pinned `Agent.subscribe` and `processEvents`: normal ordering, duplicate registration, removing a future listener, adding during dispatch, removal/readdition, self-removal, first/second listener failures, signal forwarding and repeated unsubscribe including a callback registered again. `agent-events.bend` adds three deterministic gated scenarios over the real native runtime and event-dispatch bridge. They check state reduction before callbacks, ordered awaiting, cancellation reaching a suspended subscriber, agent-end remaining active, pending completion while the gate is held, busy reset, cleanup after success/failure, first-error propagation and no callback delivery for idle events. The fixture executor finishes only after dispatch returns; provider execution and full failure-event recovery are outside this boundary. All checks and canonical library types pass on one/four threads where applicable. No named public-Agent test is promoted; inventory remains 4 ported, 4 partial and 541 pending.

`agent_failure_check.py` adds 40 source comparisons through actual pinned `runWithLifecycle`, `handleRunFailure`, event reduction and subscriptions. The matrix covers cancellation, empty/nonempty original errors, successful recovery and failure of either subscriber at each of the four recovery events. It compares subscriber order, state visible during each callback, committed history, error state, returned recovery failure and final idle cleanup. Native checks additionally require the complete failure assistant shape, zero usage/cost, absent optional metadata, current model identifiers, correct reason, wall-clock bounds and one timestamp shared by every event. All pass on one/four threads; canonical library types pass. The source executor throws directly and the native executor returns its typed failure at the claimed-run boundary; real provider/loop invocation and the named public-Agent thrown-provider test remain pending. Suite statuses are unchanged.

`agent_loop_config_check.py` adds 24 source comparisons for the run-owned configuration binding: initial steering skip on/off, both steering/follow-up modes and off/low/high thinking. Actual pinned queue readers are exercised before and after enqueues and mode changes; a second configuration has its own skip flag while observing the same queues. Assertions retain the original context across a later message, compare a fresh snapshot, and check selected model/reasoning plus session, transport, retry-cap and tool-execution options. All pass on one/four threads; canonical library types pass. The native adapter consumes a supplied base configuration. It does not yet construct all public AgentOptions, adapt the two preparation-hook forms, or invoke the provider loop. No upstream suite is promoted.

`agent_hooks_check.py` adds 64 source scenarios against actual pinned `createLoopConfig` wrappers. Each scenario invokes original/fresh stop and preparation callbacks across five checkpoints: idle creation, active replacement, fresh configuration, cancellation and idle removal. The matrix varies all four initial/later preparation combinations, initial stop presence and callback errors. Native callbacks are installed through `agent-loop-config.applyHooks`, then read from the real loop-config fields. Assertions compare capture-versus-live lookup, context-aware precedence, legacy signal-only input, full typed update shape, context markers, current signal state and errors. All pass on one/four threads; canonical library types pass. This does not yet port the named public-Agent legacy/stop tests, whose provider request counts and tool execution require the still-pending public orchestration. Suite statuses are unchanged.

Provider opening failures are now represented by a typed result at `StreamFn`, rather than requiring an already-created stream. The main-loop differential matrix adds four throwing-provider scenarios (149 total); the wrapper matrix adds sixteen combinations of prompt/continuation, explicit/default providers, missing defaults and invalid continuation (220 total). The source oracle invokes the actual upstream loop and captures its rejected call. Native wrappers additionally check the approved closed iteration and repeatable error result, without synthesizing `agent_end`. Event traces and provider/validation/execution counts check error precedence and absence of work after a failed opening. These additions do not promote the public Agent thrown-provider test: Agent/provider orchestration remains unfinished.

After the provider-result migration, all 24 named `agent-loop.test.ts` cases, 310 entry comparisons, 112 default-provider comparisons and native request-pipeline checks pass after fresh builds on one/four threads. Configuration checks reject raw-stream callbacks and option erasure, and accept the explicit typed provider result. Inventory remains 4 ported, 4 partial and 541 pending suites.

The subscription callback result is now an explicit immediate/pending `Emission`, matching the existing loop emitter protocol. `agent_listeners_reference.mts` additionally checks two causal-order scenarios against actual pinned `Agent.processEvents`: immediate first-listener effects, a second event unblocking a suspended first event, and per-event failure isolation. `agent-listener-start.bend` ports those assertions with channels and runs under one/four threads. `agent-events.bend` now calls `startEvents` directly and checks that both state reduction and first-listener effects happen before the start call returns; its existing awaiting, cancellation, idle settlement and failure checks remain. These are component tests, not public Agent prompt execution or promotion of `agent.test.ts`.

`agent_run_check.py` connects the native runtime and listeners to real loop entries via `agent-run.runClaimed`. Its oracle evaluates the actual pinned `Agent` class and full loop/helper bodies together, including the upstream validator; only the provider is a fixture. Ten scenarios compare event-by-event state snapshots, final recovery/idle state and ordered provider transcripts across ordinary prompt/continuation, queued messages, assistant-tail steering continuation, provider failure/cancellation, listener failures and tool execution followed by a second request. The native no-argument tool uses `Unit` as an explicit fixture adapter, so this is not evidence of complete schema validation or argument inference. Disposal releases per-run readers, emitters, update owners and batches while leaving borrowed provider/Agent resources to their owners. These supplemental comparisons do not promote the named public Agent suite or establish constructor/options parity.

`agent_owner_check.py` repeats the ten composed Agent/source comparisons through the new constructor, owner subscriptions and public prompt/continuation methods. Native checks additionally cover owned-versus-borrowed converters, construction-time provider resolution retained after registry removal, missing fallback rejection, auto transport and parallel tool defaults. The source and native owner both reject recursive prompt, continuation and reset attempts from an active listener, preserving the outer run. `agent_prompt_check.py` compares six text/image, single-message, custom-message and batch inputs with the actual upstream normalizer; native wall-clock timestamps are bounded by readings around the normalization call. The later named-case table records the first eight ported cases; the rest remain pending.

The first named `agent.test.ts` cases are ported through the actual native Agent owner in `packages/agent/test/agent.bend`:

| Upstream name | Native assertion group |
| --- | --- |
| should create an agent instance with default state | `constructor(0)` checks model presence, off thinking, empty tools/history/pending IDs and idle/absent runtime fields |
| should create an agent instance with custom initial state | `constructor(1)` checks the full supplied model value, low thinking and the exact initial system text/timestamp |
| converts initial prompt and tools into transcript state | `constructor(2)` checks initial system text and ordered tool declaration names |
| should subscribe to events | `subscription` checks no initial/update emission, the updated thinking level and unsubscription |
| should update state with mutators | `mutators` checks model/thinking/tool replacement, message replacement/append/clear and retained immutable inputs/snapshots |
| should support steering message queue | `queueCase(false)` checks insertion remains outside transcript state |
| should support follow-up message queue | `queueCase(true)` checks insertion remains outside transcript state |
| should handle abort controller | `abortIdle` checks abort succeeds without an active run |

Complete typed tool/message fixtures replace upstream's partial `as any` values. Model preservation is checked structurally using complete deterministic model fixtures instead of reference identity; array-copy assertions become immutable input/snapshot checks, as approved. Additional queue-query/clear/mode checks exercise native accessors. The composed owner regression now changes thinking level from every event listener, checking that these updates preserve active streaming state, partial messages, pending tool IDs, queues and recovery error state throughout real runs. Remaining named cases stay pending; these tests do not establish full Agent or package completion.

All eight mapped Agent cases and supplemental queue-mode checks pass on one/four threads after fresh builds. The Agent suite is now partial: inventory totals are 4 ported, 5 partial and 540 pending suites. Model and message fixtures use deterministic supplied values; model preservation checks every field in the fixture, while immutable snapshots replace reference-identity expectations.

Five further named Agent cases are mapped below. `agent-behavior.bend` uses a done-only provider stream, as the upstream tests do, through actual public prompt/continuation calls. The optional stream function and explicitly owned default-provider registry replace reflective construction with an omitted JS argument. Fixture timestamps and assistant text are deterministic because these cases assert queue processing, roles and request counts rather than those values.

| Upstream name | Native assertion group |
| --- | --- |
| restores the transcript baseline when reset | `agent.bend:resetBaseline` checks exactly one system message, original prompt and echo declaration; additionally checks queue clearing |
| uses the configured default when a legacy caller omits streamFn | `agent-behavior.bend:legacyDefault` checks one fallback provider call |
| forwards sessionId to streamFunction options | `agent-behavior.bend:sessionId` checks constructor value, setter/getter and both request values |
| continue() should process queued follow-up messages after an assistant turn | `agent-behavior.bend:followUp` checks retained queued user text and final assistant message |
| continue() should keep one-at-a-time steering semantics from assistant tail | `agent-behavior.bend:steering` checks exact alternating history roles and two provider calls |

All 13 mapped Agent cases and the supplemental queue-mode checks pass on one/four threads after fresh builds. Fourteen named Agent cases remain pending. The library type check and all six compiler-patch regression scripts pass on Bend 2.0.7 after compatible patches were reapplied following its automatic update. Suite totals remain 4 ported, 5 partial and 540 pending.
