--- a/bend2/base.bend
+++ b/bend2/base.bend
@@ -224,6 +224,11 @@
   import "./effs/unix_milliseconds.c"
   import "./effs/unix_milliseconds.js"
 
+# Whether a file descriptor (0 stdin, 1 stdout, 2 stderr) is a terminal.
+def IO.isTerminal(descriptor: U32) -> IO(Bool):
+  import "./effs/is_terminal.c"
+  import "./effs/is_terminal.js"
+
 def IO.now() -> IO(Nat):
   import "./effs/now.c"
   import "./effs/now.js"
--- /dev/null
+++ b/bend2/effs/is_terminal.c
@@ -0,0 +1,11 @@
+#include <unistd.h>
+
+// Whether a file descriptor refers to a terminal. Errors (EBADF, ENOTTY)
+// report false, as Node's tty.isatty does.
+Term io_isterminal_run(Env e, Term* f, IoWork* w) {
+  return io_done(e, term_pak(isatty((int)(u32)f[0]) ? CID_TRUE : CID_FALSE, 0));
+}
+
+static void __attribute__((constructor)) io_isterminal_use(void) {
+  io_eff(CID_IO_ISTERMINAL, io_isterminal_run, 0);
+}
--- /dev/null
+++ b/bend2/effs/is_terminal.js
@@ -0,0 +1,4 @@
+// Separate compiler-backend implementation; native programs never invoke JS.
+function io_isterminal(descriptor) {
+  return io_done(require("tty").isatty(descriptor));
+}
