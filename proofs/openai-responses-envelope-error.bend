import Base
import ../laws/openai-responses-envelope-error.bend as Laws

def Laws.header_diagnostic_survives_envelope_wrapping(cause): {==}
def Laws.header_field_diagnostic_survives_both_boundaries(cause): {==}
def Laws.query_position_survives_both_boundaries(cause): {==}
