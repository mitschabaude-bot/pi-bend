import Base
import ../laws/openai-responses-stream-defaults.bend as Laws

def Laws.borrowing_preserves_owner_and_configuration(E, callback, requested, grammar): {==}
def Laws.disposal_retires_owned_pricing_callback(E, callback): {==}
