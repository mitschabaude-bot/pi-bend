import Base
import ../laws/http-exchange.bend as Laws

def Laws.ipv4_bypasses_lookup(address): {==}
def Laws.ipv6_bypasses_lookup(address): {==}
def Laws.domain_spelling_retained(first, rest): {==}
def Laws.opaque_host_rejected(text): {==}
def Laws.empty_domain_rejected(): {==}
def Laws.empty_host_rejected(): {==}
