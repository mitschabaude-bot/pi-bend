import Base
import ../laws/openai-responses-system-defaults.bend as Laws

def Laws.owned_hooks_retire_before_caller_finalizer(Reason, owner, cleanup): {==}
def Laws.clock_runs_once_before_provider_initialization(configuration, model, transcript, options, clock, environment, cleanup): {==}
