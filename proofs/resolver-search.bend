import Base
import ../laws/resolver-search.bend as Laws

def Laws.preserves_request_policy(configuration, name, dots, absolute): {==}

def Laws.preserves_search_policy(configuration, name, dots, absolute): {==}
