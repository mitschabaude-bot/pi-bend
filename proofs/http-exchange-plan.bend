import Base
import ../laws/http-exchange-plan.bend as Laws

def Laws.tls_cannot_start_cleartext(R, host, port, authority, target, request, maximum): {==}
def Laws.zero_read_size_cannot_start(R, host, port, authority, target, request): {==}
