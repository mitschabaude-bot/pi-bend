import Base
import ../laws/openai-responses-preparation-error.bend as Laws

def Laws.grammar_diagnostic_is_stage_independent(cause): {==}
def Laws.transcript_tool_diagnostic_is_stage_independent(cause): {==}
def Laws.replay_grammar_diagnostic_is_stage_independent(cause): {==}
def Laws.credentials_keep_provider_diagnostic(cause): {==}
