import Base
import ../laws/resolver-transport.bend as Laws

def Laws.settings_and_entries_preserved(Server, options, entries): {==}
def Laws.preparation_failure_retained(Server, options, error): {==}
