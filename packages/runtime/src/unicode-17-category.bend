# Generated by scripts/generate-category-unicode.py from Unicode17.0.0.
# Unicode License V3; see unicode-LICENSE.txt. Constant decisions (plain definitions) allocate no table.
# General_Category class of a scalar: 1 = Letter (L*), 2 = Number (N*),
# 3 = Punctuation (P*), 4 = Symbol (S*), 0 = other.
import Base

# 2073 runs.
def categoryBelow0(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow2(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow0(code,U32.is_lt(code,33))
    case False{}: categoryBelow1(code,U32.is_lt(code,37))
def categoryBelow3(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow4(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow5(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow3(code,U32.is_lt(code,44))
    case False{}: categoryBelow4(code,U32.is_lt(code,58))
def categoryBelow6(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2(code,U32.is_lt(code,36))
    case False{}: categoryBelow5(code,U32.is_lt(code,48))
def categoryBelow7(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow8(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow9(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow7(code,U32.is_lt(code,63))
    case False{}: categoryBelow8(code,U32.is_lt(code,91))
def categoryBelow10(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow11(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow12(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow10(code,U32.is_lt(code,95))
    case False{}: categoryBelow11(code,U32.is_lt(code,97))
def categoryBelow13(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow9(code,U32.is_lt(code,65))
    case False{}: categoryBelow12(code,U32.is_lt(code,96))
def categoryBelow14(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow6(code,U32.is_lt(code,43))
    case False{}: categoryBelow13(code,U32.is_lt(code,94))
def categoryBelow15(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow16(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow17(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow15(code,U32.is_lt(code,124))
    case False{}: categoryBelow16(code,U32.is_lt(code,126))
def categoryBelow18(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow19(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow20(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow18(code,U32.is_lt(code,161))
    case False{}: categoryBelow19(code,U32.is_lt(code,167))
def categoryBelow21(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow17(code,U32.is_lt(code,125))
    case False{}: categoryBelow20(code,U32.is_lt(code,162))
def categoryBelow22(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow23(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow24(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow22(code,U32.is_lt(code,170))
    case False{}: categoryBelow23(code,U32.is_lt(code,172))
def categoryBelow25(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow26(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow27(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow25(code,U32.is_lt(code,174))
    case False{}: categoryBelow26(code,U32.is_lt(code,180))
def categoryBelow28(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow24(code,U32.is_lt(code,171))
    case False{}: categoryBelow27(code,U32.is_lt(code,178))
def categoryBelow29(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow21(code,U32.is_lt(code,127))
    case False{}: categoryBelow28(code,U32.is_lt(code,173))
def categoryBelow30(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow14(code,U32.is_lt(code,60))
    case False{}: categoryBelow29(code,U32.is_lt(code,168))
def categoryBelow31(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow32(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 2
def categoryBelow33(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow31(code,U32.is_lt(code,182))
    case False{}: categoryBelow32(code,U32.is_lt(code,185))
def categoryBelow34(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow35(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow36(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow34(code,U32.is_lt(code,187))
    case False{}: categoryBelow35(code,U32.is_lt(code,191))
def categoryBelow37(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow33(code,U32.is_lt(code,184))
    case False{}: categoryBelow36(code,U32.is_lt(code,188))
def categoryBelow38(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow39(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow40(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow38(code,U32.is_lt(code,215))
    case False{}: categoryBelow39(code,U32.is_lt(code,247))
def categoryBelow41(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow42(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow43(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow41(code,U32.is_lt(code,706))
    case False{}: categoryBelow42(code,U32.is_lt(code,722))
def categoryBelow44(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow40(code,U32.is_lt(code,216))
    case False{}: categoryBelow43(code,U32.is_lt(code,710))
def categoryBelow45(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow37(code,U32.is_lt(code,186))
    case False{}: categoryBelow44(code,U32.is_lt(code,248))
def categoryBelow46(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow47(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow48(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow46(code,U32.is_lt(code,741))
    case False{}: categoryBelow47(code,U32.is_lt(code,749))
def categoryBelow49(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow50(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow51(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow49(code,U32.is_lt(code,751))
    case False{}: categoryBelow50(code,U32.is_lt(code,880))
def categoryBelow52(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow48(code,U32.is_lt(code,748))
    case False{}: categoryBelow51(code,U32.is_lt(code,768))
def categoryBelow53(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow54(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow55(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow53(code,U32.is_lt(code,886))
    case False{}: categoryBelow54(code,U32.is_lt(code,890))
def categoryBelow56(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow57(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow58(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow56(code,U32.is_lt(code,895))
    case False{}: categoryBelow57(code,U32.is_lt(code,900))
def categoryBelow59(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow55(code,U32.is_lt(code,888))
    case False{}: categoryBelow58(code,U32.is_lt(code,896))
def categoryBelow60(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow52(code,U32.is_lt(code,750))
    case False{}: categoryBelow59(code,U32.is_lt(code,894))
def categoryBelow61(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow45(code,U32.is_lt(code,192))
    case False{}: categoryBelow60(code,U32.is_lt(code,885))
def categoryBelow62(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow30(code,U32.is_lt(code,123))
    case False{}: categoryBelow61(code,U32.is_lt(code,736))
def categoryBelow63(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow64(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow65(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow63(code,U32.is_lt(code,903))
    case False{}: categoryBelow64(code,U32.is_lt(code,907))
def categoryBelow66(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow67(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow68(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow66(code,U32.is_lt(code,909))
    case False{}: categoryBelow67(code,U32.is_lt(code,930))
def categoryBelow69(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow65(code,U32.is_lt(code,904))
    case False{}: categoryBelow68(code,U32.is_lt(code,910))
def categoryBelow70(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow71(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow72(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow70(code,U32.is_lt(code,1014))
    case False{}: categoryBelow71(code,U32.is_lt(code,1154))
def categoryBelow73(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow74(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow75(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow73(code,U32.is_lt(code,1162))
    case False{}: categoryBelow74(code,U32.is_lt(code,1329))
def categoryBelow76(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow72(code,U32.is_lt(code,1015))
    case False{}: categoryBelow75(code,U32.is_lt(code,1328))
def categoryBelow77(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow69(code,U32.is_lt(code,908))
    case False{}: categoryBelow76(code,U32.is_lt(code,1155))
def categoryBelow78(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow79(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow80(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow78(code,U32.is_lt(code,1369))
    case False{}: categoryBelow79(code,U32.is_lt(code,1376))
def categoryBelow81(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow82(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow83(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow81(code,U32.is_lt(code,1419))
    case False{}: categoryBelow82(code,U32.is_lt(code,1424))
def categoryBelow84(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow80(code,U32.is_lt(code,1370))
    case False{}: categoryBelow83(code,U32.is_lt(code,1421))
def categoryBelow85(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow86(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow87(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow85(code,U32.is_lt(code,1471))
    case False{}: categoryBelow86(code,U32.is_lt(code,1473))
def categoryBelow88(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow89(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow90(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow88(code,U32.is_lt(code,1476))
    case False{}: categoryBelow89(code,U32.is_lt(code,1479))
def categoryBelow91(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow87(code,U32.is_lt(code,1472))
    case False{}: categoryBelow90(code,U32.is_lt(code,1478))
def categoryBelow92(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow84(code,U32.is_lt(code,1417))
    case False{}: categoryBelow91(code,U32.is_lt(code,1475))
def categoryBelow93(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow77(code,U32.is_lt(code,931))
    case False{}: categoryBelow92(code,U32.is_lt(code,1470))
def categoryBelow94(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow95(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow96(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow94(code,U32.is_lt(code,1515))
    case False{}: categoryBelow95(code,U32.is_lt(code,1523))
def categoryBelow97(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow98(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow99(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow97(code,U32.is_lt(code,1542))
    case False{}: categoryBelow98(code,U32.is_lt(code,1547))
def categoryBelow100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow96(code,U32.is_lt(code,1519))
    case False{}: categoryBelow99(code,U32.is_lt(code,1545))
def categoryBelow101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow101(code,U32.is_lt(code,1550))
    case False{}: categoryBelow102(code,U32.is_lt(code,1563))
def categoryBelow104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow104(code,U32.is_lt(code,1565))
    case False{}: categoryBelow105(code,U32.is_lt(code,1611))
def categoryBelow107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow103(code,U32.is_lt(code,1552))
    case False{}: categoryBelow106(code,U32.is_lt(code,1568))
def categoryBelow108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow100(code,U32.is_lt(code,1525))
    case False{}: categoryBelow107(code,U32.is_lt(code,1564))
def categoryBelow109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow109(code,U32.is_lt(code,1642))
    case False{}: categoryBelow110(code,U32.is_lt(code,1648))
def categoryBelow112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow112(code,U32.is_lt(code,1748))
    case False{}: categoryBelow113(code,U32.is_lt(code,1750))
def categoryBelow115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow111(code,U32.is_lt(code,1646))
    case False{}: categoryBelow114(code,U32.is_lt(code,1749))
def categoryBelow116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow116(code,U32.is_lt(code,1759))
    case False{}: categoryBelow117(code,U32.is_lt(code,1767))
def categoryBelow119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow120(code,U32.is_lt(code,1786))
def categoryBelow122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow119(code,U32.is_lt(code,1770))
    case False{}: categoryBelow121(code,U32.is_lt(code,1776))
def categoryBelow123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow118(code,U32.is_lt(code,1765))
    case False{}: categoryBelow122(code,U32.is_lt(code,1774))
def categoryBelow124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow115(code,U32.is_lt(code,1649))
    case False{}: categoryBelow123(code,U32.is_lt(code,1769))
def categoryBelow125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow108(code,U32.is_lt(code,1548))
    case False{}: categoryBelow124(code,U32.is_lt(code,1758))
def categoryBelow126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow93(code,U32.is_lt(code,1367))
    case False{}: categoryBelow125(code,U32.is_lt(code,1632))
def categoryBelow127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow62(code,U32.is_lt(code,181))
    case False{}: categoryBelow126(code,U32.is_lt(code,1488))
def categoryBelow128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow128(code,U32.is_lt(code,1791))
    case False{}: categoryBelow129(code,U32.is_lt(code,1806))
def categoryBelow131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow131(code,U32.is_lt(code,1809))
    case False{}: categoryBelow132(code,U32.is_lt(code,1840))
def categoryBelow134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow130(code,U32.is_lt(code,1792))
    case False{}: categoryBelow133(code,U32.is_lt(code,1810))
def categoryBelow135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow135(code,U32.is_lt(code,1958))
    case False{}: categoryBelow136(code,U32.is_lt(code,1970))
def categoryBelow138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow138(code,U32.is_lt(code,1994))
    case False{}: categoryBelow139(code,U32.is_lt(code,2036))
def categoryBelow141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow137(code,U32.is_lt(code,1969))
    case False{}: categoryBelow140(code,U32.is_lt(code,2027))
def categoryBelow142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow134(code,U32.is_lt(code,1808))
    case False{}: categoryBelow141(code,U32.is_lt(code,1984))
def categoryBelow143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow143(code,U32.is_lt(code,2039))
    case False{}: categoryBelow144(code,U32.is_lt(code,2043))
def categoryBelow146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow146(code,U32.is_lt(code,2048))
    case False{}: categoryBelow147(code,U32.is_lt(code,2074))
def categoryBelow149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow145(code,U32.is_lt(code,2042))
    case False{}: categoryBelow148(code,U32.is_lt(code,2070))
def categoryBelow150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow150(code,U32.is_lt(code,2084))
    case False{}: categoryBelow151(code,U32.is_lt(code,2088))
def categoryBelow153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow153(code,U32.is_lt(code,2096))
    case False{}: categoryBelow154(code,U32.is_lt(code,2112))
def categoryBelow156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow152(code,U32.is_lt(code,2085))
    case False{}: categoryBelow155(code,U32.is_lt(code,2111))
def categoryBelow157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow149(code,U32.is_lt(code,2046))
    case False{}: categoryBelow156(code,U32.is_lt(code,2089))
def categoryBelow158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow142(code,U32.is_lt(code,1869))
    case False{}: categoryBelow157(code,U32.is_lt(code,2075))
def categoryBelow159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow159(code,U32.is_lt(code,2142))
    case False{}: categoryBelow160(code,U32.is_lt(code,2144))
def categoryBelow162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow162(code,U32.is_lt(code,2160))
    case False{}: categoryBelow163(code,U32.is_lt(code,2185))
def categoryBelow165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow161(code,U32.is_lt(code,2143))
    case False{}: categoryBelow164(code,U32.is_lt(code,2184))
def categoryBelow166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow166(code,U32.is_lt(code,2208))
    case False{}: categoryBelow167(code,U32.is_lt(code,2308))
def categoryBelow169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow169(code,U32.is_lt(code,2365))
    case False{}: categoryBelow170(code,U32.is_lt(code,2384))
def categoryBelow172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow168(code,U32.is_lt(code,2250))
    case False{}: categoryBelow171(code,U32.is_lt(code,2366))
def categoryBelow173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow165(code,U32.is_lt(code,2155))
    case False{}: categoryBelow172(code,U32.is_lt(code,2362))
def categoryBelow174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow174(code,U32.is_lt(code,2392))
    case False{}: categoryBelow175(code,U32.is_lt(code,2404))
def categoryBelow177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow177(code,U32.is_lt(code,2416))
    case False{}: categoryBelow178(code,U32.is_lt(code,2433))
def categoryBelow180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow176(code,U32.is_lt(code,2402))
    case False{}: categoryBelow179(code,U32.is_lt(code,2417))
def categoryBelow181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow181(code,U32.is_lt(code,2445))
    case False{}: categoryBelow182(code,U32.is_lt(code,2449))
def categoryBelow184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow185(code,U32.is_lt(code,2482))
def categoryBelow187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow184(code,U32.is_lt(code,2473))
    case False{}: categoryBelow186(code,U32.is_lt(code,2481))
def categoryBelow188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow183(code,U32.is_lt(code,2447))
    case False{}: categoryBelow187(code,U32.is_lt(code,2474))
def categoryBelow189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow180(code,U32.is_lt(code,2406))
    case False{}: categoryBelow188(code,U32.is_lt(code,2451))
def categoryBelow190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow173(code,U32.is_lt(code,2192))
    case False{}: categoryBelow189(code,U32.is_lt(code,2437))
def categoryBelow191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow158(code,U32.is_lt(code,2038))
    case False{}: categoryBelow190(code,U32.is_lt(code,2385))
def categoryBelow192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow192(code,U32.is_lt(code,2486))
    case False{}: categoryBelow193(code,U32.is_lt(code,2493))
def categoryBelow195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow195(code,U32.is_lt(code,2510))
    case False{}: categoryBelow196(code,U32.is_lt(code,2524))
def categoryBelow198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow194(code,U32.is_lt(code,2490))
    case False{}: categoryBelow197(code,U32.is_lt(code,2511))
def categoryBelow199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow199(code,U32.is_lt(code,2527))
    case False{}: categoryBelow200(code,U32.is_lt(code,2534))
def categoryBelow202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow202(code,U32.is_lt(code,2546))
    case False{}: categoryBelow203(code,U32.is_lt(code,2554))
def categoryBelow205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow201(code,U32.is_lt(code,2530))
    case False{}: categoryBelow204(code,U32.is_lt(code,2548))
def categoryBelow206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow198(code,U32.is_lt(code,2494))
    case False{}: categoryBelow205(code,U32.is_lt(code,2544))
def categoryBelow207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow207(code,U32.is_lt(code,2557))
    case False{}: categoryBelow208(code,U32.is_lt(code,2565))
def categoryBelow210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow210(code,U32.is_lt(code,2575))
    case False{}: categoryBelow211(code,U32.is_lt(code,2579))
def categoryBelow213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow209(code,U32.is_lt(code,2558))
    case False{}: categoryBelow212(code,U32.is_lt(code,2577))
def categoryBelow214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow214(code,U32.is_lt(code,2602))
    case False{}: categoryBelow215(code,U32.is_lt(code,2610))
def categoryBelow217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow217(code,U32.is_lt(code,2613))
    case False{}: categoryBelow218(code,U32.is_lt(code,2616))
def categoryBelow220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow216(code,U32.is_lt(code,2609))
    case False{}: categoryBelow219(code,U32.is_lt(code,2615))
def categoryBelow221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow213(code,U32.is_lt(code,2571))
    case False{}: categoryBelow220(code,U32.is_lt(code,2612))
def categoryBelow222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow206(code,U32.is_lt(code,2526))
    case False{}: categoryBelow221(code,U32.is_lt(code,2601))
def categoryBelow223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow223(code,U32.is_lt(code,2649))
    case False{}: categoryBelow224(code,U32.is_lt(code,2654))
def categoryBelow226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow226(code,U32.is_lt(code,2662))
    case False{}: categoryBelow227(code,U32.is_lt(code,2674))
def categoryBelow229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow225(code,U32.is_lt(code,2653))
    case False{}: categoryBelow228(code,U32.is_lt(code,2672))
def categoryBelow230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow230(code,U32.is_lt(code,2678))
    case False{}: categoryBelow231(code,U32.is_lt(code,2693))
def categoryBelow233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow233(code,U32.is_lt(code,2703))
    case False{}: categoryBelow234(code,U32.is_lt(code,2707))
def categoryBelow236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow232(code,U32.is_lt(code,2679))
    case False{}: categoryBelow235(code,U32.is_lt(code,2706))
def categoryBelow237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow229(code,U32.is_lt(code,2655))
    case False{}: categoryBelow236(code,U32.is_lt(code,2702))
def categoryBelow238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow238(code,U32.is_lt(code,2730))
    case False{}: categoryBelow239(code,U32.is_lt(code,2738))
def categoryBelow241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow241(code,U32.is_lt(code,2741))
    case False{}: categoryBelow242(code,U32.is_lt(code,2749))
def categoryBelow244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow240(code,U32.is_lt(code,2737))
    case False{}: categoryBelow243(code,U32.is_lt(code,2746))
def categoryBelow245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow245(code,U32.is_lt(code,2768))
    case False{}: categoryBelow246(code,U32.is_lt(code,2784))
def categoryBelow248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: categoryBelow249(code,U32.is_lt(code,2802))
def categoryBelow251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow248(code,U32.is_lt(code,2790))
    case False{}: categoryBelow250(code,U32.is_lt(code,2801))
def categoryBelow252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow247(code,U32.is_lt(code,2769))
    case False{}: categoryBelow251(code,U32.is_lt(code,2800))
def categoryBelow253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow244(code,U32.is_lt(code,2740))
    case False{}: categoryBelow252(code,U32.is_lt(code,2786))
def categoryBelow254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow237(code,U32.is_lt(code,2677))
    case False{}: categoryBelow253(code,U32.is_lt(code,2750))
def categoryBelow255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow222(code,U32.is_lt(code,2556))
    case False{}: categoryBelow254(code,U32.is_lt(code,2729))
def categoryBelow256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow191(code,U32.is_lt(code,2137))
    case False{}: categoryBelow255(code,U32.is_lt(code,2618))
def categoryBelow257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow127(code,U32.is_lt(code,902))
    case False{}: categoryBelow256(code,U32.is_lt(code,2483))
def categoryBelow258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow258(code,U32.is_lt(code,2810))
    case False{}: categoryBelow259(code,U32.is_lt(code,2829))
def categoryBelow261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow261(code,U32.is_lt(code,2833))
    case False{}: categoryBelow262(code,U32.is_lt(code,2857))
def categoryBelow264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow260(code,U32.is_lt(code,2821))
    case False{}: categoryBelow263(code,U32.is_lt(code,2835))
def categoryBelow265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow265(code,U32.is_lt(code,2865))
    case False{}: categoryBelow266(code,U32.is_lt(code,2868))
def categoryBelow268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow268(code,U32.is_lt(code,2874))
    case False{}: categoryBelow269(code,U32.is_lt(code,2878))
def categoryBelow271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow267(code,U32.is_lt(code,2866))
    case False{}: categoryBelow270(code,U32.is_lt(code,2877))
def categoryBelow272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow264(code,U32.is_lt(code,2831))
    case False{}: categoryBelow271(code,U32.is_lt(code,2869))
def categoryBelow273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow273(code,U32.is_lt(code,2910))
    case False{}: categoryBelow274(code,U32.is_lt(code,2914))
def categoryBelow276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow276(code,U32.is_lt(code,2928))
    case False{}: categoryBelow277(code,U32.is_lt(code,2930))
def categoryBelow279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow275(code,U32.is_lt(code,2911))
    case False{}: categoryBelow278(code,U32.is_lt(code,2929))
def categoryBelow280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow280(code,U32.is_lt(code,2947))
    case False{}: categoryBelow281(code,U32.is_lt(code,2949))
def categoryBelow283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow283(code,U32.is_lt(code,2958))
    case False{}: categoryBelow284(code,U32.is_lt(code,2962))
def categoryBelow286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow282(code,U32.is_lt(code,2948))
    case False{}: categoryBelow285(code,U32.is_lt(code,2961))
def categoryBelow287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow279(code,U32.is_lt(code,2918))
    case False{}: categoryBelow286(code,U32.is_lt(code,2955))
def categoryBelow288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow272(code,U32.is_lt(code,2858))
    case False{}: categoryBelow287(code,U32.is_lt(code,2936))
def categoryBelow289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow289(code,U32.is_lt(code,2969))
    case False{}: categoryBelow290(code,U32.is_lt(code,2972))
def categoryBelow292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow292(code,U32.is_lt(code,2974))
    case False{}: categoryBelow293(code,U32.is_lt(code,2979))
def categoryBelow295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow291(code,U32.is_lt(code,2971))
    case False{}: categoryBelow294(code,U32.is_lt(code,2976))
def categoryBelow296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow296(code,U32.is_lt(code,2984))
    case False{}: categoryBelow297(code,U32.is_lt(code,2990))
def categoryBelow299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow299(code,U32.is_lt(code,3024))
    case False{}: categoryBelow300(code,U32.is_lt(code,3046))
def categoryBelow302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow298(code,U32.is_lt(code,2987))
    case False{}: categoryBelow301(code,U32.is_lt(code,3025))
def categoryBelow303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow295(code,U32.is_lt(code,2973))
    case False{}: categoryBelow302(code,U32.is_lt(code,3002))
def categoryBelow304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow304(code,U32.is_lt(code,3067))
    case False{}: categoryBelow305(code,U32.is_lt(code,3085))
def categoryBelow307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow307(code,U32.is_lt(code,3089))
    case False{}: categoryBelow308(code,U32.is_lt(code,3113))
def categoryBelow310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow306(code,U32.is_lt(code,3077))
    case False{}: categoryBelow309(code,U32.is_lt(code,3090))
def categoryBelow311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow311(code,U32.is_lt(code,3130))
    case False{}: categoryBelow312(code,U32.is_lt(code,3134))
def categoryBelow314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow314(code,U32.is_lt(code,3163))
    case False{}: categoryBelow315(code,U32.is_lt(code,3166))
def categoryBelow317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow313(code,U32.is_lt(code,3133))
    case False{}: categoryBelow316(code,U32.is_lt(code,3164))
def categoryBelow318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow310(code,U32.is_lt(code,3086))
    case False{}: categoryBelow317(code,U32.is_lt(code,3160))
def categoryBelow319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow303(code,U32.is_lt(code,2981))
    case False{}: categoryBelow318(code,U32.is_lt(code,3114))
def categoryBelow320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow288(code,U32.is_lt(code,2908))
    case False{}: categoryBelow319(code,U32.is_lt(code,3059))
def categoryBelow321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow321(code,U32.is_lt(code,3170))
    case False{}: categoryBelow322(code,U32.is_lt(code,3184))
def categoryBelow324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow324(code,U32.is_lt(code,3192))
    case False{}: categoryBelow325(code,U32.is_lt(code,3200))
def categoryBelow327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow323(code,U32.is_lt(code,3174))
    case False{}: categoryBelow326(code,U32.is_lt(code,3199))
def categoryBelow328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow328(code,U32.is_lt(code,3204))
    case False{}: categoryBelow329(code,U32.is_lt(code,3213))
def categoryBelow331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow331(code,U32.is_lt(code,3217))
    case False{}: categoryBelow332(code,U32.is_lt(code,3241))
def categoryBelow334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow330(code,U32.is_lt(code,3205))
    case False{}: categoryBelow333(code,U32.is_lt(code,3218))
def categoryBelow335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow327(code,U32.is_lt(code,3191))
    case False{}: categoryBelow334(code,U32.is_lt(code,3214))
def categoryBelow336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow336(code,U32.is_lt(code,3252))
    case False{}: categoryBelow337(code,U32.is_lt(code,3258))
def categoryBelow339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow339(code,U32.is_lt(code,3262))
    case False{}: categoryBelow340(code,U32.is_lt(code,3295))
def categoryBelow342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow338(code,U32.is_lt(code,3253))
    case False{}: categoryBelow341(code,U32.is_lt(code,3292))
def categoryBelow343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow343(code,U32.is_lt(code,3298))
    case False{}: categoryBelow344(code,U32.is_lt(code,3312))
def categoryBelow346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow346(code,U32.is_lt(code,3315))
    case False{}: categoryBelow347(code,U32.is_lt(code,3341))
def categoryBelow349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow345(code,U32.is_lt(code,3302))
    case False{}: categoryBelow348(code,U32.is_lt(code,3332))
def categoryBelow350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow342(code,U32.is_lt(code,3261))
    case False{}: categoryBelow349(code,U32.is_lt(code,3313))
def categoryBelow351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow335(code,U32.is_lt(code,3201))
    case False{}: categoryBelow350(code,U32.is_lt(code,3296))
def categoryBelow352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow352(code,U32.is_lt(code,3345))
    case False{}: categoryBelow353(code,U32.is_lt(code,3387))
def categoryBelow355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow355(code,U32.is_lt(code,3390))
    case False{}: categoryBelow356(code,U32.is_lt(code,3407))
def categoryBelow358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow354(code,U32.is_lt(code,3346))
    case False{}: categoryBelow357(code,U32.is_lt(code,3406))
def categoryBelow359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow359(code,U32.is_lt(code,3412))
    case False{}: categoryBelow360(code,U32.is_lt(code,3416))
def categoryBelow362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow362(code,U32.is_lt(code,3426))
    case False{}: categoryBelow363(code,U32.is_lt(code,3449))
def categoryBelow365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow361(code,U32.is_lt(code,3415))
    case False{}: categoryBelow364(code,U32.is_lt(code,3430))
def categoryBelow366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow358(code,U32.is_lt(code,3389))
    case False{}: categoryBelow365(code,U32.is_lt(code,3423))
def categoryBelow367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow367(code,U32.is_lt(code,3456))
    case False{}: categoryBelow368(code,U32.is_lt(code,3479))
def categoryBelow370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow370(code,U32.is_lt(code,3506))
    case False{}: categoryBelow371(code,U32.is_lt(code,3516))
def categoryBelow373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow369(code,U32.is_lt(code,3461))
    case False{}: categoryBelow372(code,U32.is_lt(code,3507))
def categoryBelow374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow374(code,U32.is_lt(code,3518))
    case False{}: categoryBelow375(code,U32.is_lt(code,3527))
def categoryBelow377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: categoryBelow378(code,U32.is_lt(code,3585))
def categoryBelow380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow377(code,U32.is_lt(code,3568))
    case False{}: categoryBelow379(code,U32.is_lt(code,3573))
def categoryBelow381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow376(code,U32.is_lt(code,3520))
    case False{}: categoryBelow380(code,U32.is_lt(code,3572))
def categoryBelow382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow373(code,U32.is_lt(code,3482))
    case False{}: categoryBelow381(code,U32.is_lt(code,3558))
def categoryBelow383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow366(code,U32.is_lt(code,3408))
    case False{}: categoryBelow382(code,U32.is_lt(code,3517))
def categoryBelow384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow351(code,U32.is_lt(code,3242))
    case False{}: categoryBelow383(code,U32.is_lt(code,3450))
def categoryBelow385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow320(code,U32.is_lt(code,2966))
    case False{}: categoryBelow384(code,U32.is_lt(code,3342))
def categoryBelow386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow386(code,U32.is_lt(code,3634))
    case False{}: categoryBelow387(code,U32.is_lt(code,3647))
def categoryBelow389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow389(code,U32.is_lt(code,3655))
    case False{}: categoryBelow390(code,U32.is_lt(code,3664))
def categoryBelow392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow388(code,U32.is_lt(code,3636))
    case False{}: categoryBelow391(code,U32.is_lt(code,3663))
def categoryBelow393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow393(code,U32.is_lt(code,3676))
    case False{}: categoryBelow394(code,U32.is_lt(code,3715))
def categoryBelow396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow396(code,U32.is_lt(code,3717))
    case False{}: categoryBelow397(code,U32.is_lt(code,3723))
def categoryBelow399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow395(code,U32.is_lt(code,3713))
    case False{}: categoryBelow398(code,U32.is_lt(code,3718))
def categoryBelow400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow392(code,U32.is_lt(code,3648))
    case False{}: categoryBelow399(code,U32.is_lt(code,3716))
def categoryBelow401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow401(code,U32.is_lt(code,3748))
    case False{}: categoryBelow402(code,U32.is_lt(code,3750))
def categoryBelow404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow404(code,U32.is_lt(code,3761))
    case False{}: categoryBelow405(code,U32.is_lt(code,3764))
def categoryBelow407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow403(code,U32.is_lt(code,3749))
    case False{}: categoryBelow406(code,U32.is_lt(code,3762))
def categoryBelow408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow408(code,U32.is_lt(code,3774))
    case False{}: categoryBelow409(code,U32.is_lt(code,3781))
def categoryBelow411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow411(code,U32.is_lt(code,3783))
    case False{}: categoryBelow412(code,U32.is_lt(code,3802))
def categoryBelow414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow410(code,U32.is_lt(code,3776))
    case False{}: categoryBelow413(code,U32.is_lt(code,3792))
def categoryBelow415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow407(code,U32.is_lt(code,3751))
    case False{}: categoryBelow414(code,U32.is_lt(code,3782))
def categoryBelow416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow400(code,U32.is_lt(code,3674))
    case False{}: categoryBelow415(code,U32.is_lt(code,3773))
def categoryBelow417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow417(code,U32.is_lt(code,3808))
    case False{}: categoryBelow418(code,U32.is_lt(code,3841))
def categoryBelow420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow420(code,U32.is_lt(code,3859))
    case False{}: categoryBelow421(code,U32.is_lt(code,3861))
def categoryBelow423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow419(code,U32.is_lt(code,3840))
    case False{}: categoryBelow422(code,U32.is_lt(code,3860))
def categoryBelow424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow424(code,U32.is_lt(code,3866))
    case False{}: categoryBelow425(code,U32.is_lt(code,3892))
def categoryBelow427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow427(code,U32.is_lt(code,3894))
    case False{}: categoryBelow428(code,U32.is_lt(code,3896))
def categoryBelow430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow426(code,U32.is_lt(code,3872))
    case False{}: categoryBelow429(code,U32.is_lt(code,3895))
def categoryBelow431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow423(code,U32.is_lt(code,3844))
    case False{}: categoryBelow430(code,U32.is_lt(code,3893))
def categoryBelow432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow432(code,U32.is_lt(code,3898))
    case False{}: categoryBelow433(code,U32.is_lt(code,3904))
def categoryBelow435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow435(code,U32.is_lt(code,3913))
    case False{}: categoryBelow436(code,U32.is_lt(code,3973))
def categoryBelow438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow434(code,U32.is_lt(code,3902))
    case False{}: categoryBelow437(code,U32.is_lt(code,3949))
def categoryBelow439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow439(code,U32.is_lt(code,3976))
    case False{}: categoryBelow440(code,U32.is_lt(code,4030))
def categoryBelow442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow443(code,U32.is_lt(code,4048))
def categoryBelow445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow442(code,U32.is_lt(code,4039))
    case False{}: categoryBelow444(code,U32.is_lt(code,4046))
def categoryBelow446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow441(code,U32.is_lt(code,3981))
    case False{}: categoryBelow445(code,U32.is_lt(code,4045))
def categoryBelow447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow438(code,U32.is_lt(code,3912))
    case False{}: categoryBelow446(code,U32.is_lt(code,4038))
def categoryBelow448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow431(code,U32.is_lt(code,3864))
    case False{}: categoryBelow447(code,U32.is_lt(code,3974))
def categoryBelow449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow416(code,U32.is_lt(code,3724))
    case False{}: categoryBelow448(code,U32.is_lt(code,3897))
def categoryBelow450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow450(code,U32.is_lt(code,4057))
    case False{}: categoryBelow451(code,U32.is_lt(code,4096))
def categoryBelow453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow453(code,U32.is_lt(code,4159))
    case False{}: categoryBelow454(code,U32.is_lt(code,4170))
def categoryBelow456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow452(code,U32.is_lt(code,4059))
    case False{}: categoryBelow455(code,U32.is_lt(code,4160))
def categoryBelow457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow457(code,U32.is_lt(code,4182))
    case False{}: categoryBelow458(code,U32.is_lt(code,4190))
def categoryBelow460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow460(code,U32.is_lt(code,4194))
    case False{}: categoryBelow461(code,U32.is_lt(code,4199))
def categoryBelow463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow459(code,U32.is_lt(code,4186))
    case False{}: categoryBelow462(code,U32.is_lt(code,4197))
def categoryBelow464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow456(code,U32.is_lt(code,4139))
    case False{}: categoryBelow463(code,U32.is_lt(code,4193))
def categoryBelow465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow465(code,U32.is_lt(code,4209))
    case False{}: categoryBelow466(code,U32.is_lt(code,4226))
def categoryBelow468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow468(code,U32.is_lt(code,4239))
    case False{}: categoryBelow469(code,U32.is_lt(code,4250))
def categoryBelow471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow467(code,U32.is_lt(code,4213))
    case False{}: categoryBelow470(code,U32.is_lt(code,4240))
def categoryBelow472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow472(code,U32.is_lt(code,4256))
    case False{}: categoryBelow473(code,U32.is_lt(code,4295))
def categoryBelow475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow475(code,U32.is_lt(code,4301))
    case False{}: categoryBelow476(code,U32.is_lt(code,4304))
def categoryBelow478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow474(code,U32.is_lt(code,4294))
    case False{}: categoryBelow477(code,U32.is_lt(code,4302))
def categoryBelow479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow471(code,U32.is_lt(code,4238))
    case False{}: categoryBelow478(code,U32.is_lt(code,4296))
def categoryBelow480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow464(code,U32.is_lt(code,4176))
    case False{}: categoryBelow479(code,U32.is_lt(code,4254))
def categoryBelow481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow481(code,U32.is_lt(code,4348))
    case False{}: categoryBelow482(code,U32.is_lt(code,4682))
def categoryBelow484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow484(code,U32.is_lt(code,4688))
    case False{}: categoryBelow485(code,U32.is_lt(code,4696))
def categoryBelow487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow483(code,U32.is_lt(code,4681))
    case False{}: categoryBelow486(code,U32.is_lt(code,4695))
def categoryBelow488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow488(code,U32.is_lt(code,4698))
    case False{}: categoryBelow489(code,U32.is_lt(code,4704))
def categoryBelow491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow491(code,U32.is_lt(code,4746))
    case False{}: categoryBelow492(code,U32.is_lt(code,4752))
def categoryBelow494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow490(code,U32.is_lt(code,4702))
    case False{}: categoryBelow493(code,U32.is_lt(code,4750))
def categoryBelow495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow487(code,U32.is_lt(code,4686))
    case False{}: categoryBelow494(code,U32.is_lt(code,4745))
def categoryBelow496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow496(code,U32.is_lt(code,4786))
    case False{}: categoryBelow497(code,U32.is_lt(code,4792))
def categoryBelow499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow499(code,U32.is_lt(code,4800))
    case False{}: categoryBelow500(code,U32.is_lt(code,4802))
def categoryBelow502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow498(code,U32.is_lt(code,4790))
    case False{}: categoryBelow501(code,U32.is_lt(code,4801))
def categoryBelow503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow503(code,U32.is_lt(code,4808))
    case False{}: categoryBelow504(code,U32.is_lt(code,4824))
def categoryBelow506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow507(code,U32.is_lt(code,4955))
def categoryBelow509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow506(code,U32.is_lt(code,4882))
    case False{}: categoryBelow508(code,U32.is_lt(code,4888))
def categoryBelow510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow505(code,U32.is_lt(code,4823))
    case False{}: categoryBelow509(code,U32.is_lt(code,4886))
def categoryBelow511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow502(code,U32.is_lt(code,4799))
    case False{}: categoryBelow510(code,U32.is_lt(code,4881))
def categoryBelow512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow495(code,U32.is_lt(code,4697))
    case False{}: categoryBelow511(code,U32.is_lt(code,4806))
def categoryBelow513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow480(code,U32.is_lt(code,4206))
    case False{}: categoryBelow512(code,U32.is_lt(code,4785))
def categoryBelow514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow449(code,U32.is_lt(code,3804))
    case False{}: categoryBelow513(code,U32.is_lt(code,4347))
def categoryBelow515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow385(code,U32.is_lt(code,3168))
    case False{}: categoryBelow514(code,U32.is_lt(code,4053))
def categoryBelow516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow257(code,U32.is_lt(code,1789))
    case False{}: categoryBelow515(code,U32.is_lt(code,3633))
def categoryBelow517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow517(code,U32.is_lt(code,4969))
    case False{}: categoryBelow518(code,U32.is_lt(code,4992))
def categoryBelow520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow520(code,U32.is_lt(code,5018))
    case False{}: categoryBelow521(code,U32.is_lt(code,5110))
def categoryBelow523(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow519(code,U32.is_lt(code,4989))
    case False{}: categoryBelow522(code,U32.is_lt(code,5024))
def categoryBelow524(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow525(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow526(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow524(code,U32.is_lt(code,5118))
    case False{}: categoryBelow525(code,U32.is_lt(code,5121))
def categoryBelow527(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow528(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow529(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow527(code,U32.is_lt(code,5742))
    case False{}: categoryBelow528(code,U32.is_lt(code,5760))
def categoryBelow530(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow526(code,U32.is_lt(code,5120))
    case False{}: categoryBelow529(code,U32.is_lt(code,5743))
def categoryBelow531(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow523(code,U32.is_lt(code,5008))
    case False{}: categoryBelow530(code,U32.is_lt(code,5741))
def categoryBelow532(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow533(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow534(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow532(code,U32.is_lt(code,5787))
    case False{}: categoryBelow533(code,U32.is_lt(code,5792))
def categoryBelow535(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow536(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow537(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow535(code,U32.is_lt(code,5870))
    case False{}: categoryBelow536(code,U32.is_lt(code,5881))
def categoryBelow538(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow534(code,U32.is_lt(code,5789))
    case False{}: categoryBelow537(code,U32.is_lt(code,5873))
def categoryBelow539(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow540(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow541(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow539(code,U32.is_lt(code,5906))
    case False{}: categoryBelow540(code,U32.is_lt(code,5938))
def categoryBelow542(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow543(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow544(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow542(code,U32.is_lt(code,5943))
    case False{}: categoryBelow543(code,U32.is_lt(code,5970))
def categoryBelow545(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow541(code,U32.is_lt(code,5919))
    case False{}: categoryBelow544(code,U32.is_lt(code,5952))
def categoryBelow546(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow538(code,U32.is_lt(code,5867))
    case False{}: categoryBelow545(code,U32.is_lt(code,5941))
def categoryBelow547(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow531(code,U32.is_lt(code,5112))
    case False{}: categoryBelow546(code,U32.is_lt(code,5888))
def categoryBelow548(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow549(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow550(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow548(code,U32.is_lt(code,5997))
    case False{}: categoryBelow549(code,U32.is_lt(code,6001))
def categoryBelow551(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow552(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow553(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow551(code,U32.is_lt(code,6068))
    case False{}: categoryBelow552(code,U32.is_lt(code,6103))
def categoryBelow554(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow550(code,U32.is_lt(code,5998))
    case False{}: categoryBelow553(code,U32.is_lt(code,6100))
def categoryBelow555(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow556(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow557(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow555(code,U32.is_lt(code,6107))
    case False{}: categoryBelow556(code,U32.is_lt(code,6109))
def categoryBelow558(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow559(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow560(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow558(code,U32.is_lt(code,6122))
    case False{}: categoryBelow559(code,U32.is_lt(code,6138))
def categoryBelow561(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow557(code,U32.is_lt(code,6108))
    case False{}: categoryBelow560(code,U32.is_lt(code,6128))
def categoryBelow562(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow554(code,U32.is_lt(code,6016))
    case False{}: categoryBelow561(code,U32.is_lt(code,6112))
def categoryBelow563(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow564(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow565(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow563(code,U32.is_lt(code,6155))
    case False{}: categoryBelow564(code,U32.is_lt(code,6170))
def categoryBelow566(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow567(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow568(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow566(code,U32.is_lt(code,6265))
    case False{}: categoryBelow567(code,U32.is_lt(code,6277))
def categoryBelow569(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow565(code,U32.is_lt(code,6160))
    case False{}: categoryBelow568(code,U32.is_lt(code,6272))
def categoryBelow570(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow571(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow572(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow570(code,U32.is_lt(code,6313))
    case False{}: categoryBelow571(code,U32.is_lt(code,6315))
def categoryBelow573(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow574(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow575(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow573(code,U32.is_lt(code,6390))
    case False{}: categoryBelow574(code,U32.is_lt(code,6431))
def categoryBelow576(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow572(code,U32.is_lt(code,6314))
    case False{}: categoryBelow575(code,U32.is_lt(code,6400))
def categoryBelow577(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow569(code,U32.is_lt(code,6176))
    case False{}: categoryBelow576(code,U32.is_lt(code,6320))
def categoryBelow578(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow562(code,U32.is_lt(code,6104))
    case False{}: categoryBelow577(code,U32.is_lt(code,6279))
def categoryBelow579(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow547(code,U32.is_lt(code,5761))
    case False{}: categoryBelow578(code,U32.is_lt(code,6144))
def categoryBelow580(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow581(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow582(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow580(code,U32.is_lt(code,6465))
    case False{}: categoryBelow581(code,U32.is_lt(code,6470))
def categoryBelow583(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow584(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow585(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow583(code,U32.is_lt(code,6510))
    case False{}: categoryBelow584(code,U32.is_lt(code,6517))
def categoryBelow586(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow582(code,U32.is_lt(code,6468))
    case False{}: categoryBelow585(code,U32.is_lt(code,6512))
def categoryBelow587(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow588(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow589(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow587(code,U32.is_lt(code,6572))
    case False{}: categoryBelow588(code,U32.is_lt(code,6602))
def categoryBelow590(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow591(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow592(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow590(code,U32.is_lt(code,6619))
    case False{}: categoryBelow591(code,U32.is_lt(code,6656))
def categoryBelow593(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow589(code,U32.is_lt(code,6576))
    case False{}: categoryBelow592(code,U32.is_lt(code,6622))
def categoryBelow594(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow586(code,U32.is_lt(code,6480))
    case False{}: categoryBelow593(code,U32.is_lt(code,6608))
def categoryBelow595(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow596(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow597(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow595(code,U32.is_lt(code,6686))
    case False{}: categoryBelow596(code,U32.is_lt(code,6741))
def categoryBelow598(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow599(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow600(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow598(code,U32.is_lt(code,6794))
    case False{}: categoryBelow599(code,U32.is_lt(code,6810))
def categoryBelow601(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow597(code,U32.is_lt(code,6688))
    case False{}: categoryBelow600(code,U32.is_lt(code,6800))
def categoryBelow602(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow603(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow604(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow602(code,U32.is_lt(code,6823))
    case False{}: categoryBelow603(code,U32.is_lt(code,6830))
def categoryBelow605(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow606(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow607(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow605(code,U32.is_lt(code,6964))
    case False{}: categoryBelow606(code,U32.is_lt(code,6989))
def categoryBelow608(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow604(code,U32.is_lt(code,6824))
    case False{}: categoryBelow607(code,U32.is_lt(code,6981))
def categoryBelow609(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow601(code,U32.is_lt(code,6784))
    case False{}: categoryBelow608(code,U32.is_lt(code,6917))
def categoryBelow610(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow594(code,U32.is_lt(code,6528))
    case False{}: categoryBelow609(code,U32.is_lt(code,6816))
def categoryBelow611(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow612(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow613(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow611(code,U32.is_lt(code,6992))
    case False{}: categoryBelow612(code,U32.is_lt(code,7009))
def categoryBelow614(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow615(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow616(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow614(code,U32.is_lt(code,7028))
    case False{}: categoryBelow615(code,U32.is_lt(code,7040))
def categoryBelow617(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow613(code,U32.is_lt(code,7002))
    case False{}: categoryBelow616(code,U32.is_lt(code,7037))
def categoryBelow618(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow619(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow620(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow618(code,U32.is_lt(code,7073))
    case False{}: categoryBelow619(code,U32.is_lt(code,7088))
def categoryBelow621(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow622(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow623(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow621(code,U32.is_lt(code,7142))
    case False{}: categoryBelow622(code,U32.is_lt(code,7168))
def categoryBelow624(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow620(code,U32.is_lt(code,7086))
    case False{}: categoryBelow623(code,U32.is_lt(code,7164))
def categoryBelow625(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow617(code,U32.is_lt(code,7019))
    case False{}: categoryBelow624(code,U32.is_lt(code,7098))
def categoryBelow626(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow627(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow628(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow626(code,U32.is_lt(code,7227))
    case False{}: categoryBelow627(code,U32.is_lt(code,7242))
def categoryBelow629(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow630(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow631(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow629(code,U32.is_lt(code,7248))
    case False{}: categoryBelow630(code,U32.is_lt(code,7294))
def categoryBelow632(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow628(code,U32.is_lt(code,7232))
    case False{}: categoryBelow631(code,U32.is_lt(code,7258))
def categoryBelow633(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow634(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow635(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow633(code,U32.is_lt(code,7307))
    case False{}: categoryBelow634(code,U32.is_lt(code,7355))
def categoryBelow636(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow637(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow638(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow637(code,U32.is_lt(code,7380))
def categoryBelow639(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow636(code,U32.is_lt(code,7360))
    case False{}: categoryBelow638(code,U32.is_lt(code,7379))
def categoryBelow640(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow635(code,U32.is_lt(code,7312))
    case False{}: categoryBelow639(code,U32.is_lt(code,7368))
def categoryBelow641(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow632(code,U32.is_lt(code,7245))
    case False{}: categoryBelow640(code,U32.is_lt(code,7357))
def categoryBelow642(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow625(code,U32.is_lt(code,7043))
    case False{}: categoryBelow641(code,U32.is_lt(code,7296))
def categoryBelow643(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow610(code,U32.is_lt(code,6679))
    case False{}: categoryBelow642(code,U32.is_lt(code,7204))
def categoryBelow644(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow579(code,U32.is_lt(code,5984))
    case False{}: categoryBelow643(code,U32.is_lt(code,6990))
def categoryBelow645(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow646(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow647(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow645(code,U32.is_lt(code,7405))
    case False{}: categoryBelow646(code,U32.is_lt(code,7412))
def categoryBelow648(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow649(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow650(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow648(code,U32.is_lt(code,7415))
    case False{}: categoryBelow649(code,U32.is_lt(code,7419))
def categoryBelow651(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow647(code,U32.is_lt(code,7406))
    case False{}: categoryBelow650(code,U32.is_lt(code,7418))
def categoryBelow652(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow653(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow654(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow652(code,U32.is_lt(code,7616))
    case False{}: categoryBelow653(code,U32.is_lt(code,7958))
def categoryBelow655(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow656(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow657(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow655(code,U32.is_lt(code,7966))
    case False{}: categoryBelow656(code,U32.is_lt(code,8006))
def categoryBelow658(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow654(code,U32.is_lt(code,7680))
    case False{}: categoryBelow657(code,U32.is_lt(code,7968))
def categoryBelow659(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow651(code,U32.is_lt(code,7413))
    case False{}: categoryBelow658(code,U32.is_lt(code,7960))
def categoryBelow660(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow661(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow662(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow660(code,U32.is_lt(code,8014))
    case False{}: categoryBelow661(code,U32.is_lt(code,8024))
def categoryBelow663(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow664(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow665(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow663(code,U32.is_lt(code,8026))
    case False{}: categoryBelow664(code,U32.is_lt(code,8028))
def categoryBelow666(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow662(code,U32.is_lt(code,8016))
    case False{}: categoryBelow665(code,U32.is_lt(code,8027))
def categoryBelow667(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow668(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow669(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow667(code,U32.is_lt(code,8030))
    case False{}: categoryBelow668(code,U32.is_lt(code,8062))
def categoryBelow670(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow671(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow672(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow670(code,U32.is_lt(code,8117))
    case False{}: categoryBelow671(code,U32.is_lt(code,8125))
def categoryBelow673(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow669(code,U32.is_lt(code,8031))
    case False{}: categoryBelow672(code,U32.is_lt(code,8118))
def categoryBelow674(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow666(code,U32.is_lt(code,8025))
    case False{}: categoryBelow673(code,U32.is_lt(code,8064))
def categoryBelow675(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow659(code,U32.is_lt(code,7424))
    case False{}: categoryBelow674(code,U32.is_lt(code,8029))
def categoryBelow676(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow677(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow678(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow676(code,U32.is_lt(code,8127))
    case False{}: categoryBelow677(code,U32.is_lt(code,8133))
def categoryBelow679(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow680(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow681(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow679(code,U32.is_lt(code,8141))
    case False{}: categoryBelow680(code,U32.is_lt(code,8148))
def categoryBelow682(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow678(code,U32.is_lt(code,8130))
    case False{}: categoryBelow681(code,U32.is_lt(code,8144))
def categoryBelow683(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow684(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow685(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow683(code,U32.is_lt(code,8156))
    case False{}: categoryBelow684(code,U32.is_lt(code,8160))
def categoryBelow686(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow687(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow688(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow686(code,U32.is_lt(code,8176))
    case False{}: categoryBelow687(code,U32.is_lt(code,8181))
def categoryBelow689(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow685(code,U32.is_lt(code,8157))
    case False{}: categoryBelow688(code,U32.is_lt(code,8178))
def categoryBelow690(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow682(code,U32.is_lt(code,8134))
    case False{}: categoryBelow689(code,U32.is_lt(code,8173))
def categoryBelow691(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow692(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow693(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow691(code,U32.is_lt(code,8189))
    case False{}: categoryBelow692(code,U32.is_lt(code,8208))
def categoryBelow694(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow695(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow696(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow694(code,U32.is_lt(code,8240))
    case False{}: categoryBelow695(code,U32.is_lt(code,8261))
def categoryBelow697(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow693(code,U32.is_lt(code,8191))
    case False{}: categoryBelow696(code,U32.is_lt(code,8260))
def categoryBelow698(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow699(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow700(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow698(code,U32.is_lt(code,8275))
    case False{}: categoryBelow699(code,U32.is_lt(code,8304))
def categoryBelow701(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow702(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow703(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow702(code,U32.is_lt(code,8317))
def categoryBelow704(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow701(code,U32.is_lt(code,8306))
    case False{}: categoryBelow703(code,U32.is_lt(code,8314))
def categoryBelow705(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow700(code,U32.is_lt(code,8287))
    case False{}: categoryBelow704(code,U32.is_lt(code,8308))
def categoryBelow706(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow697(code,U32.is_lt(code,8232))
    case False{}: categoryBelow705(code,U32.is_lt(code,8305))
def categoryBelow707(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow690(code,U32.is_lt(code,8150))
    case False{}: categoryBelow706(code,U32.is_lt(code,8274))
def categoryBelow708(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow675(code,U32.is_lt(code,8008))
    case False{}: categoryBelow707(code,U32.is_lt(code,8182))
def categoryBelow709(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow710(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow711(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow709(code,U32.is_lt(code,8320))
    case False{}: categoryBelow710(code,U32.is_lt(code,8333))
def categoryBelow712(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow713(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow714(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow712(code,U32.is_lt(code,8336))
    case False{}: categoryBelow713(code,U32.is_lt(code,8352))
def categoryBelow715(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow711(code,U32.is_lt(code,8330))
    case False{}: categoryBelow714(code,U32.is_lt(code,8349))
def categoryBelow716(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow717(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow718(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow716(code,U32.is_lt(code,8448))
    case False{}: categoryBelow717(code,U32.is_lt(code,8451))
def categoryBelow719(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow720(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow721(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow719(code,U32.is_lt(code,8456))
    case False{}: categoryBelow720(code,U32.is_lt(code,8468))
def categoryBelow722(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow718(code,U32.is_lt(code,8450))
    case False{}: categoryBelow721(code,U32.is_lt(code,8458))
def categoryBelow723(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow715(code,U32.is_lt(code,8335))
    case False{}: categoryBelow722(code,U32.is_lt(code,8455))
def categoryBelow724(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow725(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow726(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow724(code,U32.is_lt(code,8470))
    case False{}: categoryBelow725(code,U32.is_lt(code,8478))
def categoryBelow727(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow728(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow729(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow727(code,U32.is_lt(code,8485))
    case False{}: categoryBelow728(code,U32.is_lt(code,8487))
def categoryBelow730(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow726(code,U32.is_lt(code,8473))
    case False{}: categoryBelow729(code,U32.is_lt(code,8486))
def categoryBelow731(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow732(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow733(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow731(code,U32.is_lt(code,8489))
    case False{}: categoryBelow732(code,U32.is_lt(code,8494))
def categoryBelow734(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow735(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow736(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow734(code,U32.is_lt(code,8506))
    case False{}: categoryBelow735(code,U32.is_lt(code,8512))
def categoryBelow737(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow733(code,U32.is_lt(code,8490))
    case False{}: categoryBelow736(code,U32.is_lt(code,8508))
def categoryBelow738(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow730(code,U32.is_lt(code,8484))
    case False{}: categoryBelow737(code,U32.is_lt(code,8495))
def categoryBelow739(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow723(code,U32.is_lt(code,8386))
    case False{}: categoryBelow738(code,U32.is_lt(code,8488))
def categoryBelow740(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow741(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow742(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow740(code,U32.is_lt(code,8522))
    case False{}: categoryBelow741(code,U32.is_lt(code,8527))
def categoryBelow743(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow744(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow745(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow743(code,U32.is_lt(code,8579))
    case False{}: categoryBelow744(code,U32.is_lt(code,8586))
def categoryBelow746(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow742(code,U32.is_lt(code,8526))
    case False{}: categoryBelow745(code,U32.is_lt(code,8581))
def categoryBelow747(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow748(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow749(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow747(code,U32.is_lt(code,8592))
    case False{}: categoryBelow748(code,U32.is_lt(code,8972))
def categoryBelow750(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow751(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow752(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow750(code,U32.is_lt(code,9003))
    case False{}: categoryBelow751(code,U32.is_lt(code,9280))
def categoryBelow753(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow749(code,U32.is_lt(code,8968))
    case False{}: categoryBelow752(code,U32.is_lt(code,9258))
def categoryBelow754(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow746(code,U32.is_lt(code,8528))
    case False{}: categoryBelow753(code,U32.is_lt(code,9001))
def categoryBelow755(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow756(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 2
def categoryBelow757(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow755(code,U32.is_lt(code,9312))
    case False{}: categoryBelow756(code,U32.is_lt(code,9450))
def categoryBelow758(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow759(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow760(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow758(code,U32.is_lt(code,10088))
    case False{}: categoryBelow759(code,U32.is_lt(code,10132))
def categoryBelow761(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow757(code,U32.is_lt(code,9372))
    case False{}: categoryBelow760(code,U32.is_lt(code,10102))
def categoryBelow762(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow763(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow764(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow762(code,U32.is_lt(code,10183))
    case False{}: categoryBelow763(code,U32.is_lt(code,10224))
def categoryBelow765(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow766(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow767(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: categoryBelow766(code,U32.is_lt(code,10748))
def categoryBelow768(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow765(code,U32.is_lt(code,10649))
    case False{}: categoryBelow767(code,U32.is_lt(code,10716))
def categoryBelow769(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow764(code,U32.is_lt(code,10214))
    case False{}: categoryBelow768(code,U32.is_lt(code,10712))
def categoryBelow770(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow761(code,U32.is_lt(code,9472))
    case False{}: categoryBelow769(code,U32.is_lt(code,10627))
def categoryBelow771(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow754(code,U32.is_lt(code,8588))
    case False{}: categoryBelow770(code,U32.is_lt(code,10181))
def categoryBelow772(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow739(code,U32.is_lt(code,8469))
    case False{}: categoryBelow771(code,U32.is_lt(code,9291))
def categoryBelow773(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow708(code,U32.is_lt(code,8126))
    case False{}: categoryBelow772(code,U32.is_lt(code,8517))
def categoryBelow774(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow644(code,U32.is_lt(code,6464))
    case False{}: categoryBelow773(code,U32.is_lt(code,8319))
def categoryBelow775(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow776(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow777(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow775(code,U32.is_lt(code,11124))
    case False{}: categoryBelow776(code,U32.is_lt(code,11264))
def categoryBelow778(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow779(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow780(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow778(code,U32.is_lt(code,11499))
    case False{}: categoryBelow779(code,U32.is_lt(code,11506))
def categoryBelow781(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow777(code,U32.is_lt(code,11126))
    case False{}: categoryBelow780(code,U32.is_lt(code,11503))
def categoryBelow782(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow783(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow784(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow782(code,U32.is_lt(code,11513))
    case False{}: categoryBelow783(code,U32.is_lt(code,11518))
def categoryBelow785(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow786(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow787(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow785(code,U32.is_lt(code,11558))
    case False{}: categoryBelow786(code,U32.is_lt(code,11560))
def categoryBelow788(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow784(code,U32.is_lt(code,11517))
    case False{}: categoryBelow787(code,U32.is_lt(code,11559))
def categoryBelow789(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow781(code,U32.is_lt(code,11493))
    case False{}: categoryBelow788(code,U32.is_lt(code,11520))
def categoryBelow790(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow791(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow792(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow790(code,U32.is_lt(code,11566))
    case False{}: categoryBelow791(code,U32.is_lt(code,11624))
def categoryBelow793(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow794(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow795(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow793(code,U32.is_lt(code,11632))
    case False{}: categoryBelow794(code,U32.is_lt(code,11648))
def categoryBelow796(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow792(code,U32.is_lt(code,11568))
    case False{}: categoryBelow795(code,U32.is_lt(code,11633))
def categoryBelow797(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow798(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow799(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow797(code,U32.is_lt(code,11680))
    case False{}: categoryBelow798(code,U32.is_lt(code,11688))
def categoryBelow800(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow801(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow802(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow800(code,U32.is_lt(code,11696))
    case False{}: categoryBelow801(code,U32.is_lt(code,11704))
def categoryBelow803(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow799(code,U32.is_lt(code,11687))
    case False{}: categoryBelow802(code,U32.is_lt(code,11703))
def categoryBelow804(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow796(code,U32.is_lt(code,11631))
    case False{}: categoryBelow803(code,U32.is_lt(code,11695))
def categoryBelow805(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow789(code,U32.is_lt(code,11508))
    case False{}: categoryBelow804(code,U32.is_lt(code,11671))
def categoryBelow806(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow807(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow808(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow806(code,U32.is_lt(code,11712))
    case False{}: categoryBelow807(code,U32.is_lt(code,11720))
def categoryBelow809(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow810(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow811(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow809(code,U32.is_lt(code,11728))
    case False{}: categoryBelow810(code,U32.is_lt(code,11736))
def categoryBelow812(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow808(code,U32.is_lt(code,11719))
    case False{}: categoryBelow811(code,U32.is_lt(code,11735))
def categoryBelow813(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow814(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow815(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow813(code,U32.is_lt(code,11776))
    case False{}: categoryBelow814(code,U32.is_lt(code,11824))
def categoryBelow816(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow817(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow818(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow816(code,U32.is_lt(code,11858))
    case False{}: categoryBelow817(code,U32.is_lt(code,11904))
def categoryBelow819(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow815(code,U32.is_lt(code,11823))
    case False{}: categoryBelow818(code,U32.is_lt(code,11870))
def categoryBelow820(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow812(code,U32.is_lt(code,11727))
    case False{}: categoryBelow819(code,U32.is_lt(code,11856))
def categoryBelow821(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow822(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow823(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow821(code,U32.is_lt(code,11931))
    case False{}: categoryBelow822(code,U32.is_lt(code,12032))
def categoryBelow824(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow825(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow826(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow824(code,U32.is_lt(code,12272))
    case False{}: categoryBelow825(code,U32.is_lt(code,12289))
def categoryBelow827(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow823(code,U32.is_lt(code,12020))
    case False{}: categoryBelow826(code,U32.is_lt(code,12288))
def categoryBelow828(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow829(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow830(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow828(code,U32.is_lt(code,12293))
    case False{}: categoryBelow829(code,U32.is_lt(code,12296))
def categoryBelow831(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow832(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 2
def categoryBelow833(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow831(code,U32.is_lt(code,12308))
    case False{}: categoryBelow832(code,U32.is_lt(code,12321))
def categoryBelow834(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow830(code,U32.is_lt(code,12295))
    case False{}: categoryBelow833(code,U32.is_lt(code,12320))
def categoryBelow835(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow827(code,U32.is_lt(code,12246))
    case False{}: categoryBelow834(code,U32.is_lt(code,12306))
def categoryBelow836(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow820(code,U32.is_lt(code,11743))
    case False{}: categoryBelow835(code,U32.is_lt(code,12292))
def categoryBelow837(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow805(code,U32.is_lt(code,11565))
    case False{}: categoryBelow836(code,U32.is_lt(code,11930))
def categoryBelow838(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow839(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow840(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow838(code,U32.is_lt(code,12336))
    case False{}: categoryBelow839(code,U32.is_lt(code,12342))
def categoryBelow841(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow842(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow843(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow841(code,U32.is_lt(code,12347))
    case False{}: categoryBelow842(code,U32.is_lt(code,12350))
def categoryBelow844(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow840(code,U32.is_lt(code,12337))
    case False{}: categoryBelow843(code,U32.is_lt(code,12349))
def categoryBelow845(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow846(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow847(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow845(code,U32.is_lt(code,12353))
    case False{}: categoryBelow846(code,U32.is_lt(code,12443))
def categoryBelow848(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow849(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow850(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow848(code,U32.is_lt(code,12448))
    case False{}: categoryBelow849(code,U32.is_lt(code,12539))
def categoryBelow851(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow847(code,U32.is_lt(code,12439))
    case False{}: categoryBelow850(code,U32.is_lt(code,12449))
def categoryBelow852(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow844(code,U32.is_lt(code,12344))
    case False{}: categoryBelow851(code,U32.is_lt(code,12445))
def categoryBelow853(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow854(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow855(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow853(code,U32.is_lt(code,12544))
    case False{}: categoryBelow854(code,U32.is_lt(code,12592))
def categoryBelow856(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow857(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 2
def categoryBelow858(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow856(code,U32.is_lt(code,12687))
    case False{}: categoryBelow857(code,U32.is_lt(code,12690))
def categoryBelow859(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow855(code,U32.is_lt(code,12549))
    case False{}: categoryBelow858(code,U32.is_lt(code,12688))
def categoryBelow860(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow861(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow862(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow860(code,U32.is_lt(code,12704))
    case False{}: categoryBelow861(code,U32.is_lt(code,12774))
def categoryBelow863(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow864(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow865(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow863(code,U32.is_lt(code,12784))
    case False{}: categoryBelow864(code,U32.is_lt(code,12831))
def categoryBelow866(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow862(code,U32.is_lt(code,12736))
    case False{}: categoryBelow865(code,U32.is_lt(code,12800))
def categoryBelow867(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow859(code,U32.is_lt(code,12593))
    case False{}: categoryBelow866(code,U32.is_lt(code,12783))
def categoryBelow868(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow852(code,U32.is_lt(code,12352))
    case False{}: categoryBelow867(code,U32.is_lt(code,12694))
def categoryBelow869(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow870(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow871(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow869(code,U32.is_lt(code,12842))
    case False{}: categoryBelow870(code,U32.is_lt(code,12880))
def categoryBelow872(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow873(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow874(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow872(code,U32.is_lt(code,12896))
    case False{}: categoryBelow873(code,U32.is_lt(code,12938))
def categoryBelow875(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow871(code,U32.is_lt(code,12872))
    case False{}: categoryBelow874(code,U32.is_lt(code,12928))
def categoryBelow876(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow877(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow878(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow876(code,U32.is_lt(code,12992))
    case False{}: categoryBelow877(code,U32.is_lt(code,19904))
def categoryBelow879(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow880(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow881(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow879(code,U32.is_lt(code,42125))
    case False{}: categoryBelow880(code,U32.is_lt(code,42183))
def categoryBelow882(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow878(code,U32.is_lt(code,13312))
    case False{}: categoryBelow881(code,U32.is_lt(code,42128))
def categoryBelow883(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow875(code,U32.is_lt(code,12881))
    case False{}: categoryBelow882(code,U32.is_lt(code,19968))
def categoryBelow884(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow885(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow886(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow884(code,U32.is_lt(code,42238))
    case False{}: categoryBelow885(code,U32.is_lt(code,42509))
def categoryBelow887(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow888(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow889(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow887(code,U32.is_lt(code,42528))
    case False{}: categoryBelow888(code,U32.is_lt(code,42540))
def categoryBelow890(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow886(code,U32.is_lt(code,42240))
    case False{}: categoryBelow889(code,U32.is_lt(code,42538))
def categoryBelow891(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow892(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow893(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow891(code,U32.is_lt(code,42607))
    case False{}: categoryBelow892(code,U32.is_lt(code,42612))
def categoryBelow894(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow895(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow896(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow895(code,U32.is_lt(code,42726))
def categoryBelow897(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow894(code,U32.is_lt(code,42623))
    case False{}: categoryBelow896(code,U32.is_lt(code,42656))
def categoryBelow898(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow893(code,U32.is_lt(code,42611))
    case False{}: categoryBelow897(code,U32.is_lt(code,42654))
def categoryBelow899(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow890(code,U32.is_lt(code,42512))
    case False{}: categoryBelow898(code,U32.is_lt(code,42622))
def categoryBelow900(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow883(code,U32.is_lt(code,12977))
    case False{}: categoryBelow899(code,U32.is_lt(code,42560))
def categoryBelow901(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow868(code,U32.is_lt(code,12540))
    case False{}: categoryBelow900(code,U32.is_lt(code,42192))
def categoryBelow902(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow837(code,U32.is_lt(code,11711))
    case False{}: categoryBelow901(code,U32.is_lt(code,12832))
def categoryBelow903(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow904(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow905(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow903(code,U32.is_lt(code,42738))
    case False{}: categoryBelow904(code,U32.is_lt(code,42752))
def categoryBelow906(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow907(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow908(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow906(code,U32.is_lt(code,42784))
    case False{}: categoryBelow907(code,U32.is_lt(code,42889))
def categoryBelow909(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow905(code,U32.is_lt(code,42744))
    case False{}: categoryBelow908(code,U32.is_lt(code,42786))
def categoryBelow910(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow911(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow912(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow910(code,U32.is_lt(code,42973))
    case False{}: categoryBelow911(code,U32.is_lt(code,43010))
def categoryBelow913(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow914(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow915(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow913(code,U32.is_lt(code,43014))
    case False{}: categoryBelow914(code,U32.is_lt(code,43019))
def categoryBelow916(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow912(code,U32.is_lt(code,42993))
    case False{}: categoryBelow915(code,U32.is_lt(code,43015))
def categoryBelow917(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow909(code,U32.is_lt(code,42775))
    case False{}: categoryBelow916(code,U32.is_lt(code,43011))
def categoryBelow918(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow919(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow920(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow918(code,U32.is_lt(code,43043))
    case False{}: categoryBelow919(code,U32.is_lt(code,43052))
def categoryBelow921(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow922(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow923(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow921(code,U32.is_lt(code,43062))
    case False{}: categoryBelow922(code,U32.is_lt(code,43072))
def categoryBelow924(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow920(code,U32.is_lt(code,43048))
    case False{}: categoryBelow923(code,U32.is_lt(code,43066))
def categoryBelow925(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow926(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow927(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow925(code,U32.is_lt(code,43128))
    case False{}: categoryBelow926(code,U32.is_lt(code,43188))
def categoryBelow928(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow929(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow930(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow928(code,U32.is_lt(code,43216))
    case False{}: categoryBelow929(code,U32.is_lt(code,43250))
def categoryBelow931(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow927(code,U32.is_lt(code,43138))
    case False{}: categoryBelow930(code,U32.is_lt(code,43226))
def categoryBelow932(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow924(code,U32.is_lt(code,43056))
    case False{}: categoryBelow931(code,U32.is_lt(code,43214))
def categoryBelow933(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow917(code,U32.is_lt(code,42891))
    case False{}: categoryBelow932(code,U32.is_lt(code,43124))
def categoryBelow934(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow935(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow936(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow934(code,U32.is_lt(code,43259))
    case False{}: categoryBelow935(code,U32.is_lt(code,43261))
def categoryBelow937(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow938(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow939(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow937(code,U32.is_lt(code,43264))
    case False{}: categoryBelow938(code,U32.is_lt(code,43302))
def categoryBelow940(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow936(code,U32.is_lt(code,43260))
    case False{}: categoryBelow939(code,U32.is_lt(code,43274))
def categoryBelow941(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow942(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow943(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow941(code,U32.is_lt(code,43312))
    case False{}: categoryBelow942(code,U32.is_lt(code,43359))
def categoryBelow944(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow945(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow946(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow944(code,U32.is_lt(code,43389))
    case False{}: categoryBelow945(code,U32.is_lt(code,43443))
def categoryBelow947(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow943(code,U32.is_lt(code,43335))
    case False{}: categoryBelow946(code,U32.is_lt(code,43396))
def categoryBelow948(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow940(code,U32.is_lt(code,43263))
    case False{}: categoryBelow947(code,U32.is_lt(code,43360))
def categoryBelow949(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow950(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow951(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow949(code,U32.is_lt(code,43470))
    case False{}: categoryBelow950(code,U32.is_lt(code,43472))
def categoryBelow952(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow953(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow954(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow952(code,U32.is_lt(code,43486))
    case False{}: categoryBelow953(code,U32.is_lt(code,43493))
def categoryBelow955(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow951(code,U32.is_lt(code,43471))
    case False{}: categoryBelow954(code,U32.is_lt(code,43488))
def categoryBelow956(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow957(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow958(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow956(code,U32.is_lt(code,43504))
    case False{}: categoryBelow957(code,U32.is_lt(code,43519))
def categoryBelow959(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow960(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow961(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow960(code,U32.is_lt(code,43588))
def categoryBelow962(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow959(code,U32.is_lt(code,43561))
    case False{}: categoryBelow961(code,U32.is_lt(code,43587))
def categoryBelow963(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow958(code,U32.is_lt(code,43514))
    case False{}: categoryBelow962(code,U32.is_lt(code,43584))
def categoryBelow964(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow955(code,U32.is_lt(code,43482))
    case False{}: categoryBelow963(code,U32.is_lt(code,43520))
def categoryBelow965(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow948(code,U32.is_lt(code,43310))
    case False{}: categoryBelow964(code,U32.is_lt(code,43494))
def categoryBelow966(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow933(code,U32.is_lt(code,43020))
    case False{}: categoryBelow965(code,U32.is_lt(code,43457))
def categoryBelow967(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow968(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow969(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow967(code,U32.is_lt(code,43600))
    case False{}: categoryBelow968(code,U32.is_lt(code,43612))
def categoryBelow970(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow971(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow972(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow970(code,U32.is_lt(code,43639))
    case False{}: categoryBelow971(code,U32.is_lt(code,43643))
def categoryBelow973(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow969(code,U32.is_lt(code,43610))
    case False{}: categoryBelow972(code,U32.is_lt(code,43642))
def categoryBelow974(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow975(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow976(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow974(code,U32.is_lt(code,43696))
    case False{}: categoryBelow975(code,U32.is_lt(code,43698))
def categoryBelow977(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow978(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow979(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow977(code,U32.is_lt(code,43703))
    case False{}: categoryBelow978(code,U32.is_lt(code,43710))
def categoryBelow980(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow976(code,U32.is_lt(code,43697))
    case False{}: categoryBelow979(code,U32.is_lt(code,43705))
def categoryBelow981(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow973(code,U32.is_lt(code,43616))
    case False{}: categoryBelow980(code,U32.is_lt(code,43701))
def categoryBelow982(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow983(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow984(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow982(code,U32.is_lt(code,43713))
    case False{}: categoryBelow983(code,U32.is_lt(code,43715))
def categoryBelow985(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow986(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow987(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow985(code,U32.is_lt(code,43742))
    case False{}: categoryBelow986(code,U32.is_lt(code,43755))
def categoryBelow988(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow984(code,U32.is_lt(code,43714))
    case False{}: categoryBelow987(code,U32.is_lt(code,43744))
def categoryBelow989(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow990(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow991(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow989(code,U32.is_lt(code,43762))
    case False{}: categoryBelow990(code,U32.is_lt(code,43777))
def categoryBelow992(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow993(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow994(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow992(code,U32.is_lt(code,43785))
    case False{}: categoryBelow993(code,U32.is_lt(code,43793))
def categoryBelow995(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow991(code,U32.is_lt(code,43765))
    case False{}: categoryBelow994(code,U32.is_lt(code,43791))
def categoryBelow996(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow988(code,U32.is_lt(code,43739))
    case False{}: categoryBelow995(code,U32.is_lt(code,43783))
def categoryBelow997(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow981(code,U32.is_lt(code,43646))
    case False{}: categoryBelow996(code,U32.is_lt(code,43760))
def categoryBelow998(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow999(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1000(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow998(code,U32.is_lt(code,43808))
    case False{}: categoryBelow999(code,U32.is_lt(code,43816))
def categoryBelow1001(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1002(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1003(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1001(code,U32.is_lt(code,43824))
    case False{}: categoryBelow1002(code,U32.is_lt(code,43868))
def categoryBelow1004(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1000(code,U32.is_lt(code,43815))
    case False{}: categoryBelow1003(code,U32.is_lt(code,43867))
def categoryBelow1005(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1006(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1007(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1005(code,U32.is_lt(code,43884))
    case False{}: categoryBelow1006(code,U32.is_lt(code,44003))
def categoryBelow1008(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1009(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1010(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1008(code,U32.is_lt(code,44012))
    case False{}: categoryBelow1009(code,U32.is_lt(code,44026))
def categoryBelow1011(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1007(code,U32.is_lt(code,43888))
    case False{}: categoryBelow1010(code,U32.is_lt(code,44016))
def categoryBelow1012(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1004(code,U32.is_lt(code,43823))
    case False{}: categoryBelow1011(code,U32.is_lt(code,44011))
def categoryBelow1013(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1014(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1015(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1013(code,U32.is_lt(code,55204))
    case False{}: categoryBelow1014(code,U32.is_lt(code,55239))
def categoryBelow1016(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1017(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1018(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1016(code,U32.is_lt(code,55292))
    case False{}: categoryBelow1017(code,U32.is_lt(code,64110))
def categoryBelow1019(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1015(code,U32.is_lt(code,55216))
    case False{}: categoryBelow1018(code,U32.is_lt(code,63744))
def categoryBelow1020(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1021(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1022(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1020(code,U32.is_lt(code,64218))
    case False{}: categoryBelow1021(code,U32.is_lt(code,64263))
def categoryBelow1023(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1024(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1025(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1024(code,U32.is_lt(code,64287))
def categoryBelow1026(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1023(code,U32.is_lt(code,64280))
    case False{}: categoryBelow1025(code,U32.is_lt(code,64286))
def categoryBelow1027(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1022(code,U32.is_lt(code,64256))
    case False{}: categoryBelow1026(code,U32.is_lt(code,64285))
def categoryBelow1028(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1019(code,U32.is_lt(code,55243))
    case False{}: categoryBelow1027(code,U32.is_lt(code,64275))
def categoryBelow1029(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1012(code,U32.is_lt(code,43882))
    case False{}: categoryBelow1028(code,U32.is_lt(code,64112))
def categoryBelow1030(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow997(code,U32.is_lt(code,43712))
    case False{}: categoryBelow1029(code,U32.is_lt(code,44032))
def categoryBelow1031(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow966(code,U32.is_lt(code,43256))
    case False{}: categoryBelow1030(code,U32.is_lt(code,43799))
def categoryBelow1032(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow902(code,U32.is_lt(code,12330))
    case False{}: categoryBelow1031(code,U32.is_lt(code,43596))
def categoryBelow1033(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow774(code,U32.is_lt(code,7401))
    case False{}: categoryBelow1032(code,U32.is_lt(code,42736))
def categoryBelow1034(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow516(code,U32.is_lt(code,2809))
    case False{}: categoryBelow1033(code,U32.is_lt(code,10750))
def categoryBelow1035(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1036(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1037(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1035(code,U32.is_lt(code,64298))
    case False{}: categoryBelow1036(code,U32.is_lt(code,64312))
def categoryBelow1038(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1039(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1040(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1038(code,U32.is_lt(code,64318))
    case False{}: categoryBelow1039(code,U32.is_lt(code,64320))
def categoryBelow1041(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1037(code,U32.is_lt(code,64311))
    case False{}: categoryBelow1040(code,U32.is_lt(code,64319))
def categoryBelow1042(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1043(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1044(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1042(code,U32.is_lt(code,64323))
    case False{}: categoryBelow1043(code,U32.is_lt(code,64326))
def categoryBelow1045(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1046(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow1047(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1045(code,U32.is_lt(code,64467))
    case False{}: categoryBelow1046(code,U32.is_lt(code,64832))
def categoryBelow1048(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1044(code,U32.is_lt(code,64325))
    case False{}: categoryBelow1047(code,U32.is_lt(code,64830))
def categoryBelow1049(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1041(code,U32.is_lt(code,64317))
    case False{}: categoryBelow1048(code,U32.is_lt(code,64434))
def categoryBelow1050(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1051(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1052(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1050(code,U32.is_lt(code,64912))
    case False{}: categoryBelow1051(code,U32.is_lt(code,64968))
def categoryBelow1053(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1054(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1055(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1053(code,U32.is_lt(code,65008))
    case False{}: categoryBelow1054(code,U32.is_lt(code,65024))
def categoryBelow1056(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1052(code,U32.is_lt(code,64914))
    case False{}: categoryBelow1055(code,U32.is_lt(code,65020))
def categoryBelow1057(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1058(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1059(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1057(code,U32.is_lt(code,65050))
    case False{}: categoryBelow1058(code,U32.is_lt(code,65107))
def categoryBelow1060(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow1061(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow1062(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1060(code,U32.is_lt(code,65122))
    case False{}: categoryBelow1061(code,U32.is_lt(code,65124))
def categoryBelow1063(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1059(code,U32.is_lt(code,65072))
    case False{}: categoryBelow1062(code,U32.is_lt(code,65123))
def categoryBelow1064(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1056(code,U32.is_lt(code,64976))
    case False{}: categoryBelow1063(code,U32.is_lt(code,65108))
def categoryBelow1065(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1049(code,U32.is_lt(code,64322))
    case False{}: categoryBelow1064(code,U32.is_lt(code,65040))
def categoryBelow1066(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1067(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow1068(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1066(code,U32.is_lt(code,65128))
    case False{}: categoryBelow1067(code,U32.is_lt(code,65130))
def categoryBelow1069(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1070(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1071(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1069(code,U32.is_lt(code,65136))
    case False{}: categoryBelow1070(code,U32.is_lt(code,65142))
def categoryBelow1072(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1068(code,U32.is_lt(code,65129))
    case False{}: categoryBelow1071(code,U32.is_lt(code,65141))
def categoryBelow1073(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1074(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow1075(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1073(code,U32.is_lt(code,65281))
    case False{}: categoryBelow1074(code,U32.is_lt(code,65285))
def categoryBelow1076(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow1077(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow1078(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1076(code,U32.is_lt(code,65292))
    case False{}: categoryBelow1077(code,U32.is_lt(code,65306))
def categoryBelow1079(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1075(code,U32.is_lt(code,65284))
    case False{}: categoryBelow1078(code,U32.is_lt(code,65296))
def categoryBelow1080(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1072(code,U32.is_lt(code,65132))
    case False{}: categoryBelow1079(code,U32.is_lt(code,65291))
def categoryBelow1081(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow1082(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1083(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1081(code,U32.is_lt(code,65311))
    case False{}: categoryBelow1082(code,U32.is_lt(code,65339))
def categoryBelow1084(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow1085(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1086(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1084(code,U32.is_lt(code,65343))
    case False{}: categoryBelow1085(code,U32.is_lt(code,65345))
def categoryBelow1087(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1083(code,U32.is_lt(code,65313))
    case False{}: categoryBelow1086(code,U32.is_lt(code,65344))
def categoryBelow1088(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow1089(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow1090(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1088(code,U32.is_lt(code,65372))
    case False{}: categoryBelow1089(code,U32.is_lt(code,65374))
def categoryBelow1091(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1092(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1093(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1091(code,U32.is_lt(code,65382))
    case False{}: categoryBelow1092(code,U32.is_lt(code,65474))
def categoryBelow1094(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1090(code,U32.is_lt(code,65373))
    case False{}: categoryBelow1093(code,U32.is_lt(code,65471))
def categoryBelow1095(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1087(code,U32.is_lt(code,65342))
    case False{}: categoryBelow1094(code,U32.is_lt(code,65375))
def categoryBelow1096(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1080(code,U32.is_lt(code,65277))
    case False{}: categoryBelow1095(code,U32.is_lt(code,65371))
def categoryBelow1097(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1065(code,U32.is_lt(code,64848))
    case False{}: categoryBelow1096(code,U32.is_lt(code,65308))
def categoryBelow1098(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1099(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1098(code,U32.is_lt(code,65482))
    case False{}: categoryBelow1099(code,U32.is_lt(code,65490))
def categoryBelow1101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1101(code,U32.is_lt(code,65498))
    case False{}: categoryBelow1102(code,U32.is_lt(code,65504))
def categoryBelow1104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1100(code,U32.is_lt(code,65488))
    case False{}: categoryBelow1103(code,U32.is_lt(code,65501))
def categoryBelow1105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1105(code,U32.is_lt(code,65512))
    case False{}: categoryBelow1106(code,U32.is_lt(code,65532))
def categoryBelow1108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1108(code,U32.is_lt(code,65536))
    case False{}: categoryBelow1109(code,U32.is_lt(code,65549))
def categoryBelow1111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1107(code,U32.is_lt(code,65519))
    case False{}: categoryBelow1110(code,U32.is_lt(code,65548))
def categoryBelow1112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1104(code,U32.is_lt(code,65496))
    case False{}: categoryBelow1111(code,U32.is_lt(code,65534))
def categoryBelow1113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1113(code,U32.is_lt(code,65576))
    case False{}: categoryBelow1114(code,U32.is_lt(code,65596))
def categoryBelow1116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1116(code,U32.is_lt(code,65599))
    case False{}: categoryBelow1117(code,U32.is_lt(code,65616))
def categoryBelow1119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1115(code,U32.is_lt(code,65595))
    case False{}: categoryBelow1118(code,U32.is_lt(code,65614))
def categoryBelow1120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1120(code,U32.is_lt(code,65664))
    case False{}: categoryBelow1121(code,U32.is_lt(code,65792))
def categoryBelow1123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1123(code,U32.is_lt(code,65799))
    case False{}: categoryBelow1124(code,U32.is_lt(code,65847))
def categoryBelow1126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1122(code,U32.is_lt(code,65787))
    case False{}: categoryBelow1125(code,U32.is_lt(code,65844))
def categoryBelow1127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1119(code,U32.is_lt(code,65598))
    case False{}: categoryBelow1126(code,U32.is_lt(code,65795))
def categoryBelow1128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1112(code,U32.is_lt(code,65511))
    case False{}: categoryBelow1127(code,U32.is_lt(code,65630))
def categoryBelow1129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow1130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow1131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1129(code,U32.is_lt(code,65913))
    case False{}: categoryBelow1130(code,U32.is_lt(code,65932))
def categoryBelow1132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1132(code,U32.is_lt(code,65936))
    case False{}: categoryBelow1133(code,U32.is_lt(code,65952))
def categoryBelow1135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1131(code,U32.is_lt(code,65930))
    case False{}: categoryBelow1134(code,U32.is_lt(code,65949))
def categoryBelow1136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1136(code,U32.is_lt(code,66000))
    case False{}: categoryBelow1137(code,U32.is_lt(code,66176))
def categoryBelow1139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1139(code,U32.is_lt(code,66208))
    case False{}: categoryBelow1140(code,U32.is_lt(code,66273))
def categoryBelow1142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1138(code,U32.is_lt(code,66045))
    case False{}: categoryBelow1141(code,U32.is_lt(code,66257))
def categoryBelow1143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1135(code,U32.is_lt(code,65935))
    case False{}: categoryBelow1142(code,U32.is_lt(code,66205))
def categoryBelow1144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1144(code,U32.is_lt(code,66304))
    case False{}: categoryBelow1145(code,U32.is_lt(code,66340))
def categoryBelow1147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1147(code,U32.is_lt(code,66369))
    case False{}: categoryBelow1148(code,U32.is_lt(code,66378))
def categoryBelow1150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1146(code,U32.is_lt(code,66336))
    case False{}: categoryBelow1149(code,U32.is_lt(code,66370))
def categoryBelow1151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1151(code,U32.is_lt(code,66384))
    case False{}: categoryBelow1152(code,U32.is_lt(code,66432))
def categoryBelow1154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1155(code,U32.is_lt(code,66504))
def categoryBelow1157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1154(code,U32.is_lt(code,66463))
    case False{}: categoryBelow1156(code,U32.is_lt(code,66500))
def categoryBelow1158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1153(code,U32.is_lt(code,66422))
    case False{}: categoryBelow1157(code,U32.is_lt(code,66464))
def categoryBelow1159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1150(code,U32.is_lt(code,66349))
    case False{}: categoryBelow1158(code,U32.is_lt(code,66462))
def categoryBelow1160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1143(code,U32.is_lt(code,65953))
    case False{}: categoryBelow1159(code,U32.is_lt(code,66379))
def categoryBelow1161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1128(code,U32.is_lt(code,65575))
    case False{}: categoryBelow1160(code,U32.is_lt(code,66300))
def categoryBelow1162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1097(code,U32.is_lt(code,65127))
    case False{}: categoryBelow1161(code,U32.is_lt(code,65856))
def categoryBelow1163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow1164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1163(code,U32.is_lt(code,66513))
    case False{}: categoryBelow1164(code,U32.is_lt(code,66560))
def categoryBelow1166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1166(code,U32.is_lt(code,66720))
    case False{}: categoryBelow1167(code,U32.is_lt(code,66736))
def categoryBelow1169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1165(code,U32.is_lt(code,66518))
    case False{}: categoryBelow1168(code,U32.is_lt(code,66730))
def categoryBelow1170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1170(code,U32.is_lt(code,66776))
    case False{}: categoryBelow1171(code,U32.is_lt(code,66816))
def categoryBelow1173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1173(code,U32.is_lt(code,66864))
    case False{}: categoryBelow1174(code,U32.is_lt(code,66927))
def categoryBelow1176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1172(code,U32.is_lt(code,66812))
    case False{}: categoryBelow1175(code,U32.is_lt(code,66916))
def categoryBelow1177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1169(code,U32.is_lt(code,66718))
    case False{}: categoryBelow1176(code,U32.is_lt(code,66856))
def categoryBelow1178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1178(code,U32.is_lt(code,66939))
    case False{}: categoryBelow1179(code,U32.is_lt(code,66955))
def categoryBelow1181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1181(code,U32.is_lt(code,66963))
    case False{}: categoryBelow1182(code,U32.is_lt(code,66966))
def categoryBelow1184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1180(code,U32.is_lt(code,66940))
    case False{}: categoryBelow1183(code,U32.is_lt(code,66964))
def categoryBelow1185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1185(code,U32.is_lt(code,66978))
    case False{}: categoryBelow1186(code,U32.is_lt(code,66994))
def categoryBelow1188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1188(code,U32.is_lt(code,67002))
    case False{}: categoryBelow1189(code,U32.is_lt(code,67005))
def categoryBelow1191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1187(code,U32.is_lt(code,66979))
    case False{}: categoryBelow1190(code,U32.is_lt(code,67003))
def categoryBelow1192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1184(code,U32.is_lt(code,66956))
    case False{}: categoryBelow1191(code,U32.is_lt(code,66995))
def categoryBelow1193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1177(code,U32.is_lt(code,66772))
    case False{}: categoryBelow1192(code,U32.is_lt(code,66967))
def categoryBelow1194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1194(code,U32.is_lt(code,67060))
    case False{}: categoryBelow1195(code,U32.is_lt(code,67383))
def categoryBelow1197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1197(code,U32.is_lt(code,67414))
    case False{}: categoryBelow1198(code,U32.is_lt(code,67432))
def categoryBelow1200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1196(code,U32.is_lt(code,67072))
    case False{}: categoryBelow1199(code,U32.is_lt(code,67424))
def categoryBelow1201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1201(code,U32.is_lt(code,67462))
    case False{}: categoryBelow1202(code,U32.is_lt(code,67505))
def categoryBelow1204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1204(code,U32.is_lt(code,67515))
    case False{}: categoryBelow1205(code,U32.is_lt(code,67590))
def categoryBelow1207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1203(code,U32.is_lt(code,67463))
    case False{}: categoryBelow1206(code,U32.is_lt(code,67584))
def categoryBelow1208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1200(code,U32.is_lt(code,67392))
    case False{}: categoryBelow1207(code,U32.is_lt(code,67506))
def categoryBelow1209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1209(code,U32.is_lt(code,67593))
    case False{}: categoryBelow1210(code,U32.is_lt(code,67638))
def categoryBelow1212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1212(code,U32.is_lt(code,67641))
    case False{}: categoryBelow1213(code,U32.is_lt(code,67645))
def categoryBelow1215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1211(code,U32.is_lt(code,67594))
    case False{}: categoryBelow1214(code,U32.is_lt(code,67644))
def categoryBelow1216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow1218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1216(code,U32.is_lt(code,67670))
    case False{}: categoryBelow1217(code,U32.is_lt(code,67672))
def categoryBelow1219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1220(code,U32.is_lt(code,67743))
def categoryBelow1222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1219(code,U32.is_lt(code,67703))
    case False{}: categoryBelow1221(code,U32.is_lt(code,67712))
def categoryBelow1223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1218(code,U32.is_lt(code,67671))
    case False{}: categoryBelow1222(code,U32.is_lt(code,67705))
def categoryBelow1224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1215(code,U32.is_lt(code,67639))
    case False{}: categoryBelow1223(code,U32.is_lt(code,67680))
def categoryBelow1225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1208(code,U32.is_lt(code,67456))
    case False{}: categoryBelow1224(code,U32.is_lt(code,67647))
def categoryBelow1226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1193(code,U32.is_lt(code,66928))
    case False{}: categoryBelow1225(code,U32.is_lt(code,67592))
def categoryBelow1227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1227(code,U32.is_lt(code,67760))
    case False{}: categoryBelow1228(code,U32.is_lt(code,67827))
def categoryBelow1230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1230(code,U32.is_lt(code,67830))
    case False{}: categoryBelow1231(code,U32.is_lt(code,67840))
def categoryBelow1233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1229(code,U32.is_lt(code,67808))
    case False{}: categoryBelow1232(code,U32.is_lt(code,67835))
def categoryBelow1234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1234(code,U32.is_lt(code,67868))
    case False{}: categoryBelow1235(code,U32.is_lt(code,67872))
def categoryBelow1237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1237(code,U32.is_lt(code,67903))
    case False{}: categoryBelow1238(code,U32.is_lt(code,67930))
def categoryBelow1240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1236(code,U32.is_lt(code,67871))
    case False{}: categoryBelow1239(code,U32.is_lt(code,67904))
def categoryBelow1241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1233(code,U32.is_lt(code,67828))
    case False{}: categoryBelow1240(code,U32.is_lt(code,67898))
def categoryBelow1242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1242(code,U32.is_lt(code,68024))
    case False{}: categoryBelow1243(code,U32.is_lt(code,68030))
def categoryBelow1245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1245(code,U32.is_lt(code,68048))
    case False{}: categoryBelow1246(code,U32.is_lt(code,68096))
def categoryBelow1248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1244(code,U32.is_lt(code,68028))
    case False{}: categoryBelow1247(code,U32.is_lt(code,68050))
def categoryBelow1249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1249(code,U32.is_lt(code,68112))
    case False{}: categoryBelow1250(code,U32.is_lt(code,68117))
def categoryBelow1252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1252(code,U32.is_lt(code,68121))
    case False{}: categoryBelow1253(code,U32.is_lt(code,68160))
def categoryBelow1255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1251(code,U32.is_lt(code,68116))
    case False{}: categoryBelow1254(code,U32.is_lt(code,68150))
def categoryBelow1256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1248(code,U32.is_lt(code,68032))
    case False{}: categoryBelow1255(code,U32.is_lt(code,68120))
def categoryBelow1257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1241(code,U32.is_lt(code,67862))
    case False{}: categoryBelow1256(code,U32.is_lt(code,68097))
def categoryBelow1258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1258(code,U32.is_lt(code,68176))
    case False{}: categoryBelow1259(code,U32.is_lt(code,68192))
def categoryBelow1261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow1262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1261(code,U32.is_lt(code,68223))
    case False{}: categoryBelow1262(code,U32.is_lt(code,68253))
def categoryBelow1264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1260(code,U32.is_lt(code,68185))
    case False{}: categoryBelow1263(code,U32.is_lt(code,68224))
def categoryBelow1265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1265(code,U32.is_lt(code,68288))
    case False{}: categoryBelow1266(code,U32.is_lt(code,68297))
def categoryBelow1268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1268(code,U32.is_lt(code,68331))
    case False{}: categoryBelow1269(code,U32.is_lt(code,68343))
def categoryBelow1271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1267(code,U32.is_lt(code,68296))
    case False{}: categoryBelow1270(code,U32.is_lt(code,68336))
def categoryBelow1272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1264(code,U32.is_lt(code,68221))
    case False{}: categoryBelow1271(code,U32.is_lt(code,68325))
def categoryBelow1273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1273(code,U32.is_lt(code,68406))
    case False{}: categoryBelow1274(code,U32.is_lt(code,68416))
def categoryBelow1276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1276(code,U32.is_lt(code,68440))
    case False{}: categoryBelow1277(code,U32.is_lt(code,68467))
def categoryBelow1279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1275(code,U32.is_lt(code,68409))
    case False{}: categoryBelow1278(code,U32.is_lt(code,68448))
def categoryBelow1280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1280(code,U32.is_lt(code,68480))
    case False{}: categoryBelow1281(code,U32.is_lt(code,68505))
def categoryBelow1283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1284(code,U32.is_lt(code,68681))
def categoryBelow1286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1283(code,U32.is_lt(code,68521))
    case False{}: categoryBelow1285(code,U32.is_lt(code,68608))
def categoryBelow1287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1282(code,U32.is_lt(code,68498))
    case False{}: categoryBelow1286(code,U32.is_lt(code,68528))
def categoryBelow1288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1279(code,U32.is_lt(code,68438))
    case False{}: categoryBelow1287(code,U32.is_lt(code,68509))
def categoryBelow1289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1272(code,U32.is_lt(code,68256))
    case False{}: categoryBelow1288(code,U32.is_lt(code,68472))
def categoryBelow1290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1257(code,U32.is_lt(code,67968))
    case False{}: categoryBelow1289(code,U32.is_lt(code,68352))
def categoryBelow1291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1226(code,U32.is_lt(code,67008))
    case False{}: categoryBelow1290(code,U32.is_lt(code,68169))
def categoryBelow1292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1162(code,U32.is_lt(code,65480))
    case False{}: categoryBelow1291(code,U32.is_lt(code,67751))
def categoryBelow1293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1293(code,U32.is_lt(code,68787))
    case False{}: categoryBelow1294(code,U32.is_lt(code,68851))
def categoryBelow1296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1296(code,U32.is_lt(code,68864))
    case False{}: categoryBelow1297(code,U32.is_lt(code,68912))
def categoryBelow1299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1295(code,U32.is_lt(code,68800))
    case False{}: categoryBelow1298(code,U32.is_lt(code,68900))
def categoryBelow1300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1300(code,U32.is_lt(code,68928))
    case False{}: categoryBelow1301(code,U32.is_lt(code,68966))
def categoryBelow1303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1303(code,U32.is_lt(code,68975))
    case False{}: categoryBelow1304(code,U32.is_lt(code,69006))
def categoryBelow1306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1302(code,U32.is_lt(code,68938))
    case False{}: categoryBelow1305(code,U32.is_lt(code,68998))
def categoryBelow1307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1299(code,U32.is_lt(code,68858))
    case False{}: categoryBelow1306(code,U32.is_lt(code,68974))
def categoryBelow1308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1308(code,U32.is_lt(code,69216))
    case False{}: categoryBelow1309(code,U32.is_lt(code,69248))
def categoryBelow1311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1311(code,U32.is_lt(code,69293))
    case False{}: categoryBelow1312(code,U32.is_lt(code,69296))
def categoryBelow1314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1310(code,U32.is_lt(code,69247))
    case False{}: categoryBelow1313(code,U32.is_lt(code,69294))
def categoryBelow1315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1315(code,U32.is_lt(code,69314))
    case False{}: categoryBelow1316(code,U32.is_lt(code,69328))
def categoryBelow1318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1318(code,U32.is_lt(code,69337))
    case False{}: categoryBelow1319(code,U32.is_lt(code,69405))
def categoryBelow1321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1317(code,U32.is_lt(code,69320))
    case False{}: categoryBelow1320(code,U32.is_lt(code,69376))
def categoryBelow1322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1314(code,U32.is_lt(code,69290))
    case False{}: categoryBelow1321(code,U32.is_lt(code,69329))
def categoryBelow1323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1307(code,U32.is_lt(code,68922))
    case False{}: categoryBelow1322(code,U32.is_lt(code,69298))
def categoryBelow1324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1324(code,U32.is_lt(code,69416))
    case False{}: categoryBelow1325(code,U32.is_lt(code,69446))
def categoryBelow1327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow1328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1327(code,U32.is_lt(code,69461))
    case False{}: categoryBelow1328(code,U32.is_lt(code,69488))
def categoryBelow1330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1326(code,U32.is_lt(code,69424))
    case False{}: categoryBelow1329(code,U32.is_lt(code,69466))
def categoryBelow1331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1331(code,U32.is_lt(code,69510))
    case False{}: categoryBelow1332(code,U32.is_lt(code,69552))
def categoryBelow1334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1334(code,U32.is_lt(code,69580))
    case False{}: categoryBelow1335(code,U32.is_lt(code,69623))
def categoryBelow1337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1333(code,U32.is_lt(code,69514))
    case False{}: categoryBelow1336(code,U32.is_lt(code,69600))
def categoryBelow1338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1330(code,U32.is_lt(code,69457))
    case False{}: categoryBelow1337(code,U32.is_lt(code,69573))
def categoryBelow1339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1339(code,U32.is_lt(code,69688))
    case False{}: categoryBelow1340(code,U32.is_lt(code,69710))
def categoryBelow1342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1342(code,U32.is_lt(code,69744))
    case False{}: categoryBelow1343(code,U32.is_lt(code,69747))
def categoryBelow1345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1341(code,U32.is_lt(code,69703))
    case False{}: categoryBelow1344(code,U32.is_lt(code,69745))
def categoryBelow1346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1346(code,U32.is_lt(code,69750))
    case False{}: categoryBelow1347(code,U32.is_lt(code,69808))
def categoryBelow1349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1349(code,U32.is_lt(code,69821))
    case False{}: categoryBelow1350(code,U32.is_lt(code,69826))
def categoryBelow1352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1348(code,U32.is_lt(code,69763))
    case False{}: categoryBelow1351(code,U32.is_lt(code,69822))
def categoryBelow1353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1345(code,U32.is_lt(code,69714))
    case False{}: categoryBelow1352(code,U32.is_lt(code,69819))
def categoryBelow1354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1338(code,U32.is_lt(code,69506))
    case False{}: categoryBelow1353(code,U32.is_lt(code,69749))
def categoryBelow1355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1323(code,U32.is_lt(code,69008))
    case False{}: categoryBelow1354(code,U32.is_lt(code,69635))
def categoryBelow1356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1356(code,U32.is_lt(code,69865))
    case False{}: categoryBelow1357(code,U32.is_lt(code,69882))
def categoryBelow1359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow1361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1359(code,U32.is_lt(code,69927))
    case False{}: categoryBelow1360(code,U32.is_lt(code,69952))
def categoryBelow1362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1358(code,U32.is_lt(code,69872))
    case False{}: categoryBelow1361(code,U32.is_lt(code,69942))
def categoryBelow1363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1363(code,U32.is_lt(code,69957))
    case False{}: categoryBelow1364(code,U32.is_lt(code,69960))
def categoryBelow1366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1366(code,U32.is_lt(code,70003))
    case False{}: categoryBelow1367(code,U32.is_lt(code,70006))
def categoryBelow1369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1365(code,U32.is_lt(code,69959))
    case False{}: categoryBelow1368(code,U32.is_lt(code,70004))
def categoryBelow1370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1362(code,U32.is_lt(code,69891))
    case False{}: categoryBelow1369(code,U32.is_lt(code,69968))
def categoryBelow1371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1371(code,U32.is_lt(code,70019))
    case False{}: categoryBelow1372(code,U32.is_lt(code,70081))
def categoryBelow1374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1374(code,U32.is_lt(code,70089))
    case False{}: categoryBelow1375(code,U32.is_lt(code,70094))
def categoryBelow1377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1373(code,U32.is_lt(code,70067))
    case False{}: categoryBelow1376(code,U32.is_lt(code,70093))
def categoryBelow1378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1378(code,U32.is_lt(code,70106))
    case False{}: categoryBelow1379(code,U32.is_lt(code,70108))
def categoryBelow1381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1381(code,U32.is_lt(code,70112))
    case False{}: categoryBelow1382(code,U32.is_lt(code,70133))
def categoryBelow1384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1380(code,U32.is_lt(code,70107))
    case False{}: categoryBelow1383(code,U32.is_lt(code,70113))
def categoryBelow1385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1377(code,U32.is_lt(code,70085))
    case False{}: categoryBelow1384(code,U32.is_lt(code,70109))
def categoryBelow1386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1370(code,U32.is_lt(code,69956))
    case False{}: categoryBelow1385(code,U32.is_lt(code,70096))
def categoryBelow1387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1387(code,U32.is_lt(code,70162))
    case False{}: categoryBelow1388(code,U32.is_lt(code,70188))
def categoryBelow1390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1390(code,U32.is_lt(code,70206))
    case False{}: categoryBelow1391(code,U32.is_lt(code,70209))
def categoryBelow1393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1389(code,U32.is_lt(code,70163))
    case False{}: categoryBelow1392(code,U32.is_lt(code,70207))
def categoryBelow1394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1394(code,U32.is_lt(code,70279))
    case False{}: categoryBelow1395(code,U32.is_lt(code,70281))
def categoryBelow1397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1397(code,U32.is_lt(code,70286))
    case False{}: categoryBelow1398(code,U32.is_lt(code,70302))
def categoryBelow1400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1396(code,U32.is_lt(code,70280))
    case False{}: categoryBelow1399(code,U32.is_lt(code,70287))
def categoryBelow1401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1393(code,U32.is_lt(code,70200))
    case False{}: categoryBelow1400(code,U32.is_lt(code,70282))
def categoryBelow1402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1402(code,U32.is_lt(code,70313))
    case False{}: categoryBelow1403(code,U32.is_lt(code,70320))
def categoryBelow1405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1405(code,U32.is_lt(code,70384))
    case False{}: categoryBelow1406(code,U32.is_lt(code,70405))
def categoryBelow1408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1404(code,U32.is_lt(code,70314))
    case False{}: categoryBelow1407(code,U32.is_lt(code,70394))
def categoryBelow1409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1409(code,U32.is_lt(code,70415))
    case False{}: categoryBelow1410(code,U32.is_lt(code,70419))
def categoryBelow1412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1413(code,U32.is_lt(code,70452))
def categoryBelow1415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1412(code,U32.is_lt(code,70442))
    case False{}: categoryBelow1414(code,U32.is_lt(code,70450))
def categoryBelow1416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1411(code,U32.is_lt(code,70417))
    case False{}: categoryBelow1415(code,U32.is_lt(code,70449))
def categoryBelow1417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1408(code,U32.is_lt(code,70367))
    case False{}: categoryBelow1416(code,U32.is_lt(code,70441))
def categoryBelow1418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1401(code,U32.is_lt(code,70272))
    case False{}: categoryBelow1417(code,U32.is_lt(code,70413))
def categoryBelow1419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1386(code,U32.is_lt(code,70007))
    case False{}: categoryBelow1418(code,U32.is_lt(code,70303))
def categoryBelow1420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1355(code,U32.is_lt(code,69415))
    case False{}: categoryBelow1419(code,U32.is_lt(code,70144))
def categoryBelow1421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1421(code,U32.is_lt(code,70458))
    case False{}: categoryBelow1422(code,U32.is_lt(code,70462))
def categoryBelow1424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1424(code,U32.is_lt(code,70481))
    case False{}: categoryBelow1425(code,U32.is_lt(code,70498))
def categoryBelow1427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1423(code,U32.is_lt(code,70461))
    case False{}: categoryBelow1426(code,U32.is_lt(code,70493))
def categoryBelow1428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1428(code,U32.is_lt(code,70538))
    case False{}: categoryBelow1429(code,U32.is_lt(code,70540))
def categoryBelow1431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1431(code,U32.is_lt(code,70543))
    case False{}: categoryBelow1432(code,U32.is_lt(code,70582))
def categoryBelow1434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1430(code,U32.is_lt(code,70539))
    case False{}: categoryBelow1433(code,U32.is_lt(code,70544))
def categoryBelow1435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1427(code,U32.is_lt(code,70480))
    case False{}: categoryBelow1434(code,U32.is_lt(code,70542))
def categoryBelow1436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1436(code,U32.is_lt(code,70584))
    case False{}: categoryBelow1437(code,U32.is_lt(code,70610))
def categoryBelow1439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1439(code,U32.is_lt(code,70612))
    case False{}: categoryBelow1440(code,U32.is_lt(code,70615))
def categoryBelow1442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1438(code,U32.is_lt(code,70609))
    case False{}: categoryBelow1441(code,U32.is_lt(code,70614))
def categoryBelow1443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1443(code,U32.is_lt(code,70656))
    case False{}: categoryBelow1444(code,U32.is_lt(code,70727))
def categoryBelow1446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow1447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1446(code,U32.is_lt(code,70736))
    case False{}: categoryBelow1447(code,U32.is_lt(code,70748))
def categoryBelow1449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1445(code,U32.is_lt(code,70709))
    case False{}: categoryBelow1448(code,U32.is_lt(code,70746))
def categoryBelow1450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1442(code,U32.is_lt(code,70611))
    case False{}: categoryBelow1449(code,U32.is_lt(code,70731))
def categoryBelow1451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1435(code,U32.is_lt(code,70528))
    case False{}: categoryBelow1450(code,U32.is_lt(code,70617))
def categoryBelow1452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1452(code,U32.is_lt(code,70750))
    case False{}: categoryBelow1453(code,U32.is_lt(code,70754))
def categoryBelow1455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1455(code,U32.is_lt(code,70832))
    case False{}: categoryBelow1456(code,U32.is_lt(code,70854))
def categoryBelow1458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1454(code,U32.is_lt(code,70751))
    case False{}: categoryBelow1457(code,U32.is_lt(code,70852))
def categoryBelow1459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1459(code,U32.is_lt(code,70856))
    case False{}: categoryBelow1460(code,U32.is_lt(code,70874))
def categoryBelow1462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1462(code,U32.is_lt(code,71087))
    case False{}: categoryBelow1463(code,U32.is_lt(code,71128))
def categoryBelow1465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1461(code,U32.is_lt(code,70864))
    case False{}: categoryBelow1464(code,U32.is_lt(code,71105))
def categoryBelow1466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1458(code,U32.is_lt(code,70784))
    case False{}: categoryBelow1465(code,U32.is_lt(code,71040))
def categoryBelow1467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1467(code,U32.is_lt(code,71168))
    case False{}: categoryBelow1468(code,U32.is_lt(code,71233))
def categoryBelow1470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1470(code,U32.is_lt(code,71237))
    case False{}: categoryBelow1471(code,U32.is_lt(code,71258))
def categoryBelow1473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1469(code,U32.is_lt(code,71216))
    case False{}: categoryBelow1472(code,U32.is_lt(code,71248))
def categoryBelow1474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1474(code,U32.is_lt(code,71277))
    case False{}: categoryBelow1475(code,U32.is_lt(code,71339))
def categoryBelow1477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1478(code,U32.is_lt(code,71370))
def categoryBelow1480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1477(code,U32.is_lt(code,71353))
    case False{}: categoryBelow1479(code,U32.is_lt(code,71360))
def categoryBelow1481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1476(code,U32.is_lt(code,71296))
    case False{}: categoryBelow1480(code,U32.is_lt(code,71354))
def categoryBelow1482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1473(code,U32.is_lt(code,71236))
    case False{}: categoryBelow1481(code,U32.is_lt(code,71352))
def categoryBelow1483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1466(code,U32.is_lt(code,70855))
    case False{}: categoryBelow1482(code,U32.is_lt(code,71264))
def categoryBelow1484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1451(code,U32.is_lt(code,70583))
    case False{}: categoryBelow1483(code,U32.is_lt(code,71132))
def categoryBelow1485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1485(code,U32.is_lt(code,71396))
    case False{}: categoryBelow1486(code,U32.is_lt(code,71451))
def categoryBelow1488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow1489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1488(code,U32.is_lt(code,71484))
    case False{}: categoryBelow1489(code,U32.is_lt(code,71488))
def categoryBelow1491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1487(code,U32.is_lt(code,71424))
    case False{}: categoryBelow1490(code,U32.is_lt(code,71487))
def categoryBelow1492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1492(code,U32.is_lt(code,71680))
    case False{}: categoryBelow1493(code,U32.is_lt(code,71739))
def categoryBelow1495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1495(code,U32.is_lt(code,71840))
    case False{}: categoryBelow1496(code,U32.is_lt(code,71923))
def categoryBelow1498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1494(code,U32.is_lt(code,71724))
    case False{}: categoryBelow1497(code,U32.is_lt(code,71904))
def categoryBelow1499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1491(code,U32.is_lt(code,71472))
    case False{}: categoryBelow1498(code,U32.is_lt(code,71740))
def categoryBelow1500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1500(code,U32.is_lt(code,71943))
    case False{}: categoryBelow1501(code,U32.is_lt(code,71946))
def categoryBelow1503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1503(code,U32.is_lt(code,71956))
    case False{}: categoryBelow1504(code,U32.is_lt(code,71959))
def categoryBelow1506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1502(code,U32.is_lt(code,71945))
    case False{}: categoryBelow1505(code,U32.is_lt(code,71957))
def categoryBelow1507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1507(code,U32.is_lt(code,71984))
    case False{}: categoryBelow1508(code,U32.is_lt(code,72000))
def categoryBelow1510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1510(code,U32.is_lt(code,72002))
    case False{}: categoryBelow1511(code,U32.is_lt(code,72007))
def categoryBelow1513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1509(code,U32.is_lt(code,71999))
    case False{}: categoryBelow1512(code,U32.is_lt(code,72004))
def categoryBelow1514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1506(code,U32.is_lt(code,71948))
    case False{}: categoryBelow1513(code,U32.is_lt(code,72001))
def categoryBelow1515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1499(code,U32.is_lt(code,71495))
    case False{}: categoryBelow1514(code,U32.is_lt(code,71960))
def categoryBelow1516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1516(code,U32.is_lt(code,72026))
    case False{}: categoryBelow1517(code,U32.is_lt(code,72104))
def categoryBelow1519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1519(code,U32.is_lt(code,72145))
    case False{}: categoryBelow1520(code,U32.is_lt(code,72162))
def categoryBelow1522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1518(code,U32.is_lt(code,72096))
    case False{}: categoryBelow1521(code,U32.is_lt(code,72161))
def categoryBelow1523(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1524(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1525(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1523(code,U32.is_lt(code,72164))
    case False{}: categoryBelow1524(code,U32.is_lt(code,72193))
def categoryBelow1526(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1527(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1528(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1526(code,U32.is_lt(code,72243))
    case False{}: categoryBelow1527(code,U32.is_lt(code,72251))
def categoryBelow1529(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1525(code,U32.is_lt(code,72192))
    case False{}: categoryBelow1528(code,U32.is_lt(code,72250))
def categoryBelow1530(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1522(code,U32.is_lt(code,72106))
    case False{}: categoryBelow1529(code,U32.is_lt(code,72203))
def categoryBelow1531(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1532(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1533(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1531(code,U32.is_lt(code,72263))
    case False{}: categoryBelow1532(code,U32.is_lt(code,72273))
def categoryBelow1534(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1535(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1536(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1534(code,U32.is_lt(code,72330))
    case False{}: categoryBelow1535(code,U32.is_lt(code,72349))
def categoryBelow1537(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1533(code,U32.is_lt(code,72272))
    case False{}: categoryBelow1536(code,U32.is_lt(code,72346))
def categoryBelow1538(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1539(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1540(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1538(code,U32.is_lt(code,72355))
    case False{}: categoryBelow1539(code,U32.is_lt(code,72441))
def categoryBelow1541(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1542(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1543(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1542(code,U32.is_lt(code,72674))
def categoryBelow1544(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1541(code,U32.is_lt(code,72458))
    case False{}: categoryBelow1543(code,U32.is_lt(code,72673))
def categoryBelow1545(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1540(code,U32.is_lt(code,72368))
    case False{}: categoryBelow1544(code,U32.is_lt(code,72640))
def categoryBelow1546(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1537(code,U32.is_lt(code,72284))
    case False{}: categoryBelow1545(code,U32.is_lt(code,72448))
def categoryBelow1547(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1530(code,U32.is_lt(code,72163))
    case False{}: categoryBelow1546(code,U32.is_lt(code,72350))
def categoryBelow1548(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1515(code,U32.is_lt(code,71935))
    case False{}: categoryBelow1547(code,U32.is_lt(code,72255))
def categoryBelow1549(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1484(code,U32.is_lt(code,70749))
    case False{}: categoryBelow1548(code,U32.is_lt(code,72016))
def categoryBelow1550(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1420(code,U32.is_lt(code,69840))
    case False{}: categoryBelow1549(code,U32.is_lt(code,71376))
def categoryBelow1551(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1292(code,U32.is_lt(code,66512))
    case False{}: categoryBelow1550(code,U32.is_lt(code,70453))
def categoryBelow1552(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1553(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1554(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1552(code,U32.is_lt(code,72698))
    case False{}: categoryBelow1553(code,U32.is_lt(code,72713))
def categoryBelow1555(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1556(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1557(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1555(code,U32.is_lt(code,72751))
    case False{}: categoryBelow1556(code,U32.is_lt(code,72769))
def categoryBelow1558(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1554(code,U32.is_lt(code,72704))
    case False{}: categoryBelow1557(code,U32.is_lt(code,72768))
def categoryBelow1559(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1560(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1561(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1559(code,U32.is_lt(code,72784))
    case False{}: categoryBelow1560(code,U32.is_lt(code,72816))
def categoryBelow1562(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1563(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1564(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1562(code,U32.is_lt(code,72848))
    case False{}: categoryBelow1563(code,U32.is_lt(code,72967))
def categoryBelow1565(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1561(code,U32.is_lt(code,72813))
    case False{}: categoryBelow1564(code,U32.is_lt(code,72960))
def categoryBelow1566(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1558(code,U32.is_lt(code,72714))
    case False{}: categoryBelow1565(code,U32.is_lt(code,72818))
def categoryBelow1567(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1568(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1569(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1567(code,U32.is_lt(code,72970))
    case False{}: categoryBelow1568(code,U32.is_lt(code,73009))
def categoryBelow1570(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1571(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1572(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1570(code,U32.is_lt(code,73031))
    case False{}: categoryBelow1571(code,U32.is_lt(code,73050))
def categoryBelow1573(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1569(code,U32.is_lt(code,72971))
    case False{}: categoryBelow1572(code,U32.is_lt(code,73040))
def categoryBelow1574(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1575(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1576(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1574(code,U32.is_lt(code,73062))
    case False{}: categoryBelow1575(code,U32.is_lt(code,73065))
def categoryBelow1577(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1578(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1579(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1577(code,U32.is_lt(code,73098))
    case False{}: categoryBelow1578(code,U32.is_lt(code,73113))
def categoryBelow1580(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1576(code,U32.is_lt(code,73063))
    case False{}: categoryBelow1579(code,U32.is_lt(code,73112))
def categoryBelow1581(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1573(code,U32.is_lt(code,73030))
    case False{}: categoryBelow1580(code,U32.is_lt(code,73066))
def categoryBelow1582(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1566(code,U32.is_lt(code,72774))
    case False{}: categoryBelow1581(code,U32.is_lt(code,73056))
def categoryBelow1583(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1584(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1585(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1583(code,U32.is_lt(code,73130))
    case False{}: categoryBelow1584(code,U32.is_lt(code,73180))
def categoryBelow1586(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1587(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1588(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1586(code,U32.is_lt(code,73194))
    case False{}: categoryBelow1587(code,U32.is_lt(code,73459))
def categoryBelow1589(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1585(code,U32.is_lt(code,73136))
    case False{}: categoryBelow1588(code,U32.is_lt(code,73440))
def categoryBelow1590(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1591(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1592(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1590(code,U32.is_lt(code,73465))
    case False{}: categoryBelow1591(code,U32.is_lt(code,73475))
def categoryBelow1593(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1594(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1595(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1593(code,U32.is_lt(code,73489))
    case False{}: categoryBelow1594(code,U32.is_lt(code,73524))
def categoryBelow1596(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1592(code,U32.is_lt(code,73474))
    case False{}: categoryBelow1595(code,U32.is_lt(code,73490))
def categoryBelow1597(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1589(code,U32.is_lt(code,73184))
    case False{}: categoryBelow1596(code,U32.is_lt(code,73476))
def categoryBelow1598(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow1599(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1600(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1598(code,U32.is_lt(code,73552))
    case False{}: categoryBelow1599(code,U32.is_lt(code,73648))
def categoryBelow1601(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1602(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1603(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1601(code,U32.is_lt(code,73664))
    case False{}: categoryBelow1602(code,U32.is_lt(code,73714))
def categoryBelow1604(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1600(code,U32.is_lt(code,73562))
    case False{}: categoryBelow1603(code,U32.is_lt(code,73685))
def categoryBelow1605(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1606(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1607(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1605(code,U32.is_lt(code,73728))
    case False{}: categoryBelow1606(code,U32.is_lt(code,74752))
def categoryBelow1608(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1609(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1610(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1608(code,U32.is_lt(code,74864))
    case False{}: categoryBelow1609(code,U32.is_lt(code,74880))
def categoryBelow1611(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1607(code,U32.is_lt(code,74650))
    case False{}: categoryBelow1610(code,U32.is_lt(code,74869))
def categoryBelow1612(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1604(code,U32.is_lt(code,73649))
    case False{}: categoryBelow1611(code,U32.is_lt(code,74863))
def categoryBelow1613(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1597(code,U32.is_lt(code,73463))
    case False{}: categoryBelow1612(code,U32.is_lt(code,73727))
def categoryBelow1614(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1582(code,U32.is_lt(code,72968))
    case False{}: categoryBelow1613(code,U32.is_lt(code,73539))
def categoryBelow1615(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1616(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1617(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1615(code,U32.is_lt(code,77712))
    case False{}: categoryBelow1616(code,U32.is_lt(code,77811))
def categoryBelow1618(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1619(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1620(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1618(code,U32.is_lt(code,78896))
    case False{}: categoryBelow1619(code,U32.is_lt(code,78919))
def categoryBelow1621(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1617(code,U32.is_lt(code,77809))
    case False{}: categoryBelow1620(code,U32.is_lt(code,78913))
def categoryBelow1622(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1623(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1624(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1622(code,U32.is_lt(code,82939))
    case False{}: categoryBelow1623(code,U32.is_lt(code,83527))
def categoryBelow1625(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1626(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1627(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1625(code,U32.is_lt(code,90398))
    case False{}: categoryBelow1626(code,U32.is_lt(code,90426))
def categoryBelow1628(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1624(code,U32.is_lt(code,82944))
    case False{}: categoryBelow1627(code,U32.is_lt(code,90416))
def categoryBelow1629(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1621(code,U32.is_lt(code,77824))
    case False{}: categoryBelow1628(code,U32.is_lt(code,90368))
def categoryBelow1630(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1631(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1632(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1630(code,U32.is_lt(code,92729))
    case False{}: categoryBelow1631(code,U32.is_lt(code,92767))
def categoryBelow1633(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1634(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def categoryBelow1635(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1633(code,U32.is_lt(code,92778))
    case False{}: categoryBelow1634(code,U32.is_lt(code,92784))
def categoryBelow1636(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1632(code,U32.is_lt(code,92736))
    case False{}: categoryBelow1635(code,U32.is_lt(code,92782))
def categoryBelow1637(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1638(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1639(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1637(code,U32.is_lt(code,92864))
    case False{}: categoryBelow1638(code,U32.is_lt(code,92880))
def categoryBelow1640(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1641(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1642(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1640(code,U32.is_lt(code,92917))
    case False{}: categoryBelow1641(code,U32.is_lt(code,92928))
def categoryBelow1643(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1639(code,U32.is_lt(code,92874))
    case False{}: categoryBelow1642(code,U32.is_lt(code,92918))
def categoryBelow1644(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1636(code,U32.is_lt(code,92768))
    case False{}: categoryBelow1643(code,U32.is_lt(code,92910))
def categoryBelow1645(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1629(code,U32.is_lt(code,78944))
    case False{}: categoryBelow1644(code,U32.is_lt(code,92863))
def categoryBelow1646(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1647(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1648(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1646(code,U32.is_lt(code,92983))
    case False{}: categoryBelow1647(code,U32.is_lt(code,92992))
def categoryBelow1649(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 4
def categoryBelow1650(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1651(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1649(code,U32.is_lt(code,92997))
    case False{}: categoryBelow1650(code,U32.is_lt(code,93008))
def categoryBelow1652(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1648(code,U32.is_lt(code,92988))
    case False{}: categoryBelow1651(code,U32.is_lt(code,92998))
def categoryBelow1653(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1654(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1655(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1653(code,U32.is_lt(code,93019))
    case False{}: categoryBelow1654(code,U32.is_lt(code,93027))
def categoryBelow1656(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1657(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1658(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1656(code,U32.is_lt(code,93053))
    case False{}: categoryBelow1657(code,U32.is_lt(code,93504))
def categoryBelow1659(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1655(code,U32.is_lt(code,93026))
    case False{}: categoryBelow1658(code,U32.is_lt(code,93072))
def categoryBelow1660(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1652(code,U32.is_lt(code,92996))
    case False{}: categoryBelow1659(code,U32.is_lt(code,93048))
def categoryBelow1661(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 2
def categoryBelow1662(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1663(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1661(code,U32.is_lt(code,93552))
    case False{}: categoryBelow1662(code,U32.is_lt(code,93760))
def categoryBelow1664(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 3
def categoryBelow1665(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1666(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1664(code,U32.is_lt(code,93847))
    case False{}: categoryBelow1665(code,U32.is_lt(code,93856))
def categoryBelow1667(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1663(code,U32.is_lt(code,93562))
    case False{}: categoryBelow1666(code,U32.is_lt(code,93851))
def categoryBelow1668(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1669(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1670(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1668(code,U32.is_lt(code,93883))
    case False{}: categoryBelow1669(code,U32.is_lt(code,93952))
def categoryBelow1671(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1672(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1673(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1672(code,U32.is_lt(code,94112))
def categoryBelow1674(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1671(code,U32.is_lt(code,94032))
    case False{}: categoryBelow1673(code,U32.is_lt(code,94099))
def categoryBelow1675(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1670(code,U32.is_lt(code,93908))
    case False{}: categoryBelow1674(code,U32.is_lt(code,94033))
def categoryBelow1676(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1667(code,U32.is_lt(code,93824))
    case False{}: categoryBelow1675(code,U32.is_lt(code,94027))
def categoryBelow1677(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1660(code,U32.is_lt(code,93018))
    case False{}: categoryBelow1676(code,U32.is_lt(code,93881))
def categoryBelow1678(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1645(code,U32.is_lt(code,92160))
    case False{}: categoryBelow1677(code,U32.is_lt(code,93549))
def categoryBelow1679(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1614(code,U32.is_lt(code,73120))
    case False{}: categoryBelow1678(code,U32.is_lt(code,92976))
def categoryBelow1680(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def categoryBelow1681(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1682(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1680(code,U32.is_lt(code,94178))
    case False{}: categoryBelow1681(code,U32.is_lt(code,94180))
def categoryBelow1683(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1684(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1685(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1683(code,U32.is_lt(code,94196))
    case False{}: categoryBelow1684(code,U32.is_lt(code,94208))
def categoryBelow1686(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1682(code,U32.is_lt(code,94179))
    case False{}: categoryBelow1685(code,U32.is_lt(code,94199))
def categoryBelow1687(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1688(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1689(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1687(code,U32.is_lt(code,101631))
    case False{}: categoryBelow1688(code,U32.is_lt(code,101760))
def categoryBelow1690(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1691(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1692(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1690(code,U32.is_lt(code,110576))
    case False{}: categoryBelow1691(code,U32.is_lt(code,110581))
def categoryBelow1693(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1689(code,U32.is_lt(code,101663))
    case False{}: categoryBelow1692(code,U32.is_lt(code,110580))
def categoryBelow1694(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1686(code,U32.is_lt(code,94194))
    case False{}: categoryBelow1693(code,U32.is_lt(code,101875))
def categoryBelow1695(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1696(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1697(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1695(code,U32.is_lt(code,110589))
    case False{}: categoryBelow1696(code,U32.is_lt(code,110592))
def categoryBelow1698(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1699(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1700(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1698(code,U32.is_lt(code,110898))
    case False{}: categoryBelow1699(code,U32.is_lt(code,110928))
def categoryBelow1701(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1697(code,U32.is_lt(code,110591))
    case False{}: categoryBelow1700(code,U32.is_lt(code,110899))
def categoryBelow1702(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1703(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1704(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1702(code,U32.is_lt(code,110933))
    case False{}: categoryBelow1703(code,U32.is_lt(code,110948))
def categoryBelow1705(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1706(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1707(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1705(code,U32.is_lt(code,110960))
    case False{}: categoryBelow1706(code,U32.is_lt(code,113664))
def categoryBelow1708(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1704(code,U32.is_lt(code,110934))
    case False{}: categoryBelow1707(code,U32.is_lt(code,111356))
def categoryBelow1709(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1701(code,U32.is_lt(code,110883))
    case False{}: categoryBelow1708(code,U32.is_lt(code,110952))
def categoryBelow1710(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1694(code,U32.is_lt(code,101590))
    case False{}: categoryBelow1709(code,U32.is_lt(code,110931))
def categoryBelow1711(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1712(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1713(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1711(code,U32.is_lt(code,113776))
    case False{}: categoryBelow1712(code,U32.is_lt(code,113792))
def categoryBelow1714(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1715(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1716(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1714(code,U32.is_lt(code,113808))
    case False{}: categoryBelow1715(code,U32.is_lt(code,113820))
def categoryBelow1717(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1713(code,U32.is_lt(code,113789))
    case False{}: categoryBelow1716(code,U32.is_lt(code,113818))
def categoryBelow1718(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1719(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1720(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1718(code,U32.is_lt(code,113823))
    case False{}: categoryBelow1719(code,U32.is_lt(code,117760))
def categoryBelow1721(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow1722(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1723(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1721(code,U32.is_lt(code,118010))
    case False{}: categoryBelow1722(code,U32.is_lt(code,118016))
def categoryBelow1724(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1720(code,U32.is_lt(code,113824))
    case False{}: categoryBelow1723(code,U32.is_lt(code,118013))
def categoryBelow1725(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1717(code,U32.is_lt(code,113801))
    case False{}: categoryBelow1724(code,U32.is_lt(code,118000))
def categoryBelow1726(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1727(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1728(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1726(code,U32.is_lt(code,118458))
    case False{}: categoryBelow1727(code,U32.is_lt(code,118496))
def categoryBelow1729(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1730(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1731(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1729(code,U32.is_lt(code,118608))
    case False{}: categoryBelow1730(code,U32.is_lt(code,118784))
def categoryBelow1732(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1728(code,U32.is_lt(code,118481))
    case False{}: categoryBelow1731(code,U32.is_lt(code,118724))
def categoryBelow1733(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1734(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1735(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1733(code,U32.is_lt(code,119040))
    case False{}: categoryBelow1734(code,U32.is_lt(code,119081))
def categoryBelow1736(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1737(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1738(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1737(code,U32.is_lt(code,119173))
def categoryBelow1739(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1736(code,U32.is_lt(code,119146))
    case False{}: categoryBelow1738(code,U32.is_lt(code,119171))
def categoryBelow1740(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1735(code,U32.is_lt(code,119079))
    case False{}: categoryBelow1739(code,U32.is_lt(code,119149))
def categoryBelow1741(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1732(code,U32.is_lt(code,118513))
    case False{}: categoryBelow1740(code,U32.is_lt(code,119141))
def categoryBelow1742(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1725(code,U32.is_lt(code,113821))
    case False{}: categoryBelow1741(code,U32.is_lt(code,119030))
def categoryBelow1743(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1710(code,U32.is_lt(code,110588))
    case False{}: categoryBelow1742(code,U32.is_lt(code,118452))
def categoryBelow1744(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1745(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1746(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1744(code,U32.is_lt(code,119210))
    case False{}: categoryBelow1745(code,U32.is_lt(code,119275))
def categoryBelow1747(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1748(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1749(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1747(code,U32.is_lt(code,119362))
    case False{}: categoryBelow1748(code,U32.is_lt(code,119366))
def categoryBelow1750(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1746(code,U32.is_lt(code,119214))
    case False{}: categoryBelow1749(code,U32.is_lt(code,119365))
def categoryBelow1751(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1752(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1753(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1751(code,U32.is_lt(code,119508))
    case False{}: categoryBelow1752(code,U32.is_lt(code,119540))
def categoryBelow1754(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1755(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1756(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1754(code,U32.is_lt(code,119639))
    case False{}: categoryBelow1755(code,U32.is_lt(code,119673))
def categoryBelow1757(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1753(code,U32.is_lt(code,119520))
    case False{}: categoryBelow1756(code,U32.is_lt(code,119648))
def categoryBelow1758(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1750(code,U32.is_lt(code,119296))
    case False{}: categoryBelow1757(code,U32.is_lt(code,119552))
def categoryBelow1759(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1760(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1761(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1759(code,U32.is_lt(code,119893))
    case False{}: categoryBelow1760(code,U32.is_lt(code,119965))
def categoryBelow1762(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1763(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1764(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1762(code,U32.is_lt(code,119968))
    case False{}: categoryBelow1763(code,U32.is_lt(code,119971))
def categoryBelow1765(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1761(code,U32.is_lt(code,119894))
    case False{}: categoryBelow1764(code,U32.is_lt(code,119970))
def categoryBelow1766(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1767(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1768(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1766(code,U32.is_lt(code,119975))
    case False{}: categoryBelow1767(code,U32.is_lt(code,119981))
def categoryBelow1769(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1770(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1771(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1769(code,U32.is_lt(code,119994))
    case False{}: categoryBelow1770(code,U32.is_lt(code,119996))
def categoryBelow1772(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1768(code,U32.is_lt(code,119977))
    case False{}: categoryBelow1771(code,U32.is_lt(code,119995))
def categoryBelow1773(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1765(code,U32.is_lt(code,119966))
    case False{}: categoryBelow1772(code,U32.is_lt(code,119982))
def categoryBelow1774(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1758(code,U32.is_lt(code,119488))
    case False{}: categoryBelow1773(code,U32.is_lt(code,119973))
def categoryBelow1775(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1776(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1777(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1775(code,U32.is_lt(code,120004))
    case False{}: categoryBelow1776(code,U32.is_lt(code,120070))
def categoryBelow1778(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1779(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1780(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1778(code,U32.is_lt(code,120075))
    case False{}: categoryBelow1779(code,U32.is_lt(code,120085))
def categoryBelow1781(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1777(code,U32.is_lt(code,120005))
    case False{}: categoryBelow1780(code,U32.is_lt(code,120077))
def categoryBelow1782(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1783(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1784(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1782(code,U32.is_lt(code,120093))
    case False{}: categoryBelow1783(code,U32.is_lt(code,120122))
def categoryBelow1785(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1786(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1787(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1785(code,U32.is_lt(code,120127))
    case False{}: categoryBelow1786(code,U32.is_lt(code,120133))
def categoryBelow1788(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1784(code,U32.is_lt(code,120094))
    case False{}: categoryBelow1787(code,U32.is_lt(code,120128))
def categoryBelow1789(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1781(code,U32.is_lt(code,120071))
    case False{}: categoryBelow1788(code,U32.is_lt(code,120123))
def categoryBelow1790(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1791(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1792(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1790(code,U32.is_lt(code,120135))
    case False{}: categoryBelow1791(code,U32.is_lt(code,120145))
def categoryBelow1793(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1794(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1795(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1793(code,U32.is_lt(code,120486))
    case False{}: categoryBelow1794(code,U32.is_lt(code,120513))
def categoryBelow1796(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1792(code,U32.is_lt(code,120138))
    case False{}: categoryBelow1795(code,U32.is_lt(code,120488))
def categoryBelow1797(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1798(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1799(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1797(code,U32.is_lt(code,120539))
    case False{}: categoryBelow1798(code,U32.is_lt(code,120571))
def categoryBelow1800(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 4
def categoryBelow1801(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1802(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1801(code,U32.is_lt(code,120630))
def categoryBelow1803(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1800(code,U32.is_lt(code,120597))
    case False{}: categoryBelow1802(code,U32.is_lt(code,120629))
def categoryBelow1804(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1799(code,U32.is_lt(code,120540))
    case False{}: categoryBelow1803(code,U32.is_lt(code,120598))
def categoryBelow1805(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1796(code,U32.is_lt(code,120146))
    case False{}: categoryBelow1804(code,U32.is_lt(code,120572))
def categoryBelow1806(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1789(code,U32.is_lt(code,120086))
    case False{}: categoryBelow1805(code,U32.is_lt(code,120514))
def categoryBelow1807(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1774(code,U32.is_lt(code,119808))
    case False{}: categoryBelow1806(code,U32.is_lt(code,120134))
def categoryBelow1808(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1743(code,U32.is_lt(code,113771))
    case False{}: categoryBelow1807(code,U32.is_lt(code,119997))
def categoryBelow1809(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1679(code,U32.is_lt(code,75076))
    case False{}: categoryBelow1808(code,U32.is_lt(code,119180))
def categoryBelow1810(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1811(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1812(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1810(code,U32.is_lt(code,120656))
    case False{}: categoryBelow1811(code,U32.is_lt(code,120688))
def categoryBelow1813(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1814(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1815(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1813(code,U32.is_lt(code,120714))
    case False{}: categoryBelow1814(code,U32.is_lt(code,120746))
def categoryBelow1816(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1812(code,U32.is_lt(code,120687))
    case False{}: categoryBelow1815(code,U32.is_lt(code,120745))
def categoryBelow1817(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 1
def categoryBelow1818(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1819(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1817(code,U32.is_lt(code,120772))
    case False{}: categoryBelow1818(code,U32.is_lt(code,120782))
def categoryBelow1820(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1821(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1822(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1820(code,U32.is_lt(code,121344))
    case False{}: categoryBelow1821(code,U32.is_lt(code,121403))
def categoryBelow1823(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1819(code,U32.is_lt(code,120780))
    case False{}: categoryBelow1822(code,U32.is_lt(code,121399))
def categoryBelow1824(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1816(code,U32.is_lt(code,120713))
    case False{}: categoryBelow1823(code,U32.is_lt(code,120832))
def categoryBelow1825(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1826(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1827(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1825(code,U32.is_lt(code,121461))
    case False{}: categoryBelow1826(code,U32.is_lt(code,121476))
def categoryBelow1828(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 3
def categoryBelow1829(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1830(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1828(code,U32.is_lt(code,121479))
    case False{}: categoryBelow1829(code,U32.is_lt(code,122624))
def categoryBelow1831(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1827(code,U32.is_lt(code,121462))
    case False{}: categoryBelow1830(code,U32.is_lt(code,121484))
def categoryBelow1832(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1833(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1834(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1832(code,U32.is_lt(code,122661))
    case False{}: categoryBelow1833(code,U32.is_lt(code,122928))
def categoryBelow1835(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1836(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1837(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1835(code,U32.is_lt(code,123136))
    case False{}: categoryBelow1836(code,U32.is_lt(code,123191))
def categoryBelow1838(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1834(code,U32.is_lt(code,122667))
    case False{}: categoryBelow1837(code,U32.is_lt(code,123181))
def categoryBelow1839(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1831(code,U32.is_lt(code,121477))
    case False{}: categoryBelow1838(code,U32.is_lt(code,122990))
def categoryBelow1840(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1824(code,U32.is_lt(code,120771))
    case False{}: categoryBelow1839(code,U32.is_lt(code,122655))
def categoryBelow1841(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1842(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1843(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1841(code,U32.is_lt(code,123200))
    case False{}: categoryBelow1842(code,U32.is_lt(code,123214))
def categoryBelow1844(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1845(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1846(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1844(code,U32.is_lt(code,123216))
    case False{}: categoryBelow1845(code,U32.is_lt(code,123566))
def categoryBelow1847(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1843(code,U32.is_lt(code,123210))
    case False{}: categoryBelow1846(code,U32.is_lt(code,123536))
def categoryBelow1848(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1849(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1850(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1848(code,U32.is_lt(code,123628))
    case False{}: categoryBelow1849(code,U32.is_lt(code,123642))
def categoryBelow1851(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1852(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1853(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1851(code,U32.is_lt(code,123648))
    case False{}: categoryBelow1852(code,U32.is_lt(code,124140))
def categoryBelow1854(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1850(code,U32.is_lt(code,123632))
    case False{}: categoryBelow1853(code,U32.is_lt(code,124112))
def categoryBelow1855(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1847(code,U32.is_lt(code,123215))
    case False{}: categoryBelow1854(code,U32.is_lt(code,123647))
def categoryBelow1856(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1857(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1858(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1856(code,U32.is_lt(code,124154))
    case False{}: categoryBelow1857(code,U32.is_lt(code,124398))
def categoryBelow1859(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1860(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def categoryBelow1861(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1859(code,U32.is_lt(code,124401))
    case False{}: categoryBelow1860(code,U32.is_lt(code,124415))
def categoryBelow1862(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1858(code,U32.is_lt(code,124368))
    case False{}: categoryBelow1861(code,U32.is_lt(code,124411))
def categoryBelow1863(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1864(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1865(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1863(code,U32.is_lt(code,124608))
    case False{}: categoryBelow1864(code,U32.is_lt(code,124640))
def categoryBelow1866(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1867(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1868(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1867(code,U32.is_lt(code,124654))
def categoryBelow1869(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1866(code,U32.is_lt(code,124644))
    case False{}: categoryBelow1868(code,U32.is_lt(code,124647))
def categoryBelow1870(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1865(code,U32.is_lt(code,124639))
    case False{}: categoryBelow1869(code,U32.is_lt(code,124646))
def categoryBelow1871(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1862(code,U32.is_lt(code,124400))
    case False{}: categoryBelow1870(code,U32.is_lt(code,124643))
def categoryBelow1872(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1855(code,U32.is_lt(code,123584))
    case False{}: categoryBelow1871(code,U32.is_lt(code,124416))
def categoryBelow1873(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1840(code,U32.is_lt(code,121453))
    case False{}: categoryBelow1872(code,U32.is_lt(code,124144))
def categoryBelow1874(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1875(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1876(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1874(code,U32.is_lt(code,124661))
    case False{}: categoryBelow1875(code,U32.is_lt(code,124672))
def categoryBelow1877(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1878(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1879(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1877(code,U32.is_lt(code,124903))
    case False{}: categoryBelow1878(code,U32.is_lt(code,124908))
def categoryBelow1880(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1876(code,U32.is_lt(code,124670))
    case False{}: categoryBelow1879(code,U32.is_lt(code,124904))
def categoryBelow1881(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1882(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1883(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1881(code,U32.is_lt(code,124911))
    case False{}: categoryBelow1882(code,U32.is_lt(code,124927))
def categoryBelow1884(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1885(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1886(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1884(code,U32.is_lt(code,125125))
    case False{}: categoryBelow1885(code,U32.is_lt(code,125136))
def categoryBelow1887(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1883(code,U32.is_lt(code,124912))
    case False{}: categoryBelow1886(code,U32.is_lt(code,125127))
def categoryBelow1888(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1880(code,U32.is_lt(code,124896))
    case False{}: categoryBelow1887(code,U32.is_lt(code,124928))
def categoryBelow1889(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1890(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1891(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1889(code,U32.is_lt(code,125252))
    case False{}: categoryBelow1890(code,U32.is_lt(code,125260))
def categoryBelow1892(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1893(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def categoryBelow1894(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1892(code,U32.is_lt(code,125274))
    case False{}: categoryBelow1893(code,U32.is_lt(code,125280))
def categoryBelow1895(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1891(code,U32.is_lt(code,125259))
    case False{}: categoryBelow1894(code,U32.is_lt(code,125278))
def categoryBelow1896(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow1897(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow1898(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1896(code,U32.is_lt(code,126124))
    case False{}: categoryBelow1897(code,U32.is_lt(code,126128))
def categoryBelow1899(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1900(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow1901(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1899(code,U32.is_lt(code,126133))
    case False{}: categoryBelow1900(code,U32.is_lt(code,126254))
def categoryBelow1902(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1898(code,U32.is_lt(code,126125))
    case False{}: categoryBelow1901(code,U32.is_lt(code,126209))
def categoryBelow1903(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1895(code,U32.is_lt(code,125264))
    case False{}: categoryBelow1902(code,U32.is_lt(code,126129))
def categoryBelow1904(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1888(code,U32.is_lt(code,124909))
    case False{}: categoryBelow1903(code,U32.is_lt(code,126065))
def categoryBelow1905(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1906(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1907(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1905(code,U32.is_lt(code,126270))
    case False{}: categoryBelow1906(code,U32.is_lt(code,126468))
def categoryBelow1908(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1909(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1910(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1908(code,U32.is_lt(code,126496))
    case False{}: categoryBelow1909(code,U32.is_lt(code,126499))
def categoryBelow1911(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1907(code,U32.is_lt(code,126464))
    case False{}: categoryBelow1910(code,U32.is_lt(code,126497))
def categoryBelow1912(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1913(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1914(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1912(code,U32.is_lt(code,126501))
    case False{}: categoryBelow1913(code,U32.is_lt(code,126504))
def categoryBelow1915(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1916(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1917(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1915(code,U32.is_lt(code,126515))
    case False{}: categoryBelow1916(code,U32.is_lt(code,126520))
def categoryBelow1918(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1914(code,U32.is_lt(code,126503))
    case False{}: categoryBelow1917(code,U32.is_lt(code,126516))
def categoryBelow1919(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1911(code,U32.is_lt(code,126469))
    case False{}: categoryBelow1918(code,U32.is_lt(code,126505))
def categoryBelow1920(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1921(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1922(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1920(code,U32.is_lt(code,126522))
    case False{}: categoryBelow1921(code,U32.is_lt(code,126524))
def categoryBelow1923(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1924(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1925(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1923(code,U32.is_lt(code,126531))
    case False{}: categoryBelow1924(code,U32.is_lt(code,126536))
def categoryBelow1926(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1922(code,U32.is_lt(code,126523))
    case False{}: categoryBelow1925(code,U32.is_lt(code,126535))
def categoryBelow1927(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1928(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1929(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1927(code,U32.is_lt(code,126538))
    case False{}: categoryBelow1928(code,U32.is_lt(code,126540))
def categoryBelow1930(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1931(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1932(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1931(code,U32.is_lt(code,126548))
def categoryBelow1933(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1930(code,U32.is_lt(code,126544))
    case False{}: categoryBelow1932(code,U32.is_lt(code,126547))
def categoryBelow1934(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1929(code,U32.is_lt(code,126539))
    case False{}: categoryBelow1933(code,U32.is_lt(code,126545))
def categoryBelow1935(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1926(code,U32.is_lt(code,126530))
    case False{}: categoryBelow1934(code,U32.is_lt(code,126541))
def categoryBelow1936(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1919(code,U32.is_lt(code,126500))
    case False{}: categoryBelow1935(code,U32.is_lt(code,126537))
def categoryBelow1937(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1904(code,U32.is_lt(code,125184))
    case False{}: categoryBelow1936(code,U32.is_lt(code,126521))
def categoryBelow1938(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1873(code,U32.is_lt(code,123198))
    case False{}: categoryBelow1937(code,U32.is_lt(code,126255))
def categoryBelow1939(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1940(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1941(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1939(code,U32.is_lt(code,126551))
    case False{}: categoryBelow1940(code,U32.is_lt(code,126553))
def categoryBelow1942(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1943(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1944(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1942(code,U32.is_lt(code,126555))
    case False{}: categoryBelow1943(code,U32.is_lt(code,126557))
def categoryBelow1945(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1941(code,U32.is_lt(code,126552))
    case False{}: categoryBelow1944(code,U32.is_lt(code,126556))
def categoryBelow1946(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1947(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1948(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1946(code,U32.is_lt(code,126559))
    case False{}: categoryBelow1947(code,U32.is_lt(code,126561))
def categoryBelow1949(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1950(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1951(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1949(code,U32.is_lt(code,126564))
    case False{}: categoryBelow1950(code,U32.is_lt(code,126567))
def categoryBelow1952(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1948(code,U32.is_lt(code,126560))
    case False{}: categoryBelow1951(code,U32.is_lt(code,126565))
def categoryBelow1953(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1945(code,U32.is_lt(code,126554))
    case False{}: categoryBelow1952(code,U32.is_lt(code,126563))
def categoryBelow1954(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1955(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1956(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1954(code,U32.is_lt(code,126572))
    case False{}: categoryBelow1955(code,U32.is_lt(code,126580))
def categoryBelow1957(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1958(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1959(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1957(code,U32.is_lt(code,126585))
    case False{}: categoryBelow1958(code,U32.is_lt(code,126590))
def categoryBelow1960(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1956(code,U32.is_lt(code,126579))
    case False{}: categoryBelow1959(code,U32.is_lt(code,126589))
def categoryBelow1961(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1962(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1963(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1961(code,U32.is_lt(code,126592))
    case False{}: categoryBelow1962(code,U32.is_lt(code,126603))
def categoryBelow1964(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1965(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1966(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1964(code,U32.is_lt(code,126625))
    case False{}: categoryBelow1965(code,U32.is_lt(code,126629))
def categoryBelow1967(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1963(code,U32.is_lt(code,126602))
    case False{}: categoryBelow1966(code,U32.is_lt(code,126628))
def categoryBelow1968(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1960(code,U32.is_lt(code,126584))
    case False{}: categoryBelow1967(code,U32.is_lt(code,126620))
def categoryBelow1969(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1953(code,U32.is_lt(code,126558))
    case False{}: categoryBelow1968(code,U32.is_lt(code,126591))
def categoryBelow1970(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1971(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1972(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1970(code,U32.is_lt(code,126635))
    case False{}: categoryBelow1971(code,U32.is_lt(code,126704))
def categoryBelow1973(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1974(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1975(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1973(code,U32.is_lt(code,126976))
    case False{}: categoryBelow1974(code,U32.is_lt(code,127024))
def categoryBelow1976(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1972(code,U32.is_lt(code,126652))
    case False{}: categoryBelow1975(code,U32.is_lt(code,127020))
def categoryBelow1977(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1978(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1979(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1977(code,U32.is_lt(code,127136))
    case False{}: categoryBelow1978(code,U32.is_lt(code,127153))
def categoryBelow1980(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1981(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1982(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1980(code,U32.is_lt(code,127169))
    case False{}: categoryBelow1981(code,U32.is_lt(code,127185))
def categoryBelow1983(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1979(code,U32.is_lt(code,127151))
    case False{}: categoryBelow1982(code,U32.is_lt(code,127184))
def categoryBelow1984(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1976(code,U32.is_lt(code,126706))
    case False{}: categoryBelow1983(code,U32.is_lt(code,127168))
def categoryBelow1985(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1986(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1987(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1985(code,U32.is_lt(code,127232))
    case False{}: categoryBelow1986(code,U32.is_lt(code,127406))
def categoryBelow1988(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1989(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1990(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1988(code,U32.is_lt(code,127491))
    case False{}: categoryBelow1989(code,U32.is_lt(code,127548))
def categoryBelow1991(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1987(code,U32.is_lt(code,127245))
    case False{}: categoryBelow1990(code,U32.is_lt(code,127504))
def categoryBelow1992(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1993(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1994(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1992(code,U32.is_lt(code,127561))
    case False{}: categoryBelow1993(code,U32.is_lt(code,127570))
def categoryBelow1995(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def categoryBelow1996(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow1997(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: categoryBelow1996(code,U32.is_lt(code,128732))
def categoryBelow1998(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1995(code,U32.is_lt(code,127590))
    case False{}: categoryBelow1997(code,U32.is_lt(code,128729))
def categoryBelow1999(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1994(code,U32.is_lt(code,127568))
    case False{}: categoryBelow1998(code,U32.is_lt(code,127744))
def categoryBelow2000(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1991(code,U32.is_lt(code,127462))
    case False{}: categoryBelow1999(code,U32.is_lt(code,127584))
def categoryBelow2001(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1984(code,U32.is_lt(code,127124))
    case False{}: categoryBelow2000(code,U32.is_lt(code,127552))
def categoryBelow2002(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1969(code,U32.is_lt(code,126571))
    case False{}: categoryBelow2001(code,U32.is_lt(code,127222))
def categoryBelow2003(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2004(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2005(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2003(code,U32.is_lt(code,128752))
    case False{}: categoryBelow2004(code,U32.is_lt(code,128768))
def categoryBelow2006(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2007(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2008(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2006(code,U32.is_lt(code,128992))
    case False{}: categoryBelow2007(code,U32.is_lt(code,129008))
def categoryBelow2009(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2005(code,U32.is_lt(code,128765))
    case False{}: categoryBelow2008(code,U32.is_lt(code,129004))
def categoryBelow2010(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2011(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2012(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2010(code,U32.is_lt(code,129024))
    case False{}: categoryBelow2011(code,U32.is_lt(code,129040))
def categoryBelow2013(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2014(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2015(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2013(code,U32.is_lt(code,129104))
    case False{}: categoryBelow2014(code,U32.is_lt(code,129120))
def categoryBelow2016(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2012(code,U32.is_lt(code,129036))
    case False{}: categoryBelow2015(code,U32.is_lt(code,129114))
def categoryBelow2017(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2009(code,U32.is_lt(code,128986))
    case False{}: categoryBelow2016(code,U32.is_lt(code,129096))
def categoryBelow2018(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2019(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2020(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2018(code,U32.is_lt(code,129168))
    case False{}: categoryBelow2019(code,U32.is_lt(code,129200))
def categoryBelow2021(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2022(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2023(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2021(code,U32.is_lt(code,129216))
    case False{}: categoryBelow2022(code,U32.is_lt(code,129232))
def categoryBelow2024(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2020(code,U32.is_lt(code,129198))
    case False{}: categoryBelow2023(code,U32.is_lt(code,129218))
def categoryBelow2025(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2026(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2027(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2025(code,U32.is_lt(code,129280))
    case False{}: categoryBelow2026(code,U32.is_lt(code,129632))
def categoryBelow2028(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2029(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2030(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2028(code,U32.is_lt(code,129648))
    case False{}: categoryBelow2029(code,U32.is_lt(code,129664))
def categoryBelow2031(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2027(code,U32.is_lt(code,129624))
    case False{}: categoryBelow2030(code,U32.is_lt(code,129661))
def categoryBelow2032(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2024(code,U32.is_lt(code,129212))
    case False{}: categoryBelow2031(code,U32.is_lt(code,129646))
def categoryBelow2033(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2017(code,U32.is_lt(code,129009))
    case False{}: categoryBelow2032(code,U32.is_lt(code,129241))
def categoryBelow2034(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2035(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2036(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2034(code,U32.is_lt(code,129678))
    case False{}: categoryBelow2035(code,U32.is_lt(code,129736))
def categoryBelow2037(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2038(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2039(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2037(code,U32.is_lt(code,129741))
    case False{}: categoryBelow2038(code,U32.is_lt(code,129759))
def categoryBelow2040(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2036(code,U32.is_lt(code,129735))
    case False{}: categoryBelow2039(code,U32.is_lt(code,129757))
def categoryBelow2041(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2042(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2043(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2041(code,U32.is_lt(code,129775))
    case False{}: categoryBelow2042(code,U32.is_lt(code,129792))
def categoryBelow2044(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def categoryBelow2045(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 4
def categoryBelow2046(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2044(code,U32.is_lt(code,129940))
    case False{}: categoryBelow2045(code,U32.is_lt(code,130042))
def categoryBelow2047(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2043(code,U32.is_lt(code,129785))
    case False{}: categoryBelow2046(code,U32.is_lt(code,130032))
def categoryBelow2048(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2040(code,U32.is_lt(code,129737))
    case False{}: categoryBelow2047(code,U32.is_lt(code,129939))
def categoryBelow2049(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2050(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2051(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2049(code,U32.is_lt(code,131072))
    case False{}: categoryBelow2050(code,U32.is_lt(code,173824))
def categoryBelow2052(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2053(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2054(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2052(code,U32.is_lt(code,178208))
    case False{}: categoryBelow2053(code,U32.is_lt(code,183984))
def categoryBelow2055(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2051(code,U32.is_lt(code,173792))
    case False{}: categoryBelow2054(code,U32.is_lt(code,183982))
def categoryBelow2056(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2057(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2058(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2056(code,U32.is_lt(code,191472))
    case False{}: categoryBelow2057(code,U32.is_lt(code,194560))
def categoryBelow2059(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow2060(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow2061(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow2060(code,U32.is_lt(code,210042))
def categoryBelow2062(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2059(code,U32.is_lt(code,196608))
    case False{}: categoryBelow2061(code,U32.is_lt(code,201552))
def categoryBelow2063(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2058(code,U32.is_lt(code,192094))
    case False{}: categoryBelow2062(code,U32.is_lt(code,201547))
def categoryBelow2064(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2055(code,U32.is_lt(code,178206))
    case False{}: categoryBelow2063(code,U32.is_lt(code,195102))
def categoryBelow2065(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2048(code,U32.is_lt(code,129771))
    case False{}: categoryBelow2064(code,U32.is_lt(code,191457))
def categoryBelow2066(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2033(code,U32.is_lt(code,129160))
    case False{}: categoryBelow2065(code,U32.is_lt(code,130043))
def categoryBelow2067(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow2002(code,U32.is_lt(code,126634))
    case False{}: categoryBelow2066(code,U32.is_lt(code,129675))
def categoryBelow2068(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1938(code,U32.is_lt(code,124656))
    case False{}: categoryBelow2067(code,U32.is_lt(code,128749))
def categoryBelow2069(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1809(code,U32.is_lt(code,94176))
    case False{}: categoryBelow2068(code,U32.is_lt(code,126549))
def categoryBelow2070(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1551(code,U32.is_lt(code,68736))
    case False{}: categoryBelow2069(code,U32.is_lt(code,120655))
def categoryBelow2071(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1034(code,U32.is_lt(code,4960))
    case False{}: categoryBelow2070(code,U32.is_lt(code,72688))

def category(+code: U32) -> U32:
  categoryBelow2071(code,U32.is_lt(code,64297))
def isLetter(+code: U32) -> Bool: U32.is_eq(category(code),1)
def isNumber(+code: U32) -> Bool: U32.is_eq(category(code),2)
def isPunctuation(+code: U32) -> Bool: U32.is_eq(category(code),3)
def isSymbol(+code: U32) -> Bool: U32.is_eq(category(code),4)
