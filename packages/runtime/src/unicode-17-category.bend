# Generated by scripts/generate-category-unicode.py from Unicode17.0.0.
# Unicode License V3; see unicode-LICENSE.txt. Constant decisions (plain definitions) allocate no table.
# General_Category class of a scalar: 1 = Letter (L*), 2 = Number (N*), 0 = other.
import Base

# 1609 runs.
def categoryBelow0(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow0(code,U32.is_lt(code,58))
def categoryBelow2(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow3(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow2(code,U32.is_lt(code,97))
def categoryBelow4(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1(code,U32.is_lt(code,48))
    case False{}: categoryBelow3(code,U32.is_lt(code,91))
def categoryBelow5(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow6(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow5(code,U32.is_lt(code,171))
def categoryBelow7(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow8(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow7(code,U32.is_lt(code,181))
def categoryBelow9(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow6(code,U32.is_lt(code,170))
    case False{}: categoryBelow8(code,U32.is_lt(code,180))
def categoryBelow10(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow4(code,U32.is_lt(code,65))
    case False{}: categoryBelow9(code,U32.is_lt(code,178))
def categoryBelow11(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow12(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow11(code,U32.is_lt(code,186))
def categoryBelow13(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow14(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow13(code,U32.is_lt(code,191))
def categoryBelow15(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow12(code,U32.is_lt(code,185))
    case False{}: categoryBelow14(code,U32.is_lt(code,188))
def categoryBelow16(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow17(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow16(code,U32.is_lt(code,216))
def categoryBelow18(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow19(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow20(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow18(code,U32.is_lt(code,248))
    case False{}: categoryBelow19(code,U32.is_lt(code,710))
def categoryBelow21(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow17(code,U32.is_lt(code,215))
    case False{}: categoryBelow20(code,U32.is_lt(code,706))
def categoryBelow22(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow15(code,U32.is_lt(code,187))
    case False{}: categoryBelow21(code,U32.is_lt(code,247))
def categoryBelow23(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow10(code,U32.is_lt(code,123))
    case False{}: categoryBelow22(code,U32.is_lt(code,192))
def categoryBelow24(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow25(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow24(code,U32.is_lt(code,741))
def categoryBelow26(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow27(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow26(code,U32.is_lt(code,750))
def categoryBelow28(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow25(code,U32.is_lt(code,736))
    case False{}: categoryBelow27(code,U32.is_lt(code,749))
def categoryBelow29(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow30(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow29(code,U32.is_lt(code,885))
def categoryBelow31(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow32(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow31(code,U32.is_lt(code,890))
def categoryBelow33(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow30(code,U32.is_lt(code,880))
    case False{}: categoryBelow32(code,U32.is_lt(code,888))
def categoryBelow34(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow28(code,U32.is_lt(code,748))
    case False{}: categoryBelow33(code,U32.is_lt(code,886))
def categoryBelow35(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow36(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow35(code,U32.is_lt(code,896))
def categoryBelow37(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow38(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow37(code,U32.is_lt(code,904))
def categoryBelow39(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow36(code,U32.is_lt(code,895))
    case False{}: categoryBelow38(code,U32.is_lt(code,903))
def categoryBelow40(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow41(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow40(code,U32.is_lt(code,909))
def categoryBelow42(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow43(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow44(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow42(code,U32.is_lt(code,930))
    case False{}: categoryBelow43(code,U32.is_lt(code,1014))
def categoryBelow45(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow41(code,U32.is_lt(code,908))
    case False{}: categoryBelow44(code,U32.is_lt(code,931))
def categoryBelow46(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow39(code,U32.is_lt(code,902))
    case False{}: categoryBelow45(code,U32.is_lt(code,910))
def categoryBelow47(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow34(code,U32.is_lt(code,751))
    case False{}: categoryBelow46(code,U32.is_lt(code,907))
def categoryBelow48(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow23(code,U32.is_lt(code,182))
    case False{}: categoryBelow47(code,U32.is_lt(code,894))
def categoryBelow49(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow50(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow49(code,U32.is_lt(code,1162))
def categoryBelow51(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow52(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow51(code,U32.is_lt(code,1367))
def categoryBelow53(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow50(code,U32.is_lt(code,1154))
    case False{}: categoryBelow52(code,U32.is_lt(code,1329))
def categoryBelow54(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow55(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow54(code,U32.is_lt(code,1376))
def categoryBelow56(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow57(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow56(code,U32.is_lt(code,1515))
def categoryBelow58(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow55(code,U32.is_lt(code,1370))
    case False{}: categoryBelow57(code,U32.is_lt(code,1488))
def categoryBelow59(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow53(code,U32.is_lt(code,1328))
    case False{}: categoryBelow58(code,U32.is_lt(code,1417))
def categoryBelow60(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow61(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow60(code,U32.is_lt(code,1568))
def categoryBelow62(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow63(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow62(code,U32.is_lt(code,1642))
def categoryBelow64(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow61(code,U32.is_lt(code,1523))
    case False{}: categoryBelow63(code,U32.is_lt(code,1632))
def categoryBelow65(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow66(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow65(code,U32.is_lt(code,1649))
def categoryBelow67(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow68(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow69(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow67(code,U32.is_lt(code,1749))
    case False{}: categoryBelow68(code,U32.is_lt(code,1765))
def categoryBelow70(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow66(code,U32.is_lt(code,1648))
    case False{}: categoryBelow69(code,U32.is_lt(code,1750))
def categoryBelow71(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow64(code,U32.is_lt(code,1611))
    case False{}: categoryBelow70(code,U32.is_lt(code,1748))
def categoryBelow72(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow59(code,U32.is_lt(code,1369))
    case False{}: categoryBelow71(code,U32.is_lt(code,1646))
def categoryBelow73(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow74(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow73(code,U32.is_lt(code,1776))
def categoryBelow75(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow76(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow75(code,U32.is_lt(code,1791))
def categoryBelow77(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow74(code,U32.is_lt(code,1774))
    case False{}: categoryBelow76(code,U32.is_lt(code,1789))
def categoryBelow78(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow79(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow78(code,U32.is_lt(code,1809))
def categoryBelow80(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow81(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow80(code,U32.is_lt(code,1869))
def categoryBelow82(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow79(code,U32.is_lt(code,1808))
    case False{}: categoryBelow81(code,U32.is_lt(code,1840))
def categoryBelow83(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow77(code,U32.is_lt(code,1786))
    case False{}: categoryBelow82(code,U32.is_lt(code,1810))
def categoryBelow84(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow85(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow84(code,U32.is_lt(code,1970))
def categoryBelow86(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow87(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow86(code,U32.is_lt(code,2027))
def categoryBelow88(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow85(code,U32.is_lt(code,1969))
    case False{}: categoryBelow87(code,U32.is_lt(code,1994))
def categoryBelow89(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow90(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow89(code,U32.is_lt(code,2042))
def categoryBelow91(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow92(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow93(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow91(code,U32.is_lt(code,2048))
    case False{}: categoryBelow92(code,U32.is_lt(code,2074))
def categoryBelow94(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow90(code,U32.is_lt(code,2038))
    case False{}: categoryBelow93(code,U32.is_lt(code,2070))
def categoryBelow95(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow88(code,U32.is_lt(code,1984))
    case False{}: categoryBelow94(code,U32.is_lt(code,2043))
def categoryBelow96(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow83(code,U32.is_lt(code,1792))
    case False{}: categoryBelow95(code,U32.is_lt(code,2036))
def categoryBelow97(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow72(code,U32.is_lt(code,1519))
    case False{}: categoryBelow96(code,U32.is_lt(code,1958))
def categoryBelow98(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow48(code,U32.is_lt(code,722))
    case False{}: categoryBelow97(code,U32.is_lt(code,1767))
def categoryBelow99(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow99(code,U32.is_lt(code,2085))
def categoryBelow101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow101(code,U32.is_lt(code,2112))
def categoryBelow103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow100(code,U32.is_lt(code,2084))
    case False{}: categoryBelow102(code,U32.is_lt(code,2089))
def categoryBelow104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow104(code,U32.is_lt(code,2155))
def categoryBelow106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow106(code,U32.is_lt(code,2185))
def categoryBelow108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow105(code,U32.is_lt(code,2144))
    case False{}: categoryBelow107(code,U32.is_lt(code,2184))
def categoryBelow109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow103(code,U32.is_lt(code,2088))
    case False{}: categoryBelow108(code,U32.is_lt(code,2160))
def categoryBelow110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow110(code,U32.is_lt(code,2250))
def categoryBelow112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow112(code,U32.is_lt(code,2365))
def categoryBelow114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow111(code,U32.is_lt(code,2208))
    case False{}: categoryBelow113(code,U32.is_lt(code,2362))
def categoryBelow115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow115(code,U32.is_lt(code,2385))
def categoryBelow117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow117(code,U32.is_lt(code,2402))
    case False{}: categoryBelow118(code,U32.is_lt(code,2416))
def categoryBelow120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow116(code,U32.is_lt(code,2384))
    case False{}: categoryBelow119(code,U32.is_lt(code,2406))
def categoryBelow121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow114(code,U32.is_lt(code,2308))
    case False{}: categoryBelow120(code,U32.is_lt(code,2392))
def categoryBelow122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow109(code,U32.is_lt(code,2137))
    case False{}: categoryBelow121(code,U32.is_lt(code,2366))
def categoryBelow123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow123(code,U32.is_lt(code,2437))
def categoryBelow125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow125(code,U32.is_lt(code,2449))
def categoryBelow127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow124(code,U32.is_lt(code,2433))
    case False{}: categoryBelow126(code,U32.is_lt(code,2447))
def categoryBelow128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow128(code,U32.is_lt(code,2474))
def categoryBelow130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow130(code,U32.is_lt(code,2483))
def categoryBelow132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow129(code,U32.is_lt(code,2473))
    case False{}: categoryBelow131(code,U32.is_lt(code,2482))
def categoryBelow133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow127(code,U32.is_lt(code,2445))
    case False{}: categoryBelow132(code,U32.is_lt(code,2481))
def categoryBelow134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow134(code,U32.is_lt(code,2493))
def categoryBelow136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow136(code,U32.is_lt(code,2511))
def categoryBelow138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow135(code,U32.is_lt(code,2490))
    case False{}: categoryBelow137(code,U32.is_lt(code,2510))
def categoryBelow139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow139(code,U32.is_lt(code,2527))
def categoryBelow141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow141(code,U32.is_lt(code,2534))
    case False{}: categoryBelow142(code,U32.is_lt(code,2546))
def categoryBelow144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow140(code,U32.is_lt(code,2526))
    case False{}: categoryBelow143(code,U32.is_lt(code,2544))
def categoryBelow145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow138(code,U32.is_lt(code,2494))
    case False{}: categoryBelow144(code,U32.is_lt(code,2530))
def categoryBelow146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow133(code,U32.is_lt(code,2451))
    case False{}: categoryBelow145(code,U32.is_lt(code,2524))
def categoryBelow147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow122(code,U32.is_lt(code,2192))
    case False{}: categoryBelow146(code,U32.is_lt(code,2486))
def categoryBelow148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow148(code,U32.is_lt(code,2556))
def categoryBelow150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow150(code,U32.is_lt(code,2571))
def categoryBelow152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow149(code,U32.is_lt(code,2554))
    case False{}: categoryBelow151(code,U32.is_lt(code,2565))
def categoryBelow153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow153(code,U32.is_lt(code,2579))
def categoryBelow155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow155(code,U32.is_lt(code,2609))
def categoryBelow157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow154(code,U32.is_lt(code,2577))
    case False{}: categoryBelow156(code,U32.is_lt(code,2602))
def categoryBelow158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow152(code,U32.is_lt(code,2557))
    case False{}: categoryBelow157(code,U32.is_lt(code,2601))
def categoryBelow159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow159(code,U32.is_lt(code,2613))
def categoryBelow161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow161(code,U32.is_lt(code,2618))
def categoryBelow163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow160(code,U32.is_lt(code,2612))
    case False{}: categoryBelow162(code,U32.is_lt(code,2616))
def categoryBelow164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow164(code,U32.is_lt(code,2654))
def categoryBelow166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow166(code,U32.is_lt(code,2662))
    case False{}: categoryBelow167(code,U32.is_lt(code,2674))
def categoryBelow169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow165(code,U32.is_lt(code,2653))
    case False{}: categoryBelow168(code,U32.is_lt(code,2672))
def categoryBelow170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow163(code,U32.is_lt(code,2615))
    case False{}: categoryBelow169(code,U32.is_lt(code,2655))
def categoryBelow171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow158(code,U32.is_lt(code,2575))
    case False{}: categoryBelow170(code,U32.is_lt(code,2649))
def categoryBelow172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow172(code,U32.is_lt(code,2702))
def categoryBelow174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow174(code,U32.is_lt(code,2707))
def categoryBelow176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow173(code,U32.is_lt(code,2693))
    case False{}: categoryBelow175(code,U32.is_lt(code,2706))
def categoryBelow177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow177(code,U32.is_lt(code,2737))
def categoryBelow179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow179(code,U32.is_lt(code,2740))
    case False{}: categoryBelow180(code,U32.is_lt(code,2746))
def categoryBelow182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow178(code,U32.is_lt(code,2730))
    case False{}: categoryBelow181(code,U32.is_lt(code,2741))
def categoryBelow183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow176(code,U32.is_lt(code,2703))
    case False{}: categoryBelow182(code,U32.is_lt(code,2738))
def categoryBelow184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow184(code,U32.is_lt(code,2768))
def categoryBelow186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow186(code,U32.is_lt(code,2786))
def categoryBelow188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow185(code,U32.is_lt(code,2750))
    case False{}: categoryBelow187(code,U32.is_lt(code,2784))
def categoryBelow189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow189(code,U32.is_lt(code,2809))
def categoryBelow191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow191(code,U32.is_lt(code,2821))
    case False{}: categoryBelow192(code,U32.is_lt(code,2831))
def categoryBelow194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow190(code,U32.is_lt(code,2800))
    case False{}: categoryBelow193(code,U32.is_lt(code,2829))
def categoryBelow195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow188(code,U32.is_lt(code,2769))
    case False{}: categoryBelow194(code,U32.is_lt(code,2810))
def categoryBelow196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow183(code,U32.is_lt(code,2729))
    case False{}: categoryBelow195(code,U32.is_lt(code,2790))
def categoryBelow197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow171(code,U32.is_lt(code,2610))
    case False{}: categoryBelow196(code,U32.is_lt(code,2749))
def categoryBelow198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow147(code,U32.is_lt(code,2417))
    case False{}: categoryBelow197(code,U32.is_lt(code,2677))
def categoryBelow199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow98(code,U32.is_lt(code,1015))
    case False{}: categoryBelow198(code,U32.is_lt(code,2548))
def categoryBelow200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow200(code,U32.is_lt(code,2857))
def categoryBelow202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow202(code,U32.is_lt(code,2866))
def categoryBelow204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow201(code,U32.is_lt(code,2835))
    case False{}: categoryBelow203(code,U32.is_lt(code,2865))
def categoryBelow205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow205(code,U32.is_lt(code,2874))
def categoryBelow207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow207(code,U32.is_lt(code,2908))
def categoryBelow209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow206(code,U32.is_lt(code,2869))
    case False{}: categoryBelow208(code,U32.is_lt(code,2878))
def categoryBelow210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow204(code,U32.is_lt(code,2858))
    case False{}: categoryBelow209(code,U32.is_lt(code,2877))
def categoryBelow211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow211(code,U32.is_lt(code,2914))
def categoryBelow213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow213(code,U32.is_lt(code,2929))
def categoryBelow215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow212(code,U32.is_lt(code,2911))
    case False{}: categoryBelow214(code,U32.is_lt(code,2928))
def categoryBelow216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow216(code,U32.is_lt(code,2947))
def categoryBelow218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow218(code,U32.is_lt(code,2949))
    case False{}: categoryBelow219(code,U32.is_lt(code,2958))
def categoryBelow221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow217(code,U32.is_lt(code,2936))
    case False{}: categoryBelow220(code,U32.is_lt(code,2955))
def categoryBelow222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow215(code,U32.is_lt(code,2918))
    case False{}: categoryBelow221(code,U32.is_lt(code,2948))
def categoryBelow223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow210(code,U32.is_lt(code,2868))
    case False{}: categoryBelow222(code,U32.is_lt(code,2930))
def categoryBelow224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow224(code,U32.is_lt(code,2966))
def categoryBelow226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow226(code,U32.is_lt(code,2972))
def categoryBelow228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow225(code,U32.is_lt(code,2962))
    case False{}: categoryBelow227(code,U32.is_lt(code,2971))
def categoryBelow229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow229(code,U32.is_lt(code,2976))
def categoryBelow231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow231(code,U32.is_lt(code,2984))
def categoryBelow233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow230(code,U32.is_lt(code,2974))
    case False{}: categoryBelow232(code,U32.is_lt(code,2981))
def categoryBelow234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow228(code,U32.is_lt(code,2969))
    case False{}: categoryBelow233(code,U32.is_lt(code,2979))
def categoryBelow235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow235(code,U32.is_lt(code,3002))
def categoryBelow237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow237(code,U32.is_lt(code,3046))
def categoryBelow239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow236(code,U32.is_lt(code,2990))
    case False{}: categoryBelow238(code,U32.is_lt(code,3025))
def categoryBelow240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow240(code,U32.is_lt(code,3085))
def categoryBelow242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow242(code,U32.is_lt(code,3089))
    case False{}: categoryBelow243(code,U32.is_lt(code,3113))
def categoryBelow245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow241(code,U32.is_lt(code,3077))
    case False{}: categoryBelow244(code,U32.is_lt(code,3090))
def categoryBelow246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow239(code,U32.is_lt(code,3024))
    case False{}: categoryBelow245(code,U32.is_lt(code,3086))
def categoryBelow247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow234(code,U32.is_lt(code,2973))
    case False{}: categoryBelow246(code,U32.is_lt(code,3059))
def categoryBelow248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow223(code,U32.is_lt(code,2910))
    case False{}: categoryBelow247(code,U32.is_lt(code,2987))
def categoryBelow249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow249(code,U32.is_lt(code,3133))
def categoryBelow251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow251(code,U32.is_lt(code,3163))
def categoryBelow253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow250(code,U32.is_lt(code,3130))
    case False{}: categoryBelow252(code,U32.is_lt(code,3160))
def categoryBelow254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow254(code,U32.is_lt(code,3168))
def categoryBelow256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow256(code,U32.is_lt(code,3184))
def categoryBelow258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow255(code,U32.is_lt(code,3166))
    case False{}: categoryBelow257(code,U32.is_lt(code,3174))
def categoryBelow259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow253(code,U32.is_lt(code,3134))
    case False{}: categoryBelow258(code,U32.is_lt(code,3170))
def categoryBelow260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow260(code,U32.is_lt(code,3200))
def categoryBelow262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow262(code,U32.is_lt(code,3213))
def categoryBelow264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow261(code,U32.is_lt(code,3199))
    case False{}: categoryBelow263(code,U32.is_lt(code,3205))
def categoryBelow265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow265(code,U32.is_lt(code,3218))
def categoryBelow267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow267(code,U32.is_lt(code,3242))
    case False{}: categoryBelow268(code,U32.is_lt(code,3253))
def categoryBelow270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow266(code,U32.is_lt(code,3217))
    case False{}: categoryBelow269(code,U32.is_lt(code,3252))
def categoryBelow271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow264(code,U32.is_lt(code,3201))
    case False{}: categoryBelow270(code,U32.is_lt(code,3241))
def categoryBelow272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow259(code,U32.is_lt(code,3164))
    case False{}: categoryBelow271(code,U32.is_lt(code,3214))
def categoryBelow273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow273(code,U32.is_lt(code,3262))
def categoryBelow275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow275(code,U32.is_lt(code,3296))
def categoryBelow277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow274(code,U32.is_lt(code,3261))
    case False{}: categoryBelow276(code,U32.is_lt(code,3295))
def categoryBelow278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow278(code,U32.is_lt(code,3312))
def categoryBelow280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow280(code,U32.is_lt(code,3332))
def categoryBelow282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow279(code,U32.is_lt(code,3302))
    case False{}: categoryBelow281(code,U32.is_lt(code,3315))
def categoryBelow283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow277(code,U32.is_lt(code,3292))
    case False{}: categoryBelow282(code,U32.is_lt(code,3313))
def categoryBelow284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow284(code,U32.is_lt(code,3345))
def categoryBelow286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow286(code,U32.is_lt(code,3389))
def categoryBelow288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow285(code,U32.is_lt(code,3342))
    case False{}: categoryBelow287(code,U32.is_lt(code,3387))
def categoryBelow289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow289(code,U32.is_lt(code,3407))
def categoryBelow291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow291(code,U32.is_lt(code,3415))
    case False{}: categoryBelow292(code,U32.is_lt(code,3423))
def categoryBelow294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow290(code,U32.is_lt(code,3406))
    case False{}: categoryBelow293(code,U32.is_lt(code,3416))
def categoryBelow295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow288(code,U32.is_lt(code,3346))
    case False{}: categoryBelow294(code,U32.is_lt(code,3412))
def categoryBelow296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow283(code,U32.is_lt(code,3298))
    case False{}: categoryBelow295(code,U32.is_lt(code,3390))
def categoryBelow297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow272(code,U32.is_lt(code,3192))
    case False{}: categoryBelow296(code,U32.is_lt(code,3341))
def categoryBelow298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow248(code,U32.is_lt(code,2961))
    case False{}: categoryBelow297(code,U32.is_lt(code,3258))
def categoryBelow299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow299(code,U32.is_lt(code,3449))
def categoryBelow301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow301(code,U32.is_lt(code,3461))
def categoryBelow303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow300(code,U32.is_lt(code,3430))
    case False{}: categoryBelow302(code,U32.is_lt(code,3456))
def categoryBelow304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow304(code,U32.is_lt(code,3506))
def categoryBelow306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow306(code,U32.is_lt(code,3517))
def categoryBelow308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow305(code,U32.is_lt(code,3482))
    case False{}: categoryBelow307(code,U32.is_lt(code,3516))
def categoryBelow309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow303(code,U32.is_lt(code,3450))
    case False{}: categoryBelow308(code,U32.is_lt(code,3507))
def categoryBelow310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow310(code,U32.is_lt(code,3527))
def categoryBelow312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow312(code,U32.is_lt(code,3585))
def categoryBelow314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow311(code,U32.is_lt(code,3520))
    case False{}: categoryBelow313(code,U32.is_lt(code,3568))
def categoryBelow315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow315(code,U32.is_lt(code,3636))
def categoryBelow317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow317(code,U32.is_lt(code,3655))
    case False{}: categoryBelow318(code,U32.is_lt(code,3674))
def categoryBelow320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow316(code,U32.is_lt(code,3634))
    case False{}: categoryBelow319(code,U32.is_lt(code,3664))
def categoryBelow321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow314(code,U32.is_lt(code,3558))
    case False{}: categoryBelow320(code,U32.is_lt(code,3648))
def categoryBelow322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow309(code,U32.is_lt(code,3479))
    case False{}: categoryBelow321(code,U32.is_lt(code,3633))
def categoryBelow323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow323(code,U32.is_lt(code,3716))
def categoryBelow325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow325(code,U32.is_lt(code,3723))
def categoryBelow327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow324(code,U32.is_lt(code,3715))
    case False{}: categoryBelow326(code,U32.is_lt(code,3718))
def categoryBelow328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow328(code,U32.is_lt(code,3749))
def categoryBelow330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow330(code,U32.is_lt(code,3761))
def categoryBelow332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow329(code,U32.is_lt(code,3748))
    case False{}: categoryBelow331(code,U32.is_lt(code,3751))
def categoryBelow333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow327(code,U32.is_lt(code,3717))
    case False{}: categoryBelow332(code,U32.is_lt(code,3750))
def categoryBelow334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow334(code,U32.is_lt(code,3773))
def categoryBelow336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow336(code,U32.is_lt(code,3781))
def categoryBelow338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow335(code,U32.is_lt(code,3764))
    case False{}: categoryBelow337(code,U32.is_lt(code,3776))
def categoryBelow339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow339(code,U32.is_lt(code,3792))
def categoryBelow341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow341(code,U32.is_lt(code,3804))
    case False{}: categoryBelow342(code,U32.is_lt(code,3840))
def categoryBelow344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow340(code,U32.is_lt(code,3783))
    case False{}: categoryBelow343(code,U32.is_lt(code,3808))
def categoryBelow345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow338(code,U32.is_lt(code,3774))
    case False{}: categoryBelow344(code,U32.is_lt(code,3802))
def categoryBelow346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow333(code,U32.is_lt(code,3724))
    case False{}: categoryBelow345(code,U32.is_lt(code,3782))
def categoryBelow347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow322(code,U32.is_lt(code,3518))
    case False{}: categoryBelow346(code,U32.is_lt(code,3762))
def categoryBelow348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow348(code,U32.is_lt(code,3892))
def categoryBelow350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow350(code,U32.is_lt(code,3913))
def categoryBelow352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow349(code,U32.is_lt(code,3872))
    case False{}: categoryBelow351(code,U32.is_lt(code,3912))
def categoryBelow353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow353(code,U32.is_lt(code,3981))
def categoryBelow355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow355(code,U32.is_lt(code,4159))
def categoryBelow357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow354(code,U32.is_lt(code,3976))
    case False{}: categoryBelow356(code,U32.is_lt(code,4139))
def categoryBelow358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow352(code,U32.is_lt(code,3904))
    case False{}: categoryBelow357(code,U32.is_lt(code,4096))
def categoryBelow359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow359(code,U32.is_lt(code,4176))
def categoryBelow361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow361(code,U32.is_lt(code,4190))
def categoryBelow363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow360(code,U32.is_lt(code,4170))
    case False{}: categoryBelow362(code,U32.is_lt(code,4186))
def categoryBelow364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow364(code,U32.is_lt(code,4197))
def categoryBelow366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow366(code,U32.is_lt(code,4206))
    case False{}: categoryBelow367(code,U32.is_lt(code,4213))
def categoryBelow369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow365(code,U32.is_lt(code,4194))
    case False{}: categoryBelow368(code,U32.is_lt(code,4209))
def categoryBelow370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow363(code,U32.is_lt(code,4182))
    case False{}: categoryBelow369(code,U32.is_lt(code,4199))
def categoryBelow371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow358(code,U32.is_lt(code,3949))
    case False{}: categoryBelow370(code,U32.is_lt(code,4193))
def categoryBelow372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow372(code,U32.is_lt(code,4239))
def categoryBelow374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow374(code,U32.is_lt(code,4256))
def categoryBelow376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow373(code,U32.is_lt(code,4238))
    case False{}: categoryBelow375(code,U32.is_lt(code,4250))
def categoryBelow377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow377(code,U32.is_lt(code,4296))
def categoryBelow379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow379(code,U32.is_lt(code,4302))
    case False{}: categoryBelow380(code,U32.is_lt(code,4347))
def categoryBelow382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow378(code,U32.is_lt(code,4295))
    case False{}: categoryBelow381(code,U32.is_lt(code,4304))
def categoryBelow383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow376(code,U32.is_lt(code,4240))
    case False{}: categoryBelow382(code,U32.is_lt(code,4301))
def categoryBelow384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow384(code,U32.is_lt(code,4682))
def categoryBelow386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow386(code,U32.is_lt(code,4695))
def categoryBelow388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow385(code,U32.is_lt(code,4681))
    case False{}: categoryBelow387(code,U32.is_lt(code,4688))
def categoryBelow389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow389(code,U32.is_lt(code,4698))
def categoryBelow391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow391(code,U32.is_lt(code,4704))
    case False{}: categoryBelow392(code,U32.is_lt(code,4746))
def categoryBelow394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow390(code,U32.is_lt(code,4697))
    case False{}: categoryBelow393(code,U32.is_lt(code,4745))
def categoryBelow395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow388(code,U32.is_lt(code,4686))
    case False{}: categoryBelow394(code,U32.is_lt(code,4702))
def categoryBelow396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow383(code,U32.is_lt(code,4294))
    case False{}: categoryBelow395(code,U32.is_lt(code,4696))
def categoryBelow397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow371(code,U32.is_lt(code,4160))
    case False{}: categoryBelow396(code,U32.is_lt(code,4348))
def categoryBelow398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow347(code,U32.is_lt(code,3713))
    case False{}: categoryBelow397(code,U32.is_lt(code,4226))
def categoryBelow399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow298(code,U32.is_lt(code,3114))
    case False{}: categoryBelow398(code,U32.is_lt(code,3841))
def categoryBelow400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow199(code,U32.is_lt(code,2075))
    case False{}: categoryBelow399(code,U32.is_lt(code,3426))
def categoryBelow401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow401(code,U32.is_lt(code,4785))
def categoryBelow403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow403(code,U32.is_lt(code,4792))
def categoryBelow405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow402(code,U32.is_lt(code,4752))
    case False{}: categoryBelow404(code,U32.is_lt(code,4790))
def categoryBelow406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow406(code,U32.is_lt(code,4801))
def categoryBelow408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow408(code,U32.is_lt(code,4808))
def categoryBelow410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow407(code,U32.is_lt(code,4800))
    case False{}: categoryBelow409(code,U32.is_lt(code,4806))
def categoryBelow411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow405(code,U32.is_lt(code,4786))
    case False{}: categoryBelow410(code,U32.is_lt(code,4802))
def categoryBelow412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow412(code,U32.is_lt(code,4881))
def categoryBelow414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow414(code,U32.is_lt(code,4888))
def categoryBelow416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow413(code,U32.is_lt(code,4824))
    case False{}: categoryBelow415(code,U32.is_lt(code,4886))
def categoryBelow417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow417(code,U32.is_lt(code,4989))
def categoryBelow419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow419(code,U32.is_lt(code,5008))
    case False{}: categoryBelow420(code,U32.is_lt(code,5110))
def categoryBelow422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow418(code,U32.is_lt(code,4969))
    case False{}: categoryBelow421(code,U32.is_lt(code,5024))
def categoryBelow423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow416(code,U32.is_lt(code,4882))
    case False{}: categoryBelow422(code,U32.is_lt(code,4992))
def categoryBelow424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow411(code,U32.is_lt(code,4799))
    case False{}: categoryBelow423(code,U32.is_lt(code,4955))
def categoryBelow425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow425(code,U32.is_lt(code,5121))
def categoryBelow427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow427(code,U32.is_lt(code,5760))
def categoryBelow429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow426(code,U32.is_lt(code,5118))
    case False{}: categoryBelow428(code,U32.is_lt(code,5743))
def categoryBelow430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow430(code,U32.is_lt(code,5792))
def categoryBelow432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow432(code,U32.is_lt(code,5873))
def categoryBelow434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow431(code,U32.is_lt(code,5787))
    case False{}: categoryBelow433(code,U32.is_lt(code,5870))
def categoryBelow435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow429(code,U32.is_lt(code,5741))
    case False{}: categoryBelow434(code,U32.is_lt(code,5867))
def categoryBelow436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow436(code,U32.is_lt(code,5906))
def categoryBelow438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow438(code,U32.is_lt(code,5952))
def categoryBelow440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow437(code,U32.is_lt(code,5888))
    case False{}: categoryBelow439(code,U32.is_lt(code,5938))
def categoryBelow441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow441(code,U32.is_lt(code,5997))
def categoryBelow443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow443(code,U32.is_lt(code,6001))
    case False{}: categoryBelow444(code,U32.is_lt(code,6068))
def categoryBelow446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow442(code,U32.is_lt(code,5984))
    case False{}: categoryBelow445(code,U32.is_lt(code,6016))
def categoryBelow447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow440(code,U32.is_lt(code,5919))
    case False{}: categoryBelow446(code,U32.is_lt(code,5998))
def categoryBelow448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow435(code,U32.is_lt(code,5761))
    case False{}: categoryBelow447(code,U32.is_lt(code,5970))
def categoryBelow449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow424(code,U32.is_lt(code,4823))
    case False{}: categoryBelow448(code,U32.is_lt(code,5881))
def categoryBelow450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow450(code,U32.is_lt(code,6108))
def categoryBelow452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow452(code,U32.is_lt(code,6122))
def categoryBelow454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow451(code,U32.is_lt(code,6104))
    case False{}: categoryBelow453(code,U32.is_lt(code,6112))
def categoryBelow455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow455(code,U32.is_lt(code,6160))
def categoryBelow457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow457(code,U32.is_lt(code,6265))
def categoryBelow459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow456(code,U32.is_lt(code,6138))
    case False{}: categoryBelow458(code,U32.is_lt(code,6176))
def categoryBelow460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow454(code,U32.is_lt(code,6109))
    case False{}: categoryBelow459(code,U32.is_lt(code,6170))
def categoryBelow461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow461(code,U32.is_lt(code,6279))
def categoryBelow463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow463(code,U32.is_lt(code,6315))
def categoryBelow465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow462(code,U32.is_lt(code,6277))
    case False{}: categoryBelow464(code,U32.is_lt(code,6314))
def categoryBelow466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow466(code,U32.is_lt(code,6400))
def categoryBelow468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow468(code,U32.is_lt(code,6470))
    case False{}: categoryBelow469(code,U32.is_lt(code,6510))
def categoryBelow471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow467(code,U32.is_lt(code,6390))
    case False{}: categoryBelow470(code,U32.is_lt(code,6480))
def categoryBelow472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow465(code,U32.is_lt(code,6313))
    case False{}: categoryBelow471(code,U32.is_lt(code,6431))
def categoryBelow473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow460(code,U32.is_lt(code,6128))
    case False{}: categoryBelow472(code,U32.is_lt(code,6320))
def categoryBelow474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow474(code,U32.is_lt(code,6528))
def categoryBelow476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow476(code,U32.is_lt(code,6602))
def categoryBelow478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow475(code,U32.is_lt(code,6517))
    case False{}: categoryBelow477(code,U32.is_lt(code,6576))
def categoryBelow479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow479(code,U32.is_lt(code,6656))
def categoryBelow481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow481(code,U32.is_lt(code,6741))
def categoryBelow483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow480(code,U32.is_lt(code,6619))
    case False{}: categoryBelow482(code,U32.is_lt(code,6688))
def categoryBelow484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow478(code,U32.is_lt(code,6572))
    case False{}: categoryBelow483(code,U32.is_lt(code,6679))
def categoryBelow485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow485(code,U32.is_lt(code,6800))
def categoryBelow487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow487(code,U32.is_lt(code,6824))
def categoryBelow489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow486(code,U32.is_lt(code,6794))
    case False{}: categoryBelow488(code,U32.is_lt(code,6823))
def categoryBelow490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow490(code,U32.is_lt(code,6981))
def categoryBelow492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow492(code,U32.is_lt(code,6992))
    case False{}: categoryBelow493(code,U32.is_lt(code,7043))
def categoryBelow495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow491(code,U32.is_lt(code,6964))
    case False{}: categoryBelow494(code,U32.is_lt(code,7002))
def categoryBelow496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow489(code,U32.is_lt(code,6810))
    case False{}: categoryBelow495(code,U32.is_lt(code,6989))
def categoryBelow497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow484(code,U32.is_lt(code,6608))
    case False{}: categoryBelow496(code,U32.is_lt(code,6917))
def categoryBelow498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow473(code,U32.is_lt(code,6272))
    case False{}: categoryBelow497(code,U32.is_lt(code,6784))
def categoryBelow499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow449(code,U32.is_lt(code,5112))
    case False{}: categoryBelow498(code,U32.is_lt(code,6512))
def categoryBelow500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow500(code,U32.is_lt(code,7088))
def categoryBelow502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow502(code,U32.is_lt(code,7168))
def categoryBelow504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow501(code,U32.is_lt(code,7086))
    case False{}: categoryBelow503(code,U32.is_lt(code,7142))
def categoryBelow505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow505(code,U32.is_lt(code,7242))
def categoryBelow507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow507(code,U32.is_lt(code,7258))
def categoryBelow509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow506(code,U32.is_lt(code,7232))
    case False{}: categoryBelow508(code,U32.is_lt(code,7248))
def categoryBelow510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow504(code,U32.is_lt(code,7098))
    case False{}: categoryBelow509(code,U32.is_lt(code,7245))
def categoryBelow511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow511(code,U32.is_lt(code,7307))
def categoryBelow513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow513(code,U32.is_lt(code,7357))
def categoryBelow515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow512(code,U32.is_lt(code,7296))
    case False{}: categoryBelow514(code,U32.is_lt(code,7355))
def categoryBelow516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow516(code,U32.is_lt(code,7405))
def categoryBelow518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow518(code,U32.is_lt(code,7412))
    case False{}: categoryBelow519(code,U32.is_lt(code,7415))
def categoryBelow521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow517(code,U32.is_lt(code,7401))
    case False{}: categoryBelow520(code,U32.is_lt(code,7413))
def categoryBelow522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow515(code,U32.is_lt(code,7312))
    case False{}: categoryBelow521(code,U32.is_lt(code,7406))
def categoryBelow523(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow510(code,U32.is_lt(code,7204))
    case False{}: categoryBelow522(code,U32.is_lt(code,7360))
def categoryBelow524(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow525(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow524(code,U32.is_lt(code,7424))
def categoryBelow526(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow527(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow526(code,U32.is_lt(code,7958))
def categoryBelow528(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow525(code,U32.is_lt(code,7419))
    case False{}: categoryBelow527(code,U32.is_lt(code,7680))
def categoryBelow529(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow530(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow529(code,U32.is_lt(code,7968))
def categoryBelow531(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow532(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow531(code,U32.is_lt(code,8014))
def categoryBelow533(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow530(code,U32.is_lt(code,7966))
    case False{}: categoryBelow532(code,U32.is_lt(code,8008))
def categoryBelow534(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow528(code,U32.is_lt(code,7616))
    case False{}: categoryBelow533(code,U32.is_lt(code,8006))
def categoryBelow535(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow536(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow535(code,U32.is_lt(code,8025))
def categoryBelow537(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow538(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow537(code,U32.is_lt(code,8028))
def categoryBelow539(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow536(code,U32.is_lt(code,8024))
    case False{}: categoryBelow538(code,U32.is_lt(code,8027))
def categoryBelow540(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow541(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow540(code,U32.is_lt(code,8031))
def categoryBelow542(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow543(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow544(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow542(code,U32.is_lt(code,8064))
    case False{}: categoryBelow543(code,U32.is_lt(code,8118))
def categoryBelow545(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow541(code,U32.is_lt(code,8030))
    case False{}: categoryBelow544(code,U32.is_lt(code,8117))
def categoryBelow546(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow539(code,U32.is_lt(code,8026))
    case False{}: categoryBelow545(code,U32.is_lt(code,8062))
def categoryBelow547(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow534(code,U32.is_lt(code,7960))
    case False{}: categoryBelow546(code,U32.is_lt(code,8029))
def categoryBelow548(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow523(code,U32.is_lt(code,7294))
    case False{}: categoryBelow547(code,U32.is_lt(code,8016))
def categoryBelow549(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow550(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow549(code,U32.is_lt(code,8127))
def categoryBelow551(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow552(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow551(code,U32.is_lt(code,8134))
def categoryBelow553(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow550(code,U32.is_lt(code,8126))
    case False{}: categoryBelow552(code,U32.is_lt(code,8133))
def categoryBelow554(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow555(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow554(code,U32.is_lt(code,8148))
def categoryBelow556(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow557(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow556(code,U32.is_lt(code,8160))
def categoryBelow558(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow555(code,U32.is_lt(code,8144))
    case False{}: categoryBelow557(code,U32.is_lt(code,8156))
def categoryBelow559(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow553(code,U32.is_lt(code,8130))
    case False{}: categoryBelow558(code,U32.is_lt(code,8150))
def categoryBelow560(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow561(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow560(code,U32.is_lt(code,8181))
def categoryBelow562(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow563(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow562(code,U32.is_lt(code,8304))
def categoryBelow564(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow561(code,U32.is_lt(code,8178))
    case False{}: categoryBelow563(code,U32.is_lt(code,8189))
def categoryBelow565(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow566(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow565(code,U32.is_lt(code,8308))
def categoryBelow567(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow568(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow569(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow567(code,U32.is_lt(code,8319))
    case False{}: categoryBelow568(code,U32.is_lt(code,8330))
def categoryBelow570(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow566(code,U32.is_lt(code,8306))
    case False{}: categoryBelow569(code,U32.is_lt(code,8320))
def categoryBelow571(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow564(code,U32.is_lt(code,8182))
    case False{}: categoryBelow570(code,U32.is_lt(code,8314))
def categoryBelow572(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow559(code,U32.is_lt(code,8141))
    case False{}: categoryBelow571(code,U32.is_lt(code,8305))
def categoryBelow573(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow574(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow573(code,U32.is_lt(code,8450))
def categoryBelow575(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow576(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow575(code,U32.is_lt(code,8456))
def categoryBelow577(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow574(code,U32.is_lt(code,8349))
    case False{}: categoryBelow576(code,U32.is_lt(code,8455))
def categoryBelow578(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow579(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow578(code,U32.is_lt(code,8469))
def categoryBelow580(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow581(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow582(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow580(code,U32.is_lt(code,8473))
    case False{}: categoryBelow581(code,U32.is_lt(code,8484))
def categoryBelow583(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow579(code,U32.is_lt(code,8468))
    case False{}: categoryBelow582(code,U32.is_lt(code,8478))
def categoryBelow584(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow577(code,U32.is_lt(code,8451))
    case False{}: categoryBelow583(code,U32.is_lt(code,8470))
def categoryBelow585(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow586(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow585(code,U32.is_lt(code,8487))
def categoryBelow587(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow588(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow587(code,U32.is_lt(code,8490))
def categoryBelow589(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow586(code,U32.is_lt(code,8486))
    case False{}: categoryBelow588(code,U32.is_lt(code,8489))
def categoryBelow590(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow591(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow590(code,U32.is_lt(code,8506))
def categoryBelow592(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow593(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow594(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow592(code,U32.is_lt(code,8512))
    case False{}: categoryBelow593(code,U32.is_lt(code,8522))
def categoryBelow595(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow591(code,U32.is_lt(code,8495))
    case False{}: categoryBelow594(code,U32.is_lt(code,8517))
def categoryBelow596(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow589(code,U32.is_lt(code,8488))
    case False{}: categoryBelow595(code,U32.is_lt(code,8508))
def categoryBelow597(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow584(code,U32.is_lt(code,8458))
    case False{}: categoryBelow596(code,U32.is_lt(code,8494))
def categoryBelow598(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow572(code,U32.is_lt(code,8173))
    case False{}: categoryBelow597(code,U32.is_lt(code,8485))
def categoryBelow599(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow548(code,U32.is_lt(code,7418))
    case False{}: categoryBelow598(code,U32.is_lt(code,8336))
def categoryBelow600(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow499(code,U32.is_lt(code,6103))
    case False{}: categoryBelow599(code,U32.is_lt(code,8125))
def categoryBelow601(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow602(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow601(code,U32.is_lt(code,8528))
def categoryBelow603(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow604(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow603(code,U32.is_lt(code,8586))
def categoryBelow605(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow602(code,U32.is_lt(code,8527))
    case False{}: categoryBelow604(code,U32.is_lt(code,8581))
def categoryBelow606(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow607(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow606(code,U32.is_lt(code,9450))
def categoryBelow608(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow609(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow608(code,U32.is_lt(code,10132))
def categoryBelow610(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow607(code,U32.is_lt(code,9372))
    case False{}: categoryBelow609(code,U32.is_lt(code,10102))
def categoryBelow611(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow605(code,U32.is_lt(code,8579))
    case False{}: categoryBelow610(code,U32.is_lt(code,9472))
def categoryBelow612(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow613(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow612(code,U32.is_lt(code,11499))
def categoryBelow614(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow615(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow614(code,U32.is_lt(code,11508))
def categoryBelow616(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow613(code,U32.is_lt(code,11493))
    case False{}: categoryBelow615(code,U32.is_lt(code,11506))
def categoryBelow617(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow618(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow617(code,U32.is_lt(code,11520))
def categoryBelow619(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow620(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow621(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow619(code,U32.is_lt(code,11559))
    case False{}: categoryBelow620(code,U32.is_lt(code,11565))
def categoryBelow622(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow618(code,U32.is_lt(code,11518))
    case False{}: categoryBelow621(code,U32.is_lt(code,11560))
def categoryBelow623(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow616(code,U32.is_lt(code,11503))
    case False{}: categoryBelow622(code,U32.is_lt(code,11558))
def categoryBelow624(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow611(code,U32.is_lt(code,9312))
    case False{}: categoryBelow623(code,U32.is_lt(code,11517))
def categoryBelow625(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow626(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow625(code,U32.is_lt(code,11624))
def categoryBelow627(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow628(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow627(code,U32.is_lt(code,11648))
def categoryBelow629(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow626(code,U32.is_lt(code,11568))
    case False{}: categoryBelow628(code,U32.is_lt(code,11632))
def categoryBelow630(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow631(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow630(code,U32.is_lt(code,11687))
def categoryBelow632(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow633(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow632(code,U32.is_lt(code,11696))
def categoryBelow634(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow631(code,U32.is_lt(code,11680))
    case False{}: categoryBelow633(code,U32.is_lt(code,11695))
def categoryBelow635(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow629(code,U32.is_lt(code,11631))
    case False{}: categoryBelow634(code,U32.is_lt(code,11688))
def categoryBelow636(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow637(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow636(code,U32.is_lt(code,11711))
def categoryBelow638(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow639(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow638(code,U32.is_lt(code,11720))
def categoryBelow640(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow637(code,U32.is_lt(code,11704))
    case False{}: categoryBelow639(code,U32.is_lt(code,11719))
def categoryBelow641(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow642(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow641(code,U32.is_lt(code,11735))
def categoryBelow643(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow644(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow645(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow643(code,U32.is_lt(code,11743))
    case False{}: categoryBelow644(code,U32.is_lt(code,11824))
def categoryBelow646(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow642(code,U32.is_lt(code,11728))
    case False{}: categoryBelow645(code,U32.is_lt(code,11823))
def categoryBelow647(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow640(code,U32.is_lt(code,11712))
    case False{}: categoryBelow646(code,U32.is_lt(code,11736))
def categoryBelow648(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow635(code,U32.is_lt(code,11671))
    case False{}: categoryBelow647(code,U32.is_lt(code,11727))
def categoryBelow649(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow624(code,U32.is_lt(code,11264))
    case False{}: categoryBelow648(code,U32.is_lt(code,11703))
def categoryBelow650(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow651(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow650(code,U32.is_lt(code,12296))
def categoryBelow652(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow653(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow652(code,U32.is_lt(code,12337))
def categoryBelow654(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow651(code,U32.is_lt(code,12295))
    case False{}: categoryBelow653(code,U32.is_lt(code,12330))
def categoryBelow655(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow656(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow655(code,U32.is_lt(code,12347))
def categoryBelow657(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow658(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow657(code,U32.is_lt(code,12439))
def categoryBelow659(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow656(code,U32.is_lt(code,12344))
    case False{}: categoryBelow658(code,U32.is_lt(code,12353))
def categoryBelow660(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow654(code,U32.is_lt(code,12321))
    case False{}: categoryBelow659(code,U32.is_lt(code,12349))
def categoryBelow661(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow662(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow661(code,U32.is_lt(code,12449))
def categoryBelow663(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow664(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow663(code,U32.is_lt(code,12544))
def categoryBelow665(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow662(code,U32.is_lt(code,12448))
    case False{}: categoryBelow664(code,U32.is_lt(code,12540))
def categoryBelow666(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow667(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow666(code,U32.is_lt(code,12593))
def categoryBelow668(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow669(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow670(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow668(code,U32.is_lt(code,12690))
    case False{}: categoryBelow669(code,U32.is_lt(code,12704))
def categoryBelow671(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow667(code,U32.is_lt(code,12592))
    case False{}: categoryBelow670(code,U32.is_lt(code,12694))
def categoryBelow672(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow665(code,U32.is_lt(code,12539))
    case False{}: categoryBelow671(code,U32.is_lt(code,12687))
def categoryBelow673(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow660(code,U32.is_lt(code,12342))
    case False{}: categoryBelow672(code,U32.is_lt(code,12549))
def categoryBelow674(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow675(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow674(code,U32.is_lt(code,12800))
def categoryBelow676(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow677(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow676(code,U32.is_lt(code,12872))
def categoryBelow678(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow675(code,U32.is_lt(code,12784))
    case False{}: categoryBelow677(code,U32.is_lt(code,12842))
def categoryBelow679(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow680(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow679(code,U32.is_lt(code,12896))
def categoryBelow681(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow682(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow681(code,U32.is_lt(code,12977))
def categoryBelow683(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow680(code,U32.is_lt(code,12881))
    case False{}: categoryBelow682(code,U32.is_lt(code,12938))
def categoryBelow684(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow678(code,U32.is_lt(code,12832))
    case False{}: categoryBelow683(code,U32.is_lt(code,12928))
def categoryBelow685(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow686(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow685(code,U32.is_lt(code,19904))
def categoryBelow687(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow688(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow687(code,U32.is_lt(code,42192))
def categoryBelow689(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow686(code,U32.is_lt(code,13312))
    case False{}: categoryBelow688(code,U32.is_lt(code,42125))
def categoryBelow690(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow691(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow690(code,U32.is_lt(code,42509))
def categoryBelow692(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow693(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow694(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow692(code,U32.is_lt(code,42528))
    case False{}: categoryBelow693(code,U32.is_lt(code,42540))
def categoryBelow695(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow691(code,U32.is_lt(code,42240))
    case False{}: categoryBelow694(code,U32.is_lt(code,42538))
def categoryBelow696(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow689(code,U32.is_lt(code,19968))
    case False{}: categoryBelow695(code,U32.is_lt(code,42512))
def categoryBelow697(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow684(code,U32.is_lt(code,12880))
    case False{}: categoryBelow696(code,U32.is_lt(code,42238))
def categoryBelow698(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow673(code,U32.is_lt(code,12445))
    case False{}: categoryBelow697(code,U32.is_lt(code,12992))
def categoryBelow699(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow649(code,U32.is_lt(code,11566))
    case False{}: categoryBelow698(code,U32.is_lt(code,12736))
def categoryBelow700(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow701(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow700(code,U32.is_lt(code,42623))
def categoryBelow702(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow703(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow702(code,U32.is_lt(code,42726))
def categoryBelow704(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow701(code,U32.is_lt(code,42607))
    case False{}: categoryBelow703(code,U32.is_lt(code,42656))
def categoryBelow705(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow706(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow705(code,U32.is_lt(code,42784))
def categoryBelow707(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow708(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow707(code,U32.is_lt(code,42891))
def categoryBelow709(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow706(code,U32.is_lt(code,42775))
    case False{}: categoryBelow708(code,U32.is_lt(code,42889))
def categoryBelow710(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow704(code,U32.is_lt(code,42654))
    case False{}: categoryBelow709(code,U32.is_lt(code,42786))
def categoryBelow711(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow712(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow711(code,U32.is_lt(code,43010))
def categoryBelow713(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow714(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow713(code,U32.is_lt(code,43015))
def categoryBelow715(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow712(code,U32.is_lt(code,42993))
    case False{}: categoryBelow714(code,U32.is_lt(code,43014))
def categoryBelow716(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow717(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow716(code,U32.is_lt(code,43043))
def categoryBelow718(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow719(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow720(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow718(code,U32.is_lt(code,43062))
    case False{}: categoryBelow719(code,U32.is_lt(code,43124))
def categoryBelow721(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow717(code,U32.is_lt(code,43020))
    case False{}: categoryBelow720(code,U32.is_lt(code,43072))
def categoryBelow722(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow715(code,U32.is_lt(code,43011))
    case False{}: categoryBelow721(code,U32.is_lt(code,43056))
def categoryBelow723(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow710(code,U32.is_lt(code,42736))
    case False{}: categoryBelow722(code,U32.is_lt(code,43019))
def categoryBelow724(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow725(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow724(code,U32.is_lt(code,43216))
def categoryBelow726(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow727(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow726(code,U32.is_lt(code,43256))
def categoryBelow728(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow725(code,U32.is_lt(code,43188))
    case False{}: categoryBelow727(code,U32.is_lt(code,43250))
def categoryBelow729(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow730(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow729(code,U32.is_lt(code,43261))
def categoryBelow731(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow732(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow731(code,U32.is_lt(code,43274))
def categoryBelow733(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow730(code,U32.is_lt(code,43260))
    case False{}: categoryBelow732(code,U32.is_lt(code,43264))
def categoryBelow734(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow728(code,U32.is_lt(code,43226))
    case False{}: categoryBelow733(code,U32.is_lt(code,43263))
def categoryBelow735(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow736(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow735(code,U32.is_lt(code,43335))
def categoryBelow737(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow738(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow737(code,U32.is_lt(code,43396))
def categoryBelow739(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow736(code,U32.is_lt(code,43312))
    case False{}: categoryBelow738(code,U32.is_lt(code,43389))
def categoryBelow740(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow741(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow740(code,U32.is_lt(code,43472))
def categoryBelow742(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow743(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow744(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow742(code,U32.is_lt(code,43488))
    case False{}: categoryBelow743(code,U32.is_lt(code,43494))
def categoryBelow745(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow741(code,U32.is_lt(code,43471))
    case False{}: categoryBelow744(code,U32.is_lt(code,43493))
def categoryBelow746(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow739(code,U32.is_lt(code,43360))
    case False{}: categoryBelow745(code,U32.is_lt(code,43482))
def categoryBelow747(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow734(code,U32.is_lt(code,43259))
    case False{}: categoryBelow746(code,U32.is_lt(code,43443))
def categoryBelow748(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow723(code,U32.is_lt(code,42973))
    case False{}: categoryBelow747(code,U32.is_lt(code,43302))
def categoryBelow749(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow750(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow749(code,U32.is_lt(code,43519))
def categoryBelow751(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow752(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow751(code,U32.is_lt(code,43584))
def categoryBelow753(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow750(code,U32.is_lt(code,43514))
    case False{}: categoryBelow752(code,U32.is_lt(code,43561))
def categoryBelow754(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow755(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow754(code,U32.is_lt(code,43596))
def categoryBelow756(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow757(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow756(code,U32.is_lt(code,43616))
def categoryBelow758(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow755(code,U32.is_lt(code,43588))
    case False{}: categoryBelow757(code,U32.is_lt(code,43610))
def categoryBelow759(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow753(code,U32.is_lt(code,43520))
    case False{}: categoryBelow758(code,U32.is_lt(code,43600))
def categoryBelow760(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow761(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow760(code,U32.is_lt(code,43643))
def categoryBelow762(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow763(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow762(code,U32.is_lt(code,43697))
def categoryBelow764(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow761(code,U32.is_lt(code,43642))
    case False{}: categoryBelow763(code,U32.is_lt(code,43696))
def categoryBelow765(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow766(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow765(code,U32.is_lt(code,43703))
def categoryBelow767(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow768(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow769(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow767(code,U32.is_lt(code,43710))
    case False{}: categoryBelow768(code,U32.is_lt(code,43713))
def categoryBelow770(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow766(code,U32.is_lt(code,43701))
    case False{}: categoryBelow769(code,U32.is_lt(code,43712))
def categoryBelow771(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow764(code,U32.is_lt(code,43646))
    case False{}: categoryBelow770(code,U32.is_lt(code,43705))
def categoryBelow772(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow759(code,U32.is_lt(code,43587))
    case False{}: categoryBelow771(code,U32.is_lt(code,43698))
def categoryBelow773(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow774(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow773(code,U32.is_lt(code,43739))
def categoryBelow775(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow776(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow775(code,U32.is_lt(code,43755))
def categoryBelow777(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow774(code,U32.is_lt(code,43715))
    case False{}: categoryBelow776(code,U32.is_lt(code,43744))
def categoryBelow778(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow779(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow778(code,U32.is_lt(code,43777))
def categoryBelow780(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow781(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow782(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow780(code,U32.is_lt(code,43785))
    case False{}: categoryBelow781(code,U32.is_lt(code,43793))
def categoryBelow783(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow779(code,U32.is_lt(code,43765))
    case False{}: categoryBelow782(code,U32.is_lt(code,43791))
def categoryBelow784(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow777(code,U32.is_lt(code,43742))
    case False{}: categoryBelow783(code,U32.is_lt(code,43783))
def categoryBelow785(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow786(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow785(code,U32.is_lt(code,43815))
def categoryBelow787(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow788(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow787(code,U32.is_lt(code,43824))
def categoryBelow789(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow786(code,U32.is_lt(code,43808))
    case False{}: categoryBelow788(code,U32.is_lt(code,43823))
def categoryBelow790(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow791(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow790(code,U32.is_lt(code,43882))
def categoryBelow792(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow793(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow794(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow792(code,U32.is_lt(code,44003))
    case False{}: categoryBelow793(code,U32.is_lt(code,44026))
def categoryBelow795(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow791(code,U32.is_lt(code,43868))
    case False{}: categoryBelow794(code,U32.is_lt(code,44016))
def categoryBelow796(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow789(code,U32.is_lt(code,43816))
    case False{}: categoryBelow795(code,U32.is_lt(code,43888))
def categoryBelow797(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow784(code,U32.is_lt(code,43762))
    case False{}: categoryBelow796(code,U32.is_lt(code,43867))
def categoryBelow798(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow772(code,U32.is_lt(code,43639))
    case False{}: categoryBelow797(code,U32.is_lt(code,43799))
def categoryBelow799(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow748(code,U32.is_lt(code,43138))
    case False{}: categoryBelow798(code,U32.is_lt(code,43714))
def categoryBelow800(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow699(code,U32.is_lt(code,12293))
    case False{}: categoryBelow799(code,U32.is_lt(code,43504))
def categoryBelow801(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow600(code,U32.is_lt(code,7073))
    case False{}: categoryBelow800(code,U32.is_lt(code,42560))
def categoryBelow802(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow400(code,U32.is_lt(code,2833))
    case False{}: categoryBelow801(code,U32.is_lt(code,8526))
def categoryBelow803(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow804(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow803(code,U32.is_lt(code,55216))
def categoryBelow805(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow806(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow805(code,U32.is_lt(code,55292))
def categoryBelow807(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow804(code,U32.is_lt(code,55204))
    case False{}: categoryBelow806(code,U32.is_lt(code,55243))
def categoryBelow808(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow809(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow808(code,U32.is_lt(code,64112))
def categoryBelow810(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow811(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow810(code,U32.is_lt(code,64263))
def categoryBelow812(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow809(code,U32.is_lt(code,64110))
    case False{}: categoryBelow811(code,U32.is_lt(code,64256))
def categoryBelow813(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow807(code,U32.is_lt(code,55239))
    case False{}: categoryBelow812(code,U32.is_lt(code,64218))
def categoryBelow814(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow815(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow814(code,U32.is_lt(code,64285))
def categoryBelow816(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow817(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow816(code,U32.is_lt(code,64297))
def categoryBelow818(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow815(code,U32.is_lt(code,64280))
    case False{}: categoryBelow817(code,U32.is_lt(code,64287))
def categoryBelow819(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow820(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow819(code,U32.is_lt(code,64312))
def categoryBelow821(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow822(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow823(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow821(code,U32.is_lt(code,64318))
    case False{}: categoryBelow822(code,U32.is_lt(code,64320))
def categoryBelow824(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow820(code,U32.is_lt(code,64311))
    case False{}: categoryBelow823(code,U32.is_lt(code,64319))
def categoryBelow825(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow818(code,U32.is_lt(code,64286))
    case False{}: categoryBelow824(code,U32.is_lt(code,64317))
def categoryBelow826(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow813(code,U32.is_lt(code,63744))
    case False{}: categoryBelow825(code,U32.is_lt(code,64298))
def categoryBelow827(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow828(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow827(code,U32.is_lt(code,64325))
def categoryBelow829(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow830(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow829(code,U32.is_lt(code,64467))
def categoryBelow831(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow828(code,U32.is_lt(code,64323))
    case False{}: categoryBelow830(code,U32.is_lt(code,64434))
def categoryBelow832(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow833(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow832(code,U32.is_lt(code,64912))
def categoryBelow834(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow835(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow834(code,U32.is_lt(code,65008))
def categoryBelow836(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow833(code,U32.is_lt(code,64848))
    case False{}: categoryBelow835(code,U32.is_lt(code,64968))
def categoryBelow837(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow831(code,U32.is_lt(code,64326))
    case False{}: categoryBelow836(code,U32.is_lt(code,64914))
def categoryBelow838(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow839(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow838(code,U32.is_lt(code,65141))
def categoryBelow840(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow841(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow840(code,U32.is_lt(code,65296))
def categoryBelow842(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow839(code,U32.is_lt(code,65136))
    case False{}: categoryBelow841(code,U32.is_lt(code,65277))
def categoryBelow843(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow844(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow843(code,U32.is_lt(code,65339))
def categoryBelow845(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow846(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow847(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow845(code,U32.is_lt(code,65371))
    case False{}: categoryBelow846(code,U32.is_lt(code,65471))
def categoryBelow848(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow844(code,U32.is_lt(code,65313))
    case False{}: categoryBelow847(code,U32.is_lt(code,65382))
def categoryBelow849(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow842(code,U32.is_lt(code,65142))
    case False{}: categoryBelow848(code,U32.is_lt(code,65345))
def categoryBelow850(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow837(code,U32.is_lt(code,64830))
    case False{}: categoryBelow849(code,U32.is_lt(code,65306))
def categoryBelow851(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow826(code,U32.is_lt(code,64275))
    case False{}: categoryBelow850(code,U32.is_lt(code,65020))
def categoryBelow852(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow853(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow852(code,U32.is_lt(code,65482))
def categoryBelow854(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow855(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow854(code,U32.is_lt(code,65496))
def categoryBelow856(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow853(code,U32.is_lt(code,65480))
    case False{}: categoryBelow855(code,U32.is_lt(code,65490))
def categoryBelow857(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow858(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow857(code,U32.is_lt(code,65536))
def categoryBelow859(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow860(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow859(code,U32.is_lt(code,65575))
def categoryBelow861(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow858(code,U32.is_lt(code,65501))
    case False{}: categoryBelow860(code,U32.is_lt(code,65549))
def categoryBelow862(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow856(code,U32.is_lt(code,65488))
    case False{}: categoryBelow861(code,U32.is_lt(code,65548))
def categoryBelow863(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow864(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow863(code,U32.is_lt(code,65596))
def categoryBelow865(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow866(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow865(code,U32.is_lt(code,65614))
def categoryBelow867(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow864(code,U32.is_lt(code,65595))
    case False{}: categoryBelow866(code,U32.is_lt(code,65599))
def categoryBelow868(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow869(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow868(code,U32.is_lt(code,65664))
def categoryBelow870(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow871(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow872(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow870(code,U32.is_lt(code,65799))
    case False{}: categoryBelow871(code,U32.is_lt(code,65856))
def categoryBelow873(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow869(code,U32.is_lt(code,65630))
    case False{}: categoryBelow872(code,U32.is_lt(code,65844))
def categoryBelow874(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow867(code,U32.is_lt(code,65598))
    case False{}: categoryBelow873(code,U32.is_lt(code,65787))
def categoryBelow875(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow862(code,U32.is_lt(code,65498))
    case False{}: categoryBelow874(code,U32.is_lt(code,65616))
def categoryBelow876(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow877(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow876(code,U32.is_lt(code,65932))
def categoryBelow878(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow879(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow878(code,U32.is_lt(code,66208))
def categoryBelow880(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow877(code,U32.is_lt(code,65930))
    case False{}: categoryBelow879(code,U32.is_lt(code,66205))
def categoryBelow881(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow882(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow881(code,U32.is_lt(code,66300))
def categoryBelow883(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow884(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow883(code,U32.is_lt(code,66340))
def categoryBelow885(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow882(code,U32.is_lt(code,66273))
    case False{}: categoryBelow884(code,U32.is_lt(code,66336))
def categoryBelow886(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow880(code,U32.is_lt(code,66176))
    case False{}: categoryBelow885(code,U32.is_lt(code,66304))
def categoryBelow887(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow888(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow887(code,U32.is_lt(code,66370))
def categoryBelow889(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow890(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow889(code,U32.is_lt(code,66384))
def categoryBelow891(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow888(code,U32.is_lt(code,66369))
    case False{}: categoryBelow890(code,U32.is_lt(code,66379))
def categoryBelow892(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow893(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow892(code,U32.is_lt(code,66462))
def categoryBelow894(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow895(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow896(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow894(code,U32.is_lt(code,66500))
    case False{}: categoryBelow895(code,U32.is_lt(code,66512))
def categoryBelow897(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow893(code,U32.is_lt(code,66432))
    case False{}: categoryBelow896(code,U32.is_lt(code,66504))
def categoryBelow898(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow891(code,U32.is_lt(code,66378))
    case False{}: categoryBelow897(code,U32.is_lt(code,66464))
def categoryBelow899(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow886(code,U32.is_lt(code,66257))
    case False{}: categoryBelow898(code,U32.is_lt(code,66422))
def categoryBelow900(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow875(code,U32.is_lt(code,65576))
    case False{}: categoryBelow899(code,U32.is_lt(code,66349))
def categoryBelow901(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow851(code,U32.is_lt(code,64322))
    case False{}: categoryBelow900(code,U32.is_lt(code,65913))
def categoryBelow902(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow903(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow902(code,U32.is_lt(code,66560))
def categoryBelow904(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow905(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow904(code,U32.is_lt(code,66730))
def categoryBelow906(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow903(code,U32.is_lt(code,66518))
    case False{}: categoryBelow905(code,U32.is_lt(code,66720))
def categoryBelow907(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow908(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow907(code,U32.is_lt(code,66776))
def categoryBelow909(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow910(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow909(code,U32.is_lt(code,66856))
def categoryBelow911(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow908(code,U32.is_lt(code,66772))
    case False{}: categoryBelow910(code,U32.is_lt(code,66816))
def categoryBelow912(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow906(code,U32.is_lt(code,66718))
    case False{}: categoryBelow911(code,U32.is_lt(code,66812))
def categoryBelow913(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow914(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow913(code,U32.is_lt(code,66928))
def categoryBelow915(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow916(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow915(code,U32.is_lt(code,66955))
def categoryBelow917(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow914(code,U32.is_lt(code,66916))
    case False{}: categoryBelow916(code,U32.is_lt(code,66940))
def categoryBelow918(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow919(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow918(code,U32.is_lt(code,66964))
def categoryBelow920(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow921(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow922(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow920(code,U32.is_lt(code,66967))
    case False{}: categoryBelow921(code,U32.is_lt(code,66979))
def categoryBelow923(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow919(code,U32.is_lt(code,66963))
    case False{}: categoryBelow922(code,U32.is_lt(code,66978))
def categoryBelow924(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow917(code,U32.is_lt(code,66939))
    case False{}: categoryBelow923(code,U32.is_lt(code,66966))
def categoryBelow925(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow912(code,U32.is_lt(code,66736))
    case False{}: categoryBelow924(code,U32.is_lt(code,66956))
def categoryBelow926(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow927(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow926(code,U32.is_lt(code,67002))
def categoryBelow928(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow929(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow928(code,U32.is_lt(code,67008))
def categoryBelow930(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow927(code,U32.is_lt(code,66995))
    case False{}: categoryBelow929(code,U32.is_lt(code,67005))
def categoryBelow931(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow932(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow931(code,U32.is_lt(code,67383))
def categoryBelow933(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow934(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow933(code,U32.is_lt(code,67424))
def categoryBelow935(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow932(code,U32.is_lt(code,67072))
    case False{}: categoryBelow934(code,U32.is_lt(code,67414))
def categoryBelow936(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow930(code,U32.is_lt(code,67003))
    case False{}: categoryBelow935(code,U32.is_lt(code,67392))
def categoryBelow937(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow938(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow937(code,U32.is_lt(code,67462))
def categoryBelow939(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow940(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow939(code,U32.is_lt(code,67506))
def categoryBelow941(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow938(code,U32.is_lt(code,67456))
    case False{}: categoryBelow940(code,U32.is_lt(code,67505))
def categoryBelow942(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow943(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow942(code,U32.is_lt(code,67590))
def categoryBelow944(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow945(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow946(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow944(code,U32.is_lt(code,67593))
    case False{}: categoryBelow945(code,U32.is_lt(code,67638))
def categoryBelow947(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow943(code,U32.is_lt(code,67584))
    case False{}: categoryBelow946(code,U32.is_lt(code,67594))
def categoryBelow948(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow941(code,U32.is_lt(code,67463))
    case False{}: categoryBelow947(code,U32.is_lt(code,67592))
def categoryBelow949(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow936(code,U32.is_lt(code,67060))
    case False{}: categoryBelow948(code,U32.is_lt(code,67515))
def categoryBelow950(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow925(code,U32.is_lt(code,66864))
    case False{}: categoryBelow949(code,U32.is_lt(code,67432))
def categoryBelow951(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow952(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow951(code,U32.is_lt(code,67644))
def categoryBelow953(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow954(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow953(code,U32.is_lt(code,67670))
def categoryBelow955(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow952(code,U32.is_lt(code,67641))
    case False{}: categoryBelow954(code,U32.is_lt(code,67647))
def categoryBelow956(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow957(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow956(code,U32.is_lt(code,67703))
def categoryBelow958(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow959(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow958(code,U32.is_lt(code,67743))
def categoryBelow960(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow957(code,U32.is_lt(code,67680))
    case False{}: categoryBelow959(code,U32.is_lt(code,67712))
def categoryBelow961(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow955(code,U32.is_lt(code,67645))
    case False{}: categoryBelow960(code,U32.is_lt(code,67705))
def categoryBelow962(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow963(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow962(code,U32.is_lt(code,67808))
def categoryBelow964(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow965(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow964(code,U32.is_lt(code,67830))
def categoryBelow966(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow963(code,U32.is_lt(code,67760))
    case False{}: categoryBelow965(code,U32.is_lt(code,67828))
def categoryBelow967(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow968(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow967(code,U32.is_lt(code,67862))
def categoryBelow969(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow970(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow971(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow969(code,U32.is_lt(code,67872))
    case False{}: categoryBelow970(code,U32.is_lt(code,67904))
def categoryBelow972(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow968(code,U32.is_lt(code,67840))
    case False{}: categoryBelow971(code,U32.is_lt(code,67898))
def categoryBelow973(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow966(code,U32.is_lt(code,67827))
    case False{}: categoryBelow972(code,U32.is_lt(code,67868))
def categoryBelow974(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow961(code,U32.is_lt(code,67672))
    case False{}: categoryBelow973(code,U32.is_lt(code,67835))
def categoryBelow975(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow976(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow975(code,U32.is_lt(code,68024))
def categoryBelow977(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow978(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow977(code,U32.is_lt(code,68032))
def categoryBelow979(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow976(code,U32.is_lt(code,67968))
    case False{}: categoryBelow978(code,U32.is_lt(code,68030))
def categoryBelow980(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow981(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow980(code,U32.is_lt(code,68096))
def categoryBelow982(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow983(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow984(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow982(code,U32.is_lt(code,68112))
    case False{}: categoryBelow983(code,U32.is_lt(code,68117))
def categoryBelow985(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow981(code,U32.is_lt(code,68050))
    case False{}: categoryBelow984(code,U32.is_lt(code,68116))
def categoryBelow986(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow979(code,U32.is_lt(code,68028))
    case False{}: categoryBelow985(code,U32.is_lt(code,68097))
def categoryBelow987(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow988(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow987(code,U32.is_lt(code,68150))
def categoryBelow989(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow990(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow989(code,U32.is_lt(code,68192))
def categoryBelow991(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow988(code,U32.is_lt(code,68121))
    case False{}: categoryBelow990(code,U32.is_lt(code,68169))
def categoryBelow992(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow993(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow992(code,U32.is_lt(code,68224))
def categoryBelow994(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow995(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow996(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow994(code,U32.is_lt(code,68256))
    case False{}: categoryBelow995(code,U32.is_lt(code,68296))
def categoryBelow997(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow993(code,U32.is_lt(code,68223))
    case False{}: categoryBelow996(code,U32.is_lt(code,68288))
def categoryBelow998(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow991(code,U32.is_lt(code,68160))
    case False{}: categoryBelow997(code,U32.is_lt(code,68253))
def categoryBelow999(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow986(code,U32.is_lt(code,68048))
    case False{}: categoryBelow998(code,U32.is_lt(code,68221))
def categoryBelow1000(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow974(code,U32.is_lt(code,67751))
    case False{}: categoryBelow999(code,U32.is_lt(code,68120))
def categoryBelow1001(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow950(code,U32.is_lt(code,66994))
    case False{}: categoryBelow1000(code,U32.is_lt(code,67930))
def categoryBelow1002(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow901(code,U32.is_lt(code,65474))
    case False{}: categoryBelow1001(code,U32.is_lt(code,67639))
def categoryBelow1003(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1004(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1003(code,U32.is_lt(code,68331))
def categoryBelow1005(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1006(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1005(code,U32.is_lt(code,68406))
def categoryBelow1007(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1004(code,U32.is_lt(code,68325))
    case False{}: categoryBelow1006(code,U32.is_lt(code,68352))
def categoryBelow1008(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1009(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1008(code,U32.is_lt(code,68440))
def categoryBelow1010(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1011(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1010(code,U32.is_lt(code,68472))
def categoryBelow1012(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1009(code,U32.is_lt(code,68438))
    case False{}: categoryBelow1011(code,U32.is_lt(code,68467))
def categoryBelow1013(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1007(code,U32.is_lt(code,68336))
    case False{}: categoryBelow1012(code,U32.is_lt(code,68448))
def categoryBelow1014(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1015(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1014(code,U32.is_lt(code,68521))
def categoryBelow1016(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1017(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1016(code,U32.is_lt(code,68681))
def categoryBelow1018(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1015(code,U32.is_lt(code,68498))
    case False{}: categoryBelow1017(code,U32.is_lt(code,68608))
def categoryBelow1019(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1020(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1019(code,U32.is_lt(code,68800))
def categoryBelow1021(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1022(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1023(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1021(code,U32.is_lt(code,68858))
    case False{}: categoryBelow1022(code,U32.is_lt(code,68900))
def categoryBelow1024(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1020(code,U32.is_lt(code,68787))
    case False{}: categoryBelow1023(code,U32.is_lt(code,68864))
def categoryBelow1025(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1018(code,U32.is_lt(code,68528))
    case False{}: categoryBelow1024(code,U32.is_lt(code,68851))
def categoryBelow1026(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1013(code,U32.is_lt(code,68416))
    case False{}: categoryBelow1025(code,U32.is_lt(code,68736))
def categoryBelow1027(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1028(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1027(code,U32.is_lt(code,68928))
def categoryBelow1029(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1030(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1029(code,U32.is_lt(code,68975))
def categoryBelow1031(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1028(code,U32.is_lt(code,68922))
    case False{}: categoryBelow1030(code,U32.is_lt(code,68966))
def categoryBelow1032(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1033(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1032(code,U32.is_lt(code,69247))
def categoryBelow1034(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1035(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1034(code,U32.is_lt(code,69296))
def categoryBelow1036(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1033(code,U32.is_lt(code,69216))
    case False{}: categoryBelow1035(code,U32.is_lt(code,69290))
def categoryBelow1037(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1031(code,U32.is_lt(code,68938))
    case False{}: categoryBelow1036(code,U32.is_lt(code,69248))
def categoryBelow1038(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1039(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1038(code,U32.is_lt(code,69320))
def categoryBelow1040(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1041(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1040(code,U32.is_lt(code,69415))
def categoryBelow1042(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1039(code,U32.is_lt(code,69314))
    case False{}: categoryBelow1041(code,U32.is_lt(code,69405))
def categoryBelow1043(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1044(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1043(code,U32.is_lt(code,69446))
def categoryBelow1045(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1046(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1047(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1045(code,U32.is_lt(code,69461))
    case False{}: categoryBelow1046(code,U32.is_lt(code,69506))
def categoryBelow1048(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1044(code,U32.is_lt(code,69424))
    case False{}: categoryBelow1047(code,U32.is_lt(code,69488))
def categoryBelow1049(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1042(code,U32.is_lt(code,69376))
    case False{}: categoryBelow1048(code,U32.is_lt(code,69457))
def categoryBelow1050(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1037(code,U32.is_lt(code,68998))
    case False{}: categoryBelow1049(code,U32.is_lt(code,69416))
def categoryBelow1051(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1026(code,U32.is_lt(code,68480))
    case False{}: categoryBelow1050(code,U32.is_lt(code,69298))
def categoryBelow1052(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1053(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1052(code,U32.is_lt(code,69580))
def categoryBelow1054(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1055(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1054(code,U32.is_lt(code,69635))
def categoryBelow1056(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1053(code,U32.is_lt(code,69573))
    case False{}: categoryBelow1055(code,U32.is_lt(code,69623))
def categoryBelow1057(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1058(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1057(code,U32.is_lt(code,69744))
def categoryBelow1059(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1060(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1059(code,U32.is_lt(code,69749))
def categoryBelow1061(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1058(code,U32.is_lt(code,69714))
    case False{}: categoryBelow1060(code,U32.is_lt(code,69747))
def categoryBelow1062(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1056(code,U32.is_lt(code,69600))
    case False{}: categoryBelow1061(code,U32.is_lt(code,69745))
def categoryBelow1063(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1064(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1063(code,U32.is_lt(code,69808))
def categoryBelow1065(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1066(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1065(code,U32.is_lt(code,69872))
def categoryBelow1067(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1064(code,U32.is_lt(code,69763))
    case False{}: categoryBelow1066(code,U32.is_lt(code,69865))
def categoryBelow1068(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1069(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1068(code,U32.is_lt(code,69927))
def categoryBelow1070(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1071(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1072(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1070(code,U32.is_lt(code,69952))
    case False{}: categoryBelow1071(code,U32.is_lt(code,69957))
def categoryBelow1073(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1069(code,U32.is_lt(code,69891))
    case False{}: categoryBelow1072(code,U32.is_lt(code,69956))
def categoryBelow1074(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1067(code,U32.is_lt(code,69840))
    case False{}: categoryBelow1073(code,U32.is_lt(code,69942))
def categoryBelow1075(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1062(code,U32.is_lt(code,69688))
    case False{}: categoryBelow1074(code,U32.is_lt(code,69882))
def categoryBelow1076(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1077(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1076(code,U32.is_lt(code,69968))
def categoryBelow1078(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1079(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1078(code,U32.is_lt(code,70007))
def categoryBelow1080(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1077(code,U32.is_lt(code,69960))
    case False{}: categoryBelow1079(code,U32.is_lt(code,70006))
def categoryBelow1081(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1082(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1081(code,U32.is_lt(code,70081))
def categoryBelow1083(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 1
def categoryBelow1084(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1083(code,U32.is_lt(code,70106))
def categoryBelow1085(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1082(code,U32.is_lt(code,70067))
    case False{}: categoryBelow1084(code,U32.is_lt(code,70096))
def categoryBelow1086(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1080(code,U32.is_lt(code,70003))
    case False{}: categoryBelow1085(code,U32.is_lt(code,70085))
def categoryBelow1087(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1088(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1087(code,U32.is_lt(code,70109))
def categoryBelow1089(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1090(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1089(code,U32.is_lt(code,70144))
def categoryBelow1091(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1088(code,U32.is_lt(code,70108))
    case False{}: categoryBelow1090(code,U32.is_lt(code,70133))
def categoryBelow1092(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1093(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1092(code,U32.is_lt(code,70188))
def categoryBelow1094(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1095(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1096(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1094(code,U32.is_lt(code,70209))
    case False{}: categoryBelow1095(code,U32.is_lt(code,70279))
def categoryBelow1097(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1093(code,U32.is_lt(code,70163))
    case False{}: categoryBelow1096(code,U32.is_lt(code,70272))
def categoryBelow1098(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1091(code,U32.is_lt(code,70113))
    case False{}: categoryBelow1097(code,U32.is_lt(code,70207))
def categoryBelow1099(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1086(code,U32.is_lt(code,70019))
    case False{}: categoryBelow1098(code,U32.is_lt(code,70162))
def categoryBelow1100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1075(code,U32.is_lt(code,69750))
    case False{}: categoryBelow1099(code,U32.is_lt(code,70107))
def categoryBelow1101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1051(code,U32.is_lt(code,68912))
    case False{}: categoryBelow1100(code,U32.is_lt(code,69959))
def categoryBelow1102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1102(code,U32.is_lt(code,70282))
def categoryBelow1104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1104(code,U32.is_lt(code,70302))
def categoryBelow1106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1103(code,U32.is_lt(code,70281))
    case False{}: categoryBelow1105(code,U32.is_lt(code,70287))
def categoryBelow1107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1107(code,U32.is_lt(code,70320))
def categoryBelow1109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1109(code,U32.is_lt(code,70394))
def categoryBelow1111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1108(code,U32.is_lt(code,70313))
    case False{}: categoryBelow1110(code,U32.is_lt(code,70384))
def categoryBelow1112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1106(code,U32.is_lt(code,70286))
    case False{}: categoryBelow1111(code,U32.is_lt(code,70367))
def categoryBelow1113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1113(code,U32.is_lt(code,70415))
def categoryBelow1115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1115(code,U32.is_lt(code,70441))
def categoryBelow1117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1114(code,U32.is_lt(code,70413))
    case False{}: categoryBelow1116(code,U32.is_lt(code,70419))
def categoryBelow1118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1118(code,U32.is_lt(code,70450))
def categoryBelow1120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1120(code,U32.is_lt(code,70453))
    case False{}: categoryBelow1121(code,U32.is_lt(code,70461))
def categoryBelow1123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1119(code,U32.is_lt(code,70449))
    case False{}: categoryBelow1122(code,U32.is_lt(code,70458))
def categoryBelow1124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1117(code,U32.is_lt(code,70417))
    case False{}: categoryBelow1123(code,U32.is_lt(code,70452))
def categoryBelow1125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1112(code,U32.is_lt(code,70303))
    case False{}: categoryBelow1124(code,U32.is_lt(code,70442))
def categoryBelow1126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1126(code,U32.is_lt(code,70481))
def categoryBelow1128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1128(code,U32.is_lt(code,70528))
def categoryBelow1130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1127(code,U32.is_lt(code,70480))
    case False{}: categoryBelow1129(code,U32.is_lt(code,70498))
def categoryBelow1131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1131(code,U32.is_lt(code,70540))
def categoryBelow1133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1133(code,U32.is_lt(code,70544))
def categoryBelow1135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1132(code,U32.is_lt(code,70539))
    case False{}: categoryBelow1134(code,U32.is_lt(code,70543))
def categoryBelow1136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1130(code,U32.is_lt(code,70493))
    case False{}: categoryBelow1135(code,U32.is_lt(code,70542))
def categoryBelow1137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1137(code,U32.is_lt(code,70584))
def categoryBelow1139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1139(code,U32.is_lt(code,70611))
def categoryBelow1141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1138(code,U32.is_lt(code,70583))
    case False{}: categoryBelow1140(code,U32.is_lt(code,70610))
def categoryBelow1142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1142(code,U32.is_lt(code,70709))
def categoryBelow1144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1144(code,U32.is_lt(code,70731))
    case False{}: categoryBelow1145(code,U32.is_lt(code,70746))
def categoryBelow1147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1143(code,U32.is_lt(code,70656))
    case False{}: categoryBelow1146(code,U32.is_lt(code,70736))
def categoryBelow1148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1141(code,U32.is_lt(code,70609))
    case False{}: categoryBelow1147(code,U32.is_lt(code,70727))
def categoryBelow1149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1136(code,U32.is_lt(code,70538))
    case False{}: categoryBelow1148(code,U32.is_lt(code,70612))
def categoryBelow1150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1125(code,U32.is_lt(code,70405))
    case False{}: categoryBelow1149(code,U32.is_lt(code,70582))
def categoryBelow1151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1151(code,U32.is_lt(code,70784))
def categoryBelow1153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1153(code,U32.is_lt(code,70854))
def categoryBelow1155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1152(code,U32.is_lt(code,70754))
    case False{}: categoryBelow1154(code,U32.is_lt(code,70852))
def categoryBelow1156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1156(code,U32.is_lt(code,70864))
def categoryBelow1158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1158(code,U32.is_lt(code,71087))
def categoryBelow1160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1157(code,U32.is_lt(code,70856))
    case False{}: categoryBelow1159(code,U32.is_lt(code,71040))
def categoryBelow1161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1155(code,U32.is_lt(code,70832))
    case False{}: categoryBelow1160(code,U32.is_lt(code,70874))
def categoryBelow1162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1162(code,U32.is_lt(code,71168))
def categoryBelow1164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1164(code,U32.is_lt(code,71237))
def categoryBelow1166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1163(code,U32.is_lt(code,71132))
    case False{}: categoryBelow1165(code,U32.is_lt(code,71236))
def categoryBelow1167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1167(code,U32.is_lt(code,71296))
def categoryBelow1169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1169(code,U32.is_lt(code,71352))
    case False{}: categoryBelow1170(code,U32.is_lt(code,71360))
def categoryBelow1172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1168(code,U32.is_lt(code,71258))
    case False{}: categoryBelow1171(code,U32.is_lt(code,71353))
def categoryBelow1173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1166(code,U32.is_lt(code,71216))
    case False{}: categoryBelow1172(code,U32.is_lt(code,71339))
def categoryBelow1174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1161(code,U32.is_lt(code,70855))
    case False{}: categoryBelow1173(code,U32.is_lt(code,71248))
def categoryBelow1175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1175(code,U32.is_lt(code,71396))
def categoryBelow1177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1177(code,U32.is_lt(code,71472))
def categoryBelow1179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1176(code,U32.is_lt(code,71376))
    case False{}: categoryBelow1178(code,U32.is_lt(code,71451))
def categoryBelow1180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1180(code,U32.is_lt(code,71495))
def categoryBelow1182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1182(code,U32.is_lt(code,71724))
    case False{}: categoryBelow1183(code,U32.is_lt(code,71904))
def categoryBelow1185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1181(code,U32.is_lt(code,71488))
    case False{}: categoryBelow1184(code,U32.is_lt(code,71840))
def categoryBelow1186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1179(code,U32.is_lt(code,71424))
    case False{}: categoryBelow1185(code,U32.is_lt(code,71680))
def categoryBelow1187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1187(code,U32.is_lt(code,71943))
def categoryBelow1189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1189(code,U32.is_lt(code,71948))
def categoryBelow1191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1188(code,U32.is_lt(code,71935))
    case False{}: categoryBelow1190(code,U32.is_lt(code,71946))
def categoryBelow1192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1192(code,U32.is_lt(code,71959))
def categoryBelow1194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1194(code,U32.is_lt(code,71984))
    case False{}: categoryBelow1195(code,U32.is_lt(code,72000))
def categoryBelow1197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1193(code,U32.is_lt(code,71957))
    case False{}: categoryBelow1196(code,U32.is_lt(code,71999))
def categoryBelow1198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1191(code,U32.is_lt(code,71945))
    case False{}: categoryBelow1197(code,U32.is_lt(code,71960))
def categoryBelow1199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1186(code,U32.is_lt(code,71484))
    case False{}: categoryBelow1198(code,U32.is_lt(code,71956))
def categoryBelow1200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1174(code,U32.is_lt(code,71128))
    case False{}: categoryBelow1199(code,U32.is_lt(code,71923))
def categoryBelow1201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1150(code,U32.is_lt(code,70462))
    case False{}: categoryBelow1200(code,U32.is_lt(code,71370))
def categoryBelow1202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1101(code,U32.is_lt(code,69552))
    case False{}: categoryBelow1201(code,U32.is_lt(code,70751))
def categoryBelow1203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1002(code,U32.is_lt(code,66513))
    case False{}: categoryBelow1202(code,U32.is_lt(code,70280))
def categoryBelow1204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1204(code,U32.is_lt(code,72016))
def categoryBelow1206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1206(code,U32.is_lt(code,72104))
def categoryBelow1208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1205(code,U32.is_lt(code,72002))
    case False{}: categoryBelow1207(code,U32.is_lt(code,72096))
def categoryBelow1209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1209(code,U32.is_lt(code,72161))
def categoryBelow1211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1211(code,U32.is_lt(code,72164))
def categoryBelow1213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1210(code,U32.is_lt(code,72145))
    case False{}: categoryBelow1212(code,U32.is_lt(code,72163))
def categoryBelow1214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1208(code,U32.is_lt(code,72026))
    case False{}: categoryBelow1213(code,U32.is_lt(code,72162))
def categoryBelow1215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1215(code,U32.is_lt(code,72203))
def categoryBelow1217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1217(code,U32.is_lt(code,72251))
def categoryBelow1219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1216(code,U32.is_lt(code,72193))
    case False{}: categoryBelow1218(code,U32.is_lt(code,72250))
def categoryBelow1220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1220(code,U32.is_lt(code,72284))
def categoryBelow1222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1222(code,U32.is_lt(code,72349))
    case False{}: categoryBelow1223(code,U32.is_lt(code,72368))
def categoryBelow1225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1221(code,U32.is_lt(code,72273))
    case False{}: categoryBelow1224(code,U32.is_lt(code,72350))
def categoryBelow1226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1219(code,U32.is_lt(code,72243))
    case False{}: categoryBelow1225(code,U32.is_lt(code,72330))
def categoryBelow1227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1214(code,U32.is_lt(code,72106))
    case False{}: categoryBelow1226(code,U32.is_lt(code,72272))
def categoryBelow1228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1228(code,U32.is_lt(code,72673))
def categoryBelow1230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1230(code,U32.is_lt(code,72704))
def categoryBelow1232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1229(code,U32.is_lt(code,72640))
    case False{}: categoryBelow1231(code,U32.is_lt(code,72698))
def categoryBelow1233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1233(code,U32.is_lt(code,72751))
def categoryBelow1235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1235(code,U32.is_lt(code,72784))
def categoryBelow1237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1234(code,U32.is_lt(code,72714))
    case False{}: categoryBelow1236(code,U32.is_lt(code,72769))
def categoryBelow1238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1232(code,U32.is_lt(code,72688))
    case False{}: categoryBelow1237(code,U32.is_lt(code,72768))
def categoryBelow1239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1239(code,U32.is_lt(code,72848))
def categoryBelow1241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1241(code,U32.is_lt(code,72968))
def categoryBelow1243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1240(code,U32.is_lt(code,72818))
    case False{}: categoryBelow1242(code,U32.is_lt(code,72967))
def categoryBelow1244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1244(code,U32.is_lt(code,73009))
def categoryBelow1246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1246(code,U32.is_lt(code,73031))
    case False{}: categoryBelow1247(code,U32.is_lt(code,73050))
def categoryBelow1249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1245(code,U32.is_lt(code,72971))
    case False{}: categoryBelow1248(code,U32.is_lt(code,73040))
def categoryBelow1250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1243(code,U32.is_lt(code,72960))
    case False{}: categoryBelow1249(code,U32.is_lt(code,73030))
def categoryBelow1251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1238(code,U32.is_lt(code,72713))
    case False{}: categoryBelow1250(code,U32.is_lt(code,72970))
def categoryBelow1252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1227(code,U32.is_lt(code,72192))
    case False{}: categoryBelow1251(code,U32.is_lt(code,72813))
def categoryBelow1253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1253(code,U32.is_lt(code,73063))
def categoryBelow1255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1255(code,U32.is_lt(code,73098))
def categoryBelow1257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1254(code,U32.is_lt(code,73062))
    case False{}: categoryBelow1256(code,U32.is_lt(code,73066))
def categoryBelow1258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1258(code,U32.is_lt(code,73120))
def categoryBelow1260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1260(code,U32.is_lt(code,73180))
def categoryBelow1262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1259(code,U32.is_lt(code,73113))
    case False{}: categoryBelow1261(code,U32.is_lt(code,73136))
def categoryBelow1263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1257(code,U32.is_lt(code,73065))
    case False{}: categoryBelow1262(code,U32.is_lt(code,73130))
def categoryBelow1264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1264(code,U32.is_lt(code,73440))
def categoryBelow1266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1266(code,U32.is_lt(code,73475))
def categoryBelow1268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1265(code,U32.is_lt(code,73194))
    case False{}: categoryBelow1267(code,U32.is_lt(code,73474))
def categoryBelow1269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1269(code,U32.is_lt(code,73490))
def categoryBelow1271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1271(code,U32.is_lt(code,73552))
    case False{}: categoryBelow1272(code,U32.is_lt(code,73648))
def categoryBelow1274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1270(code,U32.is_lt(code,73489))
    case False{}: categoryBelow1273(code,U32.is_lt(code,73562))
def categoryBelow1275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1268(code,U32.is_lt(code,73459))
    case False{}: categoryBelow1274(code,U32.is_lt(code,73524))
def categoryBelow1276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1263(code,U32.is_lt(code,73112))
    case False{}: categoryBelow1275(code,U32.is_lt(code,73476))
def categoryBelow1277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1277(code,U32.is_lt(code,73685))
def categoryBelow1279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1279(code,U32.is_lt(code,74752))
def categoryBelow1281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1278(code,U32.is_lt(code,73664))
    case False{}: categoryBelow1280(code,U32.is_lt(code,74650))
def categoryBelow1282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1282(code,U32.is_lt(code,75076))
def categoryBelow1284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1284(code,U32.is_lt(code,77824))
def categoryBelow1286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1283(code,U32.is_lt(code,74880))
    case False{}: categoryBelow1285(code,U32.is_lt(code,77809))
def categoryBelow1287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1281(code,U32.is_lt(code,73728))
    case False{}: categoryBelow1286(code,U32.is_lt(code,77712))
def categoryBelow1288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1288(code,U32.is_lt(code,78919))
def categoryBelow1290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1290(code,U32.is_lt(code,82944))
def categoryBelow1292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1289(code,U32.is_lt(code,78913))
    case False{}: categoryBelow1291(code,U32.is_lt(code,82939))
def categoryBelow1293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1293(code,U32.is_lt(code,90398))
def categoryBelow1295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1295(code,U32.is_lt(code,90426))
    case False{}: categoryBelow1296(code,U32.is_lt(code,92729))
def categoryBelow1298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1294(code,U32.is_lt(code,90368))
    case False{}: categoryBelow1297(code,U32.is_lt(code,92160))
def categoryBelow1299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1292(code,U32.is_lt(code,78944))
    case False{}: categoryBelow1298(code,U32.is_lt(code,90416))
def categoryBelow1300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1287(code,U32.is_lt(code,74863))
    case False{}: categoryBelow1299(code,U32.is_lt(code,83527))
def categoryBelow1301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1276(code,U32.is_lt(code,73184))
    case False{}: categoryBelow1300(code,U32.is_lt(code,78896))
def categoryBelow1302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1252(code,U32.is_lt(code,72441))
    case False{}: categoryBelow1301(code,U32.is_lt(code,73649))
def categoryBelow1303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1303(code,U32.is_lt(code,92768))
def categoryBelow1305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1305(code,U32.is_lt(code,92863))
def categoryBelow1307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1304(code,U32.is_lt(code,92767))
    case False{}: categoryBelow1306(code,U32.is_lt(code,92784))
def categoryBelow1308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1308(code,U32.is_lt(code,92880))
def categoryBelow1310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1310(code,U32.is_lt(code,92976))
def categoryBelow1312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1309(code,U32.is_lt(code,92874))
    case False{}: categoryBelow1311(code,U32.is_lt(code,92928))
def categoryBelow1313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1307(code,U32.is_lt(code,92778))
    case False{}: categoryBelow1312(code,U32.is_lt(code,92910))
def categoryBelow1314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1314(code,U32.is_lt(code,93008))
def categoryBelow1316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1316(code,U32.is_lt(code,93026))
def categoryBelow1318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1315(code,U32.is_lt(code,92996))
    case False{}: categoryBelow1317(code,U32.is_lt(code,93019))
def categoryBelow1319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1319(code,U32.is_lt(code,93053))
def categoryBelow1321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1321(code,U32.is_lt(code,93504))
    case False{}: categoryBelow1322(code,U32.is_lt(code,93552))
def categoryBelow1324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1320(code,U32.is_lt(code,93048))
    case False{}: categoryBelow1323(code,U32.is_lt(code,93549))
def categoryBelow1325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1318(code,U32.is_lt(code,93018))
    case False{}: categoryBelow1324(code,U32.is_lt(code,93072))
def categoryBelow1326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1313(code,U32.is_lt(code,92864))
    case False{}: categoryBelow1325(code,U32.is_lt(code,93027))
def categoryBelow1327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1327(code,U32.is_lt(code,93824))
def categoryBelow1329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1329(code,U32.is_lt(code,93881))
def categoryBelow1331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1328(code,U32.is_lt(code,93760))
    case False{}: categoryBelow1330(code,U32.is_lt(code,93856))
def categoryBelow1332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1332(code,U32.is_lt(code,93952))
def categoryBelow1334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1334(code,U32.is_lt(code,94033))
def categoryBelow1336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1333(code,U32.is_lt(code,93908))
    case False{}: categoryBelow1335(code,U32.is_lt(code,94032))
def categoryBelow1337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1331(code,U32.is_lt(code,93847))
    case False{}: categoryBelow1336(code,U32.is_lt(code,94027))
def categoryBelow1338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1338(code,U32.is_lt(code,94176))
def categoryBelow1340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1340(code,U32.is_lt(code,94180))
def categoryBelow1342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1339(code,U32.is_lt(code,94112))
    case False{}: categoryBelow1341(code,U32.is_lt(code,94179))
def categoryBelow1343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1343(code,U32.is_lt(code,94199))
def categoryBelow1345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1345(code,U32.is_lt(code,101590))
    case False{}: categoryBelow1346(code,U32.is_lt(code,101663))
def categoryBelow1348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1344(code,U32.is_lt(code,94196))
    case False{}: categoryBelow1347(code,U32.is_lt(code,101631))
def categoryBelow1349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1342(code,U32.is_lt(code,94178))
    case False{}: categoryBelow1348(code,U32.is_lt(code,94208))
def categoryBelow1350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1337(code,U32.is_lt(code,93883))
    case False{}: categoryBelow1349(code,U32.is_lt(code,94194))
def categoryBelow1351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1326(code,U32.is_lt(code,92992))
    case False{}: categoryBelow1350(code,U32.is_lt(code,94099))
def categoryBelow1352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1352(code,U32.is_lt(code,110576))
def categoryBelow1354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1354(code,U32.is_lt(code,110588))
def categoryBelow1356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1353(code,U32.is_lt(code,101875))
    case False{}: categoryBelow1355(code,U32.is_lt(code,110581))
def categoryBelow1357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1357(code,U32.is_lt(code,110592))
def categoryBelow1359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1359(code,U32.is_lt(code,110899))
def categoryBelow1361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1358(code,U32.is_lt(code,110591))
    case False{}: categoryBelow1360(code,U32.is_lt(code,110898))
def categoryBelow1362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1356(code,U32.is_lt(code,110580))
    case False{}: categoryBelow1361(code,U32.is_lt(code,110883))
def categoryBelow1363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1363(code,U32.is_lt(code,110933))
def categoryBelow1365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1365(code,U32.is_lt(code,110952))
def categoryBelow1367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1364(code,U32.is_lt(code,110931))
    case False{}: categoryBelow1366(code,U32.is_lt(code,110948))
def categoryBelow1368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1368(code,U32.is_lt(code,113664))
def categoryBelow1370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1370(code,U32.is_lt(code,113776))
    case False{}: categoryBelow1371(code,U32.is_lt(code,113792))
def categoryBelow1373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1369(code,U32.is_lt(code,111356))
    case False{}: categoryBelow1372(code,U32.is_lt(code,113789))
def categoryBelow1374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1367(code,U32.is_lt(code,110934))
    case False{}: categoryBelow1373(code,U32.is_lt(code,113771))
def categoryBelow1375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1362(code,U32.is_lt(code,110589))
    case False{}: categoryBelow1374(code,U32.is_lt(code,110960))
def categoryBelow1376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1376(code,U32.is_lt(code,113818))
def categoryBelow1378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1378(code,U32.is_lt(code,119488))
def categoryBelow1380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1377(code,U32.is_lt(code,113808))
    case False{}: categoryBelow1379(code,U32.is_lt(code,118010))
def categoryBelow1381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1381(code,U32.is_lt(code,119540))
def categoryBelow1383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1383(code,U32.is_lt(code,119673))
    case False{}: categoryBelow1384(code,U32.is_lt(code,119893))
def categoryBelow1386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1382(code,U32.is_lt(code,119520))
    case False{}: categoryBelow1385(code,U32.is_lt(code,119808))
def categoryBelow1387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1380(code,U32.is_lt(code,118000))
    case False{}: categoryBelow1386(code,U32.is_lt(code,119648))
def categoryBelow1388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1388(code,U32.is_lt(code,119966))
def categoryBelow1390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1390(code,U32.is_lt(code,119971))
def categoryBelow1392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1389(code,U32.is_lt(code,119965))
    case False{}: categoryBelow1391(code,U32.is_lt(code,119970))
def categoryBelow1393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1393(code,U32.is_lt(code,119977))
def categoryBelow1395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1395(code,U32.is_lt(code,119982))
    case False{}: categoryBelow1396(code,U32.is_lt(code,119995))
def categoryBelow1398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1394(code,U32.is_lt(code,119975))
    case False{}: categoryBelow1397(code,U32.is_lt(code,119994))
def categoryBelow1399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1392(code,U32.is_lt(code,119968))
    case False{}: categoryBelow1398(code,U32.is_lt(code,119981))
def categoryBelow1400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1387(code,U32.is_lt(code,119508))
    case False{}: categoryBelow1399(code,U32.is_lt(code,119973))
def categoryBelow1401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1375(code,U32.is_lt(code,110928))
    case False{}: categoryBelow1400(code,U32.is_lt(code,119894))
def categoryBelow1402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1351(code,U32.is_lt(code,93562))
    case False{}: categoryBelow1401(code,U32.is_lt(code,113801))
def categoryBelow1403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1302(code,U32.is_lt(code,73056))
    case False{}: categoryBelow1402(code,U32.is_lt(code,101760))
def categoryBelow1404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1404(code,U32.is_lt(code,120004))
def categoryBelow1406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1406(code,U32.is_lt(code,120071))
def categoryBelow1408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1405(code,U32.is_lt(code,119997))
    case False{}: categoryBelow1407(code,U32.is_lt(code,120070))
def categoryBelow1409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1409(code,U32.is_lt(code,120085))
def categoryBelow1411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1411(code,U32.is_lt(code,120094))
def categoryBelow1413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1410(code,U32.is_lt(code,120077))
    case False{}: categoryBelow1412(code,U32.is_lt(code,120093))
def categoryBelow1414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1408(code,U32.is_lt(code,120005))
    case False{}: categoryBelow1413(code,U32.is_lt(code,120086))
def categoryBelow1415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1415(code,U32.is_lt(code,120127))
def categoryBelow1417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1417(code,U32.is_lt(code,120134))
def categoryBelow1419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1416(code,U32.is_lt(code,120123))
    case False{}: categoryBelow1418(code,U32.is_lt(code,120133))
def categoryBelow1420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1420(code,U32.is_lt(code,120145))
def categoryBelow1422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1422(code,U32.is_lt(code,120486))
    case False{}: categoryBelow1423(code,U32.is_lt(code,120513))
def categoryBelow1425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1421(code,U32.is_lt(code,120138))
    case False{}: categoryBelow1424(code,U32.is_lt(code,120488))
def categoryBelow1426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1419(code,U32.is_lt(code,120128))
    case False{}: categoryBelow1425(code,U32.is_lt(code,120146))
def categoryBelow1427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1414(code,U32.is_lt(code,120075))
    case False{}: categoryBelow1426(code,U32.is_lt(code,120135))
def categoryBelow1428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1428(code,U32.is_lt(code,120540))
def categoryBelow1430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1430(code,U32.is_lt(code,120597))
def categoryBelow1432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1429(code,U32.is_lt(code,120539))
    case False{}: categoryBelow1431(code,U32.is_lt(code,120572))
def categoryBelow1433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1433(code,U32.is_lt(code,120630))
def categoryBelow1435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1435(code,U32.is_lt(code,120687))
def categoryBelow1437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1434(code,U32.is_lt(code,120629))
    case False{}: categoryBelow1436(code,U32.is_lt(code,120656))
def categoryBelow1438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1432(code,U32.is_lt(code,120571))
    case False{}: categoryBelow1437(code,U32.is_lt(code,120655))
def categoryBelow1439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1439(code,U32.is_lt(code,120714))
def categoryBelow1441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1441(code,U32.is_lt(code,120771))
def categoryBelow1443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1440(code,U32.is_lt(code,120713))
    case False{}: categoryBelow1442(code,U32.is_lt(code,120746))
def categoryBelow1444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1444(code,U32.is_lt(code,120782))
def categoryBelow1446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1446(code,U32.is_lt(code,122624))
    case False{}: categoryBelow1447(code,U32.is_lt(code,122661))
def categoryBelow1449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1445(code,U32.is_lt(code,120780))
    case False{}: categoryBelow1448(code,U32.is_lt(code,122655))
def categoryBelow1450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1443(code,U32.is_lt(code,120745))
    case False{}: categoryBelow1449(code,U32.is_lt(code,120832))
def categoryBelow1451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1438(code,U32.is_lt(code,120598))
    case False{}: categoryBelow1450(code,U32.is_lt(code,120772))
def categoryBelow1452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1427(code,U32.is_lt(code,120122))
    case False{}: categoryBelow1451(code,U32.is_lt(code,120688))
def categoryBelow1453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1453(code,U32.is_lt(code,122990))
def categoryBelow1455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1455(code,U32.is_lt(code,123191))
def categoryBelow1457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1454(code,U32.is_lt(code,122928))
    case False{}: categoryBelow1456(code,U32.is_lt(code,123181))
def categoryBelow1458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1458(code,U32.is_lt(code,123210))
def categoryBelow1460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1460(code,U32.is_lt(code,123536))
def categoryBelow1462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1459(code,U32.is_lt(code,123200))
    case False{}: categoryBelow1461(code,U32.is_lt(code,123215))
def categoryBelow1463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1457(code,U32.is_lt(code,123136))
    case False{}: categoryBelow1462(code,U32.is_lt(code,123214))
def categoryBelow1464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1464(code,U32.is_lt(code,123628))
def categoryBelow1466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1466(code,U32.is_lt(code,124112))
def categoryBelow1468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1465(code,U32.is_lt(code,123584))
    case False{}: categoryBelow1467(code,U32.is_lt(code,123642))
def categoryBelow1469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1469(code,U32.is_lt(code,124154))
def categoryBelow1471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 2
def categoryBelow1473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1471(code,U32.is_lt(code,124398))
    case False{}: categoryBelow1472(code,U32.is_lt(code,124401))
def categoryBelow1474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1470(code,U32.is_lt(code,124144))
    case False{}: categoryBelow1473(code,U32.is_lt(code,124400))
def categoryBelow1475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1468(code,U32.is_lt(code,123632))
    case False{}: categoryBelow1474(code,U32.is_lt(code,124368))
def categoryBelow1476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1463(code,U32.is_lt(code,123198))
    case False{}: categoryBelow1475(code,U32.is_lt(code,124140))
def categoryBelow1477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1477(code,U32.is_lt(code,124639))
def categoryBelow1479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1479(code,U32.is_lt(code,124644))
def categoryBelow1481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1478(code,U32.is_lt(code,124608))
    case False{}: categoryBelow1480(code,U32.is_lt(code,124643))
def categoryBelow1482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1482(code,U32.is_lt(code,124654))
def categoryBelow1484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1484(code,U32.is_lt(code,124661))
    case False{}: categoryBelow1485(code,U32.is_lt(code,124672))
def categoryBelow1487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1483(code,U32.is_lt(code,124647))
    case False{}: categoryBelow1486(code,U32.is_lt(code,124670))
def categoryBelow1488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1481(code,U32.is_lt(code,124640))
    case False{}: categoryBelow1487(code,U32.is_lt(code,124656))
def categoryBelow1489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1489(code,U32.is_lt(code,124904))
def categoryBelow1491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1491(code,U32.is_lt(code,124911))
def categoryBelow1493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1490(code,U32.is_lt(code,124903))
    case False{}: categoryBelow1492(code,U32.is_lt(code,124909))
def categoryBelow1494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1494(code,U32.is_lt(code,124928))
def categoryBelow1496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1496(code,U32.is_lt(code,125127))
    case False{}: categoryBelow1497(code,U32.is_lt(code,125184))
def categoryBelow1499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1495(code,U32.is_lt(code,124927))
    case False{}: categoryBelow1498(code,U32.is_lt(code,125136))
def categoryBelow1500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1493(code,U32.is_lt(code,124908))
    case False{}: categoryBelow1499(code,U32.is_lt(code,125125))
def categoryBelow1501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1488(code,U32.is_lt(code,124646))
    case False{}: categoryBelow1500(code,U32.is_lt(code,124912))
def categoryBelow1502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1476(code,U32.is_lt(code,123566))
    case False{}: categoryBelow1501(code,U32.is_lt(code,124896))
def categoryBelow1503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1452(code,U32.is_lt(code,120514))
    case False{}: categoryBelow1502(code,U32.is_lt(code,124411))
def categoryBelow1504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1504(code,U32.is_lt(code,125260))
def categoryBelow1506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1506(code,U32.is_lt(code,126065))
def categoryBelow1508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1505(code,U32.is_lt(code,125259))
    case False{}: categoryBelow1507(code,U32.is_lt(code,125274))
def categoryBelow1509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1509(code,U32.is_lt(code,126128))
def categoryBelow1511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1511(code,U32.is_lt(code,126209))
def categoryBelow1513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1510(code,U32.is_lt(code,126125))
    case False{}: categoryBelow1512(code,U32.is_lt(code,126133))
def categoryBelow1514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1508(code,U32.is_lt(code,125264))
    case False{}: categoryBelow1513(code,U32.is_lt(code,126129))
def categoryBelow1515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: 0
def categoryBelow1516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1515(code,U32.is_lt(code,126270))
def categoryBelow1517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1517(code,U32.is_lt(code,126469))
def categoryBelow1519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1516(code,U32.is_lt(code,126255))
    case False{}: categoryBelow1518(code,U32.is_lt(code,126468))
def categoryBelow1520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1520(code,U32.is_lt(code,126499))
def categoryBelow1522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1523(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1524(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1522(code,U32.is_lt(code,126501))
    case False{}: categoryBelow1523(code,U32.is_lt(code,126504))
def categoryBelow1525(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1521(code,U32.is_lt(code,126497))
    case False{}: categoryBelow1524(code,U32.is_lt(code,126503))
def categoryBelow1526(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1519(code,U32.is_lt(code,126464))
    case False{}: categoryBelow1525(code,U32.is_lt(code,126500))
def categoryBelow1527(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1514(code,U32.is_lt(code,126124))
    case False{}: categoryBelow1526(code,U32.is_lt(code,126496))
def categoryBelow1528(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1529(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1528(code,U32.is_lt(code,126516))
def categoryBelow1530(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1531(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1530(code,U32.is_lt(code,126522))
def categoryBelow1532(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1529(code,U32.is_lt(code,126515))
    case False{}: categoryBelow1531(code,U32.is_lt(code,126521))
def categoryBelow1533(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1534(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1533(code,U32.is_lt(code,126530))
def categoryBelow1535(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1536(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1535(code,U32.is_lt(code,126536))
def categoryBelow1537(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1534(code,U32.is_lt(code,126524))
    case False{}: categoryBelow1536(code,U32.is_lt(code,126535))
def categoryBelow1538(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1532(code,U32.is_lt(code,126520))
    case False{}: categoryBelow1537(code,U32.is_lt(code,126531))
def categoryBelow1539(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1540(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1539(code,U32.is_lt(code,126539))
def categoryBelow1541(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1542(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1541(code,U32.is_lt(code,126544))
def categoryBelow1543(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1540(code,U32.is_lt(code,126538))
    case False{}: categoryBelow1542(code,U32.is_lt(code,126541))
def categoryBelow1544(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1545(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1544(code,U32.is_lt(code,126548))
def categoryBelow1546(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1547(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1548(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1546(code,U32.is_lt(code,126551))
    case False{}: categoryBelow1547(code,U32.is_lt(code,126553))
def categoryBelow1549(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1545(code,U32.is_lt(code,126547))
    case False{}: categoryBelow1548(code,U32.is_lt(code,126552))
def categoryBelow1550(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1543(code,U32.is_lt(code,126540))
    case False{}: categoryBelow1549(code,U32.is_lt(code,126549))
def categoryBelow1551(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1538(code,U32.is_lt(code,126523))
    case False{}: categoryBelow1550(code,U32.is_lt(code,126545))
def categoryBelow1552(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1527(code,U32.is_lt(code,126254))
    case False{}: categoryBelow1551(code,U32.is_lt(code,126537))
def categoryBelow1553(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1554(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1553(code,U32.is_lt(code,126556))
def categoryBelow1555(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1556(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1555(code,U32.is_lt(code,126559))
def categoryBelow1557(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1554(code,U32.is_lt(code,126555))
    case False{}: categoryBelow1556(code,U32.is_lt(code,126558))
def categoryBelow1558(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1559(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1558(code,U32.is_lt(code,126563))
def categoryBelow1560(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1561(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1560(code,U32.is_lt(code,126567))
def categoryBelow1562(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1559(code,U32.is_lt(code,126561))
    case False{}: categoryBelow1561(code,U32.is_lt(code,126565))
def categoryBelow1563(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1557(code,U32.is_lt(code,126557))
    case False{}: categoryBelow1562(code,U32.is_lt(code,126564))
def categoryBelow1564(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1565(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1564(code,U32.is_lt(code,126579))
def categoryBelow1566(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1567(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1566(code,U32.is_lt(code,126585))
def categoryBelow1568(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1565(code,U32.is_lt(code,126572))
    case False{}: categoryBelow1567(code,U32.is_lt(code,126584))
def categoryBelow1569(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1570(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1569(code,U32.is_lt(code,126591))
def categoryBelow1571(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1572(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1573(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1571(code,U32.is_lt(code,126602))
    case False{}: categoryBelow1572(code,U32.is_lt(code,126620))
def categoryBelow1574(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1570(code,U32.is_lt(code,126590))
    case False{}: categoryBelow1573(code,U32.is_lt(code,126603))
def categoryBelow1575(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1568(code,U32.is_lt(code,126580))
    case False{}: categoryBelow1574(code,U32.is_lt(code,126592))
def categoryBelow1576(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1563(code,U32.is_lt(code,126560))
    case False{}: categoryBelow1575(code,U32.is_lt(code,126589))
def categoryBelow1577(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1578(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1577(code,U32.is_lt(code,126629))
def categoryBelow1579(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1580(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1579(code,U32.is_lt(code,126652))
def categoryBelow1581(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1578(code,U32.is_lt(code,126628))
    case False{}: categoryBelow1580(code,U32.is_lt(code,126635))
def categoryBelow1582(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 2
def categoryBelow1583(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 2
    case False{}: categoryBelow1582(code,U32.is_lt(code,130032))
def categoryBelow1584(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1585(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1586(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1584(code,U32.is_lt(code,131072))
    case False{}: categoryBelow1585(code,U32.is_lt(code,173824))
def categoryBelow1587(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1583(code,U32.is_lt(code,127245))
    case False{}: categoryBelow1586(code,U32.is_lt(code,173792))
def categoryBelow1588(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1581(code,U32.is_lt(code,126634))
    case False{}: categoryBelow1587(code,U32.is_lt(code,130042))
def categoryBelow1589(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1590(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1589(code,U32.is_lt(code,183982))
def categoryBelow1591(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def categoryBelow1592(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: categoryBelow1591(code,U32.is_lt(code,191472))
def categoryBelow1593(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1590(code,U32.is_lt(code,178208))
    case False{}: categoryBelow1592(code,U32.is_lt(code,191457))
def categoryBelow1594(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1595(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: categoryBelow1594(code,U32.is_lt(code,195102))
def categoryBelow1596(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1597(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def categoryBelow1598(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1596(code,U32.is_lt(code,201547))
    case False{}: categoryBelow1597(code,U32.is_lt(code,210042))
def categoryBelow1599(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1595(code,U32.is_lt(code,194560))
    case False{}: categoryBelow1598(code,U32.is_lt(code,201552))
def categoryBelow1600(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1593(code,U32.is_lt(code,183984))
    case False{}: categoryBelow1599(code,U32.is_lt(code,196608))
def categoryBelow1601(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1588(code,U32.is_lt(code,127232))
    case False{}: categoryBelow1600(code,U32.is_lt(code,192094))
def categoryBelow1602(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1576(code,U32.is_lt(code,126571))
    case False{}: categoryBelow1601(code,U32.is_lt(code,178206))
def categoryBelow1603(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1552(code,U32.is_lt(code,126505))
    case False{}: categoryBelow1602(code,U32.is_lt(code,126625))
def categoryBelow1604(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1503(code,U32.is_lt(code,122667))
    case False{}: categoryBelow1603(code,U32.is_lt(code,126554))
def categoryBelow1605(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1403(code,U32.is_lt(code,92736))
    case False{}: categoryBelow1604(code,U32.is_lt(code,125252))
def categoryBelow1606(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow1203(code,U32.is_lt(code,68297))
    case False{}: categoryBelow1605(code,U32.is_lt(code,119996))
def categoryBelow1607(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: categoryBelow802(code,U32.is_lt(code,4750))
    case False{}: categoryBelow1606(code,U32.is_lt(code,72001))

def category(+code: U32) -> U32:
  categoryBelow1607(code,U32.is_lt(code,44032))
def isLetter(+code: U32) -> Bool: U32.is_eq(category(code),1)
def isNumber(+code: U32) -> Bool: U32.is_eq(category(code),2)
