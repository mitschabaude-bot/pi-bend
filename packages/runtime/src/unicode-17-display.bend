# Generated by scripts/generate-display-unicode.py from Unicode17.0.0.
# Unicode License V3; see unicode-LICENSE.txt. Constant decisions (plain definitions) allocate no table.
# Bits: Mark=1, SpacingMark=2, Control/Surrogate=4, Format=8,
# Default_Ignorable=16, Wide/Fullwidth=32, singleton RGI_Emoji=64, multi-scalar RGI start=128, CJK break Script_Extensions=256.
import Base
import ./string.bend as Strings

# 1524 property runs; exact membership for 3953 RGI emoji.
def propertiesBelow0(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: 0
def propertiesBelow1(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow2(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1(code,U32.is_lt(code,42))
def propertiesBelow3(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow0(code,U32.is_lt(code,32))
    case False{}: propertiesBelow2(code,U32.is_lt(code,36))
def propertiesBelow4(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow5(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow4(code,U32.is_lt(code,58))
def propertiesBelow6(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow7(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: propertiesBelow6(code,U32.is_lt(code,169))
def propertiesBelow8(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow5(code,U32.is_lt(code,48))
    case False{}: propertiesBelow7(code,U32.is_lt(code,160))
def propertiesBelow9(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow3(code,U32.is_lt(code,35))
    case False{}: propertiesBelow8(code,U32.is_lt(code,127))
def propertiesBelow10(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 24
    case False{}: 128
def propertiesBelow11(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow10(code,U32.is_lt(code,174))
def propertiesBelow12(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: 0
def propertiesBelow13(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow12(code,U32.is_lt(code,184))
def propertiesBelow14(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow11(code,U32.is_lt(code,173))
    case False{}: propertiesBelow13(code,U32.is_lt(code,183))
def propertiesBelow15(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 256
def propertiesBelow16(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: propertiesBelow15(code,U32.is_lt(code,713))
def propertiesBelow17(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: 0
def propertiesBelow18(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow17(code,U32.is_lt(code,730))
def propertiesBelow19(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow16(code,U32.is_lt(code,712))
    case False{}: propertiesBelow18(code,U32.is_lt(code,729))
def propertiesBelow20(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow14(code,U32.is_lt(code,175))
    case False{}: propertiesBelow19(code,U32.is_lt(code,716))
def propertiesBelow21(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow9(code,U32.is_lt(code,43))
    case False{}: propertiesBelow20(code,U32.is_lt(code,711))
def propertiesBelow22(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow23(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: propertiesBelow22(code,U32.is_lt(code,768))
def propertiesBelow24(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 257
def propertiesBelow25(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 257
    case False{}: propertiesBelow24(code,U32.is_lt(code,803))
def propertiesBelow26(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow23(code,U32.is_lt(code,748))
    case False{}: propertiesBelow25(code,U32.is_lt(code,774))
def propertiesBelow27(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 17
    case False{}: 1
def propertiesBelow28(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow27(code,U32.is_lt(code,848))
def propertiesBelow29(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow30(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow29(code,U32.is_lt(code,1162))
def propertiesBelow31(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow28(code,U32.is_lt(code,847))
    case False{}: propertiesBelow30(code,U32.is_lt(code,1155))
def propertiesBelow32(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow26(code,U32.is_lt(code,773))
    case False{}: propertiesBelow31(code,U32.is_lt(code,880))
def propertiesBelow33(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow34(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow33(code,U32.is_lt(code,1471))
def propertiesBelow35(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow36(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow35(code,U32.is_lt(code,1475))
def propertiesBelow37(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow34(code,U32.is_lt(code,1470))
    case False{}: propertiesBelow36(code,U32.is_lt(code,1473))
def propertiesBelow38(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow39(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow38(code,U32.is_lt(code,1479))
def propertiesBelow40(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def propertiesBelow41(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow40(code,U32.is_lt(code,1542))
def propertiesBelow42(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow39(code,U32.is_lt(code,1478))
    case False{}: propertiesBelow41(code,U32.is_lt(code,1536))
def propertiesBelow43(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow37(code,U32.is_lt(code,1472))
    case False{}: propertiesBelow42(code,U32.is_lt(code,1480))
def propertiesBelow44(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow32(code,U32.is_lt(code,804))
    case False{}: propertiesBelow43(code,U32.is_lt(code,1476))
def propertiesBelow45(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow21(code,U32.is_lt(code,170))
    case False{}: propertiesBelow44(code,U32.is_lt(code,1425))
def propertiesBelow46(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 24
def propertiesBelow47(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow46(code,U32.is_lt(code,1564))
def propertiesBelow48(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow49(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow48(code,U32.is_lt(code,1632))
def propertiesBelow50(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow47(code,U32.is_lt(code,1563))
    case False{}: propertiesBelow49(code,U32.is_lt(code,1611))
def propertiesBelow51(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow52(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow51(code,U32.is_lt(code,1750))
def propertiesBelow53(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow54(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: propertiesBelow53(code,U32.is_lt(code,1759))
def propertiesBelow55(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow52(code,U32.is_lt(code,1649))
    case False{}: propertiesBelow54(code,U32.is_lt(code,1758))
def propertiesBelow56(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow50(code,U32.is_lt(code,1565))
    case False{}: propertiesBelow55(code,U32.is_lt(code,1757))
def propertiesBelow57(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow58(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow57(code,U32.is_lt(code,1769))
def propertiesBelow59(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def propertiesBelow60(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow59(code,U32.is_lt(code,1807))
def propertiesBelow61(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow58(code,U32.is_lt(code,1767))
    case False{}: propertiesBelow60(code,U32.is_lt(code,1774))
def propertiesBelow62(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow63(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow62(code,U32.is_lt(code,1810))
def propertiesBelow64(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow65(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow64(code,U32.is_lt(code,1958))
def propertiesBelow66(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow63(code,U32.is_lt(code,1809))
    case False{}: propertiesBelow65(code,U32.is_lt(code,1867))
def propertiesBelow67(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow61(code,U32.is_lt(code,1770))
    case False{}: propertiesBelow66(code,U32.is_lt(code,1840))
def propertiesBelow68(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow56(code,U32.is_lt(code,1648))
    case False{}: propertiesBelow67(code,U32.is_lt(code,1808))
def propertiesBelow69(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow70(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow69(code,U32.is_lt(code,2036))
def propertiesBelow71(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow72(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow71(code,U32.is_lt(code,2070))
def propertiesBelow73(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow70(code,U32.is_lt(code,2027))
    case False{}: propertiesBelow72(code,U32.is_lt(code,2046))
def propertiesBelow74(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow75(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow74(code,U32.is_lt(code,2084))
def propertiesBelow76(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow77(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow76(code,U32.is_lt(code,2089))
def propertiesBelow78(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow75(code,U32.is_lt(code,2075))
    case False{}: propertiesBelow77(code,U32.is_lt(code,2088))
def propertiesBelow79(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow73(code,U32.is_lt(code,2045))
    case False{}: propertiesBelow78(code,U32.is_lt(code,2085))
def propertiesBelow80(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow81(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow80(code,U32.is_lt(code,2140))
def propertiesBelow82(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow83(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: propertiesBelow82(code,U32.is_lt(code,2199))
def propertiesBelow84(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow81(code,U32.is_lt(code,2137))
    case False{}: propertiesBelow83(code,U32.is_lt(code,2194))
def propertiesBelow85(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 8
def propertiesBelow86(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow85(code,U32.is_lt(code,2274))
def propertiesBelow87(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow88(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow87(code,U32.is_lt(code,2308))
def propertiesBelow89(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow86(code,U32.is_lt(code,2250))
    case False{}: propertiesBelow88(code,U32.is_lt(code,2307))
def propertiesBelow90(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow84(code,U32.is_lt(code,2192))
    case False{}: propertiesBelow89(code,U32.is_lt(code,2275))
def propertiesBelow91(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow79(code,U32.is_lt(code,2074))
    case False{}: propertiesBelow90(code,U32.is_lt(code,2208))
def propertiesBelow92(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow68(code,U32.is_lt(code,1765))
    case False{}: propertiesBelow91(code,U32.is_lt(code,2094))
def propertiesBelow93(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow45(code,U32.is_lt(code,746))
    case False{}: propertiesBelow92(code,U32.is_lt(code,1969))
def propertiesBelow94(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow95(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow96(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow95(code,U32.is_lt(code,2366))
def propertiesBelow97(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow94(code,U32.is_lt(code,2363))
    case False{}: propertiesBelow96(code,U32.is_lt(code,2365))
def propertiesBelow98(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow99(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow98(code,U32.is_lt(code,2381))
def propertiesBelow100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow100(code,U32.is_lt(code,2385))
def propertiesBelow102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow99(code,U32.is_lt(code,2377))
    case False{}: propertiesBelow101(code,U32.is_lt(code,2384))
def propertiesBelow103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow97(code,U32.is_lt(code,2364))
    case False{}: propertiesBelow102(code,U32.is_lt(code,2382))
def propertiesBelow104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow104(code,U32.is_lt(code,2404))
def propertiesBelow106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow106(code,U32.is_lt(code,2436))
def propertiesBelow108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow105(code,U32.is_lt(code,2402))
    case False{}: propertiesBelow107(code,U32.is_lt(code,2434))
def propertiesBelow109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow109(code,U32.is_lt(code,2494))
def propertiesBelow111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow111(code,U32.is_lt(code,2503))
def propertiesBelow113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow110(code,U32.is_lt(code,2493))
    case False{}: propertiesBelow112(code,U32.is_lt(code,2501))
def propertiesBelow114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow108(code,U32.is_lt(code,2433))
    case False{}: propertiesBelow113(code,U32.is_lt(code,2497))
def propertiesBelow115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow103(code,U32.is_lt(code,2369))
    case False{}: propertiesBelow114(code,U32.is_lt(code,2492))
def propertiesBelow116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow116(code,U32.is_lt(code,2509))
def propertiesBelow118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow118(code,U32.is_lt(code,2520))
def propertiesBelow120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow117(code,U32.is_lt(code,2507))
    case False{}: propertiesBelow119(code,U32.is_lt(code,2519))
def propertiesBelow121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow121(code,U32.is_lt(code,2558))
def propertiesBelow123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow123(code,U32.is_lt(code,2563))
def propertiesBelow125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow122(code,U32.is_lt(code,2532))
    case False{}: propertiesBelow124(code,U32.is_lt(code,2561))
def propertiesBelow126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow120(code,U32.is_lt(code,2510))
    case False{}: propertiesBelow125(code,U32.is_lt(code,2559))
def propertiesBelow127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow127(code,U32.is_lt(code,2621))
def propertiesBelow129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow129(code,U32.is_lt(code,2627))
def propertiesBelow131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow128(code,U32.is_lt(code,2620))
    case False{}: propertiesBelow130(code,U32.is_lt(code,2625))
def propertiesBelow132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow132(code,U32.is_lt(code,2635))
def propertiesBelow134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow134(code,U32.is_lt(code,2642))
def propertiesBelow136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow133(code,U32.is_lt(code,2633))
    case False{}: propertiesBelow135(code,U32.is_lt(code,2641))
def propertiesBelow137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow131(code,U32.is_lt(code,2622))
    case False{}: propertiesBelow136(code,U32.is_lt(code,2638))
def propertiesBelow138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow126(code,U32.is_lt(code,2530))
    case False{}: propertiesBelow137(code,U32.is_lt(code,2631))
def propertiesBelow139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow115(code,U32.is_lt(code,2392))
    case False{}: propertiesBelow138(code,U32.is_lt(code,2564))
def propertiesBelow140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow140(code,U32.is_lt(code,2677))
def propertiesBelow142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow142(code,U32.is_lt(code,2691))
def propertiesBelow144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow141(code,U32.is_lt(code,2674))
    case False{}: propertiesBelow143(code,U32.is_lt(code,2689))
def propertiesBelow145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow145(code,U32.is_lt(code,2749))
def propertiesBelow147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow147(code,U32.is_lt(code,2758))
def propertiesBelow149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow146(code,U32.is_lt(code,2748))
    case False{}: propertiesBelow148(code,U32.is_lt(code,2753))
def propertiesBelow150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow144(code,U32.is_lt(code,2678))
    case False{}: propertiesBelow149(code,U32.is_lt(code,2750))
def propertiesBelow151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow151(code,U32.is_lt(code,2762))
def propertiesBelow153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow153(code,U32.is_lt(code,2766))
def propertiesBelow155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow152(code,U32.is_lt(code,2761))
    case False{}: propertiesBelow154(code,U32.is_lt(code,2765))
def propertiesBelow156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow156(code,U32.is_lt(code,2810))
def propertiesBelow158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow158(code,U32.is_lt(code,2818))
def propertiesBelow160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow157(code,U32.is_lt(code,2788))
    case False{}: propertiesBelow159(code,U32.is_lt(code,2817))
def propertiesBelow161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow155(code,U32.is_lt(code,2763))
    case False{}: propertiesBelow160(code,U32.is_lt(code,2816))
def propertiesBelow162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow150(code,U32.is_lt(code,2692))
    case False{}: propertiesBelow161(code,U32.is_lt(code,2786))
def propertiesBelow163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow163(code,U32.is_lt(code,2877))
def propertiesBelow165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow165(code,U32.is_lt(code,2880))
def propertiesBelow167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow164(code,U32.is_lt(code,2876))
    case False{}: propertiesBelow166(code,U32.is_lt(code,2879))
def propertiesBelow168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow168(code,U32.is_lt(code,2887))
def propertiesBelow170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow170(code,U32.is_lt(code,2893))
def propertiesBelow172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow169(code,U32.is_lt(code,2885))
    case False{}: propertiesBelow171(code,U32.is_lt(code,2891))
def propertiesBelow173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow167(code,U32.is_lt(code,2878))
    case False{}: propertiesBelow172(code,U32.is_lt(code,2889))
def propertiesBelow174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow174(code,U32.is_lt(code,2903))
def propertiesBelow176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow176(code,U32.is_lt(code,2916))
def propertiesBelow178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow175(code,U32.is_lt(code,2901))
    case False{}: propertiesBelow177(code,U32.is_lt(code,2914))
def propertiesBelow179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow179(code,U32.is_lt(code,3006))
def propertiesBelow181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow181(code,U32.is_lt(code,3011))
def propertiesBelow183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow180(code,U32.is_lt(code,2947))
    case False{}: propertiesBelow182(code,U32.is_lt(code,3009))
def propertiesBelow184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow178(code,U32.is_lt(code,2904))
    case False{}: propertiesBelow183(code,U32.is_lt(code,3008))
def propertiesBelow185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow173(code,U32.is_lt(code,2881))
    case False{}: propertiesBelow184(code,U32.is_lt(code,2946))
def propertiesBelow186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow162(code,U32.is_lt(code,2759))
    case False{}: propertiesBelow185(code,U32.is_lt(code,2894))
def propertiesBelow187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow139(code,U32.is_lt(code,2505))
    case False{}: propertiesBelow186(code,U32.is_lt(code,2820))
def propertiesBelow188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow93(code,U32.is_lt(code,1552))
    case False{}: propertiesBelow187(code,U32.is_lt(code,2672))
def propertiesBelow189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow190(code,U32.is_lt(code,3022))
def propertiesBelow192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow189(code,U32.is_lt(code,3017))
    case False{}: propertiesBelow191(code,U32.is_lt(code,3021))
def propertiesBelow193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow193(code,U32.is_lt(code,3072))
def propertiesBelow195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow195(code,U32.is_lt(code,3077))
def propertiesBelow197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow194(code,U32.is_lt(code,3032))
    case False{}: propertiesBelow196(code,U32.is_lt(code,3076))
def propertiesBelow198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow192(code,U32.is_lt(code,3018))
    case False{}: propertiesBelow197(code,U32.is_lt(code,3073))
def propertiesBelow199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow199(code,U32.is_lt(code,3134))
def propertiesBelow201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow201(code,U32.is_lt(code,3142))
def propertiesBelow203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow200(code,U32.is_lt(code,3133))
    case False{}: propertiesBelow202(code,U32.is_lt(code,3141))
def propertiesBelow204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow204(code,U32.is_lt(code,3150))
def propertiesBelow206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow206(code,U32.is_lt(code,3170))
def propertiesBelow208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow205(code,U32.is_lt(code,3146))
    case False{}: propertiesBelow207(code,U32.is_lt(code,3159))
def propertiesBelow209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow203(code,U32.is_lt(code,3137))
    case False{}: propertiesBelow208(code,U32.is_lt(code,3157))
def propertiesBelow210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow198(code,U32.is_lt(code,3031))
    case False{}: propertiesBelow209(code,U32.is_lt(code,3145))
def propertiesBelow211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow211(code,U32.is_lt(code,3202))
def propertiesBelow213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow213(code,U32.is_lt(code,3261))
def propertiesBelow215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow212(code,U32.is_lt(code,3201))
    case False{}: propertiesBelow214(code,U32.is_lt(code,3260))
def propertiesBelow216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow216(code,U32.is_lt(code,3264))
def propertiesBelow218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow218(code,U32.is_lt(code,3271))
def propertiesBelow220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow217(code,U32.is_lt(code,3263))
    case False{}: propertiesBelow219(code,U32.is_lt(code,3270))
def propertiesBelow221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow215(code,U32.is_lt(code,3204))
    case False{}: propertiesBelow220(code,U32.is_lt(code,3269))
def propertiesBelow222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow222(code,U32.is_lt(code,3276))
def propertiesBelow224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow224(code,U32.is_lt(code,3287))
def propertiesBelow226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow223(code,U32.is_lt(code,3274))
    case False{}: propertiesBelow225(code,U32.is_lt(code,3285))
def propertiesBelow227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow227(code,U32.is_lt(code,3315))
def propertiesBelow229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow229(code,U32.is_lt(code,3330))
def propertiesBelow231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow228(code,U32.is_lt(code,3300))
    case False{}: propertiesBelow230(code,U32.is_lt(code,3328))
def propertiesBelow232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow226(code,U32.is_lt(code,3278))
    case False{}: propertiesBelow231(code,U32.is_lt(code,3316))
def propertiesBelow233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow221(code,U32.is_lt(code,3262))
    case False{}: propertiesBelow232(code,U32.is_lt(code,3298))
def propertiesBelow234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow210(code,U32.is_lt(code,3132))
    case False{}: propertiesBelow233(code,U32.is_lt(code,3273))
def propertiesBelow235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow235(code,U32.is_lt(code,3389))
def propertiesBelow237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow237(code,U32.is_lt(code,3397))
def propertiesBelow239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow236(code,U32.is_lt(code,3387))
    case False{}: propertiesBelow238(code,U32.is_lt(code,3393))
def propertiesBelow240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow240(code,U32.is_lt(code,3402))
def propertiesBelow242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow242(code,U32.is_lt(code,3415))
def propertiesBelow244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow241(code,U32.is_lt(code,3401))
    case False{}: propertiesBelow243(code,U32.is_lt(code,3406))
def propertiesBelow245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow239(code,U32.is_lt(code,3390))
    case False{}: propertiesBelow244(code,U32.is_lt(code,3405))
def propertiesBelow246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow246(code,U32.is_lt(code,3428))
def propertiesBelow248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow248(code,U32.is_lt(code,3460))
def propertiesBelow250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow247(code,U32.is_lt(code,3426))
    case False{}: propertiesBelow249(code,U32.is_lt(code,3458))
def propertiesBelow251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow251(code,U32.is_lt(code,3535))
def propertiesBelow253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow253(code,U32.is_lt(code,3542))
def propertiesBelow255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow252(code,U32.is_lt(code,3531))
    case False{}: propertiesBelow254(code,U32.is_lt(code,3541))
def propertiesBelow256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow250(code,U32.is_lt(code,3457))
    case False{}: propertiesBelow255(code,U32.is_lt(code,3538))
def propertiesBelow257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow245(code,U32.is_lt(code,3398))
    case False{}: propertiesBelow256(code,U32.is_lt(code,3530))
def propertiesBelow258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow258(code,U32.is_lt(code,3552))
def propertiesBelow260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow260(code,U32.is_lt(code,3633))
def propertiesBelow262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow259(code,U32.is_lt(code,3544))
    case False{}: propertiesBelow261(code,U32.is_lt(code,3572))
def propertiesBelow263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow263(code,U32.is_lt(code,3643))
def propertiesBelow265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow265(code,U32.is_lt(code,3761))
def propertiesBelow267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow264(code,U32.is_lt(code,3636))
    case False{}: propertiesBelow266(code,U32.is_lt(code,3663))
def propertiesBelow268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow262(code,U32.is_lt(code,3570))
    case False{}: propertiesBelow267(code,U32.is_lt(code,3655))
def propertiesBelow269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow269(code,U32.is_lt(code,3773))
def propertiesBelow271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow271(code,U32.is_lt(code,3864))
def propertiesBelow273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow270(code,U32.is_lt(code,3764))
    case False{}: propertiesBelow272(code,U32.is_lt(code,3791))
def propertiesBelow274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow274(code,U32.is_lt(code,3894))
def propertiesBelow276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow276(code,U32.is_lt(code,3897))
def propertiesBelow278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow275(code,U32.is_lt(code,3893))
    case False{}: propertiesBelow277(code,U32.is_lt(code,3896))
def propertiesBelow279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow273(code,U32.is_lt(code,3784))
    case False{}: propertiesBelow278(code,U32.is_lt(code,3895))
def propertiesBelow280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow268(code,U32.is_lt(code,3634))
    case False{}: propertiesBelow279(code,U32.is_lt(code,3866))
def propertiesBelow281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow257(code,U32.is_lt(code,3416))
    case False{}: propertiesBelow280(code,U32.is_lt(code,3762))
def propertiesBelow282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow234(code,U32.is_lt(code,3172))
    case False{}: propertiesBelow281(code,U32.is_lt(code,3543))
def propertiesBelow283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow283(code,U32.is_lt(code,3904))
def propertiesBelow285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow285(code,U32.is_lt(code,3968))
def propertiesBelow287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow284(code,U32.is_lt(code,3902))
    case False{}: propertiesBelow286(code,U32.is_lt(code,3967))
def propertiesBelow288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow288(code,U32.is_lt(code,3976))
def propertiesBelow290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow290(code,U32.is_lt(code,3993))
def propertiesBelow292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow289(code,U32.is_lt(code,3974))
    case False{}: propertiesBelow291(code,U32.is_lt(code,3992))
def propertiesBelow293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow287(code,U32.is_lt(code,3953))
    case False{}: propertiesBelow292(code,U32.is_lt(code,3981))
def propertiesBelow294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow294(code,U32.is_lt(code,4039))
def propertiesBelow296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow296(code,U32.is_lt(code,4145))
def propertiesBelow298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow295(code,U32.is_lt(code,4038))
    case False{}: propertiesBelow297(code,U32.is_lt(code,4141))
def propertiesBelow299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow299(code,U32.is_lt(code,4153))
def propertiesBelow301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow301(code,U32.is_lt(code,4159))
def propertiesBelow303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow300(code,U32.is_lt(code,4152))
    case False{}: propertiesBelow302(code,U32.is_lt(code,4157))
def propertiesBelow304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow298(code,U32.is_lt(code,4139))
    case False{}: propertiesBelow303(code,U32.is_lt(code,4155))
def propertiesBelow305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow293(code,U32.is_lt(code,3973))
    case False{}: propertiesBelow304(code,U32.is_lt(code,4146))
def propertiesBelow306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow306(code,U32.is_lt(code,4186))
def propertiesBelow308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow308(code,U32.is_lt(code,4194))
def propertiesBelow310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow307(code,U32.is_lt(code,4184))
    case False{}: propertiesBelow309(code,U32.is_lt(code,4193))
def propertiesBelow311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow311(code,U32.is_lt(code,4206))
def propertiesBelow313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow313(code,U32.is_lt(code,4226))
def propertiesBelow315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow312(code,U32.is_lt(code,4199))
    case False{}: propertiesBelow314(code,U32.is_lt(code,4213))
def propertiesBelow316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow310(code,U32.is_lt(code,4190))
    case False{}: propertiesBelow315(code,U32.is_lt(code,4209))
def propertiesBelow317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow317(code,U32.is_lt(code,4231))
def propertiesBelow319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow319(code,U32.is_lt(code,4239))
def propertiesBelow321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow318(code,U32.is_lt(code,4229))
    case False{}: propertiesBelow320(code,U32.is_lt(code,4238))
def propertiesBelow322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow322(code,U32.is_lt(code,4253))
def propertiesBelow324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 304
def propertiesBelow325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow324(code,U32.is_lt(code,4447))
def propertiesBelow326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow323(code,U32.is_lt(code,4250))
    case False{}: propertiesBelow325(code,U32.is_lt(code,4352))
def propertiesBelow327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow321(code,U32.is_lt(code,4237))
    case False{}: propertiesBelow326(code,U32.is_lt(code,4254))
def propertiesBelow328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow316(code,U32.is_lt(code,4197))
    case False{}: propertiesBelow327(code,U32.is_lt(code,4240))
def propertiesBelow329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow305(code,U32.is_lt(code,4029))
    case False{}: propertiesBelow328(code,U32.is_lt(code,4227))
def propertiesBelow330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: 0
def propertiesBelow331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 272
    case False{}: propertiesBelow330(code,U32.is_lt(code,4608))
def propertiesBelow332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow332(code,U32.is_lt(code,5906))
def propertiesBelow334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow331(code,U32.is_lt(code,4449))
    case False{}: propertiesBelow333(code,U32.is_lt(code,4960))
def propertiesBelow335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow335(code,U32.is_lt(code,5938))
def propertiesBelow337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow337(code,U32.is_lt(code,5970))
def propertiesBelow339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow336(code,U32.is_lt(code,5910))
    case False{}: propertiesBelow338(code,U32.is_lt(code,5941))
def propertiesBelow340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow334(code,U32.is_lt(code,4957))
    case False{}: propertiesBelow339(code,U32.is_lt(code,5940))
def propertiesBelow341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow341(code,U32.is_lt(code,6004))
def propertiesBelow343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 17
    case False{}: propertiesBelow343(code,U32.is_lt(code,6071))
def propertiesBelow345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow342(code,U32.is_lt(code,6002))
    case False{}: propertiesBelow344(code,U32.is_lt(code,6070))
def propertiesBelow346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow346(code,U32.is_lt(code,6087))
def propertiesBelow348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow348(code,U32.is_lt(code,6109))
def propertiesBelow350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow347(code,U32.is_lt(code,6086))
    case False{}: propertiesBelow349(code,U32.is_lt(code,6100))
def propertiesBelow351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow345(code,U32.is_lt(code,6068))
    case False{}: propertiesBelow350(code,U32.is_lt(code,6089))
def propertiesBelow352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow340(code,U32.is_lt(code,5909))
    case False{}: propertiesBelow351(code,U32.is_lt(code,6078))
def propertiesBelow353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 17
    case False{}: 24
def propertiesBelow354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow353(code,U32.is_lt(code,6158))
def propertiesBelow355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 17
    case False{}: propertiesBelow355(code,U32.is_lt(code,6277))
def propertiesBelow357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow354(code,U32.is_lt(code,6155))
    case False{}: propertiesBelow356(code,U32.is_lt(code,6160))
def propertiesBelow358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow358(code,U32.is_lt(code,6314))
def propertiesBelow360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow360(code,U32.is_lt(code,6439))
def propertiesBelow362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow359(code,U32.is_lt(code,6313))
    case False{}: propertiesBelow361(code,U32.is_lt(code,6435))
def propertiesBelow363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow357(code,U32.is_lt(code,6159))
    case False{}: propertiesBelow362(code,U32.is_lt(code,6432))
def propertiesBelow364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow364(code,U32.is_lt(code,6448))
def propertiesBelow366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow366(code,U32.is_lt(code,6457))
def propertiesBelow368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow365(code,U32.is_lt(code,6444))
    case False{}: propertiesBelow367(code,U32.is_lt(code,6451))
def propertiesBelow369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow369(code,U32.is_lt(code,6681))
def propertiesBelow371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow371(code,U32.is_lt(code,6741))
def propertiesBelow373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow370(code,U32.is_lt(code,6679))
    case False{}: propertiesBelow372(code,U32.is_lt(code,6684))
def propertiesBelow374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow368(code,U32.is_lt(code,6450))
    case False{}: propertiesBelow373(code,U32.is_lt(code,6683))
def propertiesBelow375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow363(code,U32.is_lt(code,6279))
    case False{}: propertiesBelow374(code,U32.is_lt(code,6460))
def propertiesBelow376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow352(code,U32.is_lt(code,5972))
    case False{}: propertiesBelow375(code,U32.is_lt(code,6441))
def propertiesBelow377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow329(code,U32.is_lt(code,4182))
    case False{}: propertiesBelow376(code,U32.is_lt(code,6110))
def propertiesBelow378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow282(code,U32.is_lt(code,3332))
    case False{}: propertiesBelow377(code,U32.is_lt(code,4448))
def propertiesBelow379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow188(code,U32.is_lt(code,2362))
    case False{}: propertiesBelow378(code,U32.is_lt(code,3898))
def propertiesBelow380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow381(code,U32.is_lt(code,6752))
def propertiesBelow383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow380(code,U32.is_lt(code,6743))
    case False{}: propertiesBelow382(code,U32.is_lt(code,6751))
def propertiesBelow384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow384(code,U32.is_lt(code,6755))
def propertiesBelow386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow386(code,U32.is_lt(code,6771))
def propertiesBelow388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow385(code,U32.is_lt(code,6754))
    case False{}: propertiesBelow387(code,U32.is_lt(code,6765))
def propertiesBelow389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow383(code,U32.is_lt(code,6744))
    case False{}: propertiesBelow388(code,U32.is_lt(code,6757))
def propertiesBelow390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow390(code,U32.is_lt(code,6784))
def propertiesBelow392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow392(code,U32.is_lt(code,6880))
def propertiesBelow394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow391(code,U32.is_lt(code,6783))
    case False{}: propertiesBelow393(code,U32.is_lt(code,6878))
def propertiesBelow395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow395(code,U32.is_lt(code,6916))
def propertiesBelow397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow397(code,U32.is_lt(code,6965))
def propertiesBelow399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow396(code,U32.is_lt(code,6912))
    case False{}: propertiesBelow398(code,U32.is_lt(code,6964))
def propertiesBelow400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow394(code,U32.is_lt(code,6832))
    case False{}: propertiesBelow399(code,U32.is_lt(code,6917))
def propertiesBelow401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow389(code,U32.is_lt(code,6753))
    case False{}: propertiesBelow400(code,U32.is_lt(code,6892))
def propertiesBelow402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow402(code,U32.is_lt(code,6972))
def propertiesBelow404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow404(code,U32.is_lt(code,6979))
def propertiesBelow406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow403(code,U32.is_lt(code,6971))
    case False{}: propertiesBelow405(code,U32.is_lt(code,6978))
def propertiesBelow407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow407(code,U32.is_lt(code,7028))
def propertiesBelow409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow409(code,U32.is_lt(code,7043))
def propertiesBelow411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow408(code,U32.is_lt(code,7019))
    case False{}: propertiesBelow410(code,U32.is_lt(code,7042))
def propertiesBelow412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow406(code,U32.is_lt(code,6973))
    case False{}: propertiesBelow411(code,U32.is_lt(code,7040))
def propertiesBelow413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow413(code,U32.is_lt(code,7078))
def propertiesBelow415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow415(code,U32.is_lt(code,7083))
def propertiesBelow417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow414(code,U32.is_lt(code,7074))
    case False{}: propertiesBelow416(code,U32.is_lt(code,7082))
def propertiesBelow418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow418(code,U32.is_lt(code,7143))
def propertiesBelow420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow420(code,U32.is_lt(code,7149))
def propertiesBelow422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow419(code,U32.is_lt(code,7142))
    case False{}: propertiesBelow421(code,U32.is_lt(code,7146))
def propertiesBelow423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow417(code,U32.is_lt(code,7080))
    case False{}: propertiesBelow422(code,U32.is_lt(code,7144))
def propertiesBelow424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow412(code,U32.is_lt(code,6981))
    case False{}: propertiesBelow423(code,U32.is_lt(code,7086))
def propertiesBelow425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow401(code,U32.is_lt(code,6781))
    case False{}: propertiesBelow424(code,U32.is_lt(code,7073))
def propertiesBelow426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow426(code,U32.is_lt(code,7154))
def propertiesBelow428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow428(code,U32.is_lt(code,7212))
def propertiesBelow430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow427(code,U32.is_lt(code,7151))
    case False{}: propertiesBelow429(code,U32.is_lt(code,7204))
def propertiesBelow431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow431(code,U32.is_lt(code,7224))
def propertiesBelow433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow433(code,U32.is_lt(code,7380))
def propertiesBelow435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow432(code,U32.is_lt(code,7222))
    case False{}: propertiesBelow434(code,U32.is_lt(code,7379))
def propertiesBelow436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow430(code,U32.is_lt(code,7156))
    case False{}: propertiesBelow435(code,U32.is_lt(code,7376))
def propertiesBelow437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow437(code,U32.is_lt(code,7401))
def propertiesBelow439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow439(code,U32.is_lt(code,7412))
def propertiesBelow441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow438(code,U32.is_lt(code,7394))
    case False{}: propertiesBelow440(code,U32.is_lt(code,7406))
def propertiesBelow442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow442(code,U32.is_lt(code,7416))
def propertiesBelow444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow444(code,U32.is_lt(code,7680))
def propertiesBelow446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow443(code,U32.is_lt(code,7415))
    case False{}: propertiesBelow445(code,U32.is_lt(code,7616))
def propertiesBelow447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow441(code,U32.is_lt(code,7405))
    case False{}: propertiesBelow446(code,U32.is_lt(code,7418))
def propertiesBelow448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow436(code,U32.is_lt(code,7220))
    case False{}: propertiesBelow447(code,U32.is_lt(code,7413))
def propertiesBelow449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 24
def propertiesBelow450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 24
    case False{}: propertiesBelow449(code,U32.is_lt(code,8234))
def propertiesBelow451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow451(code,U32.is_lt(code,8253))
def propertiesBelow453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow450(code,U32.is_lt(code,8208))
    case False{}: propertiesBelow452(code,U32.is_lt(code,8252))
def propertiesBelow454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 24
def propertiesBelow455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow454(code,U32.is_lt(code,8288))
def propertiesBelow456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 24
    case False{}: 0
def propertiesBelow457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: propertiesBelow456(code,U32.is_lt(code,8304))
def propertiesBelow458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow455(code,U32.is_lt(code,8266))
    case False{}: propertiesBelow457(code,U32.is_lt(code,8294))
def propertiesBelow459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow453(code,U32.is_lt(code,8239))
    case False{}: propertiesBelow458(code,U32.is_lt(code,8293))
def propertiesBelow460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow460(code,U32.is_lt(code,8482))
def propertiesBelow462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow462(code,U32.is_lt(code,8506))
def propertiesBelow464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow461(code,U32.is_lt(code,8433))
    case False{}: propertiesBelow463(code,U32.is_lt(code,8505))
def propertiesBelow465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow465(code,U32.is_lt(code,8617))
def propertiesBelow467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow467(code,U32.is_lt(code,8988))
def propertiesBelow469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow466(code,U32.is_lt(code,8602))
    case False{}: propertiesBelow468(code,U32.is_lt(code,8986))
def propertiesBelow470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow464(code,U32.is_lt(code,8483))
    case False{}: propertiesBelow469(code,U32.is_lt(code,8619))
def propertiesBelow471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow459(code,U32.is_lt(code,8265))
    case False{}: propertiesBelow470(code,U32.is_lt(code,8596))
def propertiesBelow472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow448(code,U32.is_lt(code,7393))
    case False{}: propertiesBelow471(code,U32.is_lt(code,8400))
def propertiesBelow473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow425(code,U32.is_lt(code,6966))
    case False{}: propertiesBelow472(code,U32.is_lt(code,8203))
def propertiesBelow474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 32
def propertiesBelow475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow475(code,U32.is_lt(code,9168))
def propertiesBelow477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow474(code,U32.is_lt(code,9001))
    case False{}: propertiesBelow476(code,U32.is_lt(code,9167))
def propertiesBelow478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 96
def propertiesBelow479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow478(code,U32.is_lt(code,9200))
def propertiesBelow480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow480(code,U32.is_lt(code,9204))
def propertiesBelow482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow479(code,U32.is_lt(code,9197))
    case False{}: propertiesBelow481(code,U32.is_lt(code,9203))
def propertiesBelow483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow477(code,U32.is_lt(code,9003))
    case False{}: propertiesBelow482(code,U32.is_lt(code,9201))
def propertiesBelow484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow484(code,U32.is_lt(code,9410))
def propertiesBelow486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow486(code,U32.is_lt(code,9644))
def propertiesBelow488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow485(code,U32.is_lt(code,9211))
    case False{}: propertiesBelow487(code,U32.is_lt(code,9642))
def propertiesBelow489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow489(code,U32.is_lt(code,9664))
def propertiesBelow491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 96
def propertiesBelow492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow491(code,U32.is_lt(code,9725))
def propertiesBelow493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow490(code,U32.is_lt(code,9655))
    case False{}: propertiesBelow492(code,U32.is_lt(code,9723))
def propertiesBelow494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow488(code,U32.is_lt(code,9411))
    case False{}: propertiesBelow493(code,U32.is_lt(code,9665))
def propertiesBelow495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow483(code,U32.is_lt(code,9193))
    case False{}: propertiesBelow494(code,U32.is_lt(code,9654))
def propertiesBelow496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow496(code,U32.is_lt(code,9733))
def propertiesBelow498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow498(code,U32.is_lt(code,9745))
def propertiesBelow500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow497(code,U32.is_lt(code,9728))
    case False{}: propertiesBelow499(code,U32.is_lt(code,9743))
def propertiesBelow501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow501(code,U32.is_lt(code,9750))
def propertiesBelow503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow503(code,U32.is_lt(code,9757))
def propertiesBelow505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow502(code,U32.is_lt(code,9748))
    case False{}: propertiesBelow504(code,U32.is_lt(code,9753))
def propertiesBelow506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow500(code,U32.is_lt(code,9742))
    case False{}: propertiesBelow505(code,U32.is_lt(code,9752))
def propertiesBelow507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow507(code,U32.is_lt(code,9761))
def propertiesBelow509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow509(code,U32.is_lt(code,9766))
def propertiesBelow511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow508(code,U32.is_lt(code,9760))
    case False{}: propertiesBelow510(code,U32.is_lt(code,9764))
def propertiesBelow512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow512(code,U32.is_lt(code,9771))
def propertiesBelow514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 128
def propertiesBelow515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow514(code,U32.is_lt(code,9784))
def propertiesBelow516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow513(code,U32.is_lt(code,9770))
    case False{}: propertiesBelow515(code,U32.is_lt(code,9776))
def propertiesBelow517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow511(code,U32.is_lt(code,9762))
    case False{}: propertiesBelow516(code,U32.is_lt(code,9774))
def propertiesBelow518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow506(code,U32.is_lt(code,9746))
    case False{}: propertiesBelow517(code,U32.is_lt(code,9767))
def propertiesBelow519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow495(code,U32.is_lt(code,9208))
    case False{}: propertiesBelow518(code,U32.is_lt(code,9758))
def propertiesBelow520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow520(code,U32.is_lt(code,9793))
def propertiesBelow522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow523(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow522(code,U32.is_lt(code,9800))
def propertiesBelow524(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow521(code,U32.is_lt(code,9792))
    case False{}: propertiesBelow523(code,U32.is_lt(code,9795))
def propertiesBelow525(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow526(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow525(code,U32.is_lt(code,9825))
def propertiesBelow527(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow528(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow527(code,U32.is_lt(code,9829))
def propertiesBelow529(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow526(code,U32.is_lt(code,9823))
    case False{}: propertiesBelow528(code,U32.is_lt(code,9828))
def propertiesBelow530(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow524(code,U32.is_lt(code,9794))
    case False{}: propertiesBelow529(code,U32.is_lt(code,9827))
def propertiesBelow531(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow532(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow531(code,U32.is_lt(code,9833))
def propertiesBelow533(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow534(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow533(code,U32.is_lt(code,9854))
def propertiesBelow535(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow532(code,U32.is_lt(code,9832))
    case False{}: propertiesBelow534(code,U32.is_lt(code,9852))
def propertiesBelow536(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def propertiesBelow537(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow536(code,U32.is_lt(code,9866))
def propertiesBelow538(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 96
def propertiesBelow539(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow538(code,U32.is_lt(code,9875))
def propertiesBelow540(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow537(code,U32.is_lt(code,9856))
    case False{}: propertiesBelow539(code,U32.is_lt(code,9874))
def propertiesBelow541(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow535(code,U32.is_lt(code,9851))
    case False{}: propertiesBelow540(code,U32.is_lt(code,9872))
def propertiesBelow542(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow530(code,U32.is_lt(code,9812))
    case False{}: propertiesBelow541(code,U32.is_lt(code,9855))
def propertiesBelow543(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow544(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow543(code,U32.is_lt(code,9881))
def propertiesBelow545(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow546(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow545(code,U32.is_lt(code,9885))
def propertiesBelow547(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow544(code,U32.is_lt(code,9880))
    case False{}: propertiesBelow546(code,U32.is_lt(code,9883))
def propertiesBelow548(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow549(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow548(code,U32.is_lt(code,9890))
def propertiesBelow550(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow551(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow550(code,U32.is_lt(code,9898))
def propertiesBelow552(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow549(code,U32.is_lt(code,9889))
    case False{}: propertiesBelow551(code,U32.is_lt(code,9896))
def propertiesBelow553(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow547(code,U32.is_lt(code,9882))
    case False{}: propertiesBelow552(code,U32.is_lt(code,9895))
def propertiesBelow554(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow555(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow554(code,U32.is_lt(code,9906))
def propertiesBelow556(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow557(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow556(code,U32.is_lt(code,9924))
def propertiesBelow558(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow555(code,U32.is_lt(code,9904))
    case False{}: propertiesBelow557(code,U32.is_lt(code,9919))
def propertiesBelow559(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow560(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow559(code,U32.is_lt(code,9929))
def propertiesBelow561(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow562(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow561(code,U32.is_lt(code,9936))
def propertiesBelow563(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow560(code,U32.is_lt(code,9928))
    case False{}: propertiesBelow562(code,U32.is_lt(code,9935))
def propertiesBelow564(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow558(code,U32.is_lt(code,9917))
    case False{}: propertiesBelow563(code,U32.is_lt(code,9934))
def propertiesBelow565(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow553(code,U32.is_lt(code,9888))
    case False{}: propertiesBelow564(code,U32.is_lt(code,9926))
def propertiesBelow566(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow542(code,U32.is_lt(code,9831))
    case False{}: propertiesBelow565(code,U32.is_lt(code,9900))
def propertiesBelow567(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow519(code,U32.is_lt(code,9727))
    case False{}: propertiesBelow566(code,U32.is_lt(code,9876))
def propertiesBelow568(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow473(code,U32.is_lt(code,7150))
    case False{}: propertiesBelow567(code,U32.is_lt(code,9787))
def propertiesBelow569(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow570(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow571(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow570(code,U32.is_lt(code,9941))
def propertiesBelow572(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow569(code,U32.is_lt(code,9938))
    case False{}: propertiesBelow571(code,U32.is_lt(code,9940))
def propertiesBelow573(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow574(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow573(code,U32.is_lt(code,9963))
def propertiesBelow575(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 128
def propertiesBelow576(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow575(code,U32.is_lt(code,9972))
def propertiesBelow577(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow574(code,U32.is_lt(code,9962))
    case False{}: propertiesBelow576(code,U32.is_lt(code,9970))
def propertiesBelow578(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow572(code,U32.is_lt(code,9939))
    case False{}: propertiesBelow577(code,U32.is_lt(code,9968))
def propertiesBelow579(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow580(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow579(code,U32.is_lt(code,9975))
def propertiesBelow581(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow582(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow581(code,U32.is_lt(code,9981))
def propertiesBelow583(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow580(code,U32.is_lt(code,9974))
    case False{}: propertiesBelow582(code,U32.is_lt(code,9979))
def propertiesBelow584(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow585(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow584(code,U32.is_lt(code,9987))
def propertiesBelow586(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow587(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow586(code,U32.is_lt(code,9992))
def propertiesBelow588(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow585(code,U32.is_lt(code,9986))
    case False{}: propertiesBelow587(code,U32.is_lt(code,9990))
def propertiesBelow589(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow583(code,U32.is_lt(code,9978))
    case False{}: propertiesBelow588(code,U32.is_lt(code,9989))
def propertiesBelow590(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow578(code,U32.is_lt(code,9961))
    case False{}: propertiesBelow589(code,U32.is_lt(code,9982))
def propertiesBelow591(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow592(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow591(code,U32.is_lt(code,9998))
def propertiesBelow593(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow594(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow593(code,U32.is_lt(code,10002))
def propertiesBelow595(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow592(code,U32.is_lt(code,9996))
    case False{}: propertiesBelow594(code,U32.is_lt(code,10000))
def propertiesBelow596(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow597(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow596(code,U32.is_lt(code,10005))
def propertiesBelow598(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow599(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow598(code,U32.is_lt(code,10013))
def propertiesBelow600(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow597(code,U32.is_lt(code,10004))
    case False{}: propertiesBelow599(code,U32.is_lt(code,10007))
def propertiesBelow601(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow595(code,U32.is_lt(code,9999))
    case False{}: propertiesBelow600(code,U32.is_lt(code,10006))
def propertiesBelow602(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow603(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow602(code,U32.is_lt(code,10018))
def propertiesBelow604(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow605(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow604(code,U32.is_lt(code,10035))
def propertiesBelow606(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow603(code,U32.is_lt(code,10017))
    case False{}: propertiesBelow605(code,U32.is_lt(code,10025))
def propertiesBelow607(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow608(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow607(code,U32.is_lt(code,10053))
def propertiesBelow609(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow610(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow609(code,U32.is_lt(code,10060))
def propertiesBelow611(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow608(code,U32.is_lt(code,10052))
    case False{}: propertiesBelow610(code,U32.is_lt(code,10056))
def propertiesBelow612(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow606(code,U32.is_lt(code,10024))
    case False{}: propertiesBelow611(code,U32.is_lt(code,10055))
def propertiesBelow613(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow601(code,U32.is_lt(code,10003))
    case False{}: propertiesBelow612(code,U32.is_lt(code,10037))
def propertiesBelow614(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow590(code,U32.is_lt(code,9973))
    case False{}: propertiesBelow613(code,U32.is_lt(code,10014))
def propertiesBelow615(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow616(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow615(code,U32.is_lt(code,10063))
def propertiesBelow617(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow618(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow617(code,U32.is_lt(code,10071))
def propertiesBelow619(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow616(code,U32.is_lt(code,10062))
    case False{}: propertiesBelow618(code,U32.is_lt(code,10070))
def propertiesBelow620(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow621(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow620(code,U32.is_lt(code,10085))
def propertiesBelow622(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow623(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow622(code,U32.is_lt(code,10145))
def propertiesBelow624(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow621(code,U32.is_lt(code,10083))
    case False{}: propertiesBelow623(code,U32.is_lt(code,10136))
def propertiesBelow625(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow619(code,U32.is_lt(code,10067))
    case False{}: propertiesBelow624(code,U32.is_lt(code,10133))
def propertiesBelow626(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow627(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow626(code,U32.is_lt(code,10161))
def propertiesBelow628(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow629(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow628(code,U32.is_lt(code,10548))
def propertiesBelow630(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow627(code,U32.is_lt(code,10160))
    case False{}: propertiesBelow629(code,U32.is_lt(code,10176))
def propertiesBelow631(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow632(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow631(code,U32.is_lt(code,11016))
def propertiesBelow633(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow634(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow633(code,U32.is_lt(code,11088))
def propertiesBelow635(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow632(code,U32.is_lt(code,11013))
    case False{}: propertiesBelow634(code,U32.is_lt(code,11037))
def propertiesBelow636(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow630(code,U32.is_lt(code,10175))
    case False{}: propertiesBelow635(code,U32.is_lt(code,11035))
def propertiesBelow637(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow625(code,U32.is_lt(code,10072))
    case False{}: propertiesBelow636(code,U32.is_lt(code,10550))
def propertiesBelow638(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow639(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow638(code,U32.is_lt(code,11094))
def propertiesBelow640(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow641(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow640(code,U32.is_lt(code,11647))
def propertiesBelow642(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow639(code,U32.is_lt(code,11093))
    case False{}: propertiesBelow641(code,U32.is_lt(code,11506))
def propertiesBelow643(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow644(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow643(code,U32.is_lt(code,11776))
def propertiesBelow645(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow646(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow645(code,U32.is_lt(code,11931))
def propertiesBelow647(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow644(code,U32.is_lt(code,11744))
    case False{}: propertiesBelow646(code,U32.is_lt(code,11930))
def propertiesBelow648(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow642(code,U32.is_lt(code,11503))
    case False{}: propertiesBelow647(code,U32.is_lt(code,11904))
def propertiesBelow649(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow650(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow649(code,U32.is_lt(code,12246))
def propertiesBelow651(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow652(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow651(code,U32.is_lt(code,12289))
def propertiesBelow653(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow650(code,U32.is_lt(code,12032))
    case False{}: propertiesBelow652(code,U32.is_lt(code,12288))
def propertiesBelow654(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow655(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow654(code,U32.is_lt(code,12306))
def propertiesBelow656(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow657(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow656(code,U32.is_lt(code,12321))
def propertiesBelow658(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow655(code,U32.is_lt(code,12293))
    case False{}: propertiesBelow657(code,U32.is_lt(code,12320))
def propertiesBelow659(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow653(code,U32.is_lt(code,12272))
    case False{}: propertiesBelow658(code,U32.is_lt(code,12307))
def propertiesBelow660(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow648(code,U32.is_lt(code,11648))
    case False{}: propertiesBelow659(code,U32.is_lt(code,12292))
def propertiesBelow661(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow637(code,U32.is_lt(code,10146))
    case False{}: propertiesBelow660(code,U32.is_lt(code,12020))
def propertiesBelow662(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow614(code,U32.is_lt(code,9994))
    case False{}: propertiesBelow661(code,U32.is_lt(code,11089))
def propertiesBelow663(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 291
    case False{}: 416
def propertiesBelow664(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 289
    case False{}: propertiesBelow663(code,U32.is_lt(code,12336))
def propertiesBelow665(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow666(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow665(code,U32.is_lt(code,12343))
def propertiesBelow667(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow664(code,U32.is_lt(code,12334))
    case False{}: propertiesBelow666(code,U32.is_lt(code,12342))
def propertiesBelow668(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 256
def propertiesBelow669(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 416
    case False{}: propertiesBelow668(code,U32.is_lt(code,12351))
def propertiesBelow670(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow671(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow670(code,U32.is_lt(code,12439))
def propertiesBelow672(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow669(code,U32.is_lt(code,12350))
    case False{}: propertiesBelow671(code,U32.is_lt(code,12353))
def propertiesBelow673(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow667(code,U32.is_lt(code,12337))
    case False{}: propertiesBelow672(code,U32.is_lt(code,12352))
def propertiesBelow674(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow675(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 289
    case False{}: propertiesBelow674(code,U32.is_lt(code,12544))
def propertiesBelow676(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow677(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow676(code,U32.is_lt(code,12593))
def propertiesBelow678(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow675(code,U32.is_lt(code,12443))
    case False{}: propertiesBelow677(code,U32.is_lt(code,12592))
def propertiesBelow679(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow680(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 304
    case False{}: propertiesBelow679(code,U32.is_lt(code,12687))
def propertiesBelow681(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow682(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow681(code,U32.is_lt(code,12783))
def propertiesBelow683(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow680(code,U32.is_lt(code,12645))
    case False{}: propertiesBelow682(code,U32.is_lt(code,12774))
def propertiesBelow684(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow678(code,U32.is_lt(code,12549))
    case False{}: propertiesBelow683(code,U32.is_lt(code,12688))
def propertiesBelow685(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow673(code,U32.is_lt(code,12349))
    case False{}: propertiesBelow684(code,U32.is_lt(code,12644))
def propertiesBelow686(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow687(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow686(code,U32.is_lt(code,12872))
def propertiesBelow688(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow689(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow688(code,U32.is_lt(code,12927))
def propertiesBelow690(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow687(code,U32.is_lt(code,12832))
    case False{}: propertiesBelow689(code,U32.is_lt(code,12896))
def propertiesBelow691(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 416
    case False{}: 288
def propertiesBelow692(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow691(code,U32.is_lt(code,12952))
def propertiesBelow693(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow694(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 416
    case False{}: propertiesBelow693(code,U32.is_lt(code,12977))
def propertiesBelow695(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow692(code,U32.is_lt(code,12951))
    case False{}: propertiesBelow694(code,U32.is_lt(code,12954))
def propertiesBelow696(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow690(code,U32.is_lt(code,12880))
    case False{}: propertiesBelow695(code,U32.is_lt(code,12953))
def propertiesBelow697(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow698(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow697(code,U32.is_lt(code,13008))
def propertiesBelow699(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow700(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow699(code,U32.is_lt(code,13184))
def propertiesBelow701(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow698(code,U32.is_lt(code,13004))
    case False{}: propertiesBelow700(code,U32.is_lt(code,13179))
def propertiesBelow702(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow703(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow702(code,U32.is_lt(code,13312))
def propertiesBelow704(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow705(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow704(code,U32.is_lt(code,40960))
def propertiesBelow706(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow703(code,U32.is_lt(code,13311))
    case False{}: propertiesBelow705(code,U32.is_lt(code,19968))
def propertiesBelow707(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow701(code,U32.is_lt(code,13169))
    case False{}: propertiesBelow706(code,U32.is_lt(code,19904))
def propertiesBelow708(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow696(code,U32.is_lt(code,12928))
    case False{}: propertiesBelow707(code,U32.is_lt(code,13280))
def propertiesBelow709(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow685(code,U32.is_lt(code,12441))
    case False{}: propertiesBelow708(code,U32.is_lt(code,12992))
def propertiesBelow710(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def propertiesBelow711(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow710(code,U32.is_lt(code,42183))
def propertiesBelow712(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow713(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow712(code,U32.is_lt(code,42612))
def propertiesBelow714(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow711(code,U32.is_lt(code,42128))
    case False{}: propertiesBelow713(code,U32.is_lt(code,42611))
def propertiesBelow715(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow716(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow715(code,U32.is_lt(code,42656))
def propertiesBelow717(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 256
def propertiesBelow718(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow717(code,U32.is_lt(code,42752))
def propertiesBelow719(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow716(code,U32.is_lt(code,42654))
    case False{}: propertiesBelow718(code,U32.is_lt(code,42738))
def propertiesBelow720(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow714(code,U32.is_lt(code,42607))
    case False{}: propertiesBelow719(code,U32.is_lt(code,42736))
def propertiesBelow721(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow722(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow721(code,U32.is_lt(code,43011))
def propertiesBelow723(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow724(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow723(code,U32.is_lt(code,43019))
def propertiesBelow725(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow722(code,U32.is_lt(code,43010))
    case False{}: propertiesBelow724(code,U32.is_lt(code,43015))
def propertiesBelow726(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow727(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow726(code,U32.is_lt(code,43045))
def propertiesBelow728(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow729(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow728(code,U32.is_lt(code,43052))
def propertiesBelow730(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow727(code,U32.is_lt(code,43043))
    case False{}: propertiesBelow729(code,U32.is_lt(code,43048))
def propertiesBelow731(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow725(code,U32.is_lt(code,43014))
    case False{}: propertiesBelow730(code,U32.is_lt(code,43047))
def propertiesBelow732(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow720(code,U32.is_lt(code,42622))
    case False{}: propertiesBelow731(code,U32.is_lt(code,43020))
def propertiesBelow733(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow734(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow733(code,U32.is_lt(code,43138))
def propertiesBelow735(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow736(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow735(code,U32.is_lt(code,43206))
def propertiesBelow737(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow734(code,U32.is_lt(code,43136))
    case False{}: propertiesBelow736(code,U32.is_lt(code,43204))
def propertiesBelow738(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow739(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow738(code,U32.is_lt(code,43263))
def propertiesBelow740(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow741(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow740(code,U32.is_lt(code,43310))
def propertiesBelow742(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow739(code,U32.is_lt(code,43250))
    case False{}: propertiesBelow741(code,U32.is_lt(code,43302))
def propertiesBelow743(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow737(code,U32.is_lt(code,43188))
    case False{}: propertiesBelow742(code,U32.is_lt(code,43264))
def propertiesBelow744(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow745(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow744(code,U32.is_lt(code,43348))
def propertiesBelow746(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow747(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow746(code,U32.is_lt(code,43392))
def propertiesBelow748(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow745(code,U32.is_lt(code,43346))
    case False{}: propertiesBelow747(code,U32.is_lt(code,43389))
def propertiesBelow749(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow750(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow749(code,U32.is_lt(code,43443))
def propertiesBelow751(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow752(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow751(code,U32.is_lt(code,43450))
def propertiesBelow753(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow750(code,U32.is_lt(code,43396))
    case False{}: propertiesBelow752(code,U32.is_lt(code,43446))
def propertiesBelow754(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow748(code,U32.is_lt(code,43360))
    case False{}: propertiesBelow753(code,U32.is_lt(code,43444))
def propertiesBelow755(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow743(code,U32.is_lt(code,43232))
    case False{}: propertiesBelow754(code,U32.is_lt(code,43395))
def propertiesBelow756(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow732(code,U32.is_lt(code,42760))
    case False{}: propertiesBelow755(code,U32.is_lt(code,43335))
def propertiesBelow757(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow709(code,U32.is_lt(code,12831))
    case False{}: propertiesBelow756(code,U32.is_lt(code,43053))
def propertiesBelow758(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow662(code,U32.is_lt(code,10061))
    case False{}: propertiesBelow757(code,U32.is_lt(code,42125))
def propertiesBelow759(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow568(code,U32.is_lt(code,9000))
    case False{}: propertiesBelow758(code,U32.is_lt(code,12330))
def propertiesBelow760(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow379(code,U32.is_lt(code,3014))
    case False{}: propertiesBelow759(code,U32.is_lt(code,9937))
def propertiesBelow761(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow762(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow763(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow762(code,U32.is_lt(code,43494))
def propertiesBelow764(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow761(code,U32.is_lt(code,43454))
    case False{}: propertiesBelow763(code,U32.is_lt(code,43493))
def propertiesBelow765(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow766(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow765(code,U32.is_lt(code,43569))
def propertiesBelow767(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow768(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow767(code,U32.is_lt(code,43575))
def propertiesBelow769(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow766(code,U32.is_lt(code,43567))
    case False{}: propertiesBelow768(code,U32.is_lt(code,43573))
def propertiesBelow770(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow764(code,U32.is_lt(code,43457))
    case False{}: propertiesBelow769(code,U32.is_lt(code,43571))
def propertiesBelow771(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow772(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow771(code,U32.is_lt(code,43596))
def propertiesBelow773(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow774(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow773(code,U32.is_lt(code,43643))
def propertiesBelow775(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow772(code,U32.is_lt(code,43588))
    case False{}: propertiesBelow774(code,U32.is_lt(code,43598))
def propertiesBelow776(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow777(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow776(code,U32.is_lt(code,43646))
def propertiesBelow778(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow779(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow778(code,U32.is_lt(code,43698))
def propertiesBelow780(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow777(code,U32.is_lt(code,43645))
    case False{}: propertiesBelow779(code,U32.is_lt(code,43697))
def propertiesBelow781(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow775(code,U32.is_lt(code,43597))
    case False{}: propertiesBelow780(code,U32.is_lt(code,43696))
def propertiesBelow782(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow770(code,U32.is_lt(code,43561))
    case False{}: propertiesBelow781(code,U32.is_lt(code,43644))
def propertiesBelow783(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow784(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow783(code,U32.is_lt(code,43705))
def propertiesBelow785(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow786(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow785(code,U32.is_lt(code,43713))
def propertiesBelow787(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow784(code,U32.is_lt(code,43703))
    case False{}: propertiesBelow786(code,U32.is_lt(code,43712))
def propertiesBelow788(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow789(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow788(code,U32.is_lt(code,43756))
def propertiesBelow790(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow791(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow790(code,U32.is_lt(code,43765))
def propertiesBelow792(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow789(code,U32.is_lt(code,43755))
    case False{}: propertiesBelow791(code,U32.is_lt(code,43760))
def propertiesBelow793(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow787(code,U32.is_lt(code,43710))
    case False{}: propertiesBelow792(code,U32.is_lt(code,43758))
def propertiesBelow794(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow795(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow794(code,U32.is_lt(code,44003))
def propertiesBelow796(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow797(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow796(code,U32.is_lt(code,44008))
def propertiesBelow798(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow795(code,U32.is_lt(code,43767))
    case False{}: propertiesBelow797(code,U32.is_lt(code,44006))
def propertiesBelow799(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow800(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow799(code,U32.is_lt(code,44012))
def propertiesBelow801(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow802(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow801(code,U32.is_lt(code,44032))
def propertiesBelow803(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow800(code,U32.is_lt(code,44011))
    case False{}: propertiesBelow802(code,U32.is_lt(code,44014))
def propertiesBelow804(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow798(code,U32.is_lt(code,44005))
    case False{}: propertiesBelow803(code,U32.is_lt(code,44013))
def propertiesBelow805(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow793(code,U32.is_lt(code,43714))
    case False{}: propertiesBelow804(code,U32.is_lt(code,44009))
def propertiesBelow806(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow782(code,U32.is_lt(code,43587))
    case False{}: propertiesBelow805(code,U32.is_lt(code,43766))
def propertiesBelow807(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: 0
def propertiesBelow808(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow807(code,U32.is_lt(code,55239))
def propertiesBelow809(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 4
def propertiesBelow810(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: propertiesBelow809(code,U32.is_lt(code,55296))
def propertiesBelow811(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow808(code,U32.is_lt(code,55216))
    case False{}: propertiesBelow810(code,U32.is_lt(code,55292))
def propertiesBelow812(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow813(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow812(code,U32.is_lt(code,64110))
def propertiesBelow814(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def propertiesBelow815(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow814(code,U32.is_lt(code,64256))
def propertiesBelow816(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow813(code,U32.is_lt(code,63744))
    case False{}: propertiesBelow815(code,U32.is_lt(code,64218))
def propertiesBelow817(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow811(code,U32.is_lt(code,55243))
    case False{}: propertiesBelow816(code,U32.is_lt(code,64112))
def propertiesBelow818(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 17
def propertiesBelow819(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow818(code,U32.is_lt(code,65024))
def propertiesBelow820(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow821(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow820(code,U32.is_lt(code,65056))
def propertiesBelow822(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow819(code,U32.is_lt(code,64287))
    case False{}: propertiesBelow821(code,U32.is_lt(code,65050))
def propertiesBelow823(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow824(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow823(code,U32.is_lt(code,65095))
def propertiesBelow825(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def propertiesBelow826(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow825(code,U32.is_lt(code,65127))
def propertiesBelow827(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow824(code,U32.is_lt(code,65093))
    case False{}: propertiesBelow826(code,U32.is_lt(code,65108))
def propertiesBelow828(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow822(code,U32.is_lt(code,65040))
    case False{}: propertiesBelow827(code,U32.is_lt(code,65107))
def propertiesBelow829(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow817(code,U32.is_lt(code,57344))
    case False{}: propertiesBelow828(code,U32.is_lt(code,65072))
def propertiesBelow830(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 24
def propertiesBelow831(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow830(code,U32.is_lt(code,65279))
def propertiesBelow832(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 256
def propertiesBelow833(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow832(code,U32.is_lt(code,65377))
def propertiesBelow834(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow831(code,U32.is_lt(code,65132))
    case False{}: propertiesBelow833(code,U32.is_lt(code,65281))
def propertiesBelow835(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: 0
def propertiesBelow836(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 272
    case False{}: propertiesBelow835(code,U32.is_lt(code,65471))
def propertiesBelow837(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 256
def propertiesBelow838(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: propertiesBelow837(code,U32.is_lt(code,65482))
def propertiesBelow839(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow836(code,U32.is_lt(code,65441))
    case False{}: propertiesBelow838(code,U32.is_lt(code,65480))
def propertiesBelow840(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow834(code,U32.is_lt(code,65280))
    case False{}: propertiesBelow839(code,U32.is_lt(code,65474))
def propertiesBelow841(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: 0
def propertiesBelow842(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow841(code,U32.is_lt(code,65496))
def propertiesBelow843(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def propertiesBelow844(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 256
    case False{}: propertiesBelow843(code,U32.is_lt(code,65504))
def propertiesBelow845(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow842(code,U32.is_lt(code,65490))
    case False{}: propertiesBelow844(code,U32.is_lt(code,65501))
def propertiesBelow846(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 8
def propertiesBelow847(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow846(code,U32.is_lt(code,65529))
def propertiesBelow848(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow849(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow848(code,U32.is_lt(code,66046))
def propertiesBelow850(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow847(code,U32.is_lt(code,65520))
    case False{}: propertiesBelow849(code,U32.is_lt(code,66045))
def propertiesBelow851(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow845(code,U32.is_lt(code,65498))
    case False{}: propertiesBelow850(code,U32.is_lt(code,65532))
def propertiesBelow852(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow840(code,U32.is_lt(code,65440))
    case False{}: propertiesBelow851(code,U32.is_lt(code,65511))
def propertiesBelow853(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow829(code,U32.is_lt(code,64286))
    case False{}: propertiesBelow852(code,U32.is_lt(code,65488))
def propertiesBelow854(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow806(code,U32.is_lt(code,43701))
    case False{}: propertiesBelow853(code,U32.is_lt(code,65128))
def propertiesBelow855(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow856(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow857(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow856(code,U32.is_lt(code,68097))
def propertiesBelow858(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow855(code,U32.is_lt(code,66273))
    case False{}: propertiesBelow857(code,U32.is_lt(code,66427))
def propertiesBelow859(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow860(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow859(code,U32.is_lt(code,68103))
def propertiesBelow861(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow862(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow861(code,U32.is_lt(code,68152))
def propertiesBelow863(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow860(code,U32.is_lt(code,68101))
    case False{}: propertiesBelow862(code,U32.is_lt(code,68112))
def propertiesBelow864(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow858(code,U32.is_lt(code,66422))
    case False{}: propertiesBelow863(code,U32.is_lt(code,68108))
def propertiesBelow865(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow866(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow865(code,U32.is_lt(code,68160))
def propertiesBelow867(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow868(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow867(code,U32.is_lt(code,68900))
def propertiesBelow869(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow866(code,U32.is_lt(code,68159))
    case False{}: propertiesBelow868(code,U32.is_lt(code,68327))
def propertiesBelow870(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow871(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow870(code,U32.is_lt(code,68974))
def propertiesBelow872(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow873(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow872(code,U32.is_lt(code,69370))
def propertiesBelow874(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow871(code,U32.is_lt(code,68969))
    case False{}: propertiesBelow873(code,U32.is_lt(code,69293))
def propertiesBelow875(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow869(code,U32.is_lt(code,68325))
    case False{}: propertiesBelow874(code,U32.is_lt(code,69291))
def propertiesBelow876(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow864(code,U32.is_lt(code,68100))
    case False{}: propertiesBelow875(code,U32.is_lt(code,68904))
def propertiesBelow877(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow878(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow877(code,U32.is_lt(code,69457))
def propertiesBelow879(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow880(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow879(code,U32.is_lt(code,69632))
def propertiesBelow881(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow878(code,U32.is_lt(code,69446))
    case False{}: propertiesBelow880(code,U32.is_lt(code,69510))
def propertiesBelow882(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow883(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow882(code,U32.is_lt(code,69635))
def propertiesBelow884(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow885(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow884(code,U32.is_lt(code,69744))
def propertiesBelow886(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow883(code,U32.is_lt(code,69634))
    case False{}: propertiesBelow885(code,U32.is_lt(code,69703))
def propertiesBelow887(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow881(code,U32.is_lt(code,69506))
    case False{}: propertiesBelow886(code,U32.is_lt(code,69688))
def propertiesBelow888(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow889(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow888(code,U32.is_lt(code,69749))
def propertiesBelow890(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow891(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow890(code,U32.is_lt(code,69763))
def propertiesBelow892(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow889(code,U32.is_lt(code,69747))
    case False{}: propertiesBelow891(code,U32.is_lt(code,69762))
def propertiesBelow893(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow894(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow893(code,U32.is_lt(code,69815))
def propertiesBelow895(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def propertiesBelow896(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow895(code,U32.is_lt(code,69821))
def propertiesBelow897(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow894(code,U32.is_lt(code,69811))
    case False{}: propertiesBelow896(code,U32.is_lt(code,69819))
def propertiesBelow898(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow892(code,U32.is_lt(code,69759))
    case False{}: propertiesBelow897(code,U32.is_lt(code,69817))
def propertiesBelow899(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow887(code,U32.is_lt(code,69633))
    case False{}: propertiesBelow898(code,U32.is_lt(code,69808))
def propertiesBelow900(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow876(code,U32.is_lt(code,68155))
    case False{}: propertiesBelow899(code,U32.is_lt(code,69745))
def propertiesBelow901(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow902(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow901(code,U32.is_lt(code,69827))
def propertiesBelow903(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow904(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: propertiesBelow903(code,U32.is_lt(code,69888))
def propertiesBelow905(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow902(code,U32.is_lt(code,69826))
    case False{}: propertiesBelow904(code,U32.is_lt(code,69838))
def propertiesBelow906(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow907(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow906(code,U32.is_lt(code,69932))
def propertiesBelow908(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow909(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow908(code,U32.is_lt(code,69957))
def propertiesBelow910(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow907(code,U32.is_lt(code,69927))
    case False{}: propertiesBelow909(code,U32.is_lt(code,69941))
def propertiesBelow911(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow905(code,U32.is_lt(code,69837))
    case False{}: propertiesBelow910(code,U32.is_lt(code,69933))
def propertiesBelow912(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow913(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow912(code,U32.is_lt(code,70004))
def propertiesBelow914(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow915(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow914(code,U32.is_lt(code,70019))
def propertiesBelow916(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow913(code,U32.is_lt(code,70003))
    case False{}: propertiesBelow915(code,U32.is_lt(code,70018))
def propertiesBelow917(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow918(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow917(code,U32.is_lt(code,70079))
def propertiesBelow919(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow920(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow919(code,U32.is_lt(code,70093))
def propertiesBelow921(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow918(code,U32.is_lt(code,70070))
    case False{}: propertiesBelow920(code,U32.is_lt(code,70089))
def propertiesBelow922(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow916(code,U32.is_lt(code,70016))
    case False{}: propertiesBelow921(code,U32.is_lt(code,70081))
def propertiesBelow923(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow911(code,U32.is_lt(code,69891))
    case False{}: propertiesBelow922(code,U32.is_lt(code,70067))
def propertiesBelow924(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow925(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow924(code,U32.is_lt(code,70096))
def propertiesBelow926(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow927(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow926(code,U32.is_lt(code,70194))
def propertiesBelow928(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow925(code,U32.is_lt(code,70095))
    case False{}: propertiesBelow927(code,U32.is_lt(code,70191))
def propertiesBelow929(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow930(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow929(code,U32.is_lt(code,70198))
def propertiesBelow931(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow932(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow931(code,U32.is_lt(code,70207))
def propertiesBelow933(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow930(code,U32.is_lt(code,70197))
    case False{}: propertiesBelow932(code,U32.is_lt(code,70206))
def propertiesBelow934(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow928(code,U32.is_lt(code,70188))
    case False{}: propertiesBelow933(code,U32.is_lt(code,70200))
def propertiesBelow935(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow936(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow935(code,U32.is_lt(code,70367))
def propertiesBelow937(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow938(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow937(code,U32.is_lt(code,70379))
def propertiesBelow939(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow936(code,U32.is_lt(code,70210))
    case False{}: propertiesBelow938(code,U32.is_lt(code,70371))
def propertiesBelow940(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow941(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow940(code,U32.is_lt(code,70404))
def propertiesBelow942(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow943(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow942(code,U32.is_lt(code,70462))
def propertiesBelow944(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow941(code,U32.is_lt(code,70402))
    case False{}: propertiesBelow943(code,U32.is_lt(code,70461))
def propertiesBelow945(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow939(code,U32.is_lt(code,70368))
    case False{}: propertiesBelow944(code,U32.is_lt(code,70459))
def propertiesBelow946(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow934(code,U32.is_lt(code,70196))
    case False{}: propertiesBelow945(code,U32.is_lt(code,70400))
def propertiesBelow947(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow923(code,U32.is_lt(code,69959))
    case False{}: propertiesBelow946(code,U32.is_lt(code,70209))
def propertiesBelow948(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow900(code,U32.is_lt(code,69376))
    case False{}: propertiesBelow947(code,U32.is_lt(code,70094))
def propertiesBelow949(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow854(code,U32.is_lt(code,55204))
    case False{}: propertiesBelow948(code,U32.is_lt(code,69822))
def propertiesBelow950(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow951(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow952(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow951(code,U32.is_lt(code,70473))
def propertiesBelow953(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow950(code,U32.is_lt(code,70465))
    case False{}: propertiesBelow952(code,U32.is_lt(code,70471))
def propertiesBelow954(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow955(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow954(code,U32.is_lt(code,70487))
def propertiesBelow956(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow957(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow956(code,U32.is_lt(code,70500))
def propertiesBelow958(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow955(code,U32.is_lt(code,70478))
    case False{}: propertiesBelow957(code,U32.is_lt(code,70498))
def propertiesBelow959(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow953(code,U32.is_lt(code,70469))
    case False{}: propertiesBelow958(code,U32.is_lt(code,70488))
def propertiesBelow960(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow961(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow960(code,U32.is_lt(code,70512))
def propertiesBelow962(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow963(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow962(code,U32.is_lt(code,70587))
def propertiesBelow964(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow961(code,U32.is_lt(code,70509))
    case False{}: propertiesBelow963(code,U32.is_lt(code,70584))
def propertiesBelow965(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow966(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow965(code,U32.is_lt(code,70595))
def propertiesBelow967(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow968(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow967(code,U32.is_lt(code,70599))
def propertiesBelow969(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow966(code,U32.is_lt(code,70594))
    case False{}: propertiesBelow968(code,U32.is_lt(code,70598))
def propertiesBelow970(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow964(code,U32.is_lt(code,70517))
    case False{}: propertiesBelow969(code,U32.is_lt(code,70597))
def propertiesBelow971(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow959(code,U32.is_lt(code,70475))
    case False{}: propertiesBelow970(code,U32.is_lt(code,70593))
def propertiesBelow972(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow973(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow972(code,U32.is_lt(code,70606))
def propertiesBelow974(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow975(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow974(code,U32.is_lt(code,70609))
def propertiesBelow976(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow973(code,U32.is_lt(code,70604))
    case False{}: propertiesBelow975(code,U32.is_lt(code,70608))
def propertiesBelow977(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow978(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow977(code,U32.is_lt(code,70625))
def propertiesBelow979(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow980(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow979(code,U32.is_lt(code,70712))
def propertiesBelow981(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow978(code,U32.is_lt(code,70611))
    case False{}: propertiesBelow980(code,U32.is_lt(code,70709))
def propertiesBelow982(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow976(code,U32.is_lt(code,70607))
    case False{}: propertiesBelow981(code,U32.is_lt(code,70627))
def propertiesBelow983(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow984(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow983(code,U32.is_lt(code,70725))
def propertiesBelow985(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow986(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow985(code,U32.is_lt(code,70750))
def propertiesBelow987(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow984(code,U32.is_lt(code,70722))
    case False{}: propertiesBelow986(code,U32.is_lt(code,70727))
def propertiesBelow988(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow989(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow988(code,U32.is_lt(code,70835))
def propertiesBelow990(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow991(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow990(code,U32.is_lt(code,70843))
def propertiesBelow992(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow989(code,U32.is_lt(code,70832))
    case False{}: propertiesBelow991(code,U32.is_lt(code,70842))
def propertiesBelow993(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow987(code,U32.is_lt(code,70726))
    case False{}: propertiesBelow992(code,U32.is_lt(code,70841))
def propertiesBelow994(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow982(code,U32.is_lt(code,70610))
    case False{}: propertiesBelow993(code,U32.is_lt(code,70751))
def propertiesBelow995(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow971(code,U32.is_lt(code,70502))
    case False{}: propertiesBelow994(code,U32.is_lt(code,70720))
def propertiesBelow996(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow997(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow996(code,U32.is_lt(code,70850))
def propertiesBelow998(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow999(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow998(code,U32.is_lt(code,71090))
def propertiesBelow1000(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow997(code,U32.is_lt(code,70849))
    case False{}: propertiesBelow999(code,U32.is_lt(code,71087))
def propertiesBelow1001(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1002(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1001(code,U32.is_lt(code,71100))
def propertiesBelow1003(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1004(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1003(code,U32.is_lt(code,71105))
def propertiesBelow1005(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1002(code,U32.is_lt(code,71096))
    case False{}: propertiesBelow1004(code,U32.is_lt(code,71103))
def propertiesBelow1006(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1000(code,U32.is_lt(code,70852))
    case False{}: propertiesBelow1005(code,U32.is_lt(code,71102))
def propertiesBelow1007(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow1008(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1007(code,U32.is_lt(code,71216))
def propertiesBelow1009(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1010(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1009(code,U32.is_lt(code,71229))
def propertiesBelow1011(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1008(code,U32.is_lt(code,71134))
    case False{}: propertiesBelow1010(code,U32.is_lt(code,71227))
def propertiesBelow1012(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1013(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1012(code,U32.is_lt(code,71233))
def propertiesBelow1014(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1015(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1014(code,U32.is_lt(code,71341))
def propertiesBelow1016(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1013(code,U32.is_lt(code,71231))
    case False{}: propertiesBelow1015(code,U32.is_lt(code,71340))
def propertiesBelow1017(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1011(code,U32.is_lt(code,71219))
    case False{}: propertiesBelow1016(code,U32.is_lt(code,71339))
def propertiesBelow1018(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1006(code,U32.is_lt(code,71094))
    case False{}: propertiesBelow1017(code,U32.is_lt(code,71230))
def propertiesBelow1019(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1020(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1019(code,U32.is_lt(code,71350))
def propertiesBelow1021(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1022(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1021(code,U32.is_lt(code,71453))
def propertiesBelow1023(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1020(code,U32.is_lt(code,71344))
    case False{}: propertiesBelow1022(code,U32.is_lt(code,71352))
def propertiesBelow1024(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1025(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1024(code,U32.is_lt(code,71456))
def propertiesBelow1026(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1027(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1026(code,U32.is_lt(code,71463))
def propertiesBelow1028(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1025(code,U32.is_lt(code,71455))
    case False{}: propertiesBelow1027(code,U32.is_lt(code,71462))
def propertiesBelow1029(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1023(code,U32.is_lt(code,71351))
    case False{}: propertiesBelow1028(code,U32.is_lt(code,71458))
def propertiesBelow1030(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1031(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1030(code,U32.is_lt(code,71727))
def propertiesBelow1032(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1033(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1032(code,U32.is_lt(code,71739))
def propertiesBelow1034(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1031(code,U32.is_lt(code,71724))
    case False{}: propertiesBelow1033(code,U32.is_lt(code,71737))
def propertiesBelow1035(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow1036(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1035(code,U32.is_lt(code,71991))
def propertiesBelow1037(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1038(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1037(code,U32.is_lt(code,71997))
def propertiesBelow1039(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1036(code,U32.is_lt(code,71990))
    case False{}: propertiesBelow1038(code,U32.is_lt(code,71995))
def propertiesBelow1040(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1034(code,U32.is_lt(code,71736))
    case False{}: propertiesBelow1039(code,U32.is_lt(code,71993))
def propertiesBelow1041(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1029(code,U32.is_lt(code,71454))
    case False{}: propertiesBelow1040(code,U32.is_lt(code,71984))
def propertiesBelow1042(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1018(code,U32.is_lt(code,71132))
    case False{}: propertiesBelow1041(code,U32.is_lt(code,71468))
def propertiesBelow1043(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow995(code,U32.is_lt(code,70603))
    case False{}: propertiesBelow1042(code,U32.is_lt(code,71342))
def propertiesBelow1044(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow1045(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1044(code,U32.is_lt(code,72000))
def propertiesBelow1046(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1047(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1046(code,U32.is_lt(code,72003))
def propertiesBelow1048(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1045(code,U32.is_lt(code,71999))
    case False{}: propertiesBelow1047(code,U32.is_lt(code,72002))
def propertiesBelow1049(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1050(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1049(code,U32.is_lt(code,72148))
def propertiesBelow1051(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1052(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1051(code,U32.is_lt(code,72156))
def propertiesBelow1053(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1050(code,U32.is_lt(code,72145))
    case False{}: propertiesBelow1052(code,U32.is_lt(code,72154))
def propertiesBelow1054(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1048(code,U32.is_lt(code,72001))
    case False{}: propertiesBelow1053(code,U32.is_lt(code,72152))
def propertiesBelow1055(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow1056(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1055(code,U32.is_lt(code,72164))
def propertiesBelow1057(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1058(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1057(code,U32.is_lt(code,72203))
def propertiesBelow1059(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1056(code,U32.is_lt(code,72161))
    case False{}: propertiesBelow1058(code,U32.is_lt(code,72193))
def propertiesBelow1060(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow1061(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1060(code,U32.is_lt(code,72250))
def propertiesBelow1062(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1063(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1062(code,U32.is_lt(code,72263))
def propertiesBelow1064(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1061(code,U32.is_lt(code,72249))
    case False{}: propertiesBelow1063(code,U32.is_lt(code,72255))
def propertiesBelow1065(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1059(code,U32.is_lt(code,72165))
    case False{}: propertiesBelow1064(code,U32.is_lt(code,72251))
def propertiesBelow1066(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1054(code,U32.is_lt(code,72004))
    case False{}: propertiesBelow1065(code,U32.is_lt(code,72243))
def propertiesBelow1067(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1068(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1067(code,U32.is_lt(code,72279))
def propertiesBelow1069(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1070(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1069(code,U32.is_lt(code,72330))
def propertiesBelow1071(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1068(code,U32.is_lt(code,72273))
    case False{}: propertiesBelow1070(code,U32.is_lt(code,72284))
def propertiesBelow1072(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1073(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1072(code,U32.is_lt(code,72346))
def propertiesBelow1074(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1075(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1074(code,U32.is_lt(code,72546))
def propertiesBelow1076(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1073(code,U32.is_lt(code,72344))
    case False{}: propertiesBelow1075(code,U32.is_lt(code,72545))
def propertiesBelow1077(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1071(code,U32.is_lt(code,72281))
    case False{}: propertiesBelow1076(code,U32.is_lt(code,72544))
def propertiesBelow1078(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1079(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1078(code,U32.is_lt(code,72551))
def propertiesBelow1080(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1081(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1080(code,U32.is_lt(code,72752))
def propertiesBelow1082(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1079(code,U32.is_lt(code,72550))
    case False{}: propertiesBelow1081(code,U32.is_lt(code,72751))
def propertiesBelow1083(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1084(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1083(code,U32.is_lt(code,72766))
def propertiesBelow1085(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1086(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1085(code,U32.is_lt(code,72850))
def propertiesBelow1087(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1084(code,U32.is_lt(code,72760))
    case False{}: propertiesBelow1086(code,U32.is_lt(code,72768))
def propertiesBelow1088(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1082(code,U32.is_lt(code,72552))
    case False{}: propertiesBelow1087(code,U32.is_lt(code,72767))
def propertiesBelow1089(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1077(code,U32.is_lt(code,72343))
    case False{}: propertiesBelow1088(code,U32.is_lt(code,72759))
def propertiesBelow1090(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1066(code,U32.is_lt(code,72160))
    case False{}: propertiesBelow1089(code,U32.is_lt(code,72549))
def propertiesBelow1091(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1092(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1091(code,U32.is_lt(code,72874))
def propertiesBelow1093(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 3
def propertiesBelow1094(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1093(code,U32.is_lt(code,72884))
def propertiesBelow1095(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1092(code,U32.is_lt(code,72873))
    case False{}: propertiesBelow1094(code,U32.is_lt(code,72882))
def propertiesBelow1096(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1097(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1096(code,U32.is_lt(code,73009))
def propertiesBelow1098(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1099(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1098(code,U32.is_lt(code,73019))
def propertiesBelow1100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1097(code,U32.is_lt(code,72887))
    case False{}: propertiesBelow1099(code,U32.is_lt(code,73018))
def propertiesBelow1101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1095(code,U32.is_lt(code,72881))
    case False{}: propertiesBelow1100(code,U32.is_lt(code,73015))
def propertiesBelow1102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1102(code,U32.is_lt(code,73023))
def propertiesBelow1104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1104(code,U32.is_lt(code,73032))
def propertiesBelow1106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1103(code,U32.is_lt(code,73022))
    case False{}: propertiesBelow1105(code,U32.is_lt(code,73031))
def propertiesBelow1107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1107(code,U32.is_lt(code,73104))
def propertiesBelow1109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1109(code,U32.is_lt(code,73109))
def propertiesBelow1111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1108(code,U32.is_lt(code,73103))
    case False{}: propertiesBelow1110(code,U32.is_lt(code,73107))
def propertiesBelow1112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1106(code,U32.is_lt(code,73030))
    case False{}: propertiesBelow1111(code,U32.is_lt(code,73106))
def propertiesBelow1113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1101(code,U32.is_lt(code,72885))
    case False{}: propertiesBelow1112(code,U32.is_lt(code,73098))
def propertiesBelow1114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1114(code,U32.is_lt(code,73112))
def propertiesBelow1116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def propertiesBelow1117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1116(code,U32.is_lt(code,73463))
def propertiesBelow1118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1115(code,U32.is_lt(code,73111))
    case False{}: propertiesBelow1117(code,U32.is_lt(code,73461))
def propertiesBelow1119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def propertiesBelow1120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1119(code,U32.is_lt(code,73475))
def propertiesBelow1121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1121(code,U32.is_lt(code,73526))
def propertiesBelow1123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1120(code,U32.is_lt(code,73474))
    case False{}: propertiesBelow1122(code,U32.is_lt(code,73524))
def propertiesBelow1124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1118(code,U32.is_lt(code,73459))
    case False{}: propertiesBelow1123(code,U32.is_lt(code,73476))
def propertiesBelow1125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1125(code,U32.is_lt(code,73536))
def propertiesBelow1127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1127(code,U32.is_lt(code,73539))
def propertiesBelow1129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1126(code,U32.is_lt(code,73534))
    case False{}: propertiesBelow1128(code,U32.is_lt(code,73538))
def propertiesBelow1130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def propertiesBelow1131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1130(code,U32.is_lt(code,78896))
def propertiesBelow1132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1132(code,U32.is_lt(code,78919))
def propertiesBelow1134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1131(code,U32.is_lt(code,73563))
    case False{}: propertiesBelow1133(code,U32.is_lt(code,78913))
def propertiesBelow1135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1129(code,U32.is_lt(code,73537))
    case False{}: propertiesBelow1134(code,U32.is_lt(code,78912))
def propertiesBelow1136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1124(code,U32.is_lt(code,73472))
    case False{}: propertiesBelow1135(code,U32.is_lt(code,73562))
def propertiesBelow1137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1113(code,U32.is_lt(code,73020))
    case False{}: propertiesBelow1136(code,U32.is_lt(code,73531))
def propertiesBelow1138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1090(code,U32.is_lt(code,72264))
    case False{}: propertiesBelow1137(code,U32.is_lt(code,73110))
def propertiesBelow1139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1043(code,U32.is_lt(code,70847))
    case False{}: propertiesBelow1138(code,U32.is_lt(code,72872))
def propertiesBelow1140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow949(code,U32.is_lt(code,66272))
    case False{}: propertiesBelow1139(code,U32.is_lt(code,71998))
def propertiesBelow1141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1142(code,U32.is_lt(code,90416))
def propertiesBelow1144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1141(code,U32.is_lt(code,90398))
    case False{}: propertiesBelow1143(code,U32.is_lt(code,90413))
def propertiesBelow1145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1145(code,U32.is_lt(code,92976))
def propertiesBelow1147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1147(code,U32.is_lt(code,94032))
def propertiesBelow1149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1146(code,U32.is_lt(code,92917))
    case False{}: propertiesBelow1148(code,U32.is_lt(code,94031))
def propertiesBelow1150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1144(code,U32.is_lt(code,90410))
    case False{}: propertiesBelow1149(code,U32.is_lt(code,92983))
def propertiesBelow1151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: propertiesBelow1151(code,U32.is_lt(code,94095))
def propertiesBelow1153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow1154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1153(code,U32.is_lt(code,94178))
def propertiesBelow1155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1152(code,U32.is_lt(code,94088))
    case False{}: propertiesBelow1154(code,U32.is_lt(code,94176))
def propertiesBelow1156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 291
def propertiesBelow1157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 33
    case False{}: propertiesBelow1156(code,U32.is_lt(code,94192))
def propertiesBelow1158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def propertiesBelow1159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow1158(code,U32.is_lt(code,94208))
def propertiesBelow1160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1157(code,U32.is_lt(code,94181))
    case False{}: propertiesBelow1159(code,U32.is_lt(code,94199))
def propertiesBelow1161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1155(code,U32.is_lt(code,94099))
    case False{}: propertiesBelow1160(code,U32.is_lt(code,94194))
def propertiesBelow1162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1150(code,U32.is_lt(code,92912))
    case False{}: propertiesBelow1161(code,U32.is_lt(code,94180))
def propertiesBelow1163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def propertiesBelow1164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1163(code,U32.is_lt(code,101663))
def propertiesBelow1165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow1166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1165(code,U32.is_lt(code,110576))
def propertiesBelow1167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1164(code,U32.is_lt(code,101631))
    case False{}: propertiesBelow1166(code,U32.is_lt(code,101875))
def propertiesBelow1168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow1169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1168(code,U32.is_lt(code,110588))
def propertiesBelow1170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow1171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow1170(code,U32.is_lt(code,110592))
def propertiesBelow1172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1169(code,U32.is_lt(code,110581))
    case False{}: propertiesBelow1171(code,U32.is_lt(code,110591))
def propertiesBelow1173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1167(code,U32.is_lt(code,101760))
    case False{}: propertiesBelow1172(code,U32.is_lt(code,110589))
def propertiesBelow1174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow1175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1174(code,U32.is_lt(code,110899))
def propertiesBelow1176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow1177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow1176(code,U32.is_lt(code,110933))
def propertiesBelow1178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1175(code,U32.is_lt(code,110898))
    case False{}: propertiesBelow1177(code,U32.is_lt(code,110931))
def propertiesBelow1179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 0
def propertiesBelow1180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1179(code,U32.is_lt(code,110952))
def propertiesBelow1181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1181(code,U32.is_lt(code,113821))
def propertiesBelow1183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1180(code,U32.is_lt(code,110948))
    case False{}: propertiesBelow1182(code,U32.is_lt(code,111356))
def propertiesBelow1184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1178(code,U32.is_lt(code,110928))
    case False{}: propertiesBelow1183(code,U32.is_lt(code,110960))
def propertiesBelow1185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1173(code,U32.is_lt(code,110580))
    case False{}: propertiesBelow1184(code,U32.is_lt(code,110934))
def propertiesBelow1186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1162(code,U32.is_lt(code,94033))
    case False{}: propertiesBelow1185(code,U32.is_lt(code,110883))
def propertiesBelow1187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 24
    case False{}: 0
def propertiesBelow1188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1187(code,U32.is_lt(code,113828))
def propertiesBelow1189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1189(code,U32.is_lt(code,118576))
def propertiesBelow1191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1188(code,U32.is_lt(code,113824))
    case False{}: propertiesBelow1190(code,U32.is_lt(code,118574))
def propertiesBelow1192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 1
def propertiesBelow1193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1192(code,U32.is_lt(code,119143))
def propertiesBelow1194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 24
def propertiesBelow1195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1194(code,U32.is_lt(code,119155))
def propertiesBelow1196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1193(code,U32.is_lt(code,119141))
    case False{}: propertiesBelow1195(code,U32.is_lt(code,119149))
def propertiesBelow1197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1191(code,U32.is_lt(code,118528))
    case False{}: propertiesBelow1196(code,U32.is_lt(code,119146))
def propertiesBelow1198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1198(code,U32.is_lt(code,119173))
def propertiesBelow1200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1200(code,U32.is_lt(code,119214))
def propertiesBelow1202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1199(code,U32.is_lt(code,119171))
    case False{}: propertiesBelow1201(code,U32.is_lt(code,119210))
def propertiesBelow1203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def propertiesBelow1204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1203(code,U32.is_lt(code,119552))
def propertiesBelow1205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow1206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1205(code,U32.is_lt(code,119666))
def propertiesBelow1207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1204(code,U32.is_lt(code,119365))
    case False{}: propertiesBelow1206(code,U32.is_lt(code,119648))
def propertiesBelow1208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1202(code,U32.is_lt(code,119180))
    case False{}: propertiesBelow1207(code,U32.is_lt(code,119639))
def propertiesBelow1209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1197(code,U32.is_lt(code,118599))
    case False{}: propertiesBelow1208(code,U32.is_lt(code,119362))
def propertiesBelow1210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1210(code,U32.is_lt(code,121399))
def propertiesBelow1212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1212(code,U32.is_lt(code,121461))
def propertiesBelow1214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1211(code,U32.is_lt(code,121344))
    case False{}: propertiesBelow1213(code,U32.is_lt(code,121453))
def propertiesBelow1215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1215(code,U32.is_lt(code,121477))
def propertiesBelow1217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1217(code,U32.is_lt(code,121505))
def propertiesBelow1219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1216(code,U32.is_lt(code,121476))
    case False{}: propertiesBelow1218(code,U32.is_lt(code,121504))
def propertiesBelow1220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1214(code,U32.is_lt(code,121403))
    case False{}: propertiesBelow1219(code,U32.is_lt(code,121499))
def propertiesBelow1221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1221(code,U32.is_lt(code,122887))
def propertiesBelow1223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1223(code,U32.is_lt(code,122907))
def propertiesBelow1225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1222(code,U32.is_lt(code,122880))
    case False{}: propertiesBelow1224(code,U32.is_lt(code,122905))
def propertiesBelow1226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1226(code,U32.is_lt(code,122917))
def propertiesBelow1228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1228(code,U32.is_lt(code,123023))
def propertiesBelow1230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1227(code,U32.is_lt(code,122915))
    case False{}: propertiesBelow1229(code,U32.is_lt(code,122923))
def propertiesBelow1231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1225(code,U32.is_lt(code,122888))
    case False{}: propertiesBelow1230(code,U32.is_lt(code,122918))
def propertiesBelow1232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1220(code,U32.is_lt(code,121462))
    case False{}: propertiesBelow1231(code,U32.is_lt(code,122914))
def propertiesBelow1233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1209(code,U32.is_lt(code,119163))
    case False{}: propertiesBelow1232(code,U32.is_lt(code,121520))
def propertiesBelow1234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1186(code,U32.is_lt(code,101590))
    case False{}: propertiesBelow1233(code,U32.is_lt(code,119671))
def propertiesBelow1235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1236(code,U32.is_lt(code,123567))
def propertiesBelow1238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1235(code,U32.is_lt(code,123184))
    case False{}: propertiesBelow1237(code,U32.is_lt(code,123566))
def propertiesBelow1239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1239(code,U32.is_lt(code,124140))
def propertiesBelow1241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1241(code,U32.is_lt(code,124400))
def propertiesBelow1243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1240(code,U32.is_lt(code,123632))
    case False{}: propertiesBelow1242(code,U32.is_lt(code,124398))
def propertiesBelow1244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1238(code,U32.is_lt(code,123191))
    case False{}: propertiesBelow1243(code,U32.is_lt(code,124144))
def propertiesBelow1245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1245(code,U32.is_lt(code,124646))
def propertiesBelow1247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1247(code,U32.is_lt(code,124656))
def propertiesBelow1249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1246(code,U32.is_lt(code,124644))
    case False{}: propertiesBelow1248(code,U32.is_lt(code,124654))
def propertiesBelow1250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 1
def propertiesBelow1251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: propertiesBelow1250(code,U32.is_lt(code,125136))
def propertiesBelow1252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 1
    case False{}: 0
def propertiesBelow1253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1252(code,U32.is_lt(code,125259))
def propertiesBelow1254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1251(code,U32.is_lt(code,124662))
    case False{}: propertiesBelow1253(code,U32.is_lt(code,125252))
def propertiesBelow1255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1249(code,U32.is_lt(code,124647))
    case False{}: propertiesBelow1254(code,U32.is_lt(code,125143))
def propertiesBelow1256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1244(code,U32.is_lt(code,123628))
    case False{}: propertiesBelow1255(code,U32.is_lt(code,124661))
def propertiesBelow1257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1257(code,U32.is_lt(code,127183))
def propertiesBelow1259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1259(code,U32.is_lt(code,127346))
def propertiesBelow1261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1258(code,U32.is_lt(code,126981))
    case False{}: propertiesBelow1260(code,U32.is_lt(code,127344))
def propertiesBelow1262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1262(code,U32.is_lt(code,127374))
def propertiesBelow1264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1264(code,U32.is_lt(code,127387))
def propertiesBelow1266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1263(code,U32.is_lt(code,127360))
    case False{}: propertiesBelow1265(code,U32.is_lt(code,127377))
def propertiesBelow1267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1261(code,U32.is_lt(code,127184))
    case False{}: propertiesBelow1266(code,U32.is_lt(code,127375))
def propertiesBelow1268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 96
def propertiesBelow1269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1268(code,U32.is_lt(code,127489))
def propertiesBelow1270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def propertiesBelow1271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 160
    case False{}: propertiesBelow1270(code,U32.is_lt(code,127504))
def propertiesBelow1272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1269(code,U32.is_lt(code,127488))
    case False{}: propertiesBelow1271(code,U32.is_lt(code,127491))
def propertiesBelow1273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 96
def propertiesBelow1274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1273(code,U32.is_lt(code,127535))
def propertiesBelow1275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 160
def propertiesBelow1276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1275(code,U32.is_lt(code,127543))
def propertiesBelow1277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1274(code,U32.is_lt(code,127515))
    case False{}: propertiesBelow1276(code,U32.is_lt(code,127538))
def propertiesBelow1278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1272(code,U32.is_lt(code,127490))
    case False{}: propertiesBelow1277(code,U32.is_lt(code,127536))
def propertiesBelow1279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1267(code,U32.is_lt(code,127358))
    case False{}: propertiesBelow1278(code,U32.is_lt(code,127514))
def propertiesBelow1280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1256(code,U32.is_lt(code,124643))
    case False{}: propertiesBelow1279(code,U32.is_lt(code,127462))
def propertiesBelow1281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def propertiesBelow1282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1281(code,U32.is_lt(code,127548))
def propertiesBelow1283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 352
def propertiesBelow1284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1283(code,U32.is_lt(code,127568))
def propertiesBelow1285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1282(code,U32.is_lt(code,127547))
    case False{}: propertiesBelow1284(code,U32.is_lt(code,127561))
def propertiesBelow1286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def propertiesBelow1287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1286(code,U32.is_lt(code,127590))
def propertiesBelow1288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1288(code,U32.is_lt(code,127778))
def propertiesBelow1290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1287(code,U32.is_lt(code,127584))
    case False{}: propertiesBelow1289(code,U32.is_lt(code,127777))
def propertiesBelow1291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1285(code,U32.is_lt(code,127552))
    case False{}: propertiesBelow1290(code,U32.is_lt(code,127744))
def propertiesBelow1292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 128
def propertiesBelow1293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1292(code,U32.is_lt(code,127798))
def propertiesBelow1294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1294(code,U32.is_lt(code,127813))
def propertiesBelow1296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1293(code,U32.is_lt(code,127789))
    case False{}: propertiesBelow1295(code,U32.is_lt(code,127812))
def propertiesBelow1297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 128
def propertiesBelow1298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1297(code,U32.is_lt(code,127869))
def propertiesBelow1299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1299(code,U32.is_lt(code,127878))
def propertiesBelow1301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1298(code,U32.is_lt(code,127820))
    case False{}: propertiesBelow1300(code,U32.is_lt(code,127877))
def propertiesBelow1302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1296(code,U32.is_lt(code,127799))
    case False{}: propertiesBelow1301(code,U32.is_lt(code,127870))
def propertiesBelow1303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1291(code,U32.is_lt(code,127570))
    case False{}: propertiesBelow1302(code,U32.is_lt(code,127819))
def propertiesBelow1304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1304(code,U32.is_lt(code,127896))
def propertiesBelow1306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1306(code,U32.is_lt(code,127902))
def propertiesBelow1308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1305(code,U32.is_lt(code,127894))
    case False{}: propertiesBelow1307(code,U32.is_lt(code,127900))
def propertiesBelow1309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1309(code,U32.is_lt(code,127941))
def propertiesBelow1311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1311(code,U32.is_lt(code,127946))
def propertiesBelow1313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1310(code,U32.is_lt(code,127938))
    case False{}: propertiesBelow1312(code,U32.is_lt(code,127944))
def propertiesBelow1314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1308(code,U32.is_lt(code,127897))
    case False{}: propertiesBelow1313(code,U32.is_lt(code,127943))
def propertiesBelow1315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 128
def propertiesBelow1316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1315(code,U32.is_lt(code,127956))
def propertiesBelow1317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1317(code,U32.is_lt(code,127987))
def propertiesBelow1319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1316(code,U32.is_lt(code,127951))
    case False{}: propertiesBelow1318(code,U32.is_lt(code,127985))
def propertiesBelow1320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1320(code,U32.is_lt(code,127990))
def propertiesBelow1322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1322(code,U32.is_lt(code,128008))
def propertiesBelow1324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1321(code,U32.is_lt(code,127989))
    case False{}: propertiesBelow1323(code,U32.is_lt(code,127992))
def propertiesBelow1325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1319(code,U32.is_lt(code,127968))
    case False{}: propertiesBelow1324(code,U32.is_lt(code,127991))
def propertiesBelow1326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1314(code,U32.is_lt(code,127904))
    case False{}: propertiesBelow1325(code,U32.is_lt(code,127988))
def propertiesBelow1327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1303(code,U32.is_lt(code,127780))
    case False{}: propertiesBelow1326(code,U32.is_lt(code,127947))
def propertiesBelow1328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1280(code,U32.is_lt(code,126980))
    case False{}: propertiesBelow1327(code,U32.is_lt(code,127892))
def propertiesBelow1329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1234(code,U32.is_lt(code,113823))
    case False{}: propertiesBelow1328(code,U32.is_lt(code,127544))
def propertiesBelow1330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1331(code,U32.is_lt(code,128039))
def propertiesBelow1333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1330(code,U32.is_lt(code,128021))
    case False{}: propertiesBelow1332(code,U32.is_lt(code,128038))
def propertiesBelow1334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 128
def propertiesBelow1335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1334(code,U32.is_lt(code,128063))
def propertiesBelow1336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 224
def propertiesBelow1337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1336(code,U32.is_lt(code,128066))
def propertiesBelow1338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1335(code,U32.is_lt(code,128060))
    case False{}: propertiesBelow1337(code,U32.is_lt(code,128065))
def propertiesBelow1339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1333(code,U32.is_lt(code,128022))
    case False{}: propertiesBelow1338(code,U32.is_lt(code,128064))
def propertiesBelow1340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1340(code,U32.is_lt(code,128081))
def propertiesBelow1342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1342(code,U32.is_lt(code,128107))
def propertiesBelow1344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1341(code,U32.is_lt(code,128070))
    case False{}: propertiesBelow1343(code,U32.is_lt(code,128106))
def propertiesBelow1345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1345(code,U32.is_lt(code,128125))
def propertiesBelow1347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1347(code,U32.is_lt(code,128133))
def propertiesBelow1349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1346(code,U32.is_lt(code,128124))
    case False{}: propertiesBelow1348(code,U32.is_lt(code,128132))
def propertiesBelow1350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1344(code,U32.is_lt(code,128102))
    case False{}: propertiesBelow1349(code,U32.is_lt(code,128129))
def propertiesBelow1351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1339(code,U32.is_lt(code,128059))
    case False{}: propertiesBelow1350(code,U32.is_lt(code,128121))
def propertiesBelow1352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1352(code,U32.is_lt(code,128144))
def propertiesBelow1354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1354(code,U32.is_lt(code,128170))
def propertiesBelow1356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1353(code,U32.is_lt(code,128143))
    case False{}: propertiesBelow1355(code,U32.is_lt(code,128146))
def propertiesBelow1357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1357(code,U32.is_lt(code,128254))
def propertiesBelow1359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1359(code,U32.is_lt(code,128329))
def propertiesBelow1361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1358(code,U32.is_lt(code,128253))
    case False{}: propertiesBelow1360(code,U32.is_lt(code,128318))
def propertiesBelow1362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1356(code,U32.is_lt(code,128145))
    case False{}: propertiesBelow1361(code,U32.is_lt(code,128255))
def propertiesBelow1363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1363(code,U32.is_lt(code,128336))
def propertiesBelow1365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1365(code,U32.is_lt(code,128369))
def propertiesBelow1367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1364(code,U32.is_lt(code,128335))
    case False{}: propertiesBelow1366(code,U32.is_lt(code,128367))
def propertiesBelow1368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 0
def propertiesBelow1369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1368(code,U32.is_lt(code,128379))
def propertiesBelow1370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1370(code,U32.is_lt(code,128394))
def propertiesBelow1372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1369(code,U32.is_lt(code,128378))
    case False{}: propertiesBelow1371(code,U32.is_lt(code,128392))
def propertiesBelow1373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1367(code,U32.is_lt(code,128360))
    case False{}: propertiesBelow1372(code,U32.is_lt(code,128391))
def propertiesBelow1374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1362(code,U32.is_lt(code,128171))
    case False{}: propertiesBelow1373(code,U32.is_lt(code,128371))
def propertiesBelow1375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1351(code,U32.is_lt(code,128068))
    case False{}: propertiesBelow1374(code,U32.is_lt(code,128331))
def propertiesBelow1376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1376(code,U32.is_lt(code,128401))
def propertiesBelow1378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1378(code,U32.is_lt(code,128420))
def propertiesBelow1380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1377(code,U32.is_lt(code,128400))
    case False{}: propertiesBelow1379(code,U32.is_lt(code,128407))
def propertiesBelow1381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1381(code,U32.is_lt(code,128424))
def propertiesBelow1383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1383(code,U32.is_lt(code,128435))
def propertiesBelow1385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1382(code,U32.is_lt(code,128422))
    case False{}: propertiesBelow1384(code,U32.is_lt(code,128433))
def propertiesBelow1386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1380(code,U32.is_lt(code,128405))
    case False{}: propertiesBelow1385(code,U32.is_lt(code,128425))
def propertiesBelow1387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1387(code,U32.is_lt(code,128450))
def propertiesBelow1389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1389(code,U32.is_lt(code,128468))
def propertiesBelow1391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1388(code,U32.is_lt(code,128445))
    case False{}: propertiesBelow1390(code,U32.is_lt(code,128465))
def propertiesBelow1392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1392(code,U32.is_lt(code,128481))
def propertiesBelow1394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1394(code,U32.is_lt(code,128484))
def propertiesBelow1396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1393(code,U32.is_lt(code,128479))
    case False{}: propertiesBelow1395(code,U32.is_lt(code,128483))
def propertiesBelow1397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1391(code,U32.is_lt(code,128453))
    case False{}: propertiesBelow1396(code,U32.is_lt(code,128482))
def propertiesBelow1398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1386(code,U32.is_lt(code,128421))
    case False{}: propertiesBelow1397(code,U32.is_lt(code,128476))
def propertiesBelow1399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1399(code,U32.is_lt(code,128495))
def propertiesBelow1401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1401(code,U32.is_lt(code,128500))
def propertiesBelow1403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1400(code,U32.is_lt(code,128489))
    case False{}: propertiesBelow1402(code,U32.is_lt(code,128499))
def propertiesBelow1404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1404(code,U32.is_lt(code,128558))
def propertiesBelow1406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1406(code,U32.is_lt(code,128567))
def propertiesBelow1408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1405(code,U32.is_lt(code,128507))
    case False{}: propertiesBelow1407(code,U32.is_lt(code,128565))
def propertiesBelow1409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1403(code,U32.is_lt(code,128496))
    case False{}: propertiesBelow1408(code,U32.is_lt(code,128559))
def propertiesBelow1410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1410(code,U32.is_lt(code,128581))
def propertiesBelow1412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 0
def propertiesBelow1413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1412(code,U32.is_lt(code,128592))
def propertiesBelow1414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1411(code,U32.is_lt(code,128579))
    case False{}: propertiesBelow1413(code,U32.is_lt(code,128587))
def propertiesBelow1415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1415(code,U32.is_lt(code,128676))
def propertiesBelow1417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1417(code,U32.is_lt(code,128704))
def propertiesBelow1419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1416(code,U32.is_lt(code,128675))
    case False{}: propertiesBelow1418(code,U32.is_lt(code,128695))
def propertiesBelow1420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1414(code,U32.is_lt(code,128584))
    case False{}: propertiesBelow1419(code,U32.is_lt(code,128692))
def propertiesBelow1421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1409(code,U32.is_lt(code,128506))
    case False{}: propertiesBelow1420(code,U32.is_lt(code,128640))
def propertiesBelow1422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1398(code,U32.is_lt(code,128444))
    case False{}: propertiesBelow1421(code,U32.is_lt(code,128578))
def propertiesBelow1423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1375(code,U32.is_lt(code,128136))
    case False{}: propertiesBelow1422(code,U32.is_lt(code,128488))
def propertiesBelow1424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 128
def propertiesBelow1425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1424(code,U32.is_lt(code,128715))
def propertiesBelow1426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 96
def propertiesBelow1427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1426(code,U32.is_lt(code,128720))
def propertiesBelow1428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1425(code,U32.is_lt(code,128710))
    case False{}: propertiesBelow1427(code,U32.is_lt(code,128717))
def propertiesBelow1429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1429(code,U32.is_lt(code,128729))
def propertiesBelow1431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1431(code,U32.is_lt(code,128742))
def propertiesBelow1433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1430(code,U32.is_lt(code,128725))
    case False{}: propertiesBelow1432(code,U32.is_lt(code,128736))
def propertiesBelow1434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1428(code,U32.is_lt(code,128716))
    case False{}: propertiesBelow1433(code,U32.is_lt(code,128732))
def propertiesBelow1435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1435(code,U32.is_lt(code,128747))
def propertiesBelow1437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: 0
def propertiesBelow1438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1437(code,U32.is_lt(code,128753))
def propertiesBelow1439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1436(code,U32.is_lt(code,128746))
    case False{}: propertiesBelow1438(code,U32.is_lt(code,128752))
def propertiesBelow1440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 128
    case False{}: propertiesBelow1440(code,U32.is_lt(code,128765))
def propertiesBelow1442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1442(code,U32.is_lt(code,129008))
def propertiesBelow1444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1441(code,U32.is_lt(code,128756))
    case False{}: propertiesBelow1443(code,U32.is_lt(code,129004))
def propertiesBelow1445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1439(code,U32.is_lt(code,128749))
    case False{}: propertiesBelow1444(code,U32.is_lt(code,128992))
def propertiesBelow1446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1434(code,U32.is_lt(code,128723))
    case False{}: propertiesBelow1445(code,U32.is_lt(code,128755))
def propertiesBelow1447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1447(code,U32.is_lt(code,129293))
def propertiesBelow1449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1449(code,U32.is_lt(code,129304))
def propertiesBelow1451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1448(code,U32.is_lt(code,129292))
    case False{}: propertiesBelow1450(code,U32.is_lt(code,129296))
def propertiesBelow1452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1452(code,U32.is_lt(code,129319))
def propertiesBelow1454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1454(code,U32.is_lt(code,129339))
def propertiesBelow1456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1453(code,U32.is_lt(code,129318))
    case False{}: propertiesBelow1455(code,U32.is_lt(code,129338))
def propertiesBelow1457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1451(code,U32.is_lt(code,129295))
    case False{}: propertiesBelow1456(code,U32.is_lt(code,129328))
def propertiesBelow1458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1458(code,U32.is_lt(code,129350))
def propertiesBelow1460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1460(code,U32.is_lt(code,129400))
def propertiesBelow1462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1459(code,U32.is_lt(code,129343))
    case False{}: propertiesBelow1461(code,U32.is_lt(code,129399))
def propertiesBelow1463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1463(code,U32.is_lt(code,129464))
def propertiesBelow1465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1465(code,U32.is_lt(code,129468))
def propertiesBelow1467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1464(code,U32.is_lt(code,129463))
    case False{}: propertiesBelow1466(code,U32.is_lt(code,129467))
def propertiesBelow1468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1462(code,U32.is_lt(code,129351))
    case False{}: propertiesBelow1467(code,U32.is_lt(code,129466))
def propertiesBelow1469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1457(code,U32.is_lt(code,129312))
    case False{}: propertiesBelow1468(code,U32.is_lt(code,129461))
def propertiesBelow1470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1446(code,U32.is_lt(code,128745))
    case False{}: propertiesBelow1469(code,U32.is_lt(code,129340))
def propertiesBelow1471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: propertiesBelow1471(code,U32.is_lt(code,129489))
def propertiesBelow1473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1473(code,U32.is_lt(code,129648))
def propertiesBelow1475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1472(code,U32.is_lt(code,129488))
    case False{}: propertiesBelow1474(code,U32.is_lt(code,129536))
def propertiesBelow1476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1476(code,U32.is_lt(code,129675))
def propertiesBelow1478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 224
    case False{}: 96
def propertiesBelow1479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1478(code,U32.is_lt(code,129734))
def propertiesBelow1480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1477(code,U32.is_lt(code,129664))
    case False{}: propertiesBelow1479(code,U32.is_lt(code,129731))
def propertiesBelow1481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1475(code,U32.is_lt(code,129504))
    case False{}: propertiesBelow1480(code,U32.is_lt(code,129678))
def propertiesBelow1482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 0
def propertiesBelow1483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1482(code,U32.is_lt(code,129737))
def propertiesBelow1484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 96
def propertiesBelow1485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: propertiesBelow1484(code,U32.is_lt(code,129759))
def propertiesBelow1486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1483(code,U32.is_lt(code,129736))
    case False{}: propertiesBelow1485(code,U32.is_lt(code,129757))
def propertiesBelow1487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 96
    case False{}: 224
def propertiesBelow1488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1487(code,U32.is_lt(code,129776))
def propertiesBelow1489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow1490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1489(code,U32.is_lt(code,173792))
def propertiesBelow1491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1488(code,U32.is_lt(code,129775))
    case False{}: propertiesBelow1490(code,U32.is_lt(code,131072))
def propertiesBelow1492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1486(code,U32.is_lt(code,129741))
    case False{}: propertiesBelow1491(code,U32.is_lt(code,129785))
def propertiesBelow1493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1481(code,U32.is_lt(code,129661))
    case False{}: propertiesBelow1492(code,U32.is_lt(code,129771))
def propertiesBelow1494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow1495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow1494(code,U32.is_lt(code,178208))
def propertiesBelow1496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow1497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1496(code,U32.is_lt(code,191457))
def propertiesBelow1498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1495(code,U32.is_lt(code,178206))
    case False{}: propertiesBelow1497(code,U32.is_lt(code,183984))
def propertiesBelow1499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 288
def propertiesBelow1500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: propertiesBelow1499(code,U32.is_lt(code,194560))
def propertiesBelow1501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 288
def propertiesBelow1502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1501(code,U32.is_lt(code,196608))
def propertiesBelow1503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1500(code,U32.is_lt(code,192094))
    case False{}: propertiesBelow1502(code,U32.is_lt(code,196606))
def propertiesBelow1504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1498(code,U32.is_lt(code,183982))
    case False{}: propertiesBelow1503(code,U32.is_lt(code,195102))
def propertiesBelow1505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 288
    case False{}: 32
def propertiesBelow1506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: propertiesBelow1505(code,U32.is_lt(code,210042))
def propertiesBelow1507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 24
def propertiesBelow1508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: propertiesBelow1507(code,U32.is_lt(code,917505))
def propertiesBelow1509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1506(code,U32.is_lt(code,201552))
    case False{}: propertiesBelow1508(code,U32.is_lt(code,917504))
def propertiesBelow1510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 24
    case False{}: 16
def propertiesBelow1511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: propertiesBelow1510(code,U32.is_lt(code,917632))
def propertiesBelow1512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def propertiesBelow1513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 17
    case False{}: propertiesBelow1512(code,U32.is_lt(code,921600))
def propertiesBelow1514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1511(code,U32.is_lt(code,917536))
    case False{}: propertiesBelow1513(code,U32.is_lt(code,918000))
def propertiesBelow1515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1509(code,U32.is_lt(code,262142))
    case False{}: propertiesBelow1514(code,U32.is_lt(code,917760))
def propertiesBelow1516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1504(code,U32.is_lt(code,191472))
    case False{}: propertiesBelow1515(code,U32.is_lt(code,917506))
def propertiesBelow1517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1493(code,U32.is_lt(code,129735))
    case False{}: propertiesBelow1516(code,U32.is_lt(code,201547))
def propertiesBelow1518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1470(code,U32.is_lt(code,129009))
    case False{}: propertiesBelow1517(code,U32.is_lt(code,173824))
def propertiesBelow1519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1423(code,U32.is_lt(code,128398))
    case False{}: propertiesBelow1518(code,U32.is_lt(code,129485))
def propertiesBelow1520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1329(code,U32.is_lt(code,123024))
    case False{}: propertiesBelow1519(code,U32.is_lt(code,128705))
def propertiesBelow1521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow1140(code,U32.is_lt(code,70464))
    case False{}: propertiesBelow1520(code,U32.is_lt(code,128009))
def propertiesBelow1522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: propertiesBelow760(code,U32.is_lt(code,6742))
    case False{}: propertiesBelow1521(code,U32.is_lt(code,78934))

def properties(+code: U32) -> U32:
  propertiesBelow1522(code,U32.is_lt(code,43452))

def emojiHash(text: String,value: U32) -> U32:
  match text:
    case SNil{}: value
    case SCon{char,rest}: emojiHash(rest,U32.mul(U32.xor(value,Char.to_u32(char)),16777619))

def emojiBelow0(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1fa70}")
    case False{}: Strings.equal(text,"\u{1f62e}\u{200d}\u{1f4a8}")
def emojiBelow1(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cd}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f3a8}")
def emojiBelow2(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1(hash,text,U32.is_lt(hash,10091001))
def emojiBelow3(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow0(hash,text,U32.is_lt(hash,2268692))
    case False{}: emojiBelow2(hash,text,U32.is_lt(hash,9651369))
def emojiBelow4(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f645}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow5(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9af}")
def emojiBelow6(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow5(hash,text,U32.is_lt(hash,26249612))
def emojiBelow7(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow4(hash,text,U32.is_lt(hash,12452970))
    case False{}: emojiBelow6(hash,text,U32.is_lt(hash,25330545))
def emojiBelow8(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow3(hash,text,U32.is_lt(hash,4654099))
    case False{}: emojiBelow7(hash,text,U32.is_lt(hash,18918840))
def emojiBelow9(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow10(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f9b9}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow11(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f466}\u{200d}\u{1f466}")
    case False{}: emojiBelow10(hash,text,U32.is_lt(hash,45713896))
def emojiBelow12(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow9(hash,text,U32.is_lt(hash,30457208))
    case False{}: emojiBelow11(hash,text,U32.is_lt(hash,44363732))
def emojiBelow13(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93e}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow14(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow13(hash,text,U32.is_lt(hash,52731335))
def emojiBelow15(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow16(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow15(hash,text,U32.is_lt(hash,63870918))
def emojiBelow17(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow14(hash,text,U32.is_lt(hash,47849307))
    case False{}: emojiBelow16(hash,text,U32.is_lt(hash,58832275))
def emojiBelow18(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow12(hash,text,U32.is_lt(hash,38529419))
    case False{}: emojiBelow17(hash,text,U32.is_lt(hash,58250454))
def emojiBelow19(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow8(hash,text,U32.is_lt(hash,10953308))
    case False{}: emojiBelow18(hash,text,U32.is_lt(hash,47658690))
def emojiBelow20(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f37c}")
    case False{}: Strings.equal(text,"\u{1f486}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow21(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow22(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2696}\u{fe0f}")
    case False{}: emojiBelow21(hash,text,U32.is_lt(hash,81030041))
def emojiBelow23(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow20(hash,text,U32.is_lt(hash,71042881))
    case False{}: emojiBelow22(hash,text,U32.is_lt(hash,80760971))
def emojiBelow24(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow25(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow24(hash,text,U32.is_lt(hash,89110715))
def emojiBelow26(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f481}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow27(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow26(hash,text,U32.is_lt(hash,103969507))
def emojiBelow28(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow25(hash,text,U32.is_lt(hash,83808832))
    case False{}: emojiBelow27(hash,text,U32.is_lt(hash,103809371))
def emojiBelow29(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow23(hash,text,U32.is_lt(hash,78584091))
    case False{}: emojiBelow28(hash,text,U32.is_lt(hash,101559068))
def emojiBelow30(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d8}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f4bc}")
def emojiBelow31(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f397}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f52c}")
def emojiBelow32(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9bc}")
    case False{}: emojiBelow31(hash,text,U32.is_lt(hash,118614223))
def emojiBelow33(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow30(hash,text,U32.is_lt(hash,111680544))
    case False{}: emojiBelow32(hash,text,U32.is_lt(hash,113374811))
def emojiBelow34(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3d9}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3cd}\u{fe0f}")
def emojiBelow35(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f5}\u{fe0f}")
    case False{}: emojiBelow34(hash,text,U32.is_lt(hash,125289909))
def emojiBelow36(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f328}\u{fe0f}")
def emojiBelow37(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9bd}")
    case False{}: emojiBelow36(hash,text,U32.is_lt(hash,141023432))
def emojiBelow38(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow35(hash,text,U32.is_lt(hash,121938561))
    case False{}: emojiBelow37(hash,text,U32.is_lt(hash,140827578))
def emojiBelow39(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow33(hash,text,U32.is_lt(hash,111990048))
    case False{}: emojiBelow38(hash,text,U32.is_lt(hash,128767667))
def emojiBelow40(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow29(hash,text,U32.is_lt(hash,83473135))
    case False{}: emojiBelow39(hash,text,U32.is_lt(hash,118999885))
def emojiBelow41(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow19(hash,text,U32.is_lt(hash,29078018))
    case False{}: emojiBelow40(hash,text,U32.is_lt(hash,107144537))
def emojiBelow42(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f6a3}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow43(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow44(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2764}\u{fe0f}")
    case False{}: emojiBelow43(hash,text,U32.is_lt(hash,159763284))
def emojiBelow45(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow42(hash,text,U32.is_lt(hash,149726737))
    case False{}: emojiBelow44(hash,text,U32.is_lt(hash,158386271))
def emojiBelow46(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow47(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26a0}\u{fe0f}")
def emojiBelow48(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f393}")
    case False{}: emojiBelow47(hash,text,U32.is_lt(hash,173083856))
def emojiBelow49(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow46(hash,text,U32.is_lt(hash,161965538))
    case False{}: emojiBelow48(hash,text,U32.is_lt(hash,172565766))
def emojiBelow50(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow45(hash,text,U32.is_lt(hash,151990836))
    case False{}: emojiBelow49(hash,text,U32.is_lt(hash,163551476))
def emojiBelow51(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{269b}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow52(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2695}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f926}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow53(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2692}\u{fe0f}")
    case False{}: emojiBelow52(hash,text,U32.is_lt(hash,176802710))
def emojiBelow54(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow51(hash,text,U32.is_lt(hash,174049400))
    case False{}: emojiBelow53(hash,text,U32.is_lt(hash,176288109))
def emojiBelow55(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f9}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f692}")
def emojiBelow56(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow55(hash,text,U32.is_lt(hash,180638599))
def emojiBelow57(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26c8}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow58(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26d1}\u{fe0f}")
    case False{}: emojiBelow57(hash,text,U32.is_lt(hash,188545042))
def emojiBelow59(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow56(hash,text,U32.is_lt(hash,179639457))
    case False{}: emojiBelow58(hash,text,U32.is_lt(hash,187272680))
def emojiBelow60(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow54(hash,text,U32.is_lt(hash,175357582))
    case False{}: emojiBelow59(hash,text,U32.is_lt(hash,185929481))
def emojiBelow61(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow50(hash,text,U32.is_lt(hash,160443600))
    case False{}: emojiBelow60(hash,text,U32.is_lt(hash,178513271))
def emojiBelow62(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow63(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f33e}")
def emojiBelow64(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow63(hash,text,U32.is_lt(hash,194725876))
def emojiBelow65(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow62(hash,text,U32.is_lt(hash,191259900))
    case False{}: emojiBelow64(hash,text,U32.is_lt(hash,193998838))
def emojiBelow66(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{270f}\u{fe0f}")
def emojiBelow67(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2712}\u{fe0f}")
    case False{}: emojiBelow66(hash,text,U32.is_lt(hash,198257251))
def emojiBelow68(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{262e}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2620}\u{fe0f}")
def emojiBelow69(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow68(hash,text,U32.is_lt(hash,205014352))
def emojiBelow70(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow67(hash,text,U32.is_lt(hash,196475484))
    case False{}: emojiBelow69(hash,text,U32.is_lt(hash,203936730))
def emojiBelow71(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow65(hash,text,U32.is_lt(hash,193000930))
    case False{}: emojiBelow70(hash,text,U32.is_lt(hash,199663160))
def emojiBelow72(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3ff}")
def emojiBelow73(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f3a4}")
def emojiBelow74(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow73(hash,text,U32.is_lt(hash,211422429))
def emojiBelow75(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow72(hash,text,U32.is_lt(hash,206211471))
    case False{}: emojiBelow74(hash,text,U32.is_lt(hash,210776457))
def emojiBelow76(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow77(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow76(hash,text,U32.is_lt(hash,214671511))
def emojiBelow78(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6a3}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow79(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}")
    case False{}: emojiBelow78(hash,text,U32.is_lt(hash,221845392))
def emojiBelow80(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow77(hash,text,U32.is_lt(hash,213253103))
    case False{}: emojiBelow79(hash,text,U32.is_lt(hash,219028557))
def emojiBelow81(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow75(hash,text,U32.is_lt(hash,207787684))
    case False{}: emojiBelow80(hash,text,U32.is_lt(hash,216318496))
def emojiBelow82(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow71(hash,text,U32.is_lt(hash,195836430))
    case False{}: emojiBelow81(hash,text,U32.is_lt(hash,212460432))
def emojiBelow83(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow61(hash,text,U32.is_lt(hash,174014383))
    case False{}: emojiBelow82(hash,text,U32.is_lt(hash,205667270))
def emojiBelow84(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow41(hash,text,U32.is_lt(hash,66388945))
    case False{}: emojiBelow83(hash,text,U32.is_lt(hash,189433852))
def emojiBelow85(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f4bb}")
def emojiBelow86(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f4bb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fe}")
def emojiBelow87(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f9}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow86(hash,text,U32.is_lt(hash,229238051))
def emojiBelow88(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow85(hash,text,U32.is_lt(hash,227890544))
    case False{}: emojiBelow87(hash,text,U32.is_lt(hash,229123877))
def emojiBelow89(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}")
def emojiBelow90(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2195}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow91(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: emojiBelow90(hash,text,U32.is_lt(hash,244331695))
def emojiBelow92(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow89(hash,text,U32.is_lt(hash,233096115))
    case False{}: emojiBelow91(hash,text,U32.is_lt(hash,240561773))
def emojiBelow93(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow88(hash,text,U32.is_lt(hash,228730377))
    case False{}: emojiBelow92(hash,text,U32.is_lt(hash,239766709))
def emojiBelow94(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fd}")
def emojiBelow95(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow96(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow95(hash,text,U32.is_lt(hash,262793289))
def emojiBelow97(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow94(hash,text,U32.is_lt(hash,246808341))
    case False{}: emojiBelow96(hash,text,U32.is_lt(hash,250918165))
def emojiBelow98(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow99(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f3ed}")
    case False{}: emojiBelow98(hash,text,U32.is_lt(hash,267203066))
def emojiBelow100(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f37c}")
def emojiBelow101(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f527}")
    case False{}: emojiBelow100(hash,text,U32.is_lt(hash,276941322))
def emojiBelow102(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow99(hash,text,U32.is_lt(hash,266110975))
    case False{}: emojiBelow101(hash,text,U32.is_lt(hash,271030656))
def emojiBelow103(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow97(hash,text,U32.is_lt(hash,249333356))
    case False{}: emojiBelow102(hash,text,U32.is_lt(hash,269612794))
def emojiBelow104(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow93(hash,text,U32.is_lt(hash,232877245))
    case False{}: emojiBelow103(hash,text,U32.is_lt(hash,264217190))
def emojiBelow105(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6a3}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow106(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9da}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow107(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow106(hash,text,U32.is_lt(hash,285492404))
def emojiBelow108(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow105(hash,text,U32.is_lt(hash,281235067))
    case False{}: emojiBelow107(hash,text,U32.is_lt(hash,284367736))
def emojiBelow109(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64b}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow110(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9b0}")
    case False{}: emojiBelow109(hash,text,U32.is_lt(hash,289351303))
def emojiBelow111(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow112(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow111(hash,text,U32.is_lt(hash,294017915))
def emojiBelow113(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow110(hash,text,U32.is_lt(hash,287853188))
    case False{}: emojiBelow112(hash,text,U32.is_lt(hash,290013474))
def emojiBelow114(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow108(hash,text,U32.is_lt(hash,282888594))
    case False{}: emojiBelow113(hash,text,U32.is_lt(hash,289500616))
def emojiBelow115(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f938}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow116(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f468}\u{200d}\u{1f467}\u{200d}\u{1f467}")
def emojiBelow117(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f3eb}")
    case False{}: emojiBelow116(hash,text,U32.is_lt(hash,303173290))
def emojiBelow118(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow115(hash,text,U32.is_lt(hash,295992737))
    case False{}: emojiBelow117(hash,text,U32.is_lt(hash,299666213))
def emojiBelow119(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9b0}")
def emojiBelow120(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9b3}")
    case False{}: emojiBelow119(hash,text,U32.is_lt(hash,313321476))
def emojiBelow121(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{23f9}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f468}\u{200d}\u{1f467}\u{200d}\u{1f466}")
def emojiBelow122(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow121(hash,text,U32.is_lt(hash,319950909))
def emojiBelow123(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow120(hash,text,U32.is_lt(hash,304844041))
    case False{}: emojiBelow122(hash,text,U32.is_lt(hash,318813089))
def emojiBelow124(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow118(hash,text,U32.is_lt(hash,297772428))
    case False{}: emojiBelow123(hash,text,U32.is_lt(hash,315068399))
def emojiBelow125(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow114(hash,text,U32.is_lt(hash,287808275))
    case False{}: emojiBelow124(hash,text,U32.is_lt(hash,304585894))
def emojiBelow126(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow104(hash,text,U32.is_lt(hash,246015670))
    case False{}: emojiBelow125(hash,text,U32.is_lt(hash,294543865))
def emojiBelow127(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{23ed}\u{fe0f}")
def emojiBelow128(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{203c}\u{fe0f}")
def emojiBelow129(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow128(hash,text,U32.is_lt(hash,329797660))
def emojiBelow130(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow127(hash,text,U32.is_lt(hash,322164437))
    case False{}: emojiBelow129(hash,text,U32.is_lt(hash,328246754))
def emojiBelow131(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f6b4}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow132(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f4bc}")
def emojiBelow133(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f4}\u{e0067}\u{e0062}\u{e0065}\u{e006e}\u{e0067}\u{e007f}")
    case False{}: emojiBelow132(hash,text,U32.is_lt(hash,345333877))
def emojiBelow134(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow131(hash,text,U32.is_lt(hash,334841391))
    case False{}: emojiBelow133(hash,text,U32.is_lt(hash,345024373))
def emojiBelow135(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow130(hash,text,U32.is_lt(hash,323542776))
    case False{}: emojiBelow134(hash,text,U32.is_lt(hash,341072399))
def emojiBelow136(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{1f9d6}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow137(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9de}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow138(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow137(hash,text,U32.is_lt(hash,361186023))
def emojiBelow139(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow136(hash,text,U32.is_lt(hash,355782783))
    case False{}: emojiBelow138(hash,text,U32.is_lt(hash,357846448))
def emojiBelow140(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9b3}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fb}\u{200d}\u{1faf2}\u{1f3fe}")
def emojiBelow141(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9b3}")
    case False{}: emojiBelow140(hash,text,U32.is_lt(hash,367014272))
def emojiBelow142(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5e1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9b8}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow143(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9b2}")
    case False{}: emojiBelow142(hash,text,U32.is_lt(hash,378989149))
def emojiBelow144(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow141(hash,text,U32.is_lt(hash,363654333))
    case False{}: emojiBelow143(hash,text,U32.is_lt(hash,378866329))
def emojiBelow145(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow139(hash,text,U32.is_lt(hash,357147954))
    case False{}: emojiBelow144(hash,text,U32.is_lt(hash,378579611))
def emojiBelow146(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow135(hash,text,U32.is_lt(hash,330099095))
    case False{}: emojiBelow145(hash,text,U32.is_lt(hash,361801992))
def emojiBelow147(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f5d3}\u{fe0f}")
def emojiBelow148(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f466}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow149(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{261d}\u{1f3fb}")
    case False{}: emojiBelow148(hash,text,U32.is_lt(hash,382709827))
def emojiBelow150(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow147(hash,text,U32.is_lt(hash,381140055))
    case False{}: emojiBelow149(hash,text,U32.is_lt(hash,381708504))
def emojiBelow151(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9dc}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow152(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fb}\u{200d}\u{1faf2}\u{1f3ff}")
    case False{}: emojiBelow151(hash,text,U32.is_lt(hash,387019688))
def emojiBelow153(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{261d}\u{1f3fc}")
def emojiBelow154(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9b1}")
    case False{}: emojiBelow153(hash,text,U32.is_lt(hash,398307868))
def emojiBelow155(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow152(hash,text,U32.is_lt(hash,385172059))
    case False{}: emojiBelow154(hash,text,U32.is_lt(hash,395570571))
def emojiBelow156(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow150(hash,text,U32.is_lt(hash,381530249))
    case False{}: emojiBelow155(hash,text,U32.is_lt(hash,395357230))
def emojiBelow157(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow158(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow159(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fb}\u{200d}\u{1faf2}\u{1f3fc}")
    case False{}: emojiBelow158(hash,text,U32.is_lt(hash,404174742))
def emojiBelow160(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow157(hash,text,U32.is_lt(hash,398758753))
    case False{}: emojiBelow159(hash,text,U32.is_lt(hash,401949678))
def emojiBelow161(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f574}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow162(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow161(hash,text,U32.is_lt(hash,409760334))
def emojiBelow163(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26d3}\u{fe0f}\u{200d}\u{1f4a5}")
def emojiBelow164(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9b0}")
    case False{}: emojiBelow163(hash,text,U32.is_lt(hash,413741905))
def emojiBelow165(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow162(hash,text,U32.is_lt(hash,408788676))
    case False{}: emojiBelow164(hash,text,U32.is_lt(hash,412991386))
def emojiBelow166(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow160(hash,text,U32.is_lt(hash,400569510))
    case False{}: emojiBelow165(hash,text,U32.is_lt(hash,412134849))
def emojiBelow167(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow156(hash,text,U32.is_lt(hash,383791891))
    case False{}: emojiBelow166(hash,text,U32.is_lt(hash,404511486))
def emojiBelow168(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow146(hash,text,U32.is_lt(hash,346876714))
    case False{}: emojiBelow167(hash,text,U32.is_lt(hash,398486123))
def emojiBelow169(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow126(hash,text,U32.is_lt(hash,278807066))
    case False{}: emojiBelow168(hash,text,U32.is_lt(hash,379837987))
def emojiBelow170(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow84(hash,text,U32.is_lt(hash,146142627))
    case False{}: emojiBelow169(hash,text,U32.is_lt(hash,321363513))
def emojiBelow171(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{261d}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fb}\u{200d}\u{1faf2}\u{1f3fd}")
def emojiBelow172(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f5a5}\u{fe0f}")
def emojiBelow173(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow172(hash,text,U32.is_lt(hash,422046973))
def emojiBelow174(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow171(hash,text,U32.is_lt(hash,417347129))
    case False{}: emojiBelow173(hash,text,U32.is_lt(hash,419345831))
def emojiBelow175(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270b}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fc}")
def emojiBelow176(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270c}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{270d}\u{1f3fb}")
def emojiBelow177(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow176(hash,text,U32.is_lt(hash,426530841))
def emojiBelow178(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow175(hash,text,U32.is_lt(hash,425294360))
    case False{}: emojiBelow177(hash,text,U32.is_lt(hash,426368432))
def emojiBelow179(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow174(hash,text,U32.is_lt(hash,418727297))
    case False{}: emojiBelow178(hash,text,U32.is_lt(hash,425629274))
def emojiBelow180(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f5fa}\u{fe0f}")
def emojiBelow181(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9af}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2696}\u{fe0f}")
def emojiBelow182(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow181(hash,text,U32.is_lt(hash,430927448))
def emojiBelow183(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow180(hash,text,U32.is_lt(hash,427406470))
    case False{}: emojiBelow182(hash,text,U32.is_lt(hash,430764809))
def emojiBelow184(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9dc}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow185(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{261d}\u{1f3fe}")
    case False{}: emojiBelow184(hash,text,U32.is_lt(hash,436199232))
def emojiBelow186(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f4fd}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f4bb}")
def emojiBelow187(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow186(hash,text,U32.is_lt(hash,440148030))
def emojiBelow188(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow185(hash,text,U32.is_lt(hash,435504916))
    case False{}: emojiBelow187(hash,text,U32.is_lt(hash,439788645))
def emojiBelow189(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow183(hash,text,U32.is_lt(hash,427979875))
    case False{}: emojiBelow188(hash,text,U32.is_lt(hash,438066724))
def emojiBelow190(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow179(hash,text,U32.is_lt(hash,423905699))
    case False{}: emojiBelow189(hash,text,U32.is_lt(hash,431863106))
def emojiBelow191(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fd}")
def emojiBelow192(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270d}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f3a8}")
def emojiBelow193(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow192(hash,text,U32.is_lt(hash,446510748))
def emojiBelow194(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow191(hash,text,U32.is_lt(hash,442071979))
    case False{}: emojiBelow193(hash,text,U32.is_lt(hash,443308460))
def emojiBelow195(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow196(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{261d}\u{1f3ff}")
    case False{}: emojiBelow195(hash,text,U32.is_lt(hash,451260809))
def emojiBelow197(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9df}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow198(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9d2}")
    case False{}: emojiBelow197(hash,text,U32.is_lt(hash,455621180))
def emojiBelow199(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow196(hash,text,U32.is_lt(hash,450669263))
    case False{}: emojiBelow198(hash,text,U32.is_lt(hash,454844343))
def emojiBelow200(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow194(hash,text,U32.is_lt(hash,442861219))
    case False{}: emojiBelow199(hash,text,U32.is_lt(hash,452120099))
def emojiBelow201(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270a}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fe}")
def emojiBelow202(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{32}\u{fe0f}\u{20e3}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow203(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270d}\u{1f3fd}")
    case False{}: emojiBelow202(hash,text,U32.is_lt(hash,471621962))
def emojiBelow204(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow201(hash,text,U32.is_lt(hash,458849598))
    case False{}: emojiBelow203(hash,text,U32.is_lt(hash,461092023))
def emojiBelow205(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{270a}\u{1f3fc}")
def emojiBelow206(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9bd}")
    case False{}: emojiBelow205(hash,text,U32.is_lt(hash,474076147))
def emojiBelow207(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3ff}")
def emojiBelow208(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270b}\u{1f3fe}")
    case False{}: emojiBelow207(hash,text,U32.is_lt(hash,475627217))
def emojiBelow209(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow206(hash,text,U32.is_lt(hash,473687258))
    case False{}: emojiBelow208(hash,text,U32.is_lt(hash,474663457))
def emojiBelow210(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow204(hash,text,U32.is_lt(hash,460086079))
    case False{}: emojiBelow209(hash,text,U32.is_lt(hash,474238556))
def emojiBelow211(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow200(hash,text,U32.is_lt(hash,448640725))
    case False{}: emojiBelow210(hash,text,U32.is_lt(hash,472362084))
def emojiBelow212(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow190(hash,text,U32.is_lt(hash,426653020))
    case False{}: emojiBelow211(hash,text,U32.is_lt(hash,457298528))
def emojiBelow213(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270d}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2696}\u{fe0f}")
def emojiBelow214(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow215(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{2708}\u{fe0f}")
    case False{}: emojiBelow214(hash,text,U32.is_lt(hash,488514647))
def emojiBelow216(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow213(hash,text,U32.is_lt(hash,478300892))
    case False{}: emojiBelow215(hash,text,U32.is_lt(hash,488399581))
def emojiBelow217(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f4bc}")
def emojiBelow218(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270b}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{270c}\u{1f3ff}")
def emojiBelow219(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270a}\u{1f3ff}")
    case False{}: emojiBelow218(hash,text,U32.is_lt(hash,493478908))
def emojiBelow220(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow217(hash,text,U32.is_lt(hash,490480887))
    case False{}: emojiBelow219(hash,text,U32.is_lt(hash,491016175))
def emojiBelow221(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow216(hash,text,U32.is_lt(hash,486726759))
    case False{}: emojiBelow220(hash,text,U32.is_lt(hash,490853766))
def emojiBelow222(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270d}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f6b5}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow223(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{270a}\u{1f3fe}")
def emojiBelow224(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow223(hash,text,U32.is_lt(hash,507631385))
def emojiBelow225(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow222(hash,text,U32.is_lt(hash,501812304))
    case False{}: emojiBelow224(hash,text,U32.is_lt(hash,507047659))
def emojiBelow226(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270c}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow227(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270b}\u{1f3fc}")
    case False{}: emojiBelow226(hash,text,U32.is_lt(hash,510793484))
def emojiBelow228(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6e1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f3a4}")
def emojiBelow229(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow228(hash,text,U32.is_lt(hash,519116428))
def emojiBelow230(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow227(hash,text,U32.is_lt(hash,510256527))
    case False{}: emojiBelow229(hash,text,U32.is_lt(hash,518452633))
def emojiBelow231(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow225(hash,text,U32.is_lt(hash,503751371))
    case False{}: emojiBelow230(hash,text,U32.is_lt(hash,513621224))
def emojiBelow232(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow221(hash,text,U32.is_lt(hash,489139703))
    case False{}: emojiBelow231(hash,text,U32.is_lt(hash,507793794))
def emojiBelow233(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9dc}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow234(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270b}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f645}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow235(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f373}")
    case False{}: emojiBelow234(hash,text,U32.is_lt(hash,524752800))
def emojiBelow236(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow233(hash,text,U32.is_lt(hash,523932343))
    case False{}: emojiBelow235(hash,text,U32.is_lt(hash,524571413))
def emojiBelow237(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d6}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow238(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270c}\u{1f3fd}")
    case False{}: emojiBelow237(hash,text,U32.is_lt(hash,538935001))
def emojiBelow239(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f3eb}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fb}")
def emojiBelow240(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f37c}")
    case False{}: emojiBelow239(hash,text,U32.is_lt(hash,542737693))
def emojiBelow241(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow238(hash,text,U32.is_lt(hash,532389149))
    case False{}: emojiBelow240(hash,text,U32.is_lt(hash,541119872))
def emojiBelow242(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow236(hash,text,U32.is_lt(hash,524196565))
    case False{}: emojiBelow241(hash,text,U32.is_lt(hash,540974184))
def emojiBelow243(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270c}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f692}")
def emojiBelow244(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f37c}")
def emojiBelow245(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow244(hash,text,U32.is_lt(hash,549794198))
def emojiBelow246(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow243(hash,text,U32.is_lt(hash,545746238))
    case False{}: emojiBelow245(hash,text,U32.is_lt(hash,549427320))
def emojiBelow247(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270a}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f393}")
def emojiBelow248(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fe}\u{200d}\u{1faf2}\u{1f3fb}")
    case False{}: emojiBelow247(hash,text,U32.is_lt(hash,562214353))
def emojiBelow249(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9b8}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow250(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow249(hash,text,U32.is_lt(hash,569802588))
def emojiBelow251(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow248(hash,text,U32.is_lt(hash,557964242))
    case False{}: emojiBelow250(hash,text,U32.is_lt(hash,566204939))
def emojiBelow252(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow246(hash,text,U32.is_lt(hash,548475916))
    case False{}: emojiBelow251(hash,text,U32.is_lt(hash,562760233))
def emojiBelow253(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow242(hash,text,U32.is_lt(hash,527034146))
    case False{}: emojiBelow252(hash,text,U32.is_lt(hash,551541702))
def emojiBelow254(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow232(hash,text,U32.is_lt(hash,493641317))
    case False{}: emojiBelow253(hash,text,U32.is_lt(hash,543811765))
def emojiBelow255(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow212(hash,text,U32.is_lt(hash,441052859))
    case False{}: emojiBelow254(hash,text,U32.is_lt(hash,520910927))
def emojiBelow256(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f46f}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow257(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow258(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow257(hash,text,U32.is_lt(hash,583555905))
def emojiBelow259(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow256(hash,text,U32.is_lt(hash,575352914))
    case False{}: emojiBelow258(hash,text,U32.is_lt(hash,582982558))
def emojiBelow260(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fe}\u{200d}\u{1faf2}\u{1f3fd}")
def emojiBelow261(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9bd}")
def emojiBelow262(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f3a8}")
    case False{}: emojiBelow261(hash,text,U32.is_lt(hash,591809446))
def emojiBelow263(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow260(hash,text,U32.is_lt(hash,585096940))
    case False{}: emojiBelow262(hash,text,U32.is_lt(hash,588910166))
def emojiBelow264(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow259(hash,text,U32.is_lt(hash,581634213))
    case False{}: emojiBelow263(hash,text,U32.is_lt(hash,586226904))
def emojiBelow265(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fe}\u{200d}\u{1faf2}\u{1f3fc}")
def emojiBelow266(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f468}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9bc}")
def emojiBelow267(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f3}\u{fe0f}\u{200d}\u{26a7}\u{fe0f}")
    case False{}: emojiBelow266(hash,text,U32.is_lt(hash,608587065))
def emojiBelow268(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow265(hash,text,U32.is_lt(hash,601874559))
    case False{}: emojiBelow267(hash,text,U32.is_lt(hash,606628606))
def emojiBelow269(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f575}\u{fe0f}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow270(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f4bc}")
    case False{}: emojiBelow269(hash,text,U32.is_lt(hash,613145227))
def emojiBelow271(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fe}\u{200d}\u{1faf2}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{25b6}\u{fe0f}")
def emojiBelow272(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow271(hash,text,U32.is_lt(hash,620929650))
def emojiBelow273(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow270(hash,text,U32.is_lt(hash,611221152))
    case False{}: emojiBelow272(hash,text,U32.is_lt(hash,618652178))
def emojiBelow274(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow268(hash,text,U32.is_lt(hash,605629772))
    case False{}: emojiBelow273(hash,text,U32.is_lt(hash,614009091))
def emojiBelow275(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow264(hash,text,U32.is_lt(hash,584202353))
    case False{}: emojiBelow274(hash,text,U32.is_lt(hash,608896569))
def emojiBelow276(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f33e}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f468}\u{200d}\u{1f466}")
def emojiBelow277(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f4bb}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow278(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow277(hash,text,U32.is_lt(hash,632341733))
def emojiBelow279(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow276(hash,text,U32.is_lt(hash,623406225))
    case False{}: emojiBelow278(hash,text,U32.is_lt(hash,625674188))
def emojiBelow280(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f3ed}")
def emojiBelow281(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow280(hash,text,U32.is_lt(hash,641785586))
def emojiBelow282(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3ff}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f39f}\u{fe0f}")
def emojiBelow283(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{fe0f}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow282(hash,text,U32.is_lt(hash,649062515))
def emojiBelow284(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow281(hash,text,U32.is_lt(hash,634468563))
    case False{}: emojiBelow283(hash,text,U32.is_lt(hash,648013153))
def emojiBelow285(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow279(hash,text,U32.is_lt(hash,624392335))
    case False{}: emojiBelow284(hash,text,U32.is_lt(hash,642093591))
def emojiBelow286(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{24c2}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f396}\u{fe0f}")
def emojiBelow287(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3d8}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2708}\u{fe0f}")
def emojiBelow288(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow287(hash,text,U32.is_lt(hash,661081389))
def emojiBelow289(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow286(hash,text,U32.is_lt(hash,649993042))
    case False{}: emojiBelow288(hash,text,U32.is_lt(hash,658969464))
def emojiBelow290(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2695}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow291(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{fe0f}")
    case False{}: emojiBelow290(hash,text,U32.is_lt(hash,666870653))
def emojiBelow292(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow293(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow292(hash,text,U32.is_lt(hash,675806399))
def emojiBelow294(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow291(hash,text,U32.is_lt(hash,666732129))
    case False{}: emojiBelow293(hash,text,U32.is_lt(hash,673033295))
def emojiBelow295(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow289(hash,text,U32.is_lt(hash,658962904))
    case False{}: emojiBelow294(hash,text,U32.is_lt(hash,670722284))
def emojiBelow296(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow285(hash,text,U32.is_lt(hash,633849563))
    case False{}: emojiBelow295(hash,text,U32.is_lt(hash,661908140))
def emojiBelow297(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow275(hash,text,U32.is_lt(hash,599760177))
    case False{}: emojiBelow296(hash,text,U32.is_lt(hash,649508798))
def emojiBelow298(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f32b}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f325}\u{fe0f}")
def emojiBelow299(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f37d}\u{fe0f}")
def emojiBelow300(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f680}")
    case False{}: emojiBelow299(hash,text,U32.is_lt(hash,686618085))
def emojiBelow301(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow298(hash,text,U32.is_lt(hash,679915389))
    case False{}: emojiBelow300(hash,text,U32.is_lt(hash,681548257))
def emojiBelow302(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f645}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow303(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f3a8}")
    case False{}: emojiBelow302(hash,text,U32.is_lt(hash,697407392))
def emojiBelow304(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9af}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow305(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2708}\u{fe0f}")
    case False{}: emojiBelow304(hash,text,U32.is_lt(hash,707057812))
def emojiBelow306(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow303(hash,text,U32.is_lt(hash,694963098))
    case False{}: emojiBelow305(hash,text,U32.is_lt(hash,704289245))
def emojiBelow307(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow301(hash,text,U32.is_lt(hash,680679921))
    case False{}: emojiBelow306(hash,text,U32.is_lt(hash,703357172))
def emojiBelow308(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow309(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f8}\u{fe0f}")
def emojiBelow310(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2694}\u{fe0f}")
    case False{}: emojiBelow309(hash,text,U32.is_lt(hash,716257688))
def emojiBelow311(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow308(hash,text,U32.is_lt(hash,712691454))
    case False{}: emojiBelow310(hash,text,U32.is_lt(hash,714377949))
def emojiBelow312(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9b0}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow313(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9af}")
    case False{}: emojiBelow312(hash,text,U32.is_lt(hash,721807611))
def emojiBelow314(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow315(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow314(hash,text,U32.is_lt(hash,727730865))
def emojiBelow316(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow313(hash,text,U32.is_lt(hash,721066864))
    case False{}: emojiBelow315(hash,text,U32.is_lt(hash,725016174))
def emojiBelow317(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow311(hash,text,U32.is_lt(hash,712906340))
    case False{}: emojiBelow316(hash,text,U32.is_lt(hash,723835431))
def emojiBelow318(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow307(hash,text,U32.is_lt(hash,691502811))
    case False{}: emojiBelow317(hash,text,U32.is_lt(hash,718975666))
def emojiBelow319(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f33e}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow320(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{271d}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2714}\u{fe0f}")
def emojiBelow321(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow320(hash,text,U32.is_lt(hash,733797860))
def emojiBelow322(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow319(hash,text,U32.is_lt(hash,729212276))
    case False{}: emojiBelow321(hash,text,U32.is_lt(hash,732454661))
def emojiBelow323(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9b1}")
def emojiBelow324(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f373}")
    case False{}: emojiBelow323(hash,text,U32.is_lt(hash,737844483))
def emojiBelow325(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow326(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fd}\u{200d}\u{1faf2}\u{1f3fb}")
    case False{}: emojiBelow325(hash,text,U32.is_lt(hash,741793793))
def emojiBelow327(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow324(hash,text,U32.is_lt(hash,736719080))
    case False{}: emojiBelow326(hash,text,U32.is_lt(hash,740613050))
def emojiBelow328(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow322(hash,text,U32.is_lt(hash,730651968))
    case False{}: emojiBelow327(hash,text,U32.is_lt(hash,738741433))
def emojiBelow329(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f486}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow330(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{3297}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow331(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{261d}\u{fe0f}")
    case False{}: emojiBelow330(hash,text,U32.is_lt(hash,745197587))
def emojiBelow332(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow329(hash,text,U32.is_lt(hash,743101335))
    case False{}: emojiBelow331(hash,text,U32.is_lt(hash,744826715))
def emojiBelow333(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9b2}")
def emojiBelow334(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2601}\u{fe0f}")
    case False{}: emojiBelow333(hash,text,U32.is_lt(hash,754622102))
def emojiBelow335(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d9}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow336(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fd}\u{200d}\u{1faf2}\u{1f3fc}")
    case False{}: emojiBelow335(hash,text,U32.is_lt(hash,757260172))
def emojiBelow337(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow334(hash,text,U32.is_lt(hash,753849370))
    case False{}: emojiBelow336(hash,text,U32.is_lt(hash,757164606))
def emojiBelow338(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow332(hash,text,U32.is_lt(hash,743906309))
    case False{}: emojiBelow337(hash,text,U32.is_lt(hash,755519052))
def emojiBelow339(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow328(hash,text,U32.is_lt(hash,734348007))
    case False{}: emojiBelow338(hash,text,U32.is_lt(hash,747257657))
def emojiBelow340(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow318(hash,text,U32.is_lt(hash,708238555))
    case False{}: emojiBelow339(hash,text,U32.is_lt(hash,742718284))
def emojiBelow341(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow297(hash,text,U32.is_lt(hash,623189630))
    case False{}: emojiBelow340(hash,text,U32.is_lt(hash,729184076))
def emojiBelow342(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow255(hash,text,U32.is_lt(hash,476863698))
    case False{}: emojiBelow341(hash,text,U32.is_lt(hash,677641663))
def emojiBelow343(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow170(hash,text,U32.is_lt(hash,222989090))
    case False{}: emojiBelow342(hash,text,U32.is_lt(hash,574258184))
def emojiBelow344(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9b3}")
def emojiBelow345(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9b3}")
    case False{}: Strings.equal(text,"\u{21a9}\u{fe0f}")
def emojiBelow346(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow345(hash,text,U32.is_lt(hash,772485457))
def emojiBelow347(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow344(hash,text,U32.is_lt(hash,759585636))
    case False{}: emojiBelow346(hash,text,U32.is_lt(hash,771399721))
def emojiBelow348(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9af}")
def emojiBelow349(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2194}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f680}")
def emojiBelow350(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9b2}")
    case False{}: emojiBelow349(hash,text,U32.is_lt(hash,780632904))
def emojiBelow351(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow348(hash,text,U32.is_lt(hash,774359226))
    case False{}: emojiBelow350(hash,text,U32.is_lt(hash,777180004))
def emojiBelow352(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow347(hash,text,U32.is_lt(hash,762597739))
    case False{}: emojiBelow351(hash,text,U32.is_lt(hash,776363255))
def emojiBelow353(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fd}\u{200d}\u{1faf2}\u{1f3fe}")
def emojiBelow354(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow355(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9d1}\u{200d}\u{1f9d2}")
    case False{}: emojiBelow354(hash,text,U32.is_lt(hash,795530444))
def emojiBelow356(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow353(hash,text,U32.is_lt(hash,789074290))
    case False{}: emojiBelow355(hash,text,U32.is_lt(hash,793140874))
def emojiBelow357(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fd}\u{200d}\u{1faf2}\u{1f3ff}")
def emojiBelow358(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow357(hash,text,U32.is_lt(hash,805851909))
def emojiBelow359(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9b0}")
def emojiBelow360(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow359(hash,text,U32.is_lt(hash,809918493))
def emojiBelow361(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow358(hash,text,U32.is_lt(hash,804022715))
    case False{}: emojiBelow360(hash,text,U32.is_lt(hash,808904269))
def emojiBelow362(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow356(hash,text,U32.is_lt(hash,790464457))
    case False{}: emojiBelow361(hash,text,U32.is_lt(hash,805985394))
def emojiBelow363(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow352(hash,text,U32.is_lt(hash,773569193))
    case False{}: emojiBelow362(hash,text,U32.is_lt(hash,799511053))
def emojiBelow364(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f64d}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow365(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f3a4}")
def emojiBelow366(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow365(hash,text,U32.is_lt(hash,820196037))
def emojiBelow367(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow364(hash,text,U32.is_lt(hash,815907369))
    case False{}: emojiBelow366(hash,text,U32.is_lt(hash,819879177))
def emojiBelow368(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow369(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9af}")
    case False{}: emojiBelow368(hash,text,U32.is_lt(hash,829085682))
def emojiBelow370(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9cf}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow371(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow370(hash,text,U32.is_lt(hash,840223461))
def emojiBelow372(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow369(hash,text,U32.is_lt(hash,828868367))
    case False{}: emojiBelow371(hash,text,U32.is_lt(hash,839030511))
def emojiBelow373(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow367(hash,text,U32.is_lt(hash,817123565))
    case False{}: emojiBelow372(hash,text,U32.is_lt(hash,835799991))
def emojiBelow374(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow375(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f384}")
def emojiBelow376(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow375(hash,text,U32.is_lt(hash,847433876))
def emojiBelow377(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow374(hash,text,U32.is_lt(hash,843145707))
    case False{}: emojiBelow376(hash,text,U32.is_lt(hash,845863301))
def emojiBelow378(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{23f8}\u{fe0f}")
def emojiBelow379(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow378(hash,text,U32.is_lt(hash,855431320))
def emojiBelow380(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f52c}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f692}")
def emojiBelow381(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d8}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow380(hash,text,U32.is_lt(hash,859794841))
def emojiBelow382(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow379(hash,text,U32.is_lt(hash,848019647))
    case False{}: emojiBelow381(hash,text,U32.is_lt(hash,857457288))
def emojiBelow383(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow377(hash,text,U32.is_lt(hash,845367196))
    case False{}: emojiBelow382(hash,text,U32.is_lt(hash,857117436))
def emojiBelow384(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow373(hash,text,U32.is_lt(hash,826696112))
    case False{}: emojiBelow383(hash,text,U32.is_lt(hash,847787547))
def emojiBelow385(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow363(hash,text,U32.is_lt(hash,782251704))
    case False{}: emojiBelow384(hash,text,U32.is_lt(hash,842501382))
def emojiBelow386(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f43b}\u{200d}\u{2744}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{30}\u{fe0f}\u{20e3}")
def emojiBelow387(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2696}\u{fe0f}")
def emojiBelow388(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow387(hash,text,U32.is_lt(hash,881319735))
def emojiBelow389(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow386(hash,text,U32.is_lt(hash,868797369))
    case False{}: emojiBelow388(hash,text,U32.is_lt(hash,873774979))
def emojiBelow390(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f935}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow391(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6b4}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow392(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f3a8}")
    case False{}: emojiBelow391(hash,text,U32.is_lt(hash,891021838))
def emojiBelow393(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow390(hash,text,U32.is_lt(hash,884482078))
    case False{}: emojiBelow392(hash,text,U32.is_lt(hash,889274332))
def emojiBelow394(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow389(hash,text,U32.is_lt(hash,870323180))
    case False{}: emojiBelow393(hash,text,U32.is_lt(hash,887306513))
def emojiBelow395(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9b8}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow396(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f4bb}")
def emojiBelow397(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow396(hash,text,U32.is_lt(hash,904485825))
def emojiBelow398(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow395(hash,text,U32.is_lt(hash,896433112))
    case False{}: emojiBelow397(hash,text,U32.is_lt(hash,899304808))
def emojiBelow399(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow400(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9af}")
    case False{}: emojiBelow399(hash,text,U32.is_lt(hash,910256183))
def emojiBelow401(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow402(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow401(hash,text,U32.is_lt(hash,912601862))
def emojiBelow403(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow400(hash,text,U32.is_lt(hash,909872461))
    case False{}: emojiBelow402(hash,text,U32.is_lt(hash,910605937))
def emojiBelow404(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow398(hash,text,U32.is_lt(hash,896871102))
    case False{}: emojiBelow403(hash,text,U32.is_lt(hash,910339720))
def emojiBelow405(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow394(hash,text,U32.is_lt(hash,882121123))
    case False{}: emojiBelow404(hash,text,U32.is_lt(hash,905115812))
def emojiBelow406(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f692}")
    case False{}: Strings.equal(text,"\u{1f5d2}\u{fe0f}")
def emojiBelow407(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f4bc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9bc}")
def emojiBelow408(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow407(hash,text,U32.is_lt(hash,922398292))
def emojiBelow409(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow406(hash,text,U32.is_lt(hash,917758286))
    case False{}: emojiBelow408(hash,text,U32.is_lt(hash,921263444))
def emojiBelow410(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow411(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow410(hash,text,U32.is_lt(hash,927033802))
def emojiBelow412(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2708}\u{fe0f}")
def emojiBelow413(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow412(hash,text,U32.is_lt(hash,928858082))
def emojiBelow414(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow411(hash,text,U32.is_lt(hash,924473449))
    case False{}: emojiBelow413(hash,text,U32.is_lt(hash,927750102))
def emojiBelow415(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow409(hash,text,U32.is_lt(hash,919791254))
    case False{}: emojiBelow414(hash,text,U32.is_lt(hash,927189383))
def emojiBelow416(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow417(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow418(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow417(hash,text,U32.is_lt(hash,943811421))
def emojiBelow419(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow416(hash,text,U32.is_lt(hash,933123757))
    case False{}: emojiBelow418(hash,text,U32.is_lt(hash,939175911))
def emojiBelow420(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f577}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow421(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f3a8}")
    case False{}: emojiBelow420(hash,text,U32.is_lt(hash,949152045))
def emojiBelow422(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9bd}")
def emojiBelow423(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow422(hash,text,U32.is_lt(hash,953862332))
def emojiBelow424(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow421(hash,text,U32.is_lt(hash,945406907))
    case False{}: emojiBelow423(hash,text,U32.is_lt(hash,952579673))
def emojiBelow425(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow419(hash,text,U32.is_lt(hash,937269592))
    case False{}: emojiBelow424(hash,text,U32.is_lt(hash,950474800))
def emojiBelow426(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow415(hash,text,U32.is_lt(hash,924202154))
    case False{}: emojiBelow425(hash,text,U32.is_lt(hash,945171125))
def emojiBelow427(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow405(hash,text,U32.is_lt(hash,893478564))
    case False{}: emojiBelow426(hash,text,U32.is_lt(hash,929900982))
def emojiBelow428(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow385(hash,text,U32.is_lt(hash,812308063))
    case False{}: emojiBelow427(hash,text,U32.is_lt(hash,915566587))
def emojiBelow429(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f4bb}")
def emojiBelow430(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f4bc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9bc}")
def emojiBelow431(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow430(hash,text,U32.is_lt(hash,970639951))
def emojiBelow432(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow429(hash,text,U32.is_lt(hash,965393049))
    case False{}: emojiBelow431(hash,text,U32.is_lt(hash,970124111))
def emojiBelow433(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9b3}")
    case False{}: Strings.equal(text,"\u{1f93c}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow434(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f4bc}")
def emojiBelow435(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow434(hash,text,U32.is_lt(hash,982170668))
def emojiBelow436(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow433(hash,text,U32.is_lt(hash,975729938))
    case False{}: emojiBelow435(hash,text,U32.is_lt(hash,981035820))
def emojiBelow437(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow432(hash,text,U32.is_lt(hash,967545679))
    case False{}: emojiBelow436(hash,text,U32.is_lt(hash,976973381))
def emojiBelow438(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{37}\u{fe0f}\u{20e3}")
def emojiBelow439(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{1f575}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow440(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f680}")
    case False{}: emojiBelow439(hash,text,U32.is_lt(hash,990743273))
def emojiBelow441(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow438(hash,text,U32.is_lt(hash,983134152))
    case False{}: emojiBelow440(hash,text,U32.is_lt(hash,989003907))
def emojiBelow442(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9bd}")
def emojiBelow443(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: emojiBelow442(hash,text,U32.is_lt(hash,997813439))
def emojiBelow444(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f3a4}")
def emojiBelow445(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow444(hash,text,U32.is_lt(hash,1012281601))
def emojiBelow446(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow443(hash,text,U32.is_lt(hash,997050328))
    case False{}: emojiBelow445(hash,text,U32.is_lt(hash,1005781526))
def emojiBelow447(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow441(hash,text,U32.is_lt(hash,988580256))
    case False{}: emojiBelow446(hash,text,U32.is_lt(hash,998252023))
def emojiBelow448(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow437(hash,text,U32.is_lt(hash,972226288))
    case False{}: emojiBelow447(hash,text,U32.is_lt(hash,993130814))
def emojiBelow449(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow450(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow451(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow450(hash,text,U32.is_lt(hash,1020928512))
def emojiBelow452(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow449(hash,text,U32.is_lt(hash,1014919123))
    case False{}: emojiBelow451(hash,text,U32.is_lt(hash,1018298969))
def emojiBelow453(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9b0}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fd}")
def emojiBelow454(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9b1}")
    case False{}: emojiBelow453(hash,text,U32.is_lt(hash,1026686052))
def emojiBelow455(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f52c}")
def emojiBelow456(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow455(hash,text,U32.is_lt(hash,1031387554))
def emojiBelow457(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow454(hash,text,U32.is_lt(hash,1022559145))
    case False{}: emojiBelow456(hash,text,U32.is_lt(hash,1028540054))
def emojiBelow458(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow452(hash,text,U32.is_lt(hash,1018241376))
    case False{}: emojiBelow457(hash,text,U32.is_lt(hash,1026781869))
def emojiBelow459(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow460(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9db}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow461(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9b0}")
    case False{}: emojiBelow460(hash,text,U32.is_lt(hash,1043463671))
def emojiBelow462(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow459(hash,text,U32.is_lt(hash,1037706131))
    case False{}: emojiBelow461(hash,text,U32.is_lt(hash,1037889106))
def emojiBelow463(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9b0}")
def emojiBelow464(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f692}")
    case False{}: emojiBelow463(hash,text,U32.is_lt(hash,1048146296))
def emojiBelow465(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6e0}\u{fe0f}")
def emojiBelow466(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9b3}")
    case False{}: emojiBelow465(hash,text,U32.is_lt(hash,1055070864))
def emojiBelow467(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow464(hash,text,U32.is_lt(hash,1044839967))
    case False{}: emojiBelow466(hash,text,U32.is_lt(hash,1054529437))
def emojiBelow468(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow462(hash,text,U32.is_lt(hash,1037750427))
    case False{}: emojiBelow467(hash,text,U32.is_lt(hash,1054528046))
def emojiBelow469(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow458(hash,text,U32.is_lt(hash,1020972808))
    case False{}: emojiBelow468(hash,text,U32.is_lt(hash,1044715426))
def emojiBelow470(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow448(hash,text,U32.is_lt(hash,982339254))
    case False{}: emojiBelow469(hash,text,U32.is_lt(hash,1035018995))
def emojiBelow471(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f3}\u{fe0f}\u{200d}\u{1f308}")
    case False{}: Strings.equal(text,"\u{1f6cf}\u{fe0f}")
def emojiBelow472(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f9dc}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow473(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow472(hash,text,U32.is_lt(hash,1061855154))
def emojiBelow474(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow471(hash,text,U32.is_lt(hash,1059352739))
    case False{}: emojiBelow473(hash,text,U32.is_lt(hash,1060977205))
def emojiBelow475(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow476(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9b2}")
def emojiBelow477(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow476(hash,text,U32.is_lt(hash,1071305665))
def emojiBelow478(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow475(hash,text,U32.is_lt(hash,1068574233))
    case False{}: emojiBelow477(hash,text,U32.is_lt(hash,1071261369))
def emojiBelow479(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow474(hash,text,U32.is_lt(hash,1060241290))
    case False{}: emojiBelow478(hash,text,U32.is_lt(hash,1068656700))
def emojiBelow480(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9b2}")
def emojiBelow481(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow482(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f373}")
    case False{}: emojiBelow481(hash,text,U32.is_lt(hash,1090874385))
def emojiBelow483(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow480(hash,text,U32.is_lt(hash,1081701534))
    case False{}: emojiBelow482(hash,text,U32.is_lt(hash,1085351852))
def emojiBelow484(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9b3}")
def emojiBelow485(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow484(hash,text,U32.is_lt(hash,1098479153))
def emojiBelow486(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f4bb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f33e}")
def emojiBelow487(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow486(hash,text,U32.is_lt(hash,1106601323))
def emojiBelow488(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow485(hash,text,U32.is_lt(hash,1094681507))
    case False{}: emojiBelow487(hash,text,U32.is_lt(hash,1105931400))
def emojiBelow489(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow483(hash,text,U32.is_lt(hash,1083939683))
    case False{}: emojiBelow488(hash,text,U32.is_lt(hash,1103693670))
def emojiBelow490(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow479(hash,text,U32.is_lt(hash,1064923915))
    case False{}: emojiBelow489(hash,text,U32.is_lt(hash,1092567974))
def emojiBelow491(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f9}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow492(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow493(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow492(hash,text,U32.is_lt(hash,1119170957))
def emojiBelow494(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow491(hash,text,U32.is_lt(hash,1110171024))
    case False{}: emojiBelow493(hash,text,U32.is_lt(hash,1118950358))
def emojiBelow495(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f469}\u{200d}\u{1f467}\u{200d}\u{1f466}")
def emojiBelow496(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow495(hash,text,U32.is_lt(hash,1136604274))
def emojiBelow497(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f3eb}")
def emojiBelow498(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: emojiBelow497(hash,text,U32.is_lt(hash,1142667990))
def emojiBelow499(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow496(hash,text,U32.is_lt(hash,1132616396))
    case False{}: emojiBelow498(hash,text,U32.is_lt(hash,1139043061))
def emojiBelow500(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow494(hash,text,U32.is_lt(hash,1111446371))
    case False{}: emojiBelow499(hash,text,U32.is_lt(hash,1138371845))
def emojiBelow501(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64b}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow502(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f469}\u{200d}\u{1f467}\u{200d}\u{1f467}")
def emojiBelow503(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow502(hash,text,U32.is_lt(hash,1153381893))
def emojiBelow504(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow501(hash,text,U32.is_lt(hash,1146283902))
    case False{}: emojiBelow503(hash,text,U32.is_lt(hash,1148830887))
def emojiBelow505(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f384}")
    case False{}: Strings.equal(text,"\u{1f9cf}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow506(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow505(hash,text,U32.is_lt(hash,1162262409))
def emojiBelow507(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f3ed}")
def emojiBelow508(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{25fb}\u{fe0f}")
    case False{}: emojiBelow507(hash,text,U32.is_lt(hash,1176223228))
def emojiBelow509(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow506(hash,text,U32.is_lt(hash,1161276143))
    case False{}: emojiBelow508(hash,text,U32.is_lt(hash,1172940857))
def emojiBelow510(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow504(hash,text,U32.is_lt(hash,1146717397))
    case False{}: emojiBelow509(hash,text,U32.is_lt(hash,1166524303))
def emojiBelow511(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow500(hash,text,U32.is_lt(hash,1127526494))
    case False{}: emojiBelow510(hash,text,U32.is_lt(hash,1156284908))
def emojiBelow512(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow490(hash,text,U32.is_lt(hash,1072570670))
    case False{}: emojiBelow511(hash,text,U32.is_lt(hash,1144692015))
def emojiBelow513(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow470(hash,text,U32.is_lt(hash,1013183685))
    case False{}: emojiBelow512(hash,text,U32.is_lt(hash,1108453421))
def emojiBelow514(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow428(hash,text,U32.is_lt(hash,864962884))
    case False{}: emojiBelow513(hash,text,U32.is_lt(hash,1058540402))
def emojiBelow515(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f37c}")
def emojiBelow516(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3db}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow517(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f7}\u{fe0f}")
    case False{}: emojiBelow516(hash,text,U32.is_lt(hash,1197055851))
def emojiBelow518(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow515(hash,text,U32.is_lt(hash,1184725984))
    case False{}: emojiBelow517(hash,text,U32.is_lt(hash,1195587695))
def emojiBelow519(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3d5}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64d}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow520(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f575}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow521(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow520(hash,text,U32.is_lt(hash,1211051024))
def emojiBelow522(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow519(hash,text,U32.is_lt(hash,1204602081))
    case False{}: emojiBelow521(hash,text,U32.is_lt(hash,1207112954))
def emojiBelow523(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow518(hash,text,U32.is_lt(hash,1192236347))
    case False{}: emojiBelow522(hash,text,U32.is_lt(hash,1206981713))
def emojiBelow524(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f3a4}")
    case False{}: Strings.equal(text,"\u{1f32a}\u{fe0f}")
def emojiBelow525(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f324}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f4bc}")
def emojiBelow526(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow525(hash,text,U32.is_lt(hash,1223374733))
def emojiBelow527(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow524(hash,text,U32.is_lt(hash,1214259894))
    case False{}: emojiBelow526(hash,text,U32.is_lt(hash,1216533620))
def emojiBelow528(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f527}")
def emojiBelow529(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow528(hash,text,U32.is_lt(hash,1224250992))
def emojiBelow530(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{2744}\u{fe0f}")
def emojiBelow531(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow530(hash,text,U32.is_lt(hash,1230852372))
def emojiBelow532(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow529(hash,text,U32.is_lt(hash,1223890573))
    case False{}: emojiBelow531(hash,text,U32.is_lt(hash,1230448680))
def emojiBelow533(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow527(hash,text,U32.is_lt(hash,1215545886))
    case False{}: emojiBelow532(hash,text,U32.is_lt(hash,1224493753))
def emojiBelow534(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow523(hash,text,U32.is_lt(hash,1197861421))
    case False{}: emojiBelow533(hash,text,U32.is_lt(hash,1223375871))
def emojiBelow535(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow536(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64b}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow537(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{3030}\u{fe0f}")
    case False{}: emojiBelow536(hash,text,U32.is_lt(hash,1243479445))
def emojiBelow538(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow535(hash,text,U32.is_lt(hash,1231986846))
    case False{}: emojiBelow537(hash,text,U32.is_lt(hash,1238972640))
def emojiBelow539(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow540(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow539(hash,text,U32.is_lt(hash,1247698534))
def emojiBelow541(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2697}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow542(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{269c}\u{fe0f}")
    case False{}: emojiBelow541(hash,text,U32.is_lt(hash,1249525804))
def emojiBelow543(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow540(hash,text,U32.is_lt(hash,1247388708))
    case False{}: emojiBelow542(hash,text,U32.is_lt(hash,1249524571))
def emojiBelow544(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow538(hash,text,U32.is_lt(hash,1232850400))
    case False{}: emojiBelow543(hash,text,U32.is_lt(hash,1248594044))
def emojiBelow545(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3ff}")
def emojiBelow546(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9dd}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow547(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow546(hash,text,U32.is_lt(hash,1272899622))
def emojiBelow548(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow545(hash,text,U32.is_lt(hash,1264166327))
    case False{}: emojiBelow547(hash,text,U32.is_lt(hash,1265830507))
def emojiBelow549(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{2623}\u{fe0f}")
def emojiBelow550(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow549(hash,text,U32.is_lt(hash,1278516391))
def emojiBelow551(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f486}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow552(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow551(hash,text,U32.is_lt(hash,1281024146))
def emojiBelow553(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow550(hash,text,U32.is_lt(hash,1278292734))
    case False{}: emojiBelow552(hash,text,U32.is_lt(hash,1280943946))
def emojiBelow554(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow548(hash,text,U32.is_lt(hash,1265653676))
    case False{}: emojiBelow553(hash,text,U32.is_lt(hash,1280781537))
def emojiBelow555(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow544(hash,text,U32.is_lt(hash,1247226299))
    case False{}: emojiBelow554(hash,text,U32.is_lt(hash,1275232184))
def emojiBelow556(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow534(hash,text,U32.is_lt(hash,1211975776))
    case False{}: emojiBelow555(hash,text,U32.is_lt(hash,1264003918))
def emojiBelow557(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{260e}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2600}\u{fe0f}")
def emojiBelow558(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{267b}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3cc}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow559(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow558(hash,text,U32.is_lt(hash,1285479742))
def emojiBelow560(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow557(hash,text,U32.is_lt(hash,1283875888))
    case False{}: emojiBelow559(hash,text,U32.is_lt(hash,1284806415))
def emojiBelow561(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2666}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f680}")
def emojiBelow562(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9da}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow563(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f692}")
    case False{}: emojiBelow562(hash,text,U32.is_lt(hash,1291494200))
def emojiBelow564(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow561(hash,text,U32.is_lt(hash,1290278873))
    case False{}: emojiBelow563(hash,text,U32.is_lt(hash,1290858464))
def emojiBelow565(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow560(hash,text,U32.is_lt(hash,1284611848))
    case False{}: emojiBelow564(hash,text,U32.is_lt(hash,1290577398))
def emojiBelow566(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26b1}\u{fe0f}")
def emojiBelow567(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9af}")
def emojiBelow568(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f393}")
    case False{}: emojiBelow567(hash,text,U32.is_lt(hash,1299810581))
def emojiBelow569(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow566(hash,text,U32.is_lt(hash,1297134185))
    case False{}: emojiBelow568(hash,text,U32.is_lt(hash,1297721565))
def emojiBelow570(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f680}")
def emojiBelow571(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f37c}")
    case False{}: emojiBelow570(hash,text,U32.is_lt(hash,1305882827))
def emojiBelow572(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fd}")
def emojiBelow573(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow572(hash,text,U32.is_lt(hash,1311847972))
def emojiBelow574(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow571(hash,text,U32.is_lt(hash,1302224360))
    case False{}: emojiBelow573(hash,text,U32.is_lt(hash,1307664521))
def emojiBelow575(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow569(hash,text,U32.is_lt(hash,1297501124))
    case False{}: emojiBelow574(hash,text,U32.is_lt(hash,1306526379))
def emojiBelow576(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow565(hash,text,U32.is_lt(hash,1289500962))
    case False{}: emojiBelow575(hash,text,U32.is_lt(hash,1302048730))
def emojiBelow577(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f692}")
    case False{}: Strings.equal(text,"\u{2197}\u{fe0f}")
def emojiBelow578(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d9}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow579(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f33e}")
    case False{}: emojiBelow578(hash,text,U32.is_lt(hash,1328055273))
def emojiBelow580(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow577(hash,text,U32.is_lt(hash,1313798235))
    case False{}: emojiBelow579(hash,text,U32.is_lt(hash,1323998056))
def emojiBelow581(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9af}")
def emojiBelow582(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow581(hash,text,U32.is_lt(hash,1337620115))
def emojiBelow583(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f52c}")
def emojiBelow584(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow583(hash,text,U32.is_lt(hash,1354865593))
def emojiBelow585(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow582(hash,text,U32.is_lt(hash,1331114394))
    case False{}: emojiBelow584(hash,text,U32.is_lt(hash,1345403210))
def emojiBelow586(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow580(hash,text,U32.is_lt(hash,1317153700))
    case False{}: emojiBelow585(hash,text,U32.is_lt(hash,1339405237))
def emojiBelow587(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f384}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow588(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{23}\u{fe0f}\u{20e3}")
    case False{}: Strings.equal(text,"\u{1f486}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow589(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow588(hash,text,U32.is_lt(hash,1361089650))
def emojiBelow590(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow587(hash,text,U32.is_lt(hash,1357604198))
    case False{}: emojiBelow589(hash,text,U32.is_lt(hash,1360292108))
def emojiBelow591(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f680}")
def emojiBelow592(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow591(hash,text,U32.is_lt(hash,1369571878))
def emojiBelow593(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow594(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow593(hash,text,U32.is_lt(hash,1377460529))
def emojiBelow595(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow592(hash,text,U32.is_lt(hash,1363149750))
    case False{}: emojiBelow594(hash,text,U32.is_lt(hash,1374477716))
def emojiBelow596(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow590(hash,text,U32.is_lt(hash,1358728866))
    case False{}: emojiBelow595(hash,text,U32.is_lt(hash,1369641871))
def emojiBelow597(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow586(hash,text,U32.is_lt(hash,1328625591))
    case False{}: emojiBelow596(hash,text,U32.is_lt(hash,1362180829))
def emojiBelow598(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow576(hash,text,U32.is_lt(hash,1292677691))
    case False{}: emojiBelow597(hash,text,U32.is_lt(hash,1357079845))
def emojiBelow599(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow556(hash,text,U32.is_lt(hash,1231448618))
    case False{}: emojiBelow598(hash,text,U32.is_lt(hash,1313762903))
def emojiBelow600(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow601(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1fa70}")
    case False{}: Strings.equal(text,"\u{1f426}\u{200d}\u{1f525}")
def emojiBelow602(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow601(hash,text,U32.is_lt(hash,1387330139))
def emojiBelow603(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow600(hash,text,U32.is_lt(hash,1379832800))
    case False{}: emojiBelow602(hash,text,U32.is_lt(hash,1386985924))
def emojiBelow604(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fb}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow605(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{23ef}\u{fe0f}")
def emojiBelow606(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{23f2}\u{fe0f}")
    case False{}: emojiBelow605(hash,text,U32.is_lt(hash,1395400899))
def emojiBelow607(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow604(hash,text,U32.is_lt(hash,1391712085))
    case False{}: emojiBelow606(hash,text,U32.is_lt(hash,1393477306))
def emojiBelow608(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow603(hash,text,U32.is_lt(hash,1384317684))
    case False{}: emojiBelow607(hash,text,U32.is_lt(hash,1393392750))
def emojiBelow609(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{35}\u{fe0f}\u{20e3}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9af}")
def emojiBelow610(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f467}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow611(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow610(hash,text,U32.is_lt(hash,1406142697))
def emojiBelow612(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow609(hash,text,U32.is_lt(hash,1397148593))
    case False{}: emojiBelow611(hash,text,U32.is_lt(hash,1404338116))
def emojiBelow613(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f52c}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f3a8}")
def emojiBelow614(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2696}\u{fe0f}")
    case False{}: emojiBelow613(hash,text,U32.is_lt(hash,1413307204))
def emojiBelow615(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2328}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow616(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow615(hash,text,U32.is_lt(hash,1418740487))
def emojiBelow617(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow614(hash,text,U32.is_lt(hash,1408804801))
    case False{}: emojiBelow616(hash,text,U32.is_lt(hash,1416759496))
def emojiBelow618(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow612(hash,text,U32.is_lt(hash,1397191910))
    case False{}: emojiBelow617(hash,text,U32.is_lt(hash,1416190597))
def emojiBelow619(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow608(hash,text,U32.is_lt(hash,1391143797))
    case False{}: emojiBelow618(hash,text,U32.is_lt(hash,1406941411))
def emojiBelow620(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f37c}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f467}\u{200d}\u{1f466}")
def emojiBelow621(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f46e}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow622(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f3a8}")
    case False{}: emojiBelow621(hash,text,U32.is_lt(hash,1435049790))
def emojiBelow623(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow620(hash,text,U32.is_lt(hash,1421115735))
    case False{}: emojiBelow622(hash,text,U32.is_lt(hash,1422920316))
def emojiBelow624(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f527}")
def emojiBelow625(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f373}")
    case False{}: emojiBelow624(hash,text,U32.is_lt(hash,1438753688))
def emojiBelow626(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow627(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow626(hash,text,U32.is_lt(hash,1449579709))
def emojiBelow628(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow625(hash,text,U32.is_lt(hash,1436390269))
    case False{}: emojiBelow627(hash,text,U32.is_lt(hash,1449335961))
def emojiBelow629(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow623(hash,text,U32.is_lt(hash,1422127218))
    case False{}: emojiBelow628(hash,text,U32.is_lt(hash,1439697935))
def emojiBelow630(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9b8}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow631(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow632(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5dd}\u{fe0f}")
    case False{}: emojiBelow631(hash,text,U32.is_lt(hash,1456559790))
def emojiBelow633(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow630(hash,text,U32.is_lt(hash,1452638283))
    case False{}: emojiBelow632(hash,text,U32.is_lt(hash,1456475554))
def emojiBelow634(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow635(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow634(hash,text,U32.is_lt(hash,1471995215))
def emojiBelow636(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2696}\u{fe0f}")
def emojiBelow637(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow636(hash,text,U32.is_lt(hash,1478509556))
def emojiBelow638(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow635(hash,text,U32.is_lt(hash,1467945935))
    case False{}: emojiBelow637(hash,text,U32.is_lt(hash,1473253173))
def emojiBelow639(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow633(hash,text,U32.is_lt(hash,1454789189))
    case False{}: emojiBelow638(hash,text,U32.is_lt(hash,1472438408))
def emojiBelow640(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow629(hash,text,U32.is_lt(hash,1436085448))
    case False{}: emojiBelow639(hash,text,U32.is_lt(hash,1460668822))
def emojiBelow641(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow619(hash,text,U32.is_lt(hash,1396371882))
    case False{}: emojiBelow640(hash,text,U32.is_lt(hash,1449777237))
def emojiBelow642(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f576}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow643(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow644(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow643(hash,text,U32.is_lt(hash,1497619574))
def emojiBelow645(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow642(hash,text,U32.is_lt(hash,1483271656))
    case False{}: emojiBelow644(hash,text,U32.is_lt(hash,1494886318))
def emojiBelow646(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f58b}\u{fe0f}")
def emojiBelow647(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f590}\u{fe0f}")
    case False{}: emojiBelow646(hash,text,U32.is_lt(hash,1498634783))
def emojiBelow648(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow649(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{2708}\u{fe0f}")
    case False{}: emojiBelow648(hash,text,U32.is_lt(hash,1506759083))
def emojiBelow650(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow647(hash,text,U32.is_lt(hash,1498135105))
    case False{}: emojiBelow649(hash,text,U32.is_lt(hash,1506460449))
def emojiBelow651(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow645(hash,text,U32.is_lt(hash,1489237694))
    case False{}: emojiBelow650(hash,text,U32.is_lt(hash,1501870554))
def emojiBelow652(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f393}")
def emojiBelow653(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f692}")
def emojiBelow654(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow653(hash,text,U32.is_lt(hash,1525165539))
def emojiBelow655(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow652(hash,text,U32.is_lt(hash,1508078416))
    case False{}: emojiBelow654(hash,text,U32.is_lt(hash,1523536702))
def emojiBelow656(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow657(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow656(hash,text,U32.is_lt(hash,1528441556))
def emojiBelow658(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow659(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow658(hash,text,U32.is_lt(hash,1542895530))
def emojiBelow660(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow657(hash,text,U32.is_lt(hash,1525935168))
    case False{}: emojiBelow659(hash,text,U32.is_lt(hash,1540314321))
def emojiBelow661(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow655(hash,text,U32.is_lt(hash,1522014139))
    case False{}: emojiBelow660(hash,text,U32.is_lt(hash,1530494248))
def emojiBelow662(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow651(hash,text,U32.is_lt(hash,1497704256))
    case False{}: emojiBelow661(hash,text,U32.is_lt(hash,1525757088))
def emojiBelow663(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f9}\u{fe0f}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow664(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9b0}")
def emojiBelow665(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow664(hash,text,U32.is_lt(hash,1548147164))
def emojiBelow666(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow663(hash,text,U32.is_lt(hash,1545219175))
    case False{}: emojiBelow665(hash,text,U32.is_lt(hash,1547899919))
def emojiBelow667(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f469}\u{200d}\u{1f467}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{1f441}\u{fe0f}")
def emojiBelow668(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow667(hash,text,U32.is_lt(hash,1553932025))
def emojiBelow669(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3ff}")
def emojiBelow670(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow669(hash,text,U32.is_lt(hash,1561996794))
def emojiBelow671(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow668(hash,text,U32.is_lt(hash,1551827630))
    case False{}: emojiBelow670(hash,text,U32.is_lt(hash,1561664847))
def emojiBelow672(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow666(hash,text,U32.is_lt(hash,1547520791))
    case False{}: emojiBelow671(hash,text,U32.is_lt(hash,1557485516))
def emojiBelow673(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f469}\u{200d}\u{1f467}\u{200d}\u{1f466}")
def emojiBelow674(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{1f575}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow675(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f426}\u{200d}\u{2b1b}")
    case False{}: emojiBelow674(hash,text,U32.is_lt(hash,1584258541))
def emojiBelow676(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow673(hash,text,U32.is_lt(hash,1568605249))
    case False{}: emojiBelow675(hash,text,U32.is_lt(hash,1581702402))
def emojiBelow677(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow678(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow677(hash,text,U32.is_lt(hash,1588007764))
def emojiBelow679(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f33e}")
def emojiBelow680(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow679(hash,text,U32.is_lt(hash,1589545923))
def emojiBelow681(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow678(hash,text,U32.is_lt(hash,1585425477))
    case False{}: emojiBelow680(hash,text,U32.is_lt(hash,1589499137))
def emojiBelow682(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow676(hash,text,U32.is_lt(hash,1579092581))
    case False{}: emojiBelow681(hash,text,U32.is_lt(hash,1589124615))
def emojiBelow683(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow672(hash,text,U32.is_lt(hash,1548394512))
    case False{}: emojiBelow682(hash,text,U32.is_lt(hash,1584784823))
def emojiBelow684(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow662(hash,text,U32.is_lt(hash,1507939534))
    case False{}: emojiBelow683(hash,text,U32.is_lt(hash,1564924783))
def emojiBelow685(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow641(hash,text,U32.is_lt(hash,1419307829))
    case False{}: emojiBelow684(hash,text,U32.is_lt(hash,1544169031))
def emojiBelow686(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow599(hash,text,U32.is_lt(hash,1282798266))
    case False{}: emojiBelow685(hash,text,U32.is_lt(hash,1482437810))
def emojiBelow687(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow514(hash,text,U32.is_lt(hash,956889082))
    case False{}: emojiBelow686(hash,text,U32.is_lt(hash,1378123105))
def emojiBelow688(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow343(hash,text,U32.is_lt(hash,415085487))
    case False{}: emojiBelow687(hash,text,U32.is_lt(hash,1182986256))
def emojiBelow689(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6e9}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow690(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9b3}")
def emojiBelow691(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f393}")
    case False{}: emojiBelow690(hash,text,U32.is_lt(hash,1598480021))
def emojiBelow692(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow689(hash,text,U32.is_lt(hash,1590647178))
    case False{}: emojiBelow691(hash,text,U32.is_lt(hash,1596630784))
def emojiBelow693(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f692}")
def emojiBelow694(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f692}")
def emojiBelow695(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow694(hash,text,U32.is_lt(hash,1607879969))
def emojiBelow696(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow693(hash,text,U32.is_lt(hash,1604458544))
    case False{}: emojiBelow695(hash,text,U32.is_lt(hash,1606038283))
def emojiBelow697(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow692(hash,text,U32.is_lt(hash,1590792846))
    case False{}: emojiBelow696(hash,text,U32.is_lt(hash,1605902234))
def emojiBelow698(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f4bc}")
def emojiBelow699(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow700(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9bc}")
    case False{}: emojiBelow699(hash,text,U32.is_lt(hash,1617731552))
def emojiBelow701(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow698(hash,text,U32.is_lt(hash,1614948136))
    case False{}: emojiBelow700(hash,text,U32.is_lt(hash,1615760045))
def emojiBelow702(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cd}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f393}")
def emojiBelow703(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f52c}")
    case False{}: emojiBelow702(hash,text,U32.is_lt(hash,1620720323))
def emojiBelow704(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9bc}")
def emojiBelow705(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow704(hash,text,U32.is_lt(hash,1622839638))
def emojiBelow706(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow703(hash,text,U32.is_lt(hash,1620184311))
    case False{}: emojiBelow705(hash,text,U32.is_lt(hash,1622679853))
def emojiBelow707(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow701(hash,text,U32.is_lt(hash,1615257640))
    case False{}: emojiBelow706(hash,text,U32.is_lt(hash,1621970812))
def emojiBelow708(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow697(hash,text,U32.is_lt(hash,1599063858))
    case False{}: emojiBelow707(hash,text,U32.is_lt(hash,1618944506))
def emojiBelow709(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f4bc}")
    case False{}: Strings.equal(text,"\u{2764}\u{fe0f}\u{200d}\u{1f525}")
def emojiBelow710(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow711(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9bd}")
    case False{}: emojiBelow710(hash,text,U32.is_lt(hash,1642190717))
def emojiBelow712(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow709(hash,text,U32.is_lt(hash,1630971578))
    case False{}: emojiBelow711(hash,text,U32.is_lt(hash,1639617257))
def emojiBelow713(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f926}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow714(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow713(hash,text,U32.is_lt(hash,1654870675))
def emojiBelow715(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d8}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow716(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow715(hash,text,U32.is_lt(hash,1657415208))
def emojiBelow717(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow714(hash,text,U32.is_lt(hash,1647907318))
    case False{}: emojiBelow716(hash,text,U32.is_lt(hash,1657205039))
def emojiBelow718(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow712(hash,text,U32.is_lt(hash,1632035259))
    case False{}: emojiBelow717(hash,text,U32.is_lt(hash,1655321773))
def emojiBelow719(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f3a8}")
def emojiBelow720(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f4bb}")
def emojiBelow721(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow720(hash,text,U32.is_lt(hash,1673481999))
def emojiBelow722(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow719(hash,text,U32.is_lt(hash,1665220345))
    case False{}: emojiBelow721(hash,text,U32.is_lt(hash,1669564688))
def emojiBelow723(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9af}")
def emojiBelow724(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow723(hash,text,U32.is_lt(hash,1683029644))
def emojiBelow725(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow726(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f17f}\u{fe0f}")
    case False{}: emojiBelow725(hash,text,U32.is_lt(hash,1692082508))
def emojiBelow727(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow724(hash,text,U32.is_lt(hash,1681271683))
    case False{}: emojiBelow726(hash,text,U32.is_lt(hash,1691150311))
def emojiBelow728(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow722(hash,text,U32.is_lt(hash,1667215470))
    case False{}: emojiBelow727(hash,text,U32.is_lt(hash,1684541907))
def emojiBelow729(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow718(hash,text,U32.is_lt(hash,1646921542))
    case False{}: emojiBelow728(hash,text,U32.is_lt(hash,1678422316))
def emojiBelow730(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow708(hash,text,U32.is_lt(hash,1613741620))
    case False{}: emojiBelow729(hash,text,U32.is_lt(hash,1661861533))
def emojiBelow731(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{25ab}\u{fe0f}")
def emojiBelow732(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow733(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: emojiBelow732(hash,text,U32.is_lt(hash,1705990867))
def emojiBelow734(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow731(hash,text,U32.is_lt(hash,1695509311))
    case False{}: emojiBelow733(hash,text,U32.is_lt(hash,1703824169))
def emojiBelow735(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow736(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f39e}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow737(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2696}\u{fe0f}")
    case False{}: emojiBelow736(hash,text,U32.is_lt(hash,1724591843))
def emojiBelow738(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow735(hash,text,U32.is_lt(hash,1717264807))
    case False{}: emojiBelow737(hash,text,U32.is_lt(hash,1722564554))
def emojiBelow739(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow734(hash,text,U32.is_lt(hash,1699567902))
    case False{}: emojiBelow738(hash,text,U32.is_lt(hash,1720629086))
def emojiBelow740(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3da}\u{fe0f}")
def emojiBelow741(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f3dd}\u{fe0f}")
def emojiBelow742(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow741(hash,text,U32.is_lt(hash,1733549125))
def emojiBelow743(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow740(hash,text,U32.is_lt(hash,1732205926))
    case False{}: emojiBelow742(hash,text,U32.is_lt(hash,1733123140))
def emojiBelow744(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow745(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3d4}\u{fe0f}")
    case False{}: emojiBelow744(hash,text,U32.is_lt(hash,1741047717))
def emojiBelow746(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow747(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow746(hash,text,U32.is_lt(hash,1749900759))
def emojiBelow748(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow745(hash,text,U32.is_lt(hash,1735748184))
    case False{}: emojiBelow747(hash,text,U32.is_lt(hash,1748913872))
def emojiBelow749(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow743(hash,text,U32.is_lt(hash,1732391469))
    case False{}: emojiBelow748(hash,text,U32.is_lt(hash,1741369462))
def emojiBelow750(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow739(hash,text,U32.is_lt(hash,1711679338))
    case False{}: emojiBelow749(hash,text,U32.is_lt(hash,1734479652))
def emojiBelow751(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f32c}\u{fe0f}")
def emojiBelow752(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f327}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow753(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: emojiBelow752(hash,text,U32.is_lt(hash,1755164244))
def emojiBelow754(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow751(hash,text,U32.is_lt(hash,1752221324))
    case False{}: emojiBelow753(hash,text,U32.is_lt(hash,1753564523))
def emojiBelow755(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2747}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f202}\u{fe0f}")
def emojiBelow756(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow755(hash,text,U32.is_lt(hash,1768885374))
def emojiBelow757(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f3eb}")
def emojiBelow758(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: emojiBelow757(hash,text,U32.is_lt(hash,1782048046))
def emojiBelow759(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow756(hash,text,U32.is_lt(hash,1767470603))
    case False{}: emojiBelow758(hash,text,U32.is_lt(hash,1771941863))
def emojiBelow760(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow754(hash,text,U32.is_lt(hash,1752525803))
    case False{}: emojiBelow759(hash,text,U32.is_lt(hash,1769303422))
def emojiBelow761(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow762(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93e}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow763(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: emojiBelow762(hash,text,U32.is_lt(hash,1787614411))
def emojiBelow764(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow761(hash,text,U32.is_lt(hash,1783618406))
    case False{}: emojiBelow763(hash,text,U32.is_lt(hash,1786555474))
def emojiBelow765(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{26f4}\u{fe0f}")
def emojiBelow766(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow765(hash,text,U32.is_lt(hash,1791767876))
def emojiBelow767(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26d3}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow768(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow767(hash,text,U32.is_lt(hash,1800396025))
def emojiBelow769(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow766(hash,text,U32.is_lt(hash,1790615828))
    case False{}: emojiBelow768(hash,text,U32.is_lt(hash,1796462423))
def emojiBelow770(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow764(hash,text,U32.is_lt(hash,1786081041))
    case False{}: emojiBelow769(hash,text,U32.is_lt(hash,1792616473))
def emojiBelow771(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow760(hash,text,U32.is_lt(hash,1758147081))
    case False{}: emojiBelow770(hash,text,U32.is_lt(hash,1788719482))
def emojiBelow772(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow750(hash,text,U32.is_lt(hash,1732199366))
    case False{}: emojiBelow771(hash,text,U32.is_lt(hash,1783455997))
def emojiBelow773(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow730(hash,text,U32.is_lt(hash,1623149142))
    case False{}: emojiBelow772(hash,text,U32.is_lt(hash,1750063168))
def emojiBelow774(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow775(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{2716}\u{fe0f}")
def emojiBelow776(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow775(hash,text,U32.is_lt(hash,1807034322))
def emojiBelow777(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow774(hash,text,U32.is_lt(hash,1801792842))
    case False{}: emojiBelow776(hash,text,U32.is_lt(hash,1805497101))
def emojiBelow778(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9b3}")
    case False{}: Strings.equal(text,"\u{1f470}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow779(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow780(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2709}\u{fe0f}")
    case False{}: emojiBelow779(hash,text,U32.is_lt(hash,1810368214))
def emojiBelow781(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow778(hash,text,U32.is_lt(hash,1808308217))
    case False{}: emojiBelow780(hash,text,U32.is_lt(hash,1808479938))
def emojiBelow782(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow777(hash,text,U32.is_lt(hash,1804558382))
    case False{}: emojiBelow781(hash,text,U32.is_lt(hash,1808377521))
def emojiBelow783(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2622}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f3ed}")
def emojiBelow784(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9b0}")
def emojiBelow785(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow784(hash,text,U32.is_lt(hash,1824171066))
def emojiBelow786(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow783(hash,text,U32.is_lt(hash,1815603284))
    case False{}: emojiBelow785(hash,text,U32.is_lt(hash,1823938996))
def emojiBelow787(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26b0}\u{fe0f}")
def emojiBelow788(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fb}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow787(hash,text,U32.is_lt(hash,1833752416))
def emojiBelow789(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f37c}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow790(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow789(hash,text,U32.is_lt(hash,1838113620))
def emojiBelow791(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow788(hash,text,U32.is_lt(hash,1832378334))
    case False{}: emojiBelow790(hash,text,U32.is_lt(hash,1836506930))
def emojiBelow792(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow786(hash,text,U32.is_lt(hash,1822921460))
    case False{}: emojiBelow791(hash,text,U32.is_lt(hash,1833951263))
def emojiBelow793(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow782(hash,text,U32.is_lt(hash,1807393447))
    case False{}: emojiBelow792(hash,text,U32.is_lt(hash,1831664925))
def emojiBelow794(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow795(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{2196}\u{fe0f}")
def emojiBelow796(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9b1}")
    case False{}: emojiBelow795(hash,text,U32.is_lt(hash,1850416466))
def emojiBelow797(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow794(hash,text,U32.is_lt(hash,1838850647))
    case False{}: emojiBelow796(hash,text,U32.is_lt(hash,1844445161))
def emojiBelow798(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow799(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow798(hash,text,U32.is_lt(hash,1860103045))
def emojiBelow800(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow801(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow800(hash,text,U32.is_lt(hash,1871668858))
def emojiBelow802(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow799(hash,text,U32.is_lt(hash,1855678449))
    case False{}: emojiBelow801(hash,text,U32.is_lt(hash,1869207896))
def emojiBelow803(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow797(hash,text,U32.is_lt(hash,1840948685))
    case False{}: emojiBelow802(hash,text,U32.is_lt(hash,1866551773))
def emojiBelow804(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow805(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f4bb}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow806(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2122}\u{fe0f}")
    case False{}: emojiBelow805(hash,text,U32.is_lt(hash,1885572341))
def emojiBelow807(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow804(hash,text,U32.is_lt(hash,1874762389))
    case False{}: emojiBelow806(hash,text,U32.is_lt(hash,1883226224))
def emojiBelow808(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f93e}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow809(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f373}")
    case False{}: emojiBelow808(hash,text,U32.is_lt(hash,1889491841))
def emojiBelow810(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1fa70}")
def emojiBelow811(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow810(hash,text,U32.is_lt(hash,1902688894))
def emojiBelow812(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow809(hash,text,U32.is_lt(hash,1888446477))
    case False{}: emojiBelow811(hash,text,U32.is_lt(hash,1895570432))
def emojiBelow813(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow807(hash,text,U32.is_lt(hash,1879408286))
    case False{}: emojiBelow812(hash,text,U32.is_lt(hash,1890360027))
def emojiBelow814(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow803(hash,text,U32.is_lt(hash,1852387612))
    case False{}: emojiBelow813(hash,text,U32.is_lt(hash,1886839787))
def emojiBelow815(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow793(hash,text,U32.is_lt(hash,1815134622))
    case False{}: emojiBelow814(hash,text,U32.is_lt(hash,1872747515))
def emojiBelow816(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6b5}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow817(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow818(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow817(hash,text,U32.is_lt(hash,1912348051))
def emojiBelow819(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow816(hash,text,U32.is_lt(hash,1906492048))
    case False{}: emojiBelow818(hash,text,U32.is_lt(hash,1908788480))
def emojiBelow820(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow821(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{23fa}\u{fe0f}")
def emojiBelow822(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow821(hash,text,U32.is_lt(hash,1929080454))
def emojiBelow823(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow820(hash,text,U32.is_lt(hash,1915376983))
    case False{}: emojiBelow822(hash,text,U32.is_lt(hash,1928264713))
def emojiBelow824(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow819(hash,text,U32.is_lt(hash,1908310182))
    case False{}: emojiBelow823(hash,text,U32.is_lt(hash,1915874257))
def emojiBelow825(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow826(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f3ed}")
def emojiBelow827(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow826(hash,text,U32.is_lt(hash,1935050646))
def emojiBelow828(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow825(hash,text,U32.is_lt(hash,1929299315))
    case False{}: emojiBelow827(hash,text,U32.is_lt(hash,1931543913))
def emojiBelow829(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f46f}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow830(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow829(hash,text,U32.is_lt(hash,1947035852))
def emojiBelow831(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f3ed}")
def emojiBelow832(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow831(hash,text,U32.is_lt(hash,1948621531))
def emojiBelow833(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow830(hash,text,U32.is_lt(hash,1938341245))
    case False{}: emojiBelow832(hash,text,U32.is_lt(hash,1947774664))
def emojiBelow834(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow828(hash,text,U32.is_lt(hash,1929858080))
    case False{}: emojiBelow833(hash,text,U32.is_lt(hash,1947271526))
def emojiBelow835(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow824(hash,text,U32.is_lt(hash,1913872595))
    case False{}: emojiBelow834(hash,text,U32.is_lt(hash,1936648944))
def emojiBelow836(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f46f}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow837(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f3eb}")
def emojiBelow838(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow837(hash,text,U32.is_lt(hash,1968605884))
def emojiBelow839(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow836(hash,text,U32.is_lt(hash,1954113408))
    case False{}: emojiBelow838(hash,text,U32.is_lt(hash,1967966718))
def emojiBelow840(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f3a4}")
    case False{}: Strings.equal(text,"\u{1f3cc}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow841(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow840(hash,text,U32.is_lt(hash,1974479988))
def emojiBelow842(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow843(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow842(hash,text,U32.is_lt(hash,1983988854))
def emojiBelow844(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow841(hash,text,U32.is_lt(hash,1973050322))
    case False{}: emojiBelow843(hash,text,U32.is_lt(hash,1982167948))
def emojiBelow845(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow839(hash,text,U32.is_lt(hash,1959517780))
    case False{}: emojiBelow844(hash,text,U32.is_lt(hash,1979458527))
def emojiBelow846(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9bd}")
def emojiBelow847(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f5e8}\u{fe0f}")
def emojiBelow848(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow847(hash,text,U32.is_lt(hash,1988056072))
def emojiBelow849(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow846(hash,text,U32.is_lt(hash,1985026786))
    case False{}: emojiBelow848(hash,text,U32.is_lt(hash,1986981801))
def emojiBelow850(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5dc}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f37c}")
def emojiBelow851(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5e3}\u{fe0f}")
    case False{}: emojiBelow850(hash,text,U32.is_lt(hash,1995443732))
def emojiBelow852(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f926}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow853(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f4bc}")
    case False{}: emojiBelow852(hash,text,U32.is_lt(hash,1999280068))
def emojiBelow854(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow851(hash,text,U32.is_lt(hash,1991407420))
    case False{}: emojiBelow853(hash,text,U32.is_lt(hash,1998945567))
def emojiBelow855(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow849(hash,text,U32.is_lt(hash,1986174560))
    case False{}: emojiBelow854(hash,text,U32.is_lt(hash,1997604383))
def emojiBelow856(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow845(hash,text,U32.is_lt(hash,1969669574))
    case False{}: emojiBelow855(hash,text,U32.is_lt(hash,1989399271))
def emojiBelow857(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow835(hash,text,U32.is_lt(hash,1929125670))
    case False{}: emojiBelow856(hash,text,U32.is_lt(hash,1984255071))
def emojiBelow858(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow815(hash,text,U32.is_lt(hash,1838161244))
    case False{}: emojiBelow857(hash,text,U32.is_lt(hash,1953426563))
def emojiBelow859(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow773(hash,text,U32.is_lt(hash,1692110217))
    case False{}: emojiBelow858(hash,text,U32.is_lt(hash,1904664135))
def emojiBelow860(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f4bc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9bc}")
def emojiBelow861(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2695}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9b3}")
def emojiBelow862(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow861(hash,text,U32.is_lt(hash,2018582024))
def emojiBelow863(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow860(hash,text,U32.is_lt(hash,2001804405))
    case False{}: emojiBelow862(hash,text,U32.is_lt(hash,2015127787))
def emojiBelow864(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow865(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow866(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5b2}\u{fe0f}")
    case False{}: emojiBelow865(hash,text,U32.is_lt(hash,2033723685))
def emojiBelow867(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow864(hash,text,U32.is_lt(hash,2022801179))
    case False{}: emojiBelow866(hash,text,U32.is_lt(hash,2030231097))
def emojiBelow868(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow863(hash,text,U32.is_lt(hash,2006772891))
    case False{}: emojiBelow867(hash,text,U32.is_lt(hash,2028962990))
def emojiBelow869(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f58a}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9b2}")
def emojiBelow870(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f58d}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow871(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow870(hash,text,U32.is_lt(hash,2037185962))
def emojiBelow872(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow869(hash,text,U32.is_lt(hash,2035359643))
    case False{}: emojiBelow871(hash,text,U32.is_lt(hash,2036596213))
def emojiBelow873(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f37c}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2708}\u{fe0f}")
def emojiBelow874(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f3a8}")
    case False{}: emojiBelow873(hash,text,U32.is_lt(hash,2044801939))
def emojiBelow875(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9dc}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow876(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f373}")
    case False{}: emojiBelow875(hash,text,U32.is_lt(hash,2046319502))
def emojiBelow877(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow874(hash,text,U32.is_lt(hash,2043685391))
    case False{}: emojiBelow876(hash,text,U32.is_lt(hash,2046210964))
def emojiBelow878(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow872(hash,text,U32.is_lt(hash,2035930939))
    case False{}: emojiBelow877(hash,text,U32.is_lt(hash,2044962224))
def emojiBelow879(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow868(hash,text,U32.is_lt(hash,2020537039))
    case False{}: emojiBelow878(hash,text,U32.is_lt(hash,2040160798))
def emojiBelow880(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9b1}")
def emojiBelow881(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9b1}")
def emojiBelow882(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f3eb}")
    case False{}: emojiBelow881(hash,text,U32.is_lt(hash,2052137262))
def emojiBelow883(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow880(hash,text,U32.is_lt(hash,2049278424))
    case False{}: emojiBelow882(hash,text,U32.is_lt(hash,2050209843))
def emojiBelow884(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9b0}")
def emojiBelow885(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow884(hash,text,U32.is_lt(hash,2066056043))
def emojiBelow886(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9b0}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow887(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9df}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow886(hash,text,U32.is_lt(hash,2070327398))
def emojiBelow888(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow885(hash,text,U32.is_lt(hash,2055268776))
    case False{}: emojiBelow887(hash,text,U32.is_lt(hash,2068914881))
def emojiBelow889(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow883(hash,text,U32.is_lt(hash,2049287245))
    case False{}: emojiBelow888(hash,text,U32.is_lt(hash,2066154122))
def emojiBelow890(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f467}\u{200d}\u{1f466}")
def emojiBelow891(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9b3}")
    case False{}: Strings.equal(text,"\u{1f482}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow892(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow891(hash,text,U32.is_lt(hash,2086420147))
def emojiBelow893(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow890(hash,text,U32.is_lt(hash,2077089308))
    case False{}: emojiBelow892(hash,text,U32.is_lt(hash,2082833662))
def emojiBelow894(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9af}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f680}")
def emojiBelow895(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow894(hash,text,U32.is_lt(hash,2092716138))
def emojiBelow896(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1fa70}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow897(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f467}\u{200d}\u{1f467}")
    case False{}: emojiBelow896(hash,text,U32.is_lt(hash,2099221099))
def emojiBelow898(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow895(hash,text,U32.is_lt(hash,2091525335))
    case False{}: emojiBelow897(hash,text,U32.is_lt(hash,2096017257))
def emojiBelow899(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow893(hash,text,U32.is_lt(hash,2082443480))
    case False{}: emojiBelow898(hash,text,U32.is_lt(hash,2093866927))
def emojiBelow900(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow889(hash,text,U32.is_lt(hash,2053285036))
    case False{}: emojiBelow899(hash,text,U32.is_lt(hash,2086840274))
def emojiBelow901(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow879(hash,text,U32.is_lt(hash,2035253014))
    case False{}: emojiBelow900(hash,text,U32.is_lt(hash,2070880990))
def emojiBelow902(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow903(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow904(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow903(hash,text,U32.is_lt(hash,2115998718))
def emojiBelow905(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow902(hash,text,U32.is_lt(hash,2100431003))
    case False{}: emojiBelow904(hash,text,U32.is_lt(hash,2112345246))
def emojiBelow906(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3ff}\u{200d}\u{1faf2}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow907(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6e3}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow908(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f373}")
    case False{}: emojiBelow907(hash,text,U32.is_lt(hash,2129262166))
def emojiBelow909(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow906(hash,text,U32.is_lt(hash,2127291512))
    case False{}: emojiBelow908(hash,text,U32.is_lt(hash,2128572903))
def emojiBelow910(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow905(hash,text,U32.is_lt(hash,2103617893))
    case False{}: emojiBelow909(hash,text,U32.is_lt(hash,2127573486))
def emojiBelow911(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f6ce}\u{fe0f}")
def emojiBelow912(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9b2}")
def emojiBelow913(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow912(hash,text,U32.is_lt(hash,2141858192))
def emojiBelow914(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow911(hash,text,U32.is_lt(hash,2132854778))
    case False{}: emojiBelow913(hash,text,U32.is_lt(hash,2135562875))
def emojiBelow915(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow916(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow915(hash,text,U32.is_lt(hash,2150787941))
def emojiBelow917(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9b3}")
def emojiBelow918(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow917(hash,text,U32.is_lt(hash,2158635811))
def emojiBelow919(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow916(hash,text,U32.is_lt(hash,2149055271))
    case False{}: emojiBelow918(hash,text,U32.is_lt(hash,2158596186))
def emojiBelow920(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow914(hash,text,U32.is_lt(hash,2134873070))
    case False{}: emojiBelow919(hash,text,U32.is_lt(hash,2152028970))
def emojiBelow921(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow910(hash,text,U32.is_lt(hash,2121784035))
    case False{}: emojiBelow920(hash,text,U32.is_lt(hash,2146742885))
def emojiBelow922(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow923(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9b0}")
def emojiBelow924(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3ff}\u{200d}\u{1faf2}\u{1f3fe}")
    case False{}: emojiBelow923(hash,text,U32.is_lt(hash,2175413430))
def emojiBelow925(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow922(hash,text,U32.is_lt(hash,2165850249))
    case False{}: emojiBelow924(hash,text,U32.is_lt(hash,2172880503))
def emojiBelow926(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow927(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow926(hash,text,U32.is_lt(hash,2181689883))
def emojiBelow928(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f635}\u{200d}\u{1f4ab}")
def emojiBelow929(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow928(hash,text,U32.is_lt(hash,2189086372))
def emojiBelow930(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow927(hash,text,U32.is_lt(hash,2180335530))
    case False{}: emojiBelow929(hash,text,U32.is_lt(hash,2188952468))
def emojiBelow931(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow925(hash,text,U32.is_lt(hash,2172116892))
    case False{}: emojiBelow930(hash,text,U32.is_lt(hash,2183109194))
def emojiBelow932(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f469}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9b1}")
def emojiBelow933(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f3a8}")
def emojiBelow934(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f9}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow933(hash,text,U32.is_lt(hash,2199657987))
def emojiBelow935(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow932(hash,text,U32.is_lt(hash,2192191049))
    case False{}: emojiBelow934(hash,text,U32.is_lt(hash,2196405386))
def emojiBelow936(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3ff}\u{200d}\u{1faf2}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f469}\u{200d}\u{1f466}")
def emojiBelow937(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{fe0f}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow936(hash,text,U32.is_lt(hash,2206509941))
def emojiBelow938(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f46e}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow939(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow938(hash,text,U32.is_lt(hash,2212226412))
def emojiBelow940(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow937(hash,text,U32.is_lt(hash,2205672130))
    case False{}: emojiBelow939(hash,text,U32.is_lt(hash,2210616997))
def emojiBelow941(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow935(hash,text,U32.is_lt(hash,2192820091))
    case False{}: emojiBelow940(hash,text,U32.is_lt(hash,2210027924))
def emojiBelow942(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow931(hash,text,U32.is_lt(hash,2178927681))
    case False{}: emojiBelow941(hash,text,U32.is_lt(hash,2201043780))
def emojiBelow943(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow921(hash,text,U32.is_lt(hash,2132776337))
    case False{}: emojiBelow942(hash,text,U32.is_lt(hash,2189732322))
def emojiBelow944(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow901(hash,text,U32.is_lt(hash,2046580369))
    case False{}: emojiBelow943(hash,text,U32.is_lt(hash,2164523519))
def emojiBelow945(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow946(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3ff}\u{200d}\u{1faf2}\u{1f3fd}")
def emojiBelow947(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow946(hash,text,U32.is_lt(hash,2222449749))
def emojiBelow948(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow945(hash,text,U32.is_lt(hash,2215445296))
    case False{}: emojiBelow947(hash,text,U32.is_lt(hash,2217372988))
def emojiBelow949(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f171}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f52c}")
def emojiBelow950(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{25aa}\u{fe0f}")
def emojiBelow951(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow950(hash,text,U32.is_lt(hash,2232127542))
def emojiBelow952(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow949(hash,text,U32.is_lt(hash,2224427205))
    case False{}: emojiBelow951(hash,text,U32.is_lt(hash,2229016611))
def emojiBelow953(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow948(hash,text,U32.is_lt(hash,2216685188))
    case False{}: emojiBelow952(hash,text,U32.is_lt(hash,2228411577))
def emojiBelow954(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{2764}\u{fe0f}\u{200d}\u{1fa79}")
def emojiBelow955(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{25fc}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow956(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow955(hash,text,U32.is_lt(hash,2242742728))
def emojiBelow957(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow954(hash,text,U32.is_lt(hash,2234450022))
    case False{}: emojiBelow956(hash,text,U32.is_lt(hash,2241103964))
def emojiBelow958(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow959(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: emojiBelow958(hash,text,U32.is_lt(hash,2255099784))
def emojiBelow960(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow961(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f399}\u{fe0f}")
    case False{}: emojiBelow960(hash,text,U32.is_lt(hash,2263494896))
def emojiBelow962(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow959(hash,text,U32.is_lt(hash,2249000534))
    case False{}: emojiBelow961(hash,text,U32.is_lt(hash,2263234784))
def emojiBelow963(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow957(hash,text,U32.is_lt(hash,2235360685))
    case False{}: emojiBelow962(hash,text,U32.is_lt(hash,2259182785))
def emojiBelow964(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow953(hash,text,U32.is_lt(hash,2222503337))
    case False{}: emojiBelow963(hash,text,U32.is_lt(hash,2244207888))
def emojiBelow965(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f3dc}\u{fe0f}")
def emojiBelow966(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f3ce}\u{fe0f}")
def emojiBelow967(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3d7}\u{fe0f}")
    case False{}: emojiBelow966(hash,text,U32.is_lt(hash,2272441082))
def emojiBelow968(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow965(hash,text,U32.is_lt(hash,2270167356))
    case False{}: emojiBelow967(hash,text,U32.is_lt(hash,2272368057))
def emojiBelow969(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow970(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow969(hash,text,U32.is_lt(hash,2277440529))
def emojiBelow971(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow972(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow971(hash,text,U32.is_lt(hash,2280012403))
def emojiBelow973(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow970(hash,text,U32.is_lt(hash,2276999494))
    case False{}: emojiBelow972(hash,text,U32.is_lt(hash,2279349468))
def emojiBelow974(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow968(hash,text,U32.is_lt(hash,2271097883))
    case False{}: emojiBelow973(hash,text,U32.is_lt(hash,2278251215))
def emojiBelow975(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow976(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9af}")
    case False{}: Strings.equal(text,"\u{1f93e}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow977(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow976(hash,text,U32.is_lt(hash,2287828348))
def emojiBelow978(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow975(hash,text,U32.is_lt(hash,2280218175))
    case False{}: emojiBelow977(hash,text,U32.is_lt(hash,2284165090))
def emojiBelow979(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f326}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f33e}")
def emojiBelow980(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow979(hash,text,U32.is_lt(hash,2292048838))
def emojiBelow981(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow982(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow981(hash,text,U32.is_lt(hash,2296587470))
def emojiBelow983(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow980(hash,text,U32.is_lt(hash,2290182754))
    case False{}: emojiBelow982(hash,text,U32.is_lt(hash,2296127087))
def emojiBelow984(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow978(hash,text,U32.is_lt(hash,2280842554))
    case False{}: emojiBelow983(hash,text,U32.is_lt(hash,2292547259))
def emojiBelow985(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow974(hash,text,U32.is_lt(hash,2272775875))
    case False{}: emojiBelow984(hash,text,U32.is_lt(hash,2289145676))
def emojiBelow986(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow964(hash,text,U32.is_lt(hash,2232222915))
    case False{}: emojiBelow985(hash,text,U32.is_lt(hash,2280131679))
def emojiBelow987(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f37c}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3ff}")
def emojiBelow988(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow989(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3ff}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow988(hash,text,U32.is_lt(hash,2305923295))
def emojiBelow990(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow987(hash,text,U32.is_lt(hash,2296790022))
    case False{}: emojiBelow989(hash,text,U32.is_lt(hash,2301750518))
def emojiBelow991(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f527}")
def emojiBelow992(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f3eb}")
    case False{}: emojiBelow991(hash,text,U32.is_lt(hash,2308315300))
def emojiBelow993(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fe}")
def emojiBelow994(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow993(hash,text,U32.is_lt(hash,2313567641))
def emojiBelow995(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow992(hash,text,U32.is_lt(hash,2307527662))
    case False{}: emojiBelow994(hash,text,U32.is_lt(hash,2312904706))
def emojiBelow996(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow990(hash,text,U32.is_lt(hash,2300941433))
    case False{}: emojiBelow995(hash,text,U32.is_lt(hash,2311318364))
def emojiBelow997(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow998(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f4}\u{e0067}\u{e0062}\u{e0077}\u{e006c}\u{e0073}\u{e007f}")
    case False{}: Strings.equal(text,"\u{1f9b9}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow999(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow998(hash,text,U32.is_lt(hash,2320534217))
def emojiBelow1000(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow997(hash,text,U32.is_lt(hash,2316224313))
    case False{}: emojiBelow999(hash,text,U32.is_lt(hash,2319217403))
def emojiBelow1001(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow1002(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow1001(hash,text,U32.is_lt(hash,2326072033))
def emojiBelow1003(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f692}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fe}")
def emojiBelow1004(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1003(hash,text,U32.is_lt(hash,2328095983))
def emojiBelow1005(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1002(hash,text,U32.is_lt(hash,2325909624))
    case False{}: emojiBelow1004(hash,text,U32.is_lt(hash,2327602804))
def emojiBelow1006(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1000(hash,text,U32.is_lt(hash,2316715907))
    case False{}: emojiBelow1005(hash,text,U32.is_lt(hash,2327526405))
def emojiBelow1007(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow996(hash,text,U32.is_lt(hash,2307086729))
    case False{}: emojiBelow1006(hash,text,U32.is_lt(hash,2322700914))
def emojiBelow1008(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f7}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow1009(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9dd}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1010(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow1009(hash,text,U32.is_lt(hash,2333756537))
def emojiBelow1011(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1008(hash,text,U32.is_lt(hash,2329682325))
    case False{}: emojiBelow1010(hash,text,U32.is_lt(hash,2333001932))
def emojiBelow1012(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f3ed}")
def emojiBelow1013(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26cf}\u{fe0f}")
    case False{}: emojiBelow1012(hash,text,U32.is_lt(hash,2340641967))
def emojiBelow1014(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f393}")
def emojiBelow1015(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow1014(hash,text,U32.is_lt(hash,2344896263))
def emojiBelow1016(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1013(hash,text,U32.is_lt(hash,2336015601))
    case False{}: emojiBelow1015(hash,text,U32.is_lt(hash,2344873602))
def emojiBelow1017(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1011(hash,text,U32.is_lt(hash,2330605372))
    case False{}: emojiBelow1016(hash,text,U32.is_lt(hash,2342849652))
def emojiBelow1018(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1019(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2639}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6b4}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1020(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow1019(hash,text,U32.is_lt(hash,2348881318))
def emojiBelow1021(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1018(hash,text,U32.is_lt(hash,2345203840))
    case False{}: emojiBelow1020(hash,text,U32.is_lt(hash,2348814177))
def emojiBelow1022(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2611}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2603}\u{fe0f}")
def emojiBelow1023(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{262a}\u{fe0f}")
    case False{}: emojiBelow1022(hash,text,U32.is_lt(hash,2357377927))
def emojiBelow1024(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1025(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1024(hash,text,U32.is_lt(hash,2359980029))
def emojiBelow1026(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1023(hash,text,U32.is_lt(hash,2355104201))
    case False{}: emojiBelow1025(hash,text,U32.is_lt(hash,2359627271))
def emojiBelow1027(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1021(hash,text,U32.is_lt(hash,2347382991))
    case False{}: emojiBelow1026(hash,text,U32.is_lt(hash,2357848310))
def emojiBelow1028(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1017(hash,text,U32.is_lt(hash,2335088803))
    case False{}: emojiBelow1027(hash,text,U32.is_lt(hash,2350822326))
def emojiBelow1029(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1007(hash,text,U32.is_lt(hash,2316061904))
    case False{}: emojiBelow1028(hash,text,U32.is_lt(hash,2344995752))
def emojiBelow1030(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow986(hash,text,U32.is_lt(hash,2265778153))
    case False{}: emojiBelow1029(hash,text,U32.is_lt(hash,2328798779))
def emojiBelow1031(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow944(hash,text,U32.is_lt(hash,2099611281))
    case False{}: emojiBelow1030(hash,text,U32.is_lt(hash,2296626509))
def emojiBelow1032(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow859(hash,text,U32.is_lt(hash,1800967327))
    case False{}: emojiBelow1031(hash,text,U32.is_lt(hash,2212692195))
def emojiBelow1033(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{2660}\u{fe0f}")
def emojiBelow1034(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9da}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1035(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: emojiBelow1034(hash,text,U32.is_lt(hash,2364730662))
def emojiBelow1036(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1033(hash,text,U32.is_lt(hash,2362737424))
    case False{}: emojiBelow1035(hash,text,U32.is_lt(hash,2364160610))
def emojiBelow1037(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d9}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1038(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f4bc}")
def emojiBelow1039(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1038(hash,text,U32.is_lt(hash,2376435354))
def emojiBelow1040(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1037(hash,text,U32.is_lt(hash,2367380442))
    case False{}: emojiBelow1039(hash,text,U32.is_lt(hash,2376404890))
def emojiBelow1041(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1036(hash,text,U32.is_lt(hash,2363900498))
    case False{}: emojiBelow1040(hash,text,U32.is_lt(hash,2375460822))
def emojiBelow1042(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{36}\u{fe0f}\u{20e3}")
def emojiBelow1043(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1044(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{21aa}\u{fe0f}")
    case False{}: emojiBelow1043(hash,text,U32.is_lt(hash,2383334789))
def emojiBelow1045(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1042(hash,text,U32.is_lt(hash,2381079491))
    case False{}: emojiBelow1044(hash,text,U32.is_lt(hash,2383172380))
def emojiBelow1046(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3ff}")
def emojiBelow1047(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2708}\u{fe0f}")
    case False{}: emojiBelow1046(hash,text,U32.is_lt(hash,2393020100))
def emojiBelow1048(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f64d}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1049(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow1048(hash,text,U32.is_lt(hash,2397234518))
def emojiBelow1050(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1047(hash,text,U32.is_lt(hash,2390175560))
    case False{}: emojiBelow1049(hash,text,U32.is_lt(hash,2393522477))
def emojiBelow1051(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1045(hash,text,U32.is_lt(hash,2382752822))
    case False{}: emojiBelow1050(hash,text,U32.is_lt(hash,2393182509))
def emojiBelow1052(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1041(hash,text,U32.is_lt(hash,2366557170))
    case False{}: emojiBelow1051(hash,text,U32.is_lt(hash,2389629154))
def emojiBelow1053(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f3a4}")
def emojiBelow1054(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{38}\u{fe0f}\u{20e3}")
def emojiBelow1055(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow1054(hash,text,U32.is_lt(hash,2410556513))
def emojiBelow1056(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1053(hash,text,U32.is_lt(hash,2400989415))
    case False{}: emojiBelow1055(hash,text,U32.is_lt(hash,2409797719))
def emojiBelow1057(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow1058(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2139}\u{fe0f}")
    case False{}: emojiBelow1057(hash,text,U32.is_lt(hash,2415304521))
def emojiBelow1059(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1060(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1059(hash,text,U32.is_lt(hash,2426575338))
def emojiBelow1061(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1058(hash,text,U32.is_lt(hash,2413054371))
    case False{}: emojiBelow1060(hash,text,U32.is_lt(hash,2426027639))
def emojiBelow1062(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1056(hash,text,U32.is_lt(hash,2406238799))
    case False{}: emojiBelow1061(hash,text,U32.is_lt(hash,2416283492))
def emojiBelow1063(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1064(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1065(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow1064(hash,text,U32.is_lt(hash,2433510099))
def emojiBelow1066(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1063(hash,text,U32.is_lt(hash,2427152576))
    case False{}: emojiBelow1065(hash,text,U32.is_lt(hash,2433505237))
def emojiBelow1067(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow1068(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f3ed}")
    case False{}: emojiBelow1067(hash,text,U32.is_lt(hash,2443352957))
def emojiBelow1069(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64b}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1070(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f3ed}")
    case False{}: emojiBelow1069(hash,text,U32.is_lt(hash,2448126850))
def emojiBelow1071(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1068(hash,text,U32.is_lt(hash,2441868516))
    case False{}: emojiBelow1070(hash,text,U32.is_lt(hash,2445817161))
def emojiBelow1072(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1066(hash,text,U32.is_lt(hash,2432082140))
    case False{}: emojiBelow1071(hash,text,U32.is_lt(hash,2445561977))
def emojiBelow1073(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1062(hash,text,U32.is_lt(hash,2412675169))
    case False{}: emojiBelow1072(hash,text,U32.is_lt(hash,2436741963))
def emojiBelow1074(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1052(hash,text,U32.is_lt(hash,2376744858))
    case False{}: emojiBelow1073(hash,text,U32.is_lt(hash,2426925425))
def emojiBelow1075(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1076(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f468}\u{200d}\u{1f466}\u{200d}\u{1f466}")
def emojiBelow1077(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1076(hash,text,U32.is_lt(hash,2458915830))
def emojiBelow1078(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1075(hash,text,U32.is_lt(hash,2451109663))
    case False{}: emojiBelow1077(hash,text,U32.is_lt(hash,2455487466))
def emojiBelow1079(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f33e}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f3ed}")
def emojiBelow1080(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d8}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{23ee}\u{fe0f}")
def emojiBelow1081(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow1080(hash,text,U32.is_lt(hash,2469315610))
def emojiBelow1082(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1079(hash,text,U32.is_lt(hash,2465485294))
    case False{}: emojiBelow1081(hash,text,U32.is_lt(hash,2467650378))
def emojiBelow1083(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1078(hash,text,U32.is_lt(hash,2451359567))
    case False{}: emojiBelow1082(hash,text,U32.is_lt(hash,2465637378))
def emojiBelow1084(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{23cf}\u{fe0f}")
def emojiBelow1085(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3ff}")
def emojiBelow1086(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f3eb}")
    case False{}: emojiBelow1085(hash,text,U32.is_lt(hash,2482414997))
def emojiBelow1087(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1084(hash,text,U32.is_lt(hash,2474675107))
    case False{}: emojiBelow1086(hash,text,U32.is_lt(hash,2481429740))
def emojiBelow1088(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f3eb}")
def emojiBelow1089(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1088(hash,text,U32.is_lt(hash,2499040532))
def emojiBelow1090(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1091(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1090(hash,text,U32.is_lt(hash,2503866900))
def emojiBelow1092(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1089(hash,text,U32.is_lt(hash,2498207359))
    case False{}: emojiBelow1091(hash,text,U32.is_lt(hash,2502142829))
def emojiBelow1093(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1087(hash,text,U32.is_lt(hash,2479117215))
    case False{}: emojiBelow1092(hash,text,U32.is_lt(hash,2499807274))
def emojiBelow1094(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1083(hash,text,U32.is_lt(hash,2459520207))
    case False{}: emojiBelow1093(hash,text,U32.is_lt(hash,2489839731))
def emojiBelow1095(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1096(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1097(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow1096(hash,text,U32.is_lt(hash,2523228843))
def emojiBelow1098(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1095(hash,text,U32.is_lt(hash,2514984978))
    case False{}: emojiBelow1097(hash,text,U32.is_lt(hash,2523013699))
def emojiBelow1099(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1100(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f52c}")
    case False{}: emojiBelow1099(hash,text,U32.is_lt(hash,2532985084))
def emojiBelow1101(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f3eb}")
def emojiBelow1102(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow1101(hash,text,U32.is_lt(hash,2537407677))
def emojiBelow1103(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1100(hash,text,U32.is_lt(hash,2531762597))
    case False{}: emojiBelow1102(hash,text,U32.is_lt(hash,2534593719))
def emojiBelow1104(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1098(hash,text,U32.is_lt(hash,2517617828))
    case False{}: emojiBelow1103(hash,text,U32.is_lt(hash,2534395447))
def emojiBelow1105(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fb}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93c}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1106(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1107(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fc}\u{200d}\u{1faf2}\u{1f3ff}")
    case False{}: emojiBelow1106(hash,text,U32.is_lt(hash,2552452687))
def emojiBelow1108(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1105(hash,text,U32.is_lt(hash,2546087542))
    case False{}: emojiBelow1107(hash,text,U32.is_lt(hash,2551173066))
def emojiBelow1109(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f570}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1110(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f393}")
    case False{}: emojiBelow1109(hash,text,U32.is_lt(hash,2556508118))
def emojiBelow1111(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1112(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2696}\u{fe0f}")
    case False{}: emojiBelow1111(hash,text,U32.is_lt(hash,2559345953))
def emojiBelow1113(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1110(hash,text,U32.is_lt(hash,2555674272))
    case False{}: emojiBelow1112(hash,text,U32.is_lt(hash,2558472527))
def emojiBelow1114(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1108(hash,text,U32.is_lt(hash,2549951232))
    case False{}: emojiBelow1113(hash,text,U32.is_lt(hash,2557576457))
def emojiBelow1115(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1104(hash,text,U32.is_lt(hash,2527554512))
    case False{}: emojiBelow1114(hash,text,U32.is_lt(hash,2554434152))
def emojiBelow1116(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1094(hash,text,U32.is_lt(hash,2474584432))
    case False{}: emojiBelow1115(hash,text,U32.is_lt(hash,2538992626))
def emojiBelow1117(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1074(hash,text,U32.is_lt(hash,2399949999))
    case False{}: emojiBelow1116(hash,text,U32.is_lt(hash,2508911852))
def emojiBelow1118(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f549}\u{fe0f}")
def emojiBelow1119(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fe}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1120(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1119(hash,text,U32.is_lt(hash,2566137092))
def emojiBelow1121(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1118(hash,text,U32.is_lt(hash,2562229873))
    case False{}: emojiBelow1120(hash,text,U32.is_lt(hash,2565044484))
def emojiBelow1122(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fc}\u{200d}\u{1faf2}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow1123(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f58c}\u{fe0f}")
def emojiBelow1124(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f692}")
    case False{}: emojiBelow1123(hash,text,U32.is_lt(hash,2573214444))
def emojiBelow1125(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1122(hash,text,U32.is_lt(hash,2567950685))
    case False{}: emojiBelow1124(hash,text,U32.is_lt(hash,2571371567))
def emojiBelow1126(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1121(hash,text,U32.is_lt(hash,2562699943))
    case False{}: emojiBelow1125(hash,text,U32.is_lt(hash,2570902267))
def emojiBelow1127(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f587}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow1128(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1129(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1128(hash,text,U32.is_lt(hash,2581605821))
def emojiBelow1130(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1127(hash,text,U32.is_lt(hash,2575250146))
    case False{}: emojiBelow1129(hash,text,U32.is_lt(hash,2579696911))
def emojiBelow1131(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f642}\u{200d}\u{2194}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow1132(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fc}\u{200d}\u{1faf2}\u{1f3fd}")
    case False{}: emojiBelow1131(hash,text,U32.is_lt(hash,2592027765))
def emojiBelow1133(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1134(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1133(hash,text,U32.is_lt(hash,2600902460))
def emojiBelow1135(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1132(hash,text,U32.is_lt(hash,2586909297))
    case False{}: emojiBelow1134(hash,text,U32.is_lt(hash,2600863543))
def emojiBelow1136(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1130(hash,text,U32.is_lt(hash,2579578076))
    case False{}: emojiBelow1135(hash,text,U32.is_lt(hash,2593105972))
def emojiBelow1137(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1126(hash,text,U32.is_lt(hash,2566728851))
    case False{}: emojiBelow1136(hash,text,U32.is_lt(hash,2583506470))
def emojiBelow1138(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow1139(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1faf1}\u{1f3fc}\u{200d}\u{1faf2}\u{1f3fb}")
def emojiBelow1140(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f3eb}")
    case False{}: emojiBelow1139(hash,text,U32.is_lt(hash,2617061708))
def emojiBelow1141(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1138(hash,text,U32.is_lt(hash,2607213913))
    case False{}: emojiBelow1140(hash,text,U32.is_lt(hash,2614664643))
def emojiBelow1142(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f938}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1143(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1142(hash,text,U32.is_lt(hash,2621660760))
def emojiBelow1144(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f692}")
def emojiBelow1145(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: emojiBelow1144(hash,text,U32.is_lt(hash,2635360241))
def emojiBelow1146(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1143(hash,text,U32.is_lt(hash,2619098462))
    case False{}: emojiBelow1145(hash,text,U32.is_lt(hash,2624802692))
def emojiBelow1147(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1141(hash,text,U32.is_lt(hash,2609030363))
    case False{}: emojiBelow1146(hash,text,U32.is_lt(hash,2623991532))
def emojiBelow1148(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9da}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1149(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fe}")
def emojiBelow1150(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1149(hash,text,U32.is_lt(hash,2657546770))
def emojiBelow1151(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1148(hash,text,U32.is_lt(hash,2638695837))
    case False{}: emojiBelow1150(hash,text,U32.is_lt(hash,2649108816))
def emojiBelow1152(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1153(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1152(hash,text,U32.is_lt(hash,2660720236))
def emojiBelow1154(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6f0}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fe}")
def emojiBelow1155(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1154(hash,text,U32.is_lt(hash,2664997500))
def emojiBelow1156(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1153(hash,text,U32.is_lt(hash,2658661939))
    case False{}: emojiBelow1155(hash,text,U32.is_lt(hash,2662917408))
def emojiBelow1157(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1151(hash,text,U32.is_lt(hash,2644493654))
    case False{}: emojiBelow1156(hash,text,U32.is_lt(hash,2662690473))
def emojiBelow1158(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1147(hash,text,U32.is_lt(hash,2617543625))
    case False{}: emojiBelow1157(hash,text,U32.is_lt(hash,2657907675))
def emojiBelow1159(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1137(hash,text,U32.is_lt(hash,2574557643))
    case False{}: emojiBelow1158(hash,text,U32.is_lt(hash,2635767413))
def emojiBelow1160(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6e2}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow1161(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9b0}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f33e}")
def emojiBelow1162(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6e5}\u{fe0f}")
    case False{}: emojiBelow1161(hash,text,U32.is_lt(hash,2671158685))
def emojiBelow1163(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1160(hash,text,U32.is_lt(hash,2665886435))
    case False{}: emojiBelow1162(hash,text,U32.is_lt(hash,2669027468))
def emojiBelow1164(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d9}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1165(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9b1}")
def emojiBelow1166(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2b05}\u{fe0f}")
    case False{}: emojiBelow1165(hash,text,U32.is_lt(hash,2685805087))
def emojiBelow1167(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1164(hash,text,U32.is_lt(hash,2679189642))
    case False{}: emojiBelow1166(hash,text,U32.is_lt(hash,2681775119))
def emojiBelow1168(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1163(hash,text,U32.is_lt(hash,2666534333))
    case False{}: emojiBelow1167(hash,text,U32.is_lt(hash,2680440413))
def emojiBelow1169(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fe}")
def emojiBelow1170(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow1171(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f37c}")
    case False{}: emojiBelow1170(hash,text,U32.is_lt(hash,2699441673))
def emojiBelow1172(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1169(hash,text,U32.is_lt(hash,2688563675))
    case False{}: emojiBelow1171(hash,text,U32.is_lt(hash,2697299996))
def emojiBelow1173(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f3ed}")
def emojiBelow1174(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9b2}")
    case False{}: emojiBelow1173(hash,text,U32.is_lt(hash,2709696077))
def emojiBelow1175(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1176(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f527}")
    case False{}: emojiBelow1175(hash,text,U32.is_lt(hash,2715839851))
def emojiBelow1177(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1174(hash,text,U32.is_lt(hash,2705341294))
    case False{}: emojiBelow1176(hash,text,U32.is_lt(hash,2715330357))
def emojiBelow1178(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1172(hash,text,U32.is_lt(hash,2696868777))
    case False{}: emojiBelow1177(hash,text,U32.is_lt(hash,2712108321))
def emojiBelow1179(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1168(hash,text,U32.is_lt(hash,2674324389))
    case False{}: emojiBelow1178(hash,text,U32.is_lt(hash,2702582706))
def emojiBelow1180(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9b3}")
def emojiBelow1181(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9dd}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1182(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1181(hash,text,U32.is_lt(hash,2722451249))
def emojiBelow1183(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1180(hash,text,U32.is_lt(hash,2719360325))
    case False{}: emojiBelow1182(hash,text,U32.is_lt(hash,2722118913))
def emojiBelow1184(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1185(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f33e}")
    case False{}: emojiBelow1184(hash,text,U32.is_lt(hash,2731064342))
def emojiBelow1186(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f4bc}")
def emojiBelow1187(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1186(hash,text,U32.is_lt(hash,2736653784))
def emojiBelow1188(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1185(hash,text,U32.is_lt(hash,2728970907))
    case False{}: emojiBelow1187(hash,text,U32.is_lt(hash,2736137944))
def emojiBelow1189(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1183(hash,text,U32.is_lt(hash,2720703966))
    case False{}: emojiBelow1188(hash,text,U32.is_lt(hash,2735097995))
def emojiBelow1190(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow1191(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1faef}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow1192(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1191(hash,text,U32.is_lt(hash,2749774530))
def emojiBelow1193(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1190(hash,text,U32.is_lt(hash,2738896532))
    case False{}: emojiBelow1192(hash,text,U32.is_lt(hash,2749243097))
def emojiBelow1194(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f17e}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f170}\u{fe0f}")
def emojiBelow1195(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9bd}")
    case False{}: emojiBelow1194(hash,text,U32.is_lt(hash,2759121568))
def emojiBelow1196(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9cf}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1197(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1196(hash,text,U32.is_lt(hash,2772382679))
def emojiBelow1198(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1195(hash,text,U32.is_lt(hash,2758043946))
    case False{}: emojiBelow1197(hash,text,U32.is_lt(hash,2760603572))
def emojiBelow1199(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1193(hash,text,U32.is_lt(hash,2742736666))
    case False{}: emojiBelow1198(hash,text,U32.is_lt(hash,2759785105))
def emojiBelow1200(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1189(hash,text,U32.is_lt(hash,2723495571))
    case False{}: emojiBelow1199(hash,text,U32.is_lt(hash,2752915563))
def emojiBelow1201(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1179(hash,text,U32.is_lt(hash,2686978082))
    case False{}: emojiBelow1200(hash,text,U32.is_lt(hash,2737165689))
def emojiBelow1202(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1159(hash,text,U32.is_lt(hash,2603730710))
    case False{}: emojiBelow1201(hash,text,U32.is_lt(hash,2716262341))
def emojiBelow1203(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1117(hash,text,U32.is_lt(hash,2450507352))
    case False{}: emojiBelow1202(hash,text,U32.is_lt(hash,2665191134))
def emojiBelow1204(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow1205(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f527}")
def emojiBelow1206(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{34}\u{fe0f}\u{20e3}")
    case False{}: emojiBelow1205(hash,text,U32.is_lt(hash,2788808354))
def emojiBelow1207(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1204(hash,text,U32.is_lt(hash,2777379688))
    case False{}: emojiBelow1206(hash,text,U32.is_lt(hash,2783473799))
def emojiBelow1208(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f52c}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow1209(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3df}\u{fe0f}")
def emojiBelow1210(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f469}\u{200d}\u{1f466}\u{200d}\u{1f466}")
    case False{}: emojiBelow1209(hash,text,U32.is_lt(hash,2806785587))
def emojiBelow1211(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1208(hash,text,U32.is_lt(hash,2800894334))
    case False{}: emojiBelow1210(hash,text,U32.is_lt(hash,2805316629))
def emojiBelow1212(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1207(hash,text,U32.is_lt(hash,2781111717))
    case False{}: emojiBelow1211(hash,text,U32.is_lt(hash,2802932362))
def emojiBelow1213(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3d6}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1214(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f4bb}")
def emojiBelow1215(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1214(hash,text,U32.is_lt(hash,2826489619))
def emojiBelow1216(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1213(hash,text,U32.is_lt(hash,2814284179))
    case False{}: emojiBelow1215(hash,text,U32.is_lt(hash,2825261932))
def emojiBelow1217(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f938}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1218(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f321}\u{fe0f}")
    case False{}: emojiBelow1217(hash,text,U32.is_lt(hash,2835026695))
def emojiBelow1219(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f373}")
def emojiBelow1220(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1219(hash,text,U32.is_lt(hash,2847867348))
def emojiBelow1221(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1218(hash,text,U32.is_lt(hash,2829950253))
    case False{}: emojiBelow1220(hash,text,U32.is_lt(hash,2841887313))
def emojiBelow1222(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1216(hash,text,U32.is_lt(hash,2821171294))
    case False{}: emojiBelow1221(hash,text,U32.is_lt(hash,2841314901))
def emojiBelow1223(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1212(hash,text,U32.is_lt(hash,2800150436))
    case False{}: emojiBelow1222(hash,text,U32.is_lt(hash,2826800985))
def emojiBelow1224(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f52c}")
def emojiBelow1225(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f4}\u{e0067}\u{e0062}\u{e0073}\u{e0063}\u{e0074}\u{e007f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f4bb}")
def emojiBelow1226(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow1225(hash,text,U32.is_lt(hash,2854097117))
def emojiBelow1227(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1224(hash,text,U32.is_lt(hash,2849217439))
    case False{}: emojiBelow1226(hash,text,U32.is_lt(hash,2851274225))
def emojiBelow1228(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1229(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: emojiBelow1228(hash,text,U32.is_lt(hash,2857132125))
def emojiBelow1230(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f46e}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1231(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26a7}\u{fe0f}")
    case False{}: emojiBelow1230(hash,text,U32.is_lt(hash,2863360297))
def emojiBelow1232(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1229(hash,text,U32.is_lt(hash,2855197760))
    case False{}: emojiBelow1231(hash,text,U32.is_lt(hash,2860058746))
def emojiBelow1233(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1227(hash,text,U32.is_lt(hash,2851227191))
    case False{}: emojiBelow1232(hash,text,U32.is_lt(hash,2857783787))
def emojiBelow1234(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{26e9}\u{fe0f}")
def emojiBelow1235(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f680}")
def emojiBelow1236(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow1235(hash,text,U32.is_lt(hash,2870246907))
def emojiBelow1237(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1234(hash,text,U32.is_lt(hash,2866347537))
    case False{}: emojiBelow1236(hash,text,U32.is_lt(hash,2868671963))
def emojiBelow1238(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2733}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1239(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow1238(hash,text,U32.is_lt(hash,2875773946))
def emojiBelow1240(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2702}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1241(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fe}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1240(hash,text,U32.is_lt(hash,2883344453))
def emojiBelow1242(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1239(hash,text,U32.is_lt(hash,2875323959))
    case False{}: emojiBelow1241(hash,text,U32.is_lt(hash,2882957182))
def emojiBelow1243(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1237(hash,text,U32.is_lt(hash,2868004810))
    case False{}: emojiBelow1242(hash,text,U32.is_lt(hash,2881133443))
def emojiBelow1244(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1233(hash,text,U32.is_lt(hash,2854534825))
    case False{}: emojiBelow1243(hash,text,U32.is_lt(hash,2871390576))
def emojiBelow1245(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1223(hash,text,U32.is_lt(hash,2808128786))
    case False{}: emojiBelow1244(hash,text,U32.is_lt(hash,2866284064))
def emojiBelow1246(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow1247(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1248(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2638}\u{fe0f}")
    case False{}: emojiBelow1247(hash,text,U32.is_lt(hash,2888090063))
def emojiBelow1249(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1246(hash,text,U32.is_lt(hash,2884946056))
    case False{}: emojiBelow1248(hash,text,U32.is_lt(hash,2887512813))
def emojiBelow1250(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{2602}\u{fe0f}")
def emojiBelow1251(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1252(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1251(hash,text,U32.is_lt(hash,2897175458))
def emojiBelow1253(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1250(hash,text,U32.is_lt(hash,2893996158))
    case False{}: emojiBelow1252(hash,text,U32.is_lt(hash,2896157922))
def emojiBelow1254(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1249(hash,text,U32.is_lt(hash,2885432408))
    case False{}: emojiBelow1253(hash,text,U32.is_lt(hash,2894153812))
def emojiBelow1255(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow1256(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f52c}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f466}")
def emojiBelow1257(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: emojiBelow1256(hash,text,U32.is_lt(hash,2906557815))
def emojiBelow1258(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1255(hash,text,U32.is_lt(hash,2901723675))
    case False{}: emojiBelow1257(hash,text,U32.is_lt(hash,2906251687))
def emojiBelow1259(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f4bc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1260(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9bc}")
    case False{}: emojiBelow1259(hash,text,U32.is_lt(hash,2916219825))
def emojiBelow1261(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow1262(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1261(hash,text,U32.is_lt(hash,2918501294))
def emojiBelow1263(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1260(hash,text,U32.is_lt(hash,2910377714))
    case False{}: emojiBelow1262(hash,text,U32.is_lt(hash,2918017420))
def emojiBelow1264(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1258(hash,text,U32.is_lt(hash,2904867682))
    case False{}: emojiBelow1263(hash,text,U32.is_lt(hash,2917728893))
def emojiBelow1265(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1254(hash,text,U32.is_lt(hash,2889780196))
    case False{}: emojiBelow1264(hash,text,U32.is_lt(hash,2909242866))
def emojiBelow1266(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1267(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1268(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9d1}\u{200d}\u{1f9d2}\u{200d}\u{1f9d2}")
    case False{}: emojiBelow1267(hash,text,U32.is_lt(hash,2927120312))
def emojiBelow1269(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1266(hash,text,U32.is_lt(hash,2922308236))
    case False{}: emojiBelow1268(hash,text,U32.is_lt(hash,2926020485))
def emojiBelow1270(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3ff}")
def emojiBelow1271(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1270(hash,text,U32.is_lt(hash,2933394540))
def emojiBelow1272(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1273(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1272(hash,text,U32.is_lt(hash,2935278913))
def emojiBelow1274(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1271(hash,text,U32.is_lt(hash,2933339507))
    case False{}: emojiBelow1273(hash,text,U32.is_lt(hash,2935116504))
def emojiBelow1275(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1269(hash,text,U32.is_lt(hash,2924164090))
    case False{}: emojiBelow1274(hash,text,U32.is_lt(hash,2934941005))
def emojiBelow1276(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow1277(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{3299}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1278(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9b2}")
    case False{}: emojiBelow1277(hash,text,U32.is_lt(hash,2948411523))
def emojiBelow1279(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1276(hash,text,U32.is_lt(hash,2939085855))
    case False{}: emojiBelow1278(hash,text,U32.is_lt(hash,2943869377))
def emojiBelow1280(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{26f9}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1281(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: emojiBelow1280(hash,text,U32.is_lt(hash,2952050092))
def emojiBelow1282(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9b3}")
def emojiBelow1283(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow1282(hash,text,U32.is_lt(hash,2959575723))
def emojiBelow1284(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1281(hash,text,U32.is_lt(hash,2951894123))
    case False{}: emojiBelow1283(hash,text,U32.is_lt(hash,2959221475))
def emojiBelow1285(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1279(hash,text,U32.is_lt(hash,2942798104))
    case False{}: emojiBelow1284(hash,text,U32.is_lt(hash,2955863474))
def emojiBelow1286(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1275(hash,text,U32.is_lt(hash,2928914911))
    case False{}: emojiBelow1285(hash,text,U32.is_lt(hash,2950172159))
def emojiBelow1287(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1265(hash,text,U32.is_lt(hash,2901391406))
    case False{}: emojiBelow1286(hash,text,U32.is_lt(hash,2938588215))
def emojiBelow1288(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1245(hash,text,U32.is_lt(hash,2849092910))
    case False{}: emojiBelow1287(hash,text,U32.is_lt(hash,2921645301))
def emojiBelow1289(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1290(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1291(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1290(hash,text,U32.is_lt(hash,2968671742))
def emojiBelow1292(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1289(hash,text,U32.is_lt(hash,2963241420))
    case False{}: emojiBelow1291(hash,text,U32.is_lt(hash,2966949778))
def emojiBelow1293(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9b0}")
def emojiBelow1294(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d7}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1295(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1294(hash,text,U32.is_lt(hash,2989110719))
def emojiBelow1296(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1293(hash,text,U32.is_lt(hash,2976353342))
    case False{}: emojiBelow1295(hash,text,U32.is_lt(hash,2985449361))
def emojiBelow1297(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1292(hash,text,U32.is_lt(hash,2963396097))
    case False{}: emojiBelow1296(hash,text,U32.is_lt(hash,2981002980))
def emojiBelow1298(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9b1}")
def emojiBelow1299(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow1300(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1299(hash,text,U32.is_lt(hash,3002226980))
def emojiBelow1301(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1298(hash,text,U32.is_lt(hash,2993130961))
    case False{}: emojiBelow1300(hash,text,U32.is_lt(hash,3000216010))
def emojiBelow1302(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow1303(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow1302(hash,text,U32.is_lt(hash,3009314600))
def emojiBelow1304(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1305(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1304(hash,text,U32.is_lt(hash,3022976123))
def emojiBelow1306(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1303(hash,text,U32.is_lt(hash,3006636996))
    case False{}: emojiBelow1305(hash,text,U32.is_lt(hash,3014110308))
def emojiBelow1307(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1301(hash,text,U32.is_lt(hash,2998847725))
    case False{}: emojiBelow1306(hash,text,U32.is_lt(hash,3011990379))
def emojiBelow1308(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1297(hash,text,U32.is_lt(hash,2973785657))
    case False{}: emojiBelow1307(hash,text,U32.is_lt(hash,3002389389))
def emojiBelow1309(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f52c}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1310(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f469}\u{200d}\u{1f466}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fd}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1311(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1310(hash,text,U32.is_lt(hash,3037366436))
def emojiBelow1312(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1309(hash,text,U32.is_lt(hash,3026092219))
    case False{}: emojiBelow1311(hash,text,U32.is_lt(hash,3033843610))
def emojiBelow1313(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f441}\u{fe0f}\u{200d}\u{1f5e8}\u{fe0f}")
def emojiBelow1314(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: emojiBelow1313(hash,text,U32.is_lt(hash,3044906729))
def emojiBelow1315(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9de}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3cc}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1316(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow1315(hash,text,U32.is_lt(hash,3048129122))
def emojiBelow1317(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1314(hash,text,U32.is_lt(hash,3043251428))
    case False{}: emojiBelow1316(hash,text,U32.is_lt(hash,3045620377))
def emojiBelow1318(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1312(hash,text,U32.is_lt(hash,3028191765))
    case False{}: emojiBelow1317(hash,text,U32.is_lt(hash,3045087528))
def emojiBelow1319(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f469}\u{200d}\u{1f467}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9af}")
def emojiBelow1320(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow1321(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow1320(hash,text,U32.is_lt(hash,3061865147))
def emojiBelow1322(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1319(hash,text,U32.is_lt(hash,3054912705))
    case False{}: emojiBelow1321(hash,text,U32.is_lt(hash,3059647457))
def emojiBelow1323(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5d1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{2696}\u{fe0f}")
def emojiBelow1324(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow1323(hash,text,U32.is_lt(hash,3067412576))
def emojiBelow1325(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f37c}")
def emojiBelow1326(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5c3}\u{fe0f}")
    case False{}: emojiBelow1325(hash,text,U32.is_lt(hash,3071991007))
def emojiBelow1327(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1324(hash,text,U32.is_lt(hash,3065987081))
    case False{}: emojiBelow1326(hash,text,U32.is_lt(hash,3070078660))
def emojiBelow1328(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1322(hash,text,U32.is_lt(hash,3057009016))
    case False{}: emojiBelow1327(hash,text,U32.is_lt(hash,3068260807))
def emojiBelow1329(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1318(hash,text,U32.is_lt(hash,3042869838))
    case False{}: emojiBelow1328(hash,text,U32.is_lt(hash,3065027910))
def emojiBelow1330(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1308(hash,text,U32.is_lt(hash,2990365742))
    case False{}: emojiBelow1329(hash,text,U32.is_lt(hash,3050621229))
def emojiBelow1331(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1332(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow1333(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1332(hash,text,U32.is_lt(hash,3073786635))
def emojiBelow1334(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1331(hash,text,U32.is_lt(hash,3072378786))
    case False{}: emojiBelow1333(hash,text,U32.is_lt(hash,3072722004))
def emojiBelow1335(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1336(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f37c}")
    case False{}: emojiBelow1335(hash,text,U32.is_lt(hash,3081503207))
def emojiBelow1337(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1e6}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow1338(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f3}\u{1f1e6}")
    case False{}: emojiBelow1337(hash,text,U32.is_lt(hash,3090564254))
def emojiBelow1339(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1336(hash,text,U32.is_lt(hash,3078642766))
    case False{}: emojiBelow1338(hash,text,U32.is_lt(hash,3089854240))
def emojiBelow1340(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1334(hash,text,U32.is_lt(hash,3072516530))
    case False{}: emojiBelow1339(hash,text,U32.is_lt(hash,3086606060))
def emojiBelow1341(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{1f579}\u{fe0f}")
def emojiBelow1342(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f3eb}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1343(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow1342(hash,text,U32.is_lt(hash,3097838412))
def emojiBelow1344(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1341(hash,text,U32.is_lt(hash,3091627553))
    case False{}: emojiBelow1343(hash,text,U32.is_lt(hash,3097150795))
def emojiBelow1345(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1e7}")
    case False{}: Strings.equal(text,"\u{1f5bc}\u{fe0f}")
def emojiBelow1346(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1345(hash,text,U32.is_lt(hash,3102199452))
def emojiBelow1347(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f33e}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1348(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f52c}")
    case False{}: emojiBelow1347(hash,text,U32.is_lt(hash,3104192857))
def emojiBelow1349(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1346(hash,text,U32.is_lt(hash,3101421472))
    case False{}: emojiBelow1348(hash,text,U32.is_lt(hash,3102766586))
def emojiBelow1350(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1344(hash,text,U32.is_lt(hash,3095420385))
    case False{}: emojiBelow1349(hash,text,U32.is_lt(hash,3102571229))
def emojiBelow1351(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1340(hash,text,U32.is_lt(hash,3074437173))
    case False{}: emojiBelow1350(hash,text,U32.is_lt(hash,3100264228))
def emojiBelow1352(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3fb}")
def emojiBelow1353(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1e7}")
def emojiBelow1354(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fb}")
    case False{}: emojiBelow1353(hash,text,U32.is_lt(hash,3106631859))
def emojiBelow1355(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1352(hash,text,U32.is_lt(hash,3104733801))
    case False{}: emojiBelow1354(hash,text,U32.is_lt(hash,3106421838))
def emojiBelow1356(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1357(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f934}\u{1f3fb}")
    case False{}: emojiBelow1356(hash,text,U32.is_lt(hash,3106960147))
def emojiBelow1358(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f933}\u{1f3fb}")
def emojiBelow1359(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f936}\u{1f3fb}")
    case False{}: emojiBelow1358(hash,text,U32.is_lt(hash,3107534651))
def emojiBelow1360(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1357(hash,text,U32.is_lt(hash,3106858417))
    case False{}: emojiBelow1359(hash,text,U32.is_lt(hash,3107341873))
def emojiBelow1361(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1355(hash,text,U32.is_lt(hash,3105071918))
    case False{}: emojiBelow1360(hash,text,U32.is_lt(hash,3107196534))
def emojiBelow1362(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f527}")
def emojiBelow1363(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91d}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f91e}\u{1f3fb}")
def emojiBelow1364(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91c}\u{1f3fb}")
    case False{}: emojiBelow1363(hash,text,U32.is_lt(hash,3110269006))
def emojiBelow1365(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1362(hash,text,U32.is_lt(hash,3108761800))
    case False{}: emojiBelow1364(hash,text,U32.is_lt(hash,3109930889))
def emojiBelow1366(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f91b}\u{1f3fb}")
def emojiBelow1367(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1366(hash,text,U32.is_lt(hash,3110607123))
def emojiBelow1368(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f90c}\u{1f3fb}")
def emojiBelow1369(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow1368(hash,text,U32.is_lt(hash,3114017712))
def emojiBelow1370(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1367(hash,text,U32.is_lt(hash,3110478313))
    case False{}: emojiBelow1369(hash,text,U32.is_lt(hash,3111605974))
def emojiBelow1371(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1365(hash,text,U32.is_lt(hash,3109768480))
    case False{}: emojiBelow1370(hash,text,U32.is_lt(hash,3110634768))
def emojiBelow1372(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1361(hash,text,U32.is_lt(hash,3106696008))
    case False{}: emojiBelow1371(hash,text,U32.is_lt(hash,3110476300))
def emojiBelow1373(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1351(hash,text,U32.is_lt(hash,3091214792))
    case False{}: emojiBelow1372(hash,text,U32.is_lt(hash,3108144390))
def emojiBelow1374(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1330(hash,text,U32.is_lt(hash,3024873705))
    case False{}: emojiBelow1373(hash,text,U32.is_lt(hash,3104571392))
def emojiBelow1375(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1288(hash,text,U32.is_lt(hash,2884782429))
    case False{}: emojiBelow1374(hash,text,U32.is_lt(hash,3072018277))
def emojiBelow1376(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1203(hash,text,U32.is_lt(hash,2560595070))
    case False{}: emojiBelow1375(hash,text,U32.is_lt(hash,2962728303))
def emojiBelow1377(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1032(hash,text,U32.is_lt(hash,2000669557))
    case False{}: emojiBelow1376(hash,text,U32.is_lt(hash,2774029949))
def emojiBelow1378(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow688(hash,text,U32.is_lt(hash,757390669))
    case False{}: emojiBelow1377(hash,text,U32.is_lt(hash,2361651221))
def emojiBelow1379(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1e6}")
    case False{}: Strings.equal(text,"\u{1f470}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1380(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3fc}")
def emojiBelow1381(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1380(hash,text,U32.is_lt(hash,3121511420))
def emojiBelow1382(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1379(hash,text,U32.is_lt(hash,3119816831))
    case False{}: emojiBelow1381(hash,text,U32.is_lt(hash,3121119532))
def emojiBelow1383(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f935}\u{1f3fc}")
def emojiBelow1384(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9bb}\u{1f3fb}")
def emojiBelow1385(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f642}\u{200d}\u{2195}\u{fe0f}")
    case False{}: emojiBelow1384(hash,text,U32.is_lt(hash,3124547699))
def emojiBelow1386(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1383(hash,text,U32.is_lt(hash,3123636036))
    case False{}: emojiBelow1385(hash,text,U32.is_lt(hash,3124119492))
def emojiBelow1387(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1382(hash,text,U32.is_lt(hash,3119860098))
    case False{}: emojiBelow1386(hash,text,U32.is_lt(hash,3123940200))
def emojiBelow1388(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f931}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9b6}\u{1f3fb}")
def emojiBelow1389(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f919}\u{1f3fc}")
def emojiBelow1390(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91d}\u{1f3fc}")
    case False{}: emojiBelow1389(hash,text,U32.is_lt(hash,3126884216))
def emojiBelow1391(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1388(hash,text,U32.is_lt(hash,3126334198))
    case False{}: emojiBelow1390(hash,text,U32.is_lt(hash,3126757933))
def emojiBelow1392(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2708}\u{fe0f}")
def emojiBelow1393(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow1392(hash,text,U32.is_lt(hash,3127464518))
def emojiBelow1394(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2935}\u{fe0f}")
def emojiBelow1395(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b5}\u{1f3fb}")
    case False{}: emojiBelow1394(hash,text,U32.is_lt(hash,3134525453))
def emojiBelow1396(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1393(hash,text,U32.is_lt(hash,3127412387))
    case False{}: emojiBelow1395(hash,text,U32.is_lt(hash,3132509000))
def emojiBelow1397(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1391(hash,text,U32.is_lt(hash,3126708508))
    case False{}: emojiBelow1396(hash,text,U32.is_lt(hash,3129297457))
def emojiBelow1398(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1387(hash,text,U32.is_lt(hash,3122512472))
    case False{}: emojiBelow1397(hash,text,U32.is_lt(hash,3127255932))
def emojiBelow1399(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1400(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3fd}")
def emojiBelow1401(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow1400(hash,text,U32.is_lt(hash,3138289039))
def emojiBelow1402(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1399(hash,text,U32.is_lt(hash,3136468110))
    case False{}: emojiBelow1401(hash,text,U32.is_lt(hash,3137931141))
def emojiBelow1403(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f939}\u{1f3fd}")
def emojiBelow1404(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f932}\u{1f3fd}")
    case False{}: emojiBelow1403(hash,text,U32.is_lt(hash,3139290091))
def emojiBelow1405(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f936}\u{1f3fd}")
def emojiBelow1406(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow1405(hash,text,U32.is_lt(hash,3140751772))
def emojiBelow1407(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1404(hash,text,U32.is_lt(hash,3138627156))
    case False{}: emojiBelow1406(hash,text,U32.is_lt(hash,3140413655))
def emojiBelow1408(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1402(hash,text,U32.is_lt(hash,3137897151))
    case False{}: emojiBelow1407(hash,text,U32.is_lt(hash,3139977076))
def emojiBelow1409(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9dd}\u{1f3fb}")
def emojiBelow1410(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fb}")
def emojiBelow1411(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f931}\u{1f3fd}")
    case False{}: emojiBelow1410(hash,text,U32.is_lt(hash,3141789651))
def emojiBelow1412(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1409(hash,text,U32.is_lt(hash,3141113417))
    case False{}: emojiBelow1411(hash,text,U32.is_lt(hash,3141699628))
def emojiBelow1413(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d5}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f91d}\u{1f3fd}")
def emojiBelow1414(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fb}")
    case False{}: emojiBelow1413(hash,text,U32.is_lt(hash,3143486127))
def emojiBelow1415(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f919}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f91e}\u{1f3fd}")
def emojiBelow1416(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fb}")
    case False{}: emojiBelow1415(hash,text,U32.is_lt(hash,3143824244))
def emojiBelow1417(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1414(hash,text,U32.is_lt(hash,3143238033))
    case False{}: emojiBelow1416(hash,text,U32.is_lt(hash,3143661835))
def emojiBelow1418(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1412(hash,text,U32.is_lt(hash,3141414707))
    case False{}: emojiBelow1417(hash,text,U32.is_lt(hash,3143576150))
def emojiBelow1419(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1408(hash,text,U32.is_lt(hash,3138451448))
    case False{}: emojiBelow1418(hash,text,U32.is_lt(hash,3143075624))
def emojiBelow1420(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1398(hash,text,U32.is_lt(hash,3124637088))
    case False{}: emojiBelow1419(hash,text,U32.is_lt(hash,3140951008))
def emojiBelow1421(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d3}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f91a}\u{1f3fd}")
def emojiBelow1422(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow1423(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow1422(hash,text,U32.is_lt(hash,3144190006))
def emojiBelow1424(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1421(hash,text,U32.is_lt(hash,3143999952))
    case False{}: emojiBelow1423(hash,text,U32.is_lt(hash,3144126168))
def emojiBelow1425(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cd}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fb}")
def emojiBelow1426(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b5}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f33e}")
def emojiBelow1427(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: emojiBelow1426(hash,text,U32.is_lt(hash,3151008245))
def emojiBelow1428(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1425(hash,text,U32.is_lt(hash,3145700766))
    case False{}: emojiBelow1427(hash,text,U32.is_lt(hash,3146075076))
def emojiBelow1429(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1424(hash,text,U32.is_lt(hash,3144033551))
    case False{}: emojiBelow1428(hash,text,U32.is_lt(hash,3145753242))
def emojiBelow1430(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1ea}")
def emojiBelow1431(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f932}\u{1f3fc}")
def emojiBelow1432(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow1431(hash,text,U32.is_lt(hash,3155229067))
def emojiBelow1433(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1430(hash,text,U32.is_lt(hash,3153716536))
    case False{}: emojiBelow1432(hash,text,U32.is_lt(hash,3155066658))
def emojiBelow1434(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{1f939}\u{1f3fe}")
def emojiBelow1435(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fc}")
    case False{}: emojiBelow1434(hash,text,U32.is_lt(hash,3156067710))
def emojiBelow1436(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f936}\u{1f3fc}")
def emojiBelow1437(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow1436(hash,text,U32.is_lt(hash,3157529391))
def emojiBelow1438(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1435(hash,text,U32.is_lt(hash,3155879102))
    case False{}: emojiBelow1437(hash,text,U32.is_lt(hash,3157191274))
def emojiBelow1439(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1433(hash,text,U32.is_lt(hash,3154674770))
    case False{}: emojiBelow1438(hash,text,U32.is_lt(hash,3156754695))
def emojiBelow1440(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1429(hash,text,U32.is_lt(hash,3145362649))
    case False{}: emojiBelow1439(hash,text,U32.is_lt(hash,3155404775))
def emojiBelow1441(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f933}\u{1f3fe}")
def emojiBelow1442(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f931}\u{1f3fe}")
def emojiBelow1443(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fc}")
    case False{}: emojiBelow1442(hash,text,U32.is_lt(hash,3158192326))
def emojiBelow1444(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1441(hash,text,U32.is_lt(hash,3157867508))
    case False{}: emojiBelow1443(hash,text,U32.is_lt(hash,3158121612))
def emojiBelow1445(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d9}\u{1f3fc}")
def emojiBelow1446(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fc}")
    case False{}: emojiBelow1445(hash,text,U32.is_lt(hash,3158892088))
def emojiBelow1447(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91f}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d5}\u{1f3fc}")
def emojiBelow1448(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b6}\u{1f3fd}")
    case False{}: emojiBelow1447(hash,text,U32.is_lt(hash,3160015652))
def emojiBelow1449(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1446(hash,text,U32.is_lt(hash,3158514782))
    case False{}: emojiBelow1448(hash,text,U32.is_lt(hash,3159938928))
def emojiBelow1450(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1444(hash,text,U32.is_lt(hash,3157891036))
    case False{}: emojiBelow1449(hash,text,U32.is_lt(hash,3159889436))
def emojiBelow1451(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f91d}\u{1f3fe}")
def emojiBelow1452(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91e}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f91a}\u{1f3fc}")
def emojiBelow1453(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f919}\u{1f3fe}")
    case False{}: emojiBelow1452(hash,text,U32.is_lt(hash,3160777571))
def emojiBelow1454(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1451(hash,text,U32.is_lt(hash,3160263746))
    case False{}: emojiBelow1453(hash,text,U32.is_lt(hash,3160601863))
def emojiBelow1455(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f9af}")
    case False{}: Strings.equal(text,"\u{1f91b}\u{1f3fe}")
def emojiBelow1456(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fd}")
    case False{}: emojiBelow1455(hash,text,U32.is_lt(hash,3160939980))
def emojiBelow1457(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cd}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f33e}")
def emojiBelow1458(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow1457(hash,text,U32.is_lt(hash,3162642130))
def emojiBelow1459(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1456(hash,text,U32.is_lt(hash,3160907151))
    case False{}: emojiBelow1458(hash,text,U32.is_lt(hash,3162140268))
def emojiBelow1460(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1454(hash,text,U32.is_lt(hash,3160439454))
    case False{}: emojiBelow1459(hash,text,U32.is_lt(hash,3160967625))
def emojiBelow1461(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1450(hash,text,U32.is_lt(hash,3158477247))
    case False{}: emojiBelow1460(hash,text,U32.is_lt(hash,3160903787))
def emojiBelow1462(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1440(hash,text,U32.is_lt(hash,3151946882))
    case False{}: emojiBelow1461(hash,text,U32.is_lt(hash,3160191360))
def emojiBelow1463(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1420(hash,text,U32.is_lt(hash,3135877358))
    case False{}: emojiBelow1462(hash,text,U32.is_lt(hash,3157691800))
def emojiBelow1464(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b5}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f90f}\u{1f3fe}")
def emojiBelow1465(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f977}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1466(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow1465(hash,text,U32.is_lt(hash,3168018458))
def emojiBelow1467(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1464(hash,text,U32.is_lt(hash,3164188160))
    case False{}: emojiBelow1466(hash,text,U32.is_lt(hash,3167260632))
def emojiBelow1468(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f3}\u{1f1eb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9b3}")
def emojiBelow1469(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f1ec}\u{1f1eb}")
def emojiBelow1470(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow1469(hash,text,U32.is_lt(hash,3171780128))
def emojiBelow1471(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1468(hash,text,U32.is_lt(hash,3170764300))
    case False{}: emojiBelow1470(hash,text,U32.is_lt(hash,3171681868))
def emojiBelow1472(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1467(hash,text,U32.is_lt(hash,3165037470))
    case False{}: emojiBelow1471(hash,text,U32.is_lt(hash,3171452389))
def emojiBelow1473(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f932}\u{1f3ff}")
def emojiBelow1474(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f939}\u{1f3ff}")
def emojiBelow1475(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3ff}")
    case False{}: emojiBelow1474(hash,text,U32.is_lt(hash,3172845329))
def emojiBelow1476(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1473(hash,text,U32.is_lt(hash,3172006686))
    case False{}: emojiBelow1475(hash,text,U32.is_lt(hash,3172682920))
def emojiBelow1477(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9db}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1478(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: emojiBelow1477(hash,text,U32.is_lt(hash,3173532314))
def emojiBelow1479(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f936}\u{1f3ff}")
def emojiBelow1480(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f934}\u{1f3ff}")
    case False{}: emojiBelow1479(hash,text,U32.is_lt(hash,3174307010))
def emojiBelow1481(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1478(hash,text,U32.is_lt(hash,3173361673))
    case False{}: emojiBelow1480(hash,text,U32.is_lt(hash,3173968893))
def emojiBelow1482(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1476(hash,text,U32.is_lt(hash,3172182394))
    case False{}: emojiBelow1481(hash,text,U32.is_lt(hash,3173806484))
def emojiBelow1483(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1472(hash,text,U32.is_lt(hash,3170494155))
    case False{}: emojiBelow1482(hash,text,U32.is_lt(hash,3172869379))
def emojiBelow1484(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f933}\u{1f3ff}")
def emojiBelow1485(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f930}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d2}\u{1f3fd}")
def emojiBelow1486(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fd}")
    case False{}: emojiBelow1485(hash,text,U32.is_lt(hash,3174831064))
def emojiBelow1487(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1484(hash,text,U32.is_lt(hash,3174645127))
    case False{}: emojiBelow1486(hash,text,U32.is_lt(hash,3174807536))
def emojiBelow1488(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f931}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f926}\u{1f3ff}")
def emojiBelow1489(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9bb}\u{1f3fe}")
    case False{}: emojiBelow1488(hash,text,U32.is_lt(hash,3175254866))
def emojiBelow1490(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9b6}\u{1f3fc}")
def emojiBelow1491(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fd}")
    case False{}: emojiBelow1490(hash,text,U32.is_lt(hash,3176667055))
def emojiBelow1492(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1489(hash,text,U32.is_lt(hash,3174969945))
    case False{}: emojiBelow1491(hash,text,U32.is_lt(hash,3176007824))
def emojiBelow1493(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1487(hash,text,U32.is_lt(hash,3174668655))
    case False{}: emojiBelow1492(hash,text,U32.is_lt(hash,3175669707))
def emojiBelow1494(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91f}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d5}\u{1f3fd}")
def emojiBelow1495(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f91d}\u{1f3ff}")
def emojiBelow1496(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91c}\u{1f3ff}")
    case False{}: emojiBelow1495(hash,text,U32.is_lt(hash,3177041365))
def emojiBelow1497(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1494(hash,text,U32.is_lt(hash,3176793271))
    case False{}: emojiBelow1496(hash,text,U32.is_lt(hash,3176968979))
def emojiBelow1498(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f919}\u{1f3ff}")
def emojiBelow1499(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f918}\u{1f3ff}")
    case False{}: emojiBelow1498(hash,text,U32.is_lt(hash,3177217073))
def emojiBelow1500(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91a}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1501(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91e}\u{1f3ff}")
    case False{}: emojiBelow1500(hash,text,U32.is_lt(hash,3177588789))
def emojiBelow1502(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1499(hash,text,U32.is_lt(hash,3177131388))
    case False{}: emojiBelow1501(hash,text,U32.is_lt(hash,3177555190))
def emojiBelow1503(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1497(hash,text,U32.is_lt(hash,3176878956))
    case False{}: emojiBelow1502(hash,text,U32.is_lt(hash,3177379482))
def emojiBelow1504(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1493(hash,text,U32.is_lt(hash,3174880556))
    case False{}: emojiBelow1503(hash,text,U32.is_lt(hash,3177054664))
def emojiBelow1505(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1483(hash,text,U32.is_lt(hash,3171844277))
    case False{}: emojiBelow1504(hash,text,U32.is_lt(hash,3176716547))
def emojiBelow1506(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f91b}\u{1f3ff}")
def emojiBelow1507(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9b5}\u{1f3fe}")
def emojiBelow1508(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cd}\u{1f3fd}")
    case False{}: emojiBelow1507(hash,text,U32.is_lt(hash,3179630314))
def emojiBelow1509(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1506(hash,text,U32.is_lt(hash,3177717599))
    case False{}: emojiBelow1508(hash,text,U32.is_lt(hash,3179256004))
def emojiBelow1510(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f3eb}")
    case False{}: Strings.equal(text,"\u{1f90f}\u{1f3ff}")
def emojiBelow1511(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f977}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d6}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1512(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f90c}\u{1f3ff}")
    case False{}: emojiBelow1511(hash,text,U32.is_lt(hash,3184099224))
def emojiBelow1513(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1510(hash,text,U32.is_lt(hash,3180965779))
    case False{}: emojiBelow1512(hash,text,U32.is_lt(hash,3184038251))
def emojiBelow1514(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1509(hash,text,U32.is_lt(hash,3178917887))
    case False{}: emojiBelow1513(hash,text,U32.is_lt(hash,3181128188))
def emojiBelow1515(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fe}\u{1f1ea}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1e8}")
def emojiBelow1516(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f466}\u{200d}\u{1f466}")
    case False{}: Strings.equal(text,"\u{1f93c}\u{1f3fe}")
def emojiBelow1517(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9b2}")
    case False{}: emojiBelow1516(hash,text,U32.is_lt(hash,3188459487))
def emojiBelow1518(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1515(hash,text,U32.is_lt(hash,3187271774))
    case False{}: emojiBelow1517(hash,text,U32.is_lt(hash,3187932344))
def emojiBelow1519(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f932}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f1e9}\u{1f1ec}")
def emojiBelow1520(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1ea}")
    case False{}: emojiBelow1519(hash,text,U32.is_lt(hash,3188895864))
def emojiBelow1521(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow1522(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fe}")
    case False{}: emojiBelow1521(hash,text,U32.is_lt(hash,3189646998))
def emojiBelow1523(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1520(hash,text,U32.is_lt(hash,3188784305))
    case False{}: emojiBelow1522(hash,text,U32.is_lt(hash,3189460539))
def emojiBelow1524(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1518(hash,text,U32.is_lt(hash,3187541919))
    case False{}: emojiBelow1523(hash,text,U32.is_lt(hash,3188960013))
def emojiBelow1525(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1514(hash,text,U32.is_lt(hash,3180957824))
    case False{}: emojiBelow1524(hash,text,U32.is_lt(hash,3188557747))
def emojiBelow1526(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f934}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f936}\u{1f3fe}")
def emojiBelow1527(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f933}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9dd}\u{1f3fe}")
def emojiBelow1528(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3fc}")
    case False{}: emojiBelow1527(hash,text,U32.is_lt(hash,3191446274))
def emojiBelow1529(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1526(hash,text,U32.is_lt(hash,3191084629))
    case False{}: emojiBelow1528(hash,text,U32.is_lt(hash,3191422746))
def emojiBelow1530(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d2}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9bb}\u{1f3ff}")
def emojiBelow1531(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f930}\u{1f3fe}")
    case False{}: emojiBelow1530(hash,text,U32.is_lt(hash,3191658175))
def emojiBelow1532(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f926}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fe}")
def emojiBelow1533(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3ff}")
    case False{}: emojiBelow1532(hash,text,U32.is_lt(hash,3192122508))
def emojiBelow1534(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1531(hash,text,U32.is_lt(hash,3191608683))
    case False{}: emojiBelow1533(hash,text,U32.is_lt(hash,3192032485))
def emojiBelow1535(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1529(hash,text,U32.is_lt(hash,3191247038))
    case False{}: emojiBelow1534(hash,text,U32.is_lt(hash,3191820584))
def emojiBelow1536(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9da}\u{1f3fc}")
def emojiBelow1537(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91f}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d5}\u{1f3fe}")
def emojiBelow1538(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b6}\u{1f3ff}")
    case False{}: emojiBelow1537(hash,text,U32.is_lt(hash,3193570890))
def emojiBelow1539(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1536(hash,text,U32.is_lt(hash,3192785443))
    case False{}: emojiBelow1538(hash,text,U32.is_lt(hash,3193494166))
def emojiBelow1540(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f918}\u{1f3fe}")
def emojiBelow1541(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91c}\u{1f3fe}")
    case False{}: emojiBelow1540(hash,text,U32.is_lt(hash,3193832283))
def emojiBelow1542(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f91e}\u{1f3fe}")
def emojiBelow1543(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fc}")
    case False{}: emojiBelow1542(hash,text,U32.is_lt(hash,3194157101))
def emojiBelow1544(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1541(hash,text,U32.is_lt(hash,3193746598))
    case False{}: emojiBelow1543(hash,text,U32.is_lt(hash,3194071416))
def emojiBelow1545(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1539(hash,text,U32.is_lt(hash,3193444674))
    case False{}: emojiBelow1544(hash,text,U32.is_lt(hash,3193909007))
def emojiBelow1546(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1535(hash,text,U32.is_lt(hash,3191585155))
    case False{}: emojiBelow1545(hash,text,U32.is_lt(hash,3193656575))
def emojiBelow1547(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1525(hash,text,U32.is_lt(hash,3184809041))
    case False{}: emojiBelow1546(hash,text,U32.is_lt(hash,3192447326))
def emojiBelow1548(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1505(hash,text,U32.is_lt(hash,3174469419))
    case False{}: emojiBelow1547(hash,text,U32.is_lt(hash,3190584103))
def emojiBelow1549(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1463(hash,text,U32.is_lt(hash,3143914267))
    case False{}: emojiBelow1548(hash,text,U32.is_lt(hash,3177681406))
def emojiBelow1550(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d3}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f91a}\u{1f3fe}")
def emojiBelow1551(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91b}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow1552(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3ff}")
    case False{}: emojiBelow1551(hash,text,U32.is_lt(hash,3194522863))
def emojiBelow1553(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1550(hash,text,U32.is_lt(hash,3194332809))
    case False{}: emojiBelow1552(hash,text,U32.is_lt(hash,3194495218))
def emojiBelow1554(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3fe}")
def emojiBelow1555(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b5}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f90f}\u{1f3fc}")
def emojiBelow1556(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fc}")
    case False{}: emojiBelow1555(hash,text,U32.is_lt(hash,3197743398))
def emojiBelow1557(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1554(hash,text,U32.is_lt(hash,3195695506))
    case False{}: emojiBelow1556(hash,text,U32.is_lt(hash,3196407933))
def emojiBelow1558(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1553(hash,text,U32.is_lt(hash,3194459025))
    case False{}: emojiBelow1557(hash,text,U32.is_lt(hash,3196033623))
def emojiBelow1559(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f3ed}")
    case False{}: Strings.equal(text,"\u{1f90c}\u{1f3fe}")
def emojiBelow1560(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f977}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1561(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{33}\u{fe0f}\u{20e3}")
    case False{}: emojiBelow1560(hash,text,U32.is_lt(hash,3200940646))
def emojiBelow1562(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1559(hash,text,U32.is_lt(hash,3197905807))
    case False{}: emojiBelow1561(hash,text,U32.is_lt(hash,3200815870))
def emojiBelow1563(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f93c}\u{1f3fd}")
def emojiBelow1564(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6e4}\u{fe0f}")
    case False{}: emojiBelow1563(hash,text,U32.is_lt(hash,3205237106))
def emojiBelow1565(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1566(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1e9}")
    case False{}: emojiBelow1565(hash,text,U32.is_lt(hash,3206424617))
def emojiBelow1567(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1564(hash,text,U32.is_lt(hash,3204319538))
    case False{}: emojiBelow1566(hash,text,U32.is_lt(hash,3206238158))
def emojiBelow1568(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1562(hash,text,U32.is_lt(hash,3198409916))
    case False{}: emojiBelow1567(hash,text,U32.is_lt(hash,3205335366))
def emojiBelow1569(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1558(hash,text,U32.is_lt(hash,3195370688))
    case False{}: emojiBelow1568(hash,text,U32.is_lt(hash,3203152564))
def emojiBelow1570(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f934}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3fd}")
def emojiBelow1571(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f933}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9dd}\u{1f3ff}")
def emojiBelow1572(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3ff}")
    case False{}: emojiBelow1571(hash,text,U32.is_lt(hash,3208223893))
def emojiBelow1573(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1570(hash,text,U32.is_lt(hash,3208024657))
    case False{}: emojiBelow1572(hash,text,U32.is_lt(hash,3208200365))
def emojiBelow1574(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d2}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9bb}\u{1f3fc}")
def emojiBelow1575(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f930}\u{1f3fd}")
    case False{}: emojiBelow1574(hash,text,U32.is_lt(hash,3208435794))
def emojiBelow1576(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3ff}")
def emojiBelow1577(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fe}")
    case False{}: emojiBelow1576(hash,text,U32.is_lt(hash,3208900127))
def emojiBelow1578(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1575(hash,text,U32.is_lt(hash,3208386302))
    case False{}: emojiBelow1577(hash,text,U32.is_lt(hash,3208779369))
def emojiBelow1579(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1573(hash,text,U32.is_lt(hash,3208061484))
    case False{}: emojiBelow1578(hash,text,U32.is_lt(hash,3208598203))
def emojiBelow1580(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d8}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d9}\u{1f3ff}")
def emojiBelow1581(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9b6}\u{1f3fe}")
def emojiBelow1582(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3ff}")
    case False{}: emojiBelow1581(hash,text,U32.is_lt(hash,3210222293))
def emojiBelow1583(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1580(hash,text,U32.is_lt(hash,3209224945))
    case False{}: emojiBelow1582(hash,text,U32.is_lt(hash,3210186100))
def emojiBelow1584(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d5}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f91c}\u{1f3fd}")
def emojiBelow1585(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91f}\u{1f3fd}")
    case False{}: emojiBelow1584(hash,text,U32.is_lt(hash,3210434194))
def emojiBelow1586(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f918}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d6}\u{1f3ff}")
def emojiBelow1587(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow1586(hash,text,U32.is_lt(hash,3210686626))
def emojiBelow1588(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1585(hash,text,U32.is_lt(hash,3210348509))
    case False{}: emojiBelow1587(hash,text,U32.is_lt(hash,3210609902))
def emojiBelow1589(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1583(hash,text,U32.is_lt(hash,3209563062))
    case False{}: emojiBelow1588(hash,text,U32.is_lt(hash,3210524217))
def emojiBelow1590(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1579(hash,text,U32.is_lt(hash,3208362774))
    case False{}: emojiBelow1589(hash,text,U32.is_lt(hash,3210271785))
def emojiBelow1591(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1569(hash,text,U32.is_lt(hash,3197816509))
    case False{}: emojiBelow1590(hash,text,U32.is_lt(hash,3209062536))
def emojiBelow1592(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9d3}\u{1f3ff}")
def emojiBelow1593(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{1f3ff}")
def emojiBelow1594(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91b}\u{1f3fd}")
    case False{}: emojiBelow1593(hash,text,U32.is_lt(hash,3212473125))
def emojiBelow1595(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1592(hash,text,U32.is_lt(hash,3211024743))
    case False{}: emojiBelow1594(hash,text,U32.is_lt(hash,3212148307))
def emojiBelow1596(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f90f}\u{1f3fd}")
def emojiBelow1597(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f977}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f1fa}\u{1f1ec}")
def emojiBelow1598(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f90c}\u{1f3fd}")
    case False{}: emojiBelow1597(hash,text,U32.is_lt(hash,3218952659))
def emojiBelow1599(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1596(hash,text,U32.is_lt(hash,3214521017))
    case False{}: emojiBelow1598(hash,text,U32.is_lt(hash,3217593489))
def emojiBelow1600(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1595(hash,text,U32.is_lt(hash,3211272837))
    case False{}: emojiBelow1599(hash,text,U32.is_lt(hash,3214683426))
def emojiBelow1601(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1ee}")
def emojiBelow1602(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1eb}\u{1f1ee}")
def emojiBelow1603(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9b0}")
    case False{}: emojiBelow1602(hash,text,U32.is_lt(hash,3222951628))
def emojiBelow1604(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1601(hash,text,U32.is_lt(hash,3220827012))
    case False{}: emojiBelow1603(hash,text,U32.is_lt(hash,3222014725))
def emojiBelow1605(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf4}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1faf5}\u{1f3fb}")
def emojiBelow1606(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fc}")
    case False{}: emojiBelow1605(hash,text,U32.is_lt(hash,3223773553))
def emojiBelow1607(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf6}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f934}\u{1f3fc}")
def emojiBelow1608(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1ee}")
    case False{}: emojiBelow1607(hash,text,U32.is_lt(hash,3224139341))
def emojiBelow1609(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1606(hash,text,U32.is_lt(hash,3223611144))
    case False{}: emojiBelow1608(hash,text,U32.is_lt(hash,3224111670))
def emojiBelow1610(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1604(hash,text,U32.is_lt(hash,3221097157))
    case False{}: emojiBelow1609(hash,text,U32.is_lt(hash,3224075192))
def emojiBelow1611(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1600(hash,text,U32.is_lt(hash,3212811242))
    case False{}: emojiBelow1610(hash,text,U32.is_lt(hash,3223015777))
def emojiBelow1612(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9db}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1faf3}\u{1f3fb}")
def emojiBelow1613(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f930}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d2}\u{1f3fe}")
def emojiBelow1614(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fe}")
    case False{}: emojiBelow1613(hash,text,U32.is_lt(hash,3225163921))
def emojiBelow1615(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1612(hash,text,U32.is_lt(hash,3224449787))
    case False{}: emojiBelow1614(hash,text,U32.is_lt(hash,3225140393))
def emojiBelow1616(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fc}")
def emojiBelow1617(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9bb}\u{1f3fd}")
    case False{}: emojiBelow1616(hash,text,U32.is_lt(hash,3225677746))
def emojiBelow1618(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3fe}")
def emojiBelow1619(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d8}\u{1f3fe}")
    case False{}: emojiBelow1618(hash,text,U32.is_lt(hash,3226963719))
def emojiBelow1620(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1617(hash,text,U32.is_lt(hash,3225375822))
    case False{}: emojiBelow1619(hash,text,U32.is_lt(hash,3226340681))
def emojiBelow1621(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1615(hash,text,U32.is_lt(hash,3224839103))
    case False{}: emojiBelow1620(hash,text,U32.is_lt(hash,3225840155))
def emojiBelow1622(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91c}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f918}\u{1f3fc}")
def emojiBelow1623(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d3}\u{1f3fc}")
def emojiBelow1624(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fe}")
    case False{}: emojiBelow1623(hash,text,U32.is_lt(hash,3227802362))
def emojiBelow1625(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1622(hash,text,U32.is_lt(hash,3227387521))
    case False{}: emojiBelow1624(hash,text,U32.is_lt(hash,3227626654))
def emojiBelow1626(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f90c}\u{1f3fc}")
def emojiBelow1627(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fc}")
    case False{}: emojiBelow1626(hash,text,U32.is_lt(hash,3231461045))
def emojiBelow1628(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac4}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1fac5}\u{1f3fb}")
def emojiBelow1629(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{ae}\u{fe0f}")
    case False{}: emojiBelow1628(hash,text,U32.is_lt(hash,3232394529))
def emojiBelow1630(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1627(hash,text,U32.is_lt(hash,3229588861))
    case False{}: emojiBelow1629(hash,text,U32.is_lt(hash,3232232120))
def emojiBelow1631(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1625(hash,text,U32.is_lt(hash,3227464245))
    case False{}: emojiBelow1630(hash,text,U32.is_lt(hash,3231639898))
def emojiBelow1632(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1621(hash,text,U32.is_lt(hash,3225213413))
    case False{}: emojiBelow1631(hash,text,U32.is_lt(hash,3228925926))
def emojiBelow1633(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1611(hash,text,U32.is_lt(hash,3219979347))
    case False{}: emojiBelow1632(hash,text,U32.is_lt(hash,3227211813))
def emojiBelow1634(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1591(hash,text,U32.is_lt(hash,3207361722))
    case False{}: emojiBelow1633(hash,text,U32.is_lt(hash,3224437075))
def emojiBelow1635(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93e}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1fac3}\u{1f3fb}")
def emojiBelow1636(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow1637(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1ef}")
    case False{}: emojiBelow1636(hash,text,U32.is_lt(hash,3237657915))
def emojiBelow1638(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1635(hash,text,U32.is_lt(hash,3233070763))
    case False{}: emojiBelow1637(hash,text,U32.is_lt(hash,3235686374))
def emojiBelow1639(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f932}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1e9}\u{1f1ef}")
def emojiBelow1640(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f939}\u{1f3fb}")
def emojiBelow1641(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1eb}\u{1f1ef}")
    case False{}: emojiBelow1640(hash,text,U32.is_lt(hash,3239955805))
def emojiBelow1642(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1639(hash,text,U32.is_lt(hash,3239228721))
    case False{}: emojiBelow1641(hash,text,U32.is_lt(hash,3239793396))
def emojiBelow1643(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1638(hash,text,U32.is_lt(hash,3235642424))
    case False{}: emojiBelow1642(hash,text,U32.is_lt(hash,3239729247))
def emojiBelow1644(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f91d}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f692}")
def emojiBelow1645(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1ef}")
def emojiBelow1646(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf5}\u{1f3fc}")
    case False{}: emojiBelow1645(hash,text,U32.is_lt(hash,3240852811))
def emojiBelow1647(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1644(hash,text,U32.is_lt(hash,3240188690))
    case False{}: emojiBelow1646(hash,text,U32.is_lt(hash,3240726880))
def emojiBelow1648(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9dc}\u{1f3fd}")
def emojiBelow1649(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow1648(hash,text,U32.is_lt(hash,3241616722))
def emojiBelow1650(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f931}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9b8}\u{1f3fc}")
def emojiBelow1651(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f930}\u{1f3fb}")
    case False{}: emojiBelow1650(hash,text,U32.is_lt(hash,3242153441))
def emojiBelow1652(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1649(hash,text,U32.is_lt(hash,3241579895))
    case False{}: emojiBelow1651(hash,text,U32.is_lt(hash,3242080421))
def emojiBelow1653(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1647(hash,text,U32.is_lt(hash,3240551172))
    case False{}: emojiBelow1652(hash,text,U32.is_lt(hash,3241918012))
def emojiBelow1654(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1643(hash,text,U32.is_lt(hash,3239117162))
    case False{}: emojiBelow1653(hash,text,U32.is_lt(hash,3241062820))
def emojiBelow1655(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9db}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fd}")
def emojiBelow1656(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91f}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f918}\u{1f3fb}")
def emojiBelow1657(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fd}")
    case False{}: emojiBelow1656(hash,text,U32.is_lt(hash,3244165140))
def emojiBelow1658(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1655(hash,text,U32.is_lt(hash,3242617774))
    case False{}: emojiBelow1657(hash,text,U32.is_lt(hash,3243827023))
def emojiBelow1659(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d3}\u{1f3fd}")
def emojiBelow1660(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f919}\u{1f3fb}")
    case False{}: emojiBelow1659(hash,text,U32.is_lt(hash,3244579981))
def emojiBelow1661(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f90f}\u{1f3fb}")
def emojiBelow1662(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f91a}\u{1f3fb}")
    case False{}: emojiBelow1661(hash,text,U32.is_lt(hash,3248076255))
def emojiBelow1663(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1660(hash,text,U32.is_lt(hash,3244404273))
    case False{}: emojiBelow1662(hash,text,U32.is_lt(hash,3245703545))
def emojiBelow1664(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1658(hash,text,U32.is_lt(hash,3243741338))
    case False{}: emojiBelow1663(hash,text,U32.is_lt(hash,3244665666))
def emojiBelow1665(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac5}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1666(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1ee}")
def emojiBelow1667(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f977}\u{1f3fb}")
    case False{}: emojiBelow1666(hash,text,U32.is_lt(hash,3252420043))
def emojiBelow1668(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1665(hash,text,U32.is_lt(hash,3250396180))
    case False{}: emojiBelow1667(hash,text,U32.is_lt(hash,3252164143))
def emojiBelow1669(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f3}\u{1f1ec}")
    case False{}: Strings.equal(text,"\u{1f1ec}\u{1f1ee}")
def emojiBelow1670(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1ee}")
    case False{}: emojiBelow1669(hash,text,U32.is_lt(hash,3255668223))
def emojiBelow1671(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1faf5}\u{1f3fd}")
def emojiBelow1672(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1f0}")
    case False{}: emojiBelow1671(hash,text,U32.is_lt(hash,3257328791))
def emojiBelow1673(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1670(hash,text,U32.is_lt(hash,3254382250))
    case False{}: emojiBelow1672(hash,text,U32.is_lt(hash,3257275813))
def emojiBelow1674(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1668(hash,text,U32.is_lt(hash,3251148727))
    case False{}: emojiBelow1673(hash,text,U32.is_lt(hash,3256594720))
def emojiBelow1675(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1664(hash,text,U32.is_lt(hash,3244327549))
    case False{}: emojiBelow1674(hash,text,U32.is_lt(hash,3254131987))
def emojiBelow1676(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1654(hash,text,U32.is_lt(hash,3239979855))
    case False{}: emojiBelow1675(hash,text,U32.is_lt(hash,3249172148))
def emojiBelow1677(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1ec}")
def emojiBelow1678(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1faf2}\u{1f3fd}")
def emojiBelow1679(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf6}\u{1f3fd}")
    case False{}: emojiBelow1678(hash,text,U32.is_lt(hash,3257842616))
def emojiBelow1680(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1677(hash,text,U32.is_lt(hash,3257630430))
    case False{}: emojiBelow1679(hash,text,U32.is_lt(hash,3257840439))
def emojiBelow1681(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fc}")
def emojiBelow1682(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fc}")
    case False{}: emojiBelow1681(hash,text,U32.is_lt(hash,3259395393))
def emojiBelow1683(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f470}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1684(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fc}")
    case False{}: emojiBelow1683(hash,text,U32.is_lt(hash,3262601602))
def emojiBelow1685(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1682(hash,text,U32.is_lt(hash,3258931060))
    case False{}: emojiBelow1684(hash,text,U32.is_lt(hash,3261569501))
def emojiBelow1686(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1680(hash,text,U32.is_lt(hash,3257666908))
    case False{}: emojiBelow1685(hash,text,U32.is_lt(hash,3260518957))
def emojiBelow1687(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9b2}")
    case False{}: Strings.equal(text,"\u{1fac5}\u{1f3fd}")
def emojiBelow1688(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1ed}")
    case False{}: Strings.equal(text,"\u{1f574}\u{1f3fb}")
def emojiBelow1689(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1688(hash,text,U32.is_lt(hash,3269366152))
def emojiBelow1690(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1687(hash,text,U32.is_lt(hash,3265949767))
    case False{}: emojiBelow1689(hash,text,U32.is_lt(hash,3269197662))
def emojiBelow1691(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{25c0}\u{fe0f}")
def emojiBelow1692(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fb}")
    case False{}: emojiBelow1691(hash,text,U32.is_lt(hash,3270494832))
def emojiBelow1693(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9bd}")
def emojiBelow1694(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1ed}")
    case False{}: emojiBelow1693(hash,text,U32.is_lt(hash,3271430014))
def emojiBelow1695(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1692(hash,text,U32.is_lt(hash,3269580141))
    case False{}: emojiBelow1694(hash,text,U32.is_lt(hash,3271050744))
def emojiBelow1696(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1690(hash,text,U32.is_lt(hash,3268976852))
    case False{}: emojiBelow1695(hash,text,U32.is_lt(hash,3270909606))
def emojiBelow1697(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1686(hash,text,U32.is_lt(hash,3258394341))
    case False{}: emojiBelow1696(hash,text,U32.is_lt(hash,3269528561))
def emojiBelow1698(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1ed}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1f1}")
def emojiBelow1699(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1f1}")
    case False{}: Strings.equal(text,"\u{1faf7}\u{1f3fe}")
def emojiBelow1700(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1699(hash,text,U32.is_lt(hash,3273781592))
def emojiBelow1701(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1698(hash,text,U32.is_lt(hash,3272533696))
    case False{}: emojiBelow1700(hash,text,U32.is_lt(hash,3273372339))
def emojiBelow1702(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1ed}")
def emojiBelow1703(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf5}\u{1f3fe}")
    case False{}: emojiBelow1702(hash,text,U32.is_lt(hash,3274408049))
def emojiBelow1704(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1faf2}\u{1f3fc}")
def emojiBelow1705(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf6}\u{1f3fc}")
    case False{}: emojiBelow1704(hash,text,U32.is_lt(hash,3274620235))
def emojiBelow1706(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1703(hash,text,U32.is_lt(hash,3274282118))
    case False{}: emojiBelow1705(hash,text,U32.is_lt(hash,3274618058))
def emojiBelow1707(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1701(hash,text,U32.is_lt(hash,3272718189))
    case False{}: emojiBelow1706(hash,text,U32.is_lt(hash,3274444527))
def emojiBelow1708(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{fe0f}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1faf3}\u{1f3fe}")
def emojiBelow1709(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d2}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d8}\u{1f3fb}")
def emojiBelow1710(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow1709(hash,text,U32.is_lt(hash,3276173012))
def emojiBelow1711(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1708(hash,text,U32.is_lt(hash,3274782644))
    case False{}: emojiBelow1710(hash,text,U32.is_lt(hash,3275496778))
def emojiBelow1712(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}")
def emojiBelow1713(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fb}")
    case False{}: emojiBelow1712(hash,text,U32.is_lt(hash,3277634693))
def emojiBelow1714(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9b3}")
def emojiBelow1715(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fb}")
    case False{}: emojiBelow1714(hash,text,U32.is_lt(hash,3279516115))
def emojiBelow1716(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1713(hash,text,U32.is_lt(hash,3276673538))
    case False{}: emojiBelow1715(hash,text,U32.is_lt(hash,3279258783))
def emojiBelow1717(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1711(hash,text,U32.is_lt(hash,3274828363))
    case False{}: emojiBelow1716(hash,text,U32.is_lt(hash,3277959511))
def emojiBelow1718(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1707(hash,text,U32.is_lt(hash,3274106410))
    case False{}: emojiBelow1717(hash,text,U32.is_lt(hash,3276335421))
def emojiBelow1719(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1697(hash,text,U32.is_lt(hash,3262738496))
    case False{}: emojiBelow1718(hash,text,U32.is_lt(hash,3274692914))
def emojiBelow1720(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1676(hash,text,U32.is_lt(hash,3242455365))
    case False{}: emojiBelow1719(hash,text,U32.is_lt(hash,3272445842))
def emojiBelow1721(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1634(hash,text,U32.is_lt(hash,3210849035))
    case False{}: emojiBelow1720(hash,text,U32.is_lt(hash,3257504499))
def emojiBelow1722(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1549(hash,text,U32.is_lt(hash,3162852695))
    case False{}: emojiBelow1721(hash,text,U32.is_lt(hash,3232503754))
def emojiBelow1723(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f3ed}")
def emojiBelow1724(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac3}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f595}\u{1f3fb}")
def emojiBelow1725(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac5}\u{1f3fe}")
    case False{}: emojiBelow1724(hash,text,U32.is_lt(hash,3283469137))
def emojiBelow1726(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1723(hash,text,U32.is_lt(hash,3281623538))
    case False{}: emojiBelow1725(hash,text,U32.is_lt(hash,3283403620))
def emojiBelow1727(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f596}\u{1f3fb}")
def emojiBelow1728(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d4}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1ec}")
def emojiBelow1729(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1728(hash,text,U32.is_lt(hash,3285975281))
def emojiBelow1730(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1727(hash,text,U32.is_lt(hash,3283807254))
    case False{}: emojiBelow1729(hash,text,U32.is_lt(hash,3285601752))
def emojiBelow1731(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1726(hash,text,U32.is_lt(hash,3282727386))
    case False{}: emojiBelow1730(hash,text,U32.is_lt(hash,3283853459))
def emojiBelow1732(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow1733(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f4bc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow1734(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1ec}")
    case False{}: emojiBelow1733(hash,text,U32.is_lt(hash,3287828363))
def emojiBelow1735(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1732(hash,text,U32.is_lt(hash,3286623630))
    case False{}: emojiBelow1734(hash,text,U32.is_lt(hash,3287691793))
def emojiBelow1736(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f9cf}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1737(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow1736(hash,text,U32.is_lt(hash,3288542586))
def emojiBelow1738(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e9}\u{1f1ea}")
    case False{}: Strings.equal(text,"\u{1faf8}\u{1f3ff}")
def emojiBelow1739(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1ec}")
    case False{}: emojiBelow1738(hash,text,U32.is_lt(hash,3289598056))
def emojiBelow1740(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1737(hash,text,U32.is_lt(hash,3288207633))
    case False{}: emojiBelow1739(hash,text,U32.is_lt(hash,3289561578))
def emojiBelow1741(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1735(hash,text,U32.is_lt(hash,3287687225))
    case False{}: emojiBelow1740(hash,text,U32.is_lt(hash,3289223461))
def emojiBelow1742(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1731(hash,text,U32.is_lt(hash,3283677058))
    case False{}: emojiBelow1741(hash,text,U32.is_lt(hash,3287990772))
def emojiBelow1743(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1f2}")
def emojiBelow1744(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf4}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1faf5}\u{1f3ff}")
def emojiBelow1745(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf7}\u{1f3ff}")
    case False{}: emojiBelow1744(hash,text,U32.is_lt(hash,3290884029))
def emojiBelow1746(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1743(hash,text,U32.is_lt(hash,3290149958))
    case False{}: emojiBelow1745(hash,text,U32.is_lt(hash,3290721620))
def emojiBelow1747(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1ea}")
    case False{}: Strings.equal(text,"\u{1faf6}\u{1f3ff}")
def emojiBelow1748(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3ff}")
    case False{}: emojiBelow1747(hash,text,U32.is_lt(hash,3291222146))
def emojiBelow1749(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf2}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1faf3}\u{1f3ff}")
def emojiBelow1750(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow1749(hash,text,U32.is_lt(hash,3291560263))
def emojiBelow1751(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1748(hash,text,U32.is_lt(hash,3291185668))
    case False{}: emojiBelow1750(hash,text,U32.is_lt(hash,3291397854))
def emojiBelow1752(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1746(hash,text,U32.is_lt(hash,3290559211))
    case False{}: emojiBelow1751(hash,text,U32.is_lt(hash,3291395677))
def emojiBelow1753(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1faf0}\u{1f3ff}")
def emojiBelow1754(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9b0}")
def emojiBelow1755(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}")
    case False{}: emojiBelow1754(hash,text,U32.is_lt(hash,3296293734))
def emojiBelow1756(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1753(hash,text,U32.is_lt(hash,3291722672))
    case False{}: emojiBelow1755(hash,text,U32.is_lt(hash,3296138336))
def emojiBelow1757(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac4}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1fac5}\u{1f3ff}")
def emojiBelow1758(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f57a}\u{1f3fd}")
    case False{}: emojiBelow1757(hash,text,U32.is_lt(hash,3299505005))
def emojiBelow1759(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f595}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1760(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac3}\u{1f3ff}")
    case False{}: emojiBelow1759(hash,text,U32.is_lt(hash,3302202778))
def emojiBelow1761(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1758(hash,text,U32.is_lt(hash,3299342596))
    case False{}: emojiBelow1760(hash,text,U32.is_lt(hash,3300246756))
def emojiBelow1762(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1756(hash,text,U32.is_lt(hash,3293400184))
    case False{}: emojiBelow1761(hash,text,U32.is_lt(hash,3300181239))
def emojiBelow1763(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1752(hash,text,U32.is_lt(hash,3291059737))
    case False{}: emojiBelow1762(hash,text,U32.is_lt(hash,3298996976))
def emojiBelow1764(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1742(hash,text,U32.is_lt(hash,3286306180))
    case False{}: emojiBelow1763(hash,text,U32.is_lt(hash,3291605982))
def emojiBelow1765(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f575}\u{1f3fd}")
def emojiBelow1766(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1767(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow1766(hash,text,U32.is_lt(hash,3304768391))
def emojiBelow1768(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1765(hash,text,U32.is_lt(hash,3303083799))
    case False{}: emojiBelow1767(hash,text,U32.is_lt(hash,3304605982))
def emojiBelow1769(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1f3}")
def emojiBelow1770(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1f3}")
    case False{}: Strings.equal(text,"\u{1faf7}\u{1f3fc}")
def emojiBelow1771(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf8}\u{1f3fe}")
    case False{}: emojiBelow1770(hash,text,U32.is_lt(hash,3307336830))
def emojiBelow1772(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1769(hash,text,U32.is_lt(hash,3306088934))
    case False{}: emojiBelow1771(hash,text,U32.is_lt(hash,3306927577))
def emojiBelow1773(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1768(hash,text,U32.is_lt(hash,3304469412))
    case False{}: emojiBelow1772(hash,text,U32.is_lt(hash,3306375675))
def emojiBelow1774(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf4}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1eb}")
def emojiBelow1775(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf2}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1faf3}\u{1f3fc}")
def emojiBelow1776(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf6}\u{1f3fe}")
    case False{}: emojiBelow1775(hash,text,U32.is_lt(hash,3308337882))
def emojiBelow1777(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1774(hash,text,U32.is_lt(hash,3307963287))
    case False{}: emojiBelow1776(hash,text,U32.is_lt(hash,3308175473))
def emojiBelow1778(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf0}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}")
def emojiBelow1779(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow1778(hash,text,U32.is_lt(hash,3310177803))
def emojiBelow1780(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f9d7}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1781(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow1780(hash,text,U32.is_lt(hash,3313944439))
def emojiBelow1782(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1779(hash,text,U32.is_lt(hash,3308500291))
    case False{}: emojiBelow1781(hash,text,U32.is_lt(hash,3313071353))
def emojiBelow1783(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1777(hash,text,U32.is_lt(hash,3307999765))
    case False{}: emojiBelow1782(hash,text,U32.is_lt(hash,3312937402))
def emojiBelow1784(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1773(hash,text,U32.is_lt(hash,3305203969))
    case False{}: emojiBelow1783(hash,text,U32.is_lt(hash,3308383601))
def emojiBelow1785(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f57a}\u{1f3fc}")
def emojiBelow1786(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9db}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1fac3}\u{1f3fc}")
def emojiBelow1787(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac4}\u{1f3fe}")
    case False{}: emojiBelow1786(hash,text,U32.is_lt(hash,3316958858))
def emojiBelow1788(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1785(hash,text,U32.is_lt(hash,3315774595))
    case False{}: emojiBelow1787(hash,text,U32.is_lt(hash,3316391862))
def emojiBelow1789(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f596}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fe}")
def emojiBelow1790(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f595}\u{1f3fd}")
    case False{}: emojiBelow1789(hash,text,U32.is_lt(hash,3318297568))
def emojiBelow1791(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f4}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1ea}")
def emojiBelow1792(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f415}\u{200d}\u{1f9ba}")
    case False{}: emojiBelow1791(hash,text,U32.is_lt(hash,3319530519))
def emojiBelow1793(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1790(hash,text,U32.is_lt(hash,3317362492))
    case False{}: emojiBelow1792(hash,text,U32.is_lt(hash,3319280256))
def emojiBelow1794(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1788(hash,text,U32.is_lt(hash,3316120215))
    case False{}: emojiBelow1793(hash,text,U32.is_lt(hash,3318709703))
def emojiBelow1795(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow1796(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1ea}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fe}")
def emojiBelow1797(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f4}\u{1f1f2}")
    case False{}: emojiBelow1796(hash,text,U32.is_lt(hash,3321383601))
def emojiBelow1798(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1795(hash,text,U32.is_lt(hash,3320178868))
    case False{}: emojiBelow1797(hash,text,U32.is_lt(hash,3321242463))
def emojiBelow1799(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ee}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1e9}\u{1f1f4}")
def emojiBelow1800(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow1799(hash,text,U32.is_lt(hash,3323116816))
def emojiBelow1801(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf7}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1faf4}\u{1f3fd}")
def emojiBelow1802(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf8}\u{1f3fd}")
    case False{}: emojiBelow1801(hash,text,U32.is_lt(hash,3324276858))
def emojiBelow1803(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1800(hash,text,U32.is_lt(hash,3322866553))
    case False{}: emojiBelow1802(hash,text,U32.is_lt(hash,3324114449))
def emojiBelow1804(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1798(hash,text,U32.is_lt(hash,3320654083))
    case False{}: emojiBelow1803(hash,text,U32.is_lt(hash,3323153294))
def emojiBelow1805(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1794(hash,text,U32.is_lt(hash,3317024375))
    case False{}: emojiBelow1804(hash,text,U32.is_lt(hash,3321546010))
def emojiBelow1806(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1784(hash,text,U32.is_lt(hash,3307499239))
    case False{}: emojiBelow1805(hash,text,U32.is_lt(hash,3319861418))
def emojiBelow1807(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1764(hash,text,U32.is_lt(hash,3289921650))
    case False{}: emojiBelow1806(hash,text,U32.is_lt(hash,3313997919))
def emojiBelow1808(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf3}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1faf0}\u{1f3fd}")
def emojiBelow1809(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{fe0f}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1810(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1809(hash,text,U32.is_lt(hash,3328336246))
def emojiBelow1811(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1808(hash,text,U32.is_lt(hash,3325277910))
    case False{}: emojiBelow1810(hash,text,U32.is_lt(hash,3326527945))
def emojiBelow1812(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f474}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f475}\u{1f3fb}")
def emojiBelow1813(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f47c}\u{1f3fb}")
def emojiBelow1814(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f476}\u{1f3fb}")
    case False{}: emojiBelow1813(hash,text,U32.is_lt(hash,3332134208))
def emojiBelow1815(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1812(hash,text,U32.is_lt(hash,3331119857))
    case False{}: emojiBelow1814(hash,text,U32.is_lt(hash,3331796091))
def emojiBelow1816(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1811(hash,text,U32.is_lt(hash,3325287669))
    case False{}: emojiBelow1815(hash,text,U32.is_lt(hash,3331457974))
def emojiBelow1817(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46d}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f57a}\u{1f3ff}")
def emojiBelow1818(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac4}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f46c}\u{1f3fb}")
def emojiBelow1819(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f39b}\u{fe0f}")
    case False{}: emojiBelow1818(hash,text,U32.is_lt(hash,3333082064))
def emojiBelow1820(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1817(hash,text,U32.is_lt(hash,3332552214))
    case False{}: emojiBelow1819(hash,text,U32.is_lt(hash,3332897834))
def emojiBelow1821(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac3}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f595}\u{1f3fe}")
def emojiBelow1822(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fb}")
    case False{}: emojiBelow1821(hash,text,U32.is_lt(hash,3333801994))
def emojiBelow1823(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f596}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f466}\u{1f3fb}")
def emojiBelow1824(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46b}\u{1f3fb}")
    case False{}: emojiBelow1823(hash,text,U32.is_lt(hash,3334881862))
def emojiBelow1825(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1822(hash,text,U32.is_lt(hash,3333736477))
    case False{}: emojiBelow1824(hash,text,U32.is_lt(hash,3334140111))
def emojiBelow1826(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1820(hash,text,U32.is_lt(hash,3332831919))
    case False{}: emojiBelow1825(hash,text,U32.is_lt(hash,3333920707))
def emojiBelow1827(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1816(hash,text,U32.is_lt(hash,3330957448))
    case False{}: emojiBelow1826(hash,text,U32.is_lt(hash,3333582590))
def emojiBelow1828(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1e9}")
    case False{}: Strings.equal(text,"\u{1f574}\u{1f3ff}")
def emojiBelow1829(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f33e}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow1830(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3ff}")
    case False{}: emojiBelow1829(hash,text,U32.is_lt(hash,3336956487))
def emojiBelow1831(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1828(hash,text,U32.is_lt(hash,3336476628))
    case False{}: emojiBelow1830(hash,text,U32.is_lt(hash,3336838887))
def emojiBelow1832(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44c}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f44d}\u{1f3fb}")
def emojiBelow1833(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: emojiBelow1832(hash,text,U32.is_lt(hash,3337616217))
def emojiBelow1834(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1f1}")
def emojiBelow1835(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44e}\u{1f3fb}")
    case False{}: emojiBelow1834(hash,text,U32.is_lt(hash,3338270345))
def emojiBelow1836(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1833(hash,text,U32.is_lt(hash,3337453808))
    case False{}: emojiBelow1835(hash,text,U32.is_lt(hash,3338151824))
def emojiBelow1837(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1831(hash,text,U32.is_lt(hash,3336639037))
    case False{}: emojiBelow1836(hash,text,U32.is_lt(hash,3337954334))
def emojiBelow1838(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44b}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow1839(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf8}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f446}\u{1f3fb}")
def emojiBelow1840(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f5}")
    case False{}: emojiBelow1839(hash,text,U32.is_lt(hash,3340078950))
def emojiBelow1841(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1838(hash,text,U32.is_lt(hash,3338323629))
    case False{}: emojiBelow1840(hash,text,U32.is_lt(hash,3339930913))
def emojiBelow1842(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf4}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1e9}")
def emojiBelow1843(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f443}\u{1f3fb}")
    case False{}: emojiBelow1842(hash,text,U32.is_lt(hash,3341518525))
def emojiBelow1844(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f475}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3fc}")
def emojiBelow1845(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf0}\u{1f3fc}")
    case False{}: emojiBelow1844(hash,text,U32.is_lt(hash,3348073184))
def emojiBelow1846(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1843(hash,text,U32.is_lt(hash,3341054477))
    case False{}: emojiBelow1845(hash,text,U32.is_lt(hash,3347897476))
def emojiBelow1847(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1841(hash,text,U32.is_lt(hash,3338520608))
    case False{}: emojiBelow1846(hash,text,U32.is_lt(hash,3342055529))
def emojiBelow1848(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1837(hash,text,U32.is_lt(hash,3337118896))
    case False{}: emojiBelow1847(hash,text,U32.is_lt(hash,3340417067))
def emojiBelow1849(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1827(hash,text,U32.is_lt(hash,3332419129))
    case False{}: emojiBelow1848(hash,text,U32.is_lt(hash,3338292451))
def emojiBelow1850(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46d}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f57a}\u{1f3fe}")
def emojiBelow1851(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1fac4}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f483}\u{1f3fb}")
def emojiBelow1852(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fb}")
    case False{}: emojiBelow1851(hash,text,U32.is_lt(hash,3349985899))
def emojiBelow1853(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1850(hash,text,U32.is_lt(hash,3349329833))
    case False{}: emojiBelow1852(hash,text,U32.is_lt(hash,3349675453))
def emojiBelow1854(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f595}\u{1f3ff}")
def emojiBelow1855(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f590}\u{1f3ff}")
def emojiBelow1856(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f596}\u{1f3ff}")
    case False{}: emojiBelow1855(hash,text,U32.is_lt(hash,3351418256))
def emojiBelow1857(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1854(hash,text,U32.is_lt(hash,3350579613))
    case False{}: emojiBelow1856(hash,text,U32.is_lt(hash,3351089663))
def emojiBelow1858(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1853(hash,text,U32.is_lt(hash,3349647782))
    case False{}: emojiBelow1857(hash,text,U32.is_lt(hash,3350917730))
def emojiBelow1859(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f485}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow1860(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f574}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow1861(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1e8}")
    case False{}: emojiBelow1860(hash,text,U32.is_lt(hash,3353734106))
def emojiBelow1862(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1859(hash,text,U32.is_lt(hash,3351852806))
    case False{}: emojiBelow1861(hash,text,U32.is_lt(hash,3353254247))
def emojiBelow1863(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44d}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f449}\u{1f3fc}")
def emojiBelow1864(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: emojiBelow1863(hash,text,U32.is_lt(hash,3354569544))
def emojiBelow1865(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ff}\u{1f1e6}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1f4}")
def emojiBelow1866(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f4}")
    case False{}: emojiBelow1865(hash,text,U32.is_lt(hash,3356421791))
def emojiBelow1867(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1864(hash,text,U32.is_lt(hash,3354393836))
    case False{}: emojiBelow1866(hash,text,U32.is_lt(hash,3355636344))
def emojiBelow1868(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1862(hash,text,U32.is_lt(hash,3353085757))
    case False{}: emojiBelow1867(hash,text,U32.is_lt(hash,3355298227))
def emojiBelow1869(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1858(hash,text,U32.is_lt(hash,3350197800))
    case False{}: emojiBelow1868(hash,text,U32.is_lt(hash,3353896515))
def emojiBelow1870(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf8}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1faf7}\u{1f3fb}")
def emojiBelow1871(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1f6}")
    case False{}: Strings.equal(text,"\u{1faf2}\u{1f3fb}")
def emojiBelow1872(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf1}\u{1f3fb}")
    case False{}: emojiBelow1871(hash,text,U32.is_lt(hash,3358508330))
def emojiBelow1873(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1870(hash,text,U32.is_lt(hash,3357669687))
    case False{}: emojiBelow1872(hash,text,U32.is_lt(hash,3358296144))
def emojiBelow1874(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow1875(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1faf0}\u{1f3fb}")
    case False{}: emojiBelow1874(hash,text,U32.is_lt(hash,3363423401))
def emojiBelow1876(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f476}\u{1f3fd}")
def emojiBelow1877(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f475}\u{1f3fd}")
    case False{}: emojiBelow1876(hash,text,U32.is_lt(hash,3365013212))
def emojiBelow1878(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1875(hash,text,U32.is_lt(hash,3359034142))
    case False{}: emojiBelow1877(hash,text,U32.is_lt(hash,3364850803))
def emojiBelow1879(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1873(hash,text,U32.is_lt(hash,3358170213))
    case False{}: emojiBelow1878(hash,text,U32.is_lt(hash,3364675095))
def emojiBelow1880(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f472}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f491}\u{1f3fc}")
def emojiBelow1881(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}")
def emojiBelow1882(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46d}\u{1f3fd}")
    case False{}: emojiBelow1881(hash,text,U32.is_lt(hash,3366975419))
def emojiBelow1883(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1880(hash,text,U32.is_lt(hash,3365315136))
    case False{}: emojiBelow1882(hash,text,U32.is_lt(hash,3366262992))
def emojiBelow1884(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f596}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f590}\u{1f3fe}")
def emojiBelow1885(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fd}")
    case False{}: emojiBelow1884(hash,text,U32.is_lt(hash,3368195875))
def emojiBelow1886(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f485}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1887(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f466}\u{1f3fd}")
    case False{}: emojiBelow1886(hash,text,U32.is_lt(hash,3368630425))
def emojiBelow1888(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1885(hash,text,U32.is_lt(hash,3367695349))
    case False{}: emojiBelow1887(hash,text,U32.is_lt(hash,3368563316))
def emojiBelow1889(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1883(hash,text,U32.is_lt(hash,3365974367))
    case False{}: emojiBelow1888(hash,text,U32.is_lt(hash,3368437100))
def emojiBelow1890(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1879(hash,text,U32.is_lt(hash,3358833148))
    case False{}: emojiBelow1889(hash,text,U32.is_lt(hash,3367137828))
def emojiBelow1891(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1869(hash,text,U32.is_lt(hash,3351785697))
    case False{}: emojiBelow1890(hash,text,U32.is_lt(hash,3365188920))
def emojiBelow1892(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1849(hash,text,U32.is_lt(hash,3336308138))
    case False{}: emojiBelow1891(hash,text,U32.is_lt(hash,3356708532))
def emojiBelow1893(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1807(hash,text,U32.is_lt(hash,3302230487))
    case False{}: emojiBelow1892(hash,text,U32.is_lt(hash,3349196748))
def emojiBelow1894(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f7}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1f7}")
def emojiBelow1895(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow1896(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f574}\u{1f3fd}")
    case False{}: emojiBelow1895(hash,text,U32.is_lt(hash,3370674134))
def emojiBelow1897(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1894(hash,text,U32.is_lt(hash,3369863376))
    case False{}: emojiBelow1896(hash,text,U32.is_lt(hash,3370511725))
def emojiBelow1898(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44d}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f449}\u{1f3fd}")
def emojiBelow1899(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1f7}")
def emojiBelow1900(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44e}\u{1f3fd}")
    case False{}: emojiBelow1899(hash,text,U32.is_lt(hash,3371825583))
def emojiBelow1901(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1898(hash,text,U32.is_lt(hash,3371347163))
    case False{}: emojiBelow1900(hash,text,U32.is_lt(hash,3371716458))
def emojiBelow1902(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1897(hash,text,U32.is_lt(hash,3370031866))
    case False{}: emojiBelow1901(hash,text,U32.is_lt(hash,3371509572))
def emojiBelow1903(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f7}")
    case False{}: Strings.equal(text,"\u{2763}\u{fe0f}")
def emojiBelow1904(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1e7}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1f7}")
def emojiBelow1905(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44a}\u{1f3fd}")
    case False{}: emojiBelow1904(hash,text,U32.is_lt(hash,3373199410))
def emojiBelow1906(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1903(hash,text,U32.is_lt(hash,3372378471))
    case False{}: emojiBelow1905(hash,text,U32.is_lt(hash,3373111556))
def emojiBelow1907(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1f7}")
    case False{}: Strings.equal(text,"\u{1f442}\u{1f3fd}")
def emojiBelow1908(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f446}\u{1f3fd}")
    case False{}: emojiBelow1907(hash,text,U32.is_lt(hash,3373809896))
def emojiBelow1909(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1f7}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow1910(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1f7}")
    case False{}: emojiBelow1909(hash,text,U32.is_lt(hash,3375494077))
def emojiBelow1911(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1908(hash,text,U32.is_lt(hash,3373699936))
    case False{}: emojiBelow1910(hash,text,U32.is_lt(hash,3375073763))
def emojiBelow1912(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1906(hash,text,U32.is_lt(hash,3372510624))
    case False{}: emojiBelow1911(hash,text,U32.is_lt(hash,3374038053))
def emojiBelow1913(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1902(hash,text,U32.is_lt(hash,3371171455))
    case False{}: emojiBelow1912(hash,text,U32.is_lt(hash,3373634188))
def emojiBelow1914(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f4aa}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow1915(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{303d}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3fe}")
def emojiBelow1916(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow1915(hash,text,U32.is_lt(hash,3381127896))
def emojiBelow1917(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1914(hash,text,U32.is_lt(hash,3379704433))
    case False{}: emojiBelow1916(hash,text,U32.is_lt(hash,3380001573))
def emojiBelow1918(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f476}\u{1f3fc}")
def emojiBelow1919(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f475}\u{1f3fe}")
    case False{}: emojiBelow1918(hash,text,U32.is_lt(hash,3381790831))
def emojiBelow1920(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f491}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f473}\u{1f3fe}")
def emojiBelow1921(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f472}\u{1f3fc}")
    case False{}: emojiBelow1920(hash,text,U32.is_lt(hash,3382128948))
def emojiBelow1922(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1919(hash,text,U32.is_lt(hash,3381628422))
    case False{}: emojiBelow1921(hash,text,U32.is_lt(hash,3382092755))
def emojiBelow1923(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1917(hash,text,U32.is_lt(hash,3379810124))
    case False{}: emojiBelow1922(hash,text,U32.is_lt(hash,3381966539))
def emojiBelow1924(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46d}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f481}\u{1f3fd}")
def emojiBelow1925(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6cc}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f46f}\u{1f3fe}")
def emojiBelow1926(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fd}")
    case False{}: emojiBelow1925(hash,text,U32.is_lt(hash,3383252512))
def emojiBelow1927(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1924(hash,text,U32.is_lt(hash,3383040611))
    case False{}: emojiBelow1926(hash,text,U32.is_lt(hash,3383208816))
def emojiBelow1928(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}")
def emojiBelow1929(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow1928(hash,text,U32.is_lt(hash,3383753038))
def emojiBelow1930(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46b}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f590}\u{1f3fd}")
def emojiBelow1931(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fc}")
    case False{}: emojiBelow1930(hash,text,U32.is_lt(hash,3384973494))
def emojiBelow1932(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1929(hash,text,U32.is_lt(hash,3383378728))
    case False{}: emojiBelow1931(hash,text,U32.is_lt(hash,3384253564))
def emojiBelow1933(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1927(hash,text,U32.is_lt(hash,3383203020))
    case False{}: emojiBelow1932(hash,text,U32.is_lt(hash,3383915447))
def emojiBelow1934(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1923(hash,text,U32.is_lt(hash,3381452714))
    case False{}: emojiBelow1933(hash,text,U32.is_lt(hash,3383283700))
def emojiBelow1935(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1913(hash,text,U32.is_lt(hash,3372075846))
    case False{}: emojiBelow1934(hash,text,U32.is_lt(hash,3382751986))
def emojiBelow1936(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f466}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f485}\u{1f3fd}")
def emojiBelow1937(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f0}")
    case False{}: Strings.equal(text,"\u{1f1fa}\u{1f1e6}")
def emojiBelow1938(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f467}\u{1f3fe}")
    case False{}: emojiBelow1937(hash,text,U32.is_lt(hash,3386728849))
def emojiBelow1939(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1936(hash,text,U32.is_lt(hash,3385340935))
    case False{}: emojiBelow1938(hash,text,U32.is_lt(hash,3386390732))
def emojiBelow1940(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f574}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow1941(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f7}\u{1f1f4}")
    case False{}: Strings.equal(text,"\u{1f449}\u{1f3fe}")
def emojiBelow1942(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44d}\u{1f3fe}")
    case False{}: emojiBelow1941(hash,text,U32.is_lt(hash,3388124782))
def emojiBelow1943(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1940(hash,text,U32.is_lt(hash,3387451753))
    case False{}: emojiBelow1942(hash,text,U32.is_lt(hash,3388014822))
def emojiBelow1944(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1939(hash,text,U32.is_lt(hash,3385377128))
    case False{}: emojiBelow1943(hash,text,U32.is_lt(hash,3387949074))
def emojiBelow1945(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f6}\u{1f1e6}")
    case False{}: Strings.equal(text,"\u{1f44e}\u{1f3fc}")
def emojiBelow1946(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f3}\u{1f1f4}")
    case False{}: Strings.equal(text,"\u{1f44b}\u{1f3fe}")
def emojiBelow1947(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44f}\u{1f3fe}")
    case False{}: emojiBelow1946(hash,text,U32.is_lt(hash,3388625308))
def emojiBelow1948(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1945(hash,text,U32.is_lt(hash,3388287191))
    case False{}: emojiBelow1947(hash,text,U32.is_lt(hash,3388603202))
def emojiBelow1949(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44a}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f447}\u{1f3fe}")
def emojiBelow1950(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f6}")
    case False{}: emojiBelow1949(hash,text,U32.is_lt(hash,3389748872))
def emojiBelow1951(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ee}\u{1f1f6}")
    case False{}: Strings.equal(text,"\u{1f1e9}\u{1f1f0}")
def emojiBelow1952(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1e6}")
    case False{}: emojiBelow1951(hash,text,U32.is_lt(hash,3390227292))
def emojiBelow1953(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1950(hash,text,U32.is_lt(hash,3389288243))
    case False{}: emojiBelow1952(hash,text,U32.is_lt(hash,3389977029))
def emojiBelow1954(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1948(hash,text,U32.is_lt(hash,3388449600))
    case False{}: emojiBelow1953(hash,text,U32.is_lt(hash,3389889175))
def emojiBelow1955(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1944(hash,text,U32.is_lt(hash,3386809485))
    case False{}: emojiBelow1954(hash,text,U32.is_lt(hash,3388853465))
def emojiBelow1956(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f446}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1e8}\u{1f1f6}")
def emojiBelow1957(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f443}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1f8}")
def emojiBelow1958(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f442}\u{1f3fc}")
    case False{}: emojiBelow1957(hash,text,U32.is_lt(hash,3390815672))
def emojiBelow1959(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1956(hash,text,U32.is_lt(hash,3390477555))
    case False{}: emojiBelow1958(hash,text,U32.is_lt(hash,3390749924))
def emojiBelow1960(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f4aa}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f93d}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1961(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1f4}")
    case False{}: emojiBelow1960(hash,text,U32.is_lt(hash,3394296600))
def emojiBelow1962(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f478}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3ff}")
def emojiBelow1963(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2699}\u{fe0f}")
    case False{}: emojiBelow1962(hash,text,U32.is_lt(hash,3397905515))
def emojiBelow1964(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1961(hash,text,U32.is_lt(hash,3393659987))
    case False{}: emojiBelow1963(hash,text,U32.is_lt(hash,3396944360))
def emojiBelow1965(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1959(hash,text,U32.is_lt(hash,3390587515))
    case False{}: emojiBelow1964(hash,text,U32.is_lt(hash,3395745217))
def emojiBelow1966(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f474}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f926}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow1967(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f475}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f470}\u{1f3ff}")
def emojiBelow1968(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f52c}")
    case False{}: emojiBelow1967(hash,text,U32.is_lt(hash,3398243632))
def emojiBelow1969(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1966(hash,text,U32.is_lt(hash,3398120872))
    case False{}: emojiBelow1968(hash,text,U32.is_lt(hash,3398230333))
def emojiBelow1970(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f476}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f472}\u{1f3ff}")
def emojiBelow1971(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3ff}")
    case False{}: emojiBelow1970(hash,text,U32.is_lt(hash,3398744158))
def emojiBelow1972(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f47c}\u{1f3ff}")
def emojiBelow1973(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f491}\u{1f3fe}")
    case False{}: emojiBelow1972(hash,text,U32.is_lt(hash,3399244684))
def emojiBelow1974(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1971(hash,text,U32.is_lt(hash,3398568450))
    case False{}: emojiBelow1973(hash,text,U32.is_lt(hash,3398906567))
def emojiBelow1975(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1969(hash,text,U32.is_lt(hash,3398147804))
    case False{}: emojiBelow1974(hash,text,U32.is_lt(hash,3398870374))
def emojiBelow1976(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1965(hash,text,U32.is_lt(hash,3391851382))
    case False{}: emojiBelow1975(hash,text,U32.is_lt(hash,3398406041))
def emojiBelow1977(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1955(hash,text,U32.is_lt(hash,3388265085))
    case False{}: emojiBelow1976(hash,text,U32.is_lt(hash,3398067924))
def emojiBelow1978(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1935(hash,text,U32.is_lt(hash,3376882368))
    case False{}: emojiBelow1977(hash,text,U32.is_lt(hash,3390411807))
def emojiBelow1979(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f46d}\u{1f3ff}")
def emojiBelow1980(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f486}\u{1f3fc}")
def emojiBelow1981(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f57a}\u{1f3fb}")
    case False{}: emojiBelow1980(hash,text,U32.is_lt(hash,3399980639))
def emojiBelow1982(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1979(hash,text,U32.is_lt(hash,3399529605))
    case False{}: emojiBelow1981(hash,text,U32.is_lt(hash,3399818230))
def emojiBelow1983(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fc}")
def emojiBelow1984(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f483}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}")
def emojiBelow1985(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46c}\u{1f3ff}")
    case False{}: emojiBelow1984(hash,text,U32.is_lt(hash,3400368248))
def emojiBelow1986(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1983(hash,text,U32.is_lt(hash,3400156347))
    case False{}: emojiBelow1985(hash,text,U32.is_lt(hash,3400318756))
def emojiBelow1987(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1982(hash,text,U32.is_lt(hash,3399662690))
    case False{}: emojiBelow1986(hash,text,U32.is_lt(hash,3400192540))
def emojiBelow1988(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f48f}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}")
def emojiBelow1989(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f46b}\u{1f3ff}")
def emojiBelow1990(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3ff}")
    case False{}: emojiBelow1989(hash,text,U32.is_lt(hash,3401031183))
def emojiBelow1991(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1988(hash,text,U32.is_lt(hash,3400530657))
    case False{}: emojiBelow1990(hash,text,U32.is_lt(hash,3400762867))
def emojiBelow1992(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f466}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{26f1}\u{fe0f}")
def emojiBelow1993(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f590}\u{1f3fc}")
    case False{}: emojiBelow1992(hash,text,U32.is_lt(hash,3402035241))
def emojiBelow1994(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f467}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f430}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow1995(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f485}\u{1f3fe}")
    case False{}: emojiBelow1994(hash,text,U32.is_lt(hash,3402185663))
def emojiBelow1996(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1993(hash,text,U32.is_lt(hash,3401992338))
    case False{}: emojiBelow1995(hash,text,U32.is_lt(hash,3402154747))
def emojiBelow1997(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1991(hash,text,U32.is_lt(hash,3400693066))
    case False{}: emojiBelow1996(hash,text,U32.is_lt(hash,3402118554))
def emojiBelow1998(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1987(hash,text,U32.is_lt(hash,3400030131))
    case False{}: emojiBelow1997(hash,text,U32.is_lt(hash,3401751113))
def emojiBelow1999(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1fe}\u{1f1f9}")
def emojiBelow2000(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f450}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f44c}\u{1f3ff}")
def emojiBelow2001(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f1}")
    case False{}: emojiBelow2000(hash,text,U32.is_lt(hash,3404564284))
def emojiBelow2002(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1999(hash,text,U32.is_lt(hash,3402918088))
    case False{}: emojiBelow2001(hash,text,U32.is_lt(hash,3403440720))
def emojiBelow2003(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f449}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f44e}\u{1f3ff}")
def emojiBelow2004(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44d}\u{1f3ff}")
    case False{}: emojiBelow2003(hash,text,U32.is_lt(hash,3405064810))
def emojiBelow2005(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f3}\u{1f1f5}")
    case False{}: Strings.equal(text,"\u{1f44b}\u{1f3ff}")
def emojiBelow2006(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44f}\u{1f3ff}")
    case False{}: emojiBelow2005(hash,text,U32.is_lt(hash,3405402927))
def emojiBelow2007(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2004(hash,text,U32.is_lt(hash,3404902401))
    case False{}: emojiBelow2006(hash,text,U32.is_lt(hash,3405380821))
def emojiBelow2008(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2002(hash,text,U32.is_lt(hash,3403168351))
    case False{}: emojiBelow2007(hash,text,U32.is_lt(hash,3405227219))
def emojiBelow2009(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f448}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f1f2}\u{1f1f1}")
def emojiBelow2010(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ed}\u{1f1f9}")
    case False{}: Strings.equal(text,"\u{1f447}\u{1f3ff}")
def emojiBelow2011(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44a}\u{1f3ff}")
    case False{}: emojiBelow2010(hash,text,U32.is_lt(hash,3406526491))
def emojiBelow2012(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2009(hash,text,U32.is_lt(hash,3405631084))
    case False{}: emojiBelow2011(hash,text,U32.is_lt(hash,3406416531))
def emojiBelow2013(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f446}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f1e8}\u{1f1f5}")
def emojiBelow2014(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ee}\u{1f1f9}")
    case False{}: emojiBelow2013(hash,text,U32.is_lt(hash,3407255174))
def emojiBelow2015(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f443}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1f9}")
def emojiBelow2016(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f442}\u{1f3ff}")
    case False{}: emojiBelow2015(hash,text,U32.is_lt(hash,3407593291))
def emojiBelow2017(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2014(hash,text,U32.is_lt(hash,3407189426))
    case False{}: emojiBelow2016(hash,text,U32.is_lt(hash,3407527543))
def emojiBelow2018(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2012(hash,text,U32.is_lt(hash,3406065862))
    case False{}: emojiBelow2017(hash,text,U32.is_lt(hash,3407365134))
def emojiBelow2019(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2008(hash,text,U32.is_lt(hash,3404726693))
    case False{}: emojiBelow2018(hash,text,U32.is_lt(hash,3406754648))
def emojiBelow2020(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1998(hash,text,U32.is_lt(hash,3400494464))
    case False{}: emojiBelow2019(hash,text,U32.is_lt(hash,3405565336))
def emojiBelow2021(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1f9}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f527}")
def emojiBelow2022(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f4aa}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f478}\u{1f3fe}")
def emojiBelow2023(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2022(hash,text,U32.is_lt(hash,3413721979))
def emojiBelow2024(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2021(hash,text,U32.is_lt(hash,3410050201))
    case False{}: emojiBelow2023(hash,text,U32.is_lt(hash,3410437606))
def emojiBelow2025(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3fc}")
def emojiBelow2026(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow2025(hash,text,U32.is_lt(hash,3414683134))
def emojiBelow2027(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f476}\u{1f3fe}")
def emojiBelow2028(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f474}\u{1f3fe}")
    case False{}: emojiBelow2027(hash,text,U32.is_lt(hash,3415346069))
def emojiBelow2029(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2026(hash,text,U32.is_lt(hash,3414319092))
    case False{}: emojiBelow2028(hash,text,U32.is_lt(hash,3415021251))
def emojiBelow2030(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2024(hash,text,U32.is_lt(hash,3410275876))
    case False{}: emojiBelow2029(hash,text,U32.is_lt(hash,3414845543))
def emojiBelow2031(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f472}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f491}\u{1f3ff}")
def emojiBelow2032(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f47c}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2033(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fc}")
    case False{}: emojiBelow2032(hash,text,U32.is_lt(hash,3416028268))
def emojiBelow2034(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2031(hash,text,U32.is_lt(hash,3415647993))
    case False{}: emojiBelow2033(hash,text,U32.is_lt(hash,3416022303))
def emojiBelow2035(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f486}\u{1f3ff}")
def emojiBelow2036(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3ff}")
    case False{}: emojiBelow2035(hash,text,U32.is_lt(hash,3416758258))
def emojiBelow2037(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f46c}\u{1f3fe}")
def emojiBelow2038(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fc}")
    case False{}: emojiBelow2037(hash,text,U32.is_lt(hash,3416970159))
def emojiBelow2039(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2036(hash,text,U32.is_lt(hash,3416595849))
    case False{}: emojiBelow2038(hash,text,U32.is_lt(hash,3416933966))
def emojiBelow2040(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2034(hash,text,U32.is_lt(hash,3415684186))
    case False{}: emojiBelow2039(hash,text,U32.is_lt(hash,3416807750))
def emojiBelow2041(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2030(hash,text,U32.is_lt(hash,3414252907))
    case False{}: emojiBelow2040(hash,text,U32.is_lt(hash,3416095323))
def emojiBelow2042(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f483}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}")
def emojiBelow2043(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f46b}\u{1f3fc}")
def emojiBelow2044(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f48f}\u{1f3ff}")
    case False{}: emojiBelow2043(hash,text,U32.is_lt(hash,3417808802))
def emojiBelow2045(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2042(hash,text,U32.is_lt(hash,3417145867))
    case False{}: emojiBelow2044(hash,text,U32.is_lt(hash,3417470685))
def emojiBelow2046(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f590}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f466}\u{1f3fe}")
def emojiBelow2047(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2046(hash,text,U32.is_lt(hash,3418769957))
def emojiBelow2048(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f467}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{270d}\u{fe0f}")
def emojiBelow2049(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f485}\u{1f3ff}")
    case False{}: emojiBelow2048(hash,text,U32.is_lt(hash,3419575413))
def emojiBelow2050(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2047(hash,text,U32.is_lt(hash,3418528732))
    case False{}: emojiBelow2049(hash,text,U32.is_lt(hash,3418932366))
def emojiBelow2051(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2045(hash,text,U32.is_lt(hash,3417272083))
    case False{}: emojiBelow2050(hash,text,U32.is_lt(hash,3418896173))
def emojiBelow2052(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1fb}\u{1f1fa}")
def emojiBelow2053(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f450}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow2054(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1f4}")
    case False{}: emojiBelow2053(hash,text,U32.is_lt(hash,3421006991))
def emojiBelow2055(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2052(hash,text,U32.is_lt(hash,3420033824))
    case False{}: emojiBelow2054(hash,text,U32.is_lt(hash,3420218339))
def emojiBelow2056(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f3a4}")
    case False{}: Strings.equal(text,"\u{1f44e}\u{1f3fe}")
def emojiBelow2057(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44c}\u{1f3fe}")
    case False{}: emojiBelow2056(hash,text,U32.is_lt(hash,3421842429))
def emojiBelow2058(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1fa}")
def emojiBelow2059(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44f}\u{1f3fc}")
    case False{}: emojiBelow2058(hash,text,U32.is_lt(hash,3422158440))
def emojiBelow2060(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2057(hash,text,U32.is_lt(hash,3421809577))
    case False{}: emojiBelow2059(hash,text,U32.is_lt(hash,3422039919))
def emojiBelow2061(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2055(hash,text,U32.is_lt(hash,3420196233))
    case False{}: emojiBelow2060(hash,text,U32.is_lt(hash,3422004838))
def emojiBelow2062(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2051(hash,text,U32.is_lt(hash,3418428487))
    case False{}: emojiBelow2061(hash,text,U32.is_lt(hash,3421341903))
def emojiBelow2063(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2041(hash,text,U32.is_lt(hash,3415521777))
    case False{}: emojiBelow2062(hash,text,U32.is_lt(hash,3419945970))
def emojiBelow2064(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2020(hash,text,U32.is_lt(hash,3402736743))
    case False{}: emojiBelow2063(hash,text,U32.is_lt(hash,3417096375))
def emojiBelow2065(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1978(hash,text,U32.is_lt(hash,3385214719))
    case False{}: emojiBelow2064(hash,text,U32.is_lt(hash,3408879264))
def emojiBelow2066(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1893(hash,text,U32.is_lt(hash,3325115501))
    case False{}: emojiBelow2065(hash,text,U32.is_lt(hash,3399317704))
def emojiBelow2067(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1722(hash,text,U32.is_lt(hash,3194247124))
    case False{}: emojiBelow2066(hash,text,U32.is_lt(hash,3369613113))
def emojiBelow2068(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44b}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f448}\u{1f3fe}")
def emojiBelow2069(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f0}")
    case False{}: Strings.equal(text,"\u{1f44a}\u{1f3fe}")
def emojiBelow2070(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f4}\u{200d}\u{2620}\u{fe0f}")
    case False{}: emojiBelow2069(hash,text,U32.is_lt(hash,3422843481))
def emojiBelow2071(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2068(hash,text,U32.is_lt(hash,3422342955))
    case False{}: emojiBelow2070(hash,text,U32.is_lt(hash,3422408703))
def emojiBelow2072(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ed}\u{1f1fa}")
    case False{}: Strings.equal(text,"\u{1f447}\u{1f3fc}")
def emojiBelow2073(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e9}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f446}\u{1f3fe}")
def emojiBelow2074(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ee}\u{1f1f8}")
    case False{}: emojiBelow2073(hash,text,U32.is_lt(hash,3423967045))
def emojiBelow2075(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2072(hash,text,U32.is_lt(hash,3423304110))
    case False{}: emojiBelow2074(hash,text,U32.is_lt(hash,3423782530))
def emojiBelow2076(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2071(hash,text,U32.is_lt(hash,3422356195))
    case False{}: emojiBelow2075(hash,text,U32.is_lt(hash,3423532267))
def emojiBelow2077(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1f4}")
    case False{}: Strings.equal(text,"\u{1f442}\u{1f3fe}")
def emojiBelow2078(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{262f}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1f2}")
def emojiBelow2079(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f443}\u{1f3fc}")
    case False{}: emojiBelow2078(hash,text,U32.is_lt(hash,3425406620))
def emojiBelow2080(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2077(hash,text,U32.is_lt(hash,3424142753))
    case False{}: emojiBelow2079(hash,text,U32.is_lt(hash,3425401987))
def emojiBelow2081(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f237}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2626}\u{fe0f}")
def emojiBelow2082(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1f8}")
    case False{}: emojiBelow2081(hash,text,U32.is_lt(hash,3426332514))
def emojiBelow2083(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f482}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f486}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2084(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f4aa}\u{1f3fe}")
    case False{}: emojiBelow2083(hash,text,U32.is_lt(hash,3427948361))
def emojiBelow2085(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2082(hash,text,U32.is_lt(hash,3425684731))
    case False{}: emojiBelow2084(hash,text,U32.is_lt(hash,3427398104))
def emojiBelow2086(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2080(hash,text,U32.is_lt(hash,3424305162))
    case False{}: emojiBelow2085(hash,text,U32.is_lt(hash,3427215225))
def emojiBelow2087(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2076(hash,text,U32.is_lt(hash,3423194150))
    case False{}: emojiBelow2086(hash,text,U32.is_lt(hash,3425656883))
def emojiBelow2088(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f478}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3fd}")
def emojiBelow2089(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{2604}\u{fe0f}")
def emojiBelow2090(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f474}\u{1f3fd}")
    case False{}: emojiBelow2089(hash,text,U32.is_lt(hash,3431957588))
def emojiBelow2091(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2088(hash,text,U32.is_lt(hash,3431460753))
    case False{}: emojiBelow2090(hash,text,U32.is_lt(hash,3431798870))
def emojiBelow2092(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f47c}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fc}")
def emojiBelow2093(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f473}\u{1f3fd}")
    case False{}: emojiBelow2092(hash,text,U32.is_lt(hash,3432872942))
def emojiBelow2094(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fe}")
def emojiBelow2095(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fe}")
    case False{}: emojiBelow2094(hash,text,U32.is_lt(hash,3433711585))
def emojiBelow2096(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2093(hash,text,U32.is_lt(hash,3432799922))
    case False{}: emojiBelow2095(hash,text,U32.is_lt(hash,3433585369))
def emojiBelow2097(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2091(hash,text,U32.is_lt(hash,3431623162))
    case False{}: emojiBelow2096(hash,text,U32.is_lt(hash,3433535877))
def emojiBelow2098(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46c}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f483}\u{1f3fc}")
def emojiBelow2099(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f48f}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f46b}\u{1f3fd}")
def emojiBelow2100(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow2099(hash,text,U32.is_lt(hash,3434586421))
def emojiBelow2101(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2098(hash,text,U32.is_lt(hash,3433873994))
    case False{}: emojiBelow2100(hash,text,U32.is_lt(hash,3434049702))
def emojiBelow2102(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f467}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{2663}\u{fe0f}")
def emojiBelow2103(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2668}\u{fe0f}")
    case False{}: emojiBelow2102(hash,text,U32.is_lt(hash,3436239463))
def emojiBelow2104(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1f3}")
    case False{}: Strings.equal(text,"\u{1f450}\u{1f3fd}")
def emojiBelow2105(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f3}")
    case False{}: emojiBelow2104(hash,text,U32.is_lt(hash,3436995958))
def emojiBelow2106(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2103(hash,text,U32.is_lt(hash,3435709985))
    case False{}: emojiBelow2105(hash,text,U32.is_lt(hash,3436973852))
def emojiBelow2107(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2101(hash,text,U32.is_lt(hash,3433923486))
    case False{}: emojiBelow2106(hash,text,U32.is_lt(hash,3436723589))
def emojiBelow2108(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2097(hash,text,U32.is_lt(hash,3432461805))
    case False{}: emojiBelow2107(hash,text,U32.is_lt(hash,3435308936))
def emojiBelow2109(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2087(hash,text,U32.is_lt(hash,3424032793))
    case False{}: emojiBelow2108(hash,text,U32.is_lt(hash,3433747778))
def emojiBelow2110(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44c}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f6b5}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2111(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f44b}\u{1f3fd}")
def emojiBelow2112(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44f}\u{1f3fd}")
    case False{}: emojiBelow2111(hash,text,U32.is_lt(hash,3438958165))
def emojiBelow2113(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2110(hash,text,U32.is_lt(hash,3438283724))
    case False{}: emojiBelow2112(hash,text,U32.is_lt(hash,3438817538))
def emojiBelow2114(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f448}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f1f2}\u{1f1f3}")
def emojiBelow2115(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1f3}")
    case False{}: Strings.equal(text,"\u{1f443}\u{1f3fd}")
def emojiBelow2116(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f447}\u{1f3fd}")
    case False{}: emojiBelow2115(hash,text,U32.is_lt(hash,3441082781))
def emojiBelow2117(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2114(hash,text,U32.is_lt(hash,3439186322))
    case False{}: emojiBelow2116(hash,text,U32.is_lt(hash,3440810412))
def emojiBelow2118(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2113(hash,text,U32.is_lt(hash,3438782457))
    case False{}: emojiBelow2117(hash,text,U32.is_lt(hash,3440081729))
def emojiBelow2119(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1f3}")
def emojiBelow2120(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f474}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f470}\u{1f3fc}")
def emojiBelow2121(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f478}\u{1f3fc}")
    case False{}: emojiBelow2120(hash,text,U32.is_lt(hash,3448576489))
def emojiBelow2122(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2119(hash,text,U32.is_lt(hash,3442184239))
    case False{}: emojiBelow2121(hash,text,U32.is_lt(hash,3448400781))
def emojiBelow2123(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3ff}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f47c}\u{1f3fc}")
def emojiBelow2124(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b8}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2123(hash,text,U32.is_lt(hash,3449577541))
def emojiBelow2125(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6c0}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f6cc}\u{1f3ff}")
def emojiBelow2126(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3fd}")
    case False{}: emojiBelow2125(hash,text,U32.is_lt(hash,3450319292))
def emojiBelow2127(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2124(hash,text,U32.is_lt(hash,3448790262))
    case False{}: emojiBelow2126(hash,text,U32.is_lt(hash,3450143584))
def emojiBelow2128(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2122(hash,text,U32.is_lt(hash,3447277217))
    case False{}: emojiBelow2127(hash,text,U32.is_lt(hash,3449650561))
def emojiBelow2129(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2118(hash,text,U32.is_lt(hash,3439120574))
    case False{}: emojiBelow2128(hash,text,U32.is_lt(hash,3448694186))
def emojiBelow2130(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46c}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f483}\u{1f3fd}")
def emojiBelow2131(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f48f}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9af}")
def emojiBelow2132(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}")
    case False{}: emojiBelow2131(hash,text,U32.is_lt(hash,3450829952))
def emojiBelow2133(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2130(hash,text,U32.is_lt(hash,3450651613))
    case False{}: emojiBelow2132(hash,text,U32.is_lt(hash,3450827321))
def emojiBelow2134(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f450}\u{1f3fc}")
def emojiBelow2135(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1fc}")
    case False{}: emojiBelow2134(hash,text,U32.is_lt(hash,3453773577))
def emojiBelow2136(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f448}\u{1f3fc}")
def emojiBelow2137(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44c}\u{1f3fc}")
    case False{}: emojiBelow2136(hash,text,U32.is_lt(hash,3455898193))
def emojiBelow2138(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2135(hash,text,U32.is_lt(hash,3453751471))
    case False{}: emojiBelow2137(hash,text,U32.is_lt(hash,3455595157))
def emojiBelow2139(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2133(hash,text,U32.is_lt(hash,3450701105))
    case False{}: emojiBelow2138(hash,text,U32.is_lt(hash,3454897141))
def emojiBelow2140(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1ec}\u{1f1fa}")
def emojiBelow2141(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1fc}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1fa}")
def emojiBelow2142(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1f2}")
    case False{}: emojiBelow2141(hash,text,U32.is_lt(hash,3459212121))
def emojiBelow2143(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2140(hash,text,U32.is_lt(hash,3456999651))
    case False{}: emojiBelow2142(hash,text,U32.is_lt(hash,3457926148))
def emojiBelow2144(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f473}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2145(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2199}\u{fe0f}")
    case False{}: emojiBelow2144(hash,text,U32.is_lt(hash,3462507882))
def emojiBelow2146(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f477}\u{1f3fb}")
def emojiBelow2147(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f478}\u{1f3fb}")
    case False{}: emojiBelow2146(hash,text,U32.is_lt(hash,3465015991))
def emojiBelow2148(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2145(hash,text,U32.is_lt(hash,3461334945))
    case False{}: emojiBelow2147(hash,text,U32.is_lt(hash,3464776419))
def emojiBelow2149(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2143(hash,text,U32.is_lt(hash,3457588031))
    case False{}: emojiBelow2148(hash,text,U32.is_lt(hash,3464054836))
def emojiBelow2150(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2139(hash,text,U32.is_lt(hash,3453501208))
    case False{}: emojiBelow2149(hash,text,U32.is_lt(hash,3459606209))
def emojiBelow2151(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2129(hash,text,U32.is_lt(hash,3441598960))
    case False{}: emojiBelow2150(hash,text,U32.is_lt(hash,3455963941))
def emojiBelow2152(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2109(hash,text,U32.is_lt(hash,3430499598))
    case False{}: emojiBelow2151(hash,text,U32.is_lt(hash,3450525397))
def emojiBelow2153(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f470}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f471}\u{1f3fb}")
def emojiBelow2154(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3fb}")
def emojiBelow2155(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f472}\u{1f3fb}")
    case False{}: emojiBelow2154(hash,text,U32.is_lt(hash,3466914334))
def emojiBelow2156(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2153(hash,text,U32.is_lt(hash,3465516517))
    case False{}: emojiBelow2155(hash,text,U32.is_lt(hash,3466576217))
def emojiBelow2157(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6c0}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f6cc}\u{1f3fe}")
def emojiBelow2158(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64c}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}")
def emojiBelow2159(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fb}")
    case False{}: emojiBelow2158(hash,text,U32.is_lt(hash,3467478724))
def emojiBelow2160(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2157(hash,text,U32.is_lt(hash,3467096911))
    case False{}: emojiBelow2159(hash,text,U32.is_lt(hash,3467239152))
def emojiBelow2161(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2156(hash,text,U32.is_lt(hash,3465854634))
    case False{}: emojiBelow2160(hash,text,U32.is_lt(hash,3467140607))
def emojiBelow2162(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f645}\u{1f3fb}")
def emojiBelow2163(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fe}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f467}\u{1f3fb}")
def emojiBelow2164(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fb}")
    case False{}: emojiBelow2163(hash,text,U32.is_lt(hash,3469265223))
def emojiBelow2165(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2162(hash,text,U32.is_lt(hash,3468700833))
    case False{}: emojiBelow2164(hash,text,U32.is_lt(hash,3469121612))
def emojiBelow2166(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f6b5}\u{1f3fb}")
def emojiBelow2167(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow2166(hash,text,U32.is_lt(hash,3469648689))
def emojiBelow2168(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1f1}")
    case False{}: Strings.equal(text,"\u{1f450}\u{1f3fb}")
def emojiBelow2169(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fb}")
    case False{}: emojiBelow2168(hash,text,U32.is_lt(hash,3470551196))
def emojiBelow2170(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2167(hash,text,U32.is_lt(hash,3469486280))
    case False{}: emojiBelow2169(hash,text,U32.is_lt(hash,3470529090))
def emojiBelow2171(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2165(hash,text,U32.is_lt(hash,3469038950))
    case False{}: emojiBelow2170(hash,text,U32.is_lt(hash,3469986806))
def emojiBelow2172(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2161(hash,text,U32.is_lt(hash,3466921203))
    case False{}: emojiBelow2171(hash,text,U32.is_lt(hash,3469345832))
def emojiBelow2173(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2174(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44f}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f448}\u{1f3fb}")
def emojiBelow2175(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f449}\u{1f3fb}")
    case False{}: emojiBelow2174(hash,text,U32.is_lt(hash,3472675812))
def emojiBelow2176(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2173(hash,text,U32.is_lt(hash,3471870721))
    case False{}: emojiBelow2175(hash,text,U32.is_lt(hash,3472337695))
def emojiBelow2177(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f44a}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f447}\u{1f3fb}")
def emojiBelow2178(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1fd}")
    case False{}: emojiBelow2177(hash,text,U32.is_lt(hash,3473636967))
def emojiBelow2179(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1f1}")
    case False{}: Strings.equal(text,"\u{1f442}\u{1f3fb}")
def emojiBelow2180(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1f9}")
    case False{}: emojiBelow2179(hash,text,U32.is_lt(hash,3474475610))
def emojiBelow2181(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2178(hash,text,U32.is_lt(hash,3473176338))
    case False{}: emojiBelow2180(hash,text,U32.is_lt(hash,3474365650))
def emojiBelow2182(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2176(hash,text,U32.is_lt(hash,3472012877))
    case False{}: emojiBelow2181(hash,text,U32.is_lt(hash,3473777270))
def emojiBelow2183(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1f1}")
def emojiBelow2184(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f4aa}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2696}\u{fe0f}")
def emojiBelow2185(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1fd}")
    case False{}: emojiBelow2184(hash,text,U32.is_lt(hash,3478607948))
def emojiBelow2186(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2183(hash,text,U32.is_lt(hash,3475739477))
    case False{}: emojiBelow2185(hash,text,U32.is_lt(hash,3477548082))
def emojiBelow2187(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f491}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fb}")
def emojiBelow2188(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f692}")
    case False{}: emojiBelow2187(hash,text,U32.is_lt(hash,3483205799))
def emojiBelow2189(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6c0}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f481}\u{1f3fb}")
def emojiBelow2190(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fc}")
    case False{}: emojiBelow2189(hash,text,U32.is_lt(hash,3483706325))
def emojiBelow2191(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2188(hash,text,U32.is_lt(hash,3482758469))
    case False{}: emojiBelow2190(hash,text,U32.is_lt(hash,3483698822))
def emojiBelow2192(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2186(hash,text,U32.is_lt(hash,3475989740))
    case False{}: emojiBelow2191(hash,text,U32.is_lt(hash,3483353836))
def emojiBelow2193(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2182(hash,text,U32.is_lt(hash,3472741560))
    case False{}: emojiBelow2192(hash,text,U32.is_lt(hash,3481023096))
def emojiBelow2194(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2172(hash,text,U32.is_lt(hash,3467641133))
    case False{}: emojiBelow2193(hash,text,U32.is_lt(hash,3475075356))
def emojiBelow2195(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6cc}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fb}")
def emojiBelow2196(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2197(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f48f}\u{1f3fb}")
    case False{}: emojiBelow2196(hash,text,U32.is_lt(hash,3486168962))
def emojiBelow2198(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2195(hash,text,U32.is_lt(hash,3484044442))
    case False{}: emojiBelow2197(hash,text,U32.is_lt(hash,3485478452))
def emojiBelow2199(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1f0}")
def emojiBelow2200(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f1f2}\u{1f1fc}")
def emojiBelow2201(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f1}\u{1f1fe}")
    case False{}: emojiBelow2200(hash,text,U32.is_lt(hash,3489519179))
def emojiBelow2202(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2199(hash,text,U32.is_lt(hash,3487306709))
    case False{}: emojiBelow2201(hash,text,U32.is_lt(hash,3489284147))
def emojiBelow2203(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2198(hash,text,U32.is_lt(hash,3484382559))
    case False{}: emojiBelow2202(hash,text,U32.is_lt(hash,3488768390))
def emojiBelow2204(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1fc}\u{1f1f8}")
def emojiBelow2205(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1f0}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1fe}")
def emojiBelow2206(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1f8}")
    case False{}: emojiBelow2205(hash,text,U32.is_lt(hash,3491481386))
def emojiBelow2207(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2204(hash,text,U32.is_lt(hash,3489607033))
    case False{}: emojiBelow2206(hash,text,U32.is_lt(hash,3491143269))
def emojiBelow2208(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f52c}")
def emojiBelow2209(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1fe}")
    case False{}: emojiBelow2208(hash,text,U32.is_lt(hash,3493938296))
def emojiBelow2210(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f4bc}")
def emojiBelow2211(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f393}")
    case False{}: emojiBelow2210(hash,text,U32.is_lt(hash,3498141002))
def emojiBelow2212(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2209(hash,text,U32.is_lt(hash,3492767359))
    case False{}: emojiBelow2211(hash,text,U32.is_lt(hash,3497625162))
def emojiBelow2213(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2207(hash,text,U32.is_lt(hash,3490554889))
    case False{}: emojiBelow2212(hash,text,U32.is_lt(hash,3497491211))
def emojiBelow2214(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2203(hash,text,U32.is_lt(hash,3486426308))
    case False{}: emojiBelow2213(hash,text,U32.is_lt(hash,3492517096))
def emojiBelow2215(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f46e}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2216(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6c0}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f6cc}\u{1f3fc}")
def emojiBelow2217(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3fd}")
    case False{}: emojiBelow2216(hash,text,U32.is_lt(hash,3500652149))
def emojiBelow2218(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2215(hash,text,U32.is_lt(hash,3500161887))
    case False{}: emojiBelow2217(hash,text,U32.is_lt(hash,3500476441))
def emojiBelow2219(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f52c}")
    case False{}: Strings.equal(text,"\u{1f646}\u{1f3fd}")
def emojiBelow2220(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fd}")
    case False{}: emojiBelow2219(hash,text,U32.is_lt(hash,3502594188))
def emojiBelow2221(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fd}")
def emojiBelow2222(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2221(hash,text,U32.is_lt(hash,3503542044))
def emojiBelow2223(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2220(hash,text,U32.is_lt(hash,3502345638))
    case False{}: emojiBelow2222(hash,text,U32.is_lt(hash,3503203927))
def emojiBelow2224(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2218(hash,text,U32.is_lt(hash,3500469572))
    case False{}: emojiBelow2223(hash,text,U32.is_lt(hash,3502965901))
def emojiBelow2225(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1ff}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1ff}")
def emojiBelow2226(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1ff}")
    case False{}: Strings.equal(text,"\u{1f1f3}\u{1f1ff}")
def emojiBelow2227(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fa}\u{1f1ff}")
    case False{}: emojiBelow2226(hash,text,U32.is_lt(hash,3506046535))
def emojiBelow2228(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2225(hash,text,U32.is_lt(hash,3504084328))
    case False{}: emojiBelow2227(hash,text,U32.is_lt(hash,3505796272))
def emojiBelow2229(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1ff}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f9af}")
def emojiBelow2230(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow2229(hash,text,U32.is_lt(hash,3506316680))
def emojiBelow2231(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1ff}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1ff}")
def emojiBelow2232(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e9}\u{1f1ff}")
    case False{}: emojiBelow2231(hash,text,U32.is_lt(hash,3509294715))
def emojiBelow2233(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2230(hash,text,U32.is_lt(hash,3506296798))
    case False{}: emojiBelow2232(hash,text,U32.is_lt(hash,3507920888))
def emojiBelow2234(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2228(hash,text,U32.is_lt(hash,3504172182))
    case False{}: emojiBelow2233(hash,text,U32.is_lt(hash,3507670625))
def emojiBelow2235(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2224(hash,text,U32.is_lt(hash,3502256071))
    case False{}: emojiBelow2234(hash,text,U32.is_lt(hash,3506061766))
def emojiBelow2236(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2214(hash,text,U32.is_lt(hash,3489519954))
    case False{}: emojiBelow2235(hash,text,U32.is_lt(hash,3503834065))
def emojiBelow2237(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2194(hash,text,U32.is_lt(hash,3471272779))
    case False{}: emojiBelow2236(hash,text,U32.is_lt(hash,3500131455))
def emojiBelow2238(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2152(hash,text,U32.is_lt(hash,3438119522))
    case False{}: emojiBelow2237(hash,text,U32.is_lt(hash,3483874530))
def emojiBelow2239(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1fa70}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1ff}")
def emojiBelow2240(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow2241(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2240(hash,text,U32.is_lt(hash,3514728665))
def emojiBelow2242(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2239(hash,text,U32.is_lt(hash,3509544978))
    case False{}: emojiBelow2241(hash,text,U32.is_lt(hash,3514402781))
def emojiBelow2243(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2244(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f64d}\u{1f3fe}")
def emojiBelow2245(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f481}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2244(hash,text,U32.is_lt(hash,3516909074))
def emojiBelow2246(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2243(hash,text,U32.is_lt(hash,3515336077))
    case False{}: emojiBelow2245(hash,text,U32.is_lt(hash,3516612318))
def emojiBelow2247(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2242(hash,text,U32.is_lt(hash,3511331636))
    case False{}: emojiBelow2246(hash,text,U32.is_lt(hash,3515517650))
def emojiBelow2248(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f6c0}\u{1f3fb}")
def emojiBelow2249(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9d2}\u{200d}\u{1f9d2}")
    case False{}: Strings.equal(text,"\u{1f93e}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2250(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64f}\u{1f3fe}")
    case False{}: emojiBelow2249(hash,text,U32.is_lt(hash,3518908750))
def emojiBelow2251(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2248(hash,text,U32.is_lt(hash,3517254060))
    case False{}: emojiBelow2250(hash,text,U32.is_lt(hash,3517543080))
def emojiBelow2252(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3fe}")
def emojiBelow2253(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f645}\u{1f3fe}")
    case False{}: emojiBelow2252(hash,text,U32.is_lt(hash,3519534216))
def emojiBelow2254(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2708}\u{fe0f}")
def emojiBelow2255(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fe}")
    case False{}: emojiBelow2254(hash,text,U32.is_lt(hash,3520818889))
def emojiBelow2256(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2253(hash,text,U32.is_lt(hash,3519371807))
    case False{}: emojiBelow2255(hash,text,U32.is_lt(hash,3520319663))
def emojiBelow2257(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2251(hash,text,U32.is_lt(hash,3517409600))
    case False{}: emojiBelow2256(hash,text,U32.is_lt(hash,3519981546))
def emojiBelow2258(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2247(hash,text,U32.is_lt(hash,3515109276))
    case False{}: emojiBelow2257(hash,text,U32.is_lt(hash,3519033690))
def emojiBelow2259(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1fe}")
    case False{}: Strings.equal(text,"\u{1f1fa}\u{1f1fe}")
def emojiBelow2260(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f7}\u{1f1fc}")
    case False{}: Strings.equal(text,"\u{1f1f1}\u{1f1f8}")
def emojiBelow2261(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fe}")
    case False{}: emojiBelow2260(hash,text,U32.is_lt(hash,3522323628))
def emojiBelow2262(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2259(hash,text,U32.is_lt(hash,3520949801))
    case False{}: emojiBelow2261(hash,text,U32.is_lt(hash,3522235774))
def emojiBelow2263(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f1f2}\u{1f1fe}")
def emojiBelow2264(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1fe}")
    case False{}: emojiBelow2263(hash,text,U32.is_lt(hash,3523074417))
def emojiBelow2265(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ff}\u{1f1fc}")
    case False{}: Strings.equal(text,"\u{1f1ed}\u{1f1f0}")
def emojiBelow2266(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fd}\u{1f1f0}")
    case False{}: emojiBelow2265(hash,text,U32.is_lt(hash,3523859864))
def emojiBelow2267(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2264(hash,text,U32.is_lt(hash,3522839385))
    case False{}: emojiBelow2266(hash,text,U32.is_lt(hash,3523412534))
def emojiBelow2268(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2262(hash,text,U32.is_lt(hash,3521605636))
    case False{}: emojiBelow2267(hash,text,U32.is_lt(hash,3523324680))
def emojiBelow2269(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1fe}")
    case False{}: Strings.equal(text,"\u{1f1e8}\u{1f1fe}")
def emojiBelow2270(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f373}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow2271(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1fc}")
    case False{}: emojiBelow2270(hash,text,U32.is_lt(hash,3531506284))
def emojiBelow2272(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2269(hash,text,U32.is_lt(hash,3524698507))
    case False{}: emojiBelow2271(hash,text,U32.is_lt(hash,3526817254))
def emojiBelow2273(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f64e}\u{1f3ff}")
def emojiBelow2274(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3ff}")
    case False{}: emojiBelow2273(hash,text,U32.is_lt(hash,3534024810))
def emojiBelow2275(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64c}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f645}\u{1f3ff}")
def emojiBelow2276(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64f}\u{1f3ff}")
    case False{}: emojiBelow2275(hash,text,U32.is_lt(hash,3535811309))
def emojiBelow2277(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2274(hash,text,U32.is_lt(hash,3533686693))
    case False{}: emojiBelow2276(hash,text,U32.is_lt(hash,3534349628))
def emojiBelow2278(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2272(hash,text,U32.is_lt(hash,3526072334))
    case False{}: emojiBelow2277(hash,text,U32.is_lt(hash,3534187219))
def emojiBelow2279(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2268(hash,text,U32.is_lt(hash,3522573891))
    case False{}: emojiBelow2278(hash,text,U32.is_lt(hash,3531886895))
def emojiBelow2280(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2258(hash,text,U32.is_lt(hash,3517247191))
    case False{}: emojiBelow2279(hash,text,U32.is_lt(hash,3524110127))
def emojiBelow2281(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3ff}")
def emojiBelow2282(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3ff}")
def emojiBelow2283(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3ff}")
    case False{}: emojiBelow2282(hash,text,U32.is_lt(hash,3537097282))
def emojiBelow2284(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2281(hash,text,U32.is_lt(hash,3536311835))
    case False{}: emojiBelow2283(hash,text,U32.is_lt(hash,3536759165))
def emojiBelow2285(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1f9}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1fd}")
def emojiBelow2286(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f1}\u{1f1f9}")
def emojiBelow2287(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3ff}")
    case False{}: emojiBelow2286(hash,text,U32.is_lt(hash,3539101247))
def emojiBelow2288(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2285(hash,text,U32.is_lt(hash,3537639566))
    case False{}: emojiBelow2287(hash,text,U32.is_lt(hash,3538384983))
def emojiBelow2289(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2284(hash,text,U32.is_lt(hash,3536596756))
    case False{}: emojiBelow2288(hash,text,U32.is_lt(hash,3538383255))
def emojiBelow2290(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1f9}")
    case False{}: Strings.equal(text,"\u{1f93c}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2291(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ea}\u{1f1f9}")
    case False{}: Strings.equal(text,"\u{23f1}\u{fe0f}")
def emojiBelow2292(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1fd}")
    case False{}: emojiBelow2291(hash,text,U32.is_lt(hash,3541621545))
def emojiBelow2293(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2290(hash,text,U32.is_lt(hash,3539978350))
    case False{}: emojiBelow2292(hash,text,U32.is_lt(hash,3541563980))
def emojiBelow2294(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow2295(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1f1}")
    case False{}: emojiBelow2294(hash,text,U32.is_lt(hash,3548283903))
def emojiBelow2296(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f64f}\u{1f3fc}")
def emojiBelow2297(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fc}")
    case False{}: emojiBelow2296(hash,text,U32.is_lt(hash,3550964838))
def emojiBelow2298(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2295(hash,text,U32.is_lt(hash,3546470856))
    case False{}: emojiBelow2297(hash,text,U32.is_lt(hash,3550802429))
def emojiBelow2299(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2293(hash,text,U32.is_lt(hash,3541476126))
    case False{}: emojiBelow2298(hash,text,U32.is_lt(hash,3548664514))
def emojiBelow2300(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2289(hash,text,U32.is_lt(hash,3537389303))
    case False{}: emojiBelow2299(hash,text,U32.is_lt(hash,3543100216))
def emojiBelow2301(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64c}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f646}\u{1f3fe}")
def emojiBelow2302(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f6b6}\u{1f3fe}")
def emojiBelow2303(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3fc}")
    case False{}: emojiBelow2302(hash,text,U32.is_lt(hash,3553874901))
def emojiBelow2304(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2301(hash,text,U32.is_lt(hash,3552927045))
    case False{}: emojiBelow2303(hash,text,U32.is_lt(hash,3553374375))
def emojiBelow2305(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1f7}\u{1f1fa}")
def emojiBelow2306(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fa}\u{1f1f8}")
    case False{}: emojiBelow2305(hash,text,U32.is_lt(hash,3555791012))
def emojiBelow2307(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow2308(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f1}\u{1f1fa}")
    case False{}: emojiBelow2307(hash,text,U32.is_lt(hash,3556394623))
def emojiBelow2309(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2306(hash,text,U32.is_lt(hash,3555160874))
    case False{}: emojiBelow2308(hash,text,U32.is_lt(hash,3556129129))
def emojiBelow2310(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2304(hash,text,U32.is_lt(hash,3553089454))
    case False{}: emojiBelow2309(hash,text,U32.is_lt(hash,3555878866))
def emojiBelow2311(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f2}\u{1f1f8}")
def emojiBelow2312(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ed}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1ec}\u{1f1fc}")
def emojiBelow2313(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2312(hash,text,U32.is_lt(hash,3557665365))
def emojiBelow2314(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2311(hash,text,U32.is_lt(hash,3556629655))
    case False{}: emojiBelow2313(hash,text,U32.is_lt(hash,3557415102))
def emojiBelow2315(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ea}\u{1f1f8}")
    case False{}: Strings.equal(text,"\u{1f1eb}\u{1f1f2}")
def emojiBelow2316(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1fc}")
    case False{}: emojiBelow2315(hash,text,U32.is_lt(hash,3558504008))
def emojiBelow2317(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f64b}\u{1f3fd}")
def emojiBelow2318(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2317(hash,text,U32.is_lt(hash,3565442133))
def emojiBelow2319(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2316(hash,text,U32.is_lt(hash,3558341599))
    case False{}: emojiBelow2318(hash,text,U32.is_lt(hash,3565061522))
def emojiBelow2320(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2314(hash,text,U32.is_lt(hash,3557156122))
    case False{}: emojiBelow2319(hash,text,U32.is_lt(hash,3564646350))
def emojiBelow2321(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2310(hash,text,U32.is_lt(hash,3554505039))
    case False{}: emojiBelow2320(hash,text,U32.is_lt(hash,3558253745))
def emojiBelow2322(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2300(hash,text,U32.is_lt(hash,3539852036))
    case False{}: emojiBelow2321(hash,text,U32.is_lt(hash,3556572225))
def emojiBelow2323(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2280(hash,text,U32.is_lt(hash,3520861947))
    case False{}: emojiBelow2322(hash,text,U32.is_lt(hash,3551127247))
def emojiBelow2324(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f385}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f64f}\u{1f3fd}")
def emojiBelow2325(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f935}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2326(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64c}\u{1f3fd}")
    case False{}: emojiBelow2325(hash,text,U32.is_lt(hash,3569329104))
def emojiBelow2327(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2324(hash,text,U32.is_lt(hash,3567742457))
    case False{}: emojiBelow2326(hash,text,U32.is_lt(hash,3569304965))
def emojiBelow2328(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f6b4}\u{1f3fd}")
def emojiBelow2329(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fb}\u{1f1f3}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1fb}")
def emojiBelow2330(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1fb}")
    case False{}: emojiBelow2329(hash,text,U32.is_lt(hash,3571194804))
def emojiBelow2331(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2328(hash,text,U32.is_lt(hash,3570151994))
    case False{}: emojiBelow2330(hash,text,U32.is_lt(hash,3571032395))
def emojiBelow2332(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2327(hash,text,U32.is_lt(hash,3567904866))
    case False{}: emojiBelow2331(hash,text,U32.is_lt(hash,3570944541))
def emojiBelow2333(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{1f3fb}")
def emojiBelow2334(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1fb}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fb}")
def emojiBelow2335(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f1}\u{1f1fb}")
    case False{}: emojiBelow2334(hash,text,U32.is_lt(hash,3573421995))
def emojiBelow2336(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2333(hash,text,U32.is_lt(hash,3572122723))
    case False{}: emojiBelow2335(hash,text,U32.is_lt(hash,3573407274))
def emojiBelow2337(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1f3}")
    case False{}: Strings.equal(text,"\u{1f3cc}\u{1f3fb}")
def emojiBelow2338(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ed}\u{1f1f3}")
    case False{}: emojiBelow2337(hash,text,U32.is_lt(hash,3574585456))
def emojiBelow2339(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6b4}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2340(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1fb}")
    case False{}: emojiBelow2339(hash,text,U32.is_lt(hash,3575868864))
def emojiBelow2341(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2338(hash,text,U32.is_lt(hash,3574442984))
    case False{}: emojiBelow2340(hash,text,U32.is_lt(hash,3575379291))
def emojiBelow2342(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2336(hash,text,U32.is_lt(hash,3572656485))
    case False{}: emojiBelow2341(hash,text,U32.is_lt(hash,3575031364))
def emojiBelow2343(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2332(hash,text,U32.is_lt(hash,3569867073))
    case False{}: emojiBelow2342(hash,text,U32.is_lt(hash,3574192721))
def emojiBelow2344(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1e7}\u{1f1fb}")
def emojiBelow2345(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f646}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2346(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2345(hash,text,U32.is_lt(hash,3578466116))
def emojiBelow2347(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2344(hash,text,U32.is_lt(hash,3576405191))
    case False{}: emojiBelow2346(hash,text,U32.is_lt(hash,3577516034))
def emojiBelow2348(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f385}\u{1f3fc}")
def emojiBelow2349(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow2348(hash,text,U32.is_lt(hash,3583255924))
def emojiBelow2350(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1f7}\u{1f1f8}")
def emojiBelow2351(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64c}\u{1f3fc}")
    case False{}: emojiBelow2350(hash,text,U32.is_lt(hash,3589346250))
def emojiBelow2352(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2349(hash,text,U32.is_lt(hash,3582701613))
    case False{}: emojiBelow2351(hash,text,U32.is_lt(hash,3586929613))
def emojiBelow2353(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2347(hash,text,U32.is_lt(hash,3577513617))
    case False{}: emojiBelow2352(hash,text,U32.is_lt(hash,3584682485))
def emojiBelow2354(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1fa}")
    case False{}: Strings.equal(text,"\u{1f1ec}\u{1f1f2}")
def emojiBelow2355(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f344}\u{200d}\u{1f7eb}")
    case False{}: Strings.equal(text,"\u{1f1ea}\u{1f1fa}")
def emojiBelow2356(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1fa}")
    case False{}: emojiBelow2355(hash,text,U32.is_lt(hash,3591896837))
def emojiBelow2357(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2354(hash,text,U32.is_lt(hash,3591220603))
    case False{}: emojiBelow2356(hash,text,U32.is_lt(hash,3591828483))
def emojiBelow2358(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1f8}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1f2}")
def emojiBelow2359(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1eb}\u{1f1f0}")
    case False{}: emojiBelow2358(hash,text,U32.is_lt(hash,3593433073))
def emojiBelow2360(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64f}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f5de}\u{fe0f}")
def emojiBelow2361(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f385}\u{1f3fd}")
    case False{}: emojiBelow2360(hash,text,U32.is_lt(hash,3601527690))
def emojiBelow2362(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2359(hash,text,U32.is_lt(hash,3593182810))
    case False{}: emojiBelow2361(hash,text,U32.is_lt(hash,3601297695))
def emojiBelow2363(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2357(hash,text,U32.is_lt(hash,3591808983))
    case False{}: emojiBelow2362(hash,text,U32.is_lt(hash,3600033543))
def emojiBelow2364(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2353(hash,text,U32.is_lt(hash,3581839141))
    case False{}: emojiBelow2363(hash,text,U32.is_lt(hash,3592059246))
def emojiBelow2365(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2343(hash,text,U32.is_lt(hash,3571938493))
    case False{}: emojiBelow2364(hash,text,U32.is_lt(hash,3590184893))
def emojiBelow2366(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1f8}\u{1f1f9}")
def emojiBelow2367(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f3c2}\u{1f3fd}")
def emojiBelow2368(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5c2}\u{fe0f}")
    case False{}: emojiBelow2367(hash,text,U32.is_lt(hash,3607640168))
def emojiBelow2369(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2366(hash,text,U32.is_lt(hash,3604750042))
    case False{}: emojiBelow2368(hash,text,U32.is_lt(hash,3605515552))
def emojiBelow2370(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e7}\u{1f1f9}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2371(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1f1}")
    case False{}: emojiBelow2370(hash,text,U32.is_lt(hash,3614397275))
def emojiBelow2372(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f3ed}")
def emojiBelow2373(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f385}\u{1f3fe}")
    case False{}: emojiBelow2372(hash,text,U32.is_lt(hash,3617377571))
def emojiBelow2374(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2371(hash,text,U32.is_lt(hash,3609960429))
    case False{}: emojiBelow2373(hash,text,U32.is_lt(hash,3616888733))
def emojiBelow2375(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2369(hash,text,U32.is_lt(hash,3604879038))
    case False{}: emojiBelow2374(hash,text,U32.is_lt(hash,3616811162))
def emojiBelow2376(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f527}")
    case False{}: Strings.equal(text,"\u{1f1f9}\u{1f1e6}")
def emojiBelow2377(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{1f3fc}")
def emojiBelow2378(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f8}\u{1f1f8}")
    case False{}: emojiBelow2377(hash,text,U32.is_lt(hash,3622293171))
def emojiBelow2379(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2376(hash,text,U32.is_lt(hash,3621277398))
    case False{}: emojiBelow2378(hash,text,U32.is_lt(hash,3621757745))
def emojiBelow2380(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fe}")
def emojiBelow2381(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fe}")
    case False{}: emojiBelow2380(hash,text,U32.is_lt(hash,3623754852))
def emojiBelow2382(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c2}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9cd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2383(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2708}\u{fe0f}")
    case False{}: emojiBelow2382(hash,text,U32.is_lt(hash,3625492904))
def emojiBelow2384(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2381(hash,text,U32.is_lt(hash,3623141005))
    case False{}: emojiBelow2383(hash,text,U32.is_lt(hash,3624417787))
def emojiBelow2385(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2379(hash,text,U32.is_lt(hash,3621527661))
    case False{}: emojiBelow2384(hash,text,U32.is_lt(hash,3623879227))
def emojiBelow2386(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2375(hash,text,U32.is_lt(hash,3607998222))
    case False{}: emojiBelow2385(hash,text,U32.is_lt(hash,3622455580))
def emojiBelow2387(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1e6}")
    case False{}: Strings.equal(text,"\u{1f3c7}\u{1f3fe}")
def emojiBelow2388(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f578}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f573}\u{fe0f}")
def emojiBelow2389(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1f4}")
    case False{}: emojiBelow2388(hash,text,U32.is_lt(hash,3629176311))
def emojiBelow2390(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2387(hash,text,U32.is_lt(hash,3626880520))
    case False{}: emojiBelow2389(hash,text,U32.is_lt(hash,3628245784))
def emojiBelow2391(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f385}\u{1f3ff}")
def emojiBelow2392(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow2391(hash,text,U32.is_lt(hash,3633588781))
def emojiBelow2393(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{1f3ff}")
def emojiBelow2394(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f3a4}")
    case False{}: emojiBelow2393(hash,text,U32.is_lt(hash,3639233199))
def emojiBelow2395(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2392(hash,text,U32.is_lt(hash,3632582415))
    case False{}: emojiBelow2394(hash,text,U32.is_lt(hash,3639070790))
def emojiBelow2396(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2390(hash,text,U32.is_lt(hash,3626988311))
    case False{}: emojiBelow2395(hash,text,U32.is_lt(hash,3636415441))
def emojiBelow2397(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cd}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f1}\u{1f1f7}")
def emojiBelow2398(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2399(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1f7}")
    case False{}: emojiBelow2398(hash,text,U32.is_lt(hash,3640764039))
def emojiBelow2400(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2397(hash,text,U32.is_lt(hash,3639766961))
    case False{}: emojiBelow2399(hash,text,U32.is_lt(hash,3640532471))
def emojiBelow2401(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ed}\u{1f1f7}")
    case False{}: Strings.equal(text,"\u{1f1ec}\u{1f1f7}")
def emojiBelow2402(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c2}\u{1f3ff}")
    case False{}: emojiBelow2401(hash,text,U32.is_lt(hash,3641553460))
def emojiBelow2403(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ea}\u{1f1f7}")
    case False{}: Strings.equal(text,"\u{1f1eb}\u{1f1f7}")
def emojiBelow2404(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3ff}")
    case False{}: emojiBelow2403(hash,text,U32.is_lt(hash,3642392103))
def emojiBelow2405(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2402(hash,text,U32.is_lt(hash,3641303197))
    case False{}: emojiBelow2404(hash,text,U32.is_lt(hash,3642229694))
def emojiBelow2406(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2400(hash,text,U32.is_lt(hash,3640017224))
    case False{}: emojiBelow2405(hash,text,U32.is_lt(hash,3641695932))
def emojiBelow2407(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2396(hash,text,U32.is_lt(hash,3632465122))
    case False{}: emojiBelow2406(hash,text,U32.is_lt(hash,3641195406))
def emojiBelow2408(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2386(hash,text,U32.is_lt(hash,3619788971))
    case False{}: emojiBelow2407(hash,text,U32.is_lt(hash,3639373554))
def emojiBelow2409(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2365(hash,text,U32.is_lt(hash,3575884728))
    case False{}: emojiBelow2408(hash,text,U32.is_lt(hash,3625702338))
def emojiBelow2410(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2323(hash,text,U32.is_lt(hash,3536149426))
    case False{}: emojiBelow2409(hash,text,U32.is_lt(hash,3603422311))
def emojiBelow2411(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2238(hash,text,U32.is_lt(hash,3465354108))
    case False{}: emojiBelow2410(hash,text,U32.is_lt(hash,3566478305))
def emojiBelow2412(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f3c7}\u{1f3ff}")
def emojiBelow2413(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow2414(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1f7}")
    case False{}: emojiBelow2413(hash,text,U32.is_lt(hash,3645760936))
def emojiBelow2415(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2412(hash,text,U32.is_lt(hash,3643658139))
    case False{}: emojiBelow2414(hash,text,U32.is_lt(hash,3645163111))
def emojiBelow2416(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f5f3}\u{fe0f}")
def emojiBelow2417(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{1f3fe}")
def emojiBelow2418(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9bc}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow2417(hash,text,U32.is_lt(hash,3655848409))
def emojiBelow2419(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2416(hash,text,U32.is_lt(hash,3650067831))
    case False{}: emojiBelow2418(hash,text,U32.is_lt(hash,3654842283))
def emojiBelow2420(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2415(hash,text,U32.is_lt(hash,3643765930))
    case False{}: emojiBelow2419(hash,text,U32.is_lt(hash,3652699583))
def emojiBelow2421(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1f1}\u{1f1f0}")
def emojiBelow2422(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f3c2}\u{1f3fe}")
def emojiBelow2423(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1e6}")
    case False{}: emojiBelow2422(hash,text,U32.is_lt(hash,3657973025))
def emojiBelow2424(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2421(hash,text,U32.is_lt(hash,3656544580))
    case False{}: emojiBelow2423(hash,text,U32.is_lt(hash,3657310090))
def emojiBelow2425(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f1ef}\u{1f1f4}")
def emojiBelow2426(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1f6}")
    case False{}: emojiBelow2425(hash,text,U32.is_lt(hash,3658581342))
def emojiBelow2427(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1eb}\u{1f1f4}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{1f3fe}")
def emojiBelow2428(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1e6}")
    case False{}: emojiBelow2427(hash,text,U32.is_lt(hash,3659772823))
def emojiBelow2429(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2426(hash,text,U32.is_lt(hash,3658473551))
    case False{}: emojiBelow2428(hash,text,U32.is_lt(hash,3659169722))
def emojiBelow2430(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2424(hash,text,U32.is_lt(hash,3657295369))
    case False{}: emojiBelow2429(hash,text,U32.is_lt(hash,3658919459))
def emojiBelow2431(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2420(hash,text,U32.is_lt(hash,3649298037))
    case False{}: emojiBelow2430(hash,text,U32.is_lt(hash,3658331079))
def emojiBelow2432(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c7}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1f6}")
def emojiBelow2433(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f470}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2434(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow2433(hash,text,U32.is_lt(hash,3666342434))
def emojiBelow2435(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2432(hash,text,U32.is_lt(hash,3660543549))
    case False{}: emojiBelow2434(hash,text,U32.is_lt(hash,3664592053))
def emojiBelow2436(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2934}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f926}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2437(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow2436(hash,text,U32.is_lt(hash,3671987425))
def emojiBelow2438(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f0}\u{1f1f5}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fd}")
def emojiBelow2439(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3fd}")
    case False{}: emojiBelow2438(hash,text,U32.is_lt(hash,3674087709))
def emojiBelow2440(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2437(hash,text,U32.is_lt(hash,3671143684))
    case False{}: emojiBelow2439(hash,text,U32.is_lt(hash,3673572462))
def emojiBelow2441(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2435(hash,text,U32.is_lt(hash,3662538555))
    case False{}: emojiBelow2440(hash,text,U32.is_lt(hash,3672788437))
def emojiBelow2442(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ec}\u{1f1f5}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1e9}")
def emojiBelow2443(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ef}\u{1f1f5}")
    case False{}: Strings.equal(text,"\u{1f3c4}\u{1f3fd}")
def emojiBelow2444(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fd}")
    case False{}: emojiBelow2443(hash,text,U32.is_lt(hash,3676550442))
def emojiBelow2445(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2442(hash,text,U32.is_lt(hash,3675196552))
    case False{}: emojiBelow2444(hash,text,U32.is_lt(hash,3675358961))
def emojiBelow2446(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1e9}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow2447(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c7}\u{1f3fd}")
    case False{}: emojiBelow2446(hash,text,U32.is_lt(hash,3679316174))
def emojiBelow2448(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f935}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1fb}\u{1f1ea}")
def emojiBelow2449(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f43f}\u{fe0f}")
    case False{}: emojiBelow2448(hash,text,U32.is_lt(hash,3688475728))
def emojiBelow2450(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2447(hash,text,U32.is_lt(hash,3677321168))
    case False{}: emojiBelow2449(hash,text,U32.is_lt(hash,3683099049))
def emojiBelow2451(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2445(hash,text,U32.is_lt(hash,3675251170))
    case False{}: emojiBelow2450(hash,text,U32.is_lt(hash,3682199827))
def emojiBelow2452(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2441(hash,text,U32.is_lt(hash,3667451857))
    case False{}: emojiBelow2451(hash,text,U32.is_lt(hash,3677213377))
def emojiBelow2453(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2431(hash,text,U32.is_lt(hash,3656010818))
    case False{}: emojiBelow2452(hash,text,U32.is_lt(hash,3675108698))
def emojiBelow2454(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ff}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1e8}")
def emojiBelow2455(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ef}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1ea}")
def emojiBelow2456(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fc}")
    case False{}: emojiBelow2455(hash,text,U32.is_lt(hash,3692812814))
def emojiBelow2457(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2454(hash,text,U32.is_lt(hash,3691974171))
    case False{}: emojiBelow2456(hash,text,U32.is_lt(hash,3692136580))
def emojiBelow2458(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c4}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1e8}")
def emojiBelow2459(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f9bd}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2460(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2459(hash,text,U32.is_lt(hash,3697165929))
def emojiBelow2461(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2458(hash,text,U32.is_lt(hash,3694098787))
    case False{}: emojiBelow2460(hash,text,U32.is_lt(hash,3696093793))
def emojiBelow2462(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2457(hash,text,U32.is_lt(hash,3692028789))
    case False{}: emojiBelow2461(hash,text,U32.is_lt(hash,3694897222))
def emojiBelow2463(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f3a8}")
    case False{}: Strings.equal(text,"\u{1f1fa}\u{1f1f3}")
def emojiBelow2464(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f1f0}\u{1f1f3}")
def emojiBelow2465(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3fb}")
    case False{}: emojiBelow2464(hash,text,U32.is_lt(hash,3707127700))
def emojiBelow2466(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2463(hash,text,U32.is_lt(hash,3705503610))
    case False{}: emojiBelow2465(hash,text,U32.is_lt(hash,3707029100))
def emojiBelow2467(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c2}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1eb}")
def emojiBelow2468(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fc}\u{1f1eb}")
    case False{}: emojiBelow2467(hash,text,U32.is_lt(hash,3709590433))
def emojiBelow2469(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1eb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1f37c}")
def emojiBelow2470(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c7}\u{1f3fb}")
    case False{}: emojiBelow2469(hash,text,U32.is_lt(hash,3711371063))
def emojiBelow2471(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2468(hash,text,U32.is_lt(hash,3708305882))
    case False{}: emojiBelow2470(hash,text,U32.is_lt(hash,3710876406))
def emojiBelow2472(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2466(hash,text,U32.is_lt(hash,3706181266))
    case False{}: emojiBelow2471(hash,text,U32.is_lt(hash,3710768615))
def emojiBelow2473(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2462(hash,text,U32.is_lt(hash,3693328061))
    case False{}: emojiBelow2472(hash,text,U32.is_lt(hash,3707716080))
def emojiBelow2474(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9da}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow2475(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f3eb}")
    case False{}: Strings.equal(text,"\u{1f1f9}\u{1f1ec}")
def emojiBelow2476(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow2475(hash,text,U32.is_lt(hash,3721943112))
def emojiBelow2477(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2474(hash,text,U32.is_lt(hash,3712871412))
    case False{}: emojiBelow2476(hash,text,U32.is_lt(hash,3718043285))
def emojiBelow2478(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fa}\u{1f1f2}")
    case False{}: Strings.equal(text,"\u{1f1f0}\u{1f1f2}")
def emojiBelow2479(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fb}\u{1f1e8}")
    case False{}: emojiBelow2478(hash,text,U32.is_lt(hash,3723905319))
def emojiBelow2480(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1ee}\u{1f1ea}")
def emojiBelow2481(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f692}")
    case False{}: emojiBelow2480(hash,text,U32.is_lt(hash,3725529409))
def emojiBelow2482(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2479(hash,text,U32.is_lt(hash,3722281229))
    case False{}: emojiBelow2481(hash,text,U32.is_lt(hash,3725482205))
def emojiBelow2483(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2477(hash,text,U32.is_lt(hash,3714028712))
    case False{}: emojiBelow2482(hash,text,U32.is_lt(hash,3724116223))
def emojiBelow2484(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f5}\u{1f1ec}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1ea}")
def emojiBelow2485(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6f3}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f33e}")
def emojiBelow2486(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: emojiBelow2485(hash,text,U32.is_lt(hash,3737438640))
def emojiBelow2487(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2484(hash,text,U32.is_lt(hash,3727654025))
    case False{}: emojiBelow2486(hash,text,U32.is_lt(hash,3736419447))
def emojiBelow2488(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1ed}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2489(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1ed}")
    case False{}: emojiBelow2488(hash,text,U32.is_lt(hash,3742229089))
def emojiBelow2490(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6cb}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f5}\u{1f1ed}")
def emojiBelow2491(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ea}\u{1f1ed}")
    case False{}: emojiBelow2490(hash,text,U32.is_lt(hash,3743145671))
def emojiBelow2492(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2489(hash,text,U32.is_lt(hash,3741183464))
    case False{}: emojiBelow2491(hash,text,U32.is_lt(hash,3743122143))
def emojiBelow2493(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2487(hash,text,U32.is_lt(hash,3730806331))
    case False{}: emojiBelow2492(hash,text,U32.is_lt(hash,3742895408))
def emojiBelow2494(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2483(hash,text,U32.is_lt(hash,3722030966))
    case False{}: emojiBelow2493(hash,text,U32.is_lt(hash,3738720731))
def emojiBelow2495(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2473(hash,text,U32.is_lt(hash,3699575598))
    case False{}: emojiBelow2494(hash,text,U32.is_lt(hash,3726368052))
def emojiBelow2496(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2453(hash,text,U32.is_lt(hash,3660435758))
    case False{}: emojiBelow2495(hash,text,U32.is_lt(hash,3712344971))
def emojiBelow2497(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f9af}")
def emojiBelow2498(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9bd}")
    case False{}: Strings.equal(text,"\u{2b07}\u{fe0f}")
def emojiBelow2499(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: emojiBelow2498(hash,text,U32.is_lt(hash,3754089547))
def emojiBelow2500(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2497(hash,text,U32.is_lt(hash,3749289447))
    case False{}: emojiBelow2499(hash,text,U32.is_lt(hash,3752827094))
def emojiBelow2501(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fb}\u{1f1ee}")
    case False{}: Strings.equal(text,"\u{1f1f1}\u{1f1ee}")
def emojiBelow2502(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ea}\u{1f1ec}")
    case False{}: Strings.equal(text,"\u{1f1e6}\u{1f1ec}")
def emojiBelow2503(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1ec}")
    case False{}: emojiBelow2502(hash,text,U32.is_lt(hash,3761209263))
def emojiBelow2504(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2501(hash,text,U32.is_lt(hash,3757210294))
    case False{}: emojiBelow2503(hash,text,U32.is_lt(hash,3759673027))
def emojiBelow2505(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2500(hash,text,U32.is_lt(hash,3751794651))
    case False{}: emojiBelow2504(hash,text,U32.is_lt(hash,3757961083))
def emojiBelow2506(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow2507(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{31}\u{fe0f}\u{20e3}")
    case False{}: Strings.equal(text,"\u{a9}\u{fe0f}")
def emojiBelow2508(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow2507(hash,text,U32.is_lt(hash,3768258129))
def emojiBelow2509(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2506(hash,text,U32.is_lt(hash,3764361569))
    case False{}: emojiBelow2508(hash,text,U32.is_lt(hash,3765819326))
def emojiBelow2510(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9bc}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f4bc}")
def emojiBelow2511(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow2510(hash,text,U32.is_lt(hash,3770739561))
def emojiBelow2512(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1ef}")
    case False{}: Strings.equal(text,"\u{1f937}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2513(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f408}\u{200d}\u{2b1b}")
    case False{}: emojiBelow2512(hash,text,U32.is_lt(hash,3775970519))
def emojiBelow2514(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2511(hash,text,U32.is_lt(hash,3769604713))
    case False{}: emojiBelow2513(hash,text,U32.is_lt(hash,3772275969))
def emojiBelow2515(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2509(hash,text,U32.is_lt(hash,3765540097))
    case False{}: emojiBelow2514(hash,text,U32.is_lt(hash,3772202831))
def emojiBelow2516(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2505(hash,text,U32.is_lt(hash,3755586204))
    case False{}: emojiBelow2515(hash,text,U32.is_lt(hash,3768572270))
def emojiBelow2517(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow2518(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fc}")
def emojiBelow2519(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f680}")
    case False{}: emojiBelow2518(hash,text,U32.is_lt(hash,3787457867))
def emojiBelow2520(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2517(hash,text,U32.is_lt(hash,3781139188))
    case False{}: emojiBelow2519(hash,text,U32.is_lt(hash,3785349889))
def emojiBelow2521(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1e8}")
    case False{}: Strings.equal(text,"\u{1f1fb}\u{1f1ec}")
def emojiBelow2522(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f4bb}")
    case False{}: emojiBelow2521(hash,text,U32.is_lt(hash,3789141442))
def emojiBelow2523(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1ee}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f373}")
def emojiBelow2524(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f1}\u{1f1e8}")
    case False{}: emojiBelow2523(hash,text,U32.is_lt(hash,3794056453))
def emojiBelow2525(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2522(hash,text,U32.is_lt(hash,3789053588))
    case False{}: emojiBelow2524(hash,text,U32.is_lt(hash,3793140411))
def emojiBelow2526(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2520(hash,text,U32.is_lt(hash,3783020238))
    case False{}: emojiBelow2525(hash,text,U32.is_lt(hash,3790765532))
def emojiBelow2527(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e6}\u{1f1ee}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{1f33e}")
def emojiBelow2528(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f46f}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2529(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: emojiBelow2528(hash,text,U32.is_lt(hash,3796671076))
def emojiBelow2530(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2527(hash,text,U32.is_lt(hash,3795729102))
    case False{}: emojiBelow2529(hash,text,U32.is_lt(hash,3796100383))
def emojiBelow2531(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3ff}")
def emojiBelow2532(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f692}")
    case False{}: emojiBelow2531(hash,text,U32.is_lt(hash,3804235486))
def emojiBelow2533(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1e9}")
    case False{}: Strings.equal(text,"\u{1f1e8}\u{1f1ed}")
def emojiBelow2534(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f9}\u{1f1e9}")
    case False{}: emojiBelow2533(hash,text,U32.is_lt(hash,3809918030))
def emojiBelow2535(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2532(hash,text,U32.is_lt(hash,3802127508))
    case False{}: emojiBelow2534(hash,text,U32.is_lt(hash,3808293940))
def emojiBelow2536(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2530(hash,text,U32.is_lt(hash,3795964705))
    case False{}: emojiBelow2535(hash,text,U32.is_lt(hash,3805831207))
def emojiBelow2537(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2526(hash,text,U32.is_lt(hash,3787517180))
    case False{}: emojiBelow2536(hash,text,U32.is_lt(hash,3799931808))
def emojiBelow2538(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2516(hash,text,U32.is_lt(hash,3762409467))
    case False{}: emojiBelow2537(hash,text,U32.is_lt(hash,3794764501))
def emojiBelow2539(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f37c}")
def emojiBelow2540(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f393}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fe}")
def emojiBelow2541(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow2540(hash,text,U32.is_lt(hash,3821013105))
def emojiBelow2542(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2539(hash,text,U32.is_lt(hash,3810834072))
    case False{}: emojiBelow2541(hash,text,U32.is_lt(hash,3817018931))
def emojiBelow2543(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3c3}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2544(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1e8}")
    case False{}: Strings.equal(text,"\u{1f1ef}\u{1f1ea}")
def emojiBelow2545(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f7}\u{1f1ea}")
    case False{}: emojiBelow2544(hash,text,U32.is_lt(hash,3826357532))
def emojiBelow2546(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2543(hash,text,U32.is_lt(hash,3823756514))
    case False{}: emojiBelow2545(hash,text,U32.is_lt(hash,3825071559))
def emojiBelow2547(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2542(hash,text,U32.is_lt(hash,3815623193))
    case False{}: emojiBelow2546(hash,text,U32.is_lt(hash,3824232916))
def emojiBelow2548(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1ec}")
    case False{}: Strings.equal(text,"\u{1f1ea}\u{1f1e8}")
def emojiBelow2549(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64b}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f37c}")
def emojiBelow2550(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2549(hash,text,U32.is_lt(hash,3830818425))
def emojiBelow2551(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2548(hash,text,U32.is_lt(hash,3826783503))
    case False{}: emojiBelow2550(hash,text,U32.is_lt(hash,3830718256))
def emojiBelow2552(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{fe0f}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d6}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2553(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f575}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2552(hash,text,U32.is_lt(hash,3835739925))
def emojiBelow2554(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b6}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f1f9}\u{1f1eb}")
def emojiBelow2555(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow2554(hash,text,U32.is_lt(hash,3839386445))
def emojiBelow2556(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2553(hash,text,U32.is_lt(hash,3834876061))
    case False{}: emojiBelow2555(hash,text,U32.is_lt(hash,3838944519))
def emojiBelow2557(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2551(hash,text,U32.is_lt(hash,3827493258))
    case False{}: emojiBelow2556(hash,text,U32.is_lt(hash,3836933156))
def emojiBelow2558(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2547(hash,text,U32.is_lt(hash,3822759354))
    case False{}: emojiBelow2557(hash,text,U32.is_lt(hash,3833021567))
def emojiBelow2559(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f2}\u{1f1eb}")
    case False{}: Strings.equal(text,"\u{1f1e8}\u{1f1eb}")
def emojiBelow2560(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f384}")
    case False{}: Strings.equal(text,"\u{1f34b}\u{200d}\u{1f7e9}")
def emojiBelow2561(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: emojiBelow2560(hash,text,U32.is_lt(hash,3855360516))
def emojiBelow2562(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2559(hash,text,U32.is_lt(hash,3843473268))
    case False{}: emojiBelow2561(hash,text,U32.is_lt(hash,3849821210))
def emojiBelow2563(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f3eb}")
    case False{}: Strings.equal(text,"\u{1f1f2}\u{1f1ea}")
def emojiBelow2564(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2563(hash,text,U32.is_lt(hash,3858626797))
def emojiBelow2565(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f39a}\u{fe0f}")
def emojiBelow2566(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1ea}\u{1f1ea}")
    case False{}: emojiBelow2565(hash,text,U32.is_lt(hash,3869450150))
def emojiBelow2567(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2564(hash,text,U32.is_lt(hash,3857759441))
    case False{}: emojiBelow2566(hash,text,U32.is_lt(hash,3869218484))
def emojiBelow2568(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2562(hash,text,U32.is_lt(hash,3846297562))
    case False{}: emojiBelow2567(hash,text,U32.is_lt(hash,3860338741))
def emojiBelow2569(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f3eb}")
def emojiBelow2570(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1e9}")
    case False{}: Strings.equal(text,"\u{1f481}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2571(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3f3}\u{fe0f}")
    case False{}: emojiBelow2570(hash,text,U32.is_lt(hash,3878965763))
def emojiBelow2572(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2569(hash,text,U32.is_lt(hash,3872445862))
    case False{}: emojiBelow2571(hash,text,U32.is_lt(hash,3877028506))
def emojiBelow2573(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3de}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{fe0f}")
def emojiBelow2574(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f52c}")
    case False{}: emojiBelow2573(hash,text,U32.is_lt(hash,3882295775))
def emojiBelow2575(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow2576(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1fb}\u{1f1e6}")
    case False{}: emojiBelow2575(hash,text,U32.is_lt(hash,3890954688))
def emojiBelow2577(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2574(hash,text,U32.is_lt(hash,3880287626))
    case False{}: emojiBelow2576(hash,text,U32.is_lt(hash,3890291753))
def emojiBelow2578(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2572(hash,text,U32.is_lt(hash,3876005751))
    case False{}: emojiBelow2577(hash,text,U32.is_lt(hash,3889807156))
def emojiBelow2579(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2568(hash,text,U32.is_lt(hash,3855580397))
    case False{}: emojiBelow2578(hash,text,U32.is_lt(hash,3879556549))
def emojiBelow2580(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2558(hash,text,U32.is_lt(hash,3826695649))
    case False{}: emojiBelow2579(hash,text,U32.is_lt(hash,3871345962))
def emojiBelow2581(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2538(hash,text,U32.is_lt(hash,3779187086))
    case False{}: emojiBelow2580(hash,text,U32.is_lt(hash,3841849178))
def emojiBelow2582(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2496(hash,text,U32.is_lt(hash,3691188724))
    case False{}: emojiBelow2581(hash,text,U32.is_lt(hash,3810641000))
def emojiBelow2583(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{1f3ed}")
    case False{}: Strings.equal(text,"\u{1f1f1}\u{1f1e6}")
def emojiBelow2584(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9ce}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2585(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1e8}\u{1f1e8}")
    case False{}: emojiBelow2584(hash,text,U32.is_lt(hash,3897124561))
def emojiBelow2586(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2583(hash,text,U32.is_lt(hash,3891431246))
    case False{}: emojiBelow2585(hash,text,U32.is_lt(hash,3894351457))
def emojiBelow2587(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f336}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f93e}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2588(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64e}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f6b5}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2589(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f329}\u{fe0f}")
    case False{}: emojiBelow2588(hash,text,U32.is_lt(hash,3903599387))
def emojiBelow2590(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2587(hash,text,U32.is_lt(hash,3898361290))
    case False{}: emojiBelow2589(hash,text,U32.is_lt(hash,3900392912))
def emojiBelow2591(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2586(hash,text,U32.is_lt(hash,3893806125))
    case False{}: emojiBelow2590(hash,text,U32.is_lt(hash,3899372497))
def emojiBelow2592(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f3ed}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow2593(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f935}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2594(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f1f1}\u{1f1e7}")
    case False{}: emojiBelow2593(hash,text,U32.is_lt(hash,3915123775))
def emojiBelow2595(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2592(hash,text,U32.is_lt(hash,3907069372))
    case False{}: emojiBelow2594(hash,text,U32.is_lt(hash,3910659536))
def emojiBelow2596(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{27a1}\u{fe0f}")
def emojiBelow2597(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f938}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2596(hash,text,U32.is_lt(hash,3919981273))
def emojiBelow2598(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f1ea}\u{1f1e6}")
def emojiBelow2599(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9b3}")
    case False{}: emojiBelow2598(hash,text,U32.is_lt(hash,3927449217))
def emojiBelow2600(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2597(hash,text,U32.is_lt(hash,3916421200))
    case False{}: emojiBelow2599(hash,text,U32.is_lt(hash,3923846991))
def emojiBelow2601(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2595(hash,text,U32.is_lt(hash,3908208865))
    case False{}: emojiBelow2600(hash,text,U32.is_lt(hash,3920603284))
def emojiBelow2602(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2591(hash,text,U32.is_lt(hash,3898029298))
    case False{}: emojiBelow2601(hash,text,U32.is_lt(hash,3915882972))
def emojiBelow2603(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f482}\u{1f3fc}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2604(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{26f0}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow2605(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9b2}")
    case False{}: emojiBelow2604(hash,text,U32.is_lt(hash,3940624610))
def emojiBelow2606(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2603(hash,text,U32.is_lt(hash,3930781259))
    case False{}: emojiBelow2605(hash,text,U32.is_lt(hash,3938653472))
def emojiBelow2607(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dd}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2734}\u{fe0f}")
def emojiBelow2608(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3ca}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2607(hash,text,U32.is_lt(hash,3949903620))
def emojiBelow2609(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9b1}")
    case False{}: Strings.equal(text,"\u{1f3ca}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2610(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2721}\u{fe0f}")
    case False{}: emojiBelow2609(hash,text,U32.is_lt(hash,3955324110))
def emojiBelow2611(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2608(hash,text,U32.is_lt(hash,3943876807))
    case False{}: emojiBelow2610(hash,text,U32.is_lt(hash,3954158522))
def emojiBelow2612(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2606(hash,text,U32.is_lt(hash,3937380903))
    case False{}: emojiBelow2611(hash,text,U32.is_lt(hash,3951911769))
def emojiBelow2613(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{270c}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2614(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{263a}\u{fe0f}")
def emojiBelow2615(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow2614(hash,text,U32.is_lt(hash,3958668870))
def emojiBelow2616(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2613(hash,text,U32.is_lt(hash,3956580915))
    case False{}: emojiBelow2615(hash,text,U32.is_lt(hash,3958065164))
def emojiBelow2617(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d9}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f527}")
def emojiBelow2618(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f384}")
    case False{}: emojiBelow2617(hash,text,U32.is_lt(hash,3963444644))
def emojiBelow2619(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f471}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{267e}\u{fe0f}")
def emojiBelow2620(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2618}\u{fe0f}")
    case False{}: emojiBelow2619(hash,text,U32.is_lt(hash,3969919018))
def emojiBelow2621(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2618(hash,text,U32.is_lt(hash,3960749275))
    case False{}: emojiBelow2620(hash,text,U32.is_lt(hash,3967802946))
def emojiBelow2622(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2616(hash,text,U32.is_lt(hash,3957402229))
    case False{}: emojiBelow2621(hash,text,U32.is_lt(hash,3964293944))
def emojiBelow2623(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2612(hash,text,U32.is_lt(hash,3943125773))
    case False{}: emojiBelow2622(hash,text,U32.is_lt(hash,3958693385))
def emojiBelow2624(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2602(hash,text,U32.is_lt(hash,3906001100))
    case False{}: emojiBelow2623(hash,text,U32.is_lt(hash,3956193644))
def emojiBelow2625(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cc}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f9b0}")
def emojiBelow2626(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{265f}\u{fe0f}")
def emojiBelow2627(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2665}\u{fe0f}")
    case False{}: emojiBelow2626(hash,text,U32.is_lt(hash,3975278515))
def emojiBelow2628(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2625(hash,text,U32.is_lt(hash,3970936141))
    case False{}: emojiBelow2627(hash,text,U32.is_lt(hash,3974842783))
def emojiBelow2629(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow2630(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f647}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f473}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2631(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{1f373}")
    case False{}: emojiBelow2630(hash,text,U32.is_lt(hash,3990965355))
def emojiBelow2632(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2629(hash,text,U32.is_lt(hash,3978379944))
    case False{}: emojiBelow2631(hash,text,U32.is_lt(hash,3983915901))
def emojiBelow2633(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2628(hash,text,U32.is_lt(hash,3974200893))
    case False{}: emojiBelow2632(hash,text,U32.is_lt(hash,3981816996))
def emojiBelow2634(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow2635(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2198}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2636(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fd}")
    case False{}: emojiBelow2635(hash,text,U32.is_lt(hash,4000769446))
def emojiBelow2637(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2634(hash,text,U32.is_lt(hash,3994547631))
    case False{}: emojiBelow2636(hash,text,U32.is_lt(hash,3996224440))
def emojiBelow2638(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3ff}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fc}")
def emojiBelow2639(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow2638(hash,text,U32.is_lt(hash,4008398021))
def emojiBelow2640(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow2641(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
    case False{}: emojiBelow2640(hash,text,U32.is_lt(hash,4015638851))
def emojiBelow2642(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2639(hash,text,U32.is_lt(hash,4008177467))
    case False{}: emojiBelow2641(hash,text,U32.is_lt(hash,4011935182))
def emojiBelow2643(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2637(hash,text,U32.is_lt(hash,3995157563))
    case False{}: emojiBelow2642(hash,text,U32.is_lt(hash,4011325250))
def emojiBelow2644(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2633(hash,text,U32.is_lt(hash,3977770012))
    case False{}: emojiBelow2643(hash,text,U32.is_lt(hash,4006157419))
def emojiBelow2645(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f939}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2646(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f64d}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow2647(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f680}")
    case False{}: emojiBelow2646(hash,text,U32.is_lt(hash,4028102869))
def emojiBelow2648(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2645(hash,text,U32.is_lt(hash,4025753549))
    case False{}: emojiBelow2647(hash,text,U32.is_lt(hash,4027716228))
def emojiBelow2649(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f9db}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2650(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
    case False{}: emojiBelow2649(hash,text,U32.is_lt(hash,4037685518))
def emojiBelow2651(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
def emojiBelow2652(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: emojiBelow2651(hash,text,U32.is_lt(hash,4045328011))
def emojiBelow2653(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2650(hash,text,U32.is_lt(hash,4028712801))
    case False{}: emojiBelow2652(hash,text,U32.is_lt(hash,4043010960))
def emojiBelow2654(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2648(hash,text,U32.is_lt(hash,4026113365))
    case False{}: emojiBelow2653(hash,text,U32.is_lt(hash,4039712657))
def emojiBelow2655(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2708}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9b8}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2656(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f939}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f3eb}")
def emojiBelow2657(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2049}\u{fe0f}")
    case False{}: emojiBelow2656(hash,text,U32.is_lt(hash,4058173336))
def emojiBelow2658(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2655(hash,text,U32.is_lt(hash,4050599201))
    case False{}: emojiBelow2657(hash,text,U32.is_lt(hash,4055937431))
def emojiBelow2659(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow2660(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: emojiBelow2659(hash,text,U32.is_lt(hash,4062105630))
def emojiBelow2661(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3c3}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fd}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
def emojiBelow2662(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b5}\u{1f3fd}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2661(hash,text,U32.is_lt(hash,4076859121))
def emojiBelow2663(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2660(hash,text,U32.is_lt(hash,4060761345))
    case False{}: emojiBelow2662(hash,text,U32.is_lt(hash,4069518381))
def emojiBelow2664(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2658(hash,text,U32.is_lt(hash,4052864881))
    case False{}: emojiBelow2663(hash,text,U32.is_lt(hash,4069337809))
def emojiBelow2665(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2654(hash,text,U32.is_lt(hash,4028550392))
    case False{}: emojiBelow2664(hash,text,U32.is_lt(hash,4059788579))
def emojiBelow2666(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2644(hash,text,U32.is_lt(hash,3991620402))
    case False{}: emojiBelow2665(hash,text,U32.is_lt(hash,4045561198))
def emojiBelow2667(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2624(hash,text,U32.is_lt(hash,3930654487))
    case False{}: emojiBelow2666(hash,text,U32.is_lt(hash,4022935038))
def emojiBelow2668(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9b9}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
def emojiBelow2669(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f646}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3ff}")
def emojiBelow2670(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f487}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2669(hash,text,U32.is_lt(hash,4093343817))
def emojiBelow2671(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2668(hash,text,U32.is_lt(hash,4078883249))
    case False{}: emojiBelow2670(hash,text,U32.is_lt(hash,4087759442))
def emojiBelow2672(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6b4}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
def emojiBelow2673(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: Strings.equal(text,"\u{1f3cb}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2674(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fb}")
    case False{}: emojiBelow2673(hash,text,U32.is_lt(hash,4101840899))
def emojiBelow2675(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2672(hash,text,U32.is_lt(hash,4095660868))
    case False{}: emojiBelow2674(hash,text,U32.is_lt(hash,4096048331))
def emojiBelow2676(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2671(hash,text,U32.is_lt(hash,4079873458))
    case False{}: emojiBelow2675(hash,text,U32.is_lt(hash,4095823277))
def emojiBelow2677(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fe}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2695}\u{fe0f}")
def emojiBelow2678(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9ce}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f470}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2679(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f3cb}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2678(hash,text,U32.is_lt(hash,4103439285))
def emojiBelow2680(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2677(hash,text,U32.is_lt(hash,4102784992))
    case False{}: emojiBelow2679(hash,text,U32.is_lt(hash,4103211913))
def emojiBelow2681(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f692}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f48b}\u{200d}\u{1f9d1}\u{1f3fb}")
def emojiBelow2682(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f527}")
    case False{}: emojiBelow2681(hash,text,U32.is_lt(hash,4106823133))
def emojiBelow2683(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46e}\u{1f3ff}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f393}")
def emojiBelow2684(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f33e}")
    case False{}: emojiBelow2683(hash,text,U32.is_lt(hash,4120706141))
def emojiBelow2685(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2682(hash,text,U32.is_lt(hash,4104444362))
    case False{}: emojiBelow2684(hash,text,U32.is_lt(hash,4119896816))
def emojiBelow2686(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2680(hash,text,U32.is_lt(hash,4103174841))
    case False{}: emojiBelow2685(hash,text,U32.is_lt(hash,4114649551))
def emojiBelow2687(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2676(hash,text,U32.is_lt(hash,4095505813))
    case False{}: emojiBelow2686(hash,text,U32.is_lt(hash,4104426943))
def emojiBelow2688(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93d}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f5ef}\u{fe0f}")
def emojiBelow2689(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5c4}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f647}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2690(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{2695}\u{fe0f}")
    case False{}: emojiBelow2689(hash,text,U32.is_lt(hash,4143518026))
def emojiBelow2691(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2688(hash,text,U32.is_lt(hash,4135872195))
    case False{}: emojiBelow2690(hash,text,U32.is_lt(hash,4142840468))
def emojiBelow2692(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6a3}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f3ed}")
def emojiBelow2693(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9cf}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2692(hash,text,U32.is_lt(hash,4158839050))
def emojiBelow2694(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f575}\u{fe0f}")
def emojiBelow2695(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{1faef}\u{200d}\u{1f9d1}\u{1f3fb}")
    case False{}: emojiBelow2694(hash,text,U32.is_lt(hash,4167137741))
def emojiBelow2696(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2693(hash,text,U32.is_lt(hash,4149480936))
    case False{}: emojiBelow2695(hash,text,U32.is_lt(hash,4163158807))
def emojiBelow2697(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2691(hash,text,U32.is_lt(hash,4141327287))
    case False{}: emojiBelow2696(hash,text,U32.is_lt(hash,4160454293))
def emojiBelow2698(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f56f}\u{fe0f}")
def emojiBelow2699(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f54a}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f926}\u{1f3fb}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2700(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f3eb}")
    case False{}: emojiBelow2699(hash,text,U32.is_lt(hash,4173913362))
def emojiBelow2701(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2698(hash,text,U32.is_lt(hash,4168215363))
    case False{}: emojiBelow2700(hash,text,U32.is_lt(hash,4172497238))
def emojiBelow2702(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5a8}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fe}\u{200d}\u{1f33e}")
def emojiBelow2703(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f5b1}\u{fe0f}")
    case False{}: emojiBelow2702(hash,text,U32.is_lt(hash,4179313861))
def emojiBelow2704(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{2696}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow2705(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fc}")
    case False{}: emojiBelow2704(hash,text,U32.is_lt(hash,4184699955))
def emojiBelow2706(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2703(hash,text,U32.is_lt(hash,4178122312))
    case False{}: emojiBelow2705(hash,text,U32.is_lt(hash,4182319505))
def emojiBelow2707(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2701(hash,text,U32.is_lt(hash,4168595971))
    case False{}: emojiBelow2706(hash,text,U32.is_lt(hash,4179936426))
def emojiBelow2708(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2697(hash,text,U32.is_lt(hash,4143727794))
    case False{}: emojiBelow2707(hash,text,U32.is_lt(hash,4176779113))
def emojiBelow2709(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2687(hash,text,U32.is_lt(hash,4101928950))
    case False{}: emojiBelow2708(hash,text,U32.is_lt(hash,4167922336))
def emojiBelow2710(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fe}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f9d1}\u{1f3fd}")
    case False{}: Strings.equal(text,"\u{1f93c}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2711(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1f37c}")
def emojiBelow2712(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{200d}\u{1f469}\u{200d}\u{1f466}\u{200d}\u{1f466}")
    case False{}: emojiBelow2711(hash,text,U32.is_lt(hash,4202854508))
def emojiBelow2713(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2710(hash,text,U32.is_lt(hash,4197048100))
    case False{}: emojiBelow2712(hash,text,U32.is_lt(hash,4201477574))
def emojiBelow2714(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fc}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f487}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2715(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d7}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2714(hash,text,U32.is_lt(hash,4211022730))
def emojiBelow2716(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3ff}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow2717(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f486}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2716(hash,text,U32.is_lt(hash,4218255193))
def emojiBelow2718(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2715(hash,text,U32.is_lt(hash,4209631472))
    case False{}: emojiBelow2717(hash,text,U32.is_lt(hash,4218092784))
def emojiBelow2719(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2713(hash,text,U32.is_lt(hash,4197244457))
    case False{}: emojiBelow2718(hash,text,U32.is_lt(hash,4211167603))
def emojiBelow2720(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fc}")
    case False{}: Strings.equal(text,"\u{1f9d4}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
def emojiBelow2721(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f93c}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{26f9}\u{fe0f}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2722(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{200d}\u{1f9af}\u{200d}\u{27a1}\u{fe0f}")
    case False{}: emojiBelow2721(hash,text,U32.is_lt(hash,4229016057))
def emojiBelow2723(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2720(hash,text,U32.is_lt(hash,4227329884))
    case False{}: emojiBelow2722(hash,text,U32.is_lt(hash,4228076567))
def emojiBelow2724(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fd}")
def emojiBelow2725(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3fd}\u{200d}\u{2640}\u{fe0f}")
    case False{}: emojiBelow2724(hash,text,U32.is_lt(hash,4238031259))
def emojiBelow2726(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3ff}\u{200d}\u{1f680}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fc}\u{200d}\u{1f373}")
def emojiBelow2727(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{39}\u{fe0f}\u{20e3}")
    case False{}: emojiBelow2726(hash,text,U32.is_lt(hash,4243912908))
def emojiBelow2728(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2725(hash,text,U32.is_lt(hash,4234870403))
    case False{}: emojiBelow2727(hash,text,U32.is_lt(hash,4240719229))
def emojiBelow2729(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2723(hash,text,U32.is_lt(hash,4227545531))
    case False{}: emojiBelow2728(hash,text,U32.is_lt(hash,4240413606))
def emojiBelow2730(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2719(hash,text,U32.is_lt(hash,4206158134))
    case False{}: emojiBelow2729(hash,text,U32.is_lt(hash,4232367817))
def emojiBelow2731(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f937}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fd}")
def emojiBelow2732(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3fe}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fd}\u{200d}\u{1f52c}")
def emojiBelow2733(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{200d}\u{1f91d}\u{200d}\u{1f9d1}")
    case False{}: emojiBelow2732(hash,text,U32.is_lt(hash,4255425514))
def emojiBelow2734(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2731(hash,text,U32.is_lt(hash,4251648022))
    case False{}: emojiBelow2733(hash,text,U32.is_lt(hash,4254808878))
def emojiBelow2735(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9db}\u{1f3fe}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f9da}\u{1f3fe}\u{200d}\u{2640}\u{fe0f}")
def emojiBelow2736(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d6}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: emojiBelow2735(hash,text,U32.is_lt(hash,4266570801))
def emojiBelow2737(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3fb}\u{200d}\u{1fa70}")
    case False{}: Strings.equal(text,"\u{1f9d1}\u{1f3fd}\u{200d}\u{1f3ed}")
def emojiBelow2738(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fc}")
    case False{}: emojiBelow2737(hash,text,U32.is_lt(hash,4269261685))
def emojiBelow2739(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2736(hash,text,U32.is_lt(hash,4259207268))
    case False{}: emojiBelow2738(hash,text,U32.is_lt(hash,4269036472))
def emojiBelow2740(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2734(hash,text,U32.is_lt(hash,4252402476))
    case False{}: emojiBelow2739(hash,text,U32.is_lt(hash,4268425641))
def emojiBelow2741(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f477}\u{1f3fc}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f468}\u{1f3fb}\u{200d}\u{1f430}\u{200d}\u{1f468}\u{1f3ff}")
def emojiBelow2742(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f46f}\u{1f3fb}\u{200d}\u{2640}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f636}\u{200d}\u{1f32b}\u{fe0f}")
def emojiBelow2743(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9d1}\u{1f3ff}\u{200d}\u{1f384}")
    case False{}: emojiBelow2742(hash,text,U32.is_lt(hash,4273700254))
def emojiBelow2744(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2741(hash,text,U32.is_lt(hash,4271586497))
    case False{}: emojiBelow2743(hash,text,U32.is_lt(hash,4273223415))
def emojiBelow2745(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f9dc}\u{1f3ff}\u{200d}\u{2642}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f469}\u{1f3fb}")
def emojiBelow2746(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f6cd}\u{fe0f}")
    case False{}: emojiBelow2745(hash,text,U32.is_lt(hash,4285203260))
def emojiBelow2747(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{2b06}\u{fe0f}")
    case False{}: Strings.equal(text,"\u{2a}\u{fe0f}\u{20e3}")
def emojiBelow2748(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: Strings.equal(text,"\u{1f469}\u{1f3fb}\u{200d}\u{2764}\u{fe0f}\u{200d}\u{1f468}\u{1f3fb}")
    case False{}: emojiBelow2747(hash,text,U32.is_lt(hash,4294670543))
def emojiBelow2749(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2746(hash,text,U32.is_lt(hash,4283585988))
    case False{}: emojiBelow2748(hash,text,U32.is_lt(hash,4290707778))
def emojiBelow2750(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2744(hash,text,U32.is_lt(hash,4271704712))
    case False{}: emojiBelow2749(hash,text,U32.is_lt(hash,4285365669))
def emojiBelow2751(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2740(hash,text,U32.is_lt(hash,4257335686))
    case False{}: emojiBelow2750(hash,text,U32.is_lt(hash,4281083573))
def emojiBelow2752(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2730(hash,text,U32.is_lt(hash,4221253640))
    case False{}: emojiBelow2751(hash,text,U32.is_lt(hash,4270840506))
def emojiBelow2753(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2709(hash,text,U32.is_lt(hash,4134332696))
    case False{}: emojiBelow2752(hash,text,U32.is_lt(hash,4250270888))
def emojiBelow2754(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2667(hash,text,U32.is_lt(hash,3970326768))
    case False{}: emojiBelow2753(hash,text,U32.is_lt(hash,4196714045))
def emojiBelow2755(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2582(hash,text,U32.is_lt(hash,3747583950))
    case False{}: emojiBelow2754(hash,text,U32.is_lt(hash,4078324332))
def emojiBelow2756(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2411(hash,text,U32.is_lt(hash,3509317459))
    case False{}: emojiBelow2755(hash,text,U32.is_lt(hash,3891314679))
def emojiBelow2757(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow2067(hash,text,U32.is_lt(hash,3280097630))
    case False{}: emojiBelow2756(hash,text,U32.is_lt(hash,3642995204))
def emojiBelow2758(+hash: U32,+text: String,below: Bool) -> Bool:
  match below:
    case True{}: emojiBelow1378(hash,text,U32.is_lt(hash,1590611473))
    case False{}: emojiBelow2757(hash,text,U32.is_lt(hash,3422180546))

def emojiCandidates(+hash: U32,+text: String) -> Bool:
  emojiBelow2758(hash,text,U32.is_lt(hash,3118199091))

def rgiTail(+char: Char,rest: String) -> Bool:
  match rest:
    case SNil{}: U32.is_ne(U32.and(properties(Char.to_u32(char)),64),0)
    case +rest: Bool.pick(Unit -> Bool,U32.is_ne(U32.and(properties(Char.to_u32(char)),128),0),_ => emojiCandidates(emojiHash(SCon{char,rest},2166136261),SCon{char,rest}),_ => False{})(Unit{})
def isRgiEmoji(text: String) -> Bool:
  match text:
    case SNil{}: False{}
    case SCon{char,rest}: rgiTail(char,rest)
