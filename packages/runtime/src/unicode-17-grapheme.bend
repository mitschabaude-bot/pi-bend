# Generated by scripts/generate-grapheme.py; Unicode17.0.0, Unicode License V3.
# See unicode-LICENSE.txt. Low4 bits GCB, bit4 Extended_Pictographic, bits5..6 InCB.
# Balanced decisions (plain definitions) allocate no property table per input string.
import Base

# 1363 property runs outside ASCII and algorithmic Hangul.
def lookupBelow0(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def lookupBelow1(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def lookupBelow2(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1(code,U32.is_lt(code,173))
def lookupBelow3(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow0(code,U32.is_lt(code,160))
    case False{}: lookupBelow2(code,U32.is_lt(code,170))
def lookupBelow4(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow5(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow6(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow5(code,U32.is_lt(code,1155))
def lookupBelow7(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow4(code,U32.is_lt(code,175))
    case False{}: lookupBelow6(code,U32.is_lt(code,880))
def lookupBelow8(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow3(code,U32.is_lt(code,169))
    case False{}: lookupBelow7(code,U32.is_lt(code,768))
def lookupBelow9(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow10(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow11(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow10(code,U32.is_lt(code,1472))
def lookupBelow12(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow9(code,U32.is_lt(code,1425))
    case False{}: lookupBelow11(code,U32.is_lt(code,1471))
def lookupBelow13(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow14(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow13(code,U32.is_lt(code,1476))
def lookupBelow15(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow16(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow15(code,U32.is_lt(code,1480))
def lookupBelow17(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow14(code,U32.is_lt(code,1475))
    case False{}: lookupBelow16(code,U32.is_lt(code,1479))
def lookupBelow18(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow12(code,U32.is_lt(code,1470))
    case False{}: lookupBelow17(code,U32.is_lt(code,1478))
def lookupBelow19(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow8(code,U32.is_lt(code,174))
    case False{}: lookupBelow18(code,U32.is_lt(code,1473))
def lookupBelow20(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: 0
def lookupBelow21(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def lookupBelow22(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow21(code,U32.is_lt(code,1564))
def lookupBelow23(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow20(code,U32.is_lt(code,1542))
    case False{}: lookupBelow22(code,U32.is_lt(code,1563))
def lookupBelow24(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow25(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow26(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow25(code,U32.is_lt(code,1649))
def lookupBelow27(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow24(code,U32.is_lt(code,1611))
    case False{}: lookupBelow26(code,U32.is_lt(code,1648))
def lookupBelow28(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow23(code,U32.is_lt(code,1552))
    case False{}: lookupBelow27(code,U32.is_lt(code,1632))
def lookupBelow29(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 7
def lookupBelow30(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow31(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow30(code,U32.is_lt(code,1765))
def lookupBelow32(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow29(code,U32.is_lt(code,1757))
    case False{}: lookupBelow31(code,U32.is_lt(code,1759))
def lookupBelow33(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow34(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow33(code,U32.is_lt(code,1770))
def lookupBelow35(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: 0
def lookupBelow36(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow35(code,U32.is_lt(code,1808))
def lookupBelow37(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow34(code,U32.is_lt(code,1769))
    case False{}: lookupBelow36(code,U32.is_lt(code,1807))
def lookupBelow38(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow32(code,U32.is_lt(code,1758))
    case False{}: lookupBelow37(code,U32.is_lt(code,1774))
def lookupBelow39(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow28(code,U32.is_lt(code,1565))
    case False{}: lookupBelow38(code,U32.is_lt(code,1767))
def lookupBelow40(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow19(code,U32.is_lt(code,1162))
    case False{}: lookupBelow39(code,U32.is_lt(code,1750))
def lookupBelow41(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow42(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow43(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow42(code,U32.is_lt(code,1958))
def lookupBelow44(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow41(code,U32.is_lt(code,1810))
    case False{}: lookupBelow43(code,U32.is_lt(code,1867))
def lookupBelow45(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow46(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow47(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow46(code,U32.is_lt(code,2046))
def lookupBelow48(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow45(code,U32.is_lt(code,2027))
    case False{}: lookupBelow47(code,U32.is_lt(code,2045))
def lookupBelow49(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow44(code,U32.is_lt(code,1840))
    case False{}: lookupBelow48(code,U32.is_lt(code,2036))
def lookupBelow50(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow51(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow52(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow51(code,U32.is_lt(code,2085))
def lookupBelow53(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow50(code,U32.is_lt(code,2074))
    case False{}: lookupBelow52(code,U32.is_lt(code,2084))
def lookupBelow54(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow55(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow54(code,U32.is_lt(code,2094))
def lookupBelow56(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 7
def lookupBelow57(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow56(code,U32.is_lt(code,2192))
def lookupBelow58(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow55(code,U32.is_lt(code,2089))
    case False{}: lookupBelow57(code,U32.is_lt(code,2140))
def lookupBelow59(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow53(code,U32.is_lt(code,2075))
    case False{}: lookupBelow58(code,U32.is_lt(code,2137))
def lookupBelow60(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow49(code,U32.is_lt(code,1969))
    case False{}: lookupBelow59(code,U32.is_lt(code,2088))
def lookupBelow61(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow62(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 7
def lookupBelow63(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow62(code,U32.is_lt(code,2274))
def lookupBelow64(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow61(code,U32.is_lt(code,2199))
    case False{}: lookupBelow63(code,U32.is_lt(code,2250))
def lookupBelow65(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow66(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow65(code,U32.is_lt(code,2308))
def lookupBelow67(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow68(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow67(code,U32.is_lt(code,2363))
def lookupBelow69(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow66(code,U32.is_lt(code,2307))
    case False{}: lookupBelow68(code,U32.is_lt(code,2362))
def lookupBelow70(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow64(code,U32.is_lt(code,2208))
    case False{}: lookupBelow69(code,U32.is_lt(code,2325))
def lookupBelow71(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow72(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow73(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow72(code,U32.is_lt(code,2377))
def lookupBelow74(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow71(code,U32.is_lt(code,2365))
    case False{}: lookupBelow73(code,U32.is_lt(code,2369))
def lookupBelow75(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow76(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow75(code,U32.is_lt(code,2384))
def lookupBelow77(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow78(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow77(code,U32.is_lt(code,2400))
def lookupBelow79(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow76(code,U32.is_lt(code,2382))
    case False{}: lookupBelow78(code,U32.is_lt(code,2392))
def lookupBelow80(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow74(code,U32.is_lt(code,2366))
    case False{}: lookupBelow79(code,U32.is_lt(code,2385))
def lookupBelow81(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow70(code,U32.is_lt(code,2275))
    case False{}: lookupBelow80(code,U32.is_lt(code,2381))
def lookupBelow82(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow60(code,U32.is_lt(code,2070))
    case False{}: lookupBelow81(code,U32.is_lt(code,2364))
def lookupBelow83(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow40(code,U32.is_lt(code,1536))
    case False{}: lookupBelow82(code,U32.is_lt(code,2194))
def lookupBelow84(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow85(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow86(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow85(code,U32.is_lt(code,2433))
def lookupBelow87(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow84(code,U32.is_lt(code,2404))
    case False{}: lookupBelow86(code,U32.is_lt(code,2432))
def lookupBelow88(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow89(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow90(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow89(code,U32.is_lt(code,2474))
def lookupBelow91(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow88(code,U32.is_lt(code,2436))
    case False{}: lookupBelow90(code,U32.is_lt(code,2473))
def lookupBelow92(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow87(code,U32.is_lt(code,2424))
    case False{}: lookupBelow91(code,U32.is_lt(code,2453))
def lookupBelow93(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow94(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow95(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow94(code,U32.is_lt(code,2490))
def lookupBelow96(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow93(code,U32.is_lt(code,2482))
    case False{}: lookupBelow95(code,U32.is_lt(code,2486))
def lookupBelow97(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow98(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow97(code,U32.is_lt(code,2494))
def lookupBelow99(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow99(code,U32.is_lt(code,2501))
def lookupBelow101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow98(code,U32.is_lt(code,2493))
    case False{}: lookupBelow100(code,U32.is_lt(code,2497))
def lookupBelow102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow96(code,U32.is_lt(code,2483))
    case False{}: lookupBelow101(code,U32.is_lt(code,2495))
def lookupBelow103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow92(code,U32.is_lt(code,2434))
    case False{}: lookupBelow102(code,U32.is_lt(code,2492))
def lookupBelow104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: 0
def lookupBelow106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow105(code,U32.is_lt(code,2510))
def lookupBelow107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow104(code,U32.is_lt(code,2505))
    case False{}: lookupBelow106(code,U32.is_lt(code,2509))
def lookupBelow108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow109(code,U32.is_lt(code,2527))
def lookupBelow111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow108(code,U32.is_lt(code,2520))
    case False{}: lookupBelow110(code,U32.is_lt(code,2526))
def lookupBelow112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow107(code,U32.is_lt(code,2507))
    case False{}: lookupBelow111(code,U32.is_lt(code,2524))
def lookupBelow113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow114(code,U32.is_lt(code,2546))
def lookupBelow116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow113(code,U32.is_lt(code,2530))
    case False{}: lookupBelow115(code,U32.is_lt(code,2544))
def lookupBelow117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow117(code,U32.is_lt(code,2561))
def lookupBelow119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow119(code,U32.is_lt(code,2620))
def lookupBelow121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow118(code,U32.is_lt(code,2559))
    case False{}: lookupBelow120(code,U32.is_lt(code,2564))
def lookupBelow122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow116(code,U32.is_lt(code,2532))
    case False{}: lookupBelow121(code,U32.is_lt(code,2563))
def lookupBelow123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow112(code,U32.is_lt(code,2519))
    case False{}: lookupBelow122(code,U32.is_lt(code,2558))
def lookupBelow124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow103(code,U32.is_lt(code,2481))
    case False{}: lookupBelow123(code,U32.is_lt(code,2528))
def lookupBelow125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow126(code,U32.is_lt(code,2631))
def lookupBelow128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow125(code,U32.is_lt(code,2622))
    case False{}: lookupBelow127(code,U32.is_lt(code,2627))
def lookupBelow129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow130(code,U32.is_lt(code,2642))
def lookupBelow132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow129(code,U32.is_lt(code,2635))
    case False{}: lookupBelow131(code,U32.is_lt(code,2641))
def lookupBelow133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow128(code,U32.is_lt(code,2625))
    case False{}: lookupBelow132(code,U32.is_lt(code,2638))
def lookupBelow134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow135(code,U32.is_lt(code,2689))
def lookupBelow137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow134(code,U32.is_lt(code,2674))
    case False{}: lookupBelow136(code,U32.is_lt(code,2678))
def lookupBelow138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow138(code,U32.is_lt(code,2709))
def lookupBelow140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow140(code,U32.is_lt(code,2737))
def lookupBelow142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow139(code,U32.is_lt(code,2692))
    case False{}: lookupBelow141(code,U32.is_lt(code,2730))
def lookupBelow143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow137(code,U32.is_lt(code,2677))
    case False{}: lookupBelow142(code,U32.is_lt(code,2729))
def lookupBelow144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow133(code,U32.is_lt(code,2633))
    case False{}: lookupBelow143(code,U32.is_lt(code,2691))
def lookupBelow145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow146(code,U32.is_lt(code,2748))
def lookupBelow148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow145(code,U32.is_lt(code,2740))
    case False{}: lookupBelow147(code,U32.is_lt(code,2746))
def lookupBelow149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow149(code,U32.is_lt(code,2753))
def lookupBelow151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow151(code,U32.is_lt(code,2761))
def lookupBelow153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow150(code,U32.is_lt(code,2750))
    case False{}: lookupBelow152(code,U32.is_lt(code,2759))
def lookupBelow154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow148(code,U32.is_lt(code,2741))
    case False{}: lookupBelow153(code,U32.is_lt(code,2758))
def lookupBelow155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow156(code,U32.is_lt(code,2786))
def lookupBelow158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow155(code,U32.is_lt(code,2763))
    case False{}: lookupBelow157(code,U32.is_lt(code,2766))
def lookupBelow159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 68
def lookupBelow160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow159(code,U32.is_lt(code,2810))
def lookupBelow161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow161(code,U32.is_lt(code,2818))
def lookupBelow163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow160(code,U32.is_lt(code,2809))
    case False{}: lookupBelow162(code,U32.is_lt(code,2817))
def lookupBelow164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow158(code,U32.is_lt(code,2765))
    case False{}: lookupBelow163(code,U32.is_lt(code,2816))
def lookupBelow165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow154(code,U32.is_lt(code,2749))
    case False{}: lookupBelow164(code,U32.is_lt(code,2788))
def lookupBelow166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow144(code,U32.is_lt(code,2672))
    case False{}: lookupBelow165(code,U32.is_lt(code,2762))
def lookupBelow167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow124(code,U32.is_lt(code,2503))
    case False{}: lookupBelow166(code,U32.is_lt(code,2738))
def lookupBelow168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow83(code,U32.is_lt(code,1809))
    case False{}: lookupBelow167(code,U32.is_lt(code,2621))
def lookupBelow169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow170(code,U32.is_lt(code,2865))
def lookupBelow172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow169(code,U32.is_lt(code,2837))
    case False{}: lookupBelow171(code,U32.is_lt(code,2858))
def lookupBelow173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow174(code,U32.is_lt(code,2876))
def lookupBelow176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow173(code,U32.is_lt(code,2868))
    case False{}: lookupBelow175(code,U32.is_lt(code,2874))
def lookupBelow177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow172(code,U32.is_lt(code,2857))
    case False{}: lookupBelow176(code,U32.is_lt(code,2869))
def lookupBelow178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow179(code,U32.is_lt(code,2885))
def lookupBelow181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow178(code,U32.is_lt(code,2878))
    case False{}: lookupBelow180(code,U32.is_lt(code,2881))
def lookupBelow182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow182(code,U32.is_lt(code,2891))
def lookupBelow184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow184(code,U32.is_lt(code,2901))
def lookupBelow186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow183(code,U32.is_lt(code,2889))
    case False{}: lookupBelow185(code,U32.is_lt(code,2894))
def lookupBelow187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow181(code,U32.is_lt(code,2880))
    case False{}: lookupBelow186(code,U32.is_lt(code,2893))
def lookupBelow188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow177(code,U32.is_lt(code,2866))
    case False{}: lookupBelow187(code,U32.is_lt(code,2887))
def lookupBelow189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow190(code,U32.is_lt(code,2912))
def lookupBelow192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow189(code,U32.is_lt(code,2908))
    case False{}: lookupBelow191(code,U32.is_lt(code,2911))
def lookupBelow193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow194(code,U32.is_lt(code,2946))
def lookupBelow196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow193(code,U32.is_lt(code,2916))
    case False{}: lookupBelow195(code,U32.is_lt(code,2930))
def lookupBelow197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow192(code,U32.is_lt(code,2910))
    case False{}: lookupBelow196(code,U32.is_lt(code,2929))
def lookupBelow198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow199(code,U32.is_lt(code,3009))
def lookupBelow201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow198(code,U32.is_lt(code,3006))
    case False{}: lookupBelow200(code,U32.is_lt(code,3008))
def lookupBelow202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow202(code,U32.is_lt(code,3017))
def lookupBelow204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow204(code,U32.is_lt(code,3022))
def lookupBelow206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow203(code,U32.is_lt(code,3014))
    case False{}: lookupBelow205(code,U32.is_lt(code,3021))
def lookupBelow207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow201(code,U32.is_lt(code,3007))
    case False{}: lookupBelow206(code,U32.is_lt(code,3018))
def lookupBelow208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow197(code,U32.is_lt(code,2914))
    case False{}: lookupBelow207(code,U32.is_lt(code,3011))
def lookupBelow209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow188(code,U32.is_lt(code,2877))
    case False{}: lookupBelow208(code,U32.is_lt(code,2947))
def lookupBelow210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow211(code,U32.is_lt(code,3076))
def lookupBelow213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow210(code,U32.is_lt(code,3032))
    case False{}: lookupBelow212(code,U32.is_lt(code,3073))
def lookupBelow214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow215(code,U32.is_lt(code,3130))
def lookupBelow217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow214(code,U32.is_lt(code,3093))
    case False{}: lookupBelow216(code,U32.is_lt(code,3114))
def lookupBelow218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow213(code,U32.is_lt(code,3072))
    case False{}: lookupBelow217(code,U32.is_lt(code,3113))
def lookupBelow219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow220(code,U32.is_lt(code,3141))
def lookupBelow222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow219(code,U32.is_lt(code,3133))
    case False{}: lookupBelow221(code,U32.is_lt(code,3137))
def lookupBelow223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow223(code,U32.is_lt(code,3146))
def lookupBelow225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow225(code,U32.is_lt(code,3157))
def lookupBelow227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow224(code,U32.is_lt(code,3145))
    case False{}: lookupBelow226(code,U32.is_lt(code,3150))
def lookupBelow228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow222(code,U32.is_lt(code,3134))
    case False{}: lookupBelow227(code,U32.is_lt(code,3149))
def lookupBelow229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow218(code,U32.is_lt(code,3077))
    case False{}: lookupBelow228(code,U32.is_lt(code,3142))
def lookupBelow230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow231(code,U32.is_lt(code,3172))
def lookupBelow233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow230(code,U32.is_lt(code,3160))
    case False{}: lookupBelow232(code,U32.is_lt(code,3170))
def lookupBelow234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow234(code,U32.is_lt(code,3204))
def lookupBelow236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow236(code,U32.is_lt(code,3262))
def lookupBelow238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow235(code,U32.is_lt(code,3202))
    case False{}: lookupBelow237(code,U32.is_lt(code,3261))
def lookupBelow239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow233(code,U32.is_lt(code,3163))
    case False{}: lookupBelow238(code,U32.is_lt(code,3260))
def lookupBelow240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow241(code,U32.is_lt(code,3269))
def lookupBelow243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow240(code,U32.is_lt(code,3265))
    case False{}: lookupBelow242(code,U32.is_lt(code,3267))
def lookupBelow244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow244(code,U32.is_lt(code,3274))
def lookupBelow246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow246(code,U32.is_lt(code,3287))
def lookupBelow248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow245(code,U32.is_lt(code,3273))
    case False{}: lookupBelow247(code,U32.is_lt(code,3285))
def lookupBelow249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow243(code,U32.is_lt(code,3266))
    case False{}: lookupBelow248(code,U32.is_lt(code,3278))
def lookupBelow250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow239(code,U32.is_lt(code,3201))
    case False{}: lookupBelow249(code,U32.is_lt(code,3270))
def lookupBelow251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow229(code,U32.is_lt(code,3132))
    case False{}: lookupBelow250(code,U32.is_lt(code,3263))
def lookupBelow252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow209(code,U32.is_lt(code,2904))
    case False{}: lookupBelow251(code,U32.is_lt(code,3159))
def lookupBelow253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow254(code,U32.is_lt(code,3328))
def lookupBelow256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow253(code,U32.is_lt(code,3300))
    case False{}: lookupBelow255(code,U32.is_lt(code,3316))
def lookupBelow257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow258(code,U32.is_lt(code,3389))
def lookupBelow260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow257(code,U32.is_lt(code,3332))
    case False{}: lookupBelow259(code,U32.is_lt(code,3387))
def lookupBelow261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow256(code,U32.is_lt(code,3315))
    case False{}: lookupBelow260(code,U32.is_lt(code,3349))
def lookupBelow262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow263(code,U32.is_lt(code,3398))
def lookupBelow265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow262(code,U32.is_lt(code,3391))
    case False{}: lookupBelow264(code,U32.is_lt(code,3397))
def lookupBelow266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 100
def lookupBelow267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow266(code,U32.is_lt(code,3405))
def lookupBelow268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: lookupBelow268(code,U32.is_lt(code,3415))
def lookupBelow270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow267(code,U32.is_lt(code,3402))
    case False{}: lookupBelow269(code,U32.is_lt(code,3407))
def lookupBelow271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow265(code,U32.is_lt(code,3393))
    case False{}: lookupBelow270(code,U32.is_lt(code,3406))
def lookupBelow272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow261(code,U32.is_lt(code,3330))
    case False{}: lookupBelow271(code,U32.is_lt(code,3401))
def lookupBelow273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow274(code,U32.is_lt(code,3458))
def lookupBelow276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow273(code,U32.is_lt(code,3426))
    case False{}: lookupBelow275(code,U32.is_lt(code,3457))
def lookupBelow277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow278(code,U32.is_lt(code,3536))
def lookupBelow280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow277(code,U32.is_lt(code,3530))
    case False{}: lookupBelow279(code,U32.is_lt(code,3535))
def lookupBelow281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow276(code,U32.is_lt(code,3428))
    case False{}: lookupBelow280(code,U32.is_lt(code,3531))
def lookupBelow282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow283(code,U32.is_lt(code,3544))
def lookupBelow285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow282(code,U32.is_lt(code,3541))
    case False{}: lookupBelow284(code,U32.is_lt(code,3543))
def lookupBelow286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow286(code,U32.is_lt(code,3570))
def lookupBelow288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow288(code,U32.is_lt(code,3634))
def lookupBelow290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow287(code,U32.is_lt(code,3552))
    case False{}: lookupBelow289(code,U32.is_lt(code,3633))
def lookupBelow291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow285(code,U32.is_lt(code,3542))
    case False{}: lookupBelow290(code,U32.is_lt(code,3572))
def lookupBelow292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow281(code,U32.is_lt(code,3460))
    case False{}: lookupBelow291(code,U32.is_lt(code,3551))
def lookupBelow293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow272(code,U32.is_lt(code,3390))
    case False{}: lookupBelow292(code,U32.is_lt(code,3538))
def lookupBelow294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow295(code,U32.is_lt(code,3663))
def lookupBelow297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow294(code,U32.is_lt(code,3636))
    case False{}: lookupBelow296(code,U32.is_lt(code,3655))
def lookupBelow298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow299(code,U32.is_lt(code,3773))
def lookupBelow301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow298(code,U32.is_lt(code,3762))
    case False{}: lookupBelow300(code,U32.is_lt(code,3764))
def lookupBelow302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow297(code,U32.is_lt(code,3643))
    case False{}: lookupBelow301(code,U32.is_lt(code,3763))
def lookupBelow303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow304(code,U32.is_lt(code,3893))
def lookupBelow306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow303(code,U32.is_lt(code,3791))
    case False{}: lookupBelow305(code,U32.is_lt(code,3866))
def lookupBelow307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow307(code,U32.is_lt(code,3896))
def lookupBelow309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow309(code,U32.is_lt(code,3902))
def lookupBelow311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow308(code,U32.is_lt(code,3895))
    case False{}: lookupBelow310(code,U32.is_lt(code,3898))
def lookupBelow312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow306(code,U32.is_lt(code,3864))
    case False{}: lookupBelow311(code,U32.is_lt(code,3897))
def lookupBelow313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow302(code,U32.is_lt(code,3761))
    case False{}: lookupBelow312(code,U32.is_lt(code,3894))
def lookupBelow314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow315(code,U32.is_lt(code,3973))
def lookupBelow317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow314(code,U32.is_lt(code,3953))
    case False{}: lookupBelow316(code,U32.is_lt(code,3968))
def lookupBelow318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow318(code,U32.is_lt(code,3981))
def lookupBelow320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow320(code,U32.is_lt(code,4029))
def lookupBelow322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow319(code,U32.is_lt(code,3976))
    case False{}: lookupBelow321(code,U32.is_lt(code,3993))
def lookupBelow323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow317(code,U32.is_lt(code,3967))
    case False{}: lookupBelow322(code,U32.is_lt(code,3992))
def lookupBelow324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow325(code,U32.is_lt(code,4141))
def lookupBelow327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow324(code,U32.is_lt(code,4039))
    case False{}: lookupBelow326(code,U32.is_lt(code,4139))
def lookupBelow328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow328(code,U32.is_lt(code,4152))
def lookupBelow330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow330(code,U32.is_lt(code,4155))
def lookupBelow332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow329(code,U32.is_lt(code,4146))
    case False{}: lookupBelow331(code,U32.is_lt(code,4154))
def lookupBelow333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow327(code,U32.is_lt(code,4096))
    case False{}: lookupBelow332(code,U32.is_lt(code,4153))
def lookupBelow334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow323(code,U32.is_lt(code,3974))
    case False{}: lookupBelow333(code,U32.is_lt(code,4145))
def lookupBelow335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow313(code,U32.is_lt(code,3784))
    case False{}: lookupBelow334(code,U32.is_lt(code,4038))
def lookupBelow336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow293(code,U32.is_lt(code,3416))
    case False{}: lookupBelow335(code,U32.is_lt(code,3904))
def lookupBelow337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow252(code,U32.is_lt(code,3031))
    case False{}: lookupBelow336(code,U32.is_lt(code,3635))
def lookupBelow338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow168(code,U32.is_lt(code,2402))
    case False{}: lookupBelow337(code,U32.is_lt(code,3298))
def lookupBelow339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 32
def lookupBelow340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 8
def lookupBelow341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow340(code,U32.is_lt(code,4182))
def lookupBelow342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow339(code,U32.is_lt(code,4159))
    case False{}: lookupBelow341(code,U32.is_lt(code,4176))
def lookupBelow343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 32
def lookupBelow344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow344(code,U32.is_lt(code,4194))
def lookupBelow346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow343(code,U32.is_lt(code,4186))
    case False{}: lookupBelow345(code,U32.is_lt(code,4193))
def lookupBelow347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow342(code,U32.is_lt(code,4160))
    case False{}: lookupBelow346(code,U32.is_lt(code,4190))
def lookupBelow348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 32
def lookupBelow350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow349(code,U32.is_lt(code,4213))
def lookupBelow351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow348(code,U32.is_lt(code,4199))
    case False{}: lookupBelow350(code,U32.is_lt(code,4209))
def lookupBelow352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow352(code,U32.is_lt(code,4228))
def lookupBelow354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow354(code,U32.is_lt(code,4237))
def lookupBelow356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow353(code,U32.is_lt(code,4227))
    case False{}: lookupBelow355(code,U32.is_lt(code,4231))
def lookupBelow357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow351(code,U32.is_lt(code,4206))
    case False{}: lookupBelow356(code,U32.is_lt(code,4229))
def lookupBelow358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow347(code,U32.is_lt(code,4184))
    case False{}: lookupBelow357(code,U32.is_lt(code,4226))
def lookupBelow359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 9
def lookupBelow361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow360(code,U32.is_lt(code,4352))
def lookupBelow362(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow359(code,U32.is_lt(code,4239))
    case False{}: lookupBelow361(code,U32.is_lt(code,4254))
def lookupBelow363(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 10
    case False{}: 11
def lookupBelow364(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow365(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow364(code,U32.is_lt(code,4960))
def lookupBelow366(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow363(code,U32.is_lt(code,4520))
    case False{}: lookupBelow365(code,U32.is_lt(code,4957))
def lookupBelow367(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow362(code,U32.is_lt(code,4253))
    case False{}: lookupBelow366(code,U32.is_lt(code,4608))
def lookupBelow368(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow369(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow370(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow369(code,U32.is_lt(code,5970))
def lookupBelow371(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow368(code,U32.is_lt(code,5910))
    case False{}: lookupBelow370(code,U32.is_lt(code,5941))
def lookupBelow372(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow373(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow372(code,U32.is_lt(code,6004))
def lookupBelow374(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow375(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow374(code,U32.is_lt(code,6070))
def lookupBelow376(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow373(code,U32.is_lt(code,6002))
    case False{}: lookupBelow375(code,U32.is_lt(code,6068))
def lookupBelow377(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow371(code,U32.is_lt(code,5938))
    case False{}: lookupBelow376(code,U32.is_lt(code,6016))
def lookupBelow378(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow367(code,U32.is_lt(code,4448))
    case False{}: lookupBelow377(code,U32.is_lt(code,5972))
def lookupBelow379(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow358(code,U32.is_lt(code,4197))
    case False{}: lookupBelow378(code,U32.is_lt(code,5906))
def lookupBelow380(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow381(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow382(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow381(code,U32.is_lt(code,6089))
def lookupBelow383(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow380(code,U32.is_lt(code,6078))
    case False{}: lookupBelow382(code,U32.is_lt(code,6087))
def lookupBelow384(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: 68
def lookupBelow385(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow386(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow385(code,U32.is_lt(code,6110))
def lookupBelow387(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow384(code,U32.is_lt(code,6099))
    case False{}: lookupBelow386(code,U32.is_lt(code,6109))
def lookupBelow388(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow383(code,U32.is_lt(code,6086))
    case False{}: lookupBelow387(code,U32.is_lt(code,6100))
def lookupBelow389(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 3
def lookupBelow390(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow391(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow390(code,U32.is_lt(code,6277))
def lookupBelow392(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow389(code,U32.is_lt(code,6158))
    case False{}: lookupBelow391(code,U32.is_lt(code,6160))
def lookupBelow393(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow394(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow393(code,U32.is_lt(code,6314))
def lookupBelow395(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow396(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow395(code,U32.is_lt(code,6439))
def lookupBelow397(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow394(code,U32.is_lt(code,6313))
    case False{}: lookupBelow396(code,U32.is_lt(code,6435))
def lookupBelow398(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow392(code,U32.is_lt(code,6159))
    case False{}: lookupBelow397(code,U32.is_lt(code,6432))
def lookupBelow399(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow388(code,U32.is_lt(code,6098))
    case False{}: lookupBelow398(code,U32.is_lt(code,6279))
def lookupBelow400(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow401(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow402(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow401(code,U32.is_lt(code,6451))
def lookupBelow403(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow400(code,U32.is_lt(code,6444))
    case False{}: lookupBelow402(code,U32.is_lt(code,6450))
def lookupBelow404(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow405(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow404(code,U32.is_lt(code,6679))
def lookupBelow406(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow407(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow406(code,U32.is_lt(code,6684))
def lookupBelow408(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow405(code,U32.is_lt(code,6460))
    case False{}: lookupBelow407(code,U32.is_lt(code,6683))
def lookupBelow409(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow403(code,U32.is_lt(code,6448))
    case False{}: lookupBelow408(code,U32.is_lt(code,6681))
def lookupBelow410(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 8
def lookupBelow411(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow412(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow411(code,U32.is_lt(code,6744))
def lookupBelow413(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow410(code,U32.is_lt(code,6741))
    case False{}: lookupBelow412(code,U32.is_lt(code,6743))
def lookupBelow414(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: 0
def lookupBelow415(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow414(code,U32.is_lt(code,6753))
def lookupBelow416(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow417(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow416(code,U32.is_lt(code,6757))
def lookupBelow418(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow415(code,U32.is_lt(code,6752))
    case False{}: lookupBelow417(code,U32.is_lt(code,6755))
def lookupBelow419(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow413(code,U32.is_lt(code,6742))
    case False{}: lookupBelow418(code,U32.is_lt(code,6754))
def lookupBelow420(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow409(code,U32.is_lt(code,6457))
    case False{}: lookupBelow419(code,U32.is_lt(code,6751))
def lookupBelow421(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow399(code,U32.is_lt(code,6155))
    case False{}: lookupBelow420(code,U32.is_lt(code,6688))
def lookupBelow422(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow379(code,U32.is_lt(code,4238))
    case False{}: lookupBelow421(code,U32.is_lt(code,6441))
def lookupBelow423(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow424(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow425(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow424(code,U32.is_lt(code,6784))
def lookupBelow426(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow423(code,U32.is_lt(code,6771))
    case False{}: lookupBelow425(code,U32.is_lt(code,6783))
def lookupBelow427(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow428(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow429(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow428(code,U32.is_lt(code,6912))
def lookupBelow430(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow427(code,U32.is_lt(code,6878))
    case False{}: lookupBelow429(code,U32.is_lt(code,6892))
def lookupBelow431(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow426(code,U32.is_lt(code,6781))
    case False{}: lookupBelow430(code,U32.is_lt(code,6880))
def lookupBelow432(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow433(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow434(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow433(code,U32.is_lt(code,6931))
def lookupBelow435(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow432(code,U32.is_lt(code,6917))
    case False{}: lookupBelow434(code,U32.is_lt(code,6925))
def lookupBelow436(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow437(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow436(code,U32.is_lt(code,6978))
def lookupBelow438(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow439(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow438(code,U32.is_lt(code,6989))
def lookupBelow440(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow437(code,U32.is_lt(code,6974))
    case False{}: lookupBelow439(code,U32.is_lt(code,6981))
def lookupBelow441(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow435(code,U32.is_lt(code,6923))
    case False{}: lookupBelow440(code,U32.is_lt(code,6980))
def lookupBelow442(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow431(code,U32.is_lt(code,6832))
    case False{}: lookupBelow441(code,U32.is_lt(code,6964))
def lookupBelow443(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow444(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 32
def lookupBelow445(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow444(code,U32.is_lt(code,7043))
def lookupBelow446(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow443(code,U32.is_lt(code,7028))
    case False{}: lookupBelow445(code,U32.is_lt(code,7042))
def lookupBelow447(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow448(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 100
def lookupBelow449(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow448(code,U32.is_lt(code,7083))
def lookupBelow450(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow447(code,U32.is_lt(code,7074))
    case False{}: lookupBelow449(code,U32.is_lt(code,7080))
def lookupBelow451(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow446(code,U32.is_lt(code,7040))
    case False{}: lookupBelow450(code,U32.is_lt(code,7078))
def lookupBelow452(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 32
def lookupBelow453(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow454(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow453(code,U32.is_lt(code,7102))
def lookupBelow455(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow452(code,U32.is_lt(code,7086))
    case False{}: lookupBelow454(code,U32.is_lt(code,7099))
def lookupBelow456(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow457(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow456(code,U32.is_lt(code,7144))
def lookupBelow458(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow459(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow458(code,U32.is_lt(code,7150))
def lookupBelow460(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow457(code,U32.is_lt(code,7143))
    case False{}: lookupBelow459(code,U32.is_lt(code,7149))
def lookupBelow461(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow455(code,U32.is_lt(code,7088))
    case False{}: lookupBelow460(code,U32.is_lt(code,7146))
def lookupBelow462(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow451(code,U32.is_lt(code,7073))
    case False{}: lookupBelow461(code,U32.is_lt(code,7142))
def lookupBelow463(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow442(code,U32.is_lt(code,6916))
    case False{}: lookupBelow462(code,U32.is_lt(code,7084))
def lookupBelow464(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow465(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow466(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow465(code,U32.is_lt(code,7220))
def lookupBelow467(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow464(code,U32.is_lt(code,7156))
    case False{}: lookupBelow466(code,U32.is_lt(code,7212))
def lookupBelow468(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow469(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow470(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow469(code,U32.is_lt(code,7380))
def lookupBelow471(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow468(code,U32.is_lt(code,7224))
    case False{}: lookupBelow470(code,U32.is_lt(code,7379))
def lookupBelow472(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow467(code,U32.is_lt(code,7204))
    case False{}: lookupBelow471(code,U32.is_lt(code,7376))
def lookupBelow473(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow474(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow475(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow474(code,U32.is_lt(code,7406))
def lookupBelow476(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow473(code,U32.is_lt(code,7394))
    case False{}: lookupBelow475(code,U32.is_lt(code,7405))
def lookupBelow477(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow478(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow477(code,U32.is_lt(code,7415))
def lookupBelow479(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow480(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow479(code,U32.is_lt(code,7616))
def lookupBelow481(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow478(code,U32.is_lt(code,7413))
    case False{}: lookupBelow480(code,U32.is_lt(code,7418))
def lookupBelow482(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow476(code,U32.is_lt(code,7401))
    case False{}: lookupBelow481(code,U32.is_lt(code,7416))
def lookupBelow483(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow472(code,U32.is_lt(code,7222))
    case False{}: lookupBelow482(code,U32.is_lt(code,7412))
def lookupBelow484(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def lookupBelow485(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 69
    case False{}: 3
def lookupBelow486(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 4
    case False{}: lookupBelow485(code,U32.is_lt(code,8206))
def lookupBelow487(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow484(code,U32.is_lt(code,8203))
    case False{}: lookupBelow486(code,U32.is_lt(code,8205))
def lookupBelow488(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def lookupBelow489(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow488(code,U32.is_lt(code,8239))
def lookupBelow490(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow491(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow490(code,U32.is_lt(code,8265))
def lookupBelow492(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow489(code,U32.is_lt(code,8232))
    case False{}: lookupBelow491(code,U32.is_lt(code,8253))
def lookupBelow493(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow487(code,U32.is_lt(code,8204))
    case False{}: lookupBelow492(code,U32.is_lt(code,8252))
def lookupBelow494(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def lookupBelow495(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow496(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow495(code,U32.is_lt(code,8433))
def lookupBelow497(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow494(code,U32.is_lt(code,8288))
    case False{}: lookupBelow496(code,U32.is_lt(code,8400))
def lookupBelow498(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow499(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow498(code,U32.is_lt(code,8505))
def lookupBelow500(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow501(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow500(code,U32.is_lt(code,8602))
def lookupBelow502(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow499(code,U32.is_lt(code,8483))
    case False{}: lookupBelow501(code,U32.is_lt(code,8596))
def lookupBelow503(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow497(code,U32.is_lt(code,8304))
    case False{}: lookupBelow502(code,U32.is_lt(code,8506))
def lookupBelow504(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow493(code,U32.is_lt(code,8208))
    case False{}: lookupBelow503(code,U32.is_lt(code,8482))
def lookupBelow505(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow483(code,U32.is_lt(code,7393))
    case False{}: lookupBelow504(code,U32.is_lt(code,8266))
def lookupBelow506(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow463(code,U32.is_lt(code,7019))
    case False{}: lookupBelow505(code,U32.is_lt(code,7680))
def lookupBelow507(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow422(code,U32.is_lt(code,6071))
    case False{}: lookupBelow506(code,U32.is_lt(code,7151))
def lookupBelow508(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow509(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow510(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow509(code,U32.is_lt(code,9000))
def lookupBelow511(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow508(code,U32.is_lt(code,8619))
    case False{}: lookupBelow510(code,U32.is_lt(code,8988))
def lookupBelow512(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow513(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow514(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow513(code,U32.is_lt(code,9204))
def lookupBelow515(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow512(code,U32.is_lt(code,9167))
    case False{}: lookupBelow514(code,U32.is_lt(code,9193))
def lookupBelow516(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow511(code,U32.is_lt(code,8986))
    case False{}: lookupBelow515(code,U32.is_lt(code,9168))
def lookupBelow517(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow518(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow519(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow518(code,U32.is_lt(code,9642))
def lookupBelow520(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow517(code,U32.is_lt(code,9211))
    case False{}: lookupBelow519(code,U32.is_lt(code,9411))
def lookupBelow521(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow522(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow521(code,U32.is_lt(code,9655))
def lookupBelow523(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow524(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow523(code,U32.is_lt(code,9723))
def lookupBelow525(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow522(code,U32.is_lt(code,9654))
    case False{}: lookupBelow524(code,U32.is_lt(code,9665))
def lookupBelow526(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow520(code,U32.is_lt(code,9410))
    case False{}: lookupBelow525(code,U32.is_lt(code,9664))
def lookupBelow527(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow516(code,U32.is_lt(code,9001))
    case False{}: lookupBelow526(code,U32.is_lt(code,9644))
def lookupBelow528(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow529(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow530(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow529(code,U32.is_lt(code,9743))
def lookupBelow531(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow528(code,U32.is_lt(code,9728))
    case False{}: lookupBelow530(code,U32.is_lt(code,9742))
def lookupBelow532(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow533(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow534(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow533(code,U32.is_lt(code,9752))
def lookupBelow535(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow532(code,U32.is_lt(code,9746))
    case False{}: lookupBelow534(code,U32.is_lt(code,9750))
def lookupBelow536(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow531(code,U32.is_lt(code,9733))
    case False{}: lookupBelow535(code,U32.is_lt(code,9748))
def lookupBelow537(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow538(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow539(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow538(code,U32.is_lt(code,9761))
def lookupBelow540(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow537(code,U32.is_lt(code,9757))
    case False{}: lookupBelow539(code,U32.is_lt(code,9760))
def lookupBelow541(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow542(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow541(code,U32.is_lt(code,9766))
def lookupBelow543(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow544(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow543(code,U32.is_lt(code,9771))
def lookupBelow545(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow542(code,U32.is_lt(code,9764))
    case False{}: lookupBelow544(code,U32.is_lt(code,9770))
def lookupBelow546(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow540(code,U32.is_lt(code,9758))
    case False{}: lookupBelow545(code,U32.is_lt(code,9767))
def lookupBelow547(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow536(code,U32.is_lt(code,9745))
    case False{}: lookupBelow546(code,U32.is_lt(code,9762))
def lookupBelow548(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow527(code,U32.is_lt(code,9208))
    case False{}: lookupBelow547(code,U32.is_lt(code,9753))
def lookupBelow549(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow550(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow551(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow550(code,U32.is_lt(code,9792))
def lookupBelow552(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow549(code,U32.is_lt(code,9776))
    case False{}: lookupBelow551(code,U32.is_lt(code,9787))
def lookupBelow553(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow554(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow555(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow554(code,U32.is_lt(code,9812))
def lookupBelow556(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow553(code,U32.is_lt(code,9794))
    case False{}: lookupBelow555(code,U32.is_lt(code,9800))
def lookupBelow557(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow552(code,U32.is_lt(code,9784))
    case False{}: lookupBelow556(code,U32.is_lt(code,9795))
def lookupBelow558(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow559(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow560(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow559(code,U32.is_lt(code,9829))
def lookupBelow561(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow558(code,U32.is_lt(code,9825))
    case False{}: lookupBelow560(code,U32.is_lt(code,9828))
def lookupBelow562(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow563(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow562(code,U32.is_lt(code,9833))
def lookupBelow564(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow565(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow564(code,U32.is_lt(code,9854))
def lookupBelow566(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow563(code,U32.is_lt(code,9832))
    case False{}: lookupBelow565(code,U32.is_lt(code,9852))
def lookupBelow567(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow561(code,U32.is_lt(code,9827))
    case False{}: lookupBelow566(code,U32.is_lt(code,9851))
def lookupBelow568(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow557(code,U32.is_lt(code,9793))
    case False{}: lookupBelow567(code,U32.is_lt(code,9831))
def lookupBelow569(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow570(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow571(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow570(code,U32.is_lt(code,9882))
def lookupBelow572(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow569(code,U32.is_lt(code,9874))
    case False{}: lookupBelow571(code,U32.is_lt(code,9881))
def lookupBelow573(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow574(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow573(code,U32.is_lt(code,9888))
def lookupBelow575(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow576(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow575(code,U32.is_lt(code,9896))
def lookupBelow577(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow574(code,U32.is_lt(code,9885))
    case False{}: lookupBelow576(code,U32.is_lt(code,9895))
def lookupBelow578(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow572(code,U32.is_lt(code,9880))
    case False{}: lookupBelow577(code,U32.is_lt(code,9890))
def lookupBelow579(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow580(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow581(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow580(code,U32.is_lt(code,9917))
def lookupBelow582(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow579(code,U32.is_lt(code,9900))
    case False{}: lookupBelow581(code,U32.is_lt(code,9906))
def lookupBelow583(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow584(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow583(code,U32.is_lt(code,9926))
def lookupBelow585(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow586(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow585(code,U32.is_lt(code,9934))
def lookupBelow587(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow584(code,U32.is_lt(code,9924))
    case False{}: lookupBelow586(code,U32.is_lt(code,9929))
def lookupBelow588(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow582(code,U32.is_lt(code,9904))
    case False{}: lookupBelow587(code,U32.is_lt(code,9928))
def lookupBelow589(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow578(code,U32.is_lt(code,9883))
    case False{}: lookupBelow588(code,U32.is_lt(code,9919))
def lookupBelow590(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow568(code,U32.is_lt(code,9823))
    case False{}: lookupBelow589(code,U32.is_lt(code,9898))
def lookupBelow591(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow548(code,U32.is_lt(code,9727))
    case False{}: lookupBelow590(code,U32.is_lt(code,9856))
def lookupBelow592(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow593(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow594(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow593(code,U32.is_lt(code,9941))
def lookupBelow595(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow592(code,U32.is_lt(code,9937))
    case False{}: lookupBelow594(code,U32.is_lt(code,9939))
def lookupBelow596(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow597(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow598(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow597(code,U32.is_lt(code,9975))
def lookupBelow599(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow596(code,U32.is_lt(code,9963))
    case False{}: lookupBelow598(code,U32.is_lt(code,9974))
def lookupBelow600(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow595(code,U32.is_lt(code,9938))
    case False{}: lookupBelow599(code,U32.is_lt(code,9968))
def lookupBelow601(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow602(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow603(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow602(code,U32.is_lt(code,9987))
def lookupBelow604(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow601(code,U32.is_lt(code,9981))
    case False{}: lookupBelow603(code,U32.is_lt(code,9986))
def lookupBelow605(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow606(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow605(code,U32.is_lt(code,9992))
def lookupBelow607(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow608(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow607(code,U32.is_lt(code,10000))
def lookupBelow609(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow606(code,U32.is_lt(code,9990))
    case False{}: lookupBelow608(code,U32.is_lt(code,9999))
def lookupBelow610(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow604(code,U32.is_lt(code,9982))
    case False{}: lookupBelow609(code,U32.is_lt(code,9998))
def lookupBelow611(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow600(code,U32.is_lt(code,9961))
    case False{}: lookupBelow610(code,U32.is_lt(code,9989))
def lookupBelow612(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow613(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow614(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow613(code,U32.is_lt(code,10006))
def lookupBelow615(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow612(code,U32.is_lt(code,10003))
    case False{}: lookupBelow614(code,U32.is_lt(code,10005))
def lookupBelow616(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow617(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow616(code,U32.is_lt(code,10014))
def lookupBelow618(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow619(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow618(code,U32.is_lt(code,10024))
def lookupBelow620(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow617(code,U32.is_lt(code,10013))
    case False{}: lookupBelow619(code,U32.is_lt(code,10018))
def lookupBelow621(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow615(code,U32.is_lt(code,10004))
    case False{}: lookupBelow620(code,U32.is_lt(code,10017))
def lookupBelow622(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow623(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow624(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow623(code,U32.is_lt(code,10053))
def lookupBelow625(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow622(code,U32.is_lt(code,10035))
    case False{}: lookupBelow624(code,U32.is_lt(code,10052))
def lookupBelow626(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow627(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow626(code,U32.is_lt(code,10060))
def lookupBelow628(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow629(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow628(code,U32.is_lt(code,10063))
def lookupBelow630(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow627(code,U32.is_lt(code,10056))
    case False{}: lookupBelow629(code,U32.is_lt(code,10062))
def lookupBelow631(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow625(code,U32.is_lt(code,10037))
    case False{}: lookupBelow630(code,U32.is_lt(code,10061))
def lookupBelow632(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow621(code,U32.is_lt(code,10007))
    case False{}: lookupBelow631(code,U32.is_lt(code,10055))
def lookupBelow633(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow611(code,U32.is_lt(code,9979))
    case False{}: lookupBelow632(code,U32.is_lt(code,10025))
def lookupBelow634(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow635(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow636(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow635(code,U32.is_lt(code,10083))
def lookupBelow637(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow634(code,U32.is_lt(code,10070))
    case False{}: lookupBelow636(code,U32.is_lt(code,10072))
def lookupBelow638(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow639(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow640(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow639(code,U32.is_lt(code,10146))
def lookupBelow641(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow638(code,U32.is_lt(code,10133))
    case False{}: lookupBelow640(code,U32.is_lt(code,10145))
def lookupBelow642(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow637(code,U32.is_lt(code,10071))
    case False{}: lookupBelow641(code,U32.is_lt(code,10136))
def lookupBelow643(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow644(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow645(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow644(code,U32.is_lt(code,10548))
def lookupBelow646(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow643(code,U32.is_lt(code,10161))
    case False{}: lookupBelow645(code,U32.is_lt(code,10176))
def lookupBelow647(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow648(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow647(code,U32.is_lt(code,11016))
def lookupBelow649(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow650(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow649(code,U32.is_lt(code,11088))
def lookupBelow651(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow648(code,U32.is_lt(code,11013))
    case False{}: lookupBelow650(code,U32.is_lt(code,11037))
def lookupBelow652(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow646(code,U32.is_lt(code,10175))
    case False{}: lookupBelow651(code,U32.is_lt(code,11035))
def lookupBelow653(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow642(code,U32.is_lt(code,10085))
    case False{}: lookupBelow652(code,U32.is_lt(code,10550))
def lookupBelow654(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow655(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow656(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow655(code,U32.is_lt(code,11506))
def lookupBelow657(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow654(code,U32.is_lt(code,11093))
    case False{}: lookupBelow656(code,U32.is_lt(code,11503))
def lookupBelow658(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow659(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow658(code,U32.is_lt(code,11744))
def lookupBelow660(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 16
def lookupBelow661(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow660(code,U32.is_lt(code,12336))
def lookupBelow662(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow659(code,U32.is_lt(code,11648))
    case False{}: lookupBelow661(code,U32.is_lt(code,12330))
def lookupBelow663(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow657(code,U32.is_lt(code,11094))
    case False{}: lookupBelow662(code,U32.is_lt(code,11776))
def lookupBelow664(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow665(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow666(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow665(code,U32.is_lt(code,12443))
def lookupBelow667(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow664(code,U32.is_lt(code,12349))
    case False{}: lookupBelow666(code,U32.is_lt(code,12441))
def lookupBelow668(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow669(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow668(code,U32.is_lt(code,12953))
def lookupBelow670(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow671(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow670(code,U32.is_lt(code,42611))
def lookupBelow672(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow669(code,U32.is_lt(code,12952))
    case False{}: lookupBelow671(code,U32.is_lt(code,42607))
def lookupBelow673(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow667(code,U32.is_lt(code,12350))
    case False{}: lookupBelow672(code,U32.is_lt(code,12954))
def lookupBelow674(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow663(code,U32.is_lt(code,11647))
    case False{}: lookupBelow673(code,U32.is_lt(code,12951))
def lookupBelow675(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow653(code,U32.is_lt(code,10160))
    case False{}: lookupBelow674(code,U32.is_lt(code,12337))
def lookupBelow676(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow633(code,U32.is_lt(code,10002))
    case False{}: lookupBelow675(code,U32.is_lt(code,11089))
def lookupBelow677(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow591(code,U32.is_lt(code,9774))
    case False{}: lookupBelow676(code,U32.is_lt(code,10067))
def lookupBelow678(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow507(code,U32.is_lt(code,6765))
    case False{}: lookupBelow677(code,U32.is_lt(code,9936))
def lookupBelow679(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow338(code,U32.is_lt(code,2820))
    case False{}: lookupBelow678(code,U32.is_lt(code,8617))
def lookupBelow680(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow681(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow682(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow681(code,U32.is_lt(code,42736))
def lookupBelow683(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow680(code,U32.is_lt(code,42622))
    case False{}: lookupBelow682(code,U32.is_lt(code,42656))
def lookupBelow684(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow685(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow686(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow685(code,U32.is_lt(code,43015))
def lookupBelow687(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow684(code,U32.is_lt(code,43010))
    case False{}: lookupBelow686(code,U32.is_lt(code,43014))
def lookupBelow688(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow683(code,U32.is_lt(code,42654))
    case False{}: lookupBelow687(code,U32.is_lt(code,43011))
def lookupBelow689(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow690(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow691(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow690(code,U32.is_lt(code,43047))
def lookupBelow692(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow689(code,U32.is_lt(code,43020))
    case False{}: lookupBelow691(code,U32.is_lt(code,43045))
def lookupBelow693(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow694(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow693(code,U32.is_lt(code,43053))
def lookupBelow695(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow696(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow695(code,U32.is_lt(code,43188))
def lookupBelow697(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow694(code,U32.is_lt(code,43052))
    case False{}: lookupBelow696(code,U32.is_lt(code,43138))
def lookupBelow698(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow692(code,U32.is_lt(code,43043))
    case False{}: lookupBelow697(code,U32.is_lt(code,43136))
def lookupBelow699(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow688(code,U32.is_lt(code,42738))
    case False{}: lookupBelow698(code,U32.is_lt(code,43048))
def lookupBelow700(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow701(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow702(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow701(code,U32.is_lt(code,43263))
def lookupBelow703(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow700(code,U32.is_lt(code,43206))
    case False{}: lookupBelow702(code,U32.is_lt(code,43250))
def lookupBelow704(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow705(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow706(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow705(code,U32.is_lt(code,43346))
def lookupBelow707(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow704(code,U32.is_lt(code,43302))
    case False{}: lookupBelow706(code,U32.is_lt(code,43335))
def lookupBelow708(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow703(code,U32.is_lt(code,43232))
    case False{}: lookupBelow707(code,U32.is_lt(code,43310))
def lookupBelow709(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow710(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow711(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 9
    case False{}: lookupBelow710(code,U32.is_lt(code,43392))
def lookupBelow712(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow709(code,U32.is_lt(code,43348))
    case False{}: lookupBelow711(code,U32.is_lt(code,43389))
def lookupBelow713(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow714(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow713(code,U32.is_lt(code,43401))
def lookupBelow715(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 68
def lookupBelow716(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow715(code,U32.is_lt(code,43443))
def lookupBelow717(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow714(code,U32.is_lt(code,43396))
    case False{}: lookupBelow716(code,U32.is_lt(code,43407))
def lookupBelow718(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow712(code,U32.is_lt(code,43360))
    case False{}: lookupBelow717(code,U32.is_lt(code,43404))
def lookupBelow719(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow708(code,U32.is_lt(code,43264))
    case False{}: lookupBelow718(code,U32.is_lt(code,43395))
def lookupBelow720(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow699(code,U32.is_lt(code,43019))
    case False{}: lookupBelow719(code,U32.is_lt(code,43347))
def lookupBelow721(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow722(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow723(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow722(code,U32.is_lt(code,43454))
def lookupBelow724(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow721(code,U32.is_lt(code,43446))
    case False{}: lookupBelow723(code,U32.is_lt(code,43452))
def lookupBelow725(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: 0
def lookupBelow726(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow727(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow726(code,U32.is_lt(code,43494))
def lookupBelow728(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow725(code,U32.is_lt(code,43457))
    case False{}: lookupBelow727(code,U32.is_lt(code,43493))
def lookupBelow729(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow724(code,U32.is_lt(code,43450))
    case False{}: lookupBelow728(code,U32.is_lt(code,43488))
def lookupBelow730(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow731(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow732(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow731(code,U32.is_lt(code,43561))
def lookupBelow733(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow730(code,U32.is_lt(code,43504))
    case False{}: lookupBelow732(code,U32.is_lt(code,43519))
def lookupBelow734(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow735(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow734(code,U32.is_lt(code,43571))
def lookupBelow736(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow737(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow736(code,U32.is_lt(code,43587))
def lookupBelow738(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow735(code,U32.is_lt(code,43569))
    case False{}: lookupBelow737(code,U32.is_lt(code,43575))
def lookupBelow739(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow733(code,U32.is_lt(code,43514))
    case False{}: lookupBelow738(code,U32.is_lt(code,43573))
def lookupBelow740(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow729(code,U32.is_lt(code,43456))
    case False{}: lookupBelow739(code,U32.is_lt(code,43567))
def lookupBelow741(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow742(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow743(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow742(code,U32.is_lt(code,43616))
def lookupBelow744(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow741(code,U32.is_lt(code,43596))
    case False{}: lookupBelow743(code,U32.is_lt(code,43598))
def lookupBelow745(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow746(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow745(code,U32.is_lt(code,43636))
def lookupBelow747(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow748(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow747(code,U32.is_lt(code,43644))
def lookupBelow749(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow746(code,U32.is_lt(code,43633))
    case False{}: lookupBelow748(code,U32.is_lt(code,43643))
def lookupBelow750(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow744(code,U32.is_lt(code,43597))
    case False{}: lookupBelow749(code,U32.is_lt(code,43642))
def lookupBelow751(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow752(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow753(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow752(code,U32.is_lt(code,43697))
def lookupBelow754(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow751(code,U32.is_lt(code,43646))
    case False{}: lookupBelow753(code,U32.is_lt(code,43696))
def lookupBelow755(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow756(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow755(code,U32.is_lt(code,43703))
def lookupBelow757(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow758(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow757(code,U32.is_lt(code,43712))
def lookupBelow759(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow756(code,U32.is_lt(code,43701))
    case False{}: lookupBelow758(code,U32.is_lt(code,43710))
def lookupBelow760(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow754(code,U32.is_lt(code,43648))
    case False{}: lookupBelow759(code,U32.is_lt(code,43705))
def lookupBelow761(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow750(code,U32.is_lt(code,43632))
    case False{}: lookupBelow760(code,U32.is_lt(code,43698))
def lookupBelow762(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow740(code,U32.is_lt(code,43495))
    case False{}: lookupBelow761(code,U32.is_lt(code,43645))
def lookupBelow763(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow720(code,U32.is_lt(code,43204))
    case False{}: lookupBelow762(code,U32.is_lt(code,43588))
def lookupBelow764(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow765(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow766(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow765(code,U32.is_lt(code,43756))
def lookupBelow767(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow764(code,U32.is_lt(code,43714))
    case False{}: lookupBelow766(code,U32.is_lt(code,43755))
def lookupBelow768(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow769(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: 0
def lookupBelow770(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow769(code,U32.is_lt(code,43767))
def lookupBelow771(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow768(code,U32.is_lt(code,43760))
    case False{}: lookupBelow770(code,U32.is_lt(code,43766))
def lookupBelow772(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow767(code,U32.is_lt(code,43744))
    case False{}: lookupBelow771(code,U32.is_lt(code,43765))
def lookupBelow773(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow774(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow775(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow774(code,U32.is_lt(code,44006))
def lookupBelow776(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow773(code,U32.is_lt(code,43995))
    case False{}: lookupBelow775(code,U32.is_lt(code,44005))
def lookupBelow777(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow778(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow777(code,U32.is_lt(code,44011))
def lookupBelow779(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow780(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow779(code,U32.is_lt(code,44014))
def lookupBelow781(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow778(code,U32.is_lt(code,44009))
    case False{}: lookupBelow780(code,U32.is_lt(code,44013))
def lookupBelow782(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow776(code,U32.is_lt(code,44003))
    case False{}: lookupBelow781(code,U32.is_lt(code,44012))
def lookupBelow783(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow772(code,U32.is_lt(code,43758))
    case False{}: lookupBelow782(code,U32.is_lt(code,44008))
def lookupBelow784(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 10
    case False{}: 0
def lookupBelow785(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow786(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 11
    case False{}: lookupBelow785(code,U32.is_lt(code,64286))
def lookupBelow787(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow784(code,U32.is_lt(code,55239))
    case False{}: lookupBelow786(code,U32.is_lt(code,55292))
def lookupBelow788(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow789(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow790(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow789(code,U32.is_lt(code,65072))
def lookupBelow791(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow788(code,U32.is_lt(code,65024))
    case False{}: lookupBelow790(code,U32.is_lt(code,65056))
def lookupBelow792(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow787(code,U32.is_lt(code,55243))
    case False{}: lookupBelow791(code,U32.is_lt(code,65040))
def lookupBelow793(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def lookupBelow794(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 3
def lookupBelow795(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow794(code,U32.is_lt(code,65520))
def lookupBelow796(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow793(code,U32.is_lt(code,65280))
    case False{}: lookupBelow795(code,U32.is_lt(code,65440))
def lookupBelow797(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow798(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow797(code,U32.is_lt(code,66046))
def lookupBelow799(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow800(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow799(code,U32.is_lt(code,66422))
def lookupBelow801(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow798(code,U32.is_lt(code,66045))
    case False{}: lookupBelow800(code,U32.is_lt(code,66273))
def lookupBelow802(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow796(code,U32.is_lt(code,65438))
    case False{}: lookupBelow801(code,U32.is_lt(code,66272))
def lookupBelow803(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow792(code,U32.is_lt(code,64287))
    case False{}: lookupBelow802(code,U32.is_lt(code,65532))
def lookupBelow804(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow783(code,U32.is_lt(code,43968))
    case False{}: lookupBelow803(code,U32.is_lt(code,65279))
def lookupBelow805(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow806(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow807(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow806(code,U32.is_lt(code,68101))
def lookupBelow808(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow805(code,U32.is_lt(code,68096))
    case False{}: lookupBelow807(code,U32.is_lt(code,68100))
def lookupBelow809(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow810(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow811(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow810(code,U32.is_lt(code,68117))
def lookupBelow812(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow809(code,U32.is_lt(code,68108))
    case False{}: lookupBelow811(code,U32.is_lt(code,68116))
def lookupBelow813(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow808(code,U32.is_lt(code,68097))
    case False{}: lookupBelow812(code,U32.is_lt(code,68112))
def lookupBelow814(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow815(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow816(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow815(code,U32.is_lt(code,68155))
def lookupBelow817(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow814(code,U32.is_lt(code,68121))
    case False{}: lookupBelow816(code,U32.is_lt(code,68152))
def lookupBelow818(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow819(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: lookupBelow818(code,U32.is_lt(code,68325))
def lookupBelow820(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow821(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow820(code,U32.is_lt(code,68904))
def lookupBelow822(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow819(code,U32.is_lt(code,68160))
    case False{}: lookupBelow821(code,U32.is_lt(code,68900))
def lookupBelow823(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow817(code,U32.is_lt(code,68150))
    case False{}: lookupBelow822(code,U32.is_lt(code,68327))
def lookupBelow824(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow813(code,U32.is_lt(code,68103))
    case False{}: lookupBelow823(code,U32.is_lt(code,68159))
def lookupBelow825(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow826(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow827(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow826(code,U32.is_lt(code,69370))
def lookupBelow828(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow825(code,U32.is_lt(code,68974))
    case False{}: lookupBelow827(code,U32.is_lt(code,69293))
def lookupBelow829(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow830(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow829(code,U32.is_lt(code,69457))
def lookupBelow831(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow832(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow831(code,U32.is_lt(code,69632))
def lookupBelow833(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow830(code,U32.is_lt(code,69446))
    case False{}: lookupBelow832(code,U32.is_lt(code,69510))
def lookupBelow834(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow828(code,U32.is_lt(code,69291))
    case False{}: lookupBelow833(code,U32.is_lt(code,69506))
def lookupBelow835(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow836(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow837(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow836(code,U32.is_lt(code,69703))
def lookupBelow838(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow835(code,U32.is_lt(code,69634))
    case False{}: lookupBelow837(code,U32.is_lt(code,69688))
def lookupBelow839(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow840(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow839(code,U32.is_lt(code,69747))
def lookupBelow841(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow842(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow841(code,U32.is_lt(code,69762))
def lookupBelow843(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow840(code,U32.is_lt(code,69745))
    case False{}: lookupBelow842(code,U32.is_lt(code,69759))
def lookupBelow844(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow838(code,U32.is_lt(code,69635))
    case False{}: lookupBelow843(code,U32.is_lt(code,69749))
def lookupBelow845(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow834(code,U32.is_lt(code,69376))
    case False{}: lookupBelow844(code,U32.is_lt(code,69744))
def lookupBelow846(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow824(code,U32.is_lt(code,68120))
    case False{}: lookupBelow845(code,U32.is_lt(code,69633))
def lookupBelow847(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow804(code,U32.is_lt(code,55216))
    case False{}: lookupBelow846(code,U32.is_lt(code,68969))
def lookupBelow848(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow763(code,U32.is_lt(code,43444))
    case False{}: lookupBelow847(code,U32.is_lt(code,66427))
def lookupBelow849(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow850(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow851(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow850(code,U32.is_lt(code,69817))
def lookupBelow852(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow849(code,U32.is_lt(code,69808))
    case False{}: lookupBelow851(code,U32.is_lt(code,69815))
def lookupBelow853(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 7
def lookupBelow854(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow855(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow854(code,U32.is_lt(code,69827))
def lookupBelow856(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow853(code,U32.is_lt(code,69821))
    case False{}: lookupBelow855(code,U32.is_lt(code,69826))
def lookupBelow857(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow852(code,U32.is_lt(code,69811))
    case False{}: lookupBelow856(code,U32.is_lt(code,69822))
def lookupBelow858(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: 0
def lookupBelow859(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 68
def lookupBelow860(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow859(code,U32.is_lt(code,69927))
def lookupBelow861(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow858(code,U32.is_lt(code,69838))
    case False{}: lookupBelow860(code,U32.is_lt(code,69891))
def lookupBelow862(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 100
def lookupBelow863(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow862(code,U32.is_lt(code,69939))
def lookupBelow864(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow865(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow864(code,U32.is_lt(code,69956))
def lookupBelow866(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow863(code,U32.is_lt(code,69933))
    case False{}: lookupBelow865(code,U32.is_lt(code,69941))
def lookupBelow867(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow861(code,U32.is_lt(code,69888))
    case False{}: lookupBelow866(code,U32.is_lt(code,69940))
def lookupBelow868(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow857(code,U32.is_lt(code,69819))
    case False{}: lookupBelow867(code,U32.is_lt(code,69932))
def lookupBelow869(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 32
def lookupBelow870(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow871(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow870(code,U32.is_lt(code,70004))
def lookupBelow872(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow869(code,U32.is_lt(code,69959))
    case False{}: lookupBelow871(code,U32.is_lt(code,70003))
def lookupBelow873(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow874(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow875(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow874(code,U32.is_lt(code,70070))
def lookupBelow876(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow873(code,U32.is_lt(code,70018))
    case False{}: lookupBelow875(code,U32.is_lt(code,70067))
def lookupBelow877(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow872(code,U32.is_lt(code,69960))
    case False{}: lookupBelow876(code,U32.is_lt(code,70019))
def lookupBelow878(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow879(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: 0
def lookupBelow880(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow879(code,U32.is_lt(code,70084))
def lookupBelow881(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow878(code,U32.is_lt(code,70080))
    case False{}: lookupBelow880(code,U32.is_lt(code,70082))
def lookupBelow882(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow883(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow882(code,U32.is_lt(code,70094))
def lookupBelow884(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow885(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow884(code,U32.is_lt(code,70188))
def lookupBelow886(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow883(code,U32.is_lt(code,70093))
    case False{}: lookupBelow885(code,U32.is_lt(code,70096))
def lookupBelow887(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow881(code,U32.is_lt(code,70081))
    case False{}: lookupBelow886(code,U32.is_lt(code,70095))
def lookupBelow888(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow877(code,U32.is_lt(code,70016))
    case False{}: lookupBelow887(code,U32.is_lt(code,70089))
def lookupBelow889(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow868(code,U32.is_lt(code,69837))
    case False{}: lookupBelow888(code,U32.is_lt(code,70079))
def lookupBelow890(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow891(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow892(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow891(code,U32.is_lt(code,70206))
def lookupBelow893(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow890(code,U32.is_lt(code,70194))
    case False{}: lookupBelow892(code,U32.is_lt(code,70200))
def lookupBelow894(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow895(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow896(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow895(code,U32.is_lt(code,70368))
def lookupBelow897(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow894(code,U32.is_lt(code,70209))
    case False{}: lookupBelow896(code,U32.is_lt(code,70367))
def lookupBelow898(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow893(code,U32.is_lt(code,70196))
    case False{}: lookupBelow897(code,U32.is_lt(code,70210))
def lookupBelow899(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow900(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow901(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow900(code,U32.is_lt(code,70404))
def lookupBelow902(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow899(code,U32.is_lt(code,70379))
    case False{}: lookupBelow901(code,U32.is_lt(code,70402))
def lookupBelow903(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow904(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow903(code,U32.is_lt(code,70462))
def lookupBelow905(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow906(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow905(code,U32.is_lt(code,70465))
def lookupBelow907(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow904(code,U32.is_lt(code,70461))
    case False{}: lookupBelow906(code,U32.is_lt(code,70464))
def lookupBelow908(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow902(code,U32.is_lt(code,70400))
    case False{}: lookupBelow907(code,U32.is_lt(code,70463))
def lookupBelow909(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow898(code,U32.is_lt(code,70207))
    case False{}: lookupBelow908(code,U32.is_lt(code,70459))
def lookupBelow910(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow911(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow912(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow911(code,U32.is_lt(code,70477))
def lookupBelow913(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow910(code,U32.is_lt(code,70471))
    case False{}: lookupBelow912(code,U32.is_lt(code,70475))
def lookupBelow914(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow915(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow914(code,U32.is_lt(code,70488))
def lookupBelow916(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow917(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow916(code,U32.is_lt(code,70502))
def lookupBelow918(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow915(code,U32.is_lt(code,70487))
    case False{}: lookupBelow917(code,U32.is_lt(code,70500))
def lookupBelow919(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow913(code,U32.is_lt(code,70473))
    case False{}: lookupBelow918(code,U32.is_lt(code,70498))
def lookupBelow920(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow921(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow922(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow921(code,U32.is_lt(code,70538))
def lookupBelow923(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow920(code,U32.is_lt(code,70512))
    case False{}: lookupBelow922(code,U32.is_lt(code,70528))
def lookupBelow924(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow925(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow924(code,U32.is_lt(code,70542))
def lookupBelow926(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow927(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow926(code,U32.is_lt(code,70582))
def lookupBelow928(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow925(code,U32.is_lt(code,70540))
    case False{}: lookupBelow927(code,U32.is_lt(code,70544))
def lookupBelow929(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow923(code,U32.is_lt(code,70517))
    case False{}: lookupBelow928(code,U32.is_lt(code,70543))
def lookupBelow930(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow919(code,U32.is_lt(code,70478))
    case False{}: lookupBelow929(code,U32.is_lt(code,70539))
def lookupBelow931(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow909(code,U32.is_lt(code,70371))
    case False{}: lookupBelow930(code,U32.is_lt(code,70509))
def lookupBelow932(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow889(code,U32.is_lt(code,69957))
    case False{}: lookupBelow931(code,U32.is_lt(code,70469))
def lookupBelow933(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow934(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow935(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow934(code,U32.is_lt(code,70594))
def lookupBelow936(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow933(code,U32.is_lt(code,70585))
    case False{}: lookupBelow935(code,U32.is_lt(code,70593))
def lookupBelow937(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow938(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow939(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow938(code,U32.is_lt(code,70602))
def lookupBelow940(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow937(code,U32.is_lt(code,70597))
    case False{}: lookupBelow939(code,U32.is_lt(code,70599))
def lookupBelow941(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow936(code,U32.is_lt(code,70587))
    case False{}: lookupBelow940(code,U32.is_lt(code,70598))
def lookupBelow942(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow943(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 100
    case False{}: 7
def lookupBelow944(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow943(code,U32.is_lt(code,70609))
def lookupBelow945(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow942(code,U32.is_lt(code,70604))
    case False{}: lookupBelow944(code,U32.is_lt(code,70608))
def lookupBelow946(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow947(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow946(code,U32.is_lt(code,70625))
def lookupBelow948(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow949(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow948(code,U32.is_lt(code,70712))
def lookupBelow950(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow947(code,U32.is_lt(code,70611))
    case False{}: lookupBelow949(code,U32.is_lt(code,70709))
def lookupBelow951(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow945(code,U32.is_lt(code,70606))
    case False{}: lookupBelow950(code,U32.is_lt(code,70627))
def lookupBelow952(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow941(code,U32.is_lt(code,70595))
    case False{}: lookupBelow951(code,U32.is_lt(code,70610))
def lookupBelow953(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow954(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow955(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow954(code,U32.is_lt(code,70727))
def lookupBelow956(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow953(code,U32.is_lt(code,70722))
    case False{}: lookupBelow955(code,U32.is_lt(code,70726))
def lookupBelow957(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow958(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow957(code,U32.is_lt(code,70832))
def lookupBelow959(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow960(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow959(code,U32.is_lt(code,70841))
def lookupBelow961(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow958(code,U32.is_lt(code,70751))
    case False{}: lookupBelow960(code,U32.is_lt(code,70835))
def lookupBelow962(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow956(code,U32.is_lt(code,70725))
    case False{}: lookupBelow961(code,U32.is_lt(code,70833))
def lookupBelow963(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow964(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow965(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow964(code,U32.is_lt(code,70847))
def lookupBelow966(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow963(code,U32.is_lt(code,70843))
    case False{}: lookupBelow965(code,U32.is_lt(code,70846))
def lookupBelow967(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow968(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow967(code,U32.is_lt(code,70852))
def lookupBelow969(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow970(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow969(code,U32.is_lt(code,71090))
def lookupBelow971(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow968(code,U32.is_lt(code,70850))
    case False{}: lookupBelow970(code,U32.is_lt(code,71088))
def lookupBelow972(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow966(code,U32.is_lt(code,70845))
    case False{}: lookupBelow971(code,U32.is_lt(code,71087))
def lookupBelow973(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow962(code,U32.is_lt(code,70750))
    case False{}: lookupBelow972(code,U32.is_lt(code,70849))
def lookupBelow974(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow952(code,U32.is_lt(code,70603))
    case False{}: lookupBelow973(code,U32.is_lt(code,70842))
def lookupBelow975(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow976(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow977(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow976(code,U32.is_lt(code,71103))
def lookupBelow978(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow975(code,U32.is_lt(code,71096))
    case False{}: lookupBelow977(code,U32.is_lt(code,71102))
def lookupBelow979(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow980(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow981(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow980(code,U32.is_lt(code,71219))
def lookupBelow982(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow979(code,U32.is_lt(code,71132))
    case False{}: lookupBelow981(code,U32.is_lt(code,71216))
def lookupBelow983(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow978(code,U32.is_lt(code,71100))
    case False{}: lookupBelow982(code,U32.is_lt(code,71134))
def lookupBelow984(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow985(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow986(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow985(code,U32.is_lt(code,71233))
def lookupBelow987(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow984(code,U32.is_lt(code,71229))
    case False{}: lookupBelow986(code,U32.is_lt(code,71231))
def lookupBelow988(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow989(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow988(code,U32.is_lt(code,71341))
def lookupBelow990(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow991(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow990(code,U32.is_lt(code,71352))
def lookupBelow992(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow989(code,U32.is_lt(code,71340))
    case False{}: lookupBelow991(code,U32.is_lt(code,71344))
def lookupBelow993(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow987(code,U32.is_lt(code,71230))
    case False{}: lookupBelow992(code,U32.is_lt(code,71342))
def lookupBelow994(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow983(code,U32.is_lt(code,71105))
    case False{}: lookupBelow993(code,U32.is_lt(code,71339))
def lookupBelow995(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow996(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow997(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow996(code,U32.is_lt(code,71458))
def lookupBelow998(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow995(code,U32.is_lt(code,71454))
    case False{}: lookupBelow997(code,U32.is_lt(code,71456))
def lookupBelow999(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1000(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow999(code,U32.is_lt(code,71468))
def lookupBelow1001(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow1002(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1001(code,U32.is_lt(code,71736))
def lookupBelow1003(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1000(code,U32.is_lt(code,71463))
    case False{}: lookupBelow1002(code,U32.is_lt(code,71727))
def lookupBelow1004(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow998(code,U32.is_lt(code,71455))
    case False{}: lookupBelow1003(code,U32.is_lt(code,71724))
def lookupBelow1005(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1006(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow1007(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow1006(code,U32.is_lt(code,71945))
def lookupBelow1008(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1005(code,U32.is_lt(code,71739))
    case False{}: lookupBelow1007(code,U32.is_lt(code,71943))
def lookupBelow1009(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 0
def lookupBelow1010(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1009(code,U32.is_lt(code,71956))
def lookupBelow1011(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow1012(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow1011(code,U32.is_lt(code,71960))
def lookupBelow1013(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1010(code,U32.is_lt(code,71948))
    case False{}: lookupBelow1012(code,U32.is_lt(code,71959))
def lookupBelow1014(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1008(code,U32.is_lt(code,71936))
    case False{}: lookupBelow1013(code,U32.is_lt(code,71957))
def lookupBelow1015(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1004(code,U32.is_lt(code,71462))
    case False{}: lookupBelow1014(code,U32.is_lt(code,71946))
def lookupBelow1016(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow994(code,U32.is_lt(code,71227))
    case False{}: lookupBelow1015(code,U32.is_lt(code,71737))
def lookupBelow1017(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow974(code,U32.is_lt(code,70720))
    case False{}: lookupBelow1016(code,U32.is_lt(code,71453))
def lookupBelow1018(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow932(code,U32.is_lt(code,70191))
    case False{}: lookupBelow1017(code,U32.is_lt(code,71094))
def lookupBelow1019(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow848(code,U32.is_lt(code,43713))
    case False{}: lookupBelow1018(code,U32.is_lt(code,70584))
def lookupBelow1020(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow1021(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow1022(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1021(code,U32.is_lt(code,71993))
def lookupBelow1023(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1020(code,U32.is_lt(code,71985))
    case False{}: lookupBelow1022(code,U32.is_lt(code,71991))
def lookupBelow1024(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 100
def lookupBelow1025(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 7
def lookupBelow1026(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: lookupBelow1025(code,U32.is_lt(code,72001))
def lookupBelow1027(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1024(code,U32.is_lt(code,71998))
    case False{}: lookupBelow1026(code,U32.is_lt(code,72000))
def lookupBelow1028(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1023(code,U32.is_lt(code,71990))
    case False{}: lookupBelow1027(code,U32.is_lt(code,71999))
def lookupBelow1029(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1030(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1031(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1030(code,U32.is_lt(code,72148))
def lookupBelow1032(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1029(code,U32.is_lt(code,72003))
    case False{}: lookupBelow1031(code,U32.is_lt(code,72145))
def lookupBelow1033(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow1034(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1033(code,U32.is_lt(code,72156))
def lookupBelow1035(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow1036(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1035(code,U32.is_lt(code,72164))
def lookupBelow1037(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1034(code,U32.is_lt(code,72154))
    case False{}: lookupBelow1036(code,U32.is_lt(code,72161))
def lookupBelow1038(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1032(code,U32.is_lt(code,72004))
    case False{}: lookupBelow1037(code,U32.is_lt(code,72160))
def lookupBelow1039(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1028(code,U32.is_lt(code,71995))
    case False{}: lookupBelow1038(code,U32.is_lt(code,72152))
def lookupBelow1040(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow1041(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 68
def lookupBelow1042(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1041(code,U32.is_lt(code,72243))
def lookupBelow1043(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1040(code,U32.is_lt(code,72192))
    case False{}: lookupBelow1042(code,U32.is_lt(code,72203))
def lookupBelow1044(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow1045(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 100
def lookupBelow1046(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1045(code,U32.is_lt(code,72263))
def lookupBelow1047(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1044(code,U32.is_lt(code,72250))
    case False{}: lookupBelow1046(code,U32.is_lt(code,72255))
def lookupBelow1048(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1043(code,U32.is_lt(code,72193))
    case False{}: lookupBelow1047(code,U32.is_lt(code,72251))
def lookupBelow1049(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 32
def lookupBelow1050(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1051(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1050(code,U32.is_lt(code,72281))
def lookupBelow1052(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1049(code,U32.is_lt(code,72272))
    case False{}: lookupBelow1051(code,U32.is_lt(code,72279))
def lookupBelow1053(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 7
    case False{}: 68
def lookupBelow1054(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: lookupBelow1053(code,U32.is_lt(code,72330))
def lookupBelow1055(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 100
def lookupBelow1056(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1055(code,U32.is_lt(code,72345))
def lookupBelow1057(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1054(code,U32.is_lt(code,72324))
    case False{}: lookupBelow1056(code,U32.is_lt(code,72344))
def lookupBelow1058(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1052(code,U32.is_lt(code,72273))
    case False{}: lookupBelow1057(code,U32.is_lt(code,72343))
def lookupBelow1059(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1048(code,U32.is_lt(code,72249))
    case False{}: lookupBelow1058(code,U32.is_lt(code,72284))
def lookupBelow1060(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1039(code,U32.is_lt(code,72002))
    case False{}: lookupBelow1059(code,U32.is_lt(code,72264))
def lookupBelow1061(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1062(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow1063(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1062(code,U32.is_lt(code,72549))
def lookupBelow1064(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1061(code,U32.is_lt(code,72544))
    case False{}: lookupBelow1063(code,U32.is_lt(code,72546))
def lookupBelow1065(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow1066(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1067(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1066(code,U32.is_lt(code,72752))
def lookupBelow1068(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1065(code,U32.is_lt(code,72551))
    case False{}: lookupBelow1067(code,U32.is_lt(code,72751))
def lookupBelow1069(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1064(code,U32.is_lt(code,72545))
    case False{}: lookupBelow1068(code,U32.is_lt(code,72552))
def lookupBelow1070(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1071(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1072(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1071(code,U32.is_lt(code,72768))
def lookupBelow1073(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1070(code,U32.is_lt(code,72760))
    case False{}: lookupBelow1072(code,U32.is_lt(code,72767))
def lookupBelow1074(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 8
def lookupBelow1075(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1074(code,U32.is_lt(code,72873))
def lookupBelow1076(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1077(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1076(code,U32.is_lt(code,72882))
def lookupBelow1078(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1075(code,U32.is_lt(code,72872))
    case False{}: lookupBelow1077(code,U32.is_lt(code,72881))
def lookupBelow1079(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1073(code,U32.is_lt(code,72766))
    case False{}: lookupBelow1078(code,U32.is_lt(code,72874))
def lookupBelow1080(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1069(code,U32.is_lt(code,72550))
    case False{}: lookupBelow1079(code,U32.is_lt(code,72850))
def lookupBelow1081(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1082(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1083(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1082(code,U32.is_lt(code,73015))
def lookupBelow1084(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1081(code,U32.is_lt(code,72885))
    case False{}: lookupBelow1083(code,U32.is_lt(code,73009))
def lookupBelow1085(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1086(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1085(code,U32.is_lt(code,73020))
def lookupBelow1087(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 7
def lookupBelow1088(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1087(code,U32.is_lt(code,73030))
def lookupBelow1089(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1086(code,U32.is_lt(code,73019))
    case False{}: lookupBelow1088(code,U32.is_lt(code,73023))
def lookupBelow1090(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1084(code,U32.is_lt(code,72887))
    case False{}: lookupBelow1089(code,U32.is_lt(code,73022))
def lookupBelow1091(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1092(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1093(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1092(code,U32.is_lt(code,73104))
def lookupBelow1094(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1091(code,U32.is_lt(code,73032))
    case False{}: lookupBelow1093(code,U32.is_lt(code,73103))
def lookupBelow1095(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1096(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1095(code,U32.is_lt(code,73109))
def lookupBelow1097(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1098(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1097(code,U32.is_lt(code,73112))
def lookupBelow1099(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1096(code,U32.is_lt(code,73107))
    case False{}: lookupBelow1098(code,U32.is_lt(code,73111))
def lookupBelow1100(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1094(code,U32.is_lt(code,73098))
    case False{}: lookupBelow1099(code,U32.is_lt(code,73110))
def lookupBelow1101(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1090(code,U32.is_lt(code,73018))
    case False{}: lookupBelow1100(code,U32.is_lt(code,73106))
def lookupBelow1102(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1080(code,U32.is_lt(code,72759))
    case False{}: lookupBelow1101(code,U32.is_lt(code,73031))
def lookupBelow1103(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1060(code,U32.is_lt(code,72165))
    case False{}: lookupBelow1102(code,U32.is_lt(code,72884))
def lookupBelow1104(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 8
def lookupBelow1105(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 7
def lookupBelow1106(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1105(code,U32.is_lt(code,73474))
def lookupBelow1107(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1104(code,U32.is_lt(code,73461))
    case False{}: lookupBelow1106(code,U32.is_lt(code,73472))
def lookupBelow1108(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 32
def lookupBelow1109(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 32
    case False{}: 8
def lookupBelow1110(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1109(code,U32.is_lt(code,73524))
def lookupBelow1111(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1108(code,U32.is_lt(code,73476))
    case False{}: lookupBelow1110(code,U32.is_lt(code,73490))
def lookupBelow1112(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1107(code,U32.is_lt(code,73463))
    case False{}: lookupBelow1111(code,U32.is_lt(code,73489))
def lookupBelow1113(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1114(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 100
def lookupBelow1115(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: lookupBelow1114(code,U32.is_lt(code,73538))
def lookupBelow1116(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1113(code,U32.is_lt(code,73531))
    case False{}: lookupBelow1115(code,U32.is_lt(code,73536))
def lookupBelow1117(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1118(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1117(code,U32.is_lt(code,73563))
def lookupBelow1119(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1120(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: lookupBelow1119(code,U32.is_lt(code,78913))
def lookupBelow1121(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1118(code,U32.is_lt(code,73562))
    case False{}: lookupBelow1120(code,U32.is_lt(code,78912))
def lookupBelow1122(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1116(code,U32.is_lt(code,73534))
    case False{}: lookupBelow1121(code,U32.is_lt(code,78896))
def lookupBelow1123(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1112(code,U32.is_lt(code,73475))
    case False{}: lookupBelow1122(code,U32.is_lt(code,73539))
def lookupBelow1124(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1125(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 68
def lookupBelow1126(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1125(code,U32.is_lt(code,90413))
def lookupBelow1127(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1124(code,U32.is_lt(code,78934))
    case False{}: lookupBelow1126(code,U32.is_lt(code,90410))
def lookupBelow1128(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1129(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1130(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1129(code,U32.is_lt(code,92983))
def lookupBelow1131(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1128(code,U32.is_lt(code,92912))
    case False{}: lookupBelow1130(code,U32.is_lt(code,92976))
def lookupBelow1132(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1127(code,U32.is_lt(code,90398))
    case False{}: lookupBelow1131(code,U32.is_lt(code,92917))
def lookupBelow1133(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 10
    case False{}: 0
def lookupBelow1134(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1135(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 10
    case False{}: lookupBelow1134(code,U32.is_lt(code,94031))
def lookupBelow1136(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1133(code,U32.is_lt(code,93540))
    case False{}: lookupBelow1135(code,U32.is_lt(code,93547))
def lookupBelow1137(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 8
    case False{}: 0
def lookupBelow1138(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1137(code,U32.is_lt(code,94088))
def lookupBelow1139(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1140(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1139(code,U32.is_lt(code,94180))
def lookupBelow1141(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1138(code,U32.is_lt(code,94033))
    case False{}: lookupBelow1140(code,U32.is_lt(code,94099))
def lookupBelow1142(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1136(code,U32.is_lt(code,93543))
    case False{}: lookupBelow1141(code,U32.is_lt(code,94095))
def lookupBelow1143(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1132(code,U32.is_lt(code,90416))
    case False{}: lookupBelow1142(code,U32.is_lt(code,94032))
def lookupBelow1144(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1123(code,U32.is_lt(code,73526))
    case False{}: lookupBelow1143(code,U32.is_lt(code,93539))
def lookupBelow1145(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1146(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1147(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1146(code,U32.is_lt(code,113823))
def lookupBelow1148(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1145(code,U32.is_lt(code,94192))
    case False{}: lookupBelow1147(code,U32.is_lt(code,113821))
def lookupBelow1149(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def lookupBelow1150(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1151(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1150(code,U32.is_lt(code,118576))
def lookupBelow1152(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1149(code,U32.is_lt(code,113828))
    case False{}: lookupBelow1151(code,U32.is_lt(code,118574))
def lookupBelow1153(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1148(code,U32.is_lt(code,94194))
    case False{}: lookupBelow1152(code,U32.is_lt(code,118528))
def lookupBelow1154(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1155(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 3
def lookupBelow1156(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1155(code,U32.is_lt(code,119155))
def lookupBelow1157(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1154(code,U32.is_lt(code,119141))
    case False{}: lookupBelow1156(code,U32.is_lt(code,119149))
def lookupBelow1158(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1159(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1158(code,U32.is_lt(code,119173))
def lookupBelow1160(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1161(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1160(code,U32.is_lt(code,119214))
def lookupBelow1162(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1159(code,U32.is_lt(code,119171))
    case False{}: lookupBelow1161(code,U32.is_lt(code,119210))
def lookupBelow1163(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1157(code,U32.is_lt(code,119146))
    case False{}: lookupBelow1162(code,U32.is_lt(code,119180))
def lookupBelow1164(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1153(code,U32.is_lt(code,113824))
    case False{}: lookupBelow1163(code,U32.is_lt(code,119163))
def lookupBelow1165(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1166(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1167(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1166(code,U32.is_lt(code,121403))
def lookupBelow1168(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1165(code,U32.is_lt(code,119365))
    case False{}: lookupBelow1167(code,U32.is_lt(code,121399))
def lookupBelow1169(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1170(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1169(code,U32.is_lt(code,121462))
def lookupBelow1171(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1172(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1171(code,U32.is_lt(code,121499))
def lookupBelow1173(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1170(code,U32.is_lt(code,121461))
    case False{}: lookupBelow1172(code,U32.is_lt(code,121477))
def lookupBelow1174(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1168(code,U32.is_lt(code,121344))
    case False{}: lookupBelow1173(code,U32.is_lt(code,121476))
def lookupBelow1175(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1176(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1177(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1176(code,U32.is_lt(code,122887))
def lookupBelow1178(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1175(code,U32.is_lt(code,121505))
    case False{}: lookupBelow1177(code,U32.is_lt(code,122880))
def lookupBelow1179(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1180(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1179(code,U32.is_lt(code,122907))
def lookupBelow1181(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1182(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1181(code,U32.is_lt(code,122917))
def lookupBelow1183(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1180(code,U32.is_lt(code,122905))
    case False{}: lookupBelow1182(code,U32.is_lt(code,122915))
def lookupBelow1184(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1178(code,U32.is_lt(code,121520))
    case False{}: lookupBelow1183(code,U32.is_lt(code,122914))
def lookupBelow1185(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1174(code,U32.is_lt(code,121453))
    case False{}: lookupBelow1184(code,U32.is_lt(code,122888))
def lookupBelow1186(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1164(code,U32.is_lt(code,118599))
    case False{}: lookupBelow1185(code,U32.is_lt(code,121504))
def lookupBelow1187(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1144(code,U32.is_lt(code,78919))
    case False{}: lookupBelow1186(code,U32.is_lt(code,119362))
def lookupBelow1188(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1103(code,U32.is_lt(code,72346))
    case False{}: lookupBelow1187(code,U32.is_lt(code,94181))
def lookupBelow1189(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1190(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1191(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1190(code,U32.is_lt(code,123184))
def lookupBelow1192(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1189(code,U32.is_lt(code,122923))
    case False{}: lookupBelow1191(code,U32.is_lt(code,123024))
def lookupBelow1193(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1194(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1195(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1194(code,U32.is_lt(code,123632))
def lookupBelow1196(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1193(code,U32.is_lt(code,123566))
    case False{}: lookupBelow1195(code,U32.is_lt(code,123628))
def lookupBelow1197(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1192(code,U32.is_lt(code,123023))
    case False{}: lookupBelow1196(code,U32.is_lt(code,123567))
def lookupBelow1198(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1199(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1200(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1199(code,U32.is_lt(code,124643))
def lookupBelow1201(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1198(code,U32.is_lt(code,124144))
    case False{}: lookupBelow1200(code,U32.is_lt(code,124400))
def lookupBelow1202(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1203(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1202(code,U32.is_lt(code,124647))
def lookupBelow1204(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1205(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1204(code,U32.is_lt(code,124661))
def lookupBelow1206(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1203(code,U32.is_lt(code,124646))
    case False{}: lookupBelow1205(code,U32.is_lt(code,124656))
def lookupBelow1207(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1201(code,U32.is_lt(code,124398))
    case False{}: lookupBelow1206(code,U32.is_lt(code,124654))
def lookupBelow1208(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1197(code,U32.is_lt(code,123191))
    case False{}: lookupBelow1207(code,U32.is_lt(code,124644))
def lookupBelow1209(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 68
def lookupBelow1210(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 0
def lookupBelow1211(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1210(code,U32.is_lt(code,125259))
def lookupBelow1212(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1209(code,U32.is_lt(code,125136))
    case False{}: lookupBelow1211(code,U32.is_lt(code,125252))
def lookupBelow1213(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1214(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1215(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1214(code,U32.is_lt(code,127124))
def lookupBelow1216(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1213(code,U32.is_lt(code,126981))
    case False{}: lookupBelow1215(code,U32.is_lt(code,127024))
def lookupBelow1217(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1212(code,U32.is_lt(code,125143))
    case False{}: lookupBelow1216(code,U32.is_lt(code,127020))
def lookupBelow1218(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1219(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1220(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1219(code,U32.is_lt(code,127169))
def lookupBelow1221(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1218(code,U32.is_lt(code,127151))
    case False{}: lookupBelow1220(code,U32.is_lt(code,127168))
def lookupBelow1222(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1223(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1222(code,U32.is_lt(code,127222))
def lookupBelow1224(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1225(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1224(code,U32.is_lt(code,127346))
def lookupBelow1226(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1223(code,U32.is_lt(code,127185))
    case False{}: lookupBelow1225(code,U32.is_lt(code,127344))
def lookupBelow1227(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1221(code,U32.is_lt(code,127153))
    case False{}: lookupBelow1226(code,U32.is_lt(code,127232))
def lookupBelow1228(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1217(code,U32.is_lt(code,126980))
    case False{}: lookupBelow1227(code,U32.is_lt(code,127183))
def lookupBelow1229(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1208(code,U32.is_lt(code,124140))
    case False{}: lookupBelow1228(code,U32.is_lt(code,127136))
def lookupBelow1230(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1231(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1232(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1231(code,U32.is_lt(code,127377))
def lookupBelow1233(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1230(code,U32.is_lt(code,127360))
    case False{}: lookupBelow1232(code,U32.is_lt(code,127375))
def lookupBelow1234(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1235(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1236(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 6
    case False{}: lookupBelow1235(code,U32.is_lt(code,127489))
def lookupBelow1237(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1234(code,U32.is_lt(code,127406))
    case False{}: lookupBelow1236(code,U32.is_lt(code,127488))
def lookupBelow1238(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1233(code,U32.is_lt(code,127374))
    case False{}: lookupBelow1237(code,U32.is_lt(code,127462))
def lookupBelow1239(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1240(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1241(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1240(code,U32.is_lt(code,127536))
def lookupBelow1242(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1239(code,U32.is_lt(code,127514))
    case False{}: lookupBelow1241(code,U32.is_lt(code,127535))
def lookupBelow1243(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1244(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1243(code,U32.is_lt(code,127548))
def lookupBelow1245(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1246(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1245(code,U32.is_lt(code,127584))
def lookupBelow1247(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1244(code,U32.is_lt(code,127547))
    case False{}: lookupBelow1246(code,U32.is_lt(code,127561))
def lookupBelow1248(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1242(code,U32.is_lt(code,127515))
    case False{}: lookupBelow1247(code,U32.is_lt(code,127552))
def lookupBelow1249(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1238(code,U32.is_lt(code,127387))
    case False{}: lookupBelow1248(code,U32.is_lt(code,127538))
def lookupBelow1250(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1251(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1252(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1251(code,U32.is_lt(code,127894))
def lookupBelow1253(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1250(code,U32.is_lt(code,127778))
    case False{}: lookupBelow1252(code,U32.is_lt(code,127892))
def lookupBelow1254(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1255(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1254(code,U32.is_lt(code,127900))
def lookupBelow1256(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1257(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1256(code,U32.is_lt(code,127987))
def lookupBelow1258(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1255(code,U32.is_lt(code,127897))
    case False{}: lookupBelow1257(code,U32.is_lt(code,127985))
def lookupBelow1259(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1253(code,U32.is_lt(code,127780))
    case False{}: lookupBelow1258(code,U32.is_lt(code,127902))
def lookupBelow1260(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1261(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1262(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1261(code,U32.is_lt(code,128254))
def lookupBelow1263(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1260(code,U32.is_lt(code,127991))
    case False{}: lookupBelow1262(code,U32.is_lt(code,128000))
def lookupBelow1264(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1265(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1264(code,U32.is_lt(code,128329))
def lookupBelow1266(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1267(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1266(code,U32.is_lt(code,128360))
def lookupBelow1268(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1265(code,U32.is_lt(code,128318))
    case False{}: lookupBelow1267(code,U32.is_lt(code,128336))
def lookupBelow1269(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1263(code,U32.is_lt(code,127995))
    case False{}: lookupBelow1268(code,U32.is_lt(code,128335))
def lookupBelow1270(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1259(code,U32.is_lt(code,127896))
    case False{}: lookupBelow1269(code,U32.is_lt(code,128255))
def lookupBelow1271(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1249(code,U32.is_lt(code,127504))
    case False{}: lookupBelow1270(code,U32.is_lt(code,127990))
def lookupBelow1272(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1229(code,U32.is_lt(code,124662))
    case False{}: lookupBelow1271(code,U32.is_lt(code,127590))
def lookupBelow1273(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1274(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1275(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1274(code,U32.is_lt(code,128391))
def lookupBelow1276(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1273(code,U32.is_lt(code,128369))
    case False{}: lookupBelow1275(code,U32.is_lt(code,128379))
def lookupBelow1277(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1278(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1279(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1278(code,U32.is_lt(code,128401))
def lookupBelow1280(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1277(code,U32.is_lt(code,128394))
    case False{}: lookupBelow1279(code,U32.is_lt(code,128400))
def lookupBelow1281(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1276(code,U32.is_lt(code,128371))
    case False{}: lookupBelow1280(code,U32.is_lt(code,128398))
def lookupBelow1282(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1283(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1284(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1283(code,U32.is_lt(code,128424))
def lookupBelow1285(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1282(code,U32.is_lt(code,128407))
    case False{}: lookupBelow1284(code,U32.is_lt(code,128422))
def lookupBelow1286(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1287(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1286(code,U32.is_lt(code,128435))
def lookupBelow1288(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1289(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1288(code,U32.is_lt(code,128450))
def lookupBelow1290(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1287(code,U32.is_lt(code,128433))
    case False{}: lookupBelow1289(code,U32.is_lt(code,128445))
def lookupBelow1291(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1285(code,U32.is_lt(code,128420))
    case False{}: lookupBelow1290(code,U32.is_lt(code,128444))
def lookupBelow1292(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1281(code,U32.is_lt(code,128392))
    case False{}: lookupBelow1291(code,U32.is_lt(code,128425))
def lookupBelow1293(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1294(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1295(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1294(code,U32.is_lt(code,128479))
def lookupBelow1296(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1293(code,U32.is_lt(code,128465))
    case False{}: lookupBelow1295(code,U32.is_lt(code,128476))
def lookupBelow1297(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1298(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1297(code,U32.is_lt(code,128483))
def lookupBelow1299(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1300(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1299(code,U32.is_lt(code,128489))
def lookupBelow1301(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1298(code,U32.is_lt(code,128482))
    case False{}: lookupBelow1300(code,U32.is_lt(code,128488))
def lookupBelow1302(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1296(code,U32.is_lt(code,128468))
    case False{}: lookupBelow1301(code,U32.is_lt(code,128484))
def lookupBelow1303(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1304(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1305(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1304(code,U32.is_lt(code,128506))
def lookupBelow1306(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1303(code,U32.is_lt(code,128496))
    case False{}: lookupBelow1305(code,U32.is_lt(code,128500))
def lookupBelow1307(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1308(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1307(code,U32.is_lt(code,128710))
def lookupBelow1309(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1310(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1309(code,U32.is_lt(code,128725))
def lookupBelow1311(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1308(code,U32.is_lt(code,128640))
    case False{}: lookupBelow1310(code,U32.is_lt(code,128723))
def lookupBelow1312(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1306(code,U32.is_lt(code,128499))
    case False{}: lookupBelow1311(code,U32.is_lt(code,128715))
def lookupBelow1313(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1302(code,U32.is_lt(code,128481))
    case False{}: lookupBelow1312(code,U32.is_lt(code,128592))
def lookupBelow1314(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1292(code,U32.is_lt(code,128405))
    case False{}: lookupBelow1313(code,U32.is_lt(code,128495))
def lookupBelow1315(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1316(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1317(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1316(code,U32.is_lt(code,128753))
def lookupBelow1318(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1315(code,U32.is_lt(code,128745))
    case False{}: lookupBelow1317(code,U32.is_lt(code,128747))
def lookupBelow1319(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1320(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1321(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1320(code,U32.is_lt(code,129036))
def lookupBelow1322(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1319(code,U32.is_lt(code,128768))
    case False{}: lookupBelow1321(code,U32.is_lt(code,129024))
def lookupBelow1323(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1318(code,U32.is_lt(code,128746))
    case False{}: lookupBelow1322(code,U32.is_lt(code,128986))
def lookupBelow1324(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1325(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1326(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1325(code,U32.is_lt(code,129120))
def lookupBelow1327(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1324(code,U32.is_lt(code,129096))
    case False{}: lookupBelow1326(code,U32.is_lt(code,129114))
def lookupBelow1328(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1329(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1328(code,U32.is_lt(code,129198))
def lookupBelow1330(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1331(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1330(code,U32.is_lt(code,129216))
def lookupBelow1332(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1329(code,U32.is_lt(code,129168))
    case False{}: lookupBelow1331(code,U32.is_lt(code,129212))
def lookupBelow1333(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1327(code,U32.is_lt(code,129104))
    case False{}: lookupBelow1332(code,U32.is_lt(code,129200))
def lookupBelow1334(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1323(code,U32.is_lt(code,128755))
    case False{}: lookupBelow1333(code,U32.is_lt(code,129160))
def lookupBelow1335(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1336(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1337(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1336(code,U32.is_lt(code,129292))
def lookupBelow1338(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1335(code,U32.is_lt(code,129232))
    case False{}: lookupBelow1337(code,U32.is_lt(code,129280))
def lookupBelow1339(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1340(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1339(code,U32.is_lt(code,129350))
def lookupBelow1341(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1342(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: lookupBelow1341(code,U32.is_lt(code,129624))
def lookupBelow1343(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1340(code,U32.is_lt(code,129340))
    case False{}: lookupBelow1342(code,U32.is_lt(code,129536))
def lookupBelow1344(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1338(code,U32.is_lt(code,129241))
    case False{}: lookupBelow1343(code,U32.is_lt(code,129351))
def lookupBelow1345(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: 16
def lookupBelow1346(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 16
    case False{}: 0
def lookupBelow1347(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 0
    case False{}: lookupBelow1346(code,U32.is_lt(code,131070))
def lookupBelow1348(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1345(code,U32.is_lt(code,129646))
    case False{}: lookupBelow1347(code,U32.is_lt(code,130048))
def lookupBelow1349(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: 3
def lookupBelow1350(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: lookupBelow1349(code,U32.is_lt(code,917632))
def lookupBelow1351(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 3
    case False{}: 0
def lookupBelow1352(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: 68
    case False{}: lookupBelow1351(code,U32.is_lt(code,921600))
def lookupBelow1353(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1350(code,U32.is_lt(code,917536))
    case False{}: lookupBelow1352(code,U32.is_lt(code,918000))
def lookupBelow1354(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1348(code,U32.is_lt(code,129792))
    case False{}: lookupBelow1353(code,U32.is_lt(code,917760))
def lookupBelow1355(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1344(code,U32.is_lt(code,129339))
    case False{}: lookupBelow1354(code,U32.is_lt(code,917504))
def lookupBelow1356(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1334(code,U32.is_lt(code,129040))
    case False{}: lookupBelow1355(code,U32.is_lt(code,129632))
def lookupBelow1357(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1314(code,U32.is_lt(code,128453))
    case False{}: lookupBelow1356(code,U32.is_lt(code,129218))
def lookupBelow1358(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1272(code,U32.is_lt(code,127358))
    case False{}: lookupBelow1357(code,U32.is_lt(code,128742))
def lookupBelow1359(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1188(code,U32.is_lt(code,73459))
    case False{}: lookupBelow1358(code,U32.is_lt(code,128367))
def lookupBelow1360(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow1019(code,U32.is_lt(code,69763))
    case False{}: lookupBelow1359(code,U32.is_lt(code,122918))
def lookupBelow1361(+code: U32,below: Bool) -> U32:
  match below:
    case True{}: lookupBelow679(code,U32.is_lt(code,4157))
    case False{}: lookupBelow1360(code,U32.is_lt(code,71984))

def lookup(+code: U32) -> U32:
  lookupBelow1361(code,U32.is_lt(code,42612))
