# pi-agent library port

Reference: pi-mono `46c9de402`, `packages/agent`. This is a partial library port, separate from the bootstrap CLI. The native Agent owner executes the assembled loop. The broader API, configuration mutation accessors and most named upstream Agent tests remain unfinished.

`src/types.bend` preserves the typed tool, message, event, context, configuration and hook interfaces. Ordinary records and message/tool lists are immutable values. Custom messages and argument/result types are explicit generic parameters. Reusable callbacks, cancellation and running-agent state are effectful resources; they are not hidden inside ordinary data to reproduce JavaScript aliasing.

`src/providers.bend` connects the modular OpenAI Responses provider to `StreamFn`. Its option projection retains the host's configuration type, and each response carries an affine release action owning its provider run. The loop releases it after consuming the response; no background reaper, shared run slot or transcript conversion is needed. Provider terminal errors remain assistant error messages, while a cleanup failure after success maps into the host's error type. The callback and its borrowed TLS configuration must outlive active runs.

`tests/native-agent.bend` exercises this adapter through the public `agentLoop`, native schema validation and native HTTPS. `tests/native_agent_check.py` supplies an OpenSSL peer for two model turns: a valid tool call executes once and its result reaches the next request; invalid arguments produce a tool error without executing. The test also checks event boundaries, retained call arguments, the final response and connection retirement. It accepts `bun`, `native-1` or `native-4` against compiled `build/native-agent` artifacts. These scripted-peer tests are separate from real-model acceptance.

`toTool` projects an executable tool to its declaration without invoking callbacks. Argument preparation returns an original or replaced call, preserving metadata while allowing different raw/prepared argument types. Before-tool hooks return `BeforeToolCallUpdate` with explicit arguments, context and an optional decision. This preserves the ability to change execution arguments without revalidation. The public loop threads that update into prepared execution.

`src/agent-loop.bend` supplies the loop helpers: tool selection and execution policy, argument preparation, result merging/projection, after-tool hook finalization, awaited event emission, truncated-call failures, completion handling, queue polling, stop/next-turn hooks, context conversion, API-key resolution and injected assistant request/stream consumption. Pure transformations return updated values. The running request owns a synchronized context cell; emitted messages are immutable snapshots. Listener and hook failures use typed `Result` values and short-circuit subsequent work.

After-tool overrides use `Maybe`: absent fields retain their old values, while supplied fields replace them. Next-turn updates return context/messages/model/thinking changes explicitly. Costs, tool details, headers and configurations require no object disposal. Callback handles and running synchronization resources still require explicit lifetime management.

Run `sh tests/native-agent.sh` for request sequencing, retained event snapshots, queue/hook transitions, typed preparation, awaited emission, truncated failures and completion paths on one and four threads. Supplemental upstream-source comparisons remain in `tool_batch_vectors.py`, `tool_selection_vectors.py`, `before_tool_decision_vectors.py`, `turn_update_vectors.py`, `tool_result_vectors.py` and `failed_turn_vectors.py`. Type/interface checks are in `agent_types.py`, `loop_config_types.py`, `tool_preparation_types.py` and `tool_result_message.py`.

The package follows upstream's module boundaries: `src/types.bend` (the agent types), `src/agent-loop.bend` (`agentLoop`, `agentLoopContinue`, `runAgentLoop`, tool preparation, sequential and parallel execution, tool-change declarations and the main loop), `src/agent.bend` (the stateful `Agent` with its lifecycle, queues, listeners, hooks and failure recovery) and `src/stream-fn.bend`. Earlier layer modules were folded into sections of these files on 2026-09-22; the paragraphs below name those sections.

See [native Bend decisions](../../docs/native-bend.md) for approved behavior changes and retired compatibility fixtures. Named loop coverage is recorded in `tests/upstream-inventory.json`. It does not establish complete `Agent` API, extension runtime, schema-validator or provider parity.


`finalizeExecutedToolCall` now awaits the configured after-tool hook, supplies the original call and validated arguments, applies optional overrides and turns hook errors into error tool results. Generic embeddings provide an empty details value and a pure error renderer; JSON-detail embeddings use an empty native dictionary. Nine native cases verify successful/error executions, absent hooks/returns, termination overrides, error conversion and exactly-once invocation. Channel gates verify that asynchronous hooks settle before finalization returns, without timing assumptions.

`createAgentStream` now specializes the generic stream for agent events and final message histories. Agent-end settles the history independently of buffered event consumption; late pushes are ignored. `disposeAgentStream` releases its owned predicate/extractor callbacks after iterator users retire. The asynchronous public wrappers are implemented in the loop stream section of `agent-loop.bend`.


the tool update scope section of `agent-loop.bend` tracks accepted updates for one tool invocation. Affine tickets prevent double completion, closing rejects late updates, and the result can fail before outstanding listeners drain. Resource disposal waits for that separate drain and requires the owner to retire retained callback aliases. Before closing, failures already available are selected in registration order; after closing, the first arriving failure wins. This matches the pinned execution helper's awaited update batch. `tests/tool_update_scope.py` checks the original helper's selection/rejection behavior, native scope transitions, independent invocations, 256 concurrent completions and compiler rejection of ticket copying. The scope is integrated with prepared tool execution and update forwarding; these are supplemental checks, not full agent-suite ports.


`executePreparedToolCall` now invokes the configured executor with validated arguments, the signal and its invocation update callback. It closes update admission immediately after execution settles, awaits accepted deliveries, preserves successful results and formats tool failures as error results. Listener failures propagate separately, including when the tool also fails. A fresh `ToolUpdateRun` supplies the invocation's update resources; the caller keeps that owner alive until retained callback aliases retire, then disposes it after outstanding deliveries drain.

the event emission section of `agent-loop.bend` distinguishes ordered delivery initiation from completion. Starting emitters return a ready result or an affine pending delivery; the latter can finish concurrently with other listeners. `createAgentStreamEmitter` connects this protocol to the canonical stream. `createAwaitedEventSink` adapts it to existing lifecycle helpers that await delivery. Eight gated execution scenarios and a composed execution/finalization/result-stream test run on one/four threads in `tests/native-agent.sh`. These lifecycle components are integrated into the public loop. Full schema validation and broader upstream agent suites remain unfinished.


`prepareToolCallWith` composes first-match lookup, optional argument adaptation, required validation, before-hook updates and post-hook cancellation/blocking. Both raw and adapted calls go through the supplied validator; there is no successful-validation fallback. Typed adapter/validator/hook failures become immediate tool error results. Explicit context updates survive blocking and cancellation, and hook-updated arguments reach execution without revalidation. The required validation callback is an injection boundary while the full canonical schema validator remains pending. Twelve native pipeline cases supplement the named public-loop tests. The public fixtures inject actual native plain-schema validation; the full builder/reference vocabulary remains incomplete.

the tool batch parallel section of `agent-loop.bend` now composes the canonical lifecycle with ordered preparation and concurrent tool execution. Start events and immediate end events happen during preparation; prepared tools recheck cancellation before running. Successful batches deliver result messages in call order after all execution-end events settle. `start` returns an owned run, `result` performs one ordered delivery, and `dispose` drains workers before retiring their callback. On listener failure, callers retain the run and captured resources while sibling tools finish. Tool-update owners remain separate until retained callback aliases retire. Component and public-loop tests cover this behavior, including gated overlap, ordered results and sequential overrides.

the tool execution section of `agent-loop.bend` now exposes the common `executeToolCalls` entry point over sequential and parallel lifecycle implementations. It selects mode once from assistant tool calls and the original context/config, preserving first-match lookup and whole-batch sequential overrides. Its owned `Execution` distinguishes a settled result from a concurrent run; callers consume `result` once and `dispose` before retiring captured resources. The native main loop owns these executions through result settlement and resource draining.

the turn completion section of `agent-loop.bend` commits successful batch results to both histories, emits `turn_end`, and passes the completed immutable snapshot to stop/next-turn decisions. It preserves steering precedence and polls follow-ups only when neither tools nor steering requires another turn. `Continue` carries pending messages separately from committed history; `Finished` follows a settled `agent_end`. Batch/listener/hook failures propagate without later polls or events. The main loop connects this decision to the next request/preparation cycle.

the tool changes section of `agent-loop.bend` now reconciles executable tools with transcript declarations while respecting the last pending system message’s intent. the turn preparation section of `agent-loop.bend` combines that reconciliation with hook-returned context/model/thinking updates, conditional steering repolling and ordered turn/message events. Prepared and queued messages commit to both histories only after their message-end event succeeds. The returned `RequestState` is ready for the existing provider-request pipeline; the main loop section of `agent-loop.bend` drives this state through the next request.


the main loop section of `agent-loop.bend` assembles requests, assistant streams, tool execution, turn completion and preparation. the loop entry section of `agent-loop.bend` implements `runAgentLoop` and `runAgentLoopContinue`, including continuation validation and initial declaration/prompt events. `stream-fn.bend` supplies a host-owned default-provider registry, resolving the selected provider at loop entry. the loop stream section of `agent-loop.bend` launches `agentLoop`/`agentLoopContinue` asynchronously and exposes immutable events and a repeatable typed result. Invalid continuation is rejected before launch; failures close iteration with a typed error and no synthetic `agent_end`, as approved by the user.

A wrapper run owns its worker, stream callbacks, tool-update owners and batch executions. Retire iterator/result observers and retained tool-update aliases before disposal; `dispose` joins the worker and drains batches before releasing callbacks. Provider/configuration/validator callbacks remain borrowed until disposal completes. The generic stream publishes terminal state and event ownership atomically, with gated race regressions.


All 24 named cases from the pinned `packages/agent/test/agent-loop.test.ts` are ported across `test/agent-loop*.bend` and pass on one/four threads after fresh builds. The inventory records this suite as ported. This is a test-suite milestone, not completion of the package: `Agent` orchestration, broader agent tests, full schema integration and provider implementations remain pending. See [upstream coverage](../../tests/UPSTREAM.md) for the assertion mapping and native adaptations.


the agent state section of `agent.bend` now implements immutable `AgentState` initialization, default model/conversion, transcript-derived `systemPrompt`, run start/finish transitions and event reduction. A native string set tracks pending tool IDs. `agent_end` clears the partial message but preserves the active flag and pending IDs until `finishRun`, so the future owner can await final listeners before becoming idle. `tests/agent_state_check.py` compares 161 snapshots with the pinned constructor/reducer/lifecycle helpers on one/four threads, with additional default-model and retained-snapshot checks. This is a state component; public Agent construction and prompt/continue execution remain pending and no `agent.test.ts` cases are promoted yet.

the pending message queue section of `agent.bend` and the agent queues section of `agent.bend` implement immutable FIFO steering/follow-up queues with all-at-once and one-at-a-time modes. the agent continuation section of `agent.bend` returns a typed decision and updated queues: busy/empty-history rejection, ordinary continuation, or queued prompt selection with steering priority and the initial-poll skip flag. The future Agent owner must commit that decision and claim the run atomically. `tests/agent_queues_check.py` compares 1,754 queue snapshots and 1,024 continuation decisions with the pinned source on one/four threads, plus native pair-isolation and retained-snapshot checks. Its oracle intercepts the selected run entry; it does not test native prompt execution, skip-flag consumption or public Agent integration. No upstream suite is promoted by these component checks.

the agent lifecycle section of `agent.bend` supplies synchronized run ownership over a typed immutable state value, intended to hold Agent state and queues together. Pure preparation and run claiming are atomic; failed or competing claims preserve the state. Cancellation reaches the active signal without declaring idle. `runWithLifecycle` awaits execution and optional failure recovery, then commits the finish reduction and settles waiters even when recovery returns an error. Completed run/signal handles remain borrowed and inspectable until owner disposal; the owner must drain observers and retire aliases before disposing. Native tests cover 64 competing-start/restart sequences and six execution/recovery paths on one/four threads. This internal component is not yet wired to public Agent options or prompt/continue execution; `agent.test.ts` remains pending.

the agent runtime section of `agent.bend` now composes the real Agent state, both queues and lifecycle owner in one synchronized cell. Prompt/continue preparation commits with the run claim; concurrent continuation cannot consume a second steering batch. Reset checks active-run status in the same transaction, replays the current system-message baseline, clears queues/runtime fields and retains model, thinking level and executable tools. Event reduction commits before returning the active handle for future listener dispatch; even the upstream idle-event error preserves its prior state reduction. `tests/agent_runtime_check.py` compares 588 snapshots with actual pinned Agent methods using a gated executor, across three histories and both queue modes. Native tests additionally exercise 64 simultaneous continuation races. These pass on one/four threads, as do 161 state comparisons (now including reset) and lifecycle regressions. Provider/loop invocation and public Agent options are still unwired; run preparation is not a finished public prompt/continue API.

the agent listeners section of `agent.bend` implements ordered, awaited subscriptions over borrowed callback handles. A stable registration position and an immutable entry list preserve live addition/removal behavior while user callbacks run outside the registry lock. Duplicate registration is idempotent; unsubscribe removes the matching callback; a failed listener stops that event's remaining delivery. the agent events section of `agent.bend` commits the runtime's event reduction, obtains its active signal and awaits dispatch, returning typed outside-run/listener errors. Eight source scenarios exercise subscription changes and failure ordering; three gated native scenarios verify committed state, serial awaiting, cancellation during a listener, pending idle settlement, cleanup after delivery, and idle-event rejection. All pass on one/four threads. Registry and callback disposal require draining active dispatches. Public Agent construction and provider/loop execution remain pending; these component tests do not promote `agent.test.ts`.

the agent failure section of `agent.bend` implements the stateful Agent recovery path: construct an error/aborted assistant message from the current model with empty text, zero usage, the rendered error and one wall-clock timestamp; then await message-start, message-end, turn-end and agent-end delivery in order. `runClaimed` keeps the run active throughout recovery and applies final cleanup even if a recovery subscriber fails. This intentionally differs from the approved low-level stream wrapper behavior, which returns a typed error without synthesizing agent-end. `tests/agent_failure_check.py` compares 40 recovery scenarios with actual pinned `runWithLifecycle`/`handleRunFailure`/`processEvents`, varying cancellation, empty/nonempty errors and failure of either listener at every recovery event. Complete message shape, usage, timestamp consistency and cleanup pass on one/four threads. The executor is injected at the claimed-run boundary; public provider invocation remains unfinished and the named public Agent suite is not promoted.

the agent loop config section of `agent.bend` supplies run-owned queue readers and binds them into the loop configuration. Its steering reader atomically consumes the optional initial-poll skip once; both readers continue observing the owner's queues and current queue modes. Model, thinking level and context come from one immutable state snapshot, while the supplied base configuration retains its other options and borrowed callbacks. Separate reader instances have independent skip flags; disposing them requires drained loop work and leaves the Agent owner alive. Twenty-four comparisons with pinned `createLoopConfig`/`createContextSnapshot` cover both queue modes, three thinking levels, skip behavior, live enqueues/mode changes, retained context and a fresh reader sharing the same queues. All pass on one/four threads. Public AgentOptions construction and the loop-invocation/event-emission bridge remain pending; supplying a base loop configuration is an internal integration boundary, not the finished public API.

the agent hooks section of `agent.bend` adapts public stop/preparation signatures into loop callbacks, forwarding the current optional abort signal. Stop callbacks are captured when adapters are created. Preparation wrappers exist only if a form was initially configured; when invoked they reread the live options, prefer the context-aware form and fall back to the legacy signal-only form. Removing both forms returns an absent update. `agent-loop-config.applyHooks` installs the owned wrappers while retaining other configuration fields. Sixty-four comparisons through those installed fields match actual pinned `createLoopConfig`, covering initial/replacement combinations, fresh versus retained configurations, active/aborted/idle signals, typed context/update forwarding and callback failures. All pass on one/four threads. Wrappers are disposed after in-flight calls drain; source hook handles, option storage and lifecycle remain borrowed. Public AgentOptions construction and real provider-loop invocation are still pending.

`StreamFn` now takes an explicit error type and returns `Result<Error, AssistantMessageEventStream>`. This represents a provider throwing/rejecting before opening its stream; a successful opening still reports later model failures through assistant stream events. The error passes unchanged through request dispatch, explicit/default provider selection and the public loop result. Wrapper iteration closes on failure without a synthetic `agent_end`. Provider callbacks and the default registry share the error parameter; successful native callbacks return `Done{stream}`. This is a native typed adaptation of the upstream exception boundary. Public Agent recovery remains a separate, unfinished integration step.

Agent subscription callbacks return the shared `event-emission.Emission<Error>` type: `Ready{result}` for completed work, or `Pending{reply}` for asynchronous completion. `agent-listeners.start` invokes the first listener inline and returns ownership of the remaining delivery; later listeners are selected live and awaited in order. `agent-events.startEvents` commits the state reduction before starting listeners, and `processEvents` awaits that same operation. This preserves immediate listener effects without serializing separate events: a later event can release work awaited by an earlier event. Callers must await every returned delivery before finishing the run or disposing borrowed listeners/runtime resources. Native gated checks cover this contract and pending failure isolation; public Agent/loop construction remains unfinished.

the agent run section of `agent.bend` now executes a claimed runtime prompt/continuation plan through the real public loop entries. It binds run-owned queue readers, forwards the active abort signal, starts events through Agent state/listeners, and performs stateful failure recovery before final idle settlement. The returned `Completed` contains the recovery result and owned run resources. Keep those resources, the runtime and borrowed callbacks alive while tool-held update aliases or sibling work may survive; `dispose` drains batches before releasing update owners and callback adapters. The final public Agent owner must retain these resources until disposal is safe.

`tests/agent_run_check.py` compares ten integrated scenarios with the pinned Agent and its actual loop functions: prompt, continuation, steering/follow-ups, assistant-tail continuation with the initial steering skip, provider failure, abort, initial/recovery listener failures and a complete tool round trip. It checks each event’s committed state, final idle/error state and provider transcript sequence. The no-argument tool uses an explicit `Unit` argument adapter; full schema/argument integration remains separately unfinished. Public Agent construction, complete options adaptation and prompt-input overloads remain pending.

`agent.bend` now supplies the Agent owner and the agent options section of `agent.bend` mirrors all 19 upstream constructor option fields with native typed callbacks. Construction resolves explicit/default providers once, owns only an internally created default converter, and defaults transport/tool execution/queue modes as upstream does. Public prompt inputs are `Text`, `Message` or `Messages`; text normalization preserves image order and adds a wall-clock timestamp. `continueRun`, subscriptions, queue insertion/clearing, signal lookup, cancellation, reset and idle waiting compose the existing runtime. Each run creates the current hook adapters and retains its resources for final disposal. Join outstanding public method calls and retire borrowed aliases before disposal; it drains batches and releases owned wrappers without disposing caller callbacks.

Ten owner integration scenarios compare with the actual upstream Agent and loop, including busy prompt/continue/reset rejection from listeners, constructor defaults, default-provider retention and borrowed converter lifetime. Six separate prompt-input cases compare source normalization, with a native live-timestamp check. Configuration mutation accessors and the full named Agent test suite remain unfinished; the constructor’s validation callback is still an explicit dependency. These checks do not establish package completion.

Agent state updates now use explicit `setModel`, `setThinkingLevel`, `setTools`, `replaceMessages`, `appendMessage` and `clearMessages` operations. Each commits a pure state transition under the existing runtime lock, preserving streaming/partial-message/pending-tool/error fields and both queues. The state getter returns immutable snapshots. `systemPrompt` remains transcript-derived. Queue mode getters/setters and `hasQueuedMessages` expose the existing synchronized queues. Configuration callback/value mutation accessors and the remaining named Agent tests are still unfinished.
